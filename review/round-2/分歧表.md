# 分歧表（round-2，2026-08-26）

立场缩写：G=GLM(起草方)、K=Kimi、R=Grok、C=Codex。类型：事实=可用命令/代码证据裁决；取舍=设计决策；用户=已由用户拍板。
"裁决"列为最终结论；证据终审的命令与原始输出见文末附录。

| # | 议题 | G | K | R | C | 类型 | 裁决 |
|---|---|---|---|---|---|---|---|
| 1 | slowapi default_limits 是否作用于全部未装饰路由（§2.2 排除段） | 排除（生产 60×200） | **反对**（源码推导；推测生产 enabled=false） | 疑问但不反对 | 未提 | 事实 | **证据终审：G 胜，维持排除**。证据：①生产 `settings.rate_limit_enabled=True` 实测（docker exec 打印）；②两轮独立 60 连打 `/api/v1/health` 全部 200；③同窗口装饰路由 login 第 6/7 次请求 429（限流器活跃）。K 的"生产开关为 false"假设被①直接驳回。P3 注释文案改为"防回归提示：升级 slowapi 版本必须复测未装饰路由不限流的行为" |
| 2 | C2 中间件双 Session 耗尽连接池 | 原列[中] | 未质疑机制 | **反对**（main.py:499 `db.close()` 在 :515 `call_next` 之前，无重叠） | 未提 | 事实 | **证据终审：R 胜，C2 删除**。复核 main.py 确认 close 先于 call_next。改登记为备注："pool_size=5 偏保守，属可选调参，本轮不动" |
| 3 | D5/P3-5 删 7 个死权限码 | 删 7 个 | 只 3 个真死（`query.schedule`/`product.publish` 被 v-perms 引用、`ai.draft.view` 被角色矩阵+测试引用） | 反对删除（144 语义） | 反对（目录与 DB 孤儿不一致） | 事实+取舍 | **共识：本轮零删除**。改为执行报告登记清单（3 个真死码 + 3 个仅前端显隐码），不动 RESOURCE_CATALOG |
| 4 | F3/P5-3 删 ReCol/RePureTableBar/ReDialog | 删 3 个 | **ReDialog 被 App.vue 引用，删=坏应用** | 同 K | 未提 | 事实 | **共识（K/R 一致）**：剔除 ReDialog；仅删 ReCol/RePureTableBar（删前 grep 复核） |
| 5 | P0-1 pymssql 修法（正则重排占位符） | 正则重排 | 反对：pymssql 原生支持 dict 命名绑定；pyodbc 不支持 dict（原表述有误） | 疑问：占位符方言未钉死 | 正则可被注释/字面量干扰 | 取舍 | **共识**：pymssql 分支直接 `cursor.execute(sql, params)`（原生命名绑定）；pyodbc 保持现状；单测覆盖命名占位符 SQL 的绑定正确性 |
| 6 | A4 truncated off-by-one | `>=` 改 `>` | 必须配套连接器 fetch `max_rows+1` 探测（现状只取恰好 max_rows） | 同 K | 同 K（第 1 条） | 事实 | **全票共识**：A4=连接器取 max_rows+1 + runner `len(rows) > max_rows` 绑定同一任务，单测断言"恰好 N 行不截断、N+1 行截断" |
| 7 | A11 SqlServer fetch_metadata 补 `LIMIT 5000` | LIMIT | 同意补 | **反对：SQL Server 无 LIMIT，用 TOP** | 未提 | 事实 | **R 胜**：改 `SELECT TOP 5000 ...` |
| 8 | B5/P1-4 ai 端点权限码范围 | 新码 `ai.sql.execute` 套 5 端点 | 提醒新码需 RESOURCE_CATALOG+种子+矩阵全同步 | **反对一刀切**：system-context/export-context 是只读，套 execute 码锁死 ai_user，违反 §1 不改 144/149 语义 | 反对：缺调用方授权清单与回滚门禁 | 取舍 | **共识（采纳 R 收窄）**：只读上下文端点挂既有 `ai.context.read`；仅 drafts/execute、tool-execute 等真实执行类挂新码；P1-4 增补"调用方矩阵清单（126 调度/AI 工具/测试）+ 新码授予 ai_user 之外全部既有 admin 角色"验收 |
| 9 | C3–C7 性能项无批次承载 | 漏立项 | 补批次或显式放弃 | 逐条标"未立项" | 未逐条 | 计划完整性 | **共识**：新增性能批——C3（queries 列表 N+1 批量化 + include_sql 路径权限预判一次化）、C4（board_overview SQL 聚合改写）、C5（finding 查重先聚合后批查）、C6（导出加上限+分批）；C7 登记 §7 不做（首页可承受） |
| 10 | P3-8 `git mv` + 根目录 .gitignore | 写了 | 与 §1/§3 边界冲突 | 同+误删风险 | 同（第 3 条） | 边界一致性 | **共识**：改普通 `mv`（不动 git 索引）；.gitignore 追加 `backend/dev.log` 等精确条目；路径修正（文件在 backend/ 下非仓库根） |
| 11 | 一次性 P0–P7 vs 拆分多轮 | 一次（用户约束：免费 AI 次数限制） | 未质疑 | **反对：拆 P0 先行**（中断风险/与 146 文件重叠/表面全绿语义漂移） | 未质疑 | 用户已拍板 | **用户决策优先：保持一次性**。采纳 R 缓解：①P4/P5 按页组织（同页错误处理+类型一次改完，避免同文件两轮触碰）；②每批结束输出 checkpoint 摘要；③§6 提示词含"禁止为通过测试弱化断言"红线。R 异议原文留档 |
| 12 | G3 `/graph/diagnostics` 无测试 | "无对应测试文件" | **已有**（test_graph.py:253/285/383） | 同 K | 未提 | 事实 | **K/R 一致，G 让步**：P 测试批改为"扩展 test_graph.py 既有断言 + 仅新增 dashboard/summary 冒烟" |
| 13 | E5 行号/范围 | accounts:76 无 catch 等 | accounts `doUnbind` 已有 catch+loading，缺确认框；local-accounts 实际 278-314 | 同 K | 未提 | 事实 | **采纳修正**：accounts 仅补确认弹窗；local-accounts 按 278-314 补 catch |
| 14 | E15 编辑连接"混入回传" | 回传后端 | **反对**：saveAddConnection 白名单字段，多余字段不回传，仅表单状态污染 | 登记为孤儿低优项 | 未提 | 事实 | **K 胜**：改"表单状态污染（低）"并入 §7 不做 |
| 15 | E1 applyOne 需补确认弹窗 | apply/stop 补 | — | **applyOne 已有 ElMessageBox.confirm，勿重复** | 未提 | 事实 | **采纳 R**：仅 stop 路径按需补，apply 不动 |
| 16 | §4 门禁"≥1091 passed"措辞 | 会误导执行 AI | — | — | 未提 | 措辞 | 修订为"0 failed 且新增测试全部通过" |
| 17 | §6 必读引用不存在的"最终报告"+“开起步包”笔误 | 写错 | 指出 | 指出 | 未提 | 措辞 | 修订：指向定稿后实际路径 `review/round-2/最终报告.md`；笔误改"开发起步包" |
| 18 | E3 九处失败连弹 ElMessage | 未提 | — | 需节流/汇总 | 未提 | 设计 | P 前端批加约束："同一操作序列的失败提示合并为一条汇总" |
| 19 | B6 模板转义严重度 | 中 | 潜伏（模板当前仅测试引用） | 同意修 | 未提 | 定级 | 降为"低"，保留修复（顺手项） |
| 20 | A10 分类过滤 | 冗余条件可去 | 子串匹配可能是有意口径 | 疑问（下推会变命中集） | 未提 | 取舍 | **共识**：仅删逻辑冗余的 `==` 分支（子串包含已覆盖相等），不下推 SQL、不改命中集；测试断言命中集不变 |
| 21 | B3 finding.detail 泄露路径 | 现行泄露 | **不可达**（error_cnt>0 前置），属潜伏 | 同意修 | 未提 | 定级 | 描述改"潜伏路径"，修复不变（三处统一脱敏） |
| 22 | E11 saveSrollTop 修法 | 修拼写+grep 引用 | **全仓无路由 meta 使用该标记=死分支** | 同 K | 未提 | 事实 | **采纳 K**：修正 resolve 缺失 + 删除死分支（含拼写修正）一并处理 |
| 23 | B1 CSRF 测试文件存在性 | 写"扩展 test_auth_csrf.py" | — | **全仓无该文件，应"新建"** | 未提 | 事实 | **采纳 R**：新建 `tests/test_auth_csrf.py`（含 evil-origin 用例 + X-Requested-With 分支用例） |
| 24 | B1 X-Requested-With 短路面 | 未提 | — | 前端 axios 默认带该头，真正危险面是无该头的 cookie POST | 未提 | 设计 | 测试矩阵必须含"有/无 X-Requested-With"两组用例 |
| 25 | D2 样板计数 | 12 处 | **15 处**（relation_reviews 另 3 处默认 "reviewer" 变体） | — | 未提 | 事实 | **采纳 K**：helper 设计带 default 参数兼容两种默认值，覆盖 15 处 |

## 对辩轮说明（步骤 4）

按技能规则，对辩仅针对"仍冲突且无法确定性证据裁决"的条目。本表 25 项中：
- #1/#2 已由确定性命令/行号证据终审（附录 A/B），无解读疑义；
- #3–#10、#12–#25：起草方（G）经证据核对后让步或与外部各方实质收敛，外部三方（K/R/C）之间无互斥立场；
- #11 属用户已明示拍板的取舍（一次性执行），R 的异议原文留档且缓解措施被采纳。

结论：**无满足对辩条件的残留冲突条目，跳过对辩轮**（同时节约 kimi/grok 调用额度）。

## 附录 A：#1 证据终审原始输出（2026-08-26，8.83 生产）

```
# 证据1：生产限流开关（docker exec data-asset-api python -c "...settings...")
prod rate_limit_enabled: True

# 证据2：未装饰路由 health 第二轮 60 连打（首轮同结果，本轮复现）
200 ×60（零 429）

# 证据3：装饰路由 login 7 连打（@limiter.limit("5/minute")）
403 403 403 403 403 429 429   ← 第 6 次起限流生效
```

## 附录 B：#2 证据（main.py 行号复核）

`app/main.py:468`（middleware 开 SessionLocal）→ `:499 db.close()` → `:515 await call_next(request)`（get_db 的会话在此之后才创建）——两会话生命周期不重叠，"每请求双连接"机制不成立。
