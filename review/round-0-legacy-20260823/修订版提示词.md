# 修订版执行提示词（round-1 产出，2026-08-23）

> 本版本已经 multi-review 三方审查（GLM 起草+初审、Kimi 全量、Codex 轻量；Grok 402 缺席），
> 采纳共识 A1–A15 与证据裁决 B1，将同步替换 146 §10，成为唯一权威版本。

```text
你现在是 F:\python\数据资产 仓库本轮界面完善的唯一执行 AI。请一次性连续完成
开发起步包/146_界面完善详细执行计划_复核版.md 的 S0 与 28 个任务组 A→B→C→D→E。
不要只做评估、不要在每阶段停下来等待确认、不要另写新计划或平行状态报告。

【必读（按序）】
1. 根目录 AGENTS.md；2. 开发起步包/README.md；3. 开发起步包/55_系统未完成事项统一执行计划.md；
4. 开发起步包/146_界面完善详细执行计划_复核版.md 全文；5. 144 计划 §16.1 中 S8 与前端相关记录。
冲突裁决顺序：146 与 145 冲突以 146 为准；146/提示词与当前代码实况冲突以代码为准并记录；
行号漂移按符号名定位，不机械修改旧行号；业务口径无法确定时该项记 BLOCKED 并继续无依赖任务。

【S0 基线（先做，全部留痕）】
1. 目录自检（README↔实际文件）+ git status --short / git diff --name-only / untracked 清单，
   给 144 未提交改动与 146 新改动划定文件归属，绝不清理、reset、checkout、覆盖用户工作。
2. 冻结测试基线并记录实测数字（预期参考：后端全量约 1016+16 项通过/1 skip/0 fail、
   前端 21 文件 125 tests、双 typecheck、build/gzip 全过；以你实测为准）。
   基线失败保留证据、修复后重跑，禁止把既有失败写成通过；后续每阶段对比，144 成果零回归。
3. 需要结构变更时先 `alembic heads` 核对单 head（预期 g7b8c9d0e1f2，144 未提交），
   新迁移 down_revision 必须接当前 head，禁止硬猜、禁止 --autogenerate。

【执行纪律】
- 严格按 28 组 A→B→C→D→E 连续实施；单组真实阻断记 BLOCKED 并继续无依赖项，最后统一复核。
- 本轮是单一执行者：不得并行派多个修改者；若你自带子代理，只允许只读审查或互不重叠的测试，
  不得同时改同一文件（AGENTS.md 的 Luna 协作策略本轮让位于 146 §8）。
- 每完成一个阶段（S0/A/B/C/D/E）立即把状态、真实测试计数、变更文件、阻断追加写入 146 §11
  执行记录再进入下一阶段；若执行中断，后续以最后一条记录续跑，不重复已完成阶段。
- 高冲突文件（query-center index.vue、api/query-center.ts、router/modules/asset.ts、api/dict.ts、
  permissions.py、metadata/identity/dictionary 路由、Alembic 链）串行最小增量续改；
  禁止整仓格式化、改无关文件、用生成器覆盖手写迁移。

【交互边界（146 §0.1 全文有效，含限定句）】
不新增任何操作前二次确认；不做 sql-workbench 执行确认、ops/runs 操作人改登录态、
identity/authorizations 整页改造、relation-review 批准/驳回确认或驳回原因强制化；
asset/admin 的 Token 一次性结果模态框与 prompt→表单属于展示/输入修复，保留；
既有 confirm 可一对一组件化但不增加步骤；表单校验、参数 JSON 校验、失败提示、
错误态和重试必须实施。不得移除后端安全门禁、审批、审计或 second_confirm 契约——
该裁减只约束本轮新增前端交互。

【安全边界】
- 只修改 frontend、闭环必需的 backend/app、backend/tests、手写 Alembic migration，
  以及 README、55、146 三份权威文档。
- 业务源库零连接零写入：全程不持有/不读取任何源库凭据，不执行任何面向业务源库的
  连接、探库、采集或运维写命令；测试仅连隔离测试库。最终以此口径证明"写入为 0"。
- 不得修改服务器、生产平台、调度、凭据或部署；不得 git commit/push/tag。
- 迁移验证仅限隔离测试库（8.83 的 data_asset_test，经 SSH 隧道，凭据不入命令/文档/日志）；
  执行 alembic 时必须同时导出 APP_DB_URL=APP_TEST_DB_URL（env.py 读取 settings.db_url，
  只导出后者会误指 .env 库——144 会话真实事故）；隧道/隔离库不可用时该组标
  BLOCKED_TEST_DB，完成代码与纯逻辑测试后继续，绝不借用生产或开发库。

【平台写接口要求（新增与本轮修改的既有接口均适用）】
- 操作者从登录态派生，不信任请求体 operator；执行 require_permission；
  写治理审计（新增接口的审计与业务写入同事务、失败关闭）；敏感字段脱敏；
  服务器端强制，不得只靠前端隐藏按钮。

【重点契约（146 已裁决，不可自行更改）】
- 兼容路径定义：URL 不得删除、不得 404；/asset/queries、/asset/queries/accuracy、
  /asset/relations、/asset/candidates 等仅可按 A1/E1 改为隐藏 redirect，
  query 参数原样传递。
- query-center 保留 canonical 路径并补服务端 query.view 等真实权限；
  页面按钮从 v-auth 迁到 v-perms（属一致性与单一真源改进，非功能缺陷修复），
  权限码用 canonical dot 形式。
- system_type 不复用 dbTypes；persons 增 classification 并保留旧 person_type 兼容；
  metadata 使用零写 diff-preview；dict/general 以 /api/v1/dictionaries 与后端真实 DTO 为准；
  密码统一 8–18 位；ops 新终态 succeeded、历史 executed 只读兼容；
  快照只做受保护软归档；recipes 复用既有状态机；table API 用 source_code 隔离跨来源同名表。

【测试与门禁】
- 每组至少一个针对性测试或契约断言；每阶段跑相关测试。
- rg 门禁：本计划视图无裸 http.request；无静默业务失败 catch；无新增 ElMessageBox.confirm；
  指定页面无硬编码系统/来源；未删除或改变用户明确保留的确认行为。
- 最终依次：后端全量 pytest；隔离库 alembic upgrade head（仅安全可用时）；
  前端 pnpm install → pnpm run test → pnpm run typecheck → pnpm run build。

【收口与汇报】
更新 146 §11 执行记录（逐阶段）、README 状态与目录更新记录、55 末尾执行日志。
最终只汇报：① 28 组 DONE/BLOCKED 矩阵；② 变更文件（=与 S0 基线 diff 的对比结果，
区分 144 既有与 146 新增）；③ 测试原始结果（通过/失败/跳过真实计数）；④ 真实阻断与
剩余风险；⑤ 未经授权而未执行的生产/Git 动作；⑥ 业务源库零连接零写入的安全确认
（按上述证明口径）。不得用"基本完成"代替 DONE/BLOCKED。
```
