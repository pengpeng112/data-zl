# 最终报告：multi-review round-5（关系图谱 Neo4j 式直观化改造方案）

- 日期：2026-08-29；起草+汇总+验证：ZCode(GLM)；**外部三方全勤**（kimi/grok/codex 无缺席）。
- 产物：`方案.md`（v1）→ `审查_zcode.md`（哈希 d7e4b776… 固化）→ `检查_codex.md`/`审查_kimi.md`/`审查_grok.md` → `分歧表.md`（20 项，16 项证据/共识裁决+1 项人工+3 项附属修正）→ **`方案_v2.md`（修正版交付物）**。
- 对辩轮：跳过（事实类全部经命令证据终审裁决；取舍类三方共识+GLM 让步并记录原因；无仍冲突条目）。

## 核心结论

1. **v1 被三方一致打回，不能按原稿开工**——三大事实错误（均有 file:line 证据并经本轮命令复核）：
   - "缺邻域端点"为假：`/api/v1/graph/neighbors` 已存在且前端已接线（graph.py:1142）；
   - "力导向需新建"为假：四模式中三者已是 force 家族/radial，且发现**真 bug**——工具栏"知识图谱"绑 layered 且 `usesPresetPositions()` 关闭布局引擎，force 分支不可达（名实不符，恰是"最不像知识图谱"的根因）；
   - 搜索/属性抽屉/聚合/URL 序列化/血缘跳转均已部分存在，v1 当成新功能。
2. **附加安全发现**：`/graph/*` 全部端点 0 处 `require_permission`（仅登录态）——v2 升级为显式整改任务 P0-7。
3. **产品层共识**：不直观的主因是控件堆叠而非能力缺失；v2 以"做减法+连续手势（搜表→画布长邻居→侧栏看属性）"为主线；draft 边与复核队列是两套对象，图上内联复核后移 P1 并加权限/二次确认。
4. **v2 已按全部裁决重写**（§3 现状以证据核验为准；P0 九项全部为真增量；删除新建邻域 API/GraphStore/拼音/图库默认引入）。

## 呈用户决策点（人工终审项）

- **决策点 A**："像 neo4j 知识图谱"=①交互对标（默认，P0/P1 即达成）还是②引入 Neo4j 存储（Go/No-Go 指标门禁+沿用 GraphSyncAdapter+单独授权，另立专项）？
- **决策点 B**：minimap 用 G6 官方插件包（新增依赖，需放宽"零新依赖"一句）还是自绘缩略 SVG（零依赖，功能略简）？
- 可选：是否对 `方案_v2.md` 跑一轮 `-r2` 复审（局部修订通道；鉴于 v2 是按三方意见重写，通常可直接采用）。

## 验证收口（方案类对照）

目标（§1 一句话手势）/边界（§2 全路径+红线）/步骤（§5）/验收（§6 分层）/DoD（§8）逐项齐备；20 项分歧全部闭合（16 裁决+1 人工+3 修正落 v2）；缺席方=无；流程红线（只读/独立/文件通信/初审哈希固化）全程遵守。
