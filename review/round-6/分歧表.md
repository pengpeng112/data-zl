# 分歧表（review/round-6 · 167 号草案核查 · 2026-08-29）

立场：G=GLM（草案+自审 8 条）、K=Kimi（10+ 条）、R=Grok（P0×5/P1×10）、C=Codex（9 条，19KB 深度输出）。**三方全勤；v5 无限制审查**。全部议题三方互证或可无损合成，无对立冲突——按共识/证据裁决，跳过对辩轮。

| # | 议题 | 各方 | 类型 | 裁决 |
|---|---|---|---|---|
| 1 | sql-workbench 描述失实（它是受控 DML 运维台，非 Monaco+只读试运行；全仓无 Monaco 依赖）；"去试运行"出口不存在 | K P1-2 / R P0-1 / C 隐含 | 事实 | **成立（铁证源码）**。B 出口改为：复制+存草稿（propose-sql）+文字说明"看数=存草稿→人工审核→草稿执行（既有）"；**删除 sql-workbench 跳转与 Monaco 依赖**，编辑器=只读高亮+textarea 编辑模式 |
| 2 | API 前缀错：现网=`/api/v1/quality/ai`（ai_quality.py:35），草案写 `/ai-quality` | R P0-2 / C#5 | 事实 | **成立**。全部 patrol 端点挂 `/api/v1/quality/ai/patrol/*` |
| 3 | 同步逐表必超时（前端 HTTP 10s、LLM 90s/表、3–5 表 7.5 分钟）；现网本是异步线程+流式+10 分钟轮询 | K P1-1 / R P0-4 / C#3 | 事实 | **成立（三方一致）**。巡查执行=**复用既有异步 jobs+流式+轮询**：每表固定 findings 批次走既有 preview→jobs 完整链（不新开绕过 HMAC 的 LLM 入口），前端编排进度，`input_summary.patrol_run_id` 串联 |
| 4 | jobs/results 复用矛盾（Result.job_id 一对一、task_type Literal 三值、job_key 静默复用、GET /jobs 不分类型） | K P3-4/5 / R P0-4 / C#4 | 事实 | **成立**。采纳"每表一个现有 job+patrol_run_id 聚合"降级路线（真零迁移）；聚合视图走新 `GET /patrol/runs`（按 input_summary->>'patrol_run_id' 查询）；**"再点一次"不复用**=job_key 掺 run_id |
| 5 | build_analysis_prompt 是 finding 解读器非巡查报告；preview HMAC 门禁不可绕 | R P0-5 / C#9 | 事实 | **成立**。新增版本化 **patrol prompt**（输入=表+指标快照+findings+证据时间；输出=逐表结论/风险等级/证据引用/局限；禁虚构统计；schema 失败降级为可读文本不冒充结构化）——扩展 hospital_llm_analysis 而非原样复用；入口坚持走 preview→jobs |
| 6 | **"定期"被实质降级为手动**（阻断级）；且 QualityTask.schedule_cron/enabled/last_run_at 调度字段现成被遗漏 | C#1（阻断）；K 建议确认；R 同意 H1 但界面须明示 | 取舍 | **合成采纳**：界面新增"巡查计划卡"——复用 QualityTask 既有 cron/enabled/last_run_at 字段呈现（默认每日 02:00、**enabled=false**，演示不依赖调度）；手动执行与计划共用同一巡查链路；**真 cron 定时执行归等待域（须用户拍板启用）**；页面命名"AI 巡查演示（只读）"避免暗示已有定时。此裁决呈用户确认 |
| 7 | **context resolve 缺 join 列**（business_relations 仅 from/to_table+cardinality，无 from_columns/to_columns/join_condition；且全局前 200 条不按系统/表过滤）→"根据关系写 SQL"核心不可行 | C#2（阻断）/ R P1-1 | 事实 | **成立（最关键发现）**。新增前置批 S3a：扩展 ai_context_builder——relations 注入补 join 三要素（AssetRelation 数据源现成）；按 system_code+选中表闭包（选中表+1 跳邻居）过滤；正式/validated 关系才可进 JOIN，候选关系仅展示"待确认"；组合键测试（PATIENT_ID+VISIT_ID 缺一即失败）；24KB payload 裁剪规则（表≤20/关系≤40/值域选中表相关） |
| 8 | 方言写死 Oracle vs 系统选择器 | R P1-1 | 事实 | **成立**。首期系统选择器锁 DATA_CENTER（Oracle），其他系统置灰标注"后续开放" |
| 9 | 路由 `/ai/ai-sql`+菜单"AI 协作组"不存在 | K P2-2 / R P1-8 | 事实 | **成立**。改 `/asset/ai-sql`（router/modules/asset.ts）；菜单挂"数据资产"组与 ai-quality/ai-tools 平级；两页互链按钮 |
| 10 | ai-quality 现页=左右工作台+流式+任务表（非两 Tab）；三 Tab 重构会毁分析流 | R P0-3 / K P3-2 | 事实 | **成立**。A1 改为：**保留现页全部现有布局**，页头加引擎状态卡，巡查=同页下部新增分区（el-segment 切换"问题分析/定期巡查"两视图，默认问题分析；流式/146 E3 能力零触碰） |
| 11 | host 硬编码 10.255.255.10:9000（status 载荷写死）破坏换模型承诺 | K P3-6 / C#7 | 事实 | **成立**。S1 改 status：host 由 config 派生+脱敏（尾段掩码），展示"配置模型"（可加"服务返回模型"如响应含则展示） |
| 12 | 演示集标签须真实证据非手填；隔离库选取 vs 生产成立性矛盾 | K 提示 / R P1-4 / C#8 | 事实 | **成立**。目标集 json 每表必须带证据链（system/source/schema/table/rule_id/finding_id/指标值/采集时间/数据截至时间/快照版本）；S1 以生产平台库**只读**质量规则最近结果选取（走既有只读查询授权）；隔离库演示用同快照 |
| 13 | mock 不能作为"在应用"验收 | C#6 | 事实 | **成立**。DoD 分层：产品验收=**至少一次真实内网模型调用留证**（provider/model/job_id/耗时/成功态/脱敏摘要）；演示兜底=现场不可达时回放且全屏标注"离线回放（最后成功 HH:MM）"；mock 仅限自动化测试。状态卡累计次数=module=quality_ai 且 action=succeeded 真实计数，回放不刷数 |
| 14 | question 未强制脱敏进 LLM；取消语义不明；权限面 | R P1-2 | 事实 | **成立**。question 先 sanitize_text 再入 prompt 与审计（存脱明文摘要+hash）；取消=停止等待 UI，服务端调用仍完成并如实计费计数（界面注记）；权限维持 ai.context.read（与 propose-sql 对齐）+审计限流注记 |
| 15 | 生成历史/累计次数与零迁移 | R P1-9 | 事实 | **成立**。B 历史复用 AiToolCall（tool_name=ai_sql_generate、只本人、question 不存明文）；A 累计=GovernAuditLog 聚合（口径见 #13） |
| 16 | Monaco 依赖+gzip 预算；异步 chunk；测试风格 | K P2-1 / R P1-7 | 事实 | **成立**。无 Monaco（#1）；新页必须异步 chunk；组件测试=plan146 源码断言式（非全页 mount），"每页≥4"按该风格 |
| 17 | 与 165/166 撞名撞菜单、窗互斥未声明 | R P1-3 | 事实 | **成立**。本功能命名"AI 巡查演示"（区别 165"探查发现"）；执行窗互斥：167 在 164→165→166 之后开工（或用户明确插队）；菜单不与 probe-findings 重叠 |
| 18 | 演示环境未定（隔离库 vs 8.83） | R #1 | 取舍 | 列**用户拍板项**：默认隔离库演示（零生产风险），8.83 只读演示备选（目标集快照已在生产元数据成立） |
| 19 | 执行规格密度不足（STOP/文件清单/OpenAPI/security_audit/checkpoint/组件契约） | R P1-7 | 事实 | **成立**。v1.0 计划补齐：STOP 白名单、逐文件变更表、端点契约表、security_audit 同步项、checkpoint 格式、S3a 前置批 |
| 20 | 端点数小错（10 非 11）、last_success_at 已有、task_type 字段名 | K P3-1/3/4 | 事实 | **成立**。v1.0 事实表更正（status 扩展仅加累计次数） |

## 呈用户拍板项（v5 保留）
P1：巡查"定期"默认形态（计划卡+默认关闭+真调度待启用——本方案默认）是否接受；P2：演示环境（隔离库默认 / 8.83 只读）。
