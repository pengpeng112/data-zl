1. 【第1节 P1、第二节关注点2/3】`evidence` 仅为单字段且版本快照没有结构化来源、来源时间与冲突状态，无法可靠记录或检测多来源值域矛盾。  
2. 【第1节 P1】未明确 `system_code/source_owner/table/column/code` 的唯一约束、别名归一和并发版本规则，容易产生重复值域、跨系统串值或多个“当前版本”。  
3. 【第1节 P1、第四节 Q2】SQL 仅按精确 `owner.table.column` 命中，别名、未限定字段、视图/CTE、表达式和动态 SQL 的漏识别会让 AI 静默拿不到值域。  
4. 【第1节 P1、第二节关注点1、第四节 Q1】POST/PATCH 的角色、审批人资格和复核隔离未定义，且 P2 直接将文档实测条目标为 confirmed，存在未经独立审核即成为权威口径的风险。  
5. 【第二节关注点3、第二节 P4】冲突检测与引用影响分析被延后到二期，意味着 P1–P3 阶段即使 SQL 使用错误字面量也不会告警，核心防错目标尚未闭环。
