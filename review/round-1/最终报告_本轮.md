# 最终报告：149 值域知识库执行计划复核（multi-review v4，round-1，2026-08-24）

## 参与方与出席

| 角色 | 执行者 | 状态 |
|---|---|---|
| 起草+汇总+裁决执行 | ZCode (GLM) | 出席（初审 sha256 942b5d9a…） |
| 独立全文审查 | Kimi | 出席（12 条） |
| 独立全文审查 | Grok | 出席（23 条，含代码级事实核查） |
| 轻量检查 | Codex | 出席（5 条） |

**四方全勤，本报告为四方完整结论。**（上一轮 2026-08-23 的遗留产物已隔离至 round-0-legacy-20260823/，彼时 Grok 402 缺席。）

## 总体结论

149 方案**方向正确（平台库权威+三层通道+禁止猜测），但按当前 P1 开做不可行**——四方一致认定：数据模型对用户关注点"多来源记录"“冲突可检测"支撑不足，注入主路径设计建立在错误的接口与解析器假设上。经证据终审（7 项代码/文档事实全部验证成立），共识修订如下：

## 必改清单（P1 内，四方共识）

1. **证据一对多子表** `asset_column_value_domain_evidences`（source_type/source_system/method/sample_count/observed_at/actor），主表单值 evidence 字段废除；
2. **冲突能力进 P1**：同字段同 code 多候选互斥检测 + `conflict_status`（来源/时间冲突），SQL 字面量 ingest 告警留 P4（GLM 让步，原因见对辩记录 A）；
3. **注入主路径改道**：`POST /api/v1/ai/context/resolve` + `GET /api/v1/ai/system-context` 全量 confirmed 附带（按 system 过滤，不依赖列级解析；实测 system-context 无 sql_text 参数且 limit(30) 无稳定排序）；propose-sql 事后解析仅补充，解析失败显式提示；
4. **解析器写明 144 sqlglot**（query_gate 仅提取表名，不能复用）；
5. **domain_kind 分类**（enum/threshold/literal/trap）+ `scope_condition` 条件型口径 + 字段级陷阱（负知识：勿用 DISCHARGE_DISPOSITION_DICT、DEATH_DATE_TIME 不填）——直接针对本次事故根因；
6. **唯一约束** (system_code, source_code, schema, table, column, code)，POST 先查后插防重复；
7. **新路由模块**（/api/v1/metadata/* 为新前缀，勿塞 tables.py）+ 权限码（AI 仅 pending，confirm 人工）；
8. **P2 修正**：只导已确认枚举（6 组）为 confirmed，code9/手术标志3 等待核入 pending；
9. **148 停止手改**改为导出视图；JSON 兜底带版本/哈希/导出时间+最大龄期；
10. **技能覆盖扩至八个入口**（补 sjzc/sql-relation-intake/ods-schema-analysis），并入 query-governance 的 context/resolve 步骤；
11. **149 补登记 55 号计划**（违反"55 唯一未完成入口"）；
12. 注入响应回带 version_no 供调用方留痕追溯。

## 呈人工终审（1 项）

**前端是否纳入**（议题 13）：GLM 主张纯后端（快上线，确认走 API）vs Grok 主张最小前端（值域展示/pending 计数/冲突列表，可晚一迭代）。建议方案 B（P1 后补最小前端，不阻塞主线），请用户拍板。

## 产出清单

- 方案.md（复核对象摘要+用户关注点）
- 审查_zcode.md + 哈希.md（初审固化）
- 检查_codex_本轮.md / 审查_kimi_本轮.md / 审查_grok_本轮.md（三方独立审查）
- 分歧表_本轮.md（14 项议题+裁决）／对辩_本轮.md（让步记录+事实证据索引）

## 风险声明

本报告为方案复核结论，未修改 149 原文；必改清单经用户批准后由 ZCode 修订 149 并按 AGENTS.md 开发约束执行。证据命令输出已在裁决过程中实录（ai.py/query_gate.py/sql_ast.py/main.py/149/148/55）。
