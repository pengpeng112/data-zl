# 案例回放 147：中断交接续跑（界面完善线）

> 类别：技能参考（只读回放，不是执行计划）
> 源文件：`开发起步包/_archive/147_146界面完善中断交接与续跑提示词.md`（已归档非现行，
> 接手时不得按其正文开工；本回放只提炼接手方法）

## 场景

2026-08-24 用户要求当前 AI 停止连续开发，任务交给其他 AI。28 组任务做到一半：
9 组完成、C1 部分完成、18 组未开始。新会话如何接手而不重做/不漏做/不越权。

## 输入事实（接手者视角）

- 交接文档明确自我定位：**只记录接续事实，不是新的执行计划**；权威仍是
  `146_界面完善详细执行计划_复核版.md`，未完成总入口是 55 号。
- 给出最短读取顺序：AGENTS → README → 55 → 146 全文 → 交接文档 → 从 C1 续跑。
- 进度表逐组标 DONE / IN_PROGRESS / NOT_STARTED + 接手动作。

## 判定过程

1. **已完成项直接复用**：S0 基线冻结、A1–A5、B1–B4 报告 DONE 且 146 §11 有
   测试证据 → 不重做，只在最终全量门禁中回归。
2. **断点精确续跑**：C1 是 IN_PROGRESS → 先复核现有改动（而非推倒重写），
   补专项测试后再判 DONE；C2 起严格按 146 顺序实施。
3. **依赖按需再读**：涉及 query-center 才核对 144 §16.1，不预加载。

## 输出形态（七项产出的历史实例）

- 当前目标：按 146 复核版完成 28 组界面完善；
- 已完成证据：9 组 + 146 §11 测试记录；
- 未完成：C1 复核收尾 → C2/C3 → D → E；
- 可继续项：C1 复核（无授权依赖）；
- 需批准项：本案例无生产动作（纯本地开发）；
- 阻塞条件：无——信息齐备即可续跑。

## 回放要点（后续 AI 复现方法）

接手任何中断任务时：找"交接文档 + 权威计划"配对 → 以交接文档的进度表为
checkpoint → DONE 项只回归不重做 → IN_PROGRESS 项先复核再续写 → 按计划顺序
推进未开始项。若交接文档缺失，改用同号执行报告 + output_r<N>/progress.md
重建进度表，并对证据链断裂的批次降级为"待复核"。
