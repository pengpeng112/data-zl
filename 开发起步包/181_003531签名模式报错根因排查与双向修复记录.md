> 类别：证据链
>
> 状态：**已完成（2026-09-05 排查 + 方案 A/C 双执行；C 代码待随 G1/G3 发布）**
>
> 上位入口：`55_系统未完成事项统一执行计划.md`；关联 `176_夜间身份同步连续熔断根因排查与修复报告.md`、`177_全仓真实未完成项_执行报告.md`（R1 补验结论见本文 §6）

# 181 · 工号 003531「必须设置两种以上的签名模式」根因排查与双向修复（JHEMR）

## 0. 现象与授权

- 2026-09-05 用户报障：工号 003531 在电子病历系统提示「必须设置两种以上的签名模式」，此前一直正常使用，怀疑夜间 HIS 人员同步所致。
- 用户授权：「方案A和方案C都执行下」（A=生产数据修复，C=代码治本）。

## 1. 根因（证据链，时间精确到秒）

| 时间 | 事件 | 证据 |
|---|---|---|
| 2024-08-20 15:29 | 003531 建户（jhemr.users） | users.create_date |
| 2025-06-10 18:24–25 | JHEMR 管理端人工配置签名方式 **2/4/8**（不含 0），自洽在用 | users_subsign 3 行、users_sublogin 4 行 last_modify_time 均 2025-06-10 18:24:58；users_control_mode.last_modify_user_id='JHEMR'、in_sign_way='2,4,8'（18:25:19） |
| 2026-08-20 02:00:29–53 | 夜间主同步 RUN-511c60cae91b 将 003531 作 HIS 增量命中执行 align | 平台审计 asset_identity_sync_actions id 17128/17129（CDMS+JHEMR，status=executed，02:00:53） |
| 2026-08-20 02:00:56 | users_subsign 被插入 **sign_way=0、default_flag=1** 一行 | 该行 last_modify_time=02:00:56（executed 后 3 秒）；其余 3 行仍 2025-06-10 |

插入形态与 `jhemr_identity_adapter.align_existing_user` 原补缺逻辑精确吻合：模板 `0/2/4` 逐方式补缺——2/4 已存在跳过、0 缺失即插且 default=1；users_control_mode 已存在整体跳过（in_sign_way 未更新）。

**破坏形态**：subsign 行集 {0,2,4,8} 与 control_mode.in_sign_way "2,4,8" 不一致 + 双默认（4 与 0 均 default_flag=1）→ EMR 客户端校验异常报「必须设置两种以上的签名模式」。align 无删除路径（纯补缺），但「补缺」对人工配置的非标准形态有害。

## 2. 影响面（dry-run 收敛）

初筛 8 户（subsign 存在凌晨窗口插入的 sign_way=0 行且另有其它方式），按严格判据（**0 行时间晚于该户其它全部行**）复核后：

| 形态 | 户数 | 处置 |
|---|---|---|
| align 后补 0 破坏人工 2/4/8 配置（事故形态） | **2**：000219（0 行 2026-08-25 02:01:02）、003531（0 行 2026-08-20 02:00:56） | 本轮删除 |
| 夜间建户整包模板（0/2/4 三行同刻、in_sign_way="0,2,4" 自洽） | 5：003687/003994/004037/004043/004071 | 保留（正常） |
| 整包补齐 + control_mode 人工旧值混合 | 1：003888（subsign 三行同刻 2026-08-12，cm in_sign_way="4" 为 2025-01-20 人工值） | 保留观察；如报障走 EMR 界面重存 |

## 3. 方案 A 执行记录（生产 JHEMR 数据修复，已授权）

| 步 | 结果 |
|---|---|
| 备份固化 | 两户 users_subsign/sublogin/users_control_mode 全行快照 → 8.83 `/opt/data-asset/evidence/signway-fix-20260905/backup_before_delete.json`（0600；SHA-256 前 16 位 `b4072a081975b39d`） |
| 受控删除 | 每户独立事务：重验判据（0 行+夜间窗口+晚于其它行）→ `DELETE ... WHERE user_id=%s AND hospital_no=%s AND sign_way='0' AND file_visit_type='2' AND last_modify_time=%s` → rowcount==1 → 回读比对 → commit；任何失败整户 rollback |
| 回读验证 | 000219 与 003531 均恢复 {2,4,8}、单默认（defaults=1） |
| 平台审计 | asset_govern_audit_logs 2 行（module=identity，action=manual_repair_remove_align_inserted_signway0，operator=manual-signway-fix-20260905，before/after 快照入审计） |
| 遗留 | 003531 EMR 登录验证待临床反馈；003888 未动 |

## 4. 方案 C 执行记录（代码治本，工作区完成、生产未发布）

| 文件 | 修改 |
|---|---|
| `backend/app/services/jhemr_identity_adapter.py` `align_existing_user` | sublogin/subsign 补缺改「**已有任意方式行 → skip_configured 整体跳过**；完全无行才落模板」（003531 事故注释入码） |
| `backend/app/services/identity_login_sign_sync.py` `build_login_sign_plan` | 同款守卫（已配置用户不再按 0/2/4 补缺）；`fix_default`（已有 0 行且无默认时补默认标记）保留；新增 `skipped_configured` 观测计数；模块 docstring 同步 |
| `backend/tests/identity_login_sign_sync/test_login_sign_sync.py` | 原「已有部分行→补缺」用例改写为「人工配置保护」（repairs==[]）；空户整包/幂等/fix_default 用例不变 |
| `backend/tests/test_identity_sync.py` | 新增两用例：`test_align_skips_manual_signway_config`（2/4/8 人工配置 → 零 INSERT + skip_configured）、`test_align_fills_template_when_no_signway_rows_exist`（半户无行 → 整包模板，守卫不误伤本职） |

门禁：`identity_login_sign_sync + identity_title_sync` 纯逻辑 29P/0F；`test_identity_sync + 122_unit + cb_align + cb_resync_f2 + nightly_sync` DB 型 **190P/0F**；隔离库已重灌（12702 表/1329 关系/107 序列对齐）。

## 5. 发布关联（重要）

- **C 未发布前生产仍跑旧代码**：若夜跑主任务恢复执行（F-2 上产后），旧 align 遇到人工配置用户仍会再插 0 行。**C 必须与 G1（FFREE3）/G3（F-2）同批或先行上产**，上产后首晚观察 `skip_configured` 动作。
- **与 180 号计划的衔接（2026-09-05 核对）**：本守卫改动的四个文件（jhemr_identity_adapter.py、identity_login_sign_sync.py、test_identity_sync.py、identity_login_sign_sync/ 测试）**全部落在 180 号 A5 `feat(identity)` 组的 git add 清单内**（180 按整文件归组），随 `r178-20260905` 镜像自然上产；180 的 B 段镜像冒烟 grep 可加一条 `grep -c skip_configured /app/app/services/jhemr_identity_adapter.py`（预期 ≥2）作在产实证。

## 6. 附带：177 R1 夜跑核验补验（2026-09-05 执行）

| 夜 | run | 结果 |
|---|---|---|
| 09-04 02:00 | RUN-b3324c242e78 | failed，熔断维度 **max_change_ratio**（max_update 110<150 已被 F-1 放行；110/110≈100%>30% 拦截） |
| 09-05 02:00 | RUN-69fade57a03a | 同上；签名/职称/科室子任务均 skipped（main_account_sync_not_successful） |

结论：**F-1 生效但熔断未解除**——换了维度（55 号 08-29 预判的「三道阈值串联」第二道）。工作区 F-2（177 R3 实现，例行 resync 同时排除出 update 桶与 ratio 分子）是解除该维度的对应语义，G3 发布紧迫性上升。

## 6. 今夜风险量化与临时防护（2026-09-05 晚，180 发布后追加）

**新风险窗口**：180 号（另一会话）已于本日发布 `r178-20260905` 镜像——F-2 上产、熔断解除，但 **A5 提交早于本守卫代码落盘，镜像内 align 仍是旧补缺语义**（容器 grep skip_configured=0 实证）。夜跑管线对全部候选（含例行 resync）都调 `align_existing_user`（orchestrator `_process_single_candidate(reconcile_existing=True)` → `execute_jhemr_apply` → user_exists → align），故今夜 02:00 起每个「签名配置不含 0」的托管用户都会被插破坏行。

**量化**（容器内只读，expected=219 人）：已配置且不含 sign_way=0 的共 **13 人**（000195/000219/000378/001023/001805/002335/003529/003531/003779/003792/003796/003827/003858，含刚修复两户）；完全无方式行的 5 人（半户，模板整包属正确行为不受影响）。

**处置**（提问选项卡未获答复，按最佳判断执行）：
- **未热修容器**：docker cp+重启属生产变更，用户未点名授权，硬边界不让步。
- **已执行临时防护**：13 人写入 `asset_identity_protected_accounts`（target_system=JHEMR，reason=`temp-guard-181:...`，protected_by=manual-signway-fix-20260905；幂等 13 行，审计 1 行）。夜间候选选择对 protected 全量过滤，今夜起这 13 人不被 align 触碰。副作用：其夜间科室/职称增量同步同步暂停，属可接受的一晚代价。
- **移除条件**：守卫代码随下次镜像上产并经 `grep -c skip_configured /app/app/services/jhemr_identity_adapter.py ≥ 2` 实证后，DELETE 名单中 reason 为 temp-guard-181 的行（13 行）并回读验证。

## 7. 工件索引

- 排查/修复脚本：`开发起步包/output_r177/check_003531.py`（只读探针）、`backup_signway_2users.py`（备份）、`fix_signway_2users.py`（受控修复）
- 服务器证据：`/opt/data-asset/evidence/signway-fix-20260905/backup_before_delete.json`
