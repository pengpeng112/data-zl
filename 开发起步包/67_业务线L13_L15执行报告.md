> 类别：证据  
> 日期：2026-07-13  
> 范围：业务线 L13 主数据差异复核、L14 周边系统只读接入、L15 质量夜间只读（**不含 HRP**，用户要求暂停）

# 67 业务线 L13–L15 执行报告

## 1. 结论

| 项 | 结果 |
|---|---|
| L13 主数据差异复核 | **完成**：生成 open 差异 **2054** 条；`auto_apply=false` |
| L14 周边系统接入 | **完成**：LIS/PACS/EMR/YDHL/SM 登记 + 活库元数据快照 |
| L15 质量只读运行 | **完成**：run_id=1，10 规则，findings **1426**；夜间 cron `0 2 * * *` 已登记 |
| 源库写 | **零** |
| HRP | **未做**（用户口径） |

## 2. L13 主数据差异复核

### 实现

- 服务：`backend/app/services/identity_review.py` → `build_his_master_review`
- API：`POST /api/v1/identity/review/generate`（仅写 `asset_identity_sync_diffs`）
- 列表增强：返回 `entity_code` / `before_data` / `after_data` / `merge_suggestion`
- 前端：`identity/sync-diffs` 增加「生成复核差异」、详情抽屉；默认 source `his_source_10_10_10_15`
- 状态操作：解决 / 忽略 / 重开（**不自动改人员主档**）

### 实测（8.83）

| 指标 | 值 |
|---|---:|
| 扫描 person_code | 4260 |
| 来源行 | 6690 |
| 新开差异 | **2054** |
| multi_source_conflict | 262 |
| staff_only_supplement | 1792 |
| field_mismatch | 0 |

合并建议口径：优先 `FXHIS.SYS_EMPLOYEE`；STAFF 独有标记为补充保留。

## 3. L14 周边系统只读接入

### 实现

- `backend/app/services/peripheral_sources.py`
- `POST /api/v1/sources/peripheral/bootstrap?collect=true`
- 逻辑源挂 **ODS 同一连接**（`file:///etc/data-asset/credentials/ods_8_216`），按 owner 过滤采集

### 登记与采集结果

| source_code | system | schemas | 表 | 字段 | snapshot |
|---|---|---|---:|---:|---:|
| ods_lis | LIS | LIS | 11 | 538 | 5 |
| ods_pacs | PACS | PACS | 18 | 891 | 6 |
| ods_emr | EMR | JHEMR,MTL | 65 | 2330 | 7 |
| ods_ydhl | MOBILE_NURSING | YDHL | 31 | 702 | 8 |
| ods_sm | SM | SM | 25 | 969 | 9 |

平台现 **8 个 system / 7 个 enabled source**（含原 ODS/HIS）。

关系口径：继续沿用既有 `09`–`15` / 关系图谱证据，不在本步重做跨系统 D 类推导。

## 4. L15 质量规则夜间只读

### 实现

- `run_quality_check_core` 供手动与调度共用
- `main.py` 注册 `job_type=quality_check` → `_run_quality_nightly`
- 样本 `mask_sensitive`
- 调度行：`schedule_cron=0 2 * * *`，`trigger_mode=scheduled`（**需重启 API 进程后 APScheduler 才会加载**）

### 实测

| 项 | 值 |
|---|---|
| run_id | 1 |
| total_rules | 10 |
| total_findings | 1426 |
| pass_rate | 30% |
| triggered_by | business_line_bootstrap |

说明：当前规则以 **平台元数据** 类为主（缺注释/缺中文名等），finding 量高属预期；源库 SQL 规则仍须 `validate_sql_safety` + 只读连接。

## 5. 代码与部署

| 路径 | 说明 |
|---|---|
| `app/services/identity_review.py` | 新建 |
| `app/services/peripheral_sources.py` | 新建 |
| `app/api/v1/identity.py` | review API + diff 详情字段 |
| `app/api/v1/systems.py` | peripheral bootstrap |
| `app/api/v1/quality.py` | core 抽取 |
| `app/main.py` | quality 调度 |
| `frontend/.../sync-diffs/index.vue` | 复核 UI |
| `backend/scripts/run_business_line_l13_l15.py` | 一键执行（gitignore 临时类，可本地保留） |

8.83 容器已热更新相关 py 并执行 bootstrap。**完整调度生效请重启 `data-asset-api`**（注意凭据卷与 Instant Client 19 符号链接）。

## 6. 验收命令

```text
# 平台库
SELECT COUNT(*) FROM asset.asset_identity_sync_diffs WHERE status='open';
SELECT source_code, last_check_status FROM asset.asset_data_sources ORDER BY id;
SELECT id, total_rules, total_findings, status FROM asset.asset_quality_check_runs ORDER BY id DESC LIMIT 5;
SELECT job_type, schedule_cron, trigger_mode FROM asset.asset_scheduler_jobs WHERE job_type='quality_check';

# API（需登录）
POST /api/v1/identity/review/generate
POST /api/v1/sources/peripheral/bootstrap?collect=true
POST /api/v1/quality/checks/run
```

## 7. 与计划映射

| 59 项 | 状态 |
|---|---|
| L13 | **完成**（人工确认流 + 数据已生成） |
| L14 | **完成**（ODS 镜像周边 owner；非独立业务库直连） |
| L15 | **完成**（一次实测 + 夜间任务登记） |
| L12 HRP | **跳过** |
| L16 同步闭环审批链 | 未本轮展开（差异已可人工 resolve/ignore） |

## 8. 后续可选

1. 重启 API 验证 02:00 调度  
2. 前端质量页按 run_id 过滤展示本轮 1426 条  
3. L16：变更请求审批后才改主档  
4. 周边系统独立连接串（若不再经 ODS）  
