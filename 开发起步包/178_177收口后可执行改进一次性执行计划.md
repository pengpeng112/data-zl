> 类别：待办
>
> 状态：**v1.1 定稿待执行（2026-09-04，按独立核查修订）**
>
> 上位入口：`55_系统未完成事项统一执行计划.md`（本文件不取代 55）
>
> 制定日期：2026-09-04
>
> 承接：`177_全仓真实未完成项一次性执行计划.md`（代码批已于 2026-09-03 晚执行完成）

# 178 · 177 收口后可执行改进一次性执行计划

本文件给**另一名执行 AI**单会话落地。目标：在 177 已修缺陷、已实现 F-2、已出等待域清单之后，把**今天还能不越权完成**的改进一次做完。

**一句话结论**：系统主线（探库、关系、值域、质控台账、图谱、AI SQL）已经能用。当前最大缺口不是再写一份总计划，而是三类工作：①生产与工作区漂移（要用户授权 Git/发布，本轮不代做）；②治理人工池（探查裁决、值域 confirm、关系草稿，本轮不代点）；③仍可在隔离库完成的卫生与互链。本轮只做第③类 + 177 延后的夜跑只读核验。

---

## 0. 方向分析（执行前先读，禁止另起炉灶）

### 0.1 系统已经站住的部分（禁止重做）

| 层 | 终态 | 证据 |
|---|---|---|
| 探库 / 关系底座 | Track A 完成；正式关系 1329 | README 完成度总览；08/09/40 |
| 值域知识库 | 生产 77 条（confirmed 26 / pending 51） | 149 导入 2026-08-31 |
| 质控台账 | 五表 + 17 清单已生产 r175-20260903 | 174/175 |
| 探查入库与展示 | 生产 6 条 open finding；发现页/导出已上 | 165 X1/X2、166 |
| 图谱 | overview 23.5s→0.012s；墨团已修；边标签 `[object Object]` 已在工作区修 | 169/171/177 C4 |
| AI 巡查 / 写 SQL | 计划卡纯演示；SQL 永不执行；deepseek-r1 已调通 | 167 |
| 173 阻断级缺陷 | P1 序列、P2 系统列表/六按钮、P3-5 配方 create | 177 C1–C5 **代码完成、生产未发布** |
| 身份熔断 F-2 | 例行 resync 不计 max_update；9 测试双向锁定 | 177 R3 **代码完成、生产未发布** |

### 0.2 真正该改进的四层（优先级从高到低）

**第一层 · 运行债（价值最高，本轮默认不发布）**

工作区已有、生产镜像 `data-asset:r175-20260903` 没有：CDMS FFREE3 模板、JHEMR 登录/签名方式子任务、F-2 熔断语义、以及 177 的 **C2–C5 应用代码**（系统列表 readback、六按钮 v-perms、图谱边标签、配方 create 校验）。**C1（`output_r170/import170.py`）是隔离库重灌工装，本来就不进生产镜像**，不要当成「镜像缺失」。容器按现镜像重建会丢掉 172 热修。`APP_IDENTITY_CB_MAX_NEW=130` 仍是临时值。应用代码要 Git 分域提交 + 镜像重建，**必须用户点名**，见等待域 G1–G3/G13。

**第二层 · 今夜核验（本轮必做，只读）**

177 R1 因 09-03 未到 02:00 延后。今天是 **2026-09-04**，F-1 阈值 150 已经过至少一个夜窗。必须只读查最新 nightly run，不得自行调阈值。

**第三层 · 产品断点（本轮做最小切片）**

质量台账与探查发现已经两边都有数据（观测 `source_kind=probe_finding` / `source_record_ref=asset_probe_findings:{id}`），但详情页互不跳转。这是 166 P2「QualityFinding 互链」的最小可交付切片，用户能立刻用到。

**第四层 · 卫生债（本轮只做小而确定的两项）**

- 173 P3-3 / 177 C6：`GraphToolbar.vue` 直接改 props（上次因父组件不在白名单跳过）。
- 173 P3-6：空库 `alembic upgrade head` 因缺 `asset` schema 失败。修 `alembic/env.py`，不改历史迁移文件。

**明确不做（会把单会话做爆或越权）**

- 166 P2 其余：对账矩阵看板、趋势曲线、探查调度入口、值域批量导入。
- 173 P3-1 删死 API、P3-4 全仓 prettier、P3-8 清 `as any`、P3-2 四页整页补 v-perms（工作量大、权限码易挂错）。
- 144 黄金用例生产种子、127 自批草稿、112/96 业务库写。
- 150 医保课题：截止日即今天，工作区正文已删，**禁止虚构申报稿**。
- layout / captureMode / 竞赛截屏：他人域。

### 0.3 执行角色与硬边界

| 角色 | 做什么 |
|---|---|
| 执行 AI（本计划对象） | 按 R0→R7 推进**本计划标明可执行**的批次；不确定记 WARN/STOP |
| 主 AI / 用户 | Git、生产发布、业务库写入、值域 confirm、探查终态、CDMS 写字典 |
| 信息科 / DBA | 160 职称包；凭据轮换 |

硬边界（违反即回滚并停止）：

1. 业务源库只允许 `SELECT`。禁止 DML/DDL/EXECUTE。CDMS `KESHID` / JHEMR 写必须有用户明示授权（本轮默认没有）。
2. 密码、Token、身份证、姓名、电话、地址不进代码、日志、git、新文档。
3. 新表只在 `asset` schema。本轮**禁止新迁移**（P3-6 只改 `env.py`）。
4. 前端必须 `pnpm`。不改现有路径 `/relations/{id}/review`、`/dict-medical/*`。
5. **零 Git commit / push / tag**，除非用户在本会话明确点名文件组。
6. **零生产发布、零改 cron、零改熔断 env**。176 F-1 的 `APP_IDENTITY_CB_MAX_UPDATE=150` 已在产，不得回退；`MAX_NEW=130` 不得回落。
7. 值域永不自动 confirm。探查发现不代点终态。关系草稿不自批。
8. 他人域零触碰：`frontend/src/layout/**`、`captureMode.ts`、`store/modules/app.ts`、`tools/capture_*.py`、`competitionCaptureMode.test.ts`。禁止 `git checkout` / `git stash`。
9. 不为过测试弱化断言。失败如实记，不改口径装绿。
10. **禁止重做 177 C1–C5 / F-2 / I1 / I2**。那些代码已在工作区，看懂即可，不要重写。

### 0.4 禁止重做清单（摘自 177，仍有效）

174/175 生产、149 导入、165 X1/X2、166/167/169/171 发布、173 测试本身、177 C1–C5 与 F-2 实现、172 下乡 27 人数据修复、全库探库。详见 177 §0.3。

---

## 1. 本轮真实待办总表

优先级：

- **P0-今晚**：错过会失去观察窗口。
- **P0-代码**：隔离库可修，不碰生产。
- **P1**：有用户可见收益，单会话能做完。
- **等待域**：本轮只呈报，不执行。

### 1.1 P0-今晚（只读）

| ID | 事项 | 现状 | 执行 AI 动作 | 完成判定 |
|---|---|---|---|---|
| N1 | 176 夜跑是否还熔断 | F-1 已把 `max_update` 调到 150；F-2 **未**上生产，故今夜仍按旧语义（110 人例行 resync 会计入 update）。预期：110 < 150 → 主任务 success | 只读查最新 02:00 行，见 R1 | 报告写出 run_id / status / 是否熔断 / 子任务是否被一刀切。失败则 STOP 呈报，**不得调阈值** |
| N2 | 150 课题截止 **今天** | 正文与 `150_fill_form.py` 已删 | 开工问用户一次：还做 / 放弃 / 从备份恢复。未答则记 G9 仍开 | 用户书面裁决写入 55；AI 不写申报内容 |

### 1.2 P0-代码（默认可执行）

| ID | 事项 | 位置 | 建议修法 | 验收 |
|---|---|---|---|---|
| C6 | 173 P3-3：`GraphToolbar` 直接改 props | `GraphToolbar.vue` 模板 v-model **以及 script 里 `changeDisplay`（约 195–198 行）**；父组件 `graph/index.vue` | 见 R2 完整修法。禁止 `emit(..., { field: val })` 这种字面键。开工先 `git diff` 两文件，干净则直接做 | 该文件 `vue/no-mutating-props` 归零（含 changeDisplay）；图谱既有测试 + plan178R2 绿 |
| C7 | 173 P3-6：空库缺 schema | `backend/alembic/env.py` 的 `run_migrations_offline` / `run_migrations_online` | 见 R3。禁止改历史 revision | 纯逻辑测试断言 DDL；**允许**隔离库 `alembic upgrade head` 作 IF NOT EXISTS 幂等 no-op（head 不变）。禁止 drop schema |

### 1.3 P1-代码（默认可执行，用户可见）

| ID | 事项 | 位置 | 建议修法 | 验收 |
|---|---|---|---|---|
| L1 | 166 P2 最小切片：台账 ↔ 探查发现互链 | issue-detail / observations / probe-findings | 见 R4。① 正向链接（scoped-slot 读 `row.source_record_ref`）；② `useRoute` 读 `finding_id`；③ 用现有 `listQualityObservations({ source_kind: 'probe_finding' })` 客户端匹配反查 `issue_id`，跳 `/quality/issues/:id`。不新增后端 | ①②③ 都实现。匹配不到观测或 403 则**隐藏**按钮，不得 WARN 跳过整项③ |

### 1.4 本轮明确不做（附录，写入报告即可）

| ID | 事项 | 原因 |
|---|---|---|
| P3-1 | 删 24 个死 API | 部分是 166/167 半接入，删了可能反向接入时更痛 |
| P3-2 | admin/persons/roles/ops 四页补 v-perms | 整页权限码易挂错，单会话收益低于风险 |
| P3-4 / P3-8 | prettier / as any | 噪声，不阻塞 |
| P3-7 | QualityFinding 平行类型 | 低优先级卫生 |
| 166 P2 其余 | 看板/趋势/调度入口/批量导入 | 调度依赖 165 X4；看板另立项 |
| 174 W4 | 让 run 端点真执行 | 禁止伪造跨系统执行器 |
| F-3 | 主熔断时子任务降级 | 177 G17，可选另批 |

### 1.5 等待域（只呈报，不执行）

沿用 177 **G1–G17 全量**（R6 产出不得只抄本表子集）。下表仅作速查，漏列项仍要写入 `wait_domain.md`：G6 T7 PACS side-b、G10 144 黄金种子、G11 127 Review/配方、G12 凭据轮换、G16 112/96 业务库写保持关闭、G17 F-3 子任务降级。

| # | 事项 |
|---|---|
| G1 | 提交并重建镜像，持久化 CDMS FFREE3 |
| G2 | 生产接入 `jhemr_login_sign_sync` 夜窗子任务 |
| G3 | 发布 F-2，并授权把 `MAX_NEW` 130 回落 50（必须先看 N1） |
| G4/G5 | 165 X3 六条发现人工裁决 / X4 调度周期 |
| G6 | T7 PACSREPORT side-b |
| G7 | CDMS 004066：用户仍未选表现 A/B/C |
| G8 | 160 职称包（含身份证，禁入 git） |
| G9 | 150 课题三选一 |
| G10 | 144 黄金用例生产种子 + 评测开关 |
| G11 | 127 Review/配方/历史问题 |
| G12 | 凭据轮换 |
| G13 | Git 分域提交（177 改动 + 本轮 C6/C7/L1） |
| G14/G15 | 值域 confirm / 关系草稿升正式 |
| G16 | 112/96 业务库写保持关闭 |
| G17 | 176 F-3 主熔断时子任务降级（可选） |

165 六条 open finding 明细仍见 `output_r177/wait_domain.md`，不要重新编造数字。

---

## 2. 工作区分域

盘点时工作区仍混多域。**一次只动一个域。** 177 已改过的身份/catalog/recipes/v-perms **本轮不要再改**。

| 域 | 文件 | 本计划 |
|---|---|---|
| 图谱工具栏 | `GraphToolbar.vue`、`graph/index.vue`、新增前端测试 | **R2 主战场** |
| 迁移环境 | `backend/alembic/env.py` + 新增测试 | **R3** |
| 质量互链 | `issue-detail/index.vue`、`observations/index.vue`、`probe-findings/index.vue`、新增前端测试 | **R4**。probe-findings 已有 177 的 v-perms hunk：**只加 query 选中与链接，不重排、不改流转** |
| 身份 / 质量 174 后端 / layout | 既有 dirty | **整域禁碰** |
| 文档 | 55、README、本文件、178 报告 | 只允许本批日志 |

`frontend/src/router/modules/quality.ts` 默认不改。互链走现有 `/probe-findings` 与 `/quality/issues/:id`（以源码为准，不要发明新 path）。

---

## 3. 批次执行卡

总序：`R0 → R1 → R2 → R3 → R4 → R5 → R6 → R7`。

R1 与 R2 不共享文件，可先做 R1 再编码。R5 CDMS 无用户选择则 SKIP。

每批写入 `开发起步包/output_r178/progress.md` 一节：完成项 / 命令摘要 / 偏离。失败写入 `output_r178/exceptions.json`。

### R0 开工自检（只读，必做）

**读（按序）**：`AGENTS.md` → `开发起步包/README.md` → `55` 顶部 📌 → **本文件全文** → `177_..._执行报告.md` §0–§3 与 `output_r177/wait_domain.md` → 将要改的源文件。

**命令**：

```powershell
cd F:\python\数据资产
git status --short
git diff --stat
git log -5 --oneline
```

**核对**：

1. 记录 SHA 与脏文件到 `output_r178/workspace_snapshot.txt`。对他人域 13 个 layout/capture 文件做 `git hash-object` 基线（可对照 177 快照路径，自行重算，不要改那些文件）。
2. 隔离库只用 8.83 `data_asset_test` 隧道。`backend/.env` 可能指向 `10.20.1.153` 旧库，**脚本一律显式 URL**。
3. 15432 若被占用，自建新本地端口，不要杀别人的转发。
4. 终止非本会话 pytest。
5. 开工时向用户问两句（问一次，未答则 SKIP，不要空转等待）：
   - 004066 表现 A 登录失败 / B 能登功能空白 / C 管理界面未赋权？
   - 150 课题：还做 / 放弃 / 从备份恢复？

**验收**：快照存在；未 checkout/stash；未连业务源。

**STOP**：找不到公钥 SSH `root@10.10.8.83`、隔离库连不上、工作区被人中途把 177 修复还原 → 停止。

---

### R1 176 夜跑只读核验（今天必做）

**时机**：现在（2026-09-04 02:00 已过）。若查到最新 nightly 时间戳仍是 09-03 或更早，记 WARN「本夜 cron 未跑」，不要把旧失败行当成今夜结果。

**通道**：`ssh -o BatchMode=yes -i C:\Users\Administrator\.ssh\id_ed25519_ai root@10.10.8.83`，再查平台库。凭据从服务器 `/etc/data-asset/backend.env` 推导，**口令不落盘、不进报告**。

**查什么（脱敏）**：

1. 容器 `data-asset-api` healthy；settings `identity_cb_max_update=150`、`identity_cb_max_new`（预期仍 130，只记录不改）。
2. 最新 nightly run：`status`、`circuit_breaker_triggered`、`candidates_total`、new/update/align/resync（若有）/deactivate 计数。F-2 未上产时不应出现 `resync_unchanged` 列，缺列不是故障。
3. 签名/职称/科室子任务是否仍因主任务失败被 skip。
4. 抽样空签名账号是否开始被补（只记工号后四位掩码 + 成功/跳过原因）。

**禁止**：手动触发全量同步；改 env；补跑全院签名；把 F-2 热进生产。

**验收**：

- PASS：主任务 success，未触发 max_update，子任务未因主失败被一刀切。
- FAIL：仍熔断 → 把 run_id、维度、计数写入报告，等用户，不自行调到 200。
- 把结论写入 `output_r178/nightly_n1.md`。**不要在此处改 55**（55/README 只在 R7 写一次，避免双写）。

---

### R2 C6 GraphToolbar 单向数据流

**开工先复核（不要无故跳过）**：

```powershell
git diff -- frontend/src/views/asset/components/GraphToolbar.vue frontend/src/views/asset/graph/index.vue
```

两文件在 177 R0 时工作区干净。若 `git diff` 仍为空或只有本会话改动 → **直接做**。仅当出现无法隔离的他人 hunk 才 WARN 跳过 C6，并写明冲突行号。禁止因为「怕改父组件」直接跳过。

**文件白名单**：

- `frontend/src/views/asset/components/GraphToolbar.vue`
- `frontend/src/views/asset/graph/index.vue`（只加 `@update:locate` / `@update:filters` 与两个 `Object.assign` 处理函数，不改布局算法、不改 169 错误态工具栏逻辑）
- 新增 `frontend/tests/plan178R2.test.ts`

**修法（必须按此，禁止大重构；禁止照抄 `field: val`）**：

1. `defineEmits` 增补 `"update:locate": [value: LocateState]` 和 `"update:filters": [value: GraphFilters]`。保留现有 `view-mode-change` / `global-search` / `load-chain` / `load-data` / `refresh` / `reset` 等。
2. 模板里所有绑在 **props** 上的 `v-model` 改为受控。`advancedVisible` 是本地 `ref`，**保持** `v-model="advancedVisible"`。
3. 逐字段写法（三个代表形态，其余字段同构套用）。**禁止** `{ field: val }` 字面键，也尽量不要写动态 `[field]`，以免漏字段：

```vue
<!-- locate.table：输入框 -->
<el-input
  :model-value="locate.table"
  @update:model-value="emit('update:locate', { ...locate, table: String($event ?? '') })"
  placeholder="搜索表名、中文名、Schema 或五段物理键"
  clearable
  @keyup.enter="emit('global-search')"
/>

<!-- locate.depth：segmented（locate.direction 同构） -->
<el-segmented
  :model-value="locate.depth"
  :disabled="!locate.physical_key"
  :options="depthOptions"
  @update:model-value="emit('update:locate', { ...locate, depth: Number($event) as 1 | 2 | 3 })"
  @change="emit('load-chain')"
/>

<!-- filters.system_code：select（source_code/schema/domain/validation_status/confidence 同构） -->
<el-select
  :model-value="filters.system_code"
  placeholder="业务系统"
  clearable
  filterable
  @update:model-value="emit('update:filters', { ...filters, system_code: String($event ?? '') })"
  @change="emit('system-change')"
/>
```

`filters.limit` 用 `el-input-number` 的 `:model-value` / `@update:model-value`，payload 为 `{ ...filters, limit: Number($event) }`。  
`include_candidates` / `include_dependencies` / `show_review_layer` 用 `el-checkbox` 同样改受控，payload 为对应 boolean。

必须改完的 props 绑定清单：`locate.table` / `locate.depth` / `locate.direction`；`filters.system_code` / `source_code` / `schema` / `domain` / `validation_status` / `confidence` / `limit` / `include_candidates` / `include_dependencies` / `show_review_layer`。

4. **script 级突变（上次验收会失败的遗漏）**：`changeDisplay` 约 195–198 行当前是 `props.filters.layout_mode = command as LayoutMode; emit("refresh")`。改为：

```ts
function changeDisplay(command: string) {
  emit("update:filters", { ...props.filters, layout_mode: command as LayoutMode });
  emit("refresh");
}
```

改完后该文件不得再出现 `props.filters.` 赋值或 `props.locate.` 赋值。

5. 父组件 `graph/index.vue`：

```vue
<GraphToolbar
  :filters="filters"
  :locate="locate"
  @update:filters="onUpdateFilters"
  @update:locate="onUpdateLocate"
  ...原有监听保持
/>
```

```ts
function onUpdateFilters(next: typeof filters) {
  Object.assign(filters, next);
}
function onUpdateLocate(next: typeof locate) {
  Object.assign(locate, next);
}
```

对已有 reactive 对象 `Object.assign`，不要替换成新对象导致丢失引用。

**测试（R2 批内先跑，不要拖到 R7）**：

```powershell
cd F:\python\数据资产\frontend
pnpm exec vitest run tests/plan178R2.test.ts tests/graphPage.test.ts tests/graphPageMount.test.ts tests/plan169G2.test.ts tests/plan169G3.test.ts tests/plan169G4.test.ts tests/graphNeo4jP0.test.ts
```

`plan178R2.test.ts` 源码锁：

- `GraphToolbar.vue` 不得出现 `v-model="locate.` / `v-model="filters.`。
- 不得出现 `props.filters.layout_mode =`。
- 父组件模板含 `@update:locate` 与 `@update:filters`。
- `changeDisplay` 含 `emit("update:filters"`。

`graphNeo4jP0.test.ts` 扫 GraphToolbar 源码（「搜索并聚焦」「高级筛选」`command="force"`）。若被新写法误伤，只改断言字符串适配，**禁止**删掉这些锁。不要为过测试去改 G6 布局参数。

**验收**：对该文件 `vue/no-mutating-props` 归零；上列 vitest 0 failed；typecheck 不因本次变红。

---

### R3 C7 空库自建 schema

**文件白名单**：

- `backend/alembic/env.py`
- `backend/tests/conftest.py`（**只许**把 `"alembic_env"` 追加进 `_PURE_LOGIC_SUBDIRS` 元组，禁止改门禁逻辑）
- 新增 `backend/tests/alembic_env/conftest.py`（纯逻辑哨兵 URL + 覆盖 `clean_test_database`，照抄 `tests/plan144/conftest.py` 模式）
- 新增 `backend/tests/alembic_env/test_schema_ddl.py`（读 `alembic/env.py` 文本断言 DDL）。**不要**把测试放 `tests/test_alembic_env_schema.py` 顶层——根 conftest 会要求 `APP_TEST_DB_URL`

**修法**：

```python
from sqlalchemy import text

def _ensure_asset_schema(connection) -> None:
    connection.execute(text("CREATE SCHEMA IF NOT EXISTS asset"))
    connection.commit()
```

- online：拿到 `connection` 后先 `_ensure_asset_schema`，再 `context.configure` + `run_migrations`。
- offline：在 `run_migrations` 前发出同一 DDL（`context.execute(text("CREATE SCHEMA IF NOT EXISTS asset"))`），保证离线 SQL 脚本含该句。
- PostgreSQL 专用即可。不要不带 `IF NOT EXISTS`。

**验收**：

1. `python -m pytest tests/alembic_env -q` 0 failed（纯逻辑，不连库）。
2. **允许且建议**对隔离库跑一次 `alembic upgrade head`：当前已是 `d5e6f7a8b9c0`，应是 no-op，用来证明 `IF NOT EXISTS` 不炸；随后 `alembic current` 仍为该 revision。
3. 有空 postgres 再做从零 `upgrade head`；没有空库不记失败。禁止新建 8.83 业务库，禁止 drop schema。

**禁止**：`--autogenerate`；改历史 revision；在 `data_asset_test` 里 drop schema 做实验。

---

### R4 L1 台账 ↔ 探查互链

**文件白名单**：

- `frontend/src/views/quality/issue-detail/index.vue`
- `frontend/src/views/quality/observations/index.vue`（来源列同样改 scoped-slot；若改动超过一列则只改 issue-detail，observations 记 WARN）
- `frontend/src/views/asset/probe-findings/index.vue`（加 `useRoute`、query 选中、反查按钮；不改 transition）
- `frontend/src/api/quality.ts`（只 **import 已有** `listQualityObservations`，禁止改函数签名）
- 新增 `frontend/tests/plan178R4.test.ts`
- 允许新增纯函数文件，例如 `frontend/src/views/quality/sourceRef.ts`，禁止新建 API 模块、禁止新后端

**解析约定**（写进纯函数，三页共用）：

```text
输入 source_record_ref
匹配 ^asset_probe_findings:(\d+)$  → { type: 'probe_finding', id: number }
其它 / 空 → null
```

后端已写入格式：`quality_governance_adapters.py` 约 107–108 行 `source_kind="probe_finding"`、`source_record_ref=f"asset_probe_findings:{f.id}"`。列表/详情序列化已含这两字段和 `issue_id`（`quality_observations.py` `_serialize`）。

**① 正向：台账观测 → 探查页**

`issue-detail/index.vue` 观测表约 212 行目前是 `<el-table-column prop="source_kind" ...>`，**没有** `source_record_ref` 列。`source_record_ref` 在 **行数据** 里（API 已返回）。必须把该列改成 scoped-slot：

```vue
<el-table-column label="来源" width="160">
  <template #default="{ row }">
    <el-link
      v-if="parseProbeFindingRef(row.source_record_ref)"
      type="primary"
      @click="goProbeFinding(parseProbeFindingRef(row.source_record_ref)!.id)"
    >{{ row.source_kind }}</el-link>
    <span v-else>{{ row.source_kind || "-" }}</span>
  </template>
</el-table-column>
```

`goProbeFinding(id)` 使用现有路由 `/probe-findings?finding_id={id}`（`router/modules/quality.ts` 约 84 行）。不要发明新 path。

**② 探查页消费 query**

该页目前只 `import { useRouter } from "vue-router"`（约 247 行，给既有跳转用）。**必须补 `useRoute`**，`const route = useRoute()`，`watch(() => route.query.finding_id, ...)` + 首次 `onMounted` 在 list 加载后执行：

- `finding_id` 缺省或非数字 → 忽略。
- 在现有 `items` 里按 id 查找：找到则选中/打开已有详情（没有抽屉就高亮该行）。
- 找不到 → `ElMessage.warning` 一次「未在当前筛选结果中找到该发现」。**不要**自动清筛选去全库盲扫。

**③ 反向：探查行 → 台账详情（按此路径实现，禁止因「没现成 issue_id 字段」整项 WARN）**

发现页详情区加「查看质量台账」按钮，**不新增后端**。路径：

1. `import { listQualityObservations } from "@/api/quality"`（已有，约 322 行，参数含 `source_kind`，`page_size` 上限 100）。
2. 选中行变化时调用 `listQualityObservations({ source_kind: "probe_finding", page: 1, page_size: 100 })`。
3. 客户端找 `item.source_record_ref === \`asset_probe_findings:${currentId}\`` 且 `item.issue_id` 为数字。
4. 命中 → 显示按钮，点击 `router.push(\`/quality/issues/${issue_id}\`)`（详情路由 `quality.ts` 约 50 行 `/quality/issues/:id`）。
5. 未命中、`total>100` 时最多再拉 `page=2` 一次；仍无则隐藏按钮。
6. 接口 403（缺 `quality.observation.read`）或网络失败：隐藏按钮，**不要**把页面打成错误态，至多 `console` 级记录。现产 probe_finding 观测约 6 条，一页足够。

**禁止**：自动 `transition` 发现；把 issue 状态改成与 finding 同步；请求业务源；为互链新开后端接口。

**验收**：`plan178R4.test.ts` 覆盖解析成功/失败、非法 `finding_id` 忽略、`asset_probe_findings:12` 能拼出 `/probe-findings?finding_id=12`；`plan174QualityLedger.test.ts` / `plan166StageD2.test.ts` 0 failed。

---

### R5 CDMS（默认 SKIP）

与 177 R5 相同。用户未在本会话选择 004066 表现则整批跳过。未授权禁止写 `KESHID`、改 `FPWD`、给赵慧补权限。

---

### R6 等待域更新包

产出 `output_r178/wait_domain.md`：

1. **按 G1–G17 全量 17 项**抄 177 `output_r177/wait_domain.md` 并标注本轮未动。不要只用 §1.5 速查表的子集。
2. 写入 N1 夜跑结论（PASS/FAIL/WARN）和 run_id。
3. 150 / 004066 若用户有书面答复，记下来；没有则「仍开」。
4. 本轮新增等待：C6/C7/L1 **代码完成、待随 G13 提交并随下次前端/镜像发布**。

不要重新 COUNT 复核池。

---

### R7 门禁与文档

```powershell
cd F:\python\数据资产\backend
.\.venv\Scripts\python.exe -m pytest tests/alembic_env tests/test_r177_fixes.py tests/test_identity_cb_resync_f2.py -q --tb=line
# 隔离库幂等（允许）：
.\.venv\Scripts\python.exe -m alembic upgrade head
.\.venv\Scripts\python.exe -m alembic current
# 若时间够：
.\.venv\Scripts\python.exe -m pytest tests/ -q

cd F:\python\数据资产\frontend
pnpm run typecheck
pnpm test
pnpm run build
```

全量 pytest 若超过 40 分钟仍在跑，允许先交专项 + 前端三件套，全量结果后补，但不得把「没跑」写成「通过」。

**文档（允许改）**：

- 本文件 §8 执行日志
- `55` 追加一行 📌
- `README.md` 目录更新记录一行 + 178 行状态改为执行结果
- 新建 `开发起步包/178_177收口后可执行改进_执行报告.md` + `178_177收口后可执行改进_结果.json`
- `output_r178/`：`progress.md`、`exceptions.json`、`workspace_snapshot.txt`、`nightly_n1.md`、`wait_domain.md`

**Git**：默认不做。即使用户授权，也必须一组一提交、`git show` 零越界：

1. `fix(173): GraphToolbar stop mutating props`
2. `fix(alembic): create asset schema if not exists`
3. `feat(quality): probe finding cross links`
4. `docs(178): report`

禁止把 layout/captureMode/身份/174 后端塞进上述任何一组。

---

## 4. 环境备忘

```text
仓库：F:\python\数据资产
SSH：root@10.10.8.83:22，密钥 C:\Users\Administrator\.ssh\id_ed25519_ai，BatchMode=yes
跳板：10.10.8.53:40022；本机 Windows 不直连 Oracle
隔离测试库：8.83 data_asset_test
生产镜像现态（2026-09-03）：data-asset:r175-20260903；前端 r175-20260903
生产迁移头：d5e6f7a8b9c0
熔断：identity_cb_max_update=150（F-1 在产）；identity_cb_max_new=130（临时，勿动）
F-2 代码在工作区、不在生产
前端：必须 pnpm
177 基线门禁：后端 1415P/1S/0F；前端 267 tests
```

---

## 5. 完成定义（DoD）

宣称「178 完成」必须同时成立：

1. R1 有 `nightly_n1.md`，写明 run_id 与是否熔断；没有夜跑结果不得写「已验证通过」。
2. C6 已做：`changeDisplay` 不再赋值 `props.filters`；模板无 `v-model="locate.` / `v-model="filters.`；R2 点名的图谱既有测试绿。仅当 `git diff` 证实他人 hunk 无法隔离才允许 WARN 跳过。
3. C7 `env.py` 含 `CREATE SCHEMA IF NOT EXISTS asset`；测试在 `tests/alembic_env/`；隔离库 `upgrade head` no-op 不炸（若跑了）。
4. L1 ① scoped-slot 正向链接、② `useRoute` 消费 `finding_id`、③ `listQualityObservations` 反查后有按钮或因无匹配/403 **隐藏**（不是整项 WARN 跳过）。
5. 前端 typecheck + tests + build 过；后端至少专项 0 failed。全量 pytest 跑了就报数字，没跑就写「未跑」。
6. 执行报告 + `_结果.json` + `output_r178/`。
7. README / 55 已登记（**只在 R7 写一次**）。
8. 他人域哈希与 R0 一致。业务源写入 0。生产 env/cron/镜像未改。零 Git 写（除非用户点名）。
9. 等待域 G1–G17 全量呈报，没有被标成已完成。

**不得宣称完成的例子**：只改了文档；把 09-03 的失败 run 当成今夜结果；把 C6 因「怕改父组件」直接跳过；照抄 `{ field: val }` 造成坏数据流；为互链新增写接口；把 L1③ 因「计划没写调用路径」WARN 跳过。

---

## 6. 执行提示词（复制给执行 AI，勿增删）

```text
【角色】你是仓库 F:\python\数据资产 的执行 AI。唯一执行依据是 开发起步包/178_177收口后可执行改进一次性执行计划.md（v1.1）。55 仍是总入口，本轮待办以 178 为准。不要执行 163/164/165/171/174/177 已完成批次，不要重新探库，不要重写 177 已落地的 C1–C5 / F-2 / FFREE3 / 登录签名子任务。

【开工前必读】AGENTS.md；开发起步包/README.md；55 顶部 📌；178 全文（注意 v1.1 对 R2/R3/R4 的硬性修法）；177 执行报告 §0–§3；output_r177/wait_domain.md；将要改的源文件。

【铁律】业务源库零写入；凭据零落盘；零 Git 写（用户未点名授权前）；零生产发布/零改 env/零改 cron；值域不自动 confirm；探查发现不代点终态；他人域 layout/captureMode/竞赛截屏零触碰；禁止 git checkout/stash；不为过测试弱化断言；不确定记 WARN 继续或 STOP，不要猜。150 课题禁止虚构申报稿。

【批次】R0 快照（并问用户 004066 表现与 150 三选一，未答则 SKIP）→ R1 夜跑只读核验（今天必做，结论只写入 output_r178/nightly_n1.md，55 留到 R7）→ R2 GraphToolbar 单向数据流：git diff 干净则必做；改模板全部 props v-model + script 里 changeDisplay；禁止 emit 字面键 field: val；批内先跑 graphPage/graphPageMount/plan169G2-G4/graphNeo4jP0 → R3 alembic env.py 自建 schema，测试放 tests/alembic_env/，允许隔离库 upgrade head no-op → R4 台账/探查互链：source_kind 列改 scoped-slot 读 row.source_record_ref；probe-findings 补 useRoute；③ 用 listQualityObservations({source_kind:'probe_finding'}) 匹配 asset_probe_findings:{id} 取 issue_id 跳 /quality/issues/:id，匹配不到或 403 则隐藏按钮，禁止整项 WARN 跳过③；不新增后端 → R5 CDMS 无选择则 SKIP → R6 wait_domain.md 必须 G1–G17 全 17 项 → R7 门禁+报告（55/README 只在这里写一次）。

【环境】隔离库只用 8.83 data_asset_test 隧道，显式 URL，不信任 backend/.env。前端 pnpm。端口冲突不要杀外部 15432 转发。F-2 未上生产，夜跑仍按旧 update 语义解释。

【产出】开发起步包/178_177收口后可执行改进_执行报告.md + 同号 _结果.json；output_r178/progress.md、exceptions.json、workspace_snapshot.txt、nightly_n1.md、wait_domain.md；README 目录更新记录一行；55 追加 📌。宣称完成必须满足 178 §5。

【STOP 白名单】隔离库不可用；他人域无法避开；需要写 CDMS/JHEMR/KESHID；需要改生产 env；空库实验要 drop 已有 schema。其它失败 WARN 留痕继续。
```

---

## 7. 给用户的使用说明（本段不是执行 AI 的待办）

把 §6 整段提示词原样交给一次性免费 AI（必须标明 **v1.1**）。不要再追加「顺便把质量看板、Git 提交、发布生产、confirm 值域」——那些会把单会话带进等待域或越权。

若你打算让它连 Git/发布一起做，必须在同一条消息里**点名授权**，例如：「允许按 178 §3 R7 四组提交，但禁止 push、禁止发布」。未点名则它必须零 Git 写。

本轮做完后，真正能提升线上可用性的下一步仍是你授权的 **G13 分域提交 + 镜像重建（G1/G3）**。代码再改而不入库，容器一重建就会丢掉 FFREE3 热修。

---

## 8. 执行日志

| 时间 | 操作 | 结果 |
|---|---|---|
| 2026-09-04 | 对照 README/55/177 报告与 wait_domain、173 剩余 P3、git 工作区性质，制定 178 | 产出本计划 v1.0；未改应用代码；未连业务库；未发布 |
| 2026-09-04 | 独立核查后升 v1.1 | 采纳：R2 禁止 `field: val` 字面键并补 `changeDisplay`；L1③ 写明 `listQualityObservations` 反查路径且不得 WARN 跳过；① 点明 `row.source_record_ref` scoped-slot；② 补 `useRoute`；C7 测试改 `tests/alembic_env/`、允许隔离库 no-op upgrade；C1 不进镜像；G1–G17 全量；55 只在 R7 写一次；R2 批内跑图谱既有测试；C6 干净则必做。未改应用代码。 |
| 2026-09-04 晚 | 按 v1.1 提示词单会话执行 R0→R7 | **执行完成，生产未发布**。R1 FAIL 呈报（RUN-b3324c242e78 维度 max_change_ratio 仍熔断，未调阈值，归 G3）；C6/C7/L1 全落地；门禁后端全量 1419P/1S/0F+专项 17/0、前端 278/278+build 三绿；R5 未选表现 SKIP、两问未答记仍开；他人域 13 哈希零触碰、零 Git 写。报告+`_结果.json`+output_r178/ 五件套同号登记。 |
