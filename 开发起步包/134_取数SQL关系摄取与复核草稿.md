> 类别：证据
> 上游：`sql-relation-intake` 技能；输入：`取数/` 历史 HIS SQL；正式关系：`数据资产_HIS源端资产包/his_source_relationships.csv` + `数据资产_资产包/relationships.csv`
> 结论：67 个 SQL/TXT 解析出 164 条唯一边。25 条已是正式关系，只补证据。13 条 HIS 内部新边写入平台 `asset_relation_reviews` 草稿（id=4–16；Q07 已按用户确认改为 `FXHIS.SYS_DEPARTMENT`）。未批准、未改正式关系。其余冲突/跨系统/解析噪声仍不入库。

# 134 取数 SQL 关系摄取与复核草稿（2026-08-18）

## 1. 来源

- 系统/方言：HIS 源端为主（Oracle 11g 多 Owner）；部分 SQL 写 `HIS.表名`（数据中心口径），部分写 Owner.表名。
- 扫描范围：`取数/` 下 `.sql` / `.txt`，排除建用户、飞检建表、模板、queryctl 烟雾测试。
- 文件数：67；去重后唯一关系：164。
- 机器可读中间结果：`取数/_query_outbox/SQL_RELATION_INTAKE_20260818/summary.json`、`candidates.json`。
- 是否已脱敏：报告与候选 JSON 不含患者条件值、口令或身份信息。
- 历史 SQL 本身最高只作 B/C 级证据，未做本轮活库验证，不得标 A / `verified`。

解析器是确定性预解析，旧式逗号连接、CTE 别名、隐式 WHERE、函数包裹键已人工复核。

## 2. 分级汇总

| 分级 | 条数 | 处理 |
|---|---:|---|
| existing | 25 | 已有正式关系，只记证据，不重复入库 |
| candidate（元数据通过） | 23 | 人工筛选后 13 条进复核草稿；其余见冲突项 |
| partial | 44 | 字段不齐、跨系统或别名误解析，不入库 |
| rejected | 72 | 多为 48 项 SQL 的 CTE 别名（`VISITS`/`OPS`/`BASE_OPER` 等），不是物理表 |

## 3. 已写入平台复核草稿（13 条）

写入表：`asset.asset_relation_reviews`（生产 10.10.8.83），`review_status=draft`，`validation_status=pending`，id=4–16。  
系统编码一律 `HIS_SOURCE` / `his_source_10_10_10_15`（源端 Owner.表）。  
**未**写入 `relationships.csv`，**未**批准，**未**发布配方。  
2026-08-18 用户确认：处方号类型转换可接受；检验字典按 `ITEM_CLASS='C'+ITEM_CODE`；开单科室主口径 `FXHIS.SYS_DEPARTMENT`；检查申请科室按纠正后的 `DEPT_DICT` 边入库。

| 键 | 库 id | 关系 | 连接键 | 置信度 | 依据 |
|---|---|---|---|---|
| Q01 | 4 | `MEDREC.TRANSFER` → `MEDREC.PAT_VISIT` | PATIENT_ID+VISIT_ID | B | 工作量日报床日数 3 份 SQL |
| Q02 | 5 | `MEDREC.TRANSFER.DEPT_STAYED` → `COMM.DEPT_DICT.DEPT_CODE` | 所在科室 | C | 床日数核对 SQL |
| Q03 | 6 | `MEDADM.STAT_DEPT_MODIFYFIX.STAT_DEPTCODE` → `COMM.DEPT_DICT.DEPT_CODE` | 统计科室码 | C | 病房工作报表 |
| Q04 | 7 | `OUTPBILL.OUTP_ORDER_DESC.RCPT_NO` → `OUTPBILL.OUTP_RCPT_MASTER.RCPT_NO` | 收据号 | C | 医保飞检 / 市监费用 |
| Q05 | 8 | `INPBILL.INP_SETTLE_MASTER_YB` → `INPBILL.INP_SETTLE_MASTER` | PATIENT_ID+VISIT_ID | B | 医保飞检；同一就诊可多条结算 |
| Q06 | 9 | `OUTPBILL.OUTP_BILL_ITEMS.ORDERED_BY_DOCTOR` → `COMM.STAFF_DICT.EMP_NO` | 开单医生工号 | C | 药占比门诊 SQL |
| Q07 | 10 | `INPBILL.INP_BILL_DETAIL.ORDERED_BY` → `FXHIS.SYS_DEPARTMENT.DEPTCODE` | 开单科室（门户主口径） | C | 住院药占比 / 药品金额 SQL |
| Q08 | 11 | `MEDREC.PAT_VISIT.DISCHARGE_DISPOSITION2` → `COMM.DISCHARGE_DISPOSITION_DICT.DISCHARGE_DISPOSITION_CODE` | 出院方式 | C | 抗菌药物 SQL |
| Q09 | 12 | `MEDREC.OPERATION.OPERATION_CODE` → `COMM.OPERATION_DICT.OPERATION_CODE` | 手术编码 | C | 48 项指标 25 |
| Q10 | 13 | `DRUG_USER.PHA_INP_REQUEST_DRUG.DRUG_PRI` → `DRUG_USER.MED_DICT_PRICE.DRUG_PRI` | 药品价格主键 | C | 基本药物 SQL |
| Q11 | 14 | `DRUG_USER.PHA_CLI_REQUEST_DRUG` → `ORDADM.ORDERS` | PAT_ID+TO_CHAR(处方号) | C | 抗菌药物 SQL；用户确认类型转换 |
| Q12 | 15 | `LAB.LAB_TEST_ITEMS.ITEM_CODE` → `COMM.CLINIC_ITEM_DICT` | ITEM_CODE 且 ITEM_CLASS='C' | C | 抗菌药物 SQL；检验类限定 |
| Q13 | 16 | `EXAM.EXAM_MASTER.REQ_DEPT` → `COMM.DEPT_DICT.DEPT_CODE` | 申请科室 | C | 纠正别名误配；用户确认 |

说明：

- `TRANSFER`、`STAT_DEPT_MODIFYFIX`、`PHA_INP_REQUEST_DRUG` 只在 HIS 源端资产包，ODS `tables.csv` 无同名表，所以不登记成 `DATA_CENTER / HIS.xxx`。
- Q04 不替代已有正式边 55/558/565（开单↔费用明细三联键或收据号）。
- Q05 不替代已有正式边 54（费用明细→医保结算）。
- Q07 已按用户确认改为 `FXHIS.SYS_DEPARTMENT`（`FXHIS` 不在 HIS 源端资产包，字段来自取数 SQL `药品金额.txt`）。同文件还有 `PERFORMED_BY`、门诊 `ORDERED_BY_DEPT` 未另建草稿。
- Q11 用 `TO_CHAR(CURRENT_PRESC_NO)` 对齐 `PRESC_NO`，并保留 `CURRENT_PRESC_NO IS NOT NULL`。
- Q12 的 `LAB_TEST_ITEMS` 没有 `ITEM_CLASS` 字段，字典侧固定 `'C'`；原 SQL 的 `EXPAND2<>'微生物类'` 仍只作配方限定。
- Q13 是纠正边：原解析把 `dd` 配成费用表，实际是 `dept_dict`。开单科室主口径是门户表，本边按原 SQL 仍挂 `DEPT_DICT`。

导入脚本：`backend/scripts/import_query_sql_relation_reviews.py`。候选原文：`134_取数SQL关系摄取候选.json`。

## 4. 已经是正式关系（25 条，只补证据）

取数 SQL 再次出现、且已与正式资产等价（含反向）的边，不新建草稿。主要有：

| 正式关系 | 取数中再次出现的业务 |
|---|---|
| R001 PAT_VISIT→PAT_MASTER_INDEX | 多份费用/检验/检查 SQL |
| R002 DIAGNOSIS→PAT_VISIT | 48 项、抗菌 |
| R003 OPERATION→PAT_VISIT | 48 项手术指标 |
| R004 PATS_IN_HOSPITAL→PAT_VISIT | 工作量/在院 |
| R006 ORDERS→PAT_VISIT | 抗菌、48 项 |
| R011 LAB_TEST_MASTER→PAT_VISIT（非零 VISIT_ID 子集） | 抗菌、医保 |
| R012/R013/568 检验主从/结果 | 化验结果、抗菌 |
| R016/R017/R018 检查主从 | 影像检查、抗菌 |
| R022/R023 住院费用→就诊/结算 | 市监住院费用 |
| R028/R029 门诊收据↔明细/主索引 | 医保、市监 |
| ODS 3/4 PAT_VISIT 入/出院科室→DEPT_DICT | 工作量表二 |
| ODS 25/36 嘉和病历内部及病历→住院 | 48 项（跨系统，已有正式边） |
| ODS 40 PACS 患者内部 | 影像检查 |
| ODS 51/52 手术主表及术者工号 | 48 项 |
| ODS 61 MED_DICT_BASEINFO↔MED_DICT_PRICE | 基本药物 |

这些边「大部分是对的」，但它们已经在库里，无需再导入。

## 5. 冲突、不确定、请你拍板（未入库）

### 5.1 解析正确但不宜当无条件外键

| 解析到的边 | 原因 | 建议 |
|---|---|---|
| `PAT_VISIT` 自关联 `PATIENT_ID` | 非计划再入院比较两次住院，不是表间外键 | 做成查询配方，不登记关系边 |
| `ORDERS` 五键 `...+ORDER_CODE` → `ORDERS_COSTS.ITEM_CODE` | 正式 R007 已是四键 `P+V+ORDER_NO+ORDER_SUB_NO`；多出来的 `ORDER_CODE=ITEM_CODE` 是抗菌药匹配收紧条件 | 保留为配方限定，不拆新边 |
| `OUTP_BILL_ITEMS` 仅 `VISIT_DATE+VISIT_NO` → `OUTP_ORDER_DESC` | 弱于已验证三联键 558/565（必须再加 `RCPT_NO`，开单表有重复） | 不入库；继续用三联键 |
| `INP_BILL_DETAIL` / `LAB_TEST_MASTER` / `EXAM_MASTER` 只按 `PATIENT_ID` 挂主索引 | 就诊级事实丢 `VISIT_ID`，会放大行数 | 继续走已有 P+V 边 |
| `CLINIC_ITEM_DICT.ITEM_CODE` → `LAB_TEST_ITEMS.ITEM_CODE` | 字典主键是 `ITEM_CLASS+ITEM_CODE` | 已按用户确认入库为 Q12（`ITEM_CLASS='C'`） |
| `PHA_CLI_REQUEST_DRUG.PAT_ID+PRESC_NO` → `ORDERS.PATIENT_ID+CURRENT_PRESC_NO` | `PRESC_NO` VARCHAR2 vs `CURRENT_PRESC_NO` NUMBER(5) | 已按用户确认入库为 Q11（`TO_CHAR`） |

### 5.2 字段在，但业务上几乎肯定不是同一键空间

| 解析到的边 | 原因 |
|---|---|
| `INP_SETTLE_MASTER.RCPT_NO` = `OUTP_RCPT_MASTER.RCPT_NO` | 住院收据号与门诊收据号不是同一套编号 |
| `OUTP_RCPT_MASTER.RCPT_NO` = `INP_BILL_DETAIL.RCPT_NO` | 同上，门急诊/住院串号 |
| `OUTP_RCPT_MASTER.PATIENT_ID` = `INP_SETTLE_MASTER.PATIENT_ID` | 同一患者可同时有门诊和住院，不是一次就诊关系 |
| `DRUG_IMPORT_MASTER.DOCUMENT_NO` = `DRUG_EXPORT_DETAIL.DOCUMENT_NO` | 入库单号对出库明细，多半是逗号连接别名串了 |

### 5.3 别名/隐式连接误配（解析噪声）

典型：`DEPT_DICT.PATIENT_ID`、`PAT_MASTER_INDEX.RCPT_NO`、`PAT_MASTER_INDEX.VISIT_NO` 仍是解析噪声，不入库。  
`EXAM_MASTER.REQ_DEPT` 被误配成费用 `DEPT_CODE` 已纠正为 Q13（`dept_dict`）。

### 5.4 跨系统 / D 类（按 40 号口径暂不分析）

| 类型 | 例子 | 处理 |
|---|---|---|
| 嘉和病历 | `JHEMR.JHMR_FILE_INDEX` ↔ HIS 住院/手术 | 已有正式边 25/36 的不再重复；新的跨库边不入库 |
| 手麻/ODS 手术主表 | SQL 写 `HIS.OPERATION_MASTER`，源端无此 Owner.表 | 源端正式边是 `MEDREC.OPERATION`；`SURGERY.OPERATION_MASTER` 不在 HIS 源端表清单 |
| 移动护理 | `YDHL.MCS_VITAL_INFO` 用 `PATIENT_ID+VISIT_ID` 挂住院 | 已验证主口径是 `PATIENT_UID`/`MRN+SERIES`，这条会误导 |
| HRP 薪酬 | `HRPSEY656.WA_*` | 走 HRP 资产包，不并入 HIS 关系 |
| PACS | `ReportID=ExamID` 等 | 已有 PACS 专题，不从 HIS SQL 再升 |
| FXHIS 科室 | `SYS_DEPARTMENT.DEPTCODE` = 费用开单/执行科室 | 用户确认主口径；Q07 已改。同文件 `PERFORMED_BY`、门诊 `ORDERED_BY_DEPT` 尚未单独建草稿 |

### 5.5 用户已确认（2026-08-18）

1. 门诊处方号类型不一致：按 Q11 入库（`TO_CHAR`）。
2. 检验项目字典：按 Q12 入库，字典侧补 `ITEM_CLASS='C'`。
3. 开单科室主口径：`FXHIS.SYS_DEPARTMENT`（Q07 已改写 id=10）。
4. 检查申请科室：按纠正边 Q13 挂 `COMM.DEPT_DICT`。

仍待确认：Q01–Q13 是直接批准进正式关系，还是先抽活库验证再批。

## 6. 回写情况

- 已写：本报告、`134_取数SQL关系摄取候选.json`、导入脚本、平台复核草稿（见结果 JSON）。
- 未写：`relationships.csv`、`his_source_relationships.csv`、catalog、关系配方、正式关系批准。
- 未做：源库 DML/DDL；本轮未跑活库命中率验证。
- 查看位置：平台「关系复核」列表，状态 draft；证据前缀 `134 取数SQL关系摄取`。

## 7. 复现

```powershell
python tools\intake_query_dir_relations.py
cd backend
.\.venv\Scripts\python.exe -m scripts.import_query_sql_relation_reviews --dry-run
.\.venv\Scripts\python.exe -m scripts.import_query_sql_relation_reviews
```
