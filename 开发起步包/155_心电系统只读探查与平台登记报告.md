> 类别：证据
>
> 状态：已完成（2026-08-27，用户授权接入心电系统并"记录到服务器"）
>
> 上位入口：`55_系统未完成事项统一执行计划.md`
>
> 关联文档：sjzc 技能（新源登记后即插即用）、143 号（OA 同类接入先例）、81/82/83 号（SQLServer 源端探查范式）

# 心电系统只读探查与平台登记报告（155）

## 1. 接入信息与安全边界

| 项 | 值 |
|---|---|
| 系统 | 心电系统（EDS China，vendor） |
| 实例 | 10.10.10.80:1433，**Microsoft SQL Server 2008 R2 Standard (10.50.1600.1 X64)** |
| 账号 | sa（**高权限——建议尽快轮换为专用只读账号，同 143 号 OA admin 待办**） |
| 连接特性 | FreeTDS/pymssql 需 **tds_version=7.0**（7.1+ 握手失败）；平台连接器已内置 7.0 回退（db_connectors.py:541/:576），无需改码 |
| 探查方式 | 仅 SELECT 目录视图/INFORMATION_SCHEMA + sys.dm_db_partition_stats 行数聚合；**源库 DML/DDL=0** |

## 2. 库与对象概况（2026-08-27 快照）

| 库 | 表 | 视图 | 列 | 声明主键列 | 声明外键 |
|---|---:|---:|---:|---:|---:|
| EDSChinaDB（主库） | 57 | 4 | 931 | 55 | **0**（关系需推断） |
| cardioNet（接口库） | 2 | 2 | 188 | 0 | 0 |

行数 Top：LOG_MST 359 万、DATA_TBL 284 万（波形数据）、STUDY_TBL 39.4 万、MEASUREMENT_TBL 39.4 万、ORDER_TBL 32.7 万、REPORT_HISTORY_TBL 31.8 万。

## 3. 业务主线结构（键位证据）

**EDSChinaDB 检查链**（各表单列代理主键 + 序号键串联）：

```text
ORDER_TBL(申请, PK ORDER_SEQ) ──ORDER_SEQ/ORDER_NO──> STUDY_TBL(检查, PK STUDY_SEQ)
STUDY_TBL ──STUDY_SEQ──> MEASUREMENT_TBL(测量) / REPORT_HISTORY_TBL(报告) / DATA_TBL(波形)
全部主表带 PATIENT_ID + VISIT_ID（与 HIS 万能键同名，跨系统对齐候选）
```

- STUDY_TBL 100 列（含 ORDER_*、检查科室/项目/部位、VISIT_*、收费字段）；ORDER_TBL 59 列。
- **cardioNet 接口库**：全中文列名——`患者检查申请信息_TABLE`/`患者检查报告信息_TABLE`（各 25.4 万行），键位含 **住院号+住院次数**（≈HIS PATIENT_ID+VISIT_ID）、门诊号、申请号——HIS↔心电接口表，跨系统关系候选。
- 视图 6 个：EDSChinaDB 的 `ECG_EXAM_INFO`、`CDR_CHECK_REPORT`（CDR 检查报告）、`YJ_JCBG`（院级检查报告）、`View_ECG_BS25011`；cardioNet 的申请/报告视图（定义仅 96 字符=直接 SELECT 表）。完整 DDL 在结果 JSON。

> 关系推断仅为候选线索（源库零声明外键）；正式关系须走 sql-relation-intake 候选流程，未经审核不入 relationships.csv。

## 4. 记录到服务器（8.83）明细

1. **凭据受控文件**：`/etc/data-asset/credentials/ecg_sqlserver_10_10_10_80.readonly`（0600，内容 username:password 单行，不入 Git/文档）。
2. **平台登记**：业务系统 `ECG｜心电系统`（active）+ 双数据源 `ecg_sqlserver_10_10_10_80`（EDSChinaDB）/`cardionet_sqlserver_10_10_10_80`（cardioNet），enabled=true、credential_status=configured、write_policy=readonly、metadata_origin=ai_explore_20260827。
3. **审计**：asset_govern_audit_logs 一行（operator=ai-ecg-registration-20260827，登记明细+快照路径+轮换建议）。
4. **探查快照**：`/opt/data-asset/exports/ecg_explore_20260827.json`（282KB，含全部表/视图/列/PK/视图定义/行数）；本地镜像 `开发起步包/155_心电系统只读探查_结果.json`。
5. **端到端验证**：sjzc live 双源实测通过（EDSChinaDB 表数 57；cardioNet 申请表 COUNT 253,891）；sjzc systems 列表已显示 `ECG｜心电系统｜2 连接`。

## 5. 后续建议（未执行，待授权/排期）

1. **元数据导入平台**（asset_tables/asset_columns）：当前 sjzc columns 通道对 ECG 为 0 列（仅登记连接未导元数据）；可参照既有源导入管线补齐（平台写、需按惯例幂等+审计）。
2. **关系候选**：ORDER_SEQ/STUDY_SEQ 链 + cardioNet 住院号/次数↔HIS 键，交 sql-relation-intake 走候选分级（跨系统按 40 号 D 级延后口径）。
3. **sa 轮换**：与 OA admin 一并安排专用只读账号（六步框架见 verify/round-1/T6_建议文本.md ②）。
