> 类别：待办

# 下一步工作与整改交接说明

## 1. 交接目标

本文用于后续 AI 接手当前数据治理系统整改与资产治理工作。接手后不要重新无目标探库，应基于现有复核结果继续推进治理导入、质量分析、前端展示、AI SQL 安全和部署联调。

## 2. 接手前必须先读

按顺序读取：

1. `AGENTS.md`
2. `开发起步包/README.md`
3. `08_数据中心元数据快照.json`（复核修订：元数据底座）
4. `09_数据资产_表结构与关联关系.md`（复核修订：表结构权威）
5. `40_数据治理复核口径与方法记录.md`
6. `41_未完成工作与目录整理计划.md`（已复核修订版）
7. `38_secondary_manual_confirm_tables复核修订报告.md`
8. `39_secondary_relationships分级修订报告.md`
9. `29_系统功能对照复核与整改计划.md`
10. `29_复核意见.md`（复核修订：质控规则开发主入口，README 29b）
11. `29_复核意见_新增内容验证.md`（复核修订：质控规则二次验证，README 29c）
12. `35_前端数据分类与图谱展示整改计划.md`

## 3. 当前已确认状态

### 3.1 HIS_READY 资产口径

- 待确认表已按用户规则清零。
- 当前核心/保留表：`secondary_core_tables_rechecked.csv`
- 当前排除/不纳入治理范围表：`secondary_excluded_tables_rechecked.csv`
- 用户指定保留表：
  - `DRUG_USER.PUB_CONFIG`
  - `DRUG_USER.VIEW_SHOWDATA`
  - `INPBILL.INP_BILL_DETAIL_CANCEL`
  - `LAB.LAB_SHARINGCENTER_DETAIL`
  - `ORDADM.ORDERS_EXECUTE`
- `ORDERS_EXECUTE` 是医嘱执行记录表，执行一次生成一条记录，重要事实表。

### 3.2 关系口径

- 当前可用于治理导入/图谱的关系：`secondary_relationships_A_rechecked.csv`
- 原 B/C 关系已按用户确认采纳。
- D 类关系暂不纳入当前 HIS_READY 图谱，等待 EMR/LIS/PACS/护理/手麻等系统库信息后再分析。

### 3.3 当前主口径文件

- 表清洗和治理方法：`40_数据治理复核口径与方法记录.md`
- 未完成工作和目录整理：`41_未完成工作与目录整理计划.md`
- 前端层级和图谱整改：`35_前端数据分类与图谱展示整改计划.md`
- 应用功能整改：`29_系统功能对照复核与整改计划.md`

### 3.4 门诊检验/检查强键（最新验证成果，复核修订）

已验证门诊检验/检查可按 `RCPT_NO` 精确挂 `OUTP_ORDER_DESC`：

| 场景 | 比例 | 强键 | 匹配率 |
|---|---|---|---|
| 门诊检验 O_RCPT（有 RCPT_NO） | 94.1% | `PATIENT_ID + RCPT_NO -> OUTP_ORDER_DESC` | 99.998%（94795/100000，孤儿 2） |
| 门诊检查 O_RCPT（有 RCPT_NO） | 99.99% | `PATIENT_ID + RCPT_NO -> OUTP_ORDER_DESC` | 99.997%（99975/100000，孤儿 3） |
| ORDER_ID | 不可用 | 100% NULL |  |

剩余约 6% 无收据门诊检验需条件挂接：`PATIENT_ID + TRUNC(BUSINESS_DATE) + CLINIC_NO_CNT=1`。

当前状态：已回写 39 号关系分级，并已纳入 `governance_relations_formal_final.csv`；门诊 O_RCPT 强键关系按 `sample_pass` 进入正式治理关系包。

## 4. 下一步工作优先级

### P0：生成最终 HIS_READY 治理导入包

目标：基于最新 rechecked 文件生成可导入治理系统的最终包。

输入：

- `secondary_core_tables_rechecked.csv`
- `secondary_excluded_tables_rechecked.csv`
- `secondary_relationships_A_rechecked.csv`
- `secondary_relationships_D_rechecked.csv`
- `secondary_manual_confirm_recheck_summary.json`
- `secondary_relationships_recheck_summary.json`

输出建议：

- `governance_tables_final.csv`
- `governance_columns_final.csv`
- `governance_relations_formal_final.csv`
- `governance_deferred_relations_final.csv`
- `governance_import_final_summary.json`

注意：

- D 类关系只能作为后续任务，不进正式图谱。
- 表备注、字段备注缺失时写“需要人工确认”，不要编造。
- 保留原验证指标，例如覆盖率、孤儿数、样本量。

### P0：后端真实执行能力整改（已修订）

已完成（生产验证待补）：

- 数据源连通性检测：`backend/app/api/v1/systems.py` 第 174-211 行已实现真实调用 `connector.test_connectivity()` + `credentials.resolve()`，能真实连接并写回 `last_check_status/last_check_at`。待补：生产环境实测各数据源、超时配置、错误分类。
- 质量规则 SQL 执行器：`backend/app/services/quality_sql_runner.py` 已实现，调用 SQL 安全校验、凭据解析、连接器 `execute_readonly()`、结果脱敏和统计返回。
- 质量规则 `run_sql_rule`：`backend/app/services/quality_rule_engine.py` 已改为调用 `quality_sql_runner.execute_quality_sql()`。
- 后端质量测试已通过：`pytest tests/test_quality.py -q`；全量测试已通过：`pytest tests/ -q`（193 passed）。

仍需后续执行：

- 配置生产环境 HIS 数据源：`asset_data_sources` + `CRED_HIS_SOURCE`；
- 修复或确认 Oracle thick client / 跳板机链路后，执行 `POST /sources/{code}/check`，要求返回 connected；
- 在真实 HIS 源库上跑核心表质量分析，并写入 `asset_quality_findings`；
- 继续保留 SQL parser 风险扫描、操作审计和脱敏要求。

关键要求：

- 源库只读；
- 禁止 DML/DDL；
- 大表禁止无条件全扫；
- `LAB_RESULT` 必须用 `TEST_NO` 子查询或限定条件；
- CLOB/BLOB 不查全文；
- 外部 AI 只拿元数据、关系、质量摘要和脱敏样例。

### P1：前端资产展示和图谱整改

按用户要求重构展示：

- 系统 -> 库/表空间 -> 表 -> 字段；
- 尽量使用中文名，同时保留库名、Schema、英文表名；
- 核心表、排除表、延后关系分开展示；
- 图谱只使用 `secondary_relationships_A_rechecked.csv`；
- D 类关系放入“跨系统待分析任务”。

图谱建议：

- 使用 Cytoscape、AntV G6 或 ELK 布局；
- 按业务域分组；
- 节点展示中文名 + `Schema.表名`；
- 边展示关系字段、覆盖率、孤儿数、验证来源；
- 支持按系统、业务域、关系等级、表类型筛选。

### P1：质量分析任务

优先表：

- 患者主索引；
- 门诊/住院就诊；
- 诊断；
- 医嘱和医嘱执行；
- 检验申请/结果；
- 检查申请/报告；
- 费用/结算；
- 药品和处方；
- 用户指定保留表。

优先字段：

- 主键字段；
- 关联字段；
- 患者标识；
- 就诊标识；
- 时间字段；
- 科室、医生；
- 诊断/手术/检验/检查/药品/费用编码；
- 状态和值域字段。

规则：

- 空值率；
- 唯一性；
- 重复率；
- 值域分布；
- 时间范围；
- 编码是否能关联字典；
- 孤儿外键；
- 未来日期和异常日期。

### P2：数据中心二次优化

数据中心已经探索过，但尚未完全按 HIS_READY 的治理口径重新整理。

下一步：

1. 以 40 号口径为准；
2. 不重新盲扫全库；
3. 基于 `08_数据中心元数据快照.json`、ODS 视图、已有关系包；
4. 识别数据中心主题表、宽表、指标表、标准映射表；
5. 按核心/排除/延后/待确认重新出包。

### P2：跨系统关系分析

当前 D 类关系不处理，等待用户提供其他系统数据库后再做。

待接入系统：

- EMR/JHEMR/MTL；
- LIS；
- PACS；
- 护理/YDHL；
- 手麻/SM；
- 其他医院业务系统。

接入后必须做：

- 元数据采集；
- 表清洗；
- 候选主键分析；
- 覆盖率验证；
- 行数放大验证；
- A/B/C/D 重新分级；
- 再进入正式图谱。

## 5. 文档整理任务

当前归档已按 `41_未完成工作与目录整理计划.md`（已复核修订版）执行完成；后续如新增归档任务，继续按 41 号规则执行（复核修订）：

1. 报告 `.md` 与配套 `_结果.json` 成对迁移，不可只迁报告；
2. 24/26 在 `_archive/completed_plan_snapshots_20260704/` 已有副本，移动主目录原件覆盖，不二次复制；
3. 32 号暂不归档，待治理导入包重生成后归档；
4. 只迁不删，不删除任何文件；
5. 归档后同步更新 README 资产清单、阅读顺序和主入口文档引用。

详见 41 号第 3.2 节归档规则和第 4 节执行顺序。

## 6. 禁止事项

- 不要重新无目标扫描全库；
- 不要把字段名相同直接当作关联；
- 不要把 D 类跨系统关系放进当前正式 ER 图；
- 不要编造备注、中文名、来源章节；
- 不要对大表执行 `SELECT *`；
- 不要查询 CLOB/BLOB 全文；
- 不要把外部 AI 直接接入明细数据；
- 不要删除旧文档，只能归档迁移。

## 7. 执行前环境检查与验收命令（已修订）

后端依赖完整性检查（当前 `.venv` 缺失以下包）：

```powershell
cd F:\python\数据资产\backend
.\.venv\Scripts\python.exe -m pip install apscheduler oracledb
```

- `apscheduler`：定时采集调度依赖，缺失时 `main.py` 会 warning 但不阻止启动；
- `oracledb`：Oracle 连接器依赖，缺失时 `OracleConnector.connect()` 会报错。

确认依赖完整后再执行验收命令。

后端：

```powershell
cd F:\python\数据资产\backend
.\.venv\Scripts\python.exe -m pytest tests/ -q
.\.venv\Scripts\python.exe -m alembic upgrade head
```

前端：

```powershell
cd F:\python\数据资产\frontend
pnpm run typecheck
pnpm run build
```

## 8. 建议交付顺序

可并行阶段（互不依赖，复核修订）：

- 【A】文档归档（按 41 号修订版执行，纯文档操作；当前已完成，后续新增归档继续沿用该规则）；
- 【B】重生成 HIS_READY 最终治理导入包（已完成，输出 `governance_*_final.csv/json`）；
- 【C】后端整改（已完成质量执行器真实执行和缺失依赖补齐；生产源库连通验证待补）。

依赖阶段：

- 【D】前端接入系统/库/表/字段层级和 A_rechecked 图谱（已完成基础整改，后续可继续做交互细化）；
- 【E】跑核心表质量分析（待 HIS 源库真实连通后执行）。

后续阶段：

- 【F】回写门诊强键到 39 号关系分级（已完成）；
- 【G】数据中心按 HIS_READY 口径二次优化；
- 【H】等用户提供其他系统数据库后处理 D 类跨系统关系。

建议：先并行 A+B+C，完成后再做 D+E，最后做 F+G+H。
