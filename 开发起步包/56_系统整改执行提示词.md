> 类别：待办 / 交付提示词
> 状态：已过时，仅作参考；执行请使用 `57_线性整改执行提示词.md`，实际未完成项以 `55_系统未完成事项统一执行计划.md` 为准
> 生成日期：2026-07-11
> 配套文件：`55_系统未完成事项统一执行计划.md`（已复核修订，是执行依据）

# 系统整改执行提示词（交付给执行 AI）

你是资深安全 + 全栈工程师，接手医院数据资产治理平台（`F:\python\数据资产`，FastAPI + Vue3 + PostgreSQL + Alembic）的整改。本提示词是单一执行入口，配套 `开发起步包/55_系统未完成事项统一执行计划.md`（已逐条复核源码，证据可靠）。

## 一、开工前必读（顺序不可颠倒）

1. `AGENTS.md`（仓库根）——9 条硬约束 + 验收命令 + 安全红线，**全程不可违反**。
2. `开发起步包/README.md`——唯一权威目录。
3. `开发起步包/55_系统未完成事项统一执行计划.md`——唯一未完成事项执行计划（已复核）。
4. 按任务需要再读 51/52/53/54/50 号模块文档（均在 `开发起步包/`）。
5. **不要读 `46_文档完成状态总表.md`**——已归档且物理不存在，是幽灵引用。

## 二、硬约束（违反即回滚，来自 AGENTS.md）

1. **源库只读**：HIS/ODS/HRP 业务源库只 `SELECT`，禁 DML/DDL；平台库写操作必须经审批+审计+白名单。
2. **凭据脱敏**：密码/Token/身份证/姓名/电话/地址不进代码、日志、git。
3. **单一 schema**：新表只在 `asset` schema，前缀 `asset_<模块>_<实体>`。
4. **Alembic 手写**：迁移必须手写 `upgrade()`/`downgrade()`，禁 `--autogenerate`。
5. **AI 不执行写操作**。
6. **大表禁全扫**：`HIS.LAB_RESULT` 等必须 `ROWNUM <= N`。
7. **保留旧表**：`asset_api_keys`/`asset_table_owners`/`asset_business_terms`/`asset_metadata_snapshots` 不删。
8. **前端不改现有路径**：`/relations/{id}/review`、`/dict-medical/*` 等保留。
9. **前端必须用 pnpm**（有 `preinstall: only-allow pnpm`）；typecheck 同时过 `tsc --noEmit` 和 `vue-tsc --noEmit`；API 层按模块在 `src/api/*.ts`，优先扩展不新建。

## 三、执行顺序（严格遵守，不得跳级）

> 里程碑：完成阶段 1+2 = 可审计的安全发布基线。在此之前**禁止**恢复 HIS 同步/图谱/质量新功能开发。

### 阶段 1：P0 安全止血（逐项独立提交，见 55 号 §4）

**P0-1 固化 Git 基线**（55 号 §4.1）
- `git status` 审阅全量改动，grep 清除 `admin123`/`asset-platform-admin-2026`。
- 按后端/前端/迁移/文档分组提交，记录基线 SHA 到 README。
- 验收：`git status` clean。

**P0-2 移除公开/硬编码凭据**（55 号 §4.2）
- 证据已核实：`backend/app/api/v1/admin.py:15,30-38`、`main.py:100`、`frontend/src/utils/http/index.ts:17,76-82,146-153`、`config.py:7`、`.env.example`。
- 删除 `/api/v1/admin/init` 公开路由 + `DEFAULT_TOKEN`；新增 `backend/scripts/create_admin_token.py`（随机 `secrets.token_urlsafe(32)`，仅打印一次）。
- 删除前端 `DEV_ASSET_API_TOKEN` 及 403 自动回填；Token 仅来自登录态或 `.env.local` 的 `VITE_DEV_API_TOKEN`（仅 DEV）。
- `config.py:7` `db_url` 去默认值，启动缺 `APP_DB_URL` 直接 raise；`.env.example` 改占位符。
- **人工配合**：`admin123` 与固定 Token 已进 git 历史，必须由 DBA/运维轮换——代码整改无法撤销已发生泄露。

**P0-3 可信身份与 RBAC**（55 号 §4.3）
- 证据已核实：`config.py:31-32` `rbac_require_bound_token=False`、`main.py:139-154` 未绑定返回 None 放行、`ops_tools.py:119-138,223-260` 操作人字段客户端可伪造。
- 生产默认强制 `rbac_require_bound_token=True`（或启动时 `APP_ENV=prod` 且为 False 则拒绝启动）。
- `_lookup_roles` 未绑定一律返回 `[]`（拒绝）。
- 中间件把 `user_identifier` 挂 `request.state`；ops_tools 的 operator 全部服务端派生，忽略客户端字段；申请人/审批人/执行人职责分离校验。
- Token 改哈希存储 + 一次性显示 + 轮换/禁用；配 Alembic 迁移（加 `token_hash` 列→回填→删明文）。
- 新增 `backend/scripts/bind_token.py`。

**P0-4 测试隔离**（55 号 §4.4）
- 证据已核实：`backend/tests/conftest.py:20-28` 直连默认库建 Token，`:33-54` 对真实库 delete/setval。
- 强制 `APP_TEST_DB_URL`，库名必须含 `test`；每条用例事务回滚；无测试库时安全拒绝运行。

### 阶段 2：P0 可复现离线部署（55 号 §5）
- 证据已核实：`deploy/nginx.conf:17-25` 反射任意 Origin。
- 删 Nginx CORS 头，CORS 统一由 FastAPI 白名单管理。
- 写 `deploy/offline/README.md`（wheelhouse + pnpm 离线 store + asset schema 初始化 SQL + `alembic upgrade head`）。
- systemd 非 root（`deploy/systemd/data-asset.service`，`User=dataasset`）。
- 干净 VM/容器演练一次。

### 阶段 3：P1 核心功能契约（55 号 §6）
- **P1-1 关系复核**（§6.1，比预期严重）：后端 `relations.py:107-144` 参数名是 `review_status`、返回 `{...items}`；前端 `relation-review/index.vue:96-103` 却传 `validation_status` 且读 `res.data` 当数组；`:201-203` 标题乱码。统一参数与响应、加 `system_code` 过滤、修乱码、加类型定义。
- **P1-2 元数据对比**（§6.2）：后端 `metadata_changes.py:339-352` 用 `snapshot_id_from/to`；前端 `metadata-changes/diff/index.vue:160-163` 传 `from_id/to_id`。前端改名对齐。
- **P1-3 字典路由**（§6.2）：`router/modules/dict.ts:15-18` 错指向 mappings，改为 `@/views/dict/medical/index.vue`。
- **P1-4 质量看板/规则 DTO**（§6.2）：核对 `quality.py` 返回结构，统一分页 `{total,page,page_size,items}`。
- **P1-5 数据源表单**（§6.2）：前端补 `source_name_cn` 必填。
- **P1-6 只读 SQL 加固**（§6.3）：DB 级只读事务 + 超时 + 方言校验，拒 DML/DDL/SELECT INTO/COPY。
- **P1-7 调度真实性**（§6.3）：`main.py:54-72` 伪成功，改真实调用采集服务 + cron 校验 + 失败写 failed。

### 阶段 4：P1 质量/人员/生产验证（55 号 §7）
- 质量状态统一（`QualityFinding` 的 `status` 与 `rectification_status` 合并，配迁移）。
- HIS/HRP 人员同步先 dry-run 再审批 upsert；HRP 按 52 号补 `WA_*` 8 张核心表字段。

### 阶段 5：P2（55 号 §8 §9，基线稳定后）
- ODS/HIS/HRP 资产归一（51 号方案，D 类关系进 `cross_system_pending` 图层）。
- 关系口径库（54 号方案，导入 `数据资产_关系图谱/view_relation_recipes.json`，只读版）。

## 四、每项任务的完成标准（缺一不算完成）

1. **代码改动**：给出修改前/后片段与 `文件:行号`。
2. **验收命令实测**（不得把超时/失败写成通过）：
   - 后端：`cd backend && .\.venv\Scripts\python.exe -m pytest tests/ -q` + `.\.venv\Scripts\python.exe -m alembic upgrade head`
   - 前端：`cd frontend && pnpm run typecheck` + `pnpm run build`
3. **迁移**：涉及结构变更配手写 Alembic 迁移。
4. **测试**：每项至少一个针对性测试（pytest 或 vitest），不连生产库。
5. **文档**：更新 `README.md` 目录更新记录；在本文件"执行日志"追加：序号、改动文件、迁移名、测试命令与结果、commit SHA、遗留问题。

## 五、认领与并发协调（取代已归档的 46 号总表）

- 认领任务前，在 55 号文件对应小节标注"负责 AI/人 + 日期"。
- 完成后立即更新该小节状态行 + README 目录更新记录。
- **不要创建新的状态总表**，README + 55 号是唯二入口。

## 六、需人工配合（代码无法替代）的事项

| 事项 | 原因 | 负责方 |
|---|---|---|
| 轮换数据库密码 `admin123` | 已进 git 历史 | DBA |
| 轮换固定 Token `asset-platform-admin-2026` | 已进 git 历史 | 运维 |
| 预置 SSH `known_hosts`（P1-9） | 信任链由运维管理 | 运维 |
| 创建 `asset` schema 并授权（P0-8） | DDL 需 DBA | DBA |
| 生产打开 `APP_OPS_WRITE_*` 前的人工批准 | 三重门禁 | 用户 |

---

## 执行日志（执行 AI 逐项追加）

```
### [P0-1] 2026-07-XX
- 负责 AI：xxx
- 改动文件：...
- commit: <sha>
- 验收命令与结果：git status clean / pytest ... / typecheck 过 / build 成功
- 遗留：...

### [P0-2] 2026-07-XX
- ...
```
