# 前置机标准化表清单 与 数据中心源库

> 精简版（2026-07-02）：仅保留**表结构**与**数据中心源库说明**，删除 DataX 迁移策略、视图字段对照、三甲实施步骤等内容。
> 连接信息、HIS 源表字段详见 `02_开发环境与资产总索引.md` §1、§3。

---

## 一、前置机 10.10.8.74 — DBOPER 标准化层表清单（87 张）

> 这些是按《电子病历数据集 V1.3》对齐的**标准化目标表**结构，作为字段对齐与关系建模参考。源库探查阶段非必须，但其中主/子表关联约定（`*_ID → 主表.ID`）可直接复用。

### 入院 / 出院类
| 表名 | 字段数 | 说明 |
|------|--------|------|
| `EMR_ADMISSION_RECORD` | 56 | 入院记录（含 ID/NAME/SEX_CODE/BORN_DATE/AGE） |
| `EMR_ADMISSION_RECORD_EXAM` | 12 | 入院记录-体格检查 |
| `EMR_ADMISSION_DIAGNOSIS` | 22 | 入院记录-诊断 |
| `EMR_DISCHARGE_RECORD` | 47 | 出院记录（含住院天数/离院情况） |
| `EMR_DISCHARGE_RECORD_DIAGNOSIS` | 23 | 出院记录-诊断（含中西医诊断标记） |

### 医嘱类
| 表名 | 字段数 | 说明 |
|------|--------|------|
| `EMR_DOC_ORDER_DRUG` | 62 | 住院医嘱-药品类（含抗菌药物分级） |
| `EMR_DOC_ORDER_CHECK` | 33 | 住院医嘱-非药品类（检查/检验） |

### 门诊类
| 表名 | 字段数 | 说明 |
|------|--------|------|
| `EMR_OUTPATIENT_RECIPE` | 46 | 门诊处方主表 |
| `EMR_OUTPATIENT_RECIPE_ITEM` | 72 | 门诊处方明细（含药品费率/用药途径） |

### 手术麻醉类
| 表名 | 说明 |
|------|------|
| `EMR_OPERA_RECORD` | 手术记录 |
| `EMR_BOFORE_OPERA_RECORD` | 术前记录 |
| `EMR_AFTER_OPERA_RECORD` | 术后记录 |
| `EMR_ANESTHESIA_RECORD` | 麻醉记录 |
| `EMR_ANES_RECORD_BLOOD` | 麻醉记录-输血 |
| `EMR_ANES_RECORD_MED` | 麻醉记录-用药 |
| `EMR_OPERATION_NURSING` | 手术护理 |

### 输血类
`EMR_BLOOD_APPLY_REPORT` 输血申请报告、`_ITEM` 明细、`EMR_BLOOD_DESTROY_RECORD` 血袋销毁、`EMR_BLOOD_DIALY_DETAIL/ORDER/RECORD` 血液透析三表、`EMR_TRANSFUSION_RECORD` 输血记录。

### 护理评估类
`EMR_NURSING_ASSESSMENT` 护理评估、`EMR_NURSE_PLAN` 护理计划、`EMR_GENERAL_NURSING` 一般护理、`EMR_CRITICALLY_ILL_NURS(_21-24)` 危重护理、`EMR_VITAL_SIGN` 生命体征、`EMR_PHYSICAL_EXAM(_ITEM)` 体格检查、`EMR_PHYSICAL_REPORT` 体检报告。

### 告知文书类
`EMR_INFOR_ANESTHESIA/CRITICALLY_ILL/OPERATION/OTHERS/SPEC_EXAM_TREAT/TRANS_BLOOD` 各类知情同意。

### 费用类
| 表名 | 说明 |
|------|------|
| `EMR_IN_FEE_INFO_MAIN` / `_CHILD` | 住院费用主表 / 明细 |
| `EMR_OUT_FEE_INFO_MAIN` / `_CHILD` | 门急诊费用主表 / 明细 |
| `EMR_IN_OUT_AMOUNT(_BK)` | 住院出入量（含备份） |

### 其他（病案首页 / 检查 / 门诊 / 评估 / 记录）
- **病案首页系**：`EMR_ICH`（主表）+ `EMR_ICH_ALLERGIES/CHARGE/DIAGNOSIS/OPERATION`（子表，`*_ID → EMR_ICH.ID`）、`EMR_MEDICAL_RECORD_INFO`。
- **检查/影像**：`EMR_INSPECTION` 申请主表、`EMR_INSPECTION_ITEM` 项目、`EMR_INSPECTION_IMAGE` 影像、`EMR_EXAM_IMAGE_PATH/REP` 影像路径与报告。
- **门诊**：`EMR_OUTPATIENT_RECORD` 门急诊记录、`EMR_OUTPATIENT_DIAG(_NOSIS)` 门急诊诊断。
- **评估/记录**：`EMR_ADMISSION_ASSESSMENT/DISCHARGE_ASSESSMENT` 入出院评估、`EMR_HOS_CONSUL_RECORD` 会诊、`EMR_HOS_DND_RECORD` 死亡抢救、`EMR_DEATH_RECORD` 死亡、`EMR_REFERRAL_HOS_RECORD` 转院、`EMR_CHILDBIRTH` 分娩、`EMR_CONSUMABLES` 耗材。

### 主/子表关联约定（关系建模可直接复用）
```
病案首页系：  EMR_ICH_OPERATION/ALLERGIES/CHARGE/DIAGNOSIS.*_ID → EMR_ICH.ID
出院系：      EMR_DISCHARGE_RECORD_DIAGNOSIS.DR_ID            → EMR_DISCHARGE_RECORD.ID
门诊处方：    EMR_OUTPATIENT_RECIPE_ITEM.OR_ID                → EMR_OUTPATIENT_RECIPE.ID
住院系：      ADMISSION_ID（部分表为 ADMISSION_UN_NUM）
门诊系：      OUTPATIENT_NUM → EMR_OUTPATIENT_RECORD
```

---

## 二、数据中心备份库 10.10.8.216（探库主目标·源库）

| 项目 | 值 |
|------|-----|
| 地址 | `10.10.8.216:1521/orcl` |
| 用户 | `ods`（只读） |
| 网络 | ✅ 可达（ping <1ms，1521 端口通） |
| oracledb thin | ❌ `DPY-3010` 版本过旧 → 必须 thick 模式 + instantclient_21 |
| 定位 | 数据中心 ETL 汇聚备份库，业务系统数据已同步至此 |

**数据布局推断**（基于现有视图 SQL）：
- 源数据表在 `ods` 用户下（HIS 原始表 + `V_EMR_*` 汇聚视图）。
- 字典映射表在 `cDA` 用户下（`CDA_DICTIONARY`：院内码 ↔ 国标码），被所有 `V_EMR_*` 视图依赖。
- 视图目标字段与上述前置机标准化表高度一致 → 前置机表清单可作为字段对齐基准。

> 源库 HIS 各表字段细节、表规模、已知数据坑见 `02` §2.1 / §3 / §5。

---

*原报告 2026-06-02；精简 2026-07-02。*
