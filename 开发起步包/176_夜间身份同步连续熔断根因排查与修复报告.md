> 类别：证据链
>
> 状态：排查完成（2026-09-01 排查，2026-09-02 补充 09-02/09-03 两晚佐证）；根因已锁定，**修复需用户拍板**（生产 env 调整+容器重建）

# 夜间身份同步连续熔断根因排查与修复报告（176）

## 0. 现象

工号 004057 的 JHEMR 签名为空，未被夜间任务补上。查审计发现：**09-01/09-02/09-03 连续三晚的签名子任务全部 skipped**，reason=`main_account_sync_not_successful`——主账号同步失败时签名/职称子任务按设计连带跳过。

## 1. 根因（证据链）

| # | 事实 | 证据 |
|---|---|---|
| 1 | 三晚主任务全部 `status=failed`，熔断维度=**max_update**，reason=threshold_exceeded | `asset_identity_scheduler_runs`：RUN-ea37dd9ce962(09-01)/RUN-4ecb56230848(09-02)/RUN-2309f671219e(09-03) 三行 circuit_breaker_triggered=true |
| 2 | 三晚候选规模恒定：candidates_total=**110**、deactivate=3、new=0；**success_count=0**（熔断在预检阶段中止，未落任何账号动作，02:00 窗口 actions=0） | 同上三行 + asset_identity_sync_actions 02:00 窗口计数=0 |
| 3 | 熔断判定：`stats["update"] > identity_cb_max_update(默认 100)` 即触发 | `identity_sync_orchestrator.py:493`；`config.py:96` |
| 4 | update 分类口径：候选人在 managed_relations(active) 中即计 update（托管在册的例行状态/科室 resync） | `identity_sync_orchestrator.py:436-440` |
| 5 | 托管圈实际规模 ≈110 人（医护药技托管圈） | candidates_total=110 与 55 号"112 人对齐"事件吻合 |

**结论：阈值-规模结构性错配。** 托管圈约 110 名在册账号每晚全部落入 update 桶（例行 resync），而 `identity_cb_max_update=100`——**110>100 每晚必然熔断**。这不是数据异常，是 W10（08-29）拆分 max_new/max_align 维度后，**max_update 维度未随托管圈规模调整**的遗留：W10 修好了 112 人对齐事件（max_new/max_align），但正式托管后的 110 人随后全部落进 update 桶，从 09-01 起连续熔断。

## 2. 影响面

- 主同步中止 → 签名、职称两个子任务每晚跳过 → **所有空签名/职称差异账号无人补齐**（004057 只是浮出水面的一个）；
- deactivate=3（3 个停用候选）连带被压制；
- 09-02 18:26 有一窗 NTL-RUN-f2513dc373c1 逐账号批全部 success（另一触发窗口/登录模板批），不影响上述结论。

## 3. 已完成的应急处理（本排查会话内，用户授权）

004057 单账号补跑已执行成功：经 8.83 data-asset-api 容器调平台既有 `sync_missing_jhemr_signatures(emp_no=...)`（DIRECT_CONNECTION 直连 JHEMR），HIS 签名→规范化（JPEG 150×63, 4107B）→写入 `jhemr.users_pic`，回读验证通过（use_flag=1）；单账号补跑不推进夜间水位，后续夜间任务按幂等跳过。注意：该直接调用不产生 `asset_identity_sync_actions` 审计行（审计行由编排器路径写入），本次写入凭证以本报告与容器执行日志为准。

## 4. 修复方案（需用户拍板，任选其一或组合）

| # | 方案 | 动作 | 评估 |
|---|---|---|---|
| F-1（推荐，最小） | 调高 update 阈值：env `APP_IDENTITY_CB_MAX_UPDATE=150`（≥托管圈规模并留余量） | 生产 env 修改+同镜像容器重建（同 W3/W10 模式，需授权） | 立即解除熔断；首晚将执行 110 人例行 resync（additive 对齐，08-28 的 112 人对齐已实证安全） |
| F-2（更正确，改代码） | 语义优化：托管在册账号的例行 resync 不计入 max_update（比照 W10 对 align_existing 的处理单列维度或按 change_ratio 0.3 判定） | 代码改动+测试+发布 | 一劳永逸，属"熔断器语义优化"，可并入下一次升级批 |
| F-3（配套） | 熔断失败时的**子任务降级策略**：主熔断时签名/职称子任务不再一刀切跳过，改为"仅跳过涉及变更账号、仍补缺" | 代码改动 | 可选增强，避免同类连带停摆 |

建议：F-1 立即做（恢复签名/职称同步），F-2 列入 55 待办池下一批。

## 5. 修复执行记录（F-1，用户授权 2026-09-03）

| 步 | 结果 |
|---|---|
| env 备份 | `/etc/data-asset/backend.env.bak-cbmaxupd-20260903` |
| env 写入 | `APP_IDENTITY_CB_MAX_UPDATE=150`（backend.env:32，幂等） |
| 容器重建 | 同镜像 `data-asset:r175-20260903` 经标准脚本 `run_data_asset_api.sh` 重建（容器 b866ffee144b） |
| 验证 | health **200**；容器内 settings 实证 **identity_cb_max_update=150**；数据库 connected |
| 预期 | 今晚 02:00 主同步 110 update < 150 不再熔断；首晚执行 110 人例行 additive resync + 3 停用候选 + 签名/职称子任务恢复（空签名账号将开始被补齐） |
| 附注 | env 中 `APP_IDENTITY_CB_MAX_NEW=130` 为 W10 临时值仍在位（本报告范围未动，建议后续随 F-2 语义优化一并回落 50） |

未做（按边界）：F-2（update 维度语义优化：例行 resync 不计入）与 F-3（主熔断时子任务降级）列 55 待办池；未手动触发全量同步（让今晚 02:00 自然验证）。

## 6. 排查过程留档

排查只读：scheduler_runs 三行 → batches 逐账号批（09-02 18:26 窗全 success）→ circuit_breaker 表（旧语义未开）→ orchestrator `check_thresholds` 与分类代码。044057→004057 修复执行与回读的容器执行日志在 8.83 宿主 shell 历史（run_id=manual-sigsync-004057-20260902）。
