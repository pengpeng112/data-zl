> 类别：执行记录
>
> 状态：证据
>
> 最近更新：2026-07-28

## 2026-07-28：诊断手术字典下发（96 号）热更

- 目标：`10.10.8.83` 容器 `data-asset-api`；发布目录 `/opt/data-asset/releases/medical-push-20260728_141155`。
- 后端：`medical_code_push.py`、`dict_medical_api.py`、`config.py` 已 `docker cp` 入 `/app`；OpenAPI 含 5 个 `/api/v1/dict-medical/push/*` 路径。
- 前端：本地 `pnpm build` 后更新 `/opt/data-asset/frontend-dist`；prev 备份 `frontend-dist.pre-medical-push-20260728_141155`。
- 验收：容器 healthy，`/health` database=connected；`push_enabled=False`（默认不写业务库）。
- 业务源库 DML/DDL：0；未开启 `APP_DICT_MEDICAL_PUSH_ENABLED`。

## 2026-07-15 Plan90 空表治理最终同步

- 8.83 保持迁移 `b4c5d6e7f8a9`；最终镜像 `data-asset:20260715-row-presence-final`，容器 healthy。
- 完整备份 `data_asset_pre_row_presence_20260715_175552.dump`（6,724,745字节，SHA-256 `53eb33742da9ade004c2208a718f1bb1c0beb3133acb9cb43161ec7d18830f52`）。
- 7,766对象只读治理：可见4,331、确认空3,435、unknown/blocked 508、空表字段残留0；业务源写0。
- 隔离测试库全量 pytest `321 passed / 2 skipped`；前端 typecheck/Vitest43/build通过。

# 登录上线与本地/服务器同步记录

## 2026-07-15：90 号一级系统命名与目录治理发布

- 生产 Alembic：`a3b4c5d6e7f8` → `b4c5d6e7f8a9`（仅 upgrade；downgrade 在 `data_asset_test` 往返验证）。
- 备份：`/opt/data-asset/backups/data_asset_pre_plan90_20260715_173117.sql`（SHA256 `800c1566…`）、`data_asset_pre_plan90_inventory_20260715_173409.json`（SHA256 `95e3cbc3…`）；前端 prev：`frontend-dist.pre-plan90-20260715_173117`。
- 系统归一：`ods_*` 别名归 `DATA_CENTER`；`EMR/LIS/PACS/SM` soft-merge；十个一级系统 active。
- 发布：`/opt/data-asset/releases/plan90-20260715_173117`；容器 `data-asset-api` healthy；Nginx 首页/health 200。
- 验收：容器内 `list_systems`/`assets_tree`/`search` ACCEPT_OK；无「其他业务系统」。
- 业务源库 DML/DDL：0。细节见 `92`。

## 2026-07-15：业务系统、资产目录与关系复核统一发布

- 生产平台库备份：`data_asset_before_catalog_unify_20260715_145117.dump`，SHA256 `857b2f07511ca8f0e9d41ed37d9196b2ea3a7c1b79956bbeee9c72ca55a8beac`。
- 平台迁移至 `a3b4c5d6e7f8`；中文名称治理回填7608项，均为平台元数据。
- 系统总览和资源树均为10个一级业务系统/10个真实连接；数据中心折叠到一个连接、30个Schema、865表。
- 统一关系复核中心可分页查询535条关系，其中候选293条；支持分类、筛选、批量审核和审计。
- 前端typecheck/build通过；后端完整测试301 passed/2 skipped，分类专项3 passed。
- 发布镜像：`data-asset:20260715-catalog-unify-final`（当前容器文件系统已同步并healthy）；业务源库写0。

## 2026-07-15：三甲复审视图关系修订与平台同步

- 本地修订：`系统表结构/三甲复审数据视图_系统与连接信息_AI分析版.md`、87号AI视图SQL对接说明、88号跨系统关系复核报告。
- 本地可复现脚本：扩展 `backend/scripts/import_explored_sources_to_platform.py` 的 `mobile_nursing` profile；新增 `backend/scripts/sync_his_review_views.py`。
- 服务器脚本同步位置：容器 `/app/scripts/`；文档副本 `/opt/data-asset/docs/review/`；86号证据副本 `/opt/data-asset/evidence/86/`。
- 写入前备份：`/opt/data-asset/backups/data_asset_before_review_views_20260715_111833.dump`，6562960 bytes，SHA256 `85b0666ae75deb8fffcf0b863a132b908cc55fb18a170d4b5ff83c2571f1a6d9`。
- HIS源资产：新增2个VALID影像视图、144字段、7条Oracle数据字典依赖；同步后HIS源资产1236对象/19975字段。
- 移动护理：新增独立物理源连接、567对象、9981字段、20条限量验证关系；只读连接测试成功。
- 生产镜像保持 `data-asset:20260715-f1-r2`，容器healthy、API数据库connected；未重建前端或镜像。
- 凭据只写服务器600权限受控文件，临时凭据已删除；业务源库DML/DDL为0。

## 2026-07-15：用户裁决关系口径同步

- 新增可复现脚本 `backend/scripts/sync_review_system_lineage.py`，默认dry-run，apply必须提供确认串；脚本只读取08/80元数据快照并写平台资产元数据。
- 写入前备份：`/opt/data-asset/backups/data_asset_before_user_lineage_20260715_115140.dump`，6693528 bytes，SHA256 `9e102a08b887ae927d57e0e2e05daa097257478e660b4c3304ff2aab696854bb`。
- dry-run核查：HRP逻辑映射1条、LIS并行实现4对、Docare 646对象与SM 25对象同名交集25条，业务源写入0。
- 平台apply：写入 `REVIEW_SYSTEM_LINEAGE` 30条；MTL 34项标记2025年前生效，JHEMR 1421项标记2025年及以后生效。
- 容器保持 `data-asset:20260715-f1-r2` 且healthy；文档同步到 `/opt/data-asset/docs/review/`，脚本及脱敏证据同步到 `/opt/data-asset/evidence/`。
- 未修改HIS影像视图字段类型，未创建/修改任何源视图，业务源库DML/DDL为0。

## 2026-07-15：78–83 号探库资产服务器同步

- 新增本地可复现脚本 `backend/scripts/import_explored_sources_to_platform.py`；服务器使用同一文件完成 dry-run 和确认式 apply。
- 写入前备份：`data_asset_pre_sources_78_83_20260715072828.dump` 与 `credentials_pre_sources_78_83_20260715072828.tar.gz`。
- 服务器平台现为 14 系统、14 数据源、7,197 对象、120,395 字段、478 关系；分来源统计见 84 号报告。
- 本次只同步平台数据和受控只读凭据，没有替换后端镜像或前端 dist；API 镜像仍为 `data-asset:20260714-plan76`，Alembic head 仍为 `y1f2a3b4c5d6`。
- 业务源库 DML/DDL 为 0；服务器临时导入文件在验收后清理。

## 1. 本次范围

- 完成 59 号 **M1–M2 登录闭环**并在 `10.10.8.83` 上线。
- 建立可重复同步脚本，使本地开发结果可推送到服务器。
- **未**对 HIS/ODS/HRP/LIS/PACS/EMR 等业务源库执行任何写操作。

## 2. 服务器运行事实（2026-07-13 实测）

| 项 | 值 |
|---|---|
| 主机 | 10.10.8.83 |
| 容器 | `data-asset-api`（host 网络，127.0.0.1:8000） |
| Nginx | 80 → 前端 dist + `/api/` 反代 |
| 平台库 | PostgreSQL `data_asset`，Alembic `t6a7b8c9d0e1` |
| 测试库 | `data_asset_test`（独立，TEMPLATE 克隆产物） |
| 登录 | `/api/v1/auth/*` 可用；login/refresh 冒烟通过 |
| 管理员 | `platform-admin`（密码由运维掌握，不写本文档） |
| RBAC 种子 | roles=9，role_permissions=64 |

## 3. 本地 ↔ 服务器同步方式

脚本：`deploy/scripts/sync_local_to_server.py`

```powershell
# 1) 前端构建
cd frontend
pnpm run build

# 2) 同步（凭据用环境变量，勿写入 Git）
$env:APP_SSH_PASSWORD = "<ssh密码>"
cd ..\backend
.\.venv\Scripts\python.exe ..\deploy\scripts\sync_local_to_server.py
```

行为：
1. 打包本地 `backend/`（排除 venv/缓存/临时脚本）
2. 上传并 `docker cp` 到容器 `/app`
3. 离线安装登录依赖 wheel（若服务器已有 wheels_login）
4. 上传 `frontend/dist` 到 `/opt/data-asset/frontend-dist` 并 reload Nginx
5. 重启容器，健康检查，写入 `/opt/data-asset/LAST_SYNC_STAMP`

## 4. 计划完成度（相对 55/58/59）

### 已完成

- 59：L1–L8、M2 登录上线、Cookie HTTP 适配、前端登录崩溃修复
- 平台库 auth 迁移与管理员
- 本地/服务器登录相关代码与前端 dist 同步机制
- RBAC 角色/权限种子（平台库）

### 部分完成

- 59 L9：前缀级默认拒绝 + 写操作角色要求；**尚未**做到纯 `asset_role_permissions` 资源动作矩阵驱动全部接口
- 55 T1/T14：独立测试库可搭，认证 pytest 可绿；全量 pytest 仍有既有空库用例失败

### 未完成（需内网窗口/人工）

- 59 L10–L16 业务源只读接入与人员 dry-run
- 55 T6 离线干净环境演练；T9–T12 生产路径 dry-run/夜间质量
- 55 T2/T13/T15、58：Git release 基线、凭据轮换、TLS、正式发布签字
- 完整 Docker 镜像重建流水线（当前为热拷代码 + 旧镜像）

## 5. 安全边界

- 业务源库：仅 SELECT，禁止 DML/DDL
- 平台库：迁移/种子/管理员仅限 `data_asset`（及测试库）
- 密码/Token：不写入 Git、文档、资产 CSV

## 6. E9–E11 本地与服务器一致性基线（2026-07-14）

### 6.1 服务器发布事实

| 项目 | 当前值 |
|---|---|
| 目标服务器 | `10.10.8.83`，经 `10.10.8.53:40022` 跳板连接 |
| 运行容器 | `data-asset-api`，状态 `healthy` |
| 当前正式镜像标签 | `data-asset:20260713` |
| 当前正式镜像 ID | `sha256:e4cd1f3c6c68368c212e22c9d0bf36c74c2040dabcbe8a36e41d96d97e3a53b4` |
| 干净同步镜像 | `data-asset:20260714-e9e11-sync` |
| 独立测试镜像 | `data-asset:20260714-e9e11-test`（包含 pytest，不用于正式运行） |
| 发布目录 | `/opt/data-asset/releases/e9e11-20260714135946` |
| 前端目录 | `/opt/data-asset/frontend-dist` |
| 平台数据库 | PostgreSQL `data_asset` / schema `asset` |
| Alembic 当前版本 | `w9d0e1f2a3b4` |
| 迁移前备份 | `/opt/data-asset/backups/data_asset_pre_e9e11_20260714135721.dump` |
| 备份 SHA-256 | `bc461a9258df27aef7c491b88d61ae715ae754bd769e5e676ed3bd3bc9aa4351` |

### 6.2 文件级 SHA-256 对账

对账范围只包含仓库受管发布文件，不包含 `.env`、凭据、日志、缓存、`__pycache__`、运行数据和服务器外部运维脚本。

| 范围 | 本地文件数 | 服务器文件数 | 哈希不同 | 仅本地 | 仅服务器 | 结论 |
|---|---:|---:|---:|---:|---:|---|
| `backend/app` + `backend/alembic` + `backend/scripts` ↔ 容器 `/app` 对应目录 | 161 | 161 | 0 | 0 | 0 | 完全一致 |
| `frontend/dist` ↔ `/opt/data-asset/frontend-dist` | 4703 | 4703 | 0 | 0 | 0 | 完全一致 |

首次对账发现服务器后端存在 117 个旧项目残留文件，原因是历史发布采用覆盖式 `docker cp`，不会删除目标端多余文件。已经通过干净发布包整体替换三个受管目录，并重新固化镜像；凭据、数据库、日志和 `/etc/data-asset/` 运维挂载未改动。

### 6.3 数据库与运行验证

- 生产平台库已依次完成 `t6a7b8c9d0e1 → u7b8c9d0e1f2 → v8c9d0e1f2a3 → w9d0e1f2a3b4`。
- `/api/v1/health` 返回 `status=ok`、`database=connected`。
- Nginx 首页、主 JavaScript 和主 CSS 返回 HTTP 200。
- 系统归并仅执行 dry-run：8 个数据源归为 HIS/HRP/DATA_CENTER，冲突 0，`applied=false`。
- 业务源库写操作为 0。

### 6.4 重要边界

服务器当前发布内容与**本地当前工作区文件**一致，但本地工作区仍有未提交改动，因此暂时不能用 Git commit SHA 唯一重建该版本。下一步必须完成敏感信息扫描、分组 commit、普通 push 和新不可变 tag；在此之前不要运行旧同步脚本覆盖服务器，也不要把 `release-20260713` 当作本次 E9–E11 文件基线。

后续同步必须遵守：先构建和测试本地代码 → 生成文件清单与 SHA-256 → 上传干净目录而非覆盖式叠加 → 数据库先备份再迁移 → 健康检查 → 再次做本地/服务器哈希对账 → 记录镜像 ID、数据库 head 和 Git SHA。

## 7. 75 号计划服务器升级记录（2026-07-14）

| 项目 | 结果 |
|---|---|
| 发布目录 | `/opt/data-asset/releases/plan75-20260714163413` |
| 正式镜像 | `data-asset:20260714-plan75-r2` |
| 平台库迁移 | `w9d0e1f2a3b4 → x0e1f2a3b4c5` |
| 生产备份 | `/opt/data-asset/backups/data_asset_pre_plan75_20260714163320.dump` |
| 备份 SHA-256 | `f7010968b627e9b34a4487dc7447b8e2c7aefdb28ad2f2ccafbbe556fe617d63` |
| 测试库 | upgrade head 成功；75 号专项 `20 passed, 2 skipped` |
| 字典导入 | 8 个体系 / 130765 项 / 90265 映射，批次 `med-import-20260714084733` |
| 归并 | dry-run：8 源 / 6 变更 / 0 冲突 / 8 缺 target_host；未 apply |
| 运维写开关 | `APP_OPS_WRITE_ENABLED=false` |
| 健康检查 | 首页 200；API database connected |

首次按 bridge 端口映射启动时因服务器 Docker iptables NAT 链缺失失败，随后沿用原生产 host 网络模式恢复；凭据卷改为持久 rw 挂载，Oracle 目录保持 ro。发布过程没有写入 HIS、ODS、HRP 等业务源库。

## 8. 76号计划服务器升级记录（2026-07-14）

| 项目 | 结果 |
|---|---|
| 发布目录 | `/opt/data-asset/releases/plan76-20260714183755` |
| 正式镜像 | `data-asset:20260714-plan76` |
| Alembic | `y1f2a3b4c5d6 (head)` |
| 生产备份 | `data_asset_pre_plan76_20260714183720.dump`；SHA-256 `0fe841189972a5f7293c08108aec90aa4f417365f9669464bad5c812b62179e6` |
| HRP资产 | 67表 / 841字段 / 1 Owner |
| 连接归一 | 3物理连接 / 5历史别名 / 3 database_key / 0缺失target_host |
| 连接实测 | 数据中心成功；HIS成功；HRP凭据安全写入后成功（约124ms） |
| 安全开关 | 运维写false；审批UI false；源库写0 |

宿主机`/opt/oracle`空挂载遮住镜像Instant Client，已从部署前镜像恢复并保持ro挂载。连接回填脚本的历史别名唯一键冲突也已修订并重新apply成功。

HRP凭据随后经受控凭据目录写入，平台仅记录脱敏用户名和configured状态；连接测试成功。凭据原文未写入本文档、代码、Git或日志。

## 9. JHEMR Vastbase 只读探查与平台导入（2026-07-14）

| 项目 | 结果 |
|---|---|
| 源端 | `10.10.8.177:5432/jhemr`，Vastbase，强制只读 |
| 元数据 | 6 Schema / 1,421 表视图 / 24,697 字段 |
| 关系 | 283 条视图 SQL 静态依赖，以 candidate/B 级导入 |
| 平台 source_code | `jhemr_vastbase_10_10_8_177` |
| 连接状态 | 凭据受控 configured；saved connection connected（约17ms） |
| 导入前备份 | `data_asset_pre_jhemr_vastbase_20260714191708.dump` |
| 备份 SHA-256 | `4b70d5526134f29064000d44d4390587b159a5d59f11c0c7b14e9cec34aa41be` |
| 健康检查 | API ok；平台 PostgreSQL connected |
| 业务源库写入 | 0 |

导入脚本已同步到发布目录 `/opt/data-asset/releases/plan76-20260714183755/` 及当前运行容器 `/app/scripts/`。首次执行因 `asset_relations.rel_id` 全局唯一冲突而事务回滚，修复为从全局最大编号后分配后重新执行成功。此次没有新 Alembic 迁移和前端变更；生产运行镜像标签保持 `data-asset:20260714-plan76`。完整证据见 77 号报告。
# 2026-07-15 F1/F6/F8 同步记录

- 本地后端/前端经隔离测试库验证：pytest `300 passed / 2 skipped`，前端 typecheck、Vitest 39、build 通过。
- 服务器平台库备份后升级至 `z2f3a4b5c6d7`；后端镜像 `data-asset:20260715-f1-r2`，前端同步自本地本轮 build 产物。
- 服务器 API/Nginx/平台数据库健康；RBAC 严格绑定为 true，运维写为 false，凭据卷和 Oracle Client 挂载有效。
- SQL Server 驱动离线加入 pymssql 2.3.11；LIS 与超声内镜保存连接只读测试成功。
- 因服务器 Docker NAT 链异常，容器使用 host 网络且仅监听 `127.0.0.1:8000`；部署脚本已同步该方式。
- 回滚证据：平台库备份 `data_asset_pre_z2_202607150915.dump`、前端副本 `frontend-dist.pre-f1`、上一镜像 `data-asset:20260714-plan76` 均保留。
- 业务源库写操作：0。
