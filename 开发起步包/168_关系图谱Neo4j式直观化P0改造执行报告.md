> 类别：应用开发执行证据

# 168 关系图谱 Neo4j 式直观化 P0 改造执行报告

> 执行日期：2026-08-29～2026-08-30  
> 依据：`review/round-5/方案_v2.md`、`分歧表.md`、`决策记录.md`  
> 边界：本地代码与测试；未部署、未连接业务源库、未执行 Git 写操作。

## 1. 结论

用户已确认 P0-9 桌面端线框。本轮完成 P0 代码实现和前端全量门禁；后端数据库型 pytest 与真实浏览器截图仍待具备合法隔离测试库、部署同一构建后补验，因此当前状态为“代码完成，待隔离库与浏览器终验”，不得记作生产完成。

决策点 A 继续采用交互对标，不引入 Neo4j 存储。决策点 B 已闭合为使用 G6 核心包自带官方 Minimap，列入 P1，零新增依赖。

## 2. 已实现

- P0-1：`explore` 默认 `force`；“知识图谱”绑定 G6 force；`layered` 更名“分层视图”并限制为 path；SVG 明确作为无动画降级引擎。
- P0-2：跨模式统一搜索；已加载节点聚焦、高亮，未加载节点通过既有 `/graph/neighbors` 增量合并；保留同名候选消歧。
- P0-3：explore 双击/Enter 展开；250ms 单击竞争取消；引用计数折叠，保护共享邻域、选中节点与路径节点；失败保留现有画布。
- P0-4：既有邻域端点扩展为 depth 1～3（默认 2）、`include`、节点入/出度、展示数/实际数和继续游标；中心参数使用五段 `center_physical_key`。
- P0-5：节点/边抽屉替换为不遮画布的固定 Inspector；边详情仅返回 SHA-256 与经 `sanitize_text` 处理的 200 字摘要，原始关联条件不出详情响应。
- P0-6：关系类型、A/B/C/D、待审层三组可点击芯片；修正依赖类型为后端真实值 `dependency`；保持 129 配色并按置信度调整透明度。
- P0-7：`graph.py` 路由级统一强制 `asset.graph.view`，覆盖全部 `/api/v1/graph/*` 端点。
- P0-8：保留模式切换；布局收进“显示”；高级筛选进入右侧抽屉；画布目标高度调整为 `calc(100vh - 280px)`。

## 3. 验收证据

- 前端 `pnpm run typecheck`：PASS（`tsc --noEmit` + `vue-tsc`）。
- 前端全量 Vitest：29 files / 202 tests PASS；其中新增 `graphNeo4jP0.test.ts` 8 tests。
- 前端生产 build：PASS；gzip 预算 PASS：main 656.2/700 KB、graph 404.6/430 KB、CSS 99.2/110 KB。
- 后端模块 `py_compile`：PASS。
- 后端无数据库专项契约：3 passed；`tests/unit_standalone`：17 passed。
- `git diff --check`：无空白错误；仅报告工作区既有 CRLF→LF 提示。

## 4. 尚未完成的终验

1. 本机无 `APP_TEST_DB_URL`，仓库安全门禁按设计拒绝数据库型 pytest；不得改用业务库。待提供合法隔离测试库后执行 `pytest tests/test_graph*.py -q` 与全量 `pytest tests/ -q`。
2. 新构建尚未部署，无法取得真实 API 数据下的五类截图。待同一 SHA 部署到验收环境后补：布局修正前后、搜索聚焦、增量展开、Inspector、图例各 1 张，并验证 401/403/200。
3. 性能 P95、首见时间和布局稳定时间须在方案指定 Chrome、机型与固定数据种子下采集；本轮不以单元测试冒充浏览器性能实测。
4. P1 Minimap、视口历史、path 走查、LOD、review 队列和跨系统快捷层未进入本轮代码。

## 5. 安全与工作区说明

- 数据库写入 0、业务源库连接 0、服务器变更 0、Git commit/push/tag 0。
- 工作区原有大量未提交改动；本轮保留并叠加 `AdvancedRelationGraph.vue`、`GraphToolbar.vue`、`RelationGraph.vue` 的既有 146 阶段改动，未回滚其他协作者成果。
- 未新增前端依赖，未新增图 API 路径，未引入 Neo4j。
