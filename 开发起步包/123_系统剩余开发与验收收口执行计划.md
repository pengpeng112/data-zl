> 类别：模块整改执行计划
>
> 状态：执行中（R0–R7/R9/G8 完成；G7 全绿；R8 离线 BLOCKED；F10 apply/历史补跑/Git 分组仍待授权） | 优先级：P0/P1 | 制定日期：2026-08-09 | 执行日期：2026-08-10
>
> 当前唯一专项入口：本计划承接 `55` 当前剩余的 85-F2/F4/F5/F7/F9/F10、121-G7/G8、122-I8 后续观察和最新迁移测试；`55_系统未完成事项统一执行计划.md` 仍是系统唯一总入口。
>
> 用户裁决：本轮不新增人工申请、人工复核、人工审批、驳回、自审、审批队列、审批角色和审批页面。已有相关代码只做兼容保护，不扩建、不作为本轮验收门槛；后续是否启用另行决定。

# 123 系统剩余开发与验收收口执行计划

## 1. 计划目标

本计划只处理当前真实剩余项，目标是把系统从“主要功能已开发、部分专项已发布”收口到：

1. 最新代码和迁移在合法隔离测试库中全量通过；
2. 基础访问控制、异常脱敏和写路由安全门禁可自动验证；
3. 关系图谱按 121 的真实页面口径完成浏览器验收；
4. 每日人员同步在 122 单账号成功基础上完成状态观察，不掩盖子任务失败；
5. 待核关系保留证据等级，不伪造强关系；
6. 历史系统归并只操作平台库并实现可回滚；
7. 离线包、断网部署和 Git 工作区收口可复现；
8. README、55、121、122、123 的状态一致，不再让旧计划成为并行入口。

本轮不重新开发已完成的探库、资产导入、E9–E11 主体页面、F1/F3/F6/F8/F11 历史功能，不重新扫描 HIS/ODS/周边系统大表。

## 2. 用户裁决后的范围收敛

### 2.1 本轮明确执行

| 当前来源 | 本轮处理 | 完成定义 |
|---|---|---|
| 85-F2 | 基础角色和资源访问矩阵，不含人工审批流程 | 匿名、只读用户、业务操作员、管理员和越权场景自动测试通过 |
| 85-F4 / 121-G7 | 真实数据浏览器验收 | 系统层级、表字段、图谱三模式、同名表消歧、1/2跳、详情抽屉和刷新竞态全部通过 |
| 85-F5 | Git 和工作区收口 | 功能文件分组、秘密扫描、提交边界清晰；普通 push/tag 仅按现有授权和远端状态执行 |
| 85-F7 | 离线包与断网演练 | 同架构 Linux 离线安装、迁移、启动、回滚、二次安装通过 |
| 85-F9 | 待核关系证据收口 | 限量只读验证或继续保持 candidate/deferred，不自动升正式关系 |
| 85-F10 | 历史系统归并 | dry-run、备份、数量守恒、平台 apply 和页面复核；正式 apply 需单独生产授权 |
| 118-U5/U6/U7/U9/U10 | 写路由权限扫描、旧 JWT 撤权、异常脱敏、SSH known_hosts、前端全量门禁 | 自动化测试失败关闭，不依赖人工逐页审核 |
| 121-G8 | 图谱生产发布准备 | 形成不可变产物、manifest、回滚点和发布命令；未获授权时停在门前 |
| 122-S10/S11 | 最新迁移/全量测试、每日同步连续观察 | 最新 head 的隔离测试通过；单账号成功事实保留；历史补跑另行授权 |

### 2.2 本轮跳过，不开发

以下内容统一标记 `DEFERRED_BY_USER`，不得因旧计划出现“申请人/审核员/审批”字样而继续实现：

- 新增人工申请、审批、驳回、撤回、转审、自审禁止等状态机；
- 新增人工审批角色、审批队列、待办中心、审批通知、审批统计；
- 为诊断/手术字典增加人工审核后自动下发流程；
- 为关系配方增加人工审核、人工激活、人工停用闭环；
- 为视图生成增加人工审批执行页面；
- 把人工审核完成作为本轮验收必要条件；
- 因缺少人工审核人而阻塞本轮其他自动化开发和验收。

处理原则：

1. 已存在审批代码、表和 API 不删除、不破坏兼容，只保持默认关闭或不可从本轮入口触发；
2. 关系配方本轮只验证 CRUD、版本、预览、复制、导出和危险 SQL 拒绝，不执行目标库 DDL；
3. 诊断/手术本轮只保留平台维护、导入 dry-run、冲突检查和只读预检，业务库下发开关保持关闭；
4. 生产发布、数据库迁移、权限变更、历史补跑等外部授权是安全边界，不属于产品“人工审批功能”，不能用本裁决绕过。

### 2.3 延后可选

- Neo4j PoC：继续暂停，不作为当前封板阻塞；完成隔离测试库和 100-S0 验收后再单独立项。
- 自动建视图：仅保留 SQL 生成/预览/下载，目标库执行延后。
- 诊断手术双目标自动下发：由于人工审批链本轮跳过，正式 apply、serial_no 方案和业务库写入一并延后。
- 历史 16 条签名失败补跑：保留为独立生产授权项，不随代码开发自动执行。

## 3. 当前基线

### 3.1 已完成，不重复

- 85-F1/F11 历史全量测试和迁移往返已有通过证据；但 121/122 新增代码与 `d1e2f3a4b5c6` 仍需在最新隔离库重跑。
- 85-F3 配方主体功能、F6 生产 RBAC/挂载、F8 SQL Server 兼容已完成。
- 121 G1–G6 已完成：物理键/元数据富化、服务端 overview/filter/search、三模式、中心 physical_key、1/2跳、方向语义、节点可读性和 SVG 降级。
- 122 I0–I7 已完成；生产已升级到 `d1e2f3a4b5c6`，provider=`host_cron`，两条 watermark 已 seed。
- 122 单账号签名重试已成功：写入 1、回读成功、action/subtask success、告警 0。
- F9 以外的既有关系证据和 77–86 探库资产不重采。

### 3.2 当前阻断

- 本机无合法独立 `APP_TEST_DB_URL`，最新后端全量 pytest 和最新迁移往返尚未完成；
- 121 完整认证页面 G7 未在当前新界面上完成真实 1440×900 验收；
- 工作区包含大量用户/协作者修改，不能 reset、checkout 或批量覆盖；
- 干净 Linux 构建机/断网环境尚未确认；
- F10 正式平台 apply 和 121 生产发布需分别授权；
- 122 历史补跑未授权，连续 3 夜观察未完成。

## 4. 强制安全边界

1. HIS、ODS、HRP、CDMS、JHEMR、Docare、移动护理、LIS、PACS、超声内镜等业务库默认只读；F9 仅限量 `SELECT`。
2. 禁止重新扫描 `HIS.LAB_RESULT` 等超大表；只读验证必须按键、时间窗、`ROWNUM`/`TOP`/`LIMIT` 限量。
3. 不得使用生产平台库替代 `APP_TEST_DB_URL`；没有隔离测试库就标记 `BLOCKED`。
4. Alembic 必须手写 upgrade/downgrade；生产库禁止 downgrade。
5. 不新增或启用人工审批功能；不删除已有审批表/接口破坏兼容。
6. 诊断手术字典、视图生成和运维业务库写开关保持关闭。
7. 未经单独授权不得：生产发布、平台正式 apply、历史签名补跑、调度变更、业务库权限变更、Neo4j 部署、Git force-push。
8. 不输出姓名、完整工号、密码、Token、连接串、签名内容、患者信息或长 ID 数组；人员仅用稳定 HMAC 短指纹。
9. 不删除测试、不放宽断言、不吞异常、不把失败改为跳过来制造绿色结果。
10. 保存现有 dirty worktree；临时文件必须明确识别，不能删除未知来源文件。

## 5. 强制执行顺序

### R0 启动、自检与基线冻结

必须完整读取：

- `AGENTS.md`
- `开发起步包/README.md`
- `开发起步包/55_系统未完成事项统一执行计划.md`
- `开发起步包/85_系统最终封板与全量验收详细执行计划.md`
- `开发起步包/111_当前代码全面核查问题整改执行计划.md`
- `开发起步包/118_111与112独立复核修复与待处理交接.md`
- `开发起步包/121_关系图谱知识图谱式重构执行修复计划.md`
- `开发起步包/122_每日人员同步任务状态审计与签名失败整改执行计划.md`
- 本计划 `123_系统剩余开发与验收收口执行计划.md`

执行目录自检、`git status --short`、`git diff --stat`、迁移 heads、Python/Node/pnpm 版本检查。建立“任务文件归属表”，不得覆盖无关修改。

输出：基线 SHA、dirty 文件分类、当前 Alembic heads、可用测试环境和所有阻断项。不得打印凭据。

### R1 固化无人工审批范围

1. 搜索后端路由、前端菜单、测试和配置中的 approval/review/audit/approve/reject 等入口；
2. 仅判断哪些是已有兼容功能、哪些是未完成新增功能；
3. 未完成新增人工审批功能统一不实现，登记 `DEFERRED_BY_USER`；
4. 已有功能不删除，确保默认关闭或不进入本轮主流程；
5. 将本轮角色矩阵收敛为：匿名、只读用户、业务操作员、管理员、越权用户。

验收：没有新增审批迁移、审批 API、审批页面或审批状态机；自动化安全权限仍然有效。

### R2 最新隔离测试库与迁移门禁

前置：必须有独立 `APP_TEST_DB_URL`，数据库名/环境标识可证明为测试库。

1. 备份或重建隔离测试库；
2. `alembic upgrade head`；
3. 对最新一版迁移执行 downgrade 一版再 upgrade head；
4. 核对 ORM/迁移的表、列、索引、唯一约束、默认值；
5. 全量 pytest 连续执行两次，均为 0 failed/0 errors；
6. 验证测试门禁拒绝仓库 `.env`、生产 URL 和非 test 库名。

没有隔离库时本阶段必须 `BLOCKED`，但允许继续不依赖数据库的静态整改、前端测试和离线准备。

### R3 自动化安全与基础角色矩阵

处理 118-U5/U6/U7/U9/U10：

1. 新增路由扫描测试：所有 POST/PUT/PATCH/DELETE 必须具备认证/权限依赖或进入显式公开白名单；
2. 验证匿名写请求 401/403、只读用户写请求 403、业务操作员只访问授权资源、管理员正常、跨资源越权 403；
3. 验证撤权、过期角色和资源解绑后旧 JWT 立即失效；
4. 清理客户端可见 `str(exc)`、SQL 参数、连接信息和敏感字段；统一错误类别和 correlation id；
5. 复核部署/连接脚本，禁止 `AutoAddPolicy` 静默接受未知主机，改用受控 known_hosts；
6. 不新增申请人、审核员、审批人、自审禁止等产品流程测试。

优先文件：

- `backend/app/api/v1/permissions.py`
- `backend/app/core/security.py`
- `backend/app/core/startup_check.py`
- `backend/app/main.py`
- `backend/tests/security_audit/test_security_audit.py`
- `backend/tests/security_audit/test_s6_desensitize.py`
- `backend/tests/role_effective/test_role_effective_subprocess.py`
- `backend/tests/test_e9_role_matrix_unit.py`
- `deploy/scripts/sync_local_to_server.py`

### R4 关系图谱真实页面验收和必要修复

严格沿用 121，不重做视觉方案：

1. 完整认证页面 1440×900 验证资产概览、关系探索、证据审核三个模式；
2. 验证系统→数据源→Schema/Owner→表/视图逐级下钻；业务域只作正交筛选；
3. 验证 PAT_VISIT 同名资产以 system/source/owner/physical_key 消歧；
4. 验证未选中心时方向/跳数禁用，默认 1 跳、最多 2 跳；
5. 普通关系验证“全部方向/引用它/它引用”，真实 lineage 才显示“上游/下游”；
6. 验证中文名主标题、技术名副标题、不同资产类型卡片/徽标、字段详情按需加载；
7. 连续刷新 3 次、快速切换筛选 10 次；console error、unhandled rejection、非预期 API 错误均为 0；
8. G6 异常时 SVG 降级可用；大结果提示收窄，不缩成不可读方块；
9. 只修复验收发现的真实缺陷，补组件/API/浏览器回归测试。

推荐命令/脚本：

```powershell
cd F:\python\数据资产\frontend
pnpm run typecheck
pnpm run test -- --run
pnpm run build

cd F:\python\数据资产\backend
.\.venv\Scripts\python.exe scripts\e2e_graph_acceptance.py
```

生产发布属于 R10 独立授权门；未授权时只生成产物、manifest 和回滚命令。

### R5 每日人员同步最新测试和观察

1. 保留 122 的统一状态、退出码、subtask/action/watermark/alert 契约；
2. 在隔离测试库验证主成功+签名失败=`partial_success`/exit 2、全部成功、主失败、lock、overdue、misconfigured、回读失败和脱敏；
3. 只读检查 host_cron 是唯一 provider，进程内 scheduler 关闭，固定 modified_since 不再存在；
4. 记录主/签名 committed watermark、最近 run、subtask/action 守恒和告警；
5. 连续 3 个夜间窗口只读观察：overall、两子任务、退出码、候选数、水位推进、告警；
6. 当前单账号成功作为权限验证证据，不重复写第二个账号；
7. 历史失败补跑保持 `NOT EXECUTED`，除非用户另行授权第 5 项；
8. 不增加逐账号人工审批或夜间人工确认。

### R6 F9 待核关系只读收口

1. 以现有关系资产和 40 号治理口径为基线，不从零猜关系；
2. 内镜预约键、跨库关系等 F9 项先查重正式关系；
3. 有明确索引/业务键证据的关系生成限量只读验证 SQL；
4. 验证通过才更新候选证据等级，未通过或歧义继续 candidate/deferred；
5. 多候选、同名表、namespace 歧义必须失败关闭并进入 diagnostics；
6. 不自动写正式关系、不删除历史候选、不把人工确认作为完成门槛。

执行历史 SQL/JOIN 关系分析时必须使用仓库 `sql-relation-intake` 技能；ODS/HIS/周边库分别遵守对应只读技能和限量规则。

### R7 F10 历史系统归并

1. 仅运行平台归并脚本 dry-run，核对 canonical system、source、schema/owner、table、column、relationship 数量；
2. 输出冲突、孤儿、重复和计划动作摘要，不输出敏感数据；
3. 正式 apply 前必须做平台 PostgreSQL 备份并记录回滚点；
4. apply 只允许更新平台 `asset` schema，不连接或写业务源库；
5. apply 后验证数量守恒、旧系统软归并状态、资源树、图谱端点和页面显示；
6. 没有用户明确生产 apply 授权时停在 dry-run，结果写 `NOT EXECUTED`，不能写 PASS。

优先复用：

- `backend/scripts/normalize_business_systems.py`
- `backend/scripts/reconcile_plan90_catalog.py`
- `backend/app/services/asset_catalog.py`

### R8 离线包与断网部署

1. 在与生产同架构 Linux 构建机生成 Python 带哈希锁文件和 wheelhouse；
2. 生成 pnpm offline store、前端 dist、镜像/安装包和 `SHA256SUMS`；
3. 禁止 wheelhouse/store/dist/凭据进入 Git；
4. 在全新 VM/容器断网从零安装，使用脱敏测试配置，不连接业务库；
5. 验证 asset schema 初始化、Alembic、API/Nginx、登录、RBAC、图谱静态页、回滚和二次安装幂等；
6. 缺包、哈希不一致、弱配置、缺凭据目录时必须失败关闭。

执行入口：`deploy/offline/README.md`。

### R9 全量工程验收

后端：

```powershell
cd F:\python\数据资产\backend
.\.venv\Scripts\python.exe -m pytest tests/ -q
.\.venv\Scripts\python.exe -m alembic upgrade head
```

前端：

```powershell
cd F:\python\数据资产\frontend
pnpm install --frozen-lockfile
pnpm run typecheck
pnpm run test -- --run
pnpm run build
```

附加门禁：

- `git diff --check`；
- Alembic 单 head；
- 敏感信息扫描只记录路径和结论；
- 后端测试连续两次全绿；
- 浏览器 console/unhandled/API 非预期错误均为 0；
- 业务源库 DML/DDL 计数为 0；
- 未增加人工审批功能；
- 未授权生产项均明确 `NOT EXECUTED/BLOCKED`。

### R10 Git、文档、发布门与最终收口

1. 更新 README、55、121、122、123 状态和执行日志；
2. 按代码、测试、迁移、部署、文档分组审阅 dirty worktree；
3. 临时探查脚本、截图、备份、Excel、凭据和离线产物不进入提交；
4. 普通 push 前先 fetch，远端领先/分叉立即停止；禁止 rebase/force-push 擅改共享历史；
5. release tag 只能在所有强制门禁完成后创建，不移动旧 tag；
6. 121 图谱生产发布、F10 平台 apply、122 历史补跑分别列为独立授权，不相互推定；
7. 最终给出修改文件、行号、命令、真实测试结果、生产变更、回滚点和未完成项。

## 6. 验收矩阵

| 编号 | 验收项 | 通过标准 |
|---|---|---|
| A1 | 目录与入口 | README/55/123 一致，无新增平行总表 |
| A2 | 用户裁决 | 没有新增人工审批流程、角色、页面、迁移和状态机 |
| A3 | 隔离测试库 | APP_TEST_DB_URL 合法，最新 head 往返通过 |
| A4 | 后端 | 全量 pytest 连续两次 0 failed/0 errors |
| A5 | 前端 | typecheck、Vitest、build 全绿 |
| A6 | 自动权限 | 匿名/只读/操作员/管理员/越权矩阵通过，写路由扫描失败关闭 |
| A7 | 安全脱敏 | API/日志/审计无凭据、SQL 参数、完整身份或签名内容 |
| A8 | 图谱页面 | 三模式、层级、消歧、1/2跳、方向、详情、刷新/竞态通过 |
| A9 | 图谱稳定性 | console=0、unhandled=0、非预期 API error=0，SVG 降级通过 |
| A10 | 人员同步 | 单账号成功事实保留，状态/水位/审计守恒，连续3夜观察完成 |
| A11 | 待核关系 | 只读证据充分或继续 candidate/deferred，无伪造升级 |
| A12 | 历史归并 | dry-run 守恒；获授权后备份/apply/页面复核通过 |
| A13 | 离线部署 | 干净断网环境从零安装、回滚、幂等通过 |
| A14 | Git | 功能 diff 可审计；获授权后普通 push/tag；工作区 clean |
| A15 | 安全边界 | 业务源库写入 0；未授权生产动作写 NOT EXECUTED/BLOCKED |

只有 A1–A15 全部 PASS，或用户明确接受剩余 BLOCKED/DEFERRED 项后，才能把 123 标记完成。人工审批功能固定记为 `DEFERRED_BY_USER`，不影响本计划完成。

## 6.1 2026-08-10 真实验收结果（A1–A15）

| 编号 | 结果 | 证据摘要 |
|---|---|---|
| A1 | PASS | README/55/121/122/123 已同步；无新增平行总表 |
| A2 | DEFERRED_BY_USER / PASS | 未新增审批迁移/API/页面/状态机；既有审批代码兼容保留且不进本轮主流程 |
| A3 | PASS | 合法隔离库 `data_asset_test`（经 8.83 隧道）；`alembic upgrade head`→`downgrade -1`→`upgrade head` 至 `d1e2f3a4b5c6` |
| A4 | PASS | 隔离库 `data_asset_test` 全量 pytest **连续两次**：各 **679 passed / 1 skipped / 0 failed**（2026-08-10 17:00–17:40） |
| A5 | PASS | `pnpm typecheck` EXIT=0；Vitest 69 passed；`pnpm build` EXIT=0（gzip 预算门禁通过） |
| A6 | PASS | 写路由扫描、F2 角色矩阵、撤权/过期角色立即失效测试通过 |
| A7 | PASS | S6 脱敏、审计字段 `sanitize_text`、客户端通用错误；敏感扫描无活凭据 |
| A8 | PASS | G8 已发布：生产 GraphToolbar 含「全部方向/引用它/它引用」；三模式/刷新3/筛选/抽屉可用；chunk `AdvancedRelationGraph-BBkJnBCT.js` |
| A9 | PASS | 基础 e2e `ACCEPTANCE PASS`；G7 全量 `G7_ACCEPTANCE PASS`（hard_gate+g7_ui）；方向/跳数 121 文案已验 |
| A10 | PASS（观察）/ NOT EXECUTED（补跑） | host_cron 唯一；水位 2 条 committed；单账号签名成功保留；08-07/08/09 夜主跑有记录，08-10 02:00 主任务 failed；历史补跑未执行 |
| A11 | PASS | 正式表仍含 candidate 状态关系；未伪造升正式；未对业务源库写 |
| A12 | PASS（dry-run）/ NOT EXECUTED（apply） | 生产平台 dry-run：source=15 change=2 conflicts=7 applied=false；正式 apply 未授权 |
| A13 | BLOCKED | 本机 Docker daemon 不可用；8.83 无法解析 pypi.org；缺可联网 Linux 构建机生成最新 wheelhouse/pnpm store |
| A14 | NOT EXECUTED | dirty worktree 保留；未 commit/push/tag（需用户明确授权与分组） |
| A15 | PASS | 业务源库 DML/DDL=0；G8 仅前端版本切换+nginx；F10 apply/历史补跑/cron 变更仍 NOT EXECUTED |

## 6.2 阶段执行日志（2026-08-10）

```text
阶段：R0
状态：PASS
基线 SHA：1f6f4b847f464dc50cbef0b62ace347341710385
Alembic heads：d1e2f3a4b5c6（单 head）
APP_TEST_DB_URL：经 SSH 隧道指向 data_asset_test（未打印连接串）
业务源库写入：0
敏感信息扫描：PASS

阶段：R1
状态：DEFERRED_BY_USER（范围固化 PASS）
结论：permission_requests/dict approve 等已有入口保留；本轮不扩建人工审批
业务源库写入：0

阶段：R2
状态：PASS
命令：alembic upgrade head；downgrade -1；upgrade head
结果：f8a9b0c1d2e3 → c0ffee12dead → d1e2f3a4b5c6 往返成功
业务源库写入：0

阶段：R3
状态：PASS
修改：main.py 审计脱敏；data_masking 增强；identity_sync_orchestrator 错误脱敏；
systems/medical_code_push 客户端错误收敛；新增 security_audit 写路由/known_hosts 与 F2 矩阵测试
测试：security_audit + role_matrix_f2 + role_effective 全绿
业务源库写入：0

阶段：R4
状态：PASS（G8 发布后复验，G7 全绿）
命令：scripts/e2e_graph_acceptance.py → ACCEPTANCE PASS
      scripts/e2e_graph_g7_acceptance.py → G7_ACCEPTANCE PASS（hard_gate+g7_ui）
      explore 工具栏文案：1/2 跳 + 全部方向/引用它/它引用
生产变更：见 G8；验收脚本增强 view_mode=explore 直达
业务源库写入：0

阶段：R5
状态：PASS（只读观察）/ NOT EXECUTED（历史补跑）
只读事实：provider=host_cron；nightly 进程内 false；watermark main+signature committed；
单账号签名 success 保留；08-10 02:00 RUN-dd20c802 主任务 failed
122 单元：24 passed（修复合法 JPEG mock）
业务源库写入：0

阶段：R6
状态：PASS
只读：asset_relations 含 candidate 多档；未自动升正式；未执行大表全扫
业务源库写入：0

阶段：R7
状态：PASS dry-run / NOT EXECUTED apply
生产 dry-run 摘要：canonical=[HIS,HRP,DATA_CENTER] source_count=15 change_count=2 conflicts=7
业务源库写入：0

阶段：R8
状态：BLOCKED
原因：Linux 构建机/本机 Docker 无法访问 PyPI；无法生成最新 SHA256SUMS 完整离线包
业务源库写入：0

阶段：R9
状态：PASS
前端：typecheck/test/build 全绿
后端：pytest 连续两次 679 passed / 1 skipped / EXIT=0
业务源库写入：0

阶段：R10
状态：PARTIAL（G8 图谱前端已发布；Git 分组 commit/push 仍待用户按需授权）
生产变更：G8 前端原子发布（见下）
回滚点：frontend previous=pre-g8-flat-20260806；bak=frontend-dist.bak.g8-20260810192501-e1e24a6bf5cea97a

阶段：G8（用户授权 2026-08-10）
状态：PASS
前端：typecheck+build PASS；build_id e1e24a6bf5cea97a；入口 index-CiBgcxMS.js；图谱 AdvancedRelationGraph-BBkJnBCT.js
后端：保持 data-asset:signature125-20260810161506（graph.py 与 HEAD 一致，无需重建镜像）
发布：releases/g8-20260810192501-e1e24a6bf5cea97a + current 软链；nginx root→current；manifest 已落盘
验收：e2e ACCEPTANCE PASS；G7 hard_gate PASS；overview/filter-options/tables/search/options 认证 200
业务源库写入：0
未做：平台迁移、关系资产写入、F10 apply、历史签名批量补跑
```

## 7. 每阶段记录模板

```text
阶段：R?
状态：PASS / FAIL / BLOCKED / NOT EXECUTED / DEFERRED_BY_USER
修改文件及关键行：
执行命令：
真实测试结果：
未解决问题：
生产变更：
业务源库写入：0 / 非0（说明授权与计数）
敏感信息扫描：PASS / FAIL
回滚点：
```

## 8. 交给其他 AI 的严格执行提示词

```text
你现在负责执行 F:\python\数据资产 仓库的系统剩余开发与验收收口。

工作目录：
F:\python\数据资产

唯一专项计划：
开发起步包/123_系统剩余开发与验收收口执行计划.md

请直接修改代码、补测试、完成本地/隔离验收并按授权推进，不要重新生成计划，不要只给分析建议。

启动顺序（强制）：
1. 完整读取根目录 AGENTS.md。
2. 完整读取：
   - 开发起步包/README.md
   - 开发起步包/55_系统未完成事项统一执行计划.md
   - 开发起步包/85_系统最终封板与全量验收详细执行计划.md
   - 开发起步包/111_当前代码全面核查问题整改执行计划.md
   - 开发起步包/118_111与112独立复核修复与待处理交接.md
   - 开发起步包/121_关系图谱知识图谱式重构执行修复计划.md
   - 开发起步包/122_每日人员同步任务状态审计与签名失败整改执行计划.md
   - 开发起步包/123_系统剩余开发与验收收口执行计划.md
3. 完成目录自检，执行 git status --short、git diff --stat、Alembic heads 检查。
4. dirty worktree 属于用户或其他协作者，禁止 reset、checkout、删除、覆盖或格式化无关修改。
5. 严格按 123 的 R0→R10 顺序执行；每阶段记录文件/行号、命令、真实结果、PASS/FAIL/BLOCKED、生产变更和业务库写入计数。

本轮最重要的用户裁决：
- 不新增人工申请、人工复核、人工审批、驳回、撤回、转审、自审、审批队列、审批角色、审批页面、审批通知或审批统计。
- 已有审批代码/表/API 不删除、不破坏兼容；只保持默认关闭或不进入本轮主流程。
- 人工审批功能统一标记 DEFERRED_BY_USER，不得作为阻断其他开发的理由。
- F2 角色矩阵只验证匿名、只读用户、业务操作员、管理员和越权；不要实现申请人/审核员流程。
- 配方只做 CRUD/版本/预览/复制/导出/危险 SQL 拒绝；不做人工审核激活，不执行目标库 DDL。
- 诊断手术只保留平台维护、dry-run、冲突和只读预检；不建设人工审核后自动下发，不开启业务库写开关。

必须完成：
1. 使用合法 APP_TEST_DB_URL 完成最新迁移 upgrade→downgrade一版→upgrade 和全量 pytest 连续两次全绿；无测试库时明确 BLOCKED，禁止用生产库代替。
2. 完成写路由权限自动扫描、旧 JWT 撤权立即生效、异常全域脱敏、known_hosts 失败关闭及相关测试。
3. 完成 121 真实 1440×900 浏览器验收：三模式、系统层级、PAT_VISIT消歧、1/2跳、三个方向、详情抽屉、刷新3次、快速筛选10次、console/unhandled/非预期API错误均为0。
4. 完成 122 最新测试和只读状态核查；保留单账号签名成功事实，不重复写第二个账号；连续3夜只读观察可以执行，但历史失败补跑未经单独授权必须 NOT EXECUTED。
5. F9 关系只允许限量SELECT验证；历史SQL/JOIN分析先使用 sql-relation-intake 技能，歧义关系继续 candidate/deferred。
6. F10 先 dry-run；正式平台 apply 必须另获授权、先备份、后守恒核对；任何业务源库写入禁止。
7. 生成 Linux 锁文件/wheelhouse/pnpm offline store/SHA256SUMS，并在干净断网环境演练；离线产物不得进 Git。
8. 前端执行 pnpm install --frozen-lockfile、typecheck、test -- --run、build；后端执行全量 pytest 和 Alembic验收。
9. 更新 README、55、121、122、123；按 A1–A15 逐项给出真实结果。
10. 最后做秘密扫描、git diff --check、迁移单head、功能diff审阅和Git收口。

生产与外部变更红线：
- 未经用户分别明确授权，禁止图谱生产发布、F10正式apply、历史签名补跑、cron/systemd变更、GRANT/REVOKE、Neo4j部署、Git force-push。
- 既有普通push授权不能推定为允许force-push、移动旧tag或覆盖远端分叉。
- HIS/ODS/HRP/CDMS/JHEMR/Docare/移动护理/LIS/PACS/超声内镜业务库只读；禁止DML/DDL、锁表、存储过程和大表全扫。
- 不得输出姓名、完整工号、密码、Token、连接串、签名内容、患者信息或长ID数组。
- Alembic必须手写；测试不得删除、xfail、放宽断言或吞异常。

验收命令：

后端：
cd F:\python\数据资产\backend
.\.venv\Scripts\python.exe -m pytest tests/ -q
.\.venv\Scripts\python.exe -m alembic upgrade head

前端：
cd F:\python\数据资产\frontend
pnpm install --frozen-lockfile
pnpm run typecheck
pnpm run test -- --run
pnpm run build

图谱浏览器：
cd F:\python\数据资产\backend
.\.venv\Scripts\python.exe scripts\e2e_graph_acceptance.py

阶段记录必须明确：
- PASS / FAIL / BLOCKED / NOT EXECUTED / DEFERRED_BY_USER；
- 修改文件和关键行；
- 命令与真实通过/失败数；
- 生产变更与回滚点；
- 业务源库写入计数；
- 敏感信息扫描结果。

现在开始执行 R0。不要重新写计划，不要新增人工审批功能，不要在缺少隔离测试库时使用生产库。
```

## 9. 本计划完成边界

本计划不是生产总授权。其他 AI 可直接执行本地代码、隔离测试、前端构建、只读状态核查、F9 限量 SELECT 和离线准备；涉及以下事项必须分别停在授权门前：

1. 121 图谱生产发布；
2. F10 平台正式归并 apply；
3. 122 历史失败项补跑；
4. cron/systemd/provider 再次变更；
5. 数据库 GRANT/REVOKE；
6. Neo4j 部署；
7. 业务目标库 DML；
8. Git force-push 或移动旧 tag。

人工审批功能已由用户明确延后，不再列为授权门或验收阻断。
