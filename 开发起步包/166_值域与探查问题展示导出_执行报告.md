> 类别：证据链
>
> # 166 值域与探查问题展示导出 执行报告（D1–D6）
>
> 计划：`166_值域与探查问题展示导出功能开发计划.md`（v1.1+附录 A，round-4 B1–B14 裁决落点）
> 执行窗：2026-08-30（单执行者连续推进；前置 165 E1–E6/163/164 已确认完成）
> 结果：**隔离库完成，待主 AI 复核与发布**（发布=生产 apply 授权，属等待域）

## 1. 结论

- **D1–D6 六批全部验收通过**：F1–F7 落地；后端新增测试 35（1288→**1323 passed / 1 skipped / 0 failed**，27m53s）；前端新增测试 19（**229 passed / 34 files**）+typecheck 双过+build gzip 预算 PASS（main 656.6/700、graph 404.6/430、css 101.3/110）。
- **后端增量恰为 3 端点+种子**（§3 对照表零偏离）：`POST /probe-findings/{id}/transition`、`POST /probe-findings/export`（替换 165 的 405 占位，保持先于 /{id} 注册）、`GET /value-domains/export`（先于 /{domain_id} 注册）+ `probe.finding.manage` 种子（RESOURCE_CATALOG+quality_admin，幂等 seed 二跑收敛 0）。全部写进 165 probe 模块/149 value_domains 模块（只增不改）。
- **联调抓到并修复一处真集成缺陷**（§6）：前端把 165 裸分页响应当 ApiResponse 消费（vi.mock 按同错误假设造数未拦截），真实隔离库联调暴露 `res.data.items` TypeError；修复为裸 body 消费（probe.ts+页面+测试三处），修复后全量门禁复跑全绿。
- **人工操作零代点**：confirm/deprecate/resolve-conflict/transition 全部仅在隔离库 throwaway 行上验证，测后清理（业务行清零，审计行 19 条按设计留档为证据链）。

## 2. 批次验收（D1–D6）

| 批 | 内容 | 验收结果 |
|---|---|---|
| D1 骨架 | `/value-domains`（数据治理组，auths=value_domain.read）+`/probe-findings`（质量管理组，auths=probe.finding.read）两路由模块（governance.ts rank15/quality.ts rank16）；`src/api/probe.ts` 新建+`asset.ts` 值域函数扩展；页面骨架 | typecheck 双过；plan146 式源码断言 4 测试过（auths 含点号码+组件文件在位+filterNoPermissionTree 纯函数四场景：单码/反码/通配/无关码）；受影响既有测试 15/15 |
| D2 值域三件 | F1 表详情值域区块（关联关系卡片后，**table-detail 仅触碰一处**）；F2 值域管理页（筛选/冲突 Tab/pending 徽标/详情抽屉/版本时间线）；F3 三操作（confirm 可选 reason/conflicted 点击引导先裁决/deprecate 强制 reason） | 组件测试 **7/7**（列表+徽标/抽屉证据链+版本/冲突 Tab+引导/403 用例/v-perms 三按钮点号断言/F1 system_code 必带+200×2 页循环拉全+无 system_code 不发请求/暂无值域列提示）；typecheck 双过 |
| D3 探查页+流转 | F4 探查发现页（筛选+分页+复发徽标+脱敏管道+XSS 断言+复制按钮+runs Tab）；F5 后端 transition 端点+状态机函数（probe_service.py 只增） | 后端 **40 passed**（transition 专项 22：12 条合法迁移参数化全过+同态 422+非法值 422+action 缺失 422+reason 必填 422+404+执行器身份 403（持 platform_admin 仍 403）+审计行含 reason+resolved_by/at 快照置/清+纯函数矩阵+165 probe 回归 19）；前端 **8/8**（脱敏单测 2+列表+XSS 断言 1（载荷以文本呈现、零 img/script 注入节点）+runs Tab 脱敏+流转弹窗迁移表提示+空理由拦截+403 呈现+v-perms ≥4 处点号）；typecheck 双过 |
| D4 导出 | F6 两处导出（findings=POST+body 筛选替换 405 占位；value-domains=GET 先于 /{domain_id}） | 后端 **8/8**（六硬约束逐条实证，见 §4）；前端 166 全量 19/19+typecheck；165 的 405 占位测试按计划内替换更新（保序断言保留，exceptions W1） |
| D5 权限与矩阵 | F7 `probe.finding.manage` 种子+security_audit 同步 | **14 passed**：seed 二跑 created_permissions=0（幂等收敛）；manage 落库角色集合与 ROLE_DEFAULT_PERMISSIONS 完全一致（platform_admin+quality_admin）；live 403：ai_user read 200/transition 403/confirm 403，asset_viewer 同理，quality_admin manage 200；security_audit 全绿（含新增矩阵断言：ai_user/asset_viewer 无 manage、无 value_domain.confirm） |
| D6 总门禁 | 全量 pytest+typecheck+test+build | 后端 **1323 passed / 1 skipped / 0 failed**（基线 1288+新增 35，零未解释增量；plan127 s0 例外未触发）；前端 **229/229+typecheck+build 预算 PASS**（裸分页修复后复跑，§6） |

## 3. 后端增量对照（166 §3 承诺 vs 实际）

| 计划项 | 实际 | 偏离 |
|---|---|---|
| POST /api/v1/probe-findings/export（probe.finding.read+审计） | ✓ probe.py 内实现，替换 405 占位且保持先于 /{id} | 无 |
| GET /api/v1/value-domains/export（value_domain.read+审计） | ✓ value_domains.py 内、先于 /{domain_id} 注册 | 无 |
| POST /api/v1/probe-findings/{id}/transition（probe.finding.manage+审计） | ✓ probe.py 追加+probe_service.transition_finding 新函数 | 无 |
| permissions seed（manage）幂等 dry-run 收敛 0 | ✓ 二跑 created_permissions=0 | 无 |
| 模型零改动；零新迁移 | ✓ 两表模型/迁移零触碰 | 无 |

## 4. 导出六硬约束证据（B1 裁决逐条）

| # | 约束 | 测试实证（tests/test_export_166.py，8/8 过） | 联调实证（隔离库） |
|---|---|---|---|
| ① | require_permission 显式挂载 | `test_403_without_probe_read`/`test_403_without_value_domain_read`：受限角色 403 | — |
| ② | 审计行（操作人/筛选/行数） | 断言 audit.operator/after_data.rows/filters/filename | 联调留档 probe:export×2、value_domain:export×1 |
| ③ | 文件名含导出时间+筛选标签 | `probe-findings-<stamp>-status-open.csv`/`value-domains-<stamp>-status-pending.csv` 断言 | 实际响应头同格式（lianTiao_evidence.json） |
| ④ | 防公式注入（=+-@ 前加 '） | `'=HYPERLINK`/`'=SUM(1+1)` 断言（note/meaning 载荷） | — |
| ⑤ | 列白名单逐列常量 | header 恒等 FINDING_EXPORT_COLUMNS/值域 11 列元组；**evidence_sql 不在输出**、evidence_digest 在 | 导出采样 evidence_sql_absent=true |
| ⑥ | findings=POST+body 筛选；值域默认排除 conflicted | body status=open 筛选差分+文件名标签；conflicted 默认排除/include_conflicted=true 全量 | 值域导出（status=pending）1 行不含冲突行 |

5000 上限：FINDING_EXPORT_LIMIT==5000 断言锁死；值域导出同限。

## 5. transition 状态机测试矩阵（F5/B6）

12 条合法迁移（pytest 参数化 ×12 全 200 且终态正确）：

| 从\到 | open | confirmed | false_positive | resolved |
|---|---|---|---|---|
| **open** | — | ✓ | ✓ | ✓ |
| **confirmed** | ✓ | — | ✓ | ✓ |
| **false_positive** | ✓ | ✓ | — | ✓ |
| **resolved** | ✓ | ✓ | ✓ | — |

拒绝路径：同态原地转 422（单测+联调真同态 422）、非法终态值 422、action 非法/缺 to_status 422、reason 缺失 422、404、执行器身份（probe: 前缀，持 platform_admin）403。resolved_by/at：转 resolved 置（操作人+时间）、离开 resolved 清（与 165 复发清空口径一致）。审计：每迁移一行（module=probe/action=transition/before/after/reason）。

联调全流转：**12/12 迁移 200 且 after 态正确**，审计 13 行（12 迁移+1 导出），三拒绝路径 live 复验（同态 422/非法值 422/缺 reason 422）。

## 6. 联调（隔离库，D6 全量回归之后按附录 A.2 顺序执行）

- **空态截图**（全量回归后真实空库，0 行实证后拍摄）：`output_r166/screenshots/f4-empty-state.png`、`f2-empty-state.png`。
- **throwaway 造数**（全部带 `[throwaway-r166]` 标记）：表+2 字段、值域 2+1 冲突（409 触发实证）、finding 1、throwaway ApiKey/platform_admin 绑定。
- **UI 截图（真实后端+真实数据，仅注入 Authorization 头，零响应伪造）**：`f4-list-throwaway.png`（1 行+阈值对照）、`f4-detail-evidence.png`（脱敏代码块+digest+复制按钮）、`f5-transition-dialog.png`（迁移表提示+open→resolved 方向）、`f2-list-throwaway.png`（2 行+冲突徽标=1）、`f2-conflict-tab.png`、`f1-table-detail-value-domains.png`（DISCHARGE_DISPOSITION 分组展开+“1 个字段暂无值域：NO_DOMAIN_COL”）。
- **集成缺陷（联调价值实证）**：首拍 F4 报 `Cannot read properties of undefined (reading 'items')`——前端按 ApiResponse 包装消费 165 的裸分页响应（B2 无 mock 开关直连真实 API 才暴露）；修复 probe.ts 返回类型+页面消费+D3 测试 mock 形状（裸 body），修复后 D3 8/8、全量 229/229、build 预算复跑全绿。登记 exceptions W5。
- **导出采样**：findings 无筛选 1 数据行（throwaway 行在、evidence_sql 不在表头）；值域 status=pending 1 行（冲突行被默认排除）。CSV 存 `output_r166/export_probe_findings_sample.csv`/`export_value_domains_sample.csv`。
- **清理复验**：findings/value_domains/tables 残留 **0/0/0**；审计 19 行按设计留档（transition 13/export 3/值域 submit2+conflict1）——审计是证据链非业务数据。
- 采集脚本 `output_r166/capture_r166.py`；机器可读证据 `output_r166/lianTiao_evidence.json`。

## 7. DoD 逐项自检（166 §6）

- [x] F1–F7 落地，D1–D6 checkpoint 齐；新增测试 **51**（后端 35+前端 19）≥16
- [x] 全量 0 failed（1323/1s/0f；plan127 s0 例外未触发）；前端三件套+gzip 预算全绿
- [x] 导出六硬约束各有测试实证（§4 六行六证据）；transition 12 条合法迁移+双拒绝路径全测（§5）
- [x] ai_user/asset_viewer 无 manage、无 value_domain.confirm 实证（D5 live 403+security_audit 矩阵断言双证）
- [x] 隔离库联调截图（空态 2+数据态 6）入报告 §6；报告+`_结果.json`+README/55 登记本行；P2 清单见 §9

## 8. 红线自检

人工操作零代点（全部 throwaway）；后端仅 3 端点+种子且只增不改（165 probe 两函数一字未动，其 19 项测试全绿）；导出六硬约束全测；无 mock 开关（产品恒直连，空态=引导文案）；权限码全点号（种子/v-perms/后端依赖三处一致+源码断言锁死）；table-detail 仅触碰一处（值域区块+TableDetail 类型补 system_code/source_code 两可选字段）；新接口全进 src/api/（probe.ts 新建+asset.ts 扩展）；零迁移零 Git 写；隔离库验证（显式 APP_TEST_DB_URL，db_guard 拒绝默认连接实证一次）；pnpm 全程。

## 9. 呈用户（P2 后置清单+发布等待域）

**P2（166 §2 明示后置另批）**：对账矩阵看板、metric 趋势曲线、QualityFinding 页互链、AI 探查触发入口（依赖 165 X4 调度）、值域批量导入模板。

**发布等待域（需主 AI 复核+授权，本计划不含发布）**：
1. 生产 apply：probe.finding.manage 种子（POST /permissions/seed，幂等）——前置 165 X1 生产建表已授权范围外，本种子独立可 apply；
2. 前后端发布（backend 3 端点+frontend 两页面/两组菜单）走既有发布流程（153/161 先例）；
3. 值域确认/裁决与 finding 终态流转上线后转人工运营（165 X3 的界面载体即本批 F3/F5）。

## 10. 生产发布（2026-08-30 用户当日授权"继续执行"，已实施）

**侦察结论**：167 发布时已补 165 `b0→c1` 生产迁移（生产 alembic=c1d2e3f4a5b6、probe 两表在、`probe.finding.read` 资源+四角色授权齐全）；生产容器 `data-asset-api`（镜像 `data-asset:w10c2-202608301011`，healthy）；四后端文件与工作区 diff 全部恰为 166 语义（probe.py 删 3 行占位+198 行实现、value_domains 纯新增 111 行、probe_service 纯新增 62 行、permissions 4 行；models 零差异）——发布载体零夹带后端改动。

**执行序（165 X1 门禁模式：备份→apply→二次 dry-run 收敛→health）**：
1. **备份**：`/opt/data-asset/backups/data_asset_pre_r166_202608301345.dump`（10,970,231 bytes，106 表 TABLE DATA，probe 两表+唯一键+索引全含，pg_restore --list 校验过）。
2. **manage 种子幂等 apply**（与 /permissions/seed 同语义直接 SQL）：首跑 resources=1+grants=2（platform_admin/quality_admin），**二次 dry-run 收敛 0**，复验落库角色集合与隔离库 D5 矩阵一致。等待域①完成。
3. **后端发布**：四文件 scp→`docker cp` 入容器→`import_ok`→restart→`/health` 200（database connected）。OpenAPI 验证三端点全部注册（POST /probe-findings/export、POST /{id}/transition、GET /value-domains/export，405 占位消失）；裸 curl 三端点未授权均 **401**。
4. **前端发布**：dist（13:30 build，D6 门禁版）→`releases/r166-20260830`→HTML 引用资源完整性校验 ASSETS_OK→**原子切换 current→r166（previous→r167-20260830）**→nginx -t+reload。经 nginx 端到端：首页 200、入口 `index-s9RlzpGz.js` 200（与本地构建 hash 一致）、/api 代理 health 200。
5. **回滚路径**：前端 `ln -sfn releases/r167-20260830 current`+reload；后端以 w10c2 镜像重建容器或 cp 回旧文件重启；DB 用上述 dump。

**注记（如实登记）**：
- 本次前端 dist 为工作区整体构建，**载体含 168 号 P0 图谱改造的前端改动**（其后端零改动；该部分经本次 D6 全量门禁 229 tests+build 验证，但 168 报告自列的浏览器截图验收项仍归 168 待办，非本次发布门禁）。
- 容器为 docker cp 热更（与 sync_local_to_server.py 同机制），镜像 tag 仍 w10c2、/health 的 build_id 元数据仍 p153——**下次镜像制发布时归档**；回滚不依赖镜像（文件级可还原）。
- `probe.finding.manage` 种子与未来 seed 端点幂等兼容（端点复跑只会补齐相同行，0 新增）。
- **165 X2（生产首轮夜间探查）不在本次范围**：按 165 §1 铁律保持夜间窗+授权等待；X3 终态人工裁决的界面载体（F3/F5）已随本次发布就绪。
