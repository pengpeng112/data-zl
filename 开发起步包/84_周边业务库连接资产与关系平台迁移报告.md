> 类别：实施证据

# 周边业务库连接、资产与关系平台迁移报告

## 1. 完成结论

2026-07-15 已将 78–83 号只读探库证据中的数据库连接、表/视图、字段和关系迁移至部署服务器 `10.10.8.83` 的平台数据库。写入范围仅为平台 PostgreSQL 的 `asset` schema 和服务器受控凭据目录；所有业务源库均未执行 DML/DDL。

实施顺序为“备份 → 本地 dry-run → 服务器 dry-run → 幂等 apply → 数据对账 → 只读连通复核”。API 容器保持 healthy，Alembic head 保持 `y1f2a3b4c5d6`，本次不需要新增数据库迁移。

## 2. 迁移前备份

- 平台库：`/opt/data-asset/backups/data_asset_pre_sources_78_83_20260715072828.dump`（5,892,148 字节，权限 600）。
- 凭据目录：`/opt/data-asset/backups/credentials_pre_sources_78_83_20260715072828.tar.gz`（权限 600）。
- 失败回退只针对平台库和凭据目录；业务源库没有写入，无需回滚。

## 3. 平台资产迁移结果

| 来源 | source_code | 对象（表+视图） | 字段 | Namespace | 关系 |
|---|---|---:|---:|---:|---:|
| 无纸化 CDMS | `paperless_cdms_oracle_10_10_10_93` | 349 | 4,842 | 1 | 35 |
| Docare | `docare_oracle_10_10_10_68` | 646 | 10,529 | 3 | 21 |
| LIS | `lis_sqlserver_10_10_10_73` | 1,254 | 17,025 | 1 | 16 |
| 超声内镜 | `ultrasound_endoscopy_sqlserver_10_10_10_161` | 825 | 10,697 | 12 | 12 |
| PACS | `pacs_mysql_10_10_10_191` | 536 | 5,039 | 10 | 33 |
| JHEMR（原有复核） | `jhemr_vastbase_10_10_8_177` | 1,421 | 24,697 | 6 | 283 |
| HIS（本次补关系） | `his_source_10_10_10_15` | 1,234 | 19,831 | 既有 Owner | 31 |

平台全局对账结果：14 个系统、14 条数据源、7,197 个表/视图对象、120,395 个字段、478 条关系。

关系分级规则：数据库声明外键记为 `declared_foreign_key/verified`；只读样本全命中记为 `sample_data/verified/A`；存在历史或归档缺口记为 `partial/B`；无非空样本保持 `candidate/C`。每个来源使用独立 domain，幂等重跑只替换本来源资产和关系。

## 4. 连接与凭据

| 来源 | 类型 | 数据库/服务 | 凭据 | 连接复核 |
|---|---|---|---|---|
| 无纸化 | Oracle | `orcl` | configured | connected，约 134 ms |
| Docare | Oracle | `docare` | configured | connected，约 102 ms |
| LIS | SQL Server | `rmcloudlis7` | configured | 81 号一次性容器只读采集成功 |
| 超声内镜 | SQL Server | `MedcareUS`，实例内保留多库 | configured | 82 号一次性容器只读采集成功 |
| PACS | MySQL | `gecris` | configured | connected，约 62 ms |
| JHEMR | Vastbase | `jhemr` | configured | connected，约 14 ms |
| HIS | Oracle | `his` | configured | 修正旧引用后 connected，约 18 ms |
| HRP | Oracle | `hrpdb` | configured | connected，约 30 ms |

凭据文件位于 `/etc/data-asset/credentials/`，均为 `600 root:root`。平台表只保存 `file://` 引用、configured 状态和脱敏用户名；临时环境文件已删除。SQL Server 生产镜像未临时安装 ODBC，沿用 81/82 号真实只读采集证据。

## 5. 资源层级口径

- 同一 IP/端口只保留一个物理端点。
- 超声内镜 6 个数据库通过 `数据库.Schema` Namespace 保留在同一连接下。
- PACS MySQL 10 个数据库保留在同一端点下，`gecris` 为主业务库。
- Docare 的 `MEDSURGERY`、`MEDCOMM`、`MEDICU` 作为 Owner。
- ODS 内逻辑 PACS/LIS/SM/JHEMR 与独立物理源端使用不同 source_code，跨库关系必须另行验证。

## 6. 验收

- 本地 dry-run、服务器 dry-run、正式 apply：均成功。
- 平台分来源计数和全局计数：与导入输出一致。
- Oracle/MySQL/Vastbase/HIS/HRP 保存连接：全部 connected。
- API health：`status=ok`、`database=connected`。
- 凭据权限 600，临时凭据文件已删除。
- 业务源库写操作：0。

## 7. 复现入口

- 脚本：`backend/scripts/import_explored_sources_to_platform.py`
- 确认串：`IMPORT-EXPLORED-SOURCES-78-83`
- 输入：78–83 号关系结果及元数据快照。

脚本默认只 dry-run；正式执行必须同时提供 `--apply --confirmation IMPORT-EXPLORED-SOURCES-78-83`，凭据只能通过环境变量注入。
