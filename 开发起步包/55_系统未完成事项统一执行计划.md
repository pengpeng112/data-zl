> 类别：待办
>
> ⏳状态：**139 已于 2026-08-22 完成收口（见 142），同日医院 OA（万户 ezOFFICE）作为第五源完成引擎验证与平台导入（见 143，OA admin 高权限口令待轮换）；合计 61 条复核草稿与 40 份停用配方待人工独立复核；138 首期已批准纳入 130（确定性静态血缘；闭环后置）；130 其余现有应用收口仍按原范围管理；136/137 保留为上一轮三源凭据阻断的历史事实**；126 P1–P5 已生产，127/129 待统一发布，H7=2/3；业务库写侧保持关闭 | 最后复核：2026-08-22
> 📌 当前唯一执行说明：本文件仍是唯一未完成事项总入口。四系统只读探查已完成收口（最终报告读 `142_四系统资产导入与139收口执行报告.md`；其 56 条 review 草稿与 40 份停用配方进入正常人工复核流程，不得由原执行者自批）；现有应用连续开发/升级读 `130_系统完成度盘点与一次性开发升级执行计划.md`；数据目录—血缘—质量协同完善读 `138_数据目录血缘质量协同治理与系统完善改进计划.md`，并纳入 130 的同一发布窗口。136/137 仅作为上一轮历史证据。各计划不得互相夹带未授权业务目标写入；更早条目仅保留演进证据。
> 📌 2026-08-21 用户授权（139）：门诊病历 core2db、体检 EIS/JZCIS/ZONEKINGNET、病理 pitaya/QPIS、职业病 tjdatabase4 的独立只读账号已经创建并保存到 8.83 服务器受控凭据文件。授权另一位 AI 按 139 尽量一次完成四源可见表/视图/字段/约束/模块元数据采集、全部视图依赖与 JOIN/配方分析，并在备份、测试、dry-run 和幂等门禁通过后把成功来源写入平台 `asset` schema；单源问题如实记录后跳过继续。四个业务源库仍严禁 DML/DDL/EXECUTE；view-JOIN 只进复核草稿，复杂逻辑只进停用配方，不得同轮自动升正式。
> 📌 2026-08-22 OA 第五源纳入（143）：用户授权后按 139 同一管线完成医院 OA（10.10.10.69，万户 ezOFFICE）只读探查与导入：EZ_FLOW 工作流引擎 12 项限量聚合验证全过（HI_TASKINST/HI_ACTINST/RU_TASK/RU_EXECUTION→HI_PROCINST 命中 99.96–100%，WF_WORK→ORG_EMPLOYEE 99.91%），4 条引擎链作为带验证证据的 review 草稿入库；平台 20 系统/11,182 表/143,707 字段/1,301 关系/91 复核；二次 dry-run 全 0、健康 200 未重启。遗留：admin 口令轮换、表单-流程桥接（WF_WORK.WORKTABLE_ID/WORKRECORD_ID）专项、5 条 OA 草稿人工复核。
> 📌 2026-08-22 最终收口（139 完成，见 142）：用户于当日重新授权"将该会话和当前会话最后的一次未完成的任务授权你完成"后，由单一执行者完成 140 遗留修复（构建器分叉合并、140 勘误补记、被中断测试补完）与 139 S8–S12：平台实时计数重读→新备份（data_asset_pre_plan139_apply_20260822013602.dump，SHA-256 64f3e14e…）→dry-run 门禁（删除=0、正式 view-JOIN 新增=0、active recipe=0）→apply（+4 系统/+1,731 表/+24,021 字段/+592 声明 FK/+56 draft review/+40 draft recipe/+729 视图依赖）→二次 dry-run 全 0→四源抽查含 V_BLZLST 全命中→/health 200（仅资产导入，未重建镜像）。全量 pytest v2=914 通过/7 失败（全为 HEAD 既有，属 130 范围）；alembic 往返 PASS；业务源库 DML/DDL/EXECUTE=0；无 Git commit/push。授权记录与原停止令均保留于 `backend/_plan139_work/COORDINATION.md`。
> 📌 2026-08-22 收口（139 终止）：139 实际由两个并行 AI 会话执行至 S7 附近（四源元数据采集、本地资产包 `数据资产_四系统源端资产包/`、54 项纯逻辑测试完成；源库与平台零写入，无凭据或患者信息泄露；分层治理与诚实性核查通过）。备份提交 `c3a5d18` 曾于 08-21 22:23 被越权推送至 origin/master（内容秘密扫描干净，建议接受现状并记为执行纪律问题）。用户裁决终止后续：平台导入 S8–S12、资产包重建、聚合验证、执行者编号报告均不再执行；用户停止令与服务器 plan139-test 沙箱容器停用记录见 `backend/_plan139_work/COORDINATION.md`，全部核查结论见 `140_plan139四系统探查执行核查与收口报告.md`。两个分叉构建脚本（build_plan139_asset_package.py 与 plan139_build_package.py）如恢复执行须先合并。
> 📌 2026-08-21 用户裁决（138）：批准将 138 纳入 130 的统一开发和服务器发布窗口；考虑到数据流动难以完整分析，首期只做关系/血缘语义隔离、统一对象键、确定性表级静态依赖、证据及查询—指标—产品引用。复杂字段级、运行血缘、真实消费端、质量整改复验闭环和自动通知全部后置；方向不确定不连线，未解析项登记后继续。
> 📌 2026-08-19/20 补充（136→137 新来源与关系专项）：按 136 的只读、草稿先行、冲突不升正式门禁完成 HIS 视图关系摄取和平台收口；三个新来源因未安全注入受控只读凭据均记为 `BLOCKED_CREDENTIAL` 并跳过，未把阻断误记成功。口令不进入 Markdown/Git/日志，高权限账号不作为长期启用凭据。
> 📌 2026-08-13 补充（Dify AI 质控设计）：用户要求增加与内网 Dify 对接的数据质量分析界面。只读连通确认 8.83 到 `10.10.8.53` Dify 根入口 307、`/v1/parameters` 无 Key 返回 401；未使用 Key、未发送数据。130 已增加 U10、§2.4、S5、A21–A28：独立 AI 质控页面、手动单条/同域批量/批次摘要、安全 preview、三张任务结果表、Workflow JSON 契约、RBAC、SSRF/脱敏/幂等/审计及人工复核。Dify 不直连业务库，不接收 sample_data，不自动改 finding/规则或执行 SQL；Key 只存服务器受控文件。
> 📌 2026-08-13 补充（完成度盘点与目录收敛）：已只读核对实际目录、Git、隔离测试库和 8.83 生产。生产后端仍为 `data-asset:126p5-20260811223311`，前端为 `graph-pastel-20260812`，平台迁移 head=`h5c6d7e8f9a0`；129 四级字段图谱尚未生产发布。生产查询/指标/产品=27/48/54，关系/字段/表=537/89730/7766，配方=0、关系复核=3 且均 draft、负责人/术语=0。H7 的 2026-08-12 第二夜 success，当前 2/3。详见 130 §1–§4。
> 📌 2026-08-12 补充（129 图谱八次完善，本地）：按 `截图/images.jpg` 完成 Neo4j 式白底柔色关系图谱；前端支持业务系统→库（数据连接/Owner）→表→字段渐进下钻，表层展示正式表间关系，字段层展示数据类型、主键和关系键。Luna 实现前端、主 AI 复核并补后端字段契约与表关系边；前端 83 项测试、双 typecheck、build/gzip PASS，后端只读冒烟 PASS。隔离测试库因 `asset_data_products` 清理权限与历史脏数据未能完成全绿 pytest；未生产发布、未执行源库 DML/DDL。详见 129 §9。
> 📌 2026-08-11 补充（128 交接文档）：`128_126号查询指标闭环完成情况与接手交接.md` 汇总 126 P1–P5 生产事实、API/页面、未完成项与接手自检，供其他 AI 接收。
> 📌 2026-08-11 补充（126 P5）：看板/MCP 目录/多源能力/调度种子；`APP_QUERY_SCHEDULE_ENABLED=true` 且 27 条单调度全关；21 占位标题官方化；镜像/前端 `126p5-20260811223311`。详见 126 §23.6。
> 📌 2026-08-11 补充（126 P4 收口并生产升级）：数据产品表 `h5c6d7e8f9a0`；发布 `126complete-20260811220449`；查询 27/指标 48（27 active + 21 待补录占位）/产品 54/历史结果；镜像 `data-asset:126complete-20260811220449`。详见 126 §23.5。
> 📌 2026-08-11 补充（127 号 S9 续跑）：用户授权继续后完成测试库迁移往返 PASS、Review 流 A14/A15/A16 PASS（链接 formal、不升 candidate、draft 保持）、配方 dry-run+测试库导入幂等 PASS、pytest `620 passed/1 skipped`（排除 identity_nightly）、前端 typecheck+76 Vitest+build/gzip PASS。**仍未**生产发布、生产关系批准、生产配方 active、历史问题重建。详见 127 §17.3。
> 📌 2026-08-11 补充（127 号 S0–S8 执行）：按第 8/16/18 节已核实根因实施代码整改（不重复诊断）。质量 rules 分页、by-system 真实归属、metrics 数组、SQL TOTAL/ERROR、overview/charts、图谱 labelPlacement=center、relation-reviews 批准链接 formal（禁升 537/538）、配方导入修复、AI 取消虚假 executed。测试库专项与契约测试见后续 S9 条。
> 📌 2026-08-11 补充（127 号质量与治理功能整改计划）：生产平台、源码和生产图谱截图的只读复核确认，质量规则/总览/看板、资产总览、关系复核、关系配方、AI 协作和治理管理存在接口契约、状态模型、初始化与交互可用性问题；121 的历史自动化发布记录保留，但“节点名称可读”的用户视觉验收重新打开。详细根因、通用质量规则、关系确认建议、S0–S9、A1–A25 及与 126 的兼容边界见 `127_数据质量与资产治理功能复核整改执行计划.md`。
> 📌 2026-08-11 补充（126 P3 + 48 项导入）：生产迁移 `g4b5c6d7e8f9`；影响分析/JOIN 候选/调度表（默认关）；27 条核心制度 SQL→查询+指标已入库；专项 21 passed。调度未启用。详见 126 §23.4。
> 📌 2026-08-11 补充（126 P1+P2 已生产升级）：查询/指标资产闭环已落地并发布 8.83；迁移 `f3a4b5c6d7e8`；前端 `126p12-20260811213058`。
> 📌 2026-08-11 补充（126 P1 本地落地）：查询五表 `e2f3a4b5c6d7`、queryctl、技能路由、前端查询中心。
> 📌 2026-08-11 补充（126 号长期规划原文）：日常查询 SQL/指标闭环、固定 `取数` 工作区、Q1–Q8 已答复；先 SQL 后指标、自动门禁、结果默认可不存。
> 📌 2026-08-11 补充（名称一致性与 master 收口）：以系统总览 `system_name_cn` 为显示名称唯一基准，系统内统一使用 HIS、HRP、数据中心、嘉和电子病历、移动护理、Docare手术麻醉、LIS、PACS、无纸化病案、超声内镜；“业务系统”和“数据连接”保留为上下级不同维度，图谱筛选/分组/抽屉、目录、质量、元数据、运维等页面均改为中文显示名，物理编码保持稳定。ODS 镜像导入统一归 `DATA_CENTER` 并增加防反弹测试。最新隔离库全量 pytest `697 passed/1 skipped/0 failed`，迁移 `head→-1→head`，前端 `76 passed`、双 typecheck、build/gzip 预算 PASS；代码/构建提交 `e60c586`、`b8bb8f5` 已推送 `origin/master`。生产未发布、业务源库写入 0；H7 后续观察由用户自行跟进，不再阻塞本轮开发/Git收口。
> 📌 2026-08-11 补充（解除测试库与 R8 阻断）：合法隔离库 `data_asset_test` 经备份后完成最小所有权/权限修复，迁移 `head→-1→head` 和完整 pytest 连续两轮通过（每轮 692 passed/1 skipped）；同生产运行时的 Linux x86_64/Python 3.11 离线包含 56 个逐包哈希 wheel、pnpm 11.9.0 v11 store 和实际 Linux dist，前后端 `--network none` 从零安装/构建通过；全新 PostgreSQL 14 完成幂等升级、回退再升级及 `/health=200`。补齐 `python-multipart`/`openpyxl`/`et_xmlfile` 运行依赖，纯标准库离线门禁专项 43 passed/2 skipped。未发布生产，业务源库写入 0；该条历史阻断已解除。
> 📌 2026-08-11 较早阶段记录（Luna 低成本开发+主 AI 复核）：本地完成每日 HIS 职称必需子任务、关系配方在线编辑/版本查看/复制/JSON与SQL导出、按钮权限绑定和离线包失败关闭校验；主审修复整批事务/审计恢复、真实权限源、权限资源种子、身份/质量写接口后端细粒度授权、配方角色门禁、版本详情及连续构建旧哈希残留。安全专项 7 passed、职称专项 17 passed、离线校验 14 passed、前端 75 passed+双 typecheck+干净 build/gzip 预算 PASS。当时 `APP_TEST_DB_URL` 与 R8 实包演练仍 BLOCKED，已由上一条后续执行解除；生产发布/调度/数据库写入 0，H7 仍 1/3。
> 📌 2026-08-10 补充（用户授权 push + F10 apply + 历史签名补跑）：`6be5408` 已 push；F10 平台归并 apply（his_source→HIS、ods_ydhl→DATA_CENTER，备份 `data_asset_pre_f10_20260810195654.dump`）；历史签名 `batch-signature-20260810120039` inserted=15/failed=0/水位 committed；JHEMR 写 15、HIS 写 0。详见 `123` §6。
> 📌 2026-08-10 补充（121 G8 图谱生产发布 PASS + G7 全绿）：用户授权后完成前端原子发布 `g8-20260810192501-e1e24a6bf5cea97a`（入口 `index-CiBgcxMS.js`，图谱 `AdvancedRelationGraph-BBkJnBCT.js`，方向文案「全部方向/引用它/它引用」）；后端保持 `data-asset:signature125-20260810161506`；nginx `root` 指向 `frontend-dist/current`；`e2e_graph_acceptance` ACCEPTANCE PASS、`e2e_graph_g7_acceptance` **G7_ACCEPTANCE PASS**；previous=`pre-g8-flat-20260806`；业务源库写入 0；无迁移。详见 `121` §11.5。
> 📌 2026-08-10 补充（125 签名时区/150px 已发布并单账号完成）：H4 发布 `data-asset:signature125-20260810161506`（Pillow 12.2.0 + 时区/150px 代码）；H5 只读 dry-run 指纹 `a316d2a5892d` PNG542×195→JPEG150×54、目标空；H6 受控写入 success inserted=1、action executed rows=1、回读 4093 字节、水位未推进；HIS 写 0。H7 已完成 08-11 首夜，当前 1/3；历史批量补跑随后经单独授权完成 inserted=15/failed=0，不得重复执行。详见 `125` §13。
> 📌 2026-08-10 补充（123 收口执行）：隔离库 `data_asset_test` 迁移往返至 `d1e2f3a4b5c6`；安全写路由扫描/F2 角色矩阵/撤权立即失效/脱敏/known_hosts 测试落地；前端 typecheck+Vitest69+build 全绿；图谱基础 e2e PASS、G7 硬门禁 PASS（G8 后方向文案已切新版）；122 只读观察 host_cron+水位+单账号签名成功保留，历史补跑 NOT EXECUTED；F10 dry-run source=15/change=2/conflicts=7，apply 未授权；离线包因 PyPI/Docker 阻断 BLOCKED。业务源库写入 0。详情见 `123` §6.1–6.2。
> 📌 2026-08-10 补充：HIS 职称到 JHEMR 的一次性正式同步已完成并记录于 `124_HIS职称同步JHEMR执行与复用说明.md`。固定口径为 `FXHIS.SYS_EMPLOYEE.EMPLCODE=jhemr.users.user_id`，`LEVLCODE` 经 `PORTAL_USER.PORTAL_SYS_DICT(type_code='EmployeeTitle')` 取 `DICT_NAME` 覆盖 `education_title`；317 条差异已更新，提交后 2043 条匹配全部一致，HIS 写入 0。当前应用镜像未升级，独立每日职称子任务仍未发布，后续不得把一次性执行误记为长期调度完成。
> 📌 2026-08-09 补充：用户确认 119 号解决“能渲染”后，图谱仍存在节点方块化、名称不可读、方向/跳数/系统层级筛选混乱。生产只读核查同时发现每日人员主同步成功，但 JHEMR 签名子任务固定 16 条权限失败被 overall success 掩盖。120 保留为综合核查底稿；详细执行分别以 `121_关系图谱知识图谱式重构执行修复计划.md`、`122_每日人员同步任务状态审计与签名失败整改执行计划.md` 为模块子计划。未经再次授权不得发布生产、修改 cron/权限或补跑业务写入。本文件仍为唯一未完成事项总入口。
> 📌 2026-08-03 补充：另有一条并行轨道 `111_当前代码全面核查问题整改执行计划.md`（含 `113` 严格执行提示词）正在做代码安全整改，进度为 S0–S8 代码完成、S9 文档/Git 收口；仅本地整改+纯逻辑子进程测试，未触碰生产/业务库。本文件仍为唯一未完成事项执行入口。
> 📌 2026-08-04 补充：`116_系统剩余事项合并收口执行计划.md` 合并 85/97/100/101/111/112 的剩余工作，作为交给其他 AI 的统一执行子计划；本文件仍保留总入口、优先级和最终裁决权。
> 📌 2026-08-04 补充（111/113 执行）：S0–S8 代码经纯逻辑子进程测试验证全绿；S9 文档收口已完成（归档空目录、README 108 状态对齐、秘密扫描通过、目录更新记录登记）；全量数据库 pytest 因本机无隔离 `APP_TEST_DB_URL` 阻断，`requirements.lock`/断网演练缺目标/隔离环境阻断；未 commit/push/tag，生产/业务库零写。
> 📌 2026-08-04 补充（118 独立复核）：已修字典写后回读与 sequence 标识符校验；纯逻辑 22 passed/1 skipped。待处理清单见 118_111与112独立复核修复与待处理交接.md（U1–U22）；116 仍为合并收口子计划。本文件仍为唯一未完成事项入口。
> 📌执行入口：本文件是唯一的未完成事项执行计划。开始任何开发前，先读 `README.md`、`AGENTS.md`、本文件。
> 📌发布部署：发布门禁、离线部署、回滚和代码同步以 `58_发布部署与代码同步整改计划.md` 为唯一执行计划。
> 📌登录与接入：本地账号认证、RBAC 闭环与业务系统只读接入以 `59_用户登录与业务系统接入执行计划.md` 为模块执行计划；同步记录见 `61`。
> 📌最终封板：2026-07-15 起剩余 F1–F11 的详细执行步骤统一以 `85_系统最终封板与全量验收详细执行计划.md` 为准；不得重新执行已完成的 77–84 号探库和平台导入。
>
> **当前裁决优先级（2026-07-14）**：本文件下方“2026-07-14 用户裁决后的线性执行批次”覆盖旧 T1–T15 中尚未完成或已失效的描述；旧清单与日志仅保留历史证据。
>
> **历史复核发现（2026-07-11，逐行核实源码）**：
> - 大部分 P0/P1 代码改动**已完成**：Token 哈希存储（`q3d4e5f6a7b8` 迁移 + `governance.py:15`）、`admin/init` 公开端点已删、前端硬编码 Token 已删、未绑定 Token 兼容模式（`rbac_require_bound_token=False`；生产须显式设为 `true`）、ops 操作人服务端派生（`ops_tools.py` 用 `current_user`）、测试库强制（`conftest.py:12-14`）、前端三大契约修复（关系复核 `res.data?.items` + 标题正常、metadata diff 参数对齐、dict/medical 路由修正）、调度真实调用采集（`main.py` `_run_metadata_collect` 调 `_collect_metadata_snapshot`）、Nginx CORS 反射已删、`deploy/offline/` + `deploy/systemd/` 已建。
> - **当时列出的剩余工作**中，QualityFinding、Git 基线、资产归一、配方库最小版等后来已完成；2026-07-14 起只按 E1–E11 判断当前状态，禁止继续引用本段作为待办。
> - 已删除对 `46_文档完成状态总表.md` 的幽灵引用（该文件已归档且物理不存在）。

# 系统未完成事项统一执行计划（线性清单版）

## 0. 目标与硬边界

**目标**：从当前状态连续推进到可发布基线 + 数据治理闭环，不中途停顿、不分阶段交付。

**硬边界（全程不可违反，违反即回滚）**：
1. HIS/ODS/HRP 业务源库仅 `SELECT`，禁止任何写操作。
2. 密码/Token/身份证/姓名/电话/地址不进代码、日志、git、新文档。
3. 平台库写操作必须经审批+审计+白名单（`asset_action_executors`）。
4. 迁移手写 `upgrade()`/`downgrade()`，禁 `--autogenerate`；新表只在 `asset` schema，前缀 `asset_<模块>_<实体>`。
5. 前端必须用 pnpm；typecheck 同时过 `tsc --noEmit` 和 `vue-tsc --noEmit`；不改现有路径（`/relations/{id}/review`、`/dict-medical/*` 等）。
6. AI 工具不接入写操作执行器，只调只读端点。
7. 保留旧表不删：`asset_api_keys`/`asset_table_owners`/`asset_business_terms`/`asset_metadata_snapshots`。

**每步完成判定（5 条缺一不可）**：① 改动有 `文件:行号`；② 验收命令实测通过（超时/失败不得写成通过）；③ 涉及结构变更配手写 Alembic 迁移；④ 每步至少一个针对性测试；⑤ 更新 README 目录更新记录 + 本文件末尾执行日志。

## 2026-07-14 用户裁决后的线性执行批次（当前唯一执行序列）

> 执行顺序：`E1 → E2 → E3 → E4 → E5 → E6 → E7 → E8 → E9 → E10 → E11`。允许在不共享文件、不共享生产变更窗口时并行准备，但生产操作、数据库迁移和 Git 推送必须串行留痕。
  - 2026-08-01: Phase B code complete (107). 114 tests pass. Nightly=false. Awaiting Phase C review + Phase D controlled validation.
- 2026-08-01: Phase B P0/P1 fixes (107). Permission enforcement on trigger endpoints (identity.sync.trigger), retry logic, readback integration, watermark/row-conservation fuses, four-way diff module, alert dispatch, DBA privilege recommendation. 74 identity nightly tests pass. Nightly=false. Config cleaned (removed deprecated confirmation_token). Awaiting Phase C independent review + Phase D controlled validation.

### 2026-07-14 服务器发布后的真实剩余项

| 优先级 | 剩余工作 | 已有基础 | 完成判定 |
|---|---|---|---|
| P0 | 修复全量数据库测试 | 独立验收镜像已建；当前 `240 passed / 40 failed / 3 errors` | 补稳定测试种子、修订配方 `content_hash` 测试和旧状态机预期后，全量 pytest 通过 |
| P0 | E9 权限与审批角色矩阵 | API、页面、迁移已发布 | 匿名/普通用户/申请人/审核员/管理员/越权/自审禁止全部实测通过，审计链完整 |
| P0 | E10 配方真实闭环验收 | CRUD、版本、审核、SQL 预览代码已发布 | 新增→编辑→提交→审核→激活→停用、并发冲突和危险 SQL 拒绝均通过 |
| P0 | E11 浏览器与真实数据验收 | 统一资源树、系统页、图谱诊断已发布 | HIS/HRP/数据中心层级、懒加载、空/错/正常图、节点边详情在浏览器实测通过 |
| P0 | Git 收口和推送 | **PASS**：敏感扫描、分组提交、远端无分叉和 master 推送完成 | `e60c586`、`b8bb8f5` 已推送；本轮未生产发布，故未创建/移动 release tag |
| P1 | 生产 RBAC 与容器持久化 | 容器 healthy，迁移已到 `w9d0e1f2a3b4` | `APP_RBAC_REQUIRE_BOUND_TOKEN=true`；未绑定凭据 403；凭据卷和 Oracle Client 挂载经容器重建仍有效 |
| P1 | 离线可复现与断网演练 | pytest wheelhouse 已有，正式发布包尚未形成完整校验集 | Linux 锁文件、后端 wheelhouse、前端离线产物、SHA256SUMS 完整；干净断网环境从零部署成功 |
| P1 | HRP 直连与 WA_* 补采 | ODS.HRP 镜像和 52/64 口径已有 | 使用只读凭据完成字段补采/dry-run，缺口清零或形成不可获取清单；源库写入 0 |
| P1 | 系统历史归并正式 apply | dry-run 为 8 源、3 系统、0 冲突；`75` 已增强脚本 | 再次核对映射与数量守恒后仅写平台库执行 apply；保留回滚证据，不写业务源库 |
| P1 | 迁移往返验证 | 生产 head 曾至 `w9d0e1f2a3b4`；本地新增 `x0e1f2a3b4c5`（75） | 在独立测试库验证含 `x0e1f2a3b4c5` 的 upgrade/downgrade；禁止在生产试降级 |
| P0 | 75 系统连接/运维 SQL/字典 | S1–S6 代码已落地（见 `75` 执行日志） | 测试库 upgrade + 全量 pytest；前端已 typecheck；生产归并/字典 apply/写开关须再授权 |
| P0 | 126 查询+指标+P3–P5 治理底座 | **生产已发布 126p5**；看板/MCP/多源/调度基础设施齐；单调度全关 | 可选：单条调度启用；21 条 SQL 业务补录；深度多源适配 |
| P2 | 126 深度多源/看板增强 | P5 最小闭环已就绪 | 厂商适配器、契约回归、图表增强；非当前阻塞 |
| P0 | 127 数据质量与资产治理功能可用性整改 | S0–S9 测试门禁 PASS（620 pytest/76 vitest/build）；生产未发布 | 剩余：生产发布与 A25 视觉验收、生产 Review 批准、生产配方 active、历史质量问题重建（均须新授权）；含 identity_nightly 的完整 690+ 全量可选回归 |

> 已完成且不再重复：平台库生产备份与 upgrade、后端/前端发布、Nginx 与健康检查、基础 UI 人工验收、系统归并 dry-run、pytest 独立验收镜像、业务源库只读门禁。TLS 与人工凭据轮换按用户决定不列阻塞项。

### 一、用户裁决与边界

| 原事项 | 用户决定 | 当前处理 |
|---|---|---|
| T13 人工凭据轮换及签字 | 不需要 | 从发布阻塞中取消；不得伪造轮换记录。仍禁止把现有凭据写入代码、日志、Git 或文档 |
| HTTPS/TLS | 继续使用 HTTP | 从本轮阻塞中取消；记录“内网 HTTP 风险已由用户接受”，不得擅自配置证书或强制 Secure Cookie |
| Git push | 可以推送 | 已获得本次明确授权；执行前仍需秘密扫描、确认分支/tag/工作区和远端差异，禁止 force-push |
| Linux lock/wheelhouse/校验清单 | 可做 | 纳入 E4 |
| 干净断网 Linux 演练 | 可做 | 纳入 E5 |
| 全量验收 | 可做 | 纳入 E6 |
| HRP 直连与 WA_* 补采 | 可做 | 撤销“HRP 先不同步”，纳入 E7；源库永远只读 |
| HIS/ODS/周边/人员/夜间质量 | 可做 | 已完成的成果不重复写入或全量重跑；纳入 E8 做回归核验和缺口修复 |
| 前端人工视觉验收 | 已完成 | 标记完成，不再列待办；只有发现明确缺陷才修复 |
| 人员画像/审批流/按钮级权限 | 继续开发 | 纳入 E9 |
| 关系配方在线编辑/自动建视图 | 可做 | 纳入 E10；只写平台库，生成视图先预览/审批，禁止直接在业务源库 DDL |
| 运维写通道 | 可做但生产开关继续关闭 | 仅验证默认关闭和安全门禁，不启用生产写开关 |

### E1 工作区收敛与发布内容确认

1. 运行 `git status --short`、`git diff --stat`、`git log -5 --oneline`、`git tag -n`。
2. 审核当前未提交项：临时 `_browser_*` / `_debug_*` / `_hot_*` / `_continue_*` 脚本不得默认进入发布；71 号报告按 README 登记状态处理。
3. 对待提交文件执行敏感信息扫描；不得输出密文，只记录命中路径和整改结论。
4. 将需要保留的代码、测试、迁移、部署文件和文档按逻辑分组提交；临时脚本归档或保持忽略，不得混入生产提交。

**验收**：工作区 clean；`git diff release-20260713..HEAD --stat` 已人工审阅；无凭据进入 Git；记录最终 HEAD SHA。

### E2 推送分支与发布 tag（用户已授权）

1. 先 `git fetch`，确认远端没有需要人工裁决的分叉。
2. 非快进、远端领先或冲突时停止，禁止 rebase/force-push 擅自改历史。
3. 推送当前分支；若 `release-20260713` 已指向旧基线，不移动旧 tag。对新增功能应创建新的不可变 tag（建议 `release-20260714`），完成验收后再推送新 tag。
4. 记录远端分支 SHA、tag SHA 和推送时间，不记录凭据。

**验收**：本地 HEAD 与远端目标分支一致；远端 tag 可解析；`git status` clean。

### E3 生产安全配置与运行时持久化

1. 设置并验证 `APP_RBAC_REQUIRE_BOUND_TOKEN=true`，重启服务。
2. 使用未绑定 Token/JWT 调用受保护写接口，必须返回 403；有效管理员凭据按权限返回成功。
3. 用只读挂载持久化 `/etc/data-asset/credentials`；用既有脚本持久化 Oracle Instant Client 19.1 链接与容器启动参数。
4. HTTP 保持不变；`APP_AUTH_COOKIE_SECURE` 必须与 HTTP 部署兼容，不得误设导致 Refresh Cookie 丢失。记录用户已接受内网 HTTP。
5. 运维写通道三个生产开关保持关闭，增加启动/冒烟断言，验证业务源库连接器只有只读能力。

**验收**：容器完整重建一次后 ODS/HIS 只读连接仍正常；未绑定凭据写接口 403；登录/刷新正常；运维写开关均为 false。

### E4 生成 Linux 可复现离线依赖包

1. 在与目标环境兼容的 Linux 构建机生成锁文件（含哈希）、wheelhouse、前端 pnpm 离线 store 和构建产物。
2. 生成文件清单、版本信息和 `SHA256SUMS`；wheelhouse、store、dist 不进入 Git。
3. 校验 Python、Node、pnpm、CPU 架构与目标服务器兼容。

**验收**：锁文件可复现；所有离线文件校验通过；无公网依赖遗漏；产物位置和哈希摘要进入发布记录但不写凭据。

### E5 干净断网 Linux 环境完整演练

1. 使用全新 VM/容器，断开公网后严格按 `deploy/offline/README.md` 从零安装。
2. 初始化/迁移平台库，安装后端、前端和 Nginx；使用脱敏测试配置，不连接或写入业务源库。
3. 验证 `/health/live`、`/health`、首页、登录、RBAC、回滚和二次安装幂等性。

**验收**：断网状态从零部署成功；健康检查 200；失败步骤可回滚；输出命令摘要、耗时、包哈希和问题清单。

### E6 全量代码与发布验收

1. 后端：独立测试库运行全量 pytest；失败必须分类为本轮回归、历史用例或环境问题，禁止把失败写成通过。
2. 数据库：`alembic upgrade head`，并对本轮新增迁移验证 downgrade/upgrade。
3. 前端：`pnpm run typecheck`、`pnpm run build` 和现有前端测试。
4. 安全：敏感串、公开初始化端点、生产 Mock、危险 CORS、业务源库写能力扫描。
5. 运行时：登录、刷新、权限、资产树、关系图、质量、人员、配方库、运维开关冒烟。

**验收**：所有必须项全绿；如存在经用户接受的遗留，逐项记录风险、证据和责任边界。

### E7 HRP 直连与 WA_* 核心字段补采

1. 通过既有安全渠道获取/挂载 HRP 只读凭据，禁止写入仓库和报告。
2. 先做连通性与只读事务验证，再按 `52`/`64` 既有范围补采 WA_* 核心表、字段、索引、约束和必要样本统计。
3. 大表只采元数据或限定样本；设置超时、最大行数并留审计。
4. 与 ODS.HRP 镜像和现有 HRP 资产包做差异对比，更新机器可读资产包与报告。
5. 本轮“可做”不等于允许人员自动入库；HRP 人员同步必须先 dry-run，平台库 apply 仍需单独审批。

**验收**：源库零写；WA_* 既定字段缺口清零或形成明确不可获取清单；资产包可复现；敏感数据不落报告。

### E8 已完成数据链路回归核验

1. 不重复全量人员 apply，不重复无意义全库探查。
2. 核验 ODS/HIS/周边数据源连通、HIS 人员统计、L13–L16、02:00 质量任务和最新 run 记录。
3. 核验 71 号 EXAM_NO 纠错口径已进入当前关系资产与应用展示，避免继续沿用抽样误判。
4. 仅修复回归缺陷或明确数据漂移，并补测试/证据。

**验收**：各链路状态、最后运行时间、数量摘要和异常均可追溯；业务源库零写。

### E9 人员、审批与权限增强

1. 以 `43` 为设计入口，完善人员画像、来源与多科室展示、差异到主档的审计链。
2. 完成权限审批流、角色/资源持久化、按钮级权限和数据范围权限；后端默认拒绝，前端隐藏不能代替后端授权。
3. 补权限矩阵测试：匿名、普通用户、审核员、管理员、越权和未绑定 Token。
4. 所有平台库写入经审批、审计、幂等和回滚设计；不写业务源库。

**验收**：权限矩阵全绿；审批前不可执行；审计可关联申请人/审批人/执行人；数据范围越权被拒绝。

### E10 关系配方库扩展与受控视图生成

1. 以 `54` 和 `53` 为入口，实现配方新增、编辑、版本、审核、停用、检索、详情和图谱联动。
2. 在线编辑只写平台库配方元数据，必须保留版本和审计；AI 仍只读获取上下文。
3. 自动建视图拆为“生成 SQL → 方言/只读源校验 → 预览/影响分析 → 人工审批 → 受控目标执行”。默认只生成 SQL，不直接执行。
4. 严禁在 HIS/ODS/HRP 等业务源库执行 DDL；如未来需要在专用目标库建视图，必须新增目标白名单、独立账号、审批和回滚，且需用户再次明确授权。
5. 补 JSON Schema、重复 recipe_id、版本并发、危险 SQL、越权编辑和审计测试。

**验收**：配方 CRUD/版本/审核闭环可用；未审批不能执行；业务源库零 DDL/DML；生成 SQL 可下载/复制并有证据来源。

### E11 业务系统、数据资源与关系图谱一体化整改

> 2026-07-15补充：系统总览、资源树、表资产分类、中文名称治理和统一关系复核中心已按89号完成并部署。当前剩余只按85号执行浏览器角色矩阵、离线/Git等封板门禁。

1. E9、E10、E11 的代码、迁移和服务器发布已按原74号计划完成；原43/51/54/74/75/76号实施计划已归档为历史快照，不再作为执行入口。
2. 剩余浏览器角色矩阵、真实数据交互验收、归并apply和最终Git收口统一执行 `85_系统最终封板与全量验收详细执行计划.md`。
3. AI生成视图SQL按87号现行规范，三甲复审跨系统关系按88号用户裁决执行。
2. 当前一级业务系统按真实目标数据库 IP 只保留 HIS、HRP、数据中心。
3. 数据中心内部 LIS/PACS/YDHL/SM/JHEMR/CDA 等只作为 Owner/Schema/业务域，不作为一级系统。
4. 合并系统总览和数据源与库，统一系统 → 连接/库 → Schema/Owner → 表 → 字段层级。
5. 系统页支持安全配置连接信息；密码只写不回显，只保存安全凭据引用。
6. 修复关系图谱空白、错行、错误归组，补充任务模式、图例、帮助和浏览器验收。

**验收**：满足 72 第 12 节全部条件；系统/连接/表字段关系数量迁移前后守恒；业务源库零写；后端测试、迁移、前端 typecheck/build 和浏览器验收通过。

### 本批次完成后的统一收尾

1. 更新 README、55/58/59/65/66/72 和对应模块计划状态。
2. 写入执行日志、测试摘要、生产变更、远端 SHA/tag、离线包哈希和遗留风险。
3. 只有 E1–E11 验收全部完成或遗留已获用户书面接受，才把 55 标记完成。

---

## 线性执行清单（T1 → T15，连续推进，不要跳步）

### T1 验证已完成的 P0 安全改动跑得通（无新代码，先建立可信基线）

**背景**：上一轮已做大量 P0 代码改动，本步只验证它们真的生效，不写新代码。
**要做的验证**：
1. `git status` 审阅，确认未提交改动清单（记录下来，T2 处理）。
2. 启动后端，确认 `GET /api/v1/admin/init` 返回 401（不再是公开）。
3. `grep -rn "asset-platform-admin-2026" backend/ frontend/ deploy/` → 必须无结果。
4. `grep -rn "admin123" backend/ deploy/` → 必须无明文（`.env.example` 用占位符）。
5. `grep -rn "Access-Control-Allow-Origin.*http_origin" deploy/nginx.conf` → 必须无结果。
6. `cd backend && .\.venv\Scripts\python.exe -m alembic upgrade head` → 必须无报错（含 `q3d4e5f6a7b8` token_hash 迁移）。
7. 配置 `APP_TEST_DB_URL`（库名必须含 `test`），`cd backend && .\.venv\Scripts\python.exe -m pytest tests/ -q` → 记录通过/失败。未配置测试库时 pytest 必须拒绝运行（这是设计，不是 bug）。
**产出**：在执行日志记录 7 项验证结果。
**判定**：全绿则进 T2；若任一项红，先修到绿再进。

### T2 固化 Git 基线

**现状**：master 分支 backend/frontend/文档大量未提交（merge 后未整理），是当前唯一未完成的 P0 硬阻塞项。
**要做**：
1. 再次 `git status` + `git diff --stat`，确认无明文凭据混入。
2. 按逻辑分组提交：后端代码、前端代码、迁移、测试、文档、资产包分别 commit（conventional commits 格式）。
3. 在 `README.md` 目录更新记录追加：基线 commit SHA + T1 的 7 项验收结果。
**验收命令**：`cd F:\python\数据资产 && git status` → 必须 clean；`git log -1 --format="%H %s"` → 记录 SHA。
**判定**：工作区 clean，基线 SHA 已记录。

### T3 合并 QualityFinding 双状态字段

**现状（真实剩余）**：`backend/app/models/quality.py:56,57` `QualityFinding` 同时有 `status`（默认 `open`）和 `rectification_status`（默认 `open`）——冗余状态模型，是 P1 唯一未做的代码改动。
**要做**：
1. 保留 `status` 作为唯一整改状态字段（值域：`open/assigned/rectifying/resolved/verified/closed`），移除 `rectification_status`。
2. 全仓 grep `rectification_status`，找出所有引用（model、API、schema、前端），统一改用 `status`。
3. 手写 Alembic 迁移：① 把 `rectification_status` 非空且与 `status` 不同的值回填到 `status`；② 删除 `rectification_status` 列。迁移必须有 `downgrade()`。
4. 补 pytest：校验状态流转、旧数据迁移正确。
**验收命令**：`cd backend && .\.venv\Scripts\python.exe -m pytest tests/ -k "quality" -q` 过；`alembic upgrade head` 无报错；`grep -rn "rectification_status" backend/ frontend/` 无残留。
**判定**：单一状态模型生效，迁移可进可退。

### T4 补强只读 SQL 执行的数据库级防护

**背景**：当前只读校验在应用层，缺数据库级兜底（AGENTS.md 约束 6：大表禁全扫；安全红线：源库只读）。
**要做**：
1. 在 `backend/app/services/db_connectors.py` 所有连接器：开启只读事务（PG `BEGIN READ ONLY` / Oracle `SET TRANSACTION READ ONLY`）、语句超时（PG `statement_timeout` / Oracle `call_timeout`）、最大行数限制。
2. SQL 方言校验：拒绝 DML/DDL/`SELECT INTO`/`COPY`/多语句/`LOCK`/无 `WHERE` 的大表扫描；只放行 `SELECT`/`WITH`。
3. 补 pytest：写操作被拒、超时被中断、无 WHERE 大表扫描被拒。
**验收命令**：`pytest tests/ -k "readonly or sql_guard" -q` 过。
**判定**：DB 级只读 + 超时 + 方言校验三重防护生效。

### T5 元数据快照按 source_code 隔离与索引

**背景**：`backend/app/api/v1/metadata_changes.py` 快照对比当前未严格按 source_code 隔离，多源场景会串。
**要做**：
1. 快照表结构化保存 `source_code`（若现存在 JSON 里，迁移为独立列 + 索引）。
2. `GET /sources/{source_code}/snapshots` 与 diff 按源过滤。
3. 补多源 pytest：两个 source 的快照互不串。
**验收命令**：`pytest tests/ -k "snapshot" -q` 过；`alembic upgrade head` 无报错。
**判定**：多源快照隔离生效。

### T6 离线部署干净环境演练

**背景**：`deploy/offline/README.md` + `deploy/offline/init_db.sql` + `deploy/systemd/data-asset.service` 已建，但未在干净环境验证可复现。
**要做**：
1. 审阅 `deploy/offline/README.md` 三段流程：① Linux wheelhouse（`pip download -r requirements.txt -d wheels/ --platform manylinux2014_x86_64 --only-binary=:all:`）；② pnpm 离线 store（`pnpm fetch` + `pnpm install --offline`）或直接预构建 `frontend/dist`；③ `init_db.sql` 创建并授权 `asset` schema + `alembic upgrade head`。
2. 补齐 `deploy/offline/README.md` 缺失步骤（重点：`asset` schema 必须由 DBA 预建并授权；systemd 用 `User=dataasset` 非 root）。
3. 在干净容器/VM 演练一次，记录每步实际命令与输出摘要到 `deploy/offline/README.md` 末尾"演练记录"。
**判定**：无公网环境可从零部署到 `/health` 200 + 前端可访问。

### T7 ODS/HIS/HRP 资产归一优化包生成

**历史依据**：归档 `51_ODS_HIS大类归一与数据中心二次优化改造计划.md`；当前验收统一以85号为准。
**要做**：
1. 基于 `08_数据中心元数据快照.json`、`数据资产_HIS源端资产包/`、`39/40` 关系分级、`52_HRP源端资产复核报告.md` 生成 `数据资产_ODS_HIS归一优化包/`，**不覆盖原证据包**。
2. 表/字段/关系均携带 `system_category/source_system/source_database/schema_name`。
3. ODS 按 HIS/LIS/PACS/EMR/YDHL/SM/CDA/other 拆分；HIS/HRP 源端按 owner 和业务域展示。
4. **D 类跨系统关系**（`39/40` 定义）只进 `cross_system_pending` 图层，保留证据和待验证说明，不进治理正式导入和自动推导——**这条是用户确认的硬规则，不可改变**。
5. 后端接口返回归一字段，前端不得硬编码分类规则。
**判定**：生成摘要可复现；资产页支持五层导航（系统大类→系统/库→schema/owner→表→字段）；图谱能区分正式/候选/跨系统待验证三类图层。

### T8 关系口径库与视图配方库（最小只读版）

**历史依据**：归档 `54_关系口径库与视图配方库系统整改方案.md`；配方表、版本状态机及SQL预览已落地，当前AI SQL规范以87号为准。
**要做**：
1. 新建 model + 手写 Alembic 迁移建 `asset_relation_recipes` 表（`asset` schema，遵守约束 4）。
2. 导入 `view_relation_recipes.json`（53 号检查影像口径是首批种子）。
3. 提供只读 API：列表、详情、按表/业务域/视图检索。
4. 前端新增查看页，图谱和表详情可跳转相关配方。
5. AI 上下文只读 `formal/user_confirmed/verified` 配方；`forbidden/deferred/candidate` 必须随风险说明返回。
6. **本步不做**：在线编辑、创建视图、自动执行验证 SQL。
**验收命令**：`alembic upgrade head` 成功；`pytest tests/ -k "recipe" -q` 过；`pnpm run build` 过。
**判定**：53 号检查影像配方可导入、检索、展示、被 AI 上下文读取。

### T9 HIS 人员/科室同步 dry-run

**背景**：代码和 mock 测试已存在，生产路径待验证（AGENTS.md：源库只读）。
**要做**：
1. 在内网目标机执行 HIS 同步 dry-run（不写库），记录桥接率、`DOCTOR_GROUP.DOCTOR_USER` 语义和值域。
2. 产出 dry-run 报告（编号续当前最大 +1）。
**判定**：源库零写操作；dry-run 报告有桥接率与异常清单。

### T10 HRP 核心字段补采

**依据**：`52_HRP源端资产复核报告.md`（已读：真实业务表约 5851 张，`WA_*` 8 张核心表字段待补采）。
**要做**：
1. 按 52 号报告补采 HRP 423 张缺字段业务表，优先 8 张 `WA_*` 核心表。
2. 以 `BD_PSNDOC`/`HI_PSNJOB`/`ORG_DEPT`/`SM_USER` 接入 HRP 人员、任职、科室、账号。
3. 先产生差异报告，再人工确认合并。
**判定**：源库零写操作；HRP 核心字段齐全；差异可比较。

### T11 平台库人员/科室 upsert（经审批）

**前置**：T9 dry-run 通过 + 用户审批。
**要做**：
1. 审核后才执行平台本地库 upsert（只写平台 PostgreSQL，不写业务源库）。
2. 验证多科室、来源表、审计记录、幂等性。
**判定**：平台库 upsert 可追溯、可重跑幂等；多科室和来源可在人员画像查看。

### T12 质量规则生产夜间验证

**背景**：文档记录已完成一次只读实测，需补覆盖与生产夜间验证。
**要做**：
1. 在夜间对 HIS 主数据、就诊、医嘱、检验、检查、费用、药品跑只读规则；大表严格用 `ROWNUM <= N` 或子查询限定（约束 6）。
2. 补规则按系统/范围过滤、任务调度、失败告警、按系统聚合看板、去重策略。
3. 每次运行产出可追溯的 run、规则、脱敏样本、finding（用 T3 统一后的单一 status）、整改状态。
**判定**：每次运行有完整 run 记录；无全表扫描；样本已脱敏。

### T13 凭据轮换确认（历史任务，2026-07-14 用户已取消）

**背景**：`admin123` 数据库密码与 `asset-platform-admin-2026` Token 已进 git 历史，代码删除无法撤销已发生泄露。
**要做**：
1. 由 DBA 轮换 PostgreSQL 密码（本步只**记录轮换完成日期**到执行日志，实际操作由人完成）。
2. 由运维确认所有旧固定 Token 已禁用，新 Token 经 `create_admin_token.py` 生成。
3. 由 DBA 预置 SSH `known_hosts`（供 T4 只读连接器严格校验，不再 `AutoAddPolicy`）。
**判定**：执行日志记录三项人工操作的完成日期与负责人。

### T14 全量验收

**要做**，实测记录，不得美化：
```
cd F:\python\数据资产 && git status                                    # clean
cd backend && .\.venv\Scripts\python.exe -m pytest tests/ -q           # 记录通过/失败数
.\.venv\Scripts\python.exe -m alembic upgrade head                    # 无报错
cd ..\frontend && pnpm run typecheck                                  # tsc + vue-tsc 都过
pnpm run build                                                        # 成功
grep -rn "asset-platform-admin-2026\|admin123\|rectification_status\|http_origin" backend/ frontend/ deploy/   # 全空
```
**判定**：五项全绿，执行日志记录每项实测结果。

### T15 文档与状态收尾

**要做**：
1. `README.md` 目录更新记录追加：本批完成项、最终基线 SHA、T14 验收结果。
2. 本文件顶部状态横幅改为 `🟢已完成`，并在执行日志记录完成日期。
3. 确认无孤儿文件、无幽灵条目（Glob 比对 `开发起步包/*` 与 README）。
4. P3 延后事项（SQLGlot/SchemaCrawler/tbls/Soda、超大图布局、UI 视觉增强、运维写通道生产开关）在 README 单列"后续可选"，不纳入本轮。

---

## 待裁决项（执行中遇到再处理，不阻塞线性推进）

| 项目 | 规则 |
|---|---|
| D 类跨系统关系 | 只进 `cross_system_pending` 图层，不进正式推导/治理导入；任何改变需用户确认（T7 已遵守） |
| 运维写通道 | 三重门禁默认关闭；2026-07-14 用户决定本轮继续关闭，只做拒绝与审计验证，不得打开 |
| HRP 表数口径 | 以 52 号报告为准（真实业务表约 5851 张，非早期的 76057） |

---

## 执行日志（执行 AI 逐项追加，格式）

### [103–107 HIS 人员夜间同步复核收口] 2026-08-01

- 当前模块总入口：`103_HIS人员向无纸化与电子病历夜间同步执行计划.md`；JHEMR 新用户专项唯一执行入口：`107_JHEMR新用户登录控制角色科室与密码同步修复开发计划.md`；独立证据：104–106。冲突结论按 `107 > 106 > 104 > 103 > 105`。
- E1-E4 状态归一、SYS_EMPLOYEE owner、人员组科室来源代码已修复并有纯逻辑测试；平台既有人员/科室主档仍需 dry-run 对账后全量重刷。
- 未完成硬阻断：JHEMR 生产租户键、CDMS FTYPE=8/32 与模板列语义、增量水位/守恒采集、外包与冲突分类、目标只读采集器、纳管关系、专用写策略、单账号原子事务、补偿队列、审计秘密剔除、分布式调度锁。JHEMR 密码算法阻断已由 106 号解除；代码实现、独立复核、一次性受控验证、熔断和告警演练完成前，生产夜间开关保持关闭。
- 最终运行口径（用户 2026-08-01 裁决）：目标是 HIS→CDMS 与 HIS→JHEMR 每天夜间无人值守自动同步，正常批次不逐人核对、不实行双人审批、不使用 confirmation token。Phase B 允许完整开发自动采集、预检、计划、业务库 apply、回读、对账、重试和告警能力，但本轮开发/复核 AI 不实际执行生产业务库 DML/DDL，也不启用夜间开关。首次上线完成独立代码复核、一次性受控验证和运维启用后，后续正常夜间批次自动运行；冲突人员、保护名单、人工维护记录、超阈值批次和破坏性纠错自动隔离，不得随普通夜间任务处理。
- 用户后续裁决与角色链复核：CDMS 护士用护理质控、医师/药师用医疗质控，基础字段按两个参考账号共同值；JHEMR 完整字典链确认医师/药师=`001`（住院医师）、护士=`002`（责任护士），护理参考账号当前 `001` 为错配，三类均不写直接角色。外包全排除，人员组只纳入住院医师/病房护士，停用后不自动恢复。CDMS 密码可运行时读取覆盖 1692/1727 的共同密文模板；JHEMR 参考账号两密码字段均为空且 `user_pwd_sm` 3618 条全唯一，仍需单账号登录初始化验证。
- 写账号准备：JHEMR 专用账号名已确定，仍需按 103 §12 授最小表权限并将轮换后的密码写入 0600 凭据文件；CDMS 仍需 DBA 创建专用 Oracle 账号并授权。两个目标均不得授 DBA/RESOURCE/DELETE，不得用生产 DML 探测权限，写开关继续关闭。
- 2026-08-01 最终修订：HIS→CDMS 与 HIS→JHEMR 为并行独立目标，均按“每日自动同步、异常隔离、全程审计”建设。107 的 JHEMR 专项负责完整建号/更新链路及登录控制三表；常规夜间任务由服务端调度身份触发，不要求逐批授权。账号 HMAC 指纹和动作哈希继续用于幂等、审计和防篡改，而非人工审批凭证。

```
### [T1] 2026-07-12
- 验证结果：① git status: 205 行改动（dirty） ② /admin/init: 403（非公开） ③ grep token: 空
-              ④ grep admin123: 仅 backend/.env（未跟踪，T13 人工轮换） ⑤ nginx cors: 空
-              ⑥ alembic: 无报错（已在 head q3d4e5f6a7b8） ⑦ pytest: 按设计拒绝（无 APP_TEST_DB_URL）
- commit: 未提交（T2 待处理）
- 遗留：backend/.env 含历史密码，待 T13 人工轮换

### [T2] 2026-07-12
- 阻塞：工作区 205 行未提交改动，含多协作者的非本轮变更，无法安全代为 commit。
- 按规则"遇到阻塞如实记录并继续"，跳过 T2，T7-T12 也因此缺少基线前置。
- commit: 无

### [T2 发布基线] 2026-07-13
- 用户授权「完成发布线」。秘密扫描：临时 `_ro_*` 脚本 gitignore；剔除误入的 offline wheels/tar；`.env` 未跟踪。
- 分组 commit：backend `2e11627`、frontend `c717870`、deploy `dfb7715`/`9b1da27`、docs `a10f22a`。
- `git status`：**clean**；annotated tag **`release-20260713`** → `a10f22a19d7eaf24a7fec12c2ce03b4021b7006e`。
- **未执行** `git push` / `git push --tags`（需用户再授权）。
- 证据：`65_发布门禁与凭据轮换清单.md`。

### [T3] 2026-07-12
- 合并 QualityFinding.status 与 rectification_status：model 删列、新增迁移 r4e5f6a7b8c9（回填→删列，含 downgrade）、API assign/recheck/update 改为单一 status、schema/service 同步、前端合并列与筛选项。
- 迁移 head: r4e5f6a7b8c9；compileall 通过；前端 grep rectification_status 清零。
- commit: 未提交（T2 阻塞）

### [T4] 2026-07-11
- 改动：`backend/app/services/db_connectors.py` 增加只读 SQL 方言校验、Oracle/PG 只读事务、各连接器超时与 10,000 行上限；新增 `backend/tests/test_db_connectors_readonly.py`。
- 验证：`python -m py_compile app/services/db_connectors.py tests/test_db_connectors_readonly.py` 通过；`pytest --noconftest tests/test_db_connectors_readonly.py -q` 为 8 passed。标准 pytest 因未配置 `APP_TEST_DB_URL` 被测试隔离保护拒绝，未连接业务源库。

### [T6] 2026-07-12
- 阻塞：Windows 开发环境无干净 Linux VM/容器可执行离线部署全流程演练。
- 产出：deploy/offline/README.md + init_db.sql + systemd 服务文件在之前已完成；离线 wheelhouse、pnpm store 需在内网环境操作。
- 按规则"阻塞如实记录并继续"，跳过 T6。

### [T7] 2026-07-12
- 生成 `数据资产_ODS_HIS归一优化包/`：工具 `tools/build_normalized_package.py`，产出 normalized_tables.csv(2099表)、columns.csv(46725字段)、relationships.csv(33)、cross_system_pending_relationships.csv(47 D类)、normalized_catalog.json。
- ODS 865 表分拆到 10 个 source_system（HIS 273/EMR 79/CDA 86/LIS 11/PACS 18/YDHL 31/SM 25/Portal 139/HRP 21/other 182）；HIS 源端 1234 表；D 类关系独立图层不混入。
- commit: 未提交（T2 阻塞）

### [源库只读连通] 2026-07-13
- 用户授权：源库只读操作可核查。证据见 `62_源库只读连通核查报告.md`。
- 8.83 直连 ODS `10.10.8.216/orcl`（ods）与 HIS `10.10.10.15/his`（ready_his）SELECT 成功；修复容器 Instant Client 19 链接（原 libclntsh→11.2 导致 DPY-3010/DPI-1050）。
- 平台登记 `ods_8_216` / `his_source_10_10_10_15`；凭据仅服务器 `/etc/data-asset/credentials/`；OracleConnector + 只读守卫实测通过。
- 注意：ODS 账号 DB 侧含 INSERT/UPDATE/DELETE 等特权，平台强制 SELECT-only；容器无卷挂载，凭据与 symlink 重建会丢。
- 业务源库零写。

### [L10/L11 元数据与人员 dry-run] 2026-07-13
- 证据：`63_L10元数据采集与L11人员dryrun报告.md`。
- 修复 `metadata_collector`（schema_filter + 批量采列）与 `his_identity_sync`（可选 SYS_EMPLOYEE、source_code 对齐）。
- ODS snapshot 3：540 表/19189 字段；HIS snapshot 4：1237 表/19912 字段。
- HIS dry-run：科室 816、人员 4222、医嘱组匹配 1148/4；identity 表 0 写入；源库零写。

### [L11 apply + L12 HRP 部分] 2026-07-13
- 证据：`64_HIS人员入库与HRP补采报告.md`。
- 用户授权后 HIS identity **apply 成功**：persons 4260 / depts 816 / sources 6690 / person_depts 7831；去重修复 DOCTOR_GROUP 唯一约束；源库零写。
- HRP：`10.10.10.23` 端口 OPEN、凭据缺失；ODS.HRP 镜像补采 21 表/928 字段；WA_* 待 HRP 只读账号。
- T9 平台 upsert：**已完成**；T10 HRP 字段：部分完成。

### [业务线 L13–L15] 2026-07-13
- 证据：`67_业务线L13_L15执行报告.md`。用户要求开发业务线；**HRP 跳过**。
- L13：identity_review + API/UI；open 差异 2054；不自动覆盖主档。
- L14：周边 5 逻辑源登记并 ODS live 元数据（LIS/PACS/EMR/YDHL/SM）。
- L15：质量 run_id=1（1426 findings）；夜间 job `0 2 * * *` 登记；scheduler 支持 quality_check。
- 源库零写。

### [导航导入 + L16 + 筛选 + 热更] 2026-07-13
- 用户要求继续未完成开发。
- 导入：`scripts/import_normalized_to_platform.py`（按 schema+table 唯一键 upsert）→ 平台 `asset_tables=2099`、`asset_columns=46725`、`asset_relations=47`（既有正式关系保留，未覆盖）。
- L16：`POST /identity/sync-diffs/{id}/propose-master` + 变更请求审批/execute 写 `IdentityPerson`；前端差异页「提出主档变更」+ 变更请求列表。
- 筛选：质量 findings `run_id`；身份差异 `diff_type`。
- 部署：8.83 后端热更 + 前端 dist + 容器 healthy；业务源库零写。
- 验收：`pnpm typecheck/build` 通过；导入脚本 exit 0。

### [L16 科室 + L10-diff + R-MOUNT] 2026-07-13
- L16 扩展：`apply_department_master`；前端人员/科室均可提出主档变更。
- L10-diff：`69` — live 快照 540 vs 08 的 865；仅 live 多 2 张 HIS 表；核心 HIS/CDA 等对齐。
- 质量 UI：检查批次行点击 → findings 按 run_id。
- R-MOUNT：脚本安装到 8.83 `/etc/data-asset/scripts/`；容器 `libclntsh.so → 19.1`。
- 调度：`asset_scheduler_jobs` id=1 quality_check `0 2 * * *` registered。
- 烟雾：tables 2099 / open_diffs 2054 / persons 4260 / q_runs 1。

### [浏览器验收修 bug] 2026-07-13
- 证据：`70_浏览器验收与缺陷修复报告.md`。
- B1 首页 `dashboard/summary` PG GROUP BY 500 → 修复；复测 200（2099/46725/4260…）。
- B2 `/api/v1/health` 别名（Nginx 前端可达）；B3 健康检查不阻塞 KPI。
- B4 分页 `current-change/size-change`；B5 树搜索父节点穿透。
- 热更 8.83 后端+前端 dist；源库零写。

### [树性能 + L16 批量] 2026-07-13
- 树：`GET /assets/tree` 默认 `include_tables=false`，仅 schema 计数；`/assets/tree/tables` 懒加载；`/assets/tree/search` 表名搜索。
- 前端：展开/点击 Owner 加载表；搜索结果独立分区。
- L16：`batch-propose-master` / `batch-status` / `batch-approve` / `batch-execute`；前端多选操作条。
- 复测：tree 骨架 ok(9 源节点)；tables/search 200；空批量 400。

### [T9] 2026-07-12
- 阻塞：Windows 开发机无法经跳板机连接 HIS 源库执行 dry-run。
- 代码就绪：his_identity_sync.py + sync_executor.py + test_his_identity_sync.py 已完成；需在内网环境执行并产出报告。
- 按规则"阻塞如实记录并继续"。
- 2026-07-13：**dry-run 已在 8.83 完成**（`63`）；剩余为业务复核、异常清单与审批后平台库 upsert（不写源库）。

### [T10] 2026-07-12
- 阻塞：Windows 开发机无法经跳板机连接 HRP 源库采集 WA_* 等核心表字段。
- 工具就绪：harvest_hrp_source_assets.py / build_hrp_review_pack.py 已完成；需在内网执行。
- 按规则继续。

### [T11] 2026-07-12
- 前置未满足：T9 dry-run 未执行 + 用户未审批。代码 upsert 链已就绪（sync_executor.py + his_identity_sync.py），待 T9 产出报告后启动。

### [T12] 2026-07-12
- 阻塞：需 HIS 源库夜间只读执行质量规则。
- 代码/规则已就绪（quality checklist in main.py scheduler + hospital rules seed），需内网定时调度。
- 按规则继续。

### [T14] 2026-07-12
- 五项实测：① alembic upgrade head: PASS（r4e5f6a7b8c9 + s5f6a7b8c9d0 正常）② pytest: 按设计拒绝（无 APP_TEST_DB_URL）③ typecheck: PASS ④ build: PASS（31.89s, 226MB）⑤ grep: 仅 backend/.env（admin123, T13待轮换）+ 迁移含 rectification_status（历史迁移+新迁移，符合预期）；deploy 全空。
- git status: dirty（T2 阻塞未解决）。

### [T15] 2026-07-12
- 本次文档收尾未达成：T2/T6/T9-T13 仍阻塞，T14 未全绿，因此不得将 55 号顶部标记为已完成。
- P3 延后事项（SQLGlot/SchemaCrawler/超大图/UI视觉/运维写通道生产开关）未纳入本轮，待后续决策。
```

### [E9-E11 复核收口] 2026-07-14

- 已完成本地代码复核修订；无数据库 pytest 19 passed，前端 typecheck、Vitest 39 项和 build 通过，Alembic 唯一 head 为 `w9d0e1f2a3b4`，离线 upgrade SQL 生成通过。
- 尚未完成平台数据库恢复与实际迁移：目标服务器 SSH 在认证前中断；仓库仅有初始化 schema 脚本，不存在完整数据库备份，禁止将其当作本地数据导入。
- 数据库恢复可用后仍需执行实际 upgrade/downgrade、全量数据库 pytest、系统归并 dry-run 和真实浏览器联调；业务源库写操作保持 0。

### [E9-E11 服务器执行] 2026-07-14

- 已在 `10.10.8.83` 完成平台库备份、后端/前端发布和 `t6a7b8c9d0e1 -> w9d0e1f2a3b4` 三笔迁移；容器 healthy，Nginx、首页静态资源及数据库健康检查通过。
- 系统归并仅执行 dry-run：8 个数据源归入 HIS/HRP/DATA_CENTER，冲突 0，未 apply。
- 生产镜像无 pytest，服务器全量 pytest 尚未完成；本地无数据库测试 19 passed、前端 39 passed/typecheck/build 通过。仍需带 pytest 的离线验收镜像或 wheelhouse 完成数据库集成测试和人工浏览器角色矩阵。
- 业务源库写操作 0。

### [77 JHEMR Vastbase 只读资产接入] 2026-07-14

- 已确认用户给出的 `vastbase` 为默认管理库，实际电子病历业务库为 `jhemr`；只读采集 6 Schema、1,421 个表/视图、24,697 个字段。
- 结合 `系统表结构/电子病历数据库.md` 完成 542 个文档表名对照，活库匹配 532 个，10 个未匹配项保留差异证据。
- 99 个视图静态解析出 283 条依赖，以 `candidate / static_view_sql / B` 导入平台，不冒充已验证外键或强业务关系。
- 平台 source_code `jhemr_vastbase_10_10_8_177` 已登记，受控凭据 configured，保存连接测试 connected；平台库导入前已备份，API/数据库健康。
- 源业务库仅执行目录查询和 `SELECT 1`，DML/DDL 为 0；完整证据及复现入口见 77 号报告和资产包。

### [78 HIS 源端关系活库复核] 2026-07-14

- 33 条既有 HIS_SOURCE 关系的表与字段全部仍存在；31 条等值关系完成每条最多 10,000 键的只读样本验证，R015/R020 继续保留时间近似候选。
- R018 检查项目到检查主表命中 80.90%，继续 `partial`；R009/R010 药品执行复合关系约 93%，不得作为无条件强关系。
- 平台已有 HIS_SOURCE 表资产 1,234 个，但独立 HIS_SOURCE 关系尚未导入。本轮按用户“核查分析”范围只形成 78 号证据，未覆盖 ODS/JHEMR 关系；若执行正式导入，应使用独立 domain 和全局关系编号。
- 全程 Oracle 只读事务、聚合输出、大表限量，业务源库写 0。

### [79 无纸化系统 CDMS 活库探查] 2026-07-14

- `10.10.10.93:1521/orcl` 实际业务 Owner `CDMS` 已完成只读元数据采集：328 表、21 视图、4,842 字段。
- `无纸化表结构_AI分析版.md` 的 42 表中活库匹配 41 表，`TCTRYM` 缺失；活库另有 287 表未被旧文档覆盖。
- 核心病案、扫描图片、分类、打印队列、采集方法和套餐 6 组关系本轮样本 100% 命中；历史召回、采集配置/日志和权限映射按弱关系或候选保留。
- 21 个视图解析出 50 条静态基础表依赖。本轮未擅自写平台关系表；正式接入需建立独立 source/domain、平台备份和分级导入。
- 源端只读事务，大表限量验证，敏感明细未输出，业务源库写 0；证据见 79 号报告、快照和关系结果。

### [80 手麻 Docare 活库探查] 2026-07-14

- `10.10.10.68:1521/docare` 识别出 `MEDSURGERY`、`MEDCOMM`、`MEDICU` 三个业务 Owner，共 594 表、52 视图、10,529 字段。
- 患者、访视、排班、实际手术、麻醉计划/总结/事件、监护、镇痛和交接等 16 条关系完成每条最多 10,000 键的只读复核；手术排班到就诊约 98%，其余主干约 99.26%–100%。
- 52 个视图解析出 276 条静态基础表依赖。Docare 与数据中心 SM 是不同物理来源，后续必须分别登记，再做跨库手术键映射验证。
- 本轮未擅自写平台资产库；正式接入需独立 source/domain、平台备份和分级导入。源端零写，证据见 80 号报告、快照和关系结果。

### [81 LIS SQL Server 活库探查] 2026-07-14

- 修正库名为 `rmcloudlis7` 后成功连接 SQL Server 2016；`dbo` 下采集 1,180 表、74 视图、17,025 字段，数据库未声明外键。
- 条码申请、报告结果、仪器数据、日志及微生物样本/培养/鉴定/药敏/涂片共 16 条关系完成每条最多 10,000 键验证；报告到当前申请主表约 90%，需保留历史/归档缺口。
- 74 视图解析出 251 条静态基础表依赖。生产镜像未安装驱动，探查使用一次性容器和临时 wheel。
- 本轮未写平台资产库；正式接入需独立 SQL Server source/domain、平台备份和分级导入。全程 NOLOCK/READ UNCOMMITTED/限量/rollback，源库写 0。

### [82 超声内镜 SQL Server 多库活库探查] 2026-07-14

- `10.10.10.161:1433` 是一个 SQL Server 2012 物理实例，实例下识别 `MedcareUS`、`MedcareES`、`AnyImage`、`AnyImageSLES`、`PacsServer`、`MdcArchiveBrowse` 6 个业务数据库，共 766 表、59 视图、10,697 字段，数据库未声明外键。
- 超声预约到报告、患者到检查、PACS 检查/序列/影像、归档患者/检查/明细共 10 条关系完成每条最多 10,000 键验证，样本均 100% 命中。
- 内镜预约 `AccessionNo` 当前无非空样本，其他候选字段的在线组合试探触发超时后已停止；该跨库关系保持待核，不作为强关系导入。
- 本轮未写平台资产库；后续接入必须按“一个端点 → 六个数据库 → Schema → 表/视图 → 字段”建模，先平台备份和 dry-run。全程 READ UNCOMMITTED/NOLOCK/限量/rollback，源库写 0。

### [83 PACS MySQL 活库探查] 2026-07-14

- `10.10.10.191:3306` 为 MySQL 8.4 单一物理实例，开放 10 个非系统数据库，共 494 表、42 视图、5,039 字段、23 个声明外键；`gecris` 是 RIS/PACS 主业务库。
- 患者、申请、检查、报告、报告历史、MPPS、检查扩展和 PACS Study 共 10 条主线完成每条最多 10,000 键验证；8 条 100% 命中，检查到报告 88.74%、Study 到检查 96.39%，后二者保持 `partial`。
- 一个视图定义内嵌的影像访问参数已在快照中脱敏；凭据、患者明细和报告正文均未进入文档或 Git。
- 本轮未写平台资产库；正式接入应按“PACS → 一个端点 → 十个数据库 → 表/视图 → 字段”建模，先平台备份和 dry-run。源端会话只读、限量、rollback，业务源库写 0。

### [84 周边业务库连接、资产与关系平台迁移] 2026-07-15

- 8.83 平台库和凭据目录已备份；完成 79–83 号五个新来源的连接、对象、字段、声明外键和验证关系导入，并补入 78 号 HIS 31 条关系。
- 平台对账为 14 系统、14 数据源、7,197 对象、120,395 字段、478 关系；JHEMR 原有 1,421 对象/24,697 字段/283 关系保持不变。
- 新来源凭据均为受控引用、configured 状态、脱敏用户名和 600 权限；HIS 旧凭据引用已修正。可由生产驱动验证的 Oracle/MySQL/Vastbase/HIS/HRP 均 connected；SQL Server 沿用 81/82 号真实只读采集证据。
- API 与平台库 healthy，临时凭据文件已删除，业务源库写操作 0；完整备份、统计与回滚信息见 84 号报告。

### [E9-E11 独立测试镜像] 2026-07-14

- 已构建独立验收镜像并离线安装 pytest 8.4.2/httpx 0.28.1，正式运行镜像不增加测试依赖。
- 完整数据库测试真实结果：240 passed、40 failed、3 errors；后续需补测试资产种子、修订配方测试 `content_hash` 和对齐旧治理/运维状态机预期后重新执行。
- 生产容器 healthy，生产迁移 head 未改变，业务源库写操作 0。

### [91 对 90 计划与系统复核] 2026-07-15

- 新增证据：`91_一级业务系统命名资产导入与空表治理_计划与系统复核报告.md`。
- 结论：90 方向正确、根因与源码一致；当前存在「其他业务系统」大类、名称多源、导入不过滤空表、本机可连库 alembic/目录落后等问题；与 76 一级系统口径冲突须先裁决。
- 未执行 90 开发 apply；业务源库写 0。
- **后续**：91 已归档；90 已连续实施，交付见 92。

### [90 一级系统命名/目录/空表治理实施] 2026-07-15

- 交付：`92_一级业务系统命名资产导入与空表治理实施交付报告.md`。
- 代码：`asset_catalog` / `row_presence` / `asset_import_upsert`；`systems` 树改为业务系统→连接→Schema；前端去掉「其他业务系统」。
- 迁移：`b4c5d6e7f8a9`；`data_asset_test` up→down→up 通过；生产仅 upgrade 至 head。
- 生产：清单/JSON 备份后 `reconcile_plan90_catalog` apply writes=8；二次 dry-run writes=0；十系统 active + 旧码 merged。
- 表规模合计 7766；confirmed_empty=0（live 探测待夜间窗）。
- 前端 typecheck + Vitest 43 + build；关键文件 SHA 本地=服务器；业务源库写 0。

### [85 最终封板] 2026-07-15

- 启动 `85_系统最终封板与全量验收详细执行计划.md`：S0 基线 + S1 测试修复（conftest 种子、recipes/governance/admin/graph）+ SQL Server pymssql 回退。
- 无库单测 38 passed；前端 typecheck + Vitest 39 passed。
- **阻塞**：`data_asset_test` 不存在且 asset_app 无建库权限 → 全量 pytest/Alembic 往返未跑；生产 RBAC/离线演练/归并 apply/Git push 未执行。

### [85 F1/F11 解阻塞复核] 2026-07-15

- 已确认 8.83 上存在隔离测试库和独立 pytest 镜像；测试库备份后完成全量验证，结果 `300 passed / 2 skipped / 0 failed`。
- 新增迁移 `z2f3a4b5c6d7` 修复配方历史单列唯一索引，并在生产备份恢复出的临时库完成 upgrade/downgrade/upgrade 往返；生产库未 downgrade。
- 前端 typecheck、Vitest 39 项、build 均通过；图谱测试恢复完整契约后 11 项通过。
- F1、F11 改为完成。当前未完成集中于浏览器角色矩阵、生产 RBAC/容器发布、SQL Server 生产镜像实连、离线断网演练、归并 apply 和 Git 收口。
- 业务源库写操作保持 0。

### [85 F6/F8 生产发布] 2026-07-15

- 平台库备份后升级至 `z2f3a4b5c6d7`，生产镜像发布为 `data-asset:20260715-f1-r2`，API/Nginx/数据库健康。
- RBAC 严格绑定已启用，运维写保持关闭；凭据卷、Oracle 挂载及 700/600 权限通过。
- 生产镜像离线加入 pymssql，并增加旧 SQL Server TDS 7.0 回退；LIS、超声内镜保存连接真实只读测试均成功。
- Docker NAT 链异常已通过 host 网络 + `127.0.0.1` 监听规避并固化到部署脚本。
- F6、F8 完成；业务源库 DML/DDL 为 0。
- 业务源库写 0。

### [76 HRP/连接归一/运维闭环] 2026-07-14

- 代码：迁移 `y1f2a3b4c5d6`；`import_hrp_assets.py`（keep=67 dry-run writes=0）；`backfill_connection_targets.py`；`/connections*` 测试与去重；运维目标库强制平台写、管理员发布、events/字典 import-runs API；前端系统连接/SQL 工作台/审计三 Tab。
- 前端 typecheck + Vitest 39 通过；业务源库写 0。
- 阻塞：库未 upgrade 前 backfill/全量 pytest 不可在当前本机库验证；生产 HRP apply/归一 apply/写开关仍须授权。

### [75 系统连接/运维SQL/字典] 2026-07-14

- 用户要求按原75号计划开发；S1–S6、生产迁移、连接与字典已落地，该计划已归档；归并apply仍按85号F10执行。
- 迁移：`x0e1f2a3b4c5`（连接字段 + 运维模板版本 + 字典导入批次表），revises `w9d0e1f2a3b4`。
- 后端：组合创建系统与连接、凭据文件存储（只写不回显）、连通检测用 `target_host`、软停用系统、SQL 工作台 `/api/v1/ops/sql/*`、字典导入默认 dry-run；执行行锁防并发。
- 前端：统一系统页三 Tab + 元数据采集；隐藏重复数据源菜单；SQL 工作台；`typecheck` + Vitest 39 + build 通过。
- 字典 dry-run：8 / 130765 / 90265；平台 managed 已存在 diff 0；writes=0。
- 单元测试：31 passed（`--noconftest`）。部署凭据卷改为 rw。
- 阻塞：本机 SSH→8.83 密钥认证失败，测试库隧道不通 → 全量 pytest / alembic upgrade 未跑；未生产 apply 归并/写开关/字典 re-import。
- 业务源库写操作 0。

### [90 一级系统命名、资产导入与空表治理] 2026-07-15（已合并91复核）

- 用户确认新增整改：凡涉及第一层系统，统一动态采用系统总览 `system_name_cn`；数据库连接、数据资源、表资产、关系和图谱不得各自维护名称。
- 表资产取消“其他业务系统”虚拟父级；HIS、HRP、数据中心、嘉和电子病历、Docare手术麻醉、移动护理、LIS、PACS、无纸化病案、超声内镜平行展示，ODS 归数据中心内部。
- 将本地活库探查快照、字段、关系和中文名称治理成果幂等导入平台；确认数据行为 0 的对象及其字段/悬空关系不进入资产目录。不得仅凭陈旧统计量删除，须使用只读限1存在性探测；未知/超时对象保留待核。
- 历史详细执行依据为 `_archive/90_一级业务系统命名资产导入与空表治理整改执行计划.md`；现已完成，当前交付证据见 `92_一级业务系统命名资产导入与空表治理实施交付报告.md`，业务源库保持零写。
- 已裁决91提出的歧义：独立源端与数据中心镜像分层并存；旧码按物理来源迁到十个权威system_code；8.83为生产权威，10.20.1.153不得误作生产覆盖；HRP仅导入现有已核实keep范围。
- 用户要求一次性连续执行完成：S0–S8仅为内部检查点，除业务源写入、生产备份失败、凭据缺失或破坏人工确认数据的未解冲突外，不得逐阶段停等确认。
- **最终完成**：7,766对象只读治理为可见4,331/确认空3,435/未知508，空表字段残留0；完整dump已生成；全量pytest 321 passed/2 skipped，最终镜像healthy。业务源DML/DDL为0，证据见92§17。

### [98 S0 关系图谱前置治理与 Neo4j PoC 骨架] 2026-07-29

- 依据 98 号说明 S0 门禁和 97 号计划 G2/G3，实施关系图谱前置治理与图同步骨架（代码就绪，非生产上线）。
- **数据模型**：手写迁移 `c5d6e7f8a9b0`，为 asset_relations 补齐端点四元组（from/to system_code/source_code/schema_name/table_name）+ created_at/updated_at + relation_layer（formal/candidate/dependency/deferred/sync_mapping）+ relation_business_key（稳定幂等键）；历史回填 schema.table 拆分、system/source 唯一反查（多命中不猜置 NULL）；新增 asset_graph_sync_batches 表。迁移链唯一 head = c5d6e7f8a9b0。
- **写入点同步**：relations.py（3 处审核/编辑刷新 updated_at + layer）、candidates.py（promote 填新字段）、asset_import_upsert.py（upsert 填新字段）统一通过 relation_identity.py 工具维护；GraphNode 增 physical_key（四元组）。
- **同步抽象**：graph_sync.py 提供 GraphSyncAdapter 基类 + InMemoryGraphAdapter（全量重建/增量 upsert/全量差集/批次记录，幂等+可降级，不反向同步）。
- **图查询骨架**：graph_analysis.py 暴露 4 个固定参数化查询（上下游/最短路径/影响/环路），硬限制 depth≤10/nodes≤1000，拒绝自由 Cypher，Neo4j 未配置返回 unavailable 非 500，AI 无写权限。
- **测试**：纯逻辑单测 22 项通过（relation_identity 15 + graph_sync 7，`--noconftest`）；py_compile 全部 18 个新增/改动文件通过。
- **阻断（非失败）**：本机无 APP_TEST_DB_URL，PG17 服务停止 → conftest 按设计拦截；DB 集成测试（迁移往返/回填/不合并/多关系/updated_at/graph_analysis HTTP）代码就绪但未执行，**未用生产库替代**。
- **安全边界**：未部署生产 Neo4j，未开端口，未执行生产迁移，未连业务源库，凭据未入 git。PostgreSQL 始终唯一事实源。
- 详见 `99_Neo4j_PoC前置治理与图同步骨架实施报告.md` 与 98 号 §14。下一步需配置测试库验证迁移往返，再进 S1 资源评估和 S2 隔离 Neo4j PoC。

### [100 Neo4j S0 阻断整改] 2026-07-29

- 对 99 号首轮骨架进行逐文件复核后，纠正此前“仅缺测试库、代码条件已具备”的判断：S0 当前**未通过**，PoC 暂缓。
- P0 阻断包括：迁移唯一匹配会随机选择跨系统同名端点；纯表名 SQL/Python 拆分不一致；三段式 namespace/schema/table 未正确建模；未解析端点退化后仍进正式图；`graph.py` 仍按 `schema.table` 覆盖同名节点；影响/环路把有向边双向化；默认内存适配器误报健康；业务键不含物理端点；写入和 `updated_at` 水位未全局统一；同步批次及边身份不稳定。
- 当前唯一整改入口：`100_Neo4j_S0阻断问题修复与PoC放行计划.md`。必须按迁移身份口径→统一写入→物理节点与同步→有向分析→测试库往返的顺序执行，全部门禁通过并经独立 AI 复核后才可申请隔离 PoC。
- 当前禁止：生产迁移、生产 Neo4j 部署、开放 7473/7474/7687、使用生产库代替测试库、任何业务源库 DML/DDL。
- 本次仅更新计划与状态，未改数据库、未部署 Neo4j，业务源写 0。

### [100 Neo4j S0 代码修复完成] 2026-07-30

- 迁移重写：纯表名 schema=NULL、三段式正确映射、CTE+DISTINCT pair 唯一匹配（禁止 LIMIT 1）。
- 物理键：system|source|namespace|schema|table，缺失字段返回 None 不退化。
- 有向图：out_adj/in_adj 分离，上游只走 in_adj，下游/影响只走 out_adj，有向环检测。
- 默认 UnavailableGraphAdapter（health=False），Neo4j 未配置返回 degraded。
- 同步：batch_id 可重试、checksum 覆盖内容、未解析跳过计入 unresolved、孤立节点清理。
- 统一写入：5 脚本 + candidates + import_upsert + relations 全部接入 populate_endpoint_fields。
- 业务键含完整物理身份，跨系统同名表不碰撞。
- 纯逻辑测试 53 项通过。DB 集成验收阻断（无 APP_TEST_DB_URL）。
- Alembic head: c5d6e7f8a9b0（已被 d6e7f8a9b0c1 承接）。

### [101 诊断映射导入同步第一轮开发完成] 2026-07-30

- 4 张新表 + 手写迁移 d6e7f8a9b0c1（唯一 head）。
- Excel 增量导入：两行表头、SHA 幂等、OCR 暂存、校验（灰码/多对一/重复）、审核、增量 merge。
- 推送计划：服务端生成、content_hash 防篡改、创建人不能自审、计划过期。
- apply-one/stop-one 强制 dry_run，客户端无法触发实际写入。
- 前端四标签页（总览/维护/导入/下发）+ ImportWizard + PushWizard + OverviewPanel。
- 后端 75 项纯逻辑测试通过。前端 typecheck + build 通过。
- DB 集成验收阻断（无 APP_TEST_DB_URL）。浏览器验收待运行环境。
- 业务源写 0，生产迁移未执行，写通道关闭。

### 2026-07-31 HIS 人员同步 CDMS/JHEMR 代码开发完成

**执行内容**：按 103 号计划和 104 号复核结论，完成身份同步全部代码开发、测试和测试库迁移验证。

**新增/修改文件**：
- ackend/app/models/identity_sync.py：9 张新表模型（watermark/batch/action/role_mapping/protected/managed_relation/compensation/classification/lock）
- ackend/app/models/identity.py：IdentityPerson 增加 source_create_date/raw_job/raw_title/classification/is_managed/conflict_flag 列
- ackend/alembic/versions/a3b4c5d6e7f8_identity_sync_tables.py：手写迁移 e7f8a9b0c1d2（upgrade/downgrade 验证通过）
- ackend/app/services/identity_classification.py：人员分类器（doctor/nurse/pharmacist/excluded_outsource/excluded_management/classification_conflict/status_conflict/legacy_unmanaged/unsupported）
- ackend/app/services/cdms_identity_adapter.py：CDMS Oracle 11g thick 模式适配器（单事务、绑定变量、审计脱敏、无 DELETE）
- ackend/app/services/jhemr_identity_adapter.py：JHEMR Vastbase 适配器（hospital_no=49557032X、只写 role_group 和 user_dept、不写密码）
- ackend/app/services/identity_sync_orchestrator.py：编排器（候选选择、dry-run、apply、门禁、分布式锁、补偿队列）
- ackend/app/api/v1/identity_sync.py：API 端点（/candidates、/dry-run、/apply、/batches、/gates）
- ackend/app/core/config.py：新增 identity_sync_* 配置项（默认 enabled=false）
- ackend/app/main.py：注册 identity_sync 路由
- ackend/tests/test_identity_sync.py：51 项测试全部通过

**测试结果**：
- 	ests/test_identity_sync.py：51 passed
- 	ests/test_his_identity_sync.py + 	est_identity.py + 	est_sync_executor.py + 	est_his_identity_status_unit.py：83 passed（含上述 51）
- Alembic 测试库往返：upgrade → downgrade → upgrade 通过（data_asset_test@10.10.8.83）
- 全量 pytest 超时（网络依赖测试），身份相关子集全部通过

**当前阻断项（等待用户授权）**：
1. 生产平台库迁移（lembic upgrade head）未执行——需用户确认
2. 生产迁移后需重跑 HIS 采集以填充 classification/source_create_date 字段
3. xtwh 凭据文件未确认写入（/etc/data-asset/credentials/cdms_identity_sync.write、jhemr_identity_sync.write）
4. 单用户 dry-run 需上述前置完成后才能生成
5. 灰度 apply 需用户明确确认批次后执行

**安全确认**：业务源库写入 0；密码/密文/姓名未输出；夜间调度和批量 apply 保持关闭。

### 2026-08-01 JHEMR 密码日志复核与 105 号安全整改

- 管理后台修改单用户密码的日志确认：客户端先生成 `USER_PWD_SM`，随后数据库服务仅执行携带密文的 `UPDATE USERS`；仅凭已知明文/密文对不能现实地恢复 128 位 SM4 密钥。
- 当前调查入口调整为静态反编译 `JHEMRLoginExtend.dll` 的 `EmrGetEncodingPassword` / `EncryptPassword`，并追踪 `key_id=102` 到 `EncryptECB` 的调用链；优先获取厂商密码初始化 API。
- 105 号交接已移除数据库明文密码、平台连接密码、完整业务密文、姓名和完整工号，后续仅引用受控凭据与证据文件。
- 当时的阶段门禁为：JHEMR 密码自动初始化和夜间多用户 apply 暂停，角色、科室和状态同步仅做 dry-run 验证。该临时门禁已由下方 107 号最终运行裁决取代；业务源库写入仍为 0。

### 2026-08-01 JHEMR SM4 加密算法逆向完成（106 号）

- 用 Python `dnfile` 库手写 IL 反汇编器（绕开 dnSpy.Console 在重定向下的 `Console.OutputEncoding` 硬抛问题），完整还原 `JHServicesLib.dll` 的 `EmrGetEncodingPassword→EncryptPassword→Sm4ExternalMethod.EncryptECB` 调用链。
- **登录密码 `user_pwd_sm` 的 SM4 密钥已确认**：`key = (user_id + 设置当日yyyyMMdd + "bjgoodwill").encode("utf-8")[:16]`，SM4/ECB/PKCS7，Base64 输出。正向加密与逆向解密均用受控样本验证通过（同一明文在不同日期产生不同密文，确定性，无随机前缀）。
- **澄清**：`key_id=102` / `cryptography.svc.TKeyDetailInfo.plainkey` 属于 Thrift 密钥服务的另一套机制（疑似数据传输/配置解密），与 `user_pwd_sm` 登录密码加密无调用关系；105 号的"key_id=102 是核心阻断项"判断已撤销。
- **阻断项解除**：后续可直接在 `jhemr_identity_adapter.py` 实现 `set_default_password` 自动化，无需厂商 API。注意密钥含设置日期，服务器与 JHEMR 客户端时区不一致时需显式传 `date_str` 或用 `Asia/Shanghai`。
- 详见 `106_JHEMR密码SM4加密算法逆向完成与自动化交接说明.md`。当时要求在代码实现和单账号验证前暂停密码初始化及夜间多用户 apply；该一次性上线门禁不代表长期逐批审批，最终运行模式以下方 107 号裁决为准。业务源库写入 0。

### 2026-08-01 JHEMR 新用户登录控制与夜间自动同步计划（107 号）

- 管理界面日志确认新用户除 `users`、角色组和科室外，还需维护 `users_control_mode`、`users_sublogin`、`users_subsign`；现有适配器完全缺失该路径，且当前专用写账号无三表 SELECT 权限。
- 受控只读对比确认自动护理灰度账号同时存在 `001/002` 角色组；护理期望仅为 `002`。用户已明确护理参照工号仅用于 CDMS/无纸化对照，不作为 JHEMR 参照；JHEMR 另选活库中实际存在的同类账号只读核对。
- 107 已按用户最终运行口径修订：明确并行目标、JHEMR-only 原子写入路径、Phase A 审查→Phase B 完整开发→Phase C 另一 AI 独立复核、账号 HMAC 指纹、目标隔离、managed relation 对账、列级最小权限、分布式锁、熔断、异常隔离和自动回读；药师默认只同步主科室，既有人工控制配置只补不覆盖，默认密码只走 secret provider。
- 正式目标为每天夜间无人值守自动同步，正常批次不做双人审批或逐账号确认。本轮 AI 仍只执行开发、隔离测试、受控只读核查和平台 dry-run，不实际进行护理修复、医师建号、密码写入、角色 DELETE、GRANT/REVOKE，也不启用生产夜间任务。首次上线须一次性完成独立复核、受控账号验证、人工登录验证、熔断/回滚/告警演练并由运维显式启用；启用后常规安全动作自动执行，异常和破坏性动作进入隔离队列。
- 开发和独立复核完成后增加固定双职业双目标验收：从同一 HIS 快照选择一名医生和一名护士，两人均须同时不存在于 CDMS/JHEMR；由 `asset_action_executors` 白名单同步服务执行两人×两目标共四个事务，随后按 HIS 期望、动作计划、目标回读和同职业既有账号聚合基线做脱敏比对。四个目标、幂等复跑和受控登录全部通过后才开启 nightly；AI 不直接执行生产 DML。

### 2026-08-01 关系图谱无法打开专项整改（108 号）

- 生产只读确认：`/asset/graph` 和图谱分包均为 200，认证后的 graph/options/diagnostics API 为 200，平台有 537 条关系；故障不是路由删除、后端整体停机或无数据。
- P0 根因 1：生产仍加载 2026-07-13 旧前端图谱分包，强制组合 `confidence=A + validation_status=A_rechecked`，而生产 `A_rechecked=0`，默认必然空图；本地源码已修正但未完整发布。
- P0 根因 2：生产 `graph.py` 已是当前版本，但 `schemas/graph.py` 仍是旧版，`physical_key` 被 Pydantic 静默丢弃；前端、后端和 schema 不是同一原子版本。
- 当前唯一故障执行入口：`108_关系图谱无法打开专项排查整改与测试计划.md`。先完成不可变镜像、前端版本目录原子切换和 manifest，再发布筛选修复；随后完成物理键、错误状态、G6→SVG 降级、响应瘦身、浏览器 E2E、缓存和回滚验收。
- 当前只完成诊断和计划，未修改应用代码、未发布生产、未执行平台迁移、未部署 Neo4j；业务源库写入 0。

### 2026-08-02 108 号关系图谱整改执行完成（109 号报告）

- 后端：`schemas/graph.py` 契约统一（physical_key/display_id/GraphMeta、extra=forbid）；`graph.py` 重写（物理节点键、稳定边业务键、邻居 physical_key/409、`/edges/{id}` 边详情、diagnostics 按层级/状态/置信度/物理键/未解析/业务键重复/孤儿统计）；tables/health/config 版本化。
- 前端：幽灵筛选彻底删除（overview 字典移除 A_rechecked）、状态机（initial/loading/success/empty/filter_empty/auth/permission/api/contract/render_error）、options/graph/diagnostics 隔离、correlation ID/重试、G6 失败回退 SVG、meta 展示、边证据按需加载、表详情保留 system/source。
- 测试：后端图相关 85 passed（本地+服务器容器双确认）；后端全量 560 passed/2 skipped/4 failed（4 failed 为 gmssl 缺失，107 号 JHEMR 专项环境依赖，非 108 范围）；Alembic downgrade b4c5d6e7f8a9→upgrade head 往返通过，head 唯一 f1a2b3c4d5e6；前端 typecheck 通过、Vitest 54 passed、build 成功且 dist 无 A_rechecked 残留。
- 部署：`deploy/scripts/release_manifest.py`（manifest 生成）、`release_frontend.sh`（版本目录原子切换+资源校验）、`rollback_frontend.sh`（完整回滚）、`nginx.conf`（index no-cache/hash immutable/current 软链接）。
- 浏览器 E2E：真实 Chrome headless + CDP 验证 SPA 加载、console error=0、unhandled rejection=0、截图生成；登录表单自动化受 el-form 响应式同步限制，未完成（以组件测试+API 集成覆盖默认渲染）。
- 未执行：生产平台迁移/镜像替换/前端切换（需用户授权）；生产只读证据复核已确认根因，业务源库写入 0。

### 2026-08-06 关系图谱生产复测仍失败（119 号交接）

- 2026-08-05 已实施节点/边防御、渲染边 ID 隔离、G6 渲染串行化、外网通知头像清理和前端干净构建发布；当前生产入口为 `index-Bn1UGIQt.js`，图谱包为唯一的 `AdvancedRelationGraph-qXa-6we6.js`。
- 用户随后再次确认关系图谱仍无法打开，因此 108/109 的图谱事项恢复为 P0 进行中；类型检查、54 项图谱测试、构建和 Nginx 发布通过均不得替代真实登录浏览器验收。
- 当前缺口是有效账号下的现场证据：实际 chunk、页面状态、完整首条运行时异常，以及 options/graph/diagnostics 三个已认证 API 的状态与节点/边计数。
- 后续唯一专项交接入口为 `119_关系图谱仍无法打开故障情况说明与接手排查指引.md`；严格先执行 S0/S1 取证，再用脱敏生产响应复现和最小修复。业务源库 DML/DDL、同步任务启用和凭据输出继续禁止。

### 2026-08-06 关系图谱根因定位、修复与生产验收（119 §11）

- 真根因：`AdvancedRelationGraph.vue` 的 `nodeLabel()` 把 `normalizeGraphData()` 覆盖后的对象形态 `label`（echarts 风格 `{show, formatter}`）当字符串传给 G6 `labelText`，G6 文本布局对非字符串调 `.split` 抛 `TypeError`，G6 初始化必然失败。该故障只在真实 G6 Canvas 运行时出现，typecheck/jsdom 测试无法覆盖。
- 取证链：nginx 日志确认用户浏览器已加载新 chunk 且图谱 API 全 200 → 容器内只读提取生产响应（127 节点/106 边，0 重复/0 空端点/0 自环/0 孤儿）→ `frontend/repro/` 最小复现页 + 真实 Chrome CDP 稳定复现 split 异常 → 同数据 SVG 引擎正常 → 修复后 G6 正常。
- 修复：`nodeLabel()` 保证 labelText 为字符串；布局 d3-force → force-atlas2（旧配置 127 个大节点堆叠不可读）。新增回归测试 `frontend/tests/advancedRelationGraph.test.ts`。
- 验证：typecheck 通过、图谱 Vitest 56 passed、复现链路 G6/SVG 均 0 错误；生产真实 Chrome 验收（`e2e_graph_acceptance.py`，铸造短期 JWT + hash 路由 `/#/asset/graph`）首轮+3 次刷新全部通过、三 API 全 200、console/unhandled=0，截图存 `backend/scripts/evidence-119/`。
- 发布：生产入口 `index-DyKvfNqo.js`、图谱包 `AdvancedRelationGraph-CLhmmZnD.js`；回滚点 `/opt/data-asset/frontend-dist.bak.20260806-0832`。
- 状态：**代码修复与自动化生产验收完成，待用户在自有浏览器做最终交互确认后关闭 108/109/119 图谱事项**。业务源库 DML/DDL=0，同步开关未动。

### 2026-08-09 关系图谱可用性与每日人员同步只读核查（120 号）

- 用户确认 119 号完成“可渲染”修复后，实际页面仍是大量缩小方块，节点名称不可读，双向/上游/跳数以及系统、数据源、Schema、业务域筛选语义混乱。
- 生产只读确认默认图为 127 节点/106 边、命中 192 且已截断；0/127 节点携带中文名、0/127 携带业务域。平台表资产本身已有中文名/业务域，真根因是关系 namespace 多为空、表资产 namespace 多等于 schema，`graph.py` 富化字典键不一致导致元数据未进入节点。
- 新增 `120_关系图谱可用性重构与每日人员同步核查整改计划.md`：先修富化契约，再实施服务端完整概览、三任务模式、物理层级级联、中心 physical_key、一/二跳方向语义、知识图谱式节点视觉和真实浏览器可读性验收。
- 每日人员同步只读核查：宿主机 cron 8月6–9日 02:00 均运行，主账号同步分别 2/2/4/4 候选且主流程失败 0；但 JHEMR 签名子任务每天固定 16 条 `InsufficientPrivilege`，runner 仍返回 overall success。另有容器状态显示关闭但宿主机实际运行、固定 modified_since、watermark 0 行、动作审计 0 行等缺口。
- 本轮仅新增计划并登记目录；未改应用代码、生产 cron、权限、同步开关或数据库，未执行 ODS/HIS SQL，业务源库额外写入 0。该综合计划随后已拆分为 121 图谱专项和 122 人员同步专项；生产发布和任何业务目标变更仍须用户再次授权。

### 2026-08-09 120 号拆分为图谱 121 与人员同步 122 专项执行计划

- `120` 状态调整为综合核查底稿，不再作为直接执行入口，避免图谱纯读前后端重构与人员同步业务写通道整改在一个执行批次内互相扩大授权范围。
- 新增 `121_关系图谱知识图谱式重构执行修复计划.md`：G0–G8 覆盖 namespace 兼容富化、服务端完整概览、物理层级级联、中心 physical_key、1/2 跳方向语义、三模式页面、知识图谱式视觉、测试、真实浏览器和授权发布回滚；含 Luna 严格提示词。
- 新增 `122_每日人员同步任务状态审计与签名失败整改执行计划.md`：I0–I8 覆盖权威调度 provider、overall/subtask 状态和退出码、JHEMR 签名权限分类、独立水位、动作审计、告警、幂等、隔离测试与六个生产独立授权点；含 Luna 严格提示词。

### 2026-08-09 122 号 I0–I7 本地整改执行记录

- I0 已按计划读取必需文档、完成目录/git dirty 检查，并用本地 mock 真实复现：主同步 `success`、JHEMR 签名 `InsufficientPrivilege` 16 条，旧 runner 退出码为 0。
- I1–I3 已落地统一状态/退出码、主账号与签名子任务、动作审计、步骤级错误类别、HMAC 短指纹输出和 DBA 最小权限/REVOKE/单账号验证矩阵。主成功+签名失败现为 `partial_success`、退出码 2；failed/misconfigured/overdue 为 1；锁占用仅 `skipped`。
- I4–I6 已落地主/签名独立水位、时间+稳定标识 tie-breaker、安全回看、失败不推进、seed dry-run、provider 指纹/心跳/双调度失败关闭、overdue/权限/审计守恒/回读/熔断告警和状态 API。源表无 `MODIFIEDTIME` 时签名水位明确 stalled，不伪造推进。
- I7 针对性测试真实结果：`tests/test_identity_sync_122_unit.py` 24 passed；与既有 `tests/test_identity_nightly_sync.py` 合计 97 passed（均为 `--noconftest` 隔离逻辑测试）。`compileall` 与 Alembic heads 通过，当前本地迁移 head 为 `d1e2f3a4b5c6`。
- 全量 pytest 与 Alembic upgrade：**BLOCKED**，本轮未配置/确认独立 `APP_TEST_DB_URL`；未使用仓库 `.env` 或生产数据库替代，未执行迁移。
- 安全确认（I0–I7 阶段）：未触发生产 runner；当时未发布、未改 cron/systemd、未执行 GRANT/REVOKE、未 seed 生产水位、未补跑历史失败项、未对 HIS/CDMS/JHEMR 做额外写入；敏感信息扫描未发现新凭据/密码/Token/签名内容输出。
- I8 已按独立授权拆分执行：1 平台迁移/发布、4 生产 watermark seed、6 cron/provider 变更 PASS；3 单账号签名写入回读因缺少受控账号选择 BLOCKED；2 DBA 最小权限和 5 历史失败灰度补跑 `NOT EXECUTED`。未触发全量生产 runner，HIS/CDMS/JHEMR 业务写入仍为 0。
- 生产平台库仅发生本次授权范围内的迁移元数据、水位基线/告警和部署配置变更；业务目标库与源库新增写入 0，所有变更均留有备份/回滚点。详见 `122` §11。
- 单账号签名验证随后按授权执行 1 次，目标更新阶段返回 `InsufficientPrivilege`，事务回滚并完成脱敏回读；`target_writes=0`。该失败需要 DBA 最小权限授权（授权项 2），本轮未执行任何 GRANT/REVOKE。
- DBA 完成最小列级授权后，按同一稳定顺序完成第二次单账号验证：JHEMR 签名写入 1 条并回读成功，平台动作/子任务状态为 success，告警 0；未触发全量 runner，历史失败项仍未补跑。

### 2026-08-09 执行 121 号图谱专项 G0–G6

- 已按 121 执行后端物理键/namespace 兼容富化、服务端完整概览/级联/物理搜索、center physical_key 邻居契约和节点 enrichment meta；不改写正式关系资产。
- 已按 121 执行前端三任务模式、物理层级下钻、中心资产消歧、方向/跳数门禁、可读节点尺寸和 G6→SVG 回退；旧接口路径保留，旧 table 模式标记 deprecated 仅作兼容。
- 验证：关系端点纯逻辑 26 passed、图同步纯逻辑 11 passed、前端图谱 Vitest 52 passed、`tsc + vue-tsc` 通过、生产构建和 gzip 预算通过；完整 backend pytest 因缺少隔离 `APP_TEST_DB_URL` 阻断。
- G7 真实 1440×900 浏览器因本机无隔离 API/认证会话阻断；未运行生产验收脚本，未发布生产，未执行迁移、源库/生产写入、权限变更或 Neo4j 部署。121 不能标记完成，待隔离环境和用户授权后继续。

### 2026-08-09 继续执行 121：本地概览中文富化与渲染复核

- 修复概览聚合节点中文标签：系统/数据源/Owner 分别使用平台中文资产名称；对象层补回父 Owner 聚合节点，避免层级边悬空。
- 后端纯逻辑测试 37 passed；前端 typecheck、52 项图谱测试、build 均通过；完整后端 pytest 仍因缺少隔离 `APP_TEST_DB_URL` BLOCKED。
- 真实 Chrome 1440×900 静态脱敏渲染复核：G6/SVG 均 127 节点/106 边、renderError=0、console error=0、unhandled rejection=0。完整页面 G7 仍受隔离 API/认证环境阻断。
- 用户后续已分别授权 122 的 1/3/4/6：服务器未做操作系统升级；已执行平台应用迁移/发布、平台 watermark seed 与 host_cron 配置切换。DBA 后续外部完成最小列级授权，单账号签名写回读 PASS（业务目标写入 1）；历史补跑及 Neo4j 部署仍 **NOT EXECUTED**。

### 2026-08-09 新增 123 系统剩余开发与验收收口计划

- 新增 `123_系统剩余开发与验收收口执行计划.md`，承接当前真实剩余：85-F2/F4/F5/F7/F9/F10、121-G7/G8、122 最新迁移测试/连续观察和 118-U5/U6/U7/U9/U10。
- 按用户最新裁决，本轮不新增人工申请、人工复核、人工审批、驳回、自审、审批队列、审批角色、审批页面或人工审核后自动下发；已有相关代码兼容保留并默认不进入本轮主流程，统一标记 `DEFERRED_BY_USER`。
- 自动认证授权、越权拒绝、写路由扫描、生产授权门、业务库只读、平台 apply 备份和回滚仍是安全边界，不视为产品人工审批功能，不得绕过。
- 123 定义 R0–R10 强制顺序、A1–A15 验收矩阵、文件/命令/状态记录模板和可直接交给其他 AI 的严格执行提示词；116 保留为历史合并底稿，不再作为当前直接执行入口。
- 本轮仅新增/更新计划与目录记录；未执行应用代码修改、生产发布、平台 apply、历史签名补跑、cron/systemd 变更、GRANT/REVOKE、Neo4j 部署或业务库 DML/DDL。

### 2026-08-10 HIS 职称同步 JHEMR 一次性执行与复用记录（124 号）

- 业务口径：`FXHIS.SYS_EMPLOYEE.EMPLCODE = jhemr.users.user_id`；`LEVLCODE = PORTAL_USER.PORTAL_SYS_DICT.DICT_CODE`，限定 `TYPE_CODE='EmployeeTitle'`，把唯一非空 `DICT_NAME` 写入当前租户 `jhemr.users.education_title`。目标已有不同值必须覆盖；相同值幂等跳过；源职称为空或无映射不得清空目标。
- 写前只读对账：HIS 人员 2521、唯一职称代码 60、有映射 2089、无映射 432；JHEMR 当前租户 3950，匹配 2043，已一致 1726，需覆盖 317，目标缺失 46，超长 0；未发现字典、人员或目标键歧义。
- 正式执行：宿主机先固化 600 权限回滚备份并校验 SHA-256；JHEMR 单事务仅更新 `users.education_title` 317 行，逐行影响数为 1，提交前回读全部一致。平台 run/subtask/action 审计成功，317 actions 均 `executed`。
- 提交后二次 dry-run：匹配 2043、已一致 2043、待变更 0。HIS 只执行显式字段限量 SELECT，源库 DML/DDL/锁为 0；未执行 GRANT/REVOKE、全量人员 runner、cron/systemd 修改或服务器升级。
- 本地已新增可复用脚本 `backend/scripts/sync_jhemr_education_titles.py`、HIS EmployeeTitle 采集/歧义门禁、JHEMR 已有职称覆盖回读和纯逻辑专项测试；`tests/identity_title_sync/` 为 6 passed，完整 pytest 因无隔离 `APP_TEST_DB_URL` BLOCKED。
- 当前边界：2026-08-11 已在本地把 `jhemr_education_title_sync` 作为必需子任务纳入每日 runner，并实现整批 JHEMR 事务、planned action、`target_committed_pending_audit` 与 HMAC 只读 reconciliation；专项测试 17 passed，最新完整后端 pytest 697 passed/1 skipped。代码已进入 master，但尚未生产发布或调度；后续再次执行、回滚或每日化的唯一复用说明和提示词见 124，历史一次性写入授权不得扩展为其他字段或自动发布/调度授权。

### 2026-08-10 每日签名指定账号缺失与图片150像素整改（122 §12）

- 生产只读核查确认：指定账号 HIS 源签名有效（PNG 542×195），JHEMR 用户及图片行存在但图片长度为0，平台无该账号签名动作；签名总量1857低于20000上限，故不是截断或目标写失败。
- 真根因是当日02:00主流程在 `select_nightly_candidates()` 比较无时区 Oracle `MODIFIEDTIME` 与有时区平台 watermark 时抛 `TypeError`；主流程失败后签名子任务正确失败关闭为 `skipped/planned=0`。
- 本地已修候选时间UTC归一和timestamp+HMAC tie-breaker；新增签名图片宽度最大150、等比高度、小图不放大、透明白底、统一JPEG与损坏图片失败关闭。指定源图预期150×54。
- 专项测试6 passed，与职称纯逻辑套件合计12 passed，`pip check` PASS；完整pytest因无隔离 `APP_TEST_DB_URL` BLOCKED。
- **未执行**：生产应用发布、cron/systemd变更、生产runner、指定账号补跑、GRANT/REVOKE或任何生产数据库写入。后续仍需单独授权发布和受控补跑，并观察至少3个夜间窗口。

### 2026-08-10 新增125签名同步整改交接入口

- 新增`125_HIS签名同步时区故障与150像素整改交接说明.md`，作为其他AI承接本事项的唯一入口；122 §12保留事实和本地实施记录。
- 125固定H0-H7顺序：本地复核→生产只读复核→应用发布/单账号写入双授权门→不可变镜像发布→指定账号dry-run→单账号审计写入与尺寸回读→连续3夜观察。
- 完整工号不写仓库，只留HMAC短指纹；实际执行时由用户临时提供。禁止手拼SQL、全量runner、历史补跑、cron/watermark/权限变更。
- 本轮只新增交接文档和目录日志；应用发布、容器重启、指定账号补跑、生产runner及数据库写入均`NOT EXECUTED`。

### 2026-08-11 解除 APP_TEST_DB_URL 与 R8 离线演练阻断

- 通过现有 SSH 隧道恢复合法隔离库 `data_asset_test`；执行前生成受控 dump，仅修复该测试库内 `asset_app` 所有权/权限。Alembic 往返后保持 `d1e2f3a4b5c6 (head)`；完整 pytest 连续两轮均为 692 passed、1 skipped、0 failed。
- 复用与生产容器一致的 Python 3.11.15/glibc 2.41/Linux x86_64 运行时，生成 56 个 wheel 和完全哈希 `backend/requirements.lock`；禁网新 venv 安装、`pip check`、Alembic head 均通过。
- 前端固定 pnpm 11.9.0；v11 store 在 Linux/amd64 `--network none` 容器从空目录安装 897 包，双 typecheck、build 和 gzip 预算通过，实际 Linux dist 纳入包。
- 全新 PostgreSQL 14 internal network 演练完成 `upgrade head×2 → downgrade -1 → upgrade head`，应用 `/health` 返回 200；临时容器和网络已清理。
- 演练发现并修复生产依赖声明缺失：新增 `python-multipart`、`openpyxl`，锁内包含 `et_xmlfile`，并补 `gmssl` 所需 `pycryptodomex`。离线构建/清单/纯标准库验证脚本专项 43 passed/2 skipped，Linux 实包验证另行通过。
- 未发布生产、未改生产容器/cron/systemd、未写生产平台库或业务源库。当前 R8 与 Git 均不再是阻断；H7 后续由用户自行跟进，任何生产发布仍须另行授权。

### 2026-08-11 名称一致性、最新全量验收与 Git 收口

- 业务系统中文显示名统一来自系统总览；稳定键仍使用 `system_code`，物理连接仍使用 `source_code`，界面层级统一为“业务系统 → 数据连接 → Schema/Owner → 表 → 字段”，不合并两个维度。
- 关系图谱的业务系统/数据连接下拉、画布分组、节点元信息和证据抽屉均使用中文显示名；系统切换会清空并收窄其下的数据连接。ODS `ods_*` 镜像全部归 `DATA_CENTER`，独立源端系统保持一级系统。
- 最新隔离库完整 pytest：697 passed、1 skipped、0 failed；Alembic 完成 `d1e2f3a4b5c6 → c0ffee12dead → d1e2f3a4b5c6` 往返。前端离线安装 898 包、双 typecheck、76 项 Vitest、生产 build 和 gzip 预算全部 PASS。
- staged 敏感模式扫描命中 0，`git diff --check` 和 compileall PASS；提交 `e60c586`、`b8bb8f5` 已普通推送 `origin/master`，未 force-push、未移动旧 tag。
- 本轮未发布最新代码到生产，未改 cron/systemd/权限/平台数据，业务源库写入 0。H7 仍真实记录为 1/3，后续夜窗由用户自行观察，不作为本轮 Git 收口阻断。

### 2026-08-11 新增 126 号 AI 查询与统计指标闭环规划

- 新增 `126_AI查询SQL与统计指标闭环治理建设计划.md`，完整登记多 AI 日常查询自动留痕、SQL/指标复用、口径修订、本地固定文件夹、治理闭环和长期统一数据出口需求。
- 规划“本地工作区只是交换层、平台是唯一事实源、统一只读执行入口保证运行留痕”的总体架构；查询定义/版本/运行/结果与指标定义/版本/结果分开建模。
- 规划复用现有 AI 草稿、AI 会话、关系配方、只读连接器和治理审计，不复用运维 DML SQL 工作台或数据质量指标表承载业务指标。
- 规划 `queryctl`、多 AI 技能路由、历史 SQL 关系候选摄取、影响链、分阶段验收以及长期 REST/MCP/看板/数据产品出口。
- 本条形成时仅完成计划和目录登记；Q1–Q8 随后已由用户答复并按下方记录回写。当前仍未创建工作区、未修改代码/数据库、未发布生产、未执行源库查询或写入。

### 2026-08-11 回写 126 号用户答复

- Q1–Q8 已全部答复并回写：固定根目录为 `F:\python\数据资产\取数`；SQL/口径必须保存，查询结果默认可不保存，仅按需保存小文件或小型汇总。
- 查询和指标无需管理员确认；确定性安全、元数据和关系门禁通过后自动成为默认现行版本，未通过则进入 candidate/blocked。
- AI 允许通过平台、跳板机或仓库受控连接器执行必要的只读数据库核查，但不得获得明文凭据或执行源库 DML/DDL。
- 实施顺序确认为先 SQL 资产闭环、再指标版本；48 项核心制度作为首批试点；长期 active 参数化 API 允许业务系统按 RBAC/限流/审计调用。
- 后续实施主体修订为其他 AI 分阶段执行，不绑定具体模型；当前仍待用户最终复核，未授权开发、迁移、发布或生产调度。

### 2026-08-11 新增 127 号数据质量与资产治理功能整改计划

- 按用户提出的质量、资产总览、关系图谱、关系复核、关系配方、AI 协作和治理管理问题，完成生产平台数据库、源码、现有机器可读资产及生产图谱截图的只读复核，新增 `127_数据质量与资产治理功能复核整改执行计划.md`。
- 只读基线确认：平台登记 7766 张表、89730 个字段、537 条关系、185 条质量规则和 10439 条质量问题；关系复核草稿 3 条，关系配方、AI 会话、负责人和业务术语均为 0。页面空白并不等于底层无数据。
- 根因已定位到前后端分页/字段契约不一致、资产总览请求超过后端上限而 `Promise.all` 连坐、质量执行 SQL 统计契约不统一、关系复核页面接错正式关系模型、配方导入路径/状态/结构失配、AI 工具只记审计却返回虚假成功、图谱标签位置与颜色冲突等。
- 关系模拟人工复核建议：`INP_BILL_DETAIL → PAT_VISIT` 和 `CLINIC_MASTER → PAT_MASTER_INDEX` 可在测试库验证后确认并链接现有候选关系；跨库 `MEDREC.PAT_VISIT → ODS HIS.PAT_VISIT` 证据不足，继续保留 draft 交用户复核。本轮未实际批准或拒绝任何关系。
- 与 126 无目标冲突：127 修复现有资产/关系/质量/配方/AI/治理底座，126 专门建设查询 SQL 和业务指标版本闭环；禁止重复创建查询、指标、关系、配方、负责人或术语主档。推荐先完成 127 的 S0–S8，再启动 126 P1。
- 本轮仅修改计划与目录状态；未改应用代码，未执行平台 DML/DDL、关系审批、配方导入、质量规则启用或生产发布，未查询或写入业务源库。

### 2026-08-11 完成 126 P4 收口并生产升级

- 迁移 `g4b5c6d7e8f9 → h5c6d7e8f9a0`（`asset_data_products`）；备份 `data_asset_pre_126complete-20260811220449.dump`。
- 数据产品 API 已上线：目录/发布 CORE/参数化执行/结果导入/AI context；禁止任意 SQL。
- 收尾导入：21 条缺失核心制度占位（blocked）、5 CSV→历史月度结果、发布 54 个 `DP_*` 产品。
- 镜像固化 `data-asset:126complete-20260811220449`；前端原子切换。
- 验收：专项 pytest 14 passed；前端 typecheck+build/gzip PASS。

### 2026-08-11 完成 126 P5 最小闭环并生产升级

- 看板 `GET /metrics/board/overview`、MCP `GET /ai/mcp/catalog`、多源 `GET /queries/sources/capabilities`、调度种子 `POST /schedules/seed-core`。
- `APP_QUERY_SCHEDULE_ENABLED=true` 写入 `/etc/data-asset/backend.env` 并重建容器；27 条 CORE 调度 `enabled=0`；日志确认 `Query schedules registered: 0`。
- 21 条占位标题按 2025 版官方 docx 回写；结果导入幂等 + 生产去重。
- 镜像 `data-asset:126p5-20260811223311`；前端 `current→126p5-20260811223311`。
- 专项 18 passed；业务源库写入 0。可选：单条调度启用、21 条 SQL 补录、深度多源适配。

### 2026-08-12 完成 129 号图谱八次完善（本地）

- 仅以 `截图/images.jpg` 为视觉参考，未使用 `截图/数据图谱.png`；采用白底、柔和节点、黑色中心节点和带文字关系边。
- 保持物理身份不合并，界面按“业务系统 → 库（数据连接/Owner）→ 表 → 字段”渐进下钻；表层返回当前库内正式表间关系，字段层标注类型、主键和关系键。
- Luna 负责限定范围的前端实现，主 AI 完成代码复核、后端字段层接口、表关系边和验收收口。
- 前端 `pnpm test` 83 passed、双 typecheck PASS、build/gzip PASS；后端字段层与表关系层只读冒烟 PASS。完整 `tests/test_graph.py` 为 24 passed、3 failed、27 teardown errors，失败来自隔离测试库新增表清理权限及历史脏数据，不记为通过。
- 本轮未执行生产发布、平台迁移、业务源库 DML/DDL、Git push；后续须先修复隔离测试库清理权限并重跑完整后端门禁，再申请生产发布和浏览器视觉验收。

### 2026-08-13 完成系统盘点、历史计划归档与 130 号计划制定

- 按 `AGENTS.md` 启动规则完成 README、55、02 和实际目录比对；逐项核对当前代码、Git、测试库、8.83 容器/前端软链接/迁移、平台资产计数和夜间观察状态。
- 28 份已被实施证据或后续计划替代的发布、图谱、人员同步、系统收口平行文档已迁入 `_archive/`，未删除；完整原文件名和原因见 `_archive/README.md`。
- 新增 `130_系统完成度盘点与一次性开发升级执行计划.md`，并经 Dify 补充设计将剩余工作收敛为 U01–U10、S0–S11 和 A01–A28；其为用户复核后的唯一连续执行子计划。
- 本轮没有修改应用代码、测试/生产权限、平台数据、服务器容器或前端软链接，没有 Git push/tag；所有业务源库/目标库写入均为 0。

### 2026-08-19 病案 SFFJHZRY 口径说明（135）

- 只读核实：HIS `T_BA_BASE.SFFJHZRY` 来自 `PAT_VISIT_EXTEND.PLAN_ADMISSION`，活库有效值全是 2；嘉和 `pat_visit1.sffjhzry` 仍有 410 条「是」。
- `v_HISshouye.plan_admission` 读 `report.r_pat_visit1`，与现用 `pat_visit1` 对上后差 822 条；回写 HIS 后「是」全部变成 2。
- 已写入 `135_病案首页是否非计划再入院字段口径说明.md`。源库写入 0。

### 2026-08-18 HIS 停用同步嘉和锁定（单账号已脱敏 + 夜间增量）

- 用户授权：一名指定员工（工号已脱敏）HIS 已禁用，立即锁定嘉和 `jhemr.users`（`account_status=8` + `locked_time`，不 DELETE）；并纳入每晚 host cron。
- 夜间主流程在建号前处理增量停用：HIS `VALIDSTATE=0` 或 `ISDELETED` 非 0，且 `SYS_EMPLOYEE.MODIFIEDTIME` 进入水位窗口；状态冲突/保护账号不锁；历史全量不自动锁（熔断 `max_deactivate=20`）。
- 嘉和只改状态字段，不改 `state`、不写密码、不建号。单账号脚本 `python -m scripts.disable_jhemr_account --emp-no <工号>`。
- 2026-08-18 只读复核：该工号 HIS `VALIDSTATE=1`、`ISDELETED=0`、`STAFF_DICT.STATUS=1`，嘉和 `account_status=0`。按“HIS 已停用才锁嘉和”门禁未执行写入；代码已热补进 8.83 容器供当夜 cron 使用。
- HIS 业务源库写入 0。

### 2026-08-18 取数 SQL 关系摄取（134，仅复核草稿）

- 按 `sql-relation-intake` 扫描 `取数/`：67 文件、164 条去重边（existing 25 / candidate 23 / partial 44 / rejected 72）。
- 13 条 HIS 内部新边已写入 8.83 `asset_relation_reviews` draft（id=4–16）：含用户确认后的门诊处方号 Q11、检验字典 Q12（`ITEM_CLASS='C'`）、检查申请科室 Q13，以及开单科室主口径改为 `FXHIS.SYS_DEPARTMENT`（Q07/id=10）。
- 未批准、未改正式关系、未跑活库验证。住院/门诊收据串号、入库单对出库明细、PAT_VISIT 自关联、JHEMR/YDHL/HRP/PACS 等仍不入库。
- 业务源库写入 0。

### 2026-08-13 将内网 Dify AI 质控工作台并入 130

- 复核现有 `ai.py`、质量规则/问题/批次模型、质量页面、权限种子和部署配置，确认已有 AI 工具目录与质量事实底座，但不存在 Dify 出站调用、任务结果持久化、结构化结果校验和专用分析页面。
- 从 8.83 做无凭据只读连通检查，确认 Dify Service API 网络入口可达；401 为未携带 Key 的预期拒绝，没有在命令、文档或日志中使用/暴露 API Key。
- 设计固定为“平台白名单取数与脱敏 → 安全预览 → Dify Workflow 分析 → digest/JSON Schema/敏感检查 → 平台建议草稿 → 质控人员复核”；Dify 永不获得数据库凭据或业务明细，也没有改变平台质量事实的权限。
- 130 新增 U10、§2.4、S5、A21–A28；其余阶段顺延为 S6–S11。用户批准 130 后一并开发、测试、部署；若届时没有服务器受控 Workflow Key，则关闭态可发布，真实 Dify E2E 必须标记 BLOCKED。

### 2026-08-19 新增 136 号新三库探查、HIS 视图关系与服务器升级计划

- 登记三个新来源：`10.10.8.135:3306/core2db`、体检 SQL Server `10.10.10.96:1433`、职业病 SQL Server `10.10.8.96:1433`（端口暂定，仅尝试 1433，不扫描）。数据库名/系统名不确定项在执行中只读识别，无法确认即跳过并记录。
- 用户提供的高权限账号口令不写文档、Git、命令或日志；执行前必须经服务器受控凭据文件安全注入，采集后不得作为长期启用凭据。
- HIS 视图按 `sql-relation-intake` 分为依赖、候选关系和配方；先查重、限量验证、冲突复核，再落 `asset_relation_reviews` draft / `asset_relation_recipes` inactive。未独立审核不升正式，冲突未决列出后继续。
- 本机基线为 34 个 dirty 条目；前端图谱专项 42 tests、双 typecheck PASS，后端测试因缺合法 `APP_TEST_DB_URL` 被安全门禁拒绝。136 要求发布 allowlist，人员/JHEMR 目标写改动不得夹带进本次探库升级。
- 本轮只新增计划和目录登记；三个新源/HIS/平台/服务器写入 0，未执行 Git push/tag。

### 2026-08-20 136 执行收口（结果见 137）

- HIS 视图关系摄取：426 个视图中 335 个 `VALID`；运行探针保持 `runtime_skipped`，不等同于行级业务验证。
- 平台草稿与正式资产：新增 14 条 `asset_relation_reviews` draft、24 份 `asset_relation_recipes` draft 且均 inactive；正式关系 568 条保持不变，二次 dry-run writes=0。
- 验收：后端 858 passed / 1 skipped / 0 failed；前端 20 files / 119 tests PASS；`tsc`、`vue-tsc`、生产 build、gzip budget PASS；生产健康检查 200，未认证保护 API 401。
- 三个新来源均为 `BLOCKED_CREDENTIAL`，未登录数据库、未生成伪元数据、未导入平台；后续仅在安全注入受控只读凭据后重跑三源。业务源 DML/DDL=0，未把凭据阻断计为成功。
- 2026-08-20 用户授权免逐条复核后，137 的 16 条解析器冲突已完成生产收口：5 条误拆独立候选 review（24–28）标记 rejected 并关联既有正式关系；5 个正式关系只追加证据说明，2 份复杂配方仅补复核标记且保持 inactive；其他 9 条无关 draft 不动。生产备份、apply、二次零写 dry-run 与健康检查均通过，HIS 源库写入 0。

### 2026-08-21 新增 138 号目录—血缘—质量协同治理计划

- 对照两篇治理文章、现有资产、源码和 126/127/130/136/137，确认系统已有目录、业务关系、质量及查询指标产品底座，无需推倒重建。
- 当前核心缺口是：业务关系边与数据流边语义未隔离、视图依赖缺统一物理身份/版本/证据、查询仅落表级依赖、质量问题没有影响快照与复验事件、缺真实消费端和 AI 来源快照。
- 138 初稿规划完整 lineage edge/process/run、typed object reference、关系/血缘双图、字段解析、消费端和质量闭环；用户裁决后，首期只保留确定性静态边、对象键、证据和查询—指标—产品引用，后期能力不进入当前门禁。
- 原建议的住院患者—医嘱—检验/检查闭环场景已按用户意见后置。Q01–Q10 无法确认时继续按计划标记 static/unknown/blocked，不伪造结果。
- 本轮仅新增计划和目录登记；应用代码、测试库、生产平台、服务器和业务源库写入均为 0，未执行 Git push/tag。
- 用户随后批准将 138 纳入 130 统一开发/服务器发布，但明确闭环管理后期考虑。首期范围已收敛为确定性表级静态血缘；复杂字段转换、ETL 运行、真实消费和质量闭环不纳入当前完成门禁。
