> 类别：证据链

# 165 数据问题 AI 探查与入库 执行报告（E1–E6）

> 计划：`165_数据问题AI探查与入库执行计划.md`（v1.2，2026-08-30 用户授权 E3 日间一次性豁免）
> 执行窗：2026-08-29（E1/E2/E4/E5 白天批次，见 output_r165/morning_checkpoint.md 引 progress 表）+ 2026-08-30（E3 双轮 + E6 收口）
> 结果：**隔离库完成；生产 apply 待授权（X1–X4）**

## 1. 结论

- **E3 首轮+同窗收敛双轮达成**：首轮 `probe-20260830-094723` finding_new=6；同窗收敛轮 `probe-20260830-094820` **finding_new=0**、finding_updated=6（幂等更新），6 条 findings 全部 last_seen 推进至收敛轮。
- **partial 唯一来源为 T7 计划内 BLOCKED**（ODS.PACSREPORT 70 列无申请键/患者键，2026-08-29 核验，按 164 §5 口径登记，定位键位后补 side-b），非执行故障。
- 执行期间发现并修复 **run_probe.py 两处缺陷**（§5）；修复前后失败轮次全部原样登记于 `output_r165/exceptions.json` 与 `progress.md`。
- 门禁：probe 专项 **91 passed / 0 failed**（2026-08-30 复跑）；全量回归结果见 §6。

## 2. E1–E5 状态（引用 2026-08-29 checkpoint，不重做）

| 批 | 状态 | 证据 |
|---|---|---|
| E1 模型/服务/种子 | DONE | 迁移 `c1d2e3f4a5b6_add_probe_tables_165.py`（down=b0c1d2e3f4a5，单 head）；probe.finding.read 种子；upsert_finding 无终态写入路径（单测断言） |
| E2 模板库 | DONE | `scripts/probe_templates/` 12 条 T*.json；T3/T4/T6/T7 结构核验后冻结；每模板单测在 `test_probe_templates.py` |
| E4 只读 API | DONE | `app/api/v1/probe.py` 四端点 + `/probe-findings/export` 405 占位先于 `/{id}`；`test_probe_api.py` |
| E5 复发验证 | DONE | fixture 直改库构造 resolved；同窗幂等/跨窗复发/confirmed 保持/false_positive 不动专项 11 测试全绿 |
| 当日复验 | PASS | `pytest -k probe -q`：91 passed / 0 failed（APP_TEST_DB_URL=隔离库隧道） |

## 3. E3 执行明细（2026-08-30，窗口 2026-07-01 → 2026-08-30）

### 3.1 失败与试跑轮（原样存证，全部登记 exceptions.json）

| run | 结果 | 说明 |
|---|---|---|
| probe-20260830-082738 / 082838 | failed，零落库 | 缺陷一（写侧未指 --write-db） |
| 09:03 轮 | failed，零落库 | 同上 |
| probe-20260830-092312 | partial，5 findings | 缺陷二（T2 恒 0.0）暴露；定性试跑，两表清空后重做（审计行保留，JSON 留存 runs/） |

### 3.2 正式双轮

| 字段 | 首轮 | 收敛轮 |
|---|---|---|
| run_id | probe-20260830-094723 | probe-20260830-094820 |
| status | partial（T7 计划内 BLOCKED） | partial（同左） |
| probe_count | 12 | 12 |
| finding_new / updated / relapse | 6 / 0 / 0 | **0** / 6 / 0 |
| GovernAuditLog | 1 条（module=probe, action=run） | 1 条 |

12 模板首轮值：T1=81.37%（越阈）、T2=4.93%（越阈）、T5=34.48%（越阈）、T6=5.80%（越阈）、T10=10.58%（越阈）、T11=43.22%（越阈）、T8=0.32%、T9=键回写 50/50、T12=0%、T3/T4=0%（T3/T4/T8/T9/T12 未越阈只进 metrics_summary）；T7=BLOCKED。

### 3.3 入库 findings（6 条，全部 status=open，执行器只写 open/观测）

| probe_type | system_pair | 对象 | metric | 值 | 严重度 |
|---|---|---|---|---|---|
| R-REF | HIS(单库) | EXAM.EXAM_MASTER.DOCTOR_USER 缺失率 | doctor_code_missing_rate | 81.37% | P2 |
| R-REF | HIS(单库) | MEDREC.OPERATION.ANESTHESIA_DOCTOR 缺失率 | anes_doctor_missing_rate | 10.58% | P3 |
| R-XSYS | HIS↔JHEMR | TREAT_RESULT 语义桶差异率（152 E5 映射） | treat_result_bucket_max_diff_rate | 43.22% | P2 |
| R-CNT | HIS(单库) | EXAM.EXAM_MASTER 报告时间回写率 | exam_report_writeback_rate | 34.48% | P1 |
| R-CNT | HIS↔LIS | LAB.LAB_TEST_MASTER 与 LIS.lab_report 月度数量对账 | lab_month_cnt_max_diff_rate | 5.80% | P2 |
| R-REF | HIS(单库) | EXAM 申请医生姓名与 STAFF_DICT 不一致率 | doctor_name_mismatch_rate | 4.93% | P2 |

evidence_sql 均为模板参数化文本（零 ID/住院号/姓名字面量）；T8/T9 双源键集仅执行器内存流转。

## 4. 基线对照（T1/T2/T5/T10 ↔ 164 §2，偏差>5% WARN）

| 模板 | 本轮（07-01→08-30） | 164 §2 基线（2026 窗口） | 偏差（pp 主口径） | 判定 |
|---|---|---|---|---|
| T1 doctor_code_missing_rate | 81.37% | 83.2% | 1.83pp（相对 2.2%） | <5% ✓ |
| T2 doctor_name_mismatch_rate | 4.93% | 5.97% | 1.04pp（相对 17.4%） | pp 口径 <5% ✓；相对口径超 5% 已注记 WARN-3（窗口不一致：基线含上半年，本轮 7-1 起） |
| T5 exam_report_writeback_rate | 34.48% | 35.5% | 1.02pp（相对 2.9%） | <5% ✓ |
| T10 anes_doctor_missing_rate | 10.58% | 164 §2 无对应基线（未测手术线） | — | 无法对照，如实记录 |

T12（离院方式越域率 0%）与 164「code9 仅 10 例（9=其他，属允许值）」相容；T11 43.22% 与 164 R2「TREAT_RESULT 双编码 2/3/4 错位、实际走 JHEMR 语义（confirm 留人工 W3）」方向一致。

## 5. 缺陷修复登记（本轮新增，均属执行器正确性范畴）

| # | 缺陷 | 根因 | 修复 | 验证 |
|---|---|---|---|---|
| 1 | 写侧从未指向 --write-db，三次 failed（UndefinedTable） | `app.core.db` 在 import 时即以 .env 的 APP_DB_URL 建 engine，main() 内再设环境变量无效（.env 指向 10.20.1.153 旧库，164 号已预警） | ①导入前 `_preset_write_db()` 从 argv 预置；②main() 双保险断言（引擎 host/port/db ≠ --write-db 即拒绝执行） | 首轮/收敛轮成功落库且 alembic_version/表位全对 |
| 2 | T2 恒 0.0（对照基线 5.97% 判为计算缺陷） | run_single_source 指标分支缺 `name_mismatch_rate`（SQL 别名 NONNULL/MISMATCH 未被读取） | 新增分支按 MISMATCH/NONNULL 计算 | T2=4.93% 落库并产 finding |

两处失败期的「并发会话迁移往返撞窗」初步推测不成立，已在 exceptions.json 更正；但当日确有并行会话（167 号发布、168 顺延）在本仓库工作，README 亦载 153 期间并行互踩先例——后续全量 run 建议维持单执行者独占。

## 6. 门禁

- probe 专项：**91 passed / 0 failed**（2026-08-30，APP_TEST_DB_URL 隧道串显式传入）。
- 全量回归：**1288 passed / 1 skipped / 0 failed，27 分 22 秒**（2026-08-30 第二遍；第一遍因并行 W10 会话重建 8.83 容器致隧道失效而挂起，已终止并登记 WARN-5；plan127 s0 既有基线例外口径同 A10，本轮未见该失败项）。
- 收尾双轮（恢复库内证据）：回归夹具清空 probe 两表与审计表后，重做首轮 probe-20260830-111903（new=6）+ 收敛轮 probe-20260830-111935（**new=0**/updated=6）；最终库态 2 run 行/6 findings/2 审计行与 §3.2 双轮结论一致。

## 7. DoD 逐项自检（165 §7）

- [x] E1–E6 checkpoint 齐；probe 专项全绿；全量 0 failed（见 §6 补记；plan127 s0 例外如实记录）
- [x] 隔离库首轮 run 入库 + 164 §2 基线对照表（§3.3/§4）
- [x] 幂等（同窗 0 新增，收敛轮）+ 跨窗复发双实证（E5 专项 11 测试 + 服务层无终态写入断言在库）
- [x] E4 四端点+405 占位顺序测试过（8-29 批次，test_probe_api.py）；每 run 一条审计行（本轮 3 条在库）
- [x] 报告 + _结果.json + README/55 登记；X1–X4 呈用户（§8）

## 8. 等待域（呈用户裁决）

- **X1** 生产建表+read 种子 apply（备份→dry-run→apply→二次 dry-run 0→/health 200）
- **X2** 生产首轮夜间探查（X1 后，夜间窗）
- **X3** 终态人工裁决（166 界面就绪后）
- **X4** 常态化调度周期（用户定；建议维持夜间窗+单执行者）

## 9. 红线自检

业务源库零写入（源侧仅 8.83 受控连接器 SELECT）；生产平台库零连接零写入；凭据零落盘；零 Git 写；人工终态零代点；evidence 零患者字面量；隔离库写侧仅 probe 两表+审计行。
