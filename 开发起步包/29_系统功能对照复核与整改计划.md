> 类别：方案

# 29 系统功能对照复核与整改计划

> 编写日期：2026-07-05  
> 文档仓库：`F:\python\数据资产\开发起步包`  
> 代码仓库：`F:\python\data-asset-platform`  
> 目的：把文档侧 `20/27/28` 的目标功能与当前代码系统逐项对照，形成今天可复核确认、后续 AI 可直接执行的整改清单。  
> 结论：当前系统已从 28 号文档描述的“页面严重缺失”推进到“页面/模型/API 骨架基本齐备”，下一步重点是**真实源库采集、真实同步/执行、权限限流有效性、部署验收闭环**。

---

## 0. 本次复核依据与验证结果

### 0.1 读取的文档依据

| 文档 | 用途 |
|---|---|
| `20_应用层选型与开发方案.md` | FastAPI + Vue Admin + 离线部署 + AI 安全边界 |
| `27_平台二次整改计划.md` | F01-F46 全量功能目标，P5.5-P14 阶段计划 |
| `28_核查与整改计划.md` | 上一轮缺口、Bug、优先级路线图 |
| `AGENTS.md` / `02_开发环境与资产总索引.md` | 源库只读、Oracle 11g、跳板机、敏感数据红线 |

### 0.2 当前代码验证

在 `F:\python\data-asset-platform` 执行：

```powershell
cd F:\python\data-asset-platform\backend
.\.venv\Scripts\pytest.exe tests -q
# 180 passed, 2 warnings

cd F:\python\data-asset-platform\frontend
npm.cmd run typecheck
# passed

npm.cmd run build
# passed
```

当前代码仓库已是 Git 仓库，`git status --short` 为空。

---

## 1. 与 28 号文档相比的状态变化

### 1.1 已完成或基本完成

| 28 号缺口 | 当前代码状态 | 复核结论 |
|---|---|---|
| 前端缺 P11/P12/P13/P14 顶层模块 | 已有 `router/modules/ops.ts`、`identity.ts`、`dict.ts`、`metadata.ts` | **已补齐页面骨架** |
| 缺系统总览、数据源与库、关系复核 | 已有 `views/asset/systems`、`sources`、`relation-review` | **已补齐页面入口** |
| 缺表/字段中文注释编辑 UI | `table-detail` 已有表和字段注释编辑；后端有 `/tables/{id}/annotation`、`/columns/{id}/annotation` | **已基本完成** |
| 缺通用字典 5 张表 | 已有 `asset_dict_categories/standard_items/system_items/item_mappings/versions` | **已建模并迁移** |
| 缺质量任务/指标表 | 已有 `asset_quality_tasks`、`asset_quality_metrics` | **已建模并迁移** |
| 缺关系复核表 | 已有 `asset_relation_reviews` | **表已建，但 API 未真正使用，见 3.5** |
| 运维缺 reject/execute/audit 端点 | 已有 `/ops/runs/{id}/reject`、`execute`、`audit` | **端点已补，执行仍占位** |
| AI 缺 `tool-execute`、系统上下文、SQL 风险扫描 | 已有 `/ai/tool-execute`、`/ai/system-context`、`/ai/sql-risk-scan` | **接口已补，tool-execute 仍是假代理** |
| 缺部署文档 | 已有 `deploy/README.md`、`nginx.conf`、`backend.env.example` | **已补部署说明，待实机验收** |
| 无请求限流依赖 | 已有 `slowapi` 和 `core/rate_limit.py` | **只初始化，未绑定具体限流规则** |
| Token 过期 | `ApiKey.expires_at` 已存在，中间件校验过期 | **基本完成** |

### 1.2 仍未闭环的核心事实

| 模块 | 当前状态 | 问题性质 |
|---|---|---|
| 元数据采集 | `/sources/{source_code}/collect-metadata` 仍从本地 `AssetTable/AssetColumn` 生成快照 | **未真正探源库** |
| 数据源连接 | `check_source()` 创建连接器但未传 user/password，也未解析 `credential_ref` | **连接大概率不可用** |
| Oracle 旧库 | `OracleConnector` 未 `init_oracle_client()`，未处理 thick 模式路径 | **不满足 8.216/11g 硬约束** |
| 跳板/网络拓扑 | 连接器只支持 direct，没有通过 8.53 跳板执行探库 Worker 的实现 | **与文档安全拓扑存在差距** |
| 人员科室采集 | `/identity/collect-sources` 仅返回 `scheduled` | **无真实采集/差异比对** |
| 医疗字典采集 | 有 CRUD、映射、版本，但无 `collect-sources` | **无真实 HIS/EMR 字典比对** |
| 运维执行 | `readonly_sql/stored_procedure/http_api` 执行器返回 placeholder | **高风险模块不能上线使用** |
| 同步框架 | `sync_executor.py` 明确返回 `status="stub"` | **未实现** |
| 定时任务 | APScheduler 启动后 `_run_metadata_collect()` 只写成功记录 | **没有执行真实任务** |
| 关系复核 | `asset_relation_reviews` 表存在，但 `relations.py` 仍直接操作 `AssetRelation` | **新表未接入工作流** |
| RBAC | `TOKEN_ROLES` 是静态字典，未查 `asset_user_roles/role_permissions`；`dict-medical` 路由映射错误 | **权限不可信** |
| 限流 | 未发现 `@limiter.limit(...)` 或全局限流中间件 | **限流未实际生效** |

---

## 2. 今日需要复核确认的决策

这些项决定后续 AI 如何落地，建议今天先确认。

| 编号 | 待确认事项 | 推荐决策 |
|---|---|---|
| C1 | 8.83 应用服务器是否允许直连 8.216/10.10.10.15 源库 | 推荐明确为“只允许通过 8.53 跳板/Worker 执行源库探查”，除非网络已正式开通 8.83 → 源库只读链路 |
| C2 | 源库凭据如何提供给平台 | 推荐 `credential_ref` 指向环境变量或密钥文件；数据库仅存引用，不存明文 |
| C3 | AI `/tool-execute` 是否允许代替 AI 真正调用内部只读接口 | 推荐允许“只读代理 + 白名单 + 审计 + 限流”，但禁止任何写操作 |
| C4 | `asset_relation_reviews` 是否作为关系复核主表 | 推荐确认“是”，后续 `relations.py/candidates.py` 改为写 review 表，正式关系只在批准后回写 |
| C5 | 运维工具第一批是否只开放只读核查 | 推荐第一批只上线 `readonly_sql` 状态核查；补发/重推/修复先保持 disabled |
| C6 | 是否把 29 号文档作为当前最新整改入口 | 推荐确认“是”，28 号保留为历史核查，后续以 29 为执行入口 |

---

## 3. 按功能目标逐项对照

### 3.1 数据资产与质控 F01-F05/F46

| 功能 | 文档要求 | 当前实现 | 整改要求 |
|---|---|---|---|
| F01 院内多数据库检测 | 多系统、多库连接检测、元数据检测、质量规则检测 | 数据源模型和连接器有了；连接未解析凭据；采集未连源库 | P1-1/P1-2 优先 |
| F02 多系统分层资产目录 | 区分数据中心/HIS/LIS/PACS/YDHL/EMR/SM | `asset_systems`、`asset_data_sources`、资产树已实现 | 补导入/归并 HIS 源端资产包的验收脚本 |
| F03 系统下分库/owner/表/字段 | 五层结构 | 前后端基本支持 | 需要 UI 展示 owner/schema 统计与筛选 |
| F04 中文注释治理 | 表字段中文名、业务含义、取值说明 | 编辑 UI 和缺失清单已有；AI 建议仍占位 | 补 AI 建议草稿或移除占位入口 |
| F05 数据质量台账 | 按系统/库/表/字段跟踪问题 | 质量规则、finding、summary 基本有 | 采样质检、连通性实时检测未闭环 |
| F46 元数据变更检测 | 手动/定时采集、快照 diff、事件告警 | 快照/diff/事件页面和 API 有；采集仍读本地资产 | 必须接入真实源库采集 |

### 3.2 多数据库连接驱动 F06-F11

| 功能 | 当前实现 | 问题 |
|---|---|---|
| Oracle | 有 `OracleConnector` | 未 thick 初始化；未传凭据；未跳板；未设置超时；旧库兼容不足 |
| PostgreSQL | 有 `PostgresConnector` | `execute_readonly()` 直接追加 `LIMIT`，若 SQL 已有限制或含分号可能出错 |
| MySQL | 有 `MysqlConnector` | 参数风格与 SQL 拼接需统一，只读保护不足 |
| SQL Server | 有 `SqlServerConnector` | 参数传递使用 dict 但 SQL 示例用 `?`，可能不兼容 pyodbc |
| 统一数据源配置 | 有 `asset_data_sources` | `credential_ref/write_credential_ref` 未落地解析 |
| 海量数据库 | 未实现 | 可保留扩展点，不列入本轮上线 |

### 3.3 AI SQL 协作 F12-F16

| 功能 | 当前实现 | 整改要求 |
|---|---|---|
| F12 AI 获取系统上下文 | `/ai/system-context` 已有 | 增加字段详情、质量问题、数据坑提示 |
| F13 AI 获取表关系 | 工具目录已有 relation/path/graph/lineage | `tool-execute` 应真正代理调用这些只读接口 |
| F14 SQL 草稿 | `/ai/propose-sql` 已有 | 增加草稿版本、关联任务、风险解释 |
| F15 SQL 风险扫描 | `/ai/sql-risk-scan` 已有 | 升级为 SQL parser，不只做字符串 contains |
| F16 人工审核 SQL | 草稿审核已有 | 审核后仍不能执行；需明确“只保存草稿”或接 SQL 沙箱 |

### 3.4 运维工具 F21-F26

| 功能 | 当前实现 | 风险 |
|---|---|---|
| 工具模板 | CRUD + seed 脚本 | 需确认 seed 是否部署时执行 |
| 任务申请/审批/拒绝/执行/审计 | 端点和页面已存在 | 执行器是假执行，不能处理真实补发/重推 |
| 二次确认 | `require_second_confirm` 已校验 | 只有布尔值，缺确认短语/操作摘要签名 |
| 写操作安全 | 模型有 `write_credential_ref` | 未实现凭据解析、白名单 SP/API 调用、回滚说明强校验 |

### 3.5 关系复核 F17-F20

| 功能 | 当前实现 | 整改要求 |
|---|---|---|
| 关系编辑 | `relations.py` 可改 `AssetRelation` | 应改为先进入 `asset_relation_reviews` 草稿 |
| 审核通过/拒绝 | 直接改 `validation_status` | 应引入 draft/reviewing/approved/rejected 工作流 |
| 字段映射 | 只从 `AssetRelation.from_columns/to_columns` 派生 | 应支持独立字段级映射和业务说明 |
| 审计 | 有 `GovernAuditLog` | operator 固定为 `"reviewer"`，应来自登录用户/token |
| 图谱联动 | 有图谱页面和关系页 | 需补边点击跳转复核页、带 relation id |

### 3.6 人员科室 F27-F32

| 功能 | 当前实现 | 整改要求 |
|---|---|---|
| 人员画像 | 有 `persons/{person_code}` | 依赖本地数据；未采集 HRP/HIS |
| 科室画像 | 有 `departments/{dept_code}` | 未从 HIS `dept_dict` 刷新 |
| 账号映射 | 有账号列表和 bind | 缺同步目标系统、审批执行 |
| 差异检测 | 有 `IdentitySyncDiff` 列表 | 无真实生成逻辑 |
| 变更审计 | 有 change request | execute 只改状态，不调用执行器 |

### 3.7 字典中心 F33-F40

| 功能 | 当前实现 | 整改要求 |
|---|---|---|
| 通用字典框架 | 已有 5 张表和 API | 需补审核/发布/回滚动作 |
| 诊断/手术字典 | `dict-medical` CRUD 已有 | 缺从 HIS/EMR 源库读取和比对 |
| 跨系统映射 | mapping 表和页面已有 | 缺批量导入、AI 辅助匹配、审核流 |
| 版本发布 | version 表/API 有 | 未形成发布包和下游同步执行 |
| 向下兼容旧编码 | 系统原始字典项模型有 | 需采集源表字段和有效期 |

---

## 4. 整改任务清单

### P0：今天复核后立即修正

| 任务 | 文件 | 说明 | 验收 |
|---|---|---|---|
| P0-1 修复 RBAC 路由映射 | `backend/app/main.py` | `dict-medical` 应映射 `/api/v1/dict-medical`；移除或修正不存在的 `/api/v1/dictionaries/medical` | 无权限 token 访问 dict-medical 返回 403 |
| P0-2 RBAC 改为查库 | `main.py`、`models/governance_base.py` | 用 token 对应用户查 `asset_user_roles` 与 `asset_role_permissions`，不要用静态 `TOKEN_ROLES` | 新增权限测试 |
| P0-3 限流真正生效 | `core/rate_limit.py`、`api/v1/ai.py`、`ops_tools.py`、`metadata_changes.py` | 对 AI tool-execute、运维执行、采集触发加限制 | 高频请求返回 429 |
| P0-4 明确源库连接拓扑 | `29` 本文档确认后更新 `deploy/README.md` | 明确 8.83 直连还是 8.53 Worker | 文档与代码一致 |

### P1：真实探库与元数据变更闭环

| 任务 | 文件 | 说明 | 验收 |
|---|---|---|---|
| P1-1 凭据解析 | `core/config.py`、新增 `services/credentials.py` | `credential_ref` 解析到只读账号密码；日志脱敏 | 单测覆盖不存在/缺失/正常凭据 |
| P1-2 Oracle thick 初始化 | `services/db_connectors.py` | 支持 `APP_ORACLE_CLIENT_LIB_DIR`，初始化 `/opt/oracle/instantclient_21` | 旧 Oracle 不报 DPY-3010 |
| P1-3 真实元数据采集 | `api/v1/metadata_changes.py`、`services/metadata_collector.py` | `collect_metadata()` 调用采集器，不再只读本地资产 | 新快照来自源库元数据 |
| P1-4 采集结果回写资产 | 同上 + `models/asset.py` | 采集后更新/新增 `AssetTable/AssetColumn`，并生成 column snapshot | 表数/字段数可追踪 |
| P1-5 定时任务执行真实采集 | `main.py` 或新增 `services/scheduler.py` | APScheduler 调用 P1-3 服务 | scheduled job 不再只写成功记录 |

### P2：同步、人员、字典业务闭环

| 任务 | 文件 | 说明 | 验收 |
|---|---|---|---|
| P2-1 实现 `sync_executor` | `services/sync_executor.py` | 根据 entity_type 分发实际服务，禁止返回 `stub` | 测试断言无 stub |
| P2-2 人员科室采集 | `api/v1/identity.py`、新增服务 | 从 HIS/HRP 采集科室、人员、账号，生成 `IdentitySyncDiff` | collect 后 diff 可查询 |
| P2-3 字典源端采集 | `dict_medical_api.py`、`dict_general_api.py` | 从 HIS/EMR 读取诊断/手术/系统字典，生成系统项和差异 | sync-diffs 有真实来源 |
| P2-4 变更请求执行器 | `identity.py`、`dict_medical_api.py`、`governance.py` | execute 不只改状态，调用 sync/写审计 | 审批后可执行或明确阻断 |

### P3：运维工具安全上线

| 任务 | 文件 | 说明 | 验收 |
|---|---|---|---|
| P3-1 只读 SQL 执行器 | `ops_tools.py`、`db_connectors.py` | 第一批只允许 SELECT，强制 max_rows，禁止患者明细外泄 | `readonly_sql` 返回真实查询摘要 |
| P3-2 SP/API 白名单 | `ops_tools.py`、`governance.py` | stored_procedure/http_api 只能调用注册白名单 | 非白名单返回 400/403 |
| P3-3 二次确认升级 | `ops_tools.py`、前端 runs 页面 | 二次确认要求确认短语和操作摘要 hash | 缺确认短语不可执行 |
| P3-4 运维参数脱敏 | `ops_tools.py`、审计工具 | `input_params_masked` 入库前脱敏 | 日志无身份证/电话/明文密码 |

### P4：关系复核工作流

| 任务 | 文件 | 说明 | 验收 |
|---|---|---|---|
| P4-1 接入 `asset_relation_reviews` | `relations.py`、`candidates.py` | 复核页读写 review 表，不直接改正式关系 | review 表有草稿和审核记录 |
| P4-2 审核后回写正式关系 | 同上 | approved 后写回 `asset_relations`，rejected 不回写 | 状态流转可追踪 |
| P4-3 字段级映射 | 新模型或扩展 review 表 | 支持字段映射、中文逻辑、证据来源 | 前端可编辑字段级映射 |
| P4-4 图谱联动 | `frontend/views/asset/graph` | 边点击跳转 `/asset/relation-review?id=...` | 页面跳转带上下文 |

### P5：AI SQL 协作增强

| 任务 | 文件 | 说明 | 验收 |
|---|---|---|---|
| P5-1 `tool-execute` 真代理 | `api/v1/ai.py` | 只代理 AVAILABLE_TOOLS 中的只读 GET/POST，不只是返回 executed | 调用 search_tables 返回真实数据 |
| P5-2 SQL 风险扫描升级 | `api/v1/ai.py` 或新增 `services/sql_risk.py` | 使用 SQL parser；识别多语句、注释绕过、CTE、大表全扫 | 新增对抗测试 |
| P5-3 系统上下文增强 | `ai.py` | 加字段、质量问题、数据坑、样本禁止说明 | 导出上下文可直接给 AI 写 SQL |

### P6：部署与运行验收

| 任务 | 文件 | 说明 | 验收 |
|---|---|---|---|
| P6-1 补 systemd 文件 | `deploy/data-asset-api.service` | 不只在 README 内嵌，提供独立文件 | 可复制部署 |
| P6-2 补离线 wheel 清单 | `deploy/README.md` | 明确 Python 版本、wheel 下载命令、离线包结构 | 8.83 可复现安装 |
| P6-3 Nginx 路径校验 | `deploy/nginx.conf` | 校验 `/api/` proxy_pass 是否与后端路由完全匹配 | curl `/api/v1/health` 通过 |
| P6-4 生产冒烟脚本 | 新增 `deploy/smoke_check.ps1` 或 `.sh` | 检查 health、前端、登录、关键 API | 部署后 5 分钟内验收 |

---

## 5. 不建议后续 AI 重复做的事项

以下已经完成或已有足够骨架，不要重复造轮子：

1. 不要重建 `asset_systems/asset_data_sources`，已有模型和 API。
2. 不要再新建一套通用字典表，`asset_dict_categories/standard_items/system_items/item_mappings/versions` 已存在。
3. 不要再新建一套质量任务/指标表，`asset_quality_tasks/asset_quality_metrics` 已存在。
4. 不要再补前端顶层菜单骨架，`ops/identity/dict/metadata` 已有。
5. 不要让 AI 或页面直接执行生产 DML/DDL。
6. 不要把明文数据库密码写入代码、报告、日志或提交。

---

## 6. 今日复核确认清单

请今天重点确认下列项，确认后后续 AI 可直接按第 4 节执行：

- [ ] 29 号文档是否作为最新整改入口，28 号作为历史参考。
- [ ] 8.83 到源库的真实网络路径：直连源库，还是必须经 8.53 跳板/Worker。
- [ ] `asset_relation_reviews` 是否确认为关系复核主表。
- [ ] 第一批运维工具是否只上线只读核查，补发/重推/修复暂不开放。
- [ ] AI `/tool-execute` 是否允许代理内部只读接口。
- [ ] 源库凭据使用 `credential_ref -> 环境变量/密钥文件`，不入库明文。

---

## 7. 后续 AI 执行约束

1. 执行前先跑：

```powershell
cd F:\python\data-asset-platform\backend
.\.venv\Scripts\pytest.exe tests -q

cd F:\python\data-asset-platform\frontend
npm.cmd run typecheck
npm.cmd run build
```

2. 每完成一个 P0/P1/P2/P3 任务必须补测试。
3. 所有源库访问默认只读；Oracle 11g 使用 `ROWNUM <= N`，禁止 `FETCH FIRST`。
4. `HIS.LAB_RESULT`、`INP_BILL_DETAIL`、`ORDERS` 等大表禁止全表扫描。
5. 审计日志、AI 上下文、错误信息不得包含患者姓名、身份证、电话、地址、明文密码。
6. 修改部署方式时同步更新 `deploy/README.md` 和本文件状态。

---

## 8. 建议执行顺序

```text
P0-1/P0-2/P0-3  权限与限流先修，避免高风险接口裸奔
P1-1/P1-2       凭据与 Oracle thick 先打通
P1-3/P1-4       元数据真实采集与快照
P1-5            定时任务接真实采集
P2-1~P2-4       人员、字典、同步差异闭环
P3-1~P3-4       运维工具只读核查上线
P4-1~P4-4       关系复核主表与图谱联动
P5-1~P5-3       AI 工具真代理与 SQL 风险增强
P6-1~P6-4       8.83 部署验收
```

---

## 9. 当前完成度估算

| 维度 | 完成度 | 说明 |
|---|---:|---|
| 前端页面覆盖 | 85% | 菜单和主要页面已齐，需端到端交互增强 |
| 后端模型/API 骨架 | 90% | 主要表和路由已具备 |
| 真实业务逻辑 | 45% | 采集、同步、运维执行、定时任务仍有占位 |
| 安全治理 | 55% | Token 过期有了，RBAC/限流/脱敏仍不完整 |
| 部署交付 | 60% | 文档和 nginx 有了，缺独立 systemd/smoke/实机验收 |
| 综合完成度 | 70% | 已可作为管理平台骨架，但还不能作为生产探库/运维执行平台上线 |

---

*本文件为 2026-07-05 当日复核入口。后续执行完成后，应在本文件中更新状态，并同步 `README.md` 清单。*
