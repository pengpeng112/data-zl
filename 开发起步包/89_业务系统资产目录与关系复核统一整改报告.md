> 类别：实施证据
>
> 状态：已完成
>
> 安全结论：本次只修改平台代码、平台元数据和前端展示；未连接或写入任何业务源库，业务源库DML/DDL为0。
# 业务系统、资产目录与关系复核统一整改报告

## 1. 用户确认口径

- 一级业务系统固定为：HIS、HRP、数据中心、嘉和电子病历、Docare手术麻醉、移动护理、LIS、PACS、无纸化病案、超声内镜。
- 数据中心内部HIS/LIS/PACS/MTL/JHEMR/YDHL/SM/CDA/ODS只作为Owner/Schema展示。
- 层级统一为：业务系统中文名 → IP:端口/库或Service → Owner中文用途（物理名）→ 表中文名（物理名）→ 字段中文名（物理名）。
- 中文名称区分数据库原始注释、已有/文档确认名称、AI建议名称；AI建议必须标记待确认。
- 候选关系与正式关系合并为统一关系复核中心。

## 2. 根因与修复

1. 原系统总览展示全部active系统行，导致数据中心历史别名成为一级系统；现改为由启用的真实物理连接决定一级系统。
2. 原资产分类依赖系统编码硬编码，新探查系统落入“平台元数据”；现改为使用物理连接、source_kind、canonical_source_code和探库Schema清单分类。
3. 数据中心历史别名现统一折叠到真实连接 `10.10.8.216:1521/orcl`，Schema保留原资产source_code用于懒加载。
4. 原候选页面读取空的旧候选表，而实际293条候选位于正式关系表；现统一从实际关系状态分页查询。
5. 关系复核增加全部、待复核、已确认、已拒绝、候选、同步/镜像血缘、视图依赖七类，并增加分页、系统/关键词筛选、批量批准/驳回和审计。

## 3. 中文名称治理

迁移 `a3b4c5d6e7f8` 新增表、字段和Schema中文名称来源/确认状态字段。回填结果：

- 已有资产确认名称：229张表；
- 数据库原始中文注释：1482张表；
- AI建议、待人工确认：6055张表；
- 新建Schema目录记录：43条；
- 已确认中文用途的Schema：19条。

AI建议名称不会覆盖数据库原始注释或已有确认名称。

## 4. 生产验证

- 一级业务系统：10个；每个系统只统计真实物理连接。
- 统一资源树：10个系统、10个物理连接。
- 数据中心：1个连接、30个Owner/Schema、865张表。
- 独立资产：HIS 1236、HRP 67、JHEMR 1421、Docare 646、移动护理567、LIS 1254、PACS 536、无纸化349、超声内镜825。
- 关系总数535；候选293；血缘37；视图依赖7；待复核55；已确认176。
- 生产Alembic head：`a3b4c5d6e7f8`。
- 容器：healthy；Nginx：首页200。

## 5. 验收

- 后端完整测试：301 passed、2 skipped；旧分类用例按用户新口径修订后专项3 passed。
- 前端：`pnpm run typecheck`通过；`pnpm run build`通过。
- Python编译、Alembic heads/离线SQL、`git diff --check`通过。
- 业务源库写操作：0。

## 6. 回滚点

```text
/opt/data-asset/backups/data_asset_before_catalog_unify_20260715_145117.dump
sha256: 857b2f07511ca8f0e9d41ed37d9196b2ea3a7c1b79956bbeee9c72ca55a8beac
```

前端备份：`/opt/data-asset/frontend-dist.pre-catalog-20260715_145117`。
