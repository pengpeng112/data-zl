> 类别：证据
>
> 日期：2026-08-14

# 关系复核升级报告

本轮按用户授权：证据清楚的升正式并回写平台库与 `数据资产_资产包`；不明确的单独列出待复核。源库只读抽样，未写业务库。

## 1. 已升级为正式关系

| rel_id | 关系 | 键 | 证据 | 库内结果 |
|---|---|---|---|---|
| 11 | `HIS.OUTP_RCPT_MASTER → HIS.OUTP_BILL_ITEMS` | `RCPT_NO` | 视图 `V_EMR_OUT_FEE_INFO_CHILD/MAIN`；源端 formal 473；ODS 5万样本孤儿 0 | formal / sample_verified / B |
| 21 | `HIS.DIAGNOSIS → HIS.DIAGNOSIS_TYPE_DICT` | `DIAGNOSIS_TYPE` | 09 文档 A；ODS 5万样本孤儿 0 | formal / sample_verified / A |
| 9 | `HIS.PAT_VISIT → HIS.ORDERS` | `PATIENT_ID+VISIT_ID` | 10 号 R09 限定验证；源端 formal 453 | formal / verified / B（巨表禁止全扫） |
| 19 | `HIS.LAB_TEST_MASTER → HIS.LAB_RESULT` | `TEST_NO` | 10 号 R19；源端 formal 460 | formal / verified / B（禁止 LAB_RESULT 全扫） |
| 20 | `HIS.DIAGNOSIS → CDA.CDA_DICTIONARY` | 院标编码 / ICD-10 | 10 号 R25 映射率 94.18% | formal / sample_verified / B（未映射回退） |
| 23 | `MTL.EPR_INPATIENT_DOCINDEX → MTL.EPR_INPATIENT` | `UHID` | 10 号 5万样本 0 孤儿 | formal / verified / A+ |
| 24 | `MTL.EPR_INPATIENT → MTL.EMRSD_CONTENT` | `UHID+段落编号` | 10 号 5万样本孤儿 0.058% | formal / verified / A |
| 30 | `MTL.EMRSD_CONTENT → MTL.EPR_INPATIENT_DOCINDEX` | `UHID` | 10 号 10万样本孤儿 1 | formal / verified / A+ |

## 2. 明确不升级（已改状态或加说明）

| rel_id | 关系 | 原因 |
|---|---|---|
| 10 | `HIS.CLINIC_MASTER.RCPT_NO → HIS.OUTP_RCPT_MASTER` | `HIS.CLINIC_MASTER` **没有** `RCPT_NO` 字段。已标 `rejected`。门诊收据应改走 `CLINIC_NO` / `OUTP_ORDER_DESC`。 |
| 28 | `YDHL.MCS_ASSESS_FORM → YDHL.INPATIENTS` | 与 formal **45** 同键重复，不再升第二条。 |
| 537 / 538 | 住院费用、门诊患者主索引候选 | 已有 formal 467 / 472，保持候选副本不重复升。 |

## 3. 用户确认后已继续处理（2026-08-14 第二轮）

| 项 | 处理 |
|---|---|
| 1 检查拆分 | 新增正式 **553** 住院（`VISIT_ID<>0`，5万样本孤儿 4）和 **554** 门诊（`VISIT_ID=0/NULL` + `RCPT_NO`，5万样本孤儿 0）。原 14 保留为混合说明。 |
| 2 检验拆分 | 新增正式 **555** 住院（孤儿 22/5万）和 **556** 门诊收据（孤儿 0/5万）。原 16 保留为混合说明。 |
| 3 结算→费用明细就诊键 | rel **8** 升正式。 |
| 4 检验项目→结果复合键 | rel **18** 升正式。 |
| 5 嘉和文书科室→HIS 科室 | rel **26** 升正式（5万样本孤儿 0）。 |
| 6 医师执业对照 | 8.216 和 HIS 源端都**没有** `DOC_REGISTER_EMP_MATCH`。源端只见到 `OUTPDOCT.OUTP_DOCTOR_REGIST`。rel 27 仍不升。 |

## 4. 嘉和那批候选（把第 7 条说清楚）

平台里剩余候选一共三类，不是一锅粥：

### 4.1 视图依赖（203 条）——不当正式业务关系

例如 `jhemr.pat_visit → jhemr.v_cdaupload`、`jhemr.pats_in_hospital → jhemr.v_jhnis_*`。  
这是视图 SQL 解析出来的“表被视图引用”，应留在候选/依赖层，**默认不升正式**。

### 4.2 真表对真表，且已有就诊键（6 条）——可以点名再升

| rel_id | 从 | 到 |
|---|---|---|
| 85 | `jhemr.jhmr_file_index` | `jhemr.emr_bcjl` |
| 86 | `jhemr.pat_diagnosis` | `jhemr.emr_phms` |
| 87 | `jhemr.pat_visit` | `jhemr.emr_phms` |
| 88 | `jhemr.diagnosis` | `jhemr.final_chief_diagnosis` |
| 89 | `jhemr.pat_visit` | `jhemr.his_sybccx` |
| 90 | `jhemr.jhmr_file_index` | `jhemr.jhmr_file_index_jc` |

2026-08-14 用户点名后已处理：

| rel_id | 处理 |
|---|---|
| 85 `jhmr_file_index → emr_bcjl` | **不纳入**。用户确认 `emr_bcjl` 无用。已 `rejected`。活库对该表 `SELECT` 会触发 `blob_to_char` GBK 失败。 |
| 86 `pat_diagnosis → emr_phms` | **不纳入**。用户确认 `emr_phms` 无用。已 `rejected`。 |
| 87 `pat_visit → emr_phms` | **不纳入**。同上。 |
| 88 `diagnosis → final_chief_diagnosis` | **升正式**。嘉和源库：主诊断表 79925 行；按 `PATIENT_ID+VISIT_ID` 挂 `diagnosis` 0 孤儿。诊断可多条（1:N）。反向 2 万就诊键仅 2 条无主诊断。 |
| 89 `pat_visit → his_sybccx` | **升正式**。嘉和源库全表 1281 行，0 孤儿。 |
| 90 `jhmr_file_index → jhmr_file_index_jc` | **升正式**。嘉和源库抽样 0 孤儿；检查文书索引现约 124 万行，仍在写入。 |

### 4.3 真表对真表，但没有关联字段（约 81 条）——缺键，不能猜

主要是两类报表/首页：

- 单病种/慢病：`ba_manbing`、`dbz_jbxx`、`dbz_jbxx0327`、`dbz_zdxx`、`dbz_ssxx`、`dbz_icuxx`
- 病案首页切片：`mt_medical_record_first_*`、`discharge_summary`、`emr_medical_record_info`

常见左边是 `pat_visit` / `pat_master_index` / `diagnosis` / `dept_dict` / `emr_first_page_item_dict`，右边是上面这些表，**CSV 解析时没带 JOIN 键**。  
例如：`jhemr.pat_visit → jhemr.ba_manbing`（rel 48）现在仍无键。

请按业务点名，例如：「48、50、60 用 PATIENT_ID+VISIT_ID 去验」。未点名的继续放着。

## 6. 用户提供的难对照费用关系（2026-08-14 第三轮）

| 用户口径 | 复核 | 处理 |
|---|---|---|
| 住院检验检查费用：`ORDERS.APP_NO = INP_BILL_DETAIL.OPER_CODE` | ODS 5000 条 `LENGTH(OPER_CODE)>=8` 命中 98%；HIS 源端 5990 条命中 91.5%。短码多为操作员，不能全表无条件 JOIN。`ORDERS_COSTS` 仍按四键挂 `ORDERS`（已有 formal 454）。 | **正式 557**（子集） |
| 门诊开单 `OUTP_ORDER_DESC` 有重复，用 `VISIT_DATE+VISIT_NO+RCPT_NO` 挂 `OUTP_BILL_ITEMS` | ODS/HIS 样本孤儿 0；仅 `RCPT_NO` 会一对多放大。 | **正式 558** |
| `OUTP_BILL_ITEMS.APPOINT_NO+ITEM_NO = LAB_RESULT.TEST_NO+ITEM_NO` | HIS 源端 20451 行命中 65.9%。全量费用行还含检查等，不能当全表关系。 | **正式 559**（仅检验收费行） |
| `OUTP_ORDERS_COSTS` 用 `ORDER_ID+ORDER_ITEM_ID+ORDER_ITEM_COST_ID` 挂费用；`CHARGE_INDICATOR` 1 收费 / 2 退费 / 空未收费 | 表在 HIS 源端 `OUTPDOCT`，三键都在。已收费样本三键命中约 37%；`VISIT_DATE+VISIT_NO+RCPT_NO` 命中 0。活库标记是 **1=收费、0=未收费、2=退费**（不是空）。ODS 无此表。 | **不升正式** |

## 5. 回写位置

- 平台：`asset.asset_relations`（上表 rel_id）
- 本地：`开发起步包/数据资产_资产包/relationships.csv`、`catalog.json`

## 7. 第四轮（2026-08-14）：嘉和点名 + HISUSER 同步 + 命中率界面

### 7.1 HISUSER 权威规则已入库

`asset.asset_change_rules.rule_code = HIS_SOURCE_AUTHORITATIVE_SYNC`

更新 ODS `HIS.*` 正式关系时，必须同步 HISUSER/HIS_SOURCE 对应 Owner 表。HISUSER 是原始库，以它的实测为准。

本轮补的 HISUSER 正式孪生：

| rel_id | HISUSER 关系 | 对应 ODS | 源端样本 |
|---|---|---|---|
| 560 | `MEDREC.PAT_VISIT → EXAM.EXAM_MASTER` 住院 | 553 | 20000 / 孤儿 4 |
| 561 | `EXAM.EXAM_MASTER → OUTPBILL.OUTP_RCPT_MASTER` 门诊 | 554 | 20000 / 孤儿 0 |
| 562 | `MEDREC.PAT_VISIT → LAB.LAB_TEST_MASTER` 住院 | 555 | 20000 / 孤儿 7 |
| 563 | `LAB.LAB_TEST_MASTER → OUTPBILL.OUTP_RCPT_MASTER` 门诊 | 556 | 20000 / 孤儿 0 |
| 564 | `INPBILL.INP_BILL_DETAIL.OPER_CODE → ORDADM.ORDERS.APP_NO` | 557 | 5990 / 未命中 511 |
| 565 | `OUTPBILL.OUTP_ORDER_DESC` 三联键 `OUTP_BILL_ITEMS` | 558 | 20000 / 孤儿 0 |
| 566 | `OUTPBILL.OUTP_BILL_ITEMS → LAB.LAB_RESULT` | 559 | 20451 / 命中 65.9% |
| 567 | `MEDREC.DIAGNOSIS → COMM.DIAGNOSIS_TYPE_DICT` | 21 | 20000 / 孤儿 0 |
| 568 | `LAB.LAB_TEST_ITEMS → LAB.LAB_RESULT` | 18 | 22721 / 孤儿 324 |
| 569 | `INPBILL.INP_SETTLE_MASTER → INPBILL.INP_BILL_DETAIL` 就诊键 | 8 | 沿用 ODS 1996/2000 |

### 7.2 建议标无用的患者相关表（超过约 1 个月无有效写入，或用户已点名）

| 表 | 理由 | 本轮是否改资产状态 |
|---|---|---|
| `jhemr.emr_bcjl` | 用户确认无用；对象是**视图**，`SELECT` 触发 `blob_to_char` GBK 失败 | 已 `excluded_unused` |
| `jhemr.emr_phms` | 用户确认无用；125 万行但无写入时间列，不纳入正式关系 | 已 `excluded_unused` |
| `jhemr.dbz_jbxx0327` | 表名带日期切片，像一次性导出 | 仅建议，未改状态 |
| `jhemr.dbz_jbxx` 等单病种 | 无 JOIN 键；计数/最大出院日超时 | 仅建议，C 组未点名不升 |
| `jhemr.BA_MANBING` | 44.5 万行，入院时间最晚 2026-08-12，**仍在写** | 不判无用 |
| `jhemr.discharge_summary` | 7.9 万行，出院时间最晚 2026-08-13，**仍在写** | 不判无用 |
| HIS 核心就诊/费用/检验检查表 | `PAT_VISIT`/`ORDERS`/`OUTP_RCPT_MASTER` 等 8 月仍有统计 | 保持 |

`jhmr_file_index` / `jhmr_file_index_jc` 最晚写入 2026-08-14，是活表。

### 7.3 命中率界面

前端 `/asset/relation-rates`：正式关系命中率表 + 检查/检验门诊住院四张卡片。接口 `/api/v1/relations/hit-rates`、`/api/v1/relations/authority-rule`。

业务源库零写。
