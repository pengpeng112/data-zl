> 类别：交接
>
> 状态：待处理 | 优先级：P0 | 制定日期：2026-08-04
> 关联：`111`/`113`（代码安全）、`112`/`114`/`115`（双同步）、`116`（合并收口）、`55`（唯一未完成入口）
> 用途：独立复核后**已修确定项**说明 + **不确定/未闭环**交接清单，供后续 AI/人工按授权处理

# 111 与 112 独立复核：已修确定项与待处理交接

## 0. 一句话结论

- **确定可修的代码问题已落地**（字典写后回读、sequence 标识符白名单），纯逻辑测试 **22 passed / 1 skipped**。
- **不能写成“111/112 全部验收完成”或“可正式同步/可开 nightly”**。
- 下文「待处理」项需隔离测试库、DBA、运维或用户书面授权；处理时以 `55` + `116` 为执行入口。

---

## 1. 本轮已完成的确定修复（代码）

| ID | 问题 | 处理 | 关键文件 |
|---|---|---|---|
| F1 | 字典目标 `dispatch_target` 在 DML 成功后直接 commit 并标 `succeeded`，无独立回读 | **commit 前**同连接 `readback_actions_on_conn`：0 行/多行/停用态不符 → HTTP 409/500，整包 rollback，run=`failed` + `reconcile_required` | `backend/app/services/medical_code_push.py`、`backend/app/services/dict_sync_executor.py` |
| F2 | `APP_JHEMR_SERIAL_WHITELISTED_SEQUENCE` 拼进 SQL，无标识符校验 | 新增 `validate_sequence_identifier`（仅 `schema.object` 简单标识）；Oracle `NEXTVAL` 仍用已校验标识符；PostgreSQL 用 `nextval(%s)` 绑定 | `medical_code_push.py` |
| F3 | `115` 夸大：写后回读“已完备”、Git“未 commit” | 已勘误：回读曾缺失现已修；代码在**本地 commit 链**中，**未 push/tag** | `115_...完成记录.md` §一/§九 |

### 1.1 验收命令（本机已跑）

```powershell
cd F:\python\数据资产\backend
.\.venv\Scripts\python.exe -m pytest tests/db_guard tests/role_effective tests/startup_check tests/security_audit tests/dict_sync_112 -q
```

结果：**22 passed, 1 skipped**（2026-08-04）。

新增/加强用例：

- `backend/tests/dict_sync_112/test_readback_and_sequence.py`
- `backend/tests/dict_sync_112/test_additional_boundaries.py`（sequence 校验补充）

### 1.2 本轮明确未做

- 未连接/未写入生产平台库与 HIS/JHEMR/CDMS
- 未开启任何写开关或 nightly
- 未 `alembic upgrade` 实跑
- 未 commit/push/tag（**本轮修复相对 HEAD 的工作区改动，由用户决定是否纳入提交**）
- 未处理 Excel/历史资产 CSV/前端纯行尾改动

### 1.3 当前默认开关（本机 settings，无 secret）

| 开关 | 默认 |
|---|---|
| `dict_medical_push_enabled` | false |
| `identity_sync_enabled` | false |
| `identity_nightly_enabled` | false |
| `identity_jhemr_password_write_enabled` | false |
| `identity_cdms_fid_semantics_confirmed` | false |
| `jhemr_serial_whitelisted_sequence` | 空 |
| `ops_write_enabled` | false |

Alembic head（迁移图）：`c0ffee12dead`（down=`f8a9b0c1d2e3`）。**生产是否已 upgrade 本机未复核。**

---

## 2. 待处理清单（不确定 / 缺环境 / 需授权）

> 后续 AI：**只处理用户点名项**；缺隔离库或写授权时必须报告阻断，不得用生产替代。

### 2.1 P0 — 测试与迁移（111 S1/S2 + 112 实跑）

| ID | 事项 | 为何未做 | 建议动作 | 验收标准 |
|---|---|---|---|---|
| U1 | 合法隔离 `APP_TEST_DB_URL` | 本机未配置 | 运维提供**仅测试**库；库名含 `test`；与 denylist 不冲突 | 打印脱敏库名分类；禁止含生产库名 |
| U2 | `alembic upgrade head` 含 `c0ffee12dead` | 无隔离库 | 仅在 U1 库执行 upgrade → downgrade 一版 → upgrade | ORM 与 schema 一致；outbox 表存在 |
| U3 | 全量 pytest **连续两次** 0 failed | 依赖 U1 | `pytest tests/ -q` 跑两次 | 不得用 `--noconftest`；不得回退 `.env` 生产 URL |
| U4 | outbox 租约/审批同事务原子性 DB 测 | 纯逻辑测无真 DB | 在 U1 写集成测：审批 rollback 无 outbox 行；SKIP LOCKED 并发 | 与 114 T1 清单对齐 |

### 2.2 P0 — 111 安全收口缺口

| ID | 事项 | 证据现状 | 建议动作 | 验收标准 |
|---|---|---|---|---|
| U5 | S5 **自动路由审计** | 未见扫描全部 POST/PUT/PATCH/DELETE 的模块/测试；仅有部分端点 `require_permission` + 权限码登记 | 实现 OpenAPI/路由扫描：写方法必须有依赖或公开白名单 | 新增端点缺权限则测试失败 |
| U6 | S3 撤权后旧 JWT 立即 403 | 设计为实时查 `_effective_role_codes`；**无隔离库 E2E** | U1 后做：授权 → 发 JWT → 撤权/过期 → 同 token 403 | 不得依赖 JWT 过期 |
| U7 | S6 异常脱敏全域 | 仍有 `sanitize_text(str(exc))` 作 API detail、`job.error_message=str(exc)` 等 | 逐文件清零客户端可见 `str(exc)`；审计字段统一脱敏器 | 注入恶意异常文本测试全绿 |
| U8 | S7 `requirements.lock` + 断网演练 | 仓库无 lock；未断网演练 | 在目标 Linux 同架构生成带哈希 lock；隔离环境离线装 | 缺 wheel/哈希失败关闭 |
| U9 | S7 脚本 `AutoAddPolicy` | `deploy/scripts/sync_local_to_server.py` 已改；**大量** `backend/scripts/*` 仍 AutoAdd | 裁决：归档临时脚本 / known_hosts 后拒绝未知主机 | 部署链脚本无 AutoAddPolicy |
| U10 | S8 前端 typecheck/test/build | 历史文档称通过；**本轮未重跑** | `pnpm install --frozen-lockfile && pnpm run typecheck && pnpm test && pnpm run build` | 两者 typecheck + Vitest + build 全绿 |
| U11 | S9 Git 收口 | 工作区大量未跟踪/行尾/Excel；功能 diff 与噪声混杂 | 用户授权后**仅**提交功能相关文件；禁止 Excel/备份/临时脚本 | 秘密扫描通过；功能 diff 可审计 |

### 2.3 P0 — 112 业务同步启用前

| ID | 事项 | 为何阻断 | 建议动作 | 验收标准 |
|---|---|---|---|---|
| U12 | 字典写通道登记 | 历史上源端 `write_policy=readonly`、无 write credential | 平台登记 HIS/JHEMR **字典专用**写凭据与 `medical_dict_push` 策略 | 与身份写凭据 ref **分离**；最小权限 |
| U13 | HIS 字典重复编码治理 | 无 PK/UK，存在同编码多行 | DBA/业务裁决：去重或唯一约束（**仅建议，代码不 DDL**） | 同编码 0/1/多行策略在目标库可复现 |
| U14 | JHEMR `serial_no` 取号方案 | 无关联 sequence；代码仅接受白名单名 | **用户/DBA 书面**选定 sequence 名或并发安全 allocator；配置 `APP_JHEMR_SERIAL_WHITELISTED_SEQUENCE` | 隔离目标库并发取号无冲突；**禁止 MAX+1** |
| U15 | 字典双目标真实联调 | 无隔离目标库授权 | mock 之后：HIS 成功/JHEMR 失败 → `reconcile_required`；回读失败不标成功 | 开关仍默认 false；仅测试库 DML |
| U16 | 身份 Phase D | secrets/HMAC/默认密码/Phase D 审批版本/可连通性 | 按 107/110 § 前置清单；受控灰 1 医 1 护 | 四方比对通过才考虑 nightly |
| U17 | CDMS 语义开关 | 代码默认 `identity_cdms_fid_semantics_confirmed=false`；115 写用户已确认映射 | **用户书面确认**后，仅在目标环境设 true | 生产默认仍 false 直到放行 |
| U18 | JHEMR 密码写 | 默认关；需 SM4 交叉验证 | 独立授权后再开 `identity_jhemr_password_write_enabled` | 无 DELETE；最小权限 |
| U19 | 生产平台 revision | 历史只读为 `f8a9b0c1d2e3`；本地 head 已是 `c0ffee12dead` | 只读 `alembic current`；**另授权**再 upgrade | 升级前备份；禁止业务库 DML |

### 2.4 P1 — 文档与状态一致

| ID | 事项 | 说明 |
|---|---|---|
| U20 | `111` 文首仍「待执行」vs README「S0–S8 完成」 | 统一为：代码完成+纯逻辑测；DB/Git 未闭环 |
| U21 | `116`「S9 待完成」与 README「S9 文档收口完成」 | 建议：S9 **文档部分**完成，**Git clean/提交**未完成 |
| U22 | origin 未含 111/112 代码 | `master` 本地 ahead；push 需用户明确授权 |

---

## 3. 禁止事项（交接执行时仍有效）

1. 禁止生产平台库 / HIS / JHEMR / CDMS / ODS 未授权 DML/DDL  
2. 禁止启用 `APP_DICT_MEDICAL_PUSH_ENABLED`、`APP_IDENTITY_SYNC_ENABLED`、nightly、密码写等生产写开关  
3. 禁止 `MAX(serial_no)+1`、随机取号、BackgroundTasks/裸线程当队列  
4. 禁止客户端提交 SQL/表名/连接串/action  
5. 禁止 force-push、移动旧 tag、创建 release tag（除非用户明示）  
6. 禁止把 dry-run / pending_reconcile / mock 成功写成真实同步成功  
7. 禁止提交 Excel、备份、临时 `_*.py` 探测脚本、用户其他未审核改动  

---

## 4. 建议处理顺序

```text
U1 隔离测试库
 → U2 迁移 c0ffee12dead
 → U3+U4 全量/集成测
 → U5–U7 安全缺口（可与 U10 并行）
 → U14 serial 书面方案
 → U12 字典写通道（测试环境）
 → U15 字典双目标隔离联调
 → U16–U18 身份 Phase D
 → U19 生产 upgrade（另授权）
 → U11/U22 Git 收口与 push（另授权）
```

与 `116` 合并收口顺序一致；冲突时以 `55` 裁决。

---

## 5. 交给后续 AI 的最短提示词

```text
严格执行 开发起步包/118_111与112独立复核修复与待处理交接.md。

先读 AGENTS.md、开发起步包/README.md、55、116、118。
已完成 F1/F2 代码修复，不得无故重写 outbox/worker/bridge。
只处理用户点名的 U 编号；无隔离 APP_TEST_DB_URL 时禁止全量 DB 测试与业务写。
禁止生产 DML/DDL、禁止开写开关、禁止提交 Excel/备份/临时脚本。
每项回报：状态、命令与结果、库环境分类、DML 计数、是否阻断。
```

---

## 6. 本轮修改文件清单（确定修复）

| 文件 | 变更 |
|---|---|
| `backend/app/services/medical_code_push.py` | sequence 校验；同连接 SELECT 回读 |
| `backend/app/services/dict_sync_executor.py` | commit 前 readback；失败标 failed/reconcile |
| `backend/tests/dict_sync_112/test_readback_and_sequence.py` | 新增 |
| `backend/tests/dict_sync_112/test_additional_boundaries.py` | sequence 边界 |
| `开发起步包/115_112号双同步专项整改完成记录.md` | 勘误 + §九 |
| `开发起步包/118_111与112独立复核修复与待处理交接.md` | 本文 |
| `开发起步包/README.md` | 登记 |

---

## 7. 状态摘要（供 55/README 引用）

| 轨道 | 状态 |
|---|---|
| 111 | S1–S4 代码+纯逻辑测可信；S5 自动审计/S6 全域/S7 lock/S8 本轮未复跑/S9 Git 未闭环 → 见 U5–U11 |
| 112 | A/B 代码主体 + F1/F2 修复；纯逻辑 22p/1s；目标库/DBA/Phase D → 见 U12–U19 |
| 生产同步 | **未启用**；不得标记完成 |
