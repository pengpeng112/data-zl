> 类别：交接
>
> 状态：当前有效 | 形成：2026-08-11 | 服务对象：其他 AI / 接手人
> 上位入口：`55_系统未完成事项统一执行计划.md` + `开发起步包/README.md`
> 对应计划：`126_AI查询SQL与统计指标闭环治理建设计划.md`（§23.2–§23.6）

# 126 号 AI 查询 SQL 与统计指标闭环 — 完成情况与接手交接

> **144 实施注记（2026-08-23）**：144 S2–S9 已落地以下实现，本文件中与之不一致的描述以运行时 `/docs` 与 144 §16.1 执行记录为准——
> 参数真实 bind 连接器、状态/来源/大表（AST 含 WHERE 1=1 拒绝）门禁、result/schema digest 与 data_as_of、指标真实计算引擎（批次/维度唯一键）、
> 精确 object_key 与静态血缘（asset_lineage_edges）、统一 AI context（/api/v1/ai/context/resolve）、反馈—评测闭环（/api/v1/ai/feedback|evaluations）。
> 旧 AI context 返回完整 SQL 的行为已改为默认仅哈希（需 ai:sql:full_read 权限）。


本文供**其他 AI 或接手人**快速接手，无需回溯长会话。只写已核实事实、生产状态、禁止动作与下一步入口。

---

## 1. 一句话结论

**126 号 P1–P5 最小闭环已在生产 10.10.8.83 落地并可用。**

- 平台库 head：`h5c6d7e8f9a0`
- 后端镜像：`data-asset:126p5-20260811223311`（tag 同时有 `latest-hot`）
- 前端发布：`/opt/data-asset/frontend-dist/current` → `releases/126p5-20260811223311`
- 业务源库（HIS/ODS 等）写入：**0**
- **未做完、需业务/授权的项**见 §7；**127 质量治理生产发布尚未做**（与 126 并行但独立）

---

## 2. 接手前必读（顺序）

1. 根目录 `AGENTS.md`（连接、只读红线、技能路由）
2. `开发起步包/README.md`（唯一目录）
3. `开发起步包/55_系统未完成事项统一执行计划.md`（唯一未完成事项入口）
4. `开发起步包/126_AI查询SQL与统计指标闭环治理建设计划.md` §23
5. 技能：`.agents/skills/query-governance-intake/SKILL.md`（取数/查询摄取）
6. 本地工作区：`F:\python\数据资产\取数\START_HERE.md`

**禁止**把聊天记忆当权威；以平台 API / 库表 / 本文 / 126 文档为准。

---

## 3. 目标回顾（126 做了什么）

| 阶段 | 目标 | 生产状态 |
|---|---|---|
| P1 | 查询资产：版本化 SQL、门禁、运行留痕、`queryctl`、取数工作区 | **已生产** |
| P2 | 指标资产：口径版本、可选结果、引用 query | **已生产** |
| P3 | 影响分析、JOIN 候选、调度表（默认关）+ 48 项试点导入 | **已生产** |
| P4 | 数据产品目录 + 参数化执行（禁任意 SQL） | **已生产** |
| P5 最小 | 指标看板、MCP 工具目录、多源能力探测、调度安全启用 | **已生产** |

---

## 4. 生产事实（2026-08-11 核实）

### 4.1 环境

| 项 | 值 |
|---|---|
| 平台服务器 | `10.10.8.83`（SSH 公钥 `C:\Users\Administrator\.ssh\id_ed25519_ai`） |
| 平台库 | PostgreSQL `data_asset`（schema `asset`） |
| 测试库 | `data_asset_test`（本机经隧道 `127.0.0.1:15432`） |
| API 容器 | `data-asset-api`，`--network host`，监听 `127.0.0.1:8000` |
| 凭据挂载 | `/etc/data-asset/credentials`；环境文件 `/etc/data-asset/backend.env` |
| Oracle thick | `/opt/oracle` 只读挂载 |
| 前端 | nginx → `/opt/data-asset/frontend-dist/current` |
| 备份目录 | `/opt/data-asset/backups/` |
| 发布目录 | `/opt/data-asset/releases/` |

### 4.2 数据库迁移与计数（生产）

| 项 | 值 |
|---|---|
| Alembic head | `h5c6d7e8f9a0`（`asset_data_products`） |
| 查询定义 | 27（active 版本 27，`QRY_CORE_*`） |
| 指标定义 | 48（active 27 + 待补录占位 21） |
| 数据产品 | 54（`DP_QRY_CORE_*` / `DP_MET_CORE_*`） |
| 历史指标结果 | 去重后约 **189** 行（每 metric+period 保留最新；导入已幂等） |
| 调度定义 | 27 条，**enabled 全部为 false** |
| 全局调度开关 | `APP_QUERY_SCHEDULE_ENABLED=true`（单条全关 → **0 个查询定时任务注册**） |

### 4.3 关键发布与回滚锚点

| 类型 | 路径 / 标识 |
|---|---|
| 本轮镜像 | `data-asset:126p5-20260811223311` |
| 前一镜像族 | `data-asset:126complete-20260811220449`、`signature125-20260810161506` |
| 本轮前端 | `frontend-dist/releases/126p5-20260811223311` |
| 前端 previous | `126complete-20260811220449` |
| 本轮 DB 备份 | `data_asset_pre_126p5-20260811223311.dump` |
| 更早 126 备份 | `data_asset_pre_126complete-20260811220449.dump`、`data_asset_pre_126p12-...` 等 |

**前端回滚示例**：

```bash
ln -sfn /opt/data-asset/frontend-dist/releases/126complete-20260811220449 \
  /opt/data-asset/frontend-dist/current
nginx -t && systemctl reload nginx
```

**后端回滚**：停掉当前容器，用旧镜像 + 同一 `--env-file` / 挂载重新 `docker run`（host 网络、8000）；DB 需时用对应 dump 恢复（高风险，需授权）。

### 4.4 容器启动要点（重建时必须一致）

```text
--network host
--restart unless-stopped
--env-file /etc/data-asset/backend.env
-v /opt/oracle:/opt/oracle:ro
-v /etc/data-asset/credentials:/etc/data-asset/credentials:rw
CMD: bash -lc "bash /app/deploy/scripts/ensure_oracle_ro_runtime.sh || true; uvicorn app.main:app --host 127.0.0.1 --port 8000 --workers 1"
```

历史发布多为 **docker cp 热补丁 + docker commit**，不是从零 `Dockerfile` 全量 wheel 构建。  
热补丁模板：`deploy/docker/Dockerfile.hotpatch`（`FROM` 已知良好镜像，只叠 `app/` + `alembic/`）。

---

## 5. 功能地图（API / 页面 / 工具）

### 5.1 前端

- 路由：`/asset/queries`（查询与指标中心）
- 页签：查询资产 | 运行记录 | 统计指标 | **数据产品** | **指标看板** | **调度与多源**

### 5.2 后端 API（前缀 `/api/v1`）

| 模块 | 路径要点 |
|---|---|
| 查询 | `/queries` list/detail/ingest/gate/run/versions/diff；`/queries/import/core-48`；`/queries/impact/table`；`/queries/{code}/relation-candidates` |
| 调度 | `/queries/schedules/list`；`POST /queries/schedules`；`POST /queries/schedules/seed-core` |
| 多源 | `GET /queries/sources/capabilities` |
| 指标 | `/metrics` list/detail/ingest；`POST /metrics/results`；`GET /metrics/{code}/results`；**`GET /metrics/board/overview`** |
| 数据产品 | `/data-products`；`POST .../publish-core`；`POST .../{code}/execute`；`POST .../import/metric-results`；`/data-products/ai/context` |
| AI/MCP | `GET /ai/tools`；**`GET /ai/mcp/catalog`**（126 工具子集，禁 arbitrary_sql） |

**硬约束**：

- 自动门禁通过后可直接 active（无管理员确认环节）
- 结果默认 `result_storage=none`（可不存明细）
- **数据产品禁止任意 SQL**，只能跑已发布产品
- 源库只读 SELECT；大表禁全扫

### 5.3 本地工具与工作区

| 路径 | 用途 |
|---|---|
| `tools/queryctl.py` | init / validate / submit（outbox） |
| `tools/import_core_48_metrics.py` | 48 项 SQL 批量导入（仓库现有 27 个文件） |
| `取数/` | 固定工作区；`48项目核心制度/指标SQL/` 现有 27 个指标 SQL |
| `backend/var/finish_126_remaining.py` | 生产收尾：占位标题刷新、CSV 结果、发布产品、种子调度 |
| `.agents/skills/query-governance-intake/` | 日常取数摄取技能 |

### 5.4 核心代码位置（仓库）

```
backend/app/models/query_asset.py
backend/app/models/metric_asset.py
backend/app/models/query_schedule.py
backend/app/models/data_product.py
backend/app/services/query_*.py
backend/app/services/metric_*.py
backend/app/services/data_product_service.py
backend/app/services/core_metric_import.py
backend/app/api/v1/queries.py
backend/app/api/v1/metrics.py
backend/app/api/v1/data_products.py
backend/app/api/v1/ai.py          # tools + mcp/catalog
backend/alembic/versions/
  e2f3a4b5c6d7_add_query_asset_tables_126.py
  f3a4b5c6d7e8_add_metric_asset_tables_126.py
  g4b5c6d7e8f9_add_query_schedules_126p3.py
  h5c6d7e8f9a0_add_data_products_126p4.py
frontend/src/views/query-center/queries/index.vue
```

### 5.5 关键表（asset schema）

- 查询：`asset_query_definitions` / `versions` / `dependencies` / `runs` / `results`
- 指标：`asset_metric_definitions` / `versions` / `results`
- 调度：`asset_query_schedules`
- 产品：`asset_data_products`

---

## 6. 48 项核心制度试点现状

### 6.1 有 SQL、已 active（27）

仓库路径：`取数/48项目核心制度/指标SQL/`  
编号大致覆盖：03–10、16–21、23–28、31–32、41–45（以库内 `QRY_CORE_*` / `MET_CORE_*` 为准）。

导入来源：拆分 SQL + `core_metric_import` / `POST /queries/import/core-48`。

### 6.2 无 SQL、已登记占位（21，blocked，非 active）

官方标题已从 `48项核心制度监测指标(1).docx` 回写，例如：

| 编码 | 标题（节选） |
|---|---|
| MET_CORE_01 | 患者入院48小时内转科率 |
| MET_CORE_02 | 患者入院8小时内查房率 |
| MET_CORE_11–15 | 会诊/护理/值班等 |
| MET_CORE_22 | 抢救成功率 |
| MET_CORE_29–30 | 医嘱终止 / 术者授权 |
| MET_CORE_33–40 | 手术并发症比、新技术、危急值、安核等 |
| MET_CORE_46–48 | 抗菌药/用血等 |

**不得**在无业务确认的情况下伪造 SQL 并 auto_activate。

### 6.3 历史月度结果

- 来源：`取数/48项目核心制度/*.csv`（结果/核查类）
- 导入服务：`metric_result_import.py`（已做同 metric+period+分子分母幂等）
- 看板读最新批次：`GET /metrics/board/overview`

---

## 7. 未完成 / 可选下一步（接手任务池）

按优先级建议：

| 优先级 | 事项 | 说明 | 授权 |
|---|---|---|---|
| P0 若做治理 | **127 生产发布** | 质量/总览/关系复核/配方/图谱标签等整改仅测试门禁完成 | 生产发布 + 数据 apply 需单独授权 |
| P1 | **21 条指标 SQL 补录** | 按官方口径写只读 SQL → gate → ingest → 修订 active | 源库只读；勿误激活错误口径 |
| P1 | **单条调度启用** | 全局已开；仅对确认安全的 `query_code` 设 `enabled=true` | 改调度/跑生产定时需明确授权 |
| P2 | 看板图表 / 导出增强 | 现为表格 + CSV | 普通开发 |
| P2 | 深度多源适配 | 现为 capabilities 探测，非全厂商契约测试 | 按系统 |
| P2 | 从零镜像重建 | 现 hotpatch+commit；全量 Dockerfile+wheels 未做 | 运维窗口 |
| — | Git 收口 | 本地大量未提交变更（含 126/127） | push/tag 需秘密扫描 + 授权 |

**不要**默认开启：identity nightly、ops write、对业务库 DML。

---

## 8. 验收命令（接手自检）

### 8.1 本机后端专项（需测试库隧道）

```powershell
# SSH 隧道示例：本机 15432 -> 8.83:5432
cd F:\python\数据资产\backend
$env:APP_TEST_DB_URL="postgresql+psycopg://postgres:***@127.0.0.1:15432/data_asset_test"
$env:APP_DB_URL=$env:APP_TEST_DB_URL
.\.venv\Scripts\python.exe -m pytest tests/test_query_asset.py tests/test_metric_asset.py tests/test_data_products.py tests/test_plan126_p5.py -q
```

历史结果：相关专项约 **18 passed**（P5 批次）。

### 8.2 前端

```powershell
cd F:\python\数据资产\frontend
pnpm install
pnpm run typecheck
pnpm run build
```

### 8.3 生产只读检查（SSH 到 8.83）

```bash
curl -fsS http://127.0.0.1:8000/api/v1/health
docker exec data-asset-api printenv APP_QUERY_SCHEDULE_ENABLED
sudo -u postgres /usr/local/pgsql/bin/psql -d data_asset -c "SELECT version_num FROM alembic_version;"
sudo -u postgres /usr/local/pgsql/bin/psql -d data_asset -c \
  "SELECT count(*) q FROM asset.asset_query_definitions;
   SELECT count(*) m FROM asset.asset_metric_definitions;
   SELECT count(*) p FROM asset.asset_data_products;
   SELECT count(*) s, count(*) FILTER (WHERE enabled) se FROM asset.asset_query_schedules;"
# OpenAPI 关键路径应存在：
# /api/v1/metrics/board/overview
# /api/v1/data-products
# /api/v1/ai/mcp/catalog
# /api/v1/queries/sources/capabilities
readlink -f /opt/data-asset/frontend-dist/current
```

看板/产品列表接口通常需登录 RBAC（匿名 401 属预期）。

---

## 9. 安全红线（接手必守）

1. HIS / ODS / HRP / 手麻 / 护理等**业务源库只读**，禁止 DML/DDL。  
2. 密码、Token、身份证、姓名、电话、地址不进代码、日志、git、新文档。  
3. 平台写操作走审批/审计/白名单；AI 不接写执行器。  
4. 迁移手写 upgrade/downgrade，禁 autogenerate；新表仅 `asset` schema + `asset_*` 前缀。  
5. 生产发布、调度真正跑批、平台 apply、Git push/tag：**需用户明确授权**。  
6. 大表（如 `HIS.LAB_RESULT`）禁止无键全扫。  

---

## 10. 给接手 AI 的推荐首步

**若继续 126：**

1. 只读核对 §8.3 生产状态是否与本文一致。  
2. 打开 `/asset/queries` 确认看板、产品、调度页可用。  
3. 若补 21 条 SQL：先读 docx 口径 + ODS/HIS 技能，只写 SELECT，走 gate/ingest，**不要**静默改已有 active 口径。  
4. 若启用某条调度：先 dry-run 手动 `POST` 执行对应 query，再 `enabled=true`。

**若转 127：**

1. 读 `127_数据质量与资产治理功能复核整改执行计划.md`。  
2. 生产发布前确认 127 代码是否已随 126p5 热补丁进镜像（代码树已含 127 改动时可能已在容器中，但仍需按 127 验收清单做生产验证与 apply 授权）。  

**若做 Git 收口：**

1. 秘密扫描；不 commit 凭据与 dump。  
2. 分组提交文档/后端/前端；无 force-push。  

---

## 11. 会话中形成的辅助产物（可参考，非权威）

| 路径 | 说明 |
|---|---|
| `backend/var/finish_126_remaining.py` | 生产收尾脚本 |
| `backend/var/126p5-*-backend.tar.gz` / frontend tar | 发布包 |
| `backend/var/deploy_p3_prod.sh` | 早期 P3 部署参考 |
| `deploy/docker/Dockerfile.hotpatch` | 热补丁镜像构建说明 |
| `backend/tests/test_plan126_p5.py` | P5 契约测试 |

---

## 12. 文档维护

- 本文件编号：**128**
- 完成状态细节权威回写：`126` §23.2–§23.6、`55` 执行日志、`README` 目录更新记录
- 新增后续报告时：续号登记 README，不另建平行总表

---

**交接完成标志**：接手 AI 能独立完成 §8 自检，并按 §7 在授权范围内推进，而无需依赖原会话上下文。
