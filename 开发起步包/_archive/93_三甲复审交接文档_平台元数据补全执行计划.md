> 类别：执行计划（已复核修订，可执行）
>
> 状态：v3 · 2026-07-27 · 按《93_复核意见_基线失实与表归属错误.md》修订；v2 基线取自陈旧库（10.20.1.153），v3 基线由编写者经 SSH 8.83 → 容器 data-asset-api 对平台活库**亲自复测确认**（复测同时验证了复核全部结论，并新增 2 项复核未发现的关键事实，见 §1.4）
>
> 输入文档：`D:\Users\Administrator\Desktop\三甲复审\1、三甲复审统计指标\00_入口与说明\SQL与数据架构交接文档_20260727.md`（下称「交接文档」）
>
> 关联文档：93_复核意见_基线失实与表归属错误.md、88_三甲复审视图跨系统关系复核报告

# 三甲复审交接文档 · 平台元数据补全执行计划（v3 修订版）

## 0. v2→v3 修订说明

复核报告 §6 指出强行执行 v2 的 6 个失败点，v3 逐条响应：

| 复核失败点 | v3 处置 |
|-----------|---------|
| T1.1/T1.2 upsert 变空操作 | 阶段 1 重写为「小修补」：仅补 DATA_CENTER.target_host、刷新 asset_source_schemas 统计（§3） |
| 6 张表 source_code 错配 | 全部更正：MTL/JHEMR 5 张 → `ods_emr`；MCS_DOC_FORM → `ods_ydhl`；CSV 按源拆 3 个（§4.1） |
| R10–R14 定位失败 | 关系定位全部按修正后 source_code；且实测发现 **R01–R14 大部分已存在且 verified**，缺口仅 3 条（§5.1） |
| R15 from_source=his_ready 已不存在 | 改 `his_source_10_10_10_15`（§5.1 R15） |
| A2.1「期望 17」变 11 | 验收改为按 business_desc_cn 计数，3 源合计 17（§4.4 A2.1） |
| A1.3「期望 31」变 23 | 验收改为按各源实算（23+3+1+1+1+1+1=31 行已存在，仅刷新统计）（§3 A1.3） |

**v3 新增实测发现（复核未覆盖）**：
1. **26,894 行 asset_columns 的 source_code 为 NULL**——ODS 六源全部列数据悬空（实测可无歧义匹配回 6 个 ods_* 源，歧义表 0 张）。这是列级补全的前置修复（§3 T1.3）。
2. **列级中文名/值域全平台为 0**（连 his_source 13,913 列也是 0），表级中文名已 100% 覆盖但 business_desc_cn 全 NULL。→ 本计划的真正缺口是：**表业务描述、列中文名/值域/敏感标记、3 条关系缺口**。

---

## 1. 目标、边界与裁决

**目标**：将交接文档已确认的架构知识（核心表用途、关键列值域口径、统计口径、双源病历模式）沉淀到平台，补全 17 张核心表的业务描述与关键列元数据，补齐 3 条关系缺口，修复列 source_code 悬空。

**边界（YAGNI）**：
- 仅 17 张核心表 + 关键列（约 130 列）；不做 865/1236 张表全量。
- 不覆盖任何既有非空中文名/domain/grain/pk（实测已有，见 §1.3）。
- **不重登已存在的 ODS 层关系**（实测 37 条已覆盖 R01–R14 大部分，§5.2）。
- 不改 merged 状态系统（EMR/SM/LIS/PACS 已归并，不在本计划范围）。
- 不修改业务源库、不碰凭据实体、不执行 git 操作。

**用户裁决（沿用）**：D1 HIS/ODS 两系统都补全（实测两者登记已完整，见 §1.2，本计划转为核对确认）；D2 关系执行只读抽样验证，ODS 不通则跳过留 draft；D3 关键列先行；D4 一律 unreviewed/draft。

## 2. 环境与前置

- **执行位置**：平台活库在 8.83 容器内。执行 AI 须通过 `ssh -F "F:\python\数据资产\.ssh\config_ai" data-asset-83 "docker exec -i data-asset-api python -" < script.py` 方式运行脚本（管道传入，避免引号问题），或将脚本 docker cp 进容器执行。**禁止再连 10.20.1.153**（陈旧库，v2 基线失实根源）。
- 导入脚本仍写到本仓 `backend/scripts/`（仓库会部署到容器），用 `app.core.db.SessionLocal`。
- 结构验证 V0.1–V0.3（asset_source_schemas 表、data_sources 新列、systems.target_host）：**实测已通过**（复核 §1.6 确认），执行 AI 复核一遍即可，不需等升级。

### 1.2 实测基线（v3，2026-07-27 SSH 8.83 活库实测，以此为准）

**asset_systems（14 行）**：DATA_CENTER（system_type='ODS'，target_host=**NULL**，description 已有）/ DOCARE / EMR(merged) / HIS_SOURCE / HRP / JHEMR_VASTBASE / LIS(merged→LIS_SOURCE) / LIS_SOURCE / MOBILE_NURSING / PACS(merged) / PACS_SOURCE / PAPERLESS_CDMS / SM(merged) / ULTRASOUND_ENDOSCOPY。无空记录。

**asset_data_sources（15 行）**：ODS 侧 6 源共用同一物理库与凭据（P5 已解）：

| source_code | system_code | canonical_source_code | credential_ref |
|-------------|-------------|----------------------|----------------|
| ods_8_216 | DATA_CENTER | （主，NULL） | file:///etc/data-asset/credentials/ods_8_216 |
| ods_emr | DATA_CENTER | ods_8_216 | 同上 |
| ods_lis | DATA_CENTER | ods_8_216 | 同上 |
| ods_pacs | DATA_CENTER | ods_8_216 | 同上 |
| ods_sm | DATA_CENTER | ods_8_216 | 同上 |
| ods_ydhl | MOBILE_NURSING | ods_8_216 | 同上 |

`his_source_10_10_10_15` 登记完整（oracle/his/1521/readonly/凭据齐）——**D1 裁决事项实测已满足，无需补登记**。

**asset_tables**：his_source 1,236 张（中文名 100%）；ods_8_216 701 / ods_emr 79 / ods_lis 11 / ods_pacs 18 / ods_sm 25 / ods_ydhl 31（中文名均 100%，name_cn_source='已有资产'/'AI建议名称'）；**business_desc_cn 全 NULL**；review_status 全 NULL。

**asset_columns**：85,730 行；**26,894 行 source_code=NULL（全部 ODS 列，可唯一匹配回 6 源，歧义 0）**；column_name_cn 与 value_desc_cn **全平台 0 覆盖**。

**asset_source_schemas**：78 行已存在（ods_8_216=23、ods_emr=3、ods_lis/pacs/sm/ydhl 各 1），schema_name_cn 关键区已填（HIS=HIS业务镜像区、MTL=老电子病历区、JHEMR=新电子病历区、YDHL=移动护理镜像区、CDA=标准字典区、ODS=标准视图区）；**table_count 已过时**（如 HIS 记 128 实为 273、PORTAL_USER 记 21 实为 60）。

**asset_relations 535 条 / reviews 0 条**。ODS 层核心路径实测已存在且多已验证（§5.2）。

### 1.3 17 张核心表现状（实测逐表确认）

| 实际位置 | 表 | 既有中文名（不覆盖） | business_desc_cn | domain/grain/pk |
|---------|-----|--------------------|------------------|-----------------|
| ods_8_216/HIS | PAT_VISIT | 病人住院主记录 | NULL ✅待补 | 已有 |
| ods_8_216/HIS | PAT_MASTER_INDEX | 病人主索引 | NULL ✅待补 | 已有 |
| ods_8_216/HIS | DIAGNOSIS | 诊断记录 | NULL ✅待补 | 已有 |
| ods_8_216/HIS | OPERATION | 手术记录 | NULL ✅待补 | 已有 |
| ods_8_216/HIS | ORDERS | 医嘱 | NULL ✅待补 | 已有 |
| ods_8_216/HIS | INP_BILL_DETAIL | 住院病人费用明细记录 | NULL ✅待补 | 已有 |
| ods_8_216/HIS | DEPT_DICT | 科室字典 | NULL ✅待补 | 已有 |
| ods_8_216/HIS | EXAM_MASTER | 检查主记录 | NULL ✅待补 | 已有 |
| ods_8_216/HIS | EXAM_REPORT | 检查报告 | NULL ✅待补 | 已有 |
| ods_8_216/HIS | CLINIC_MASTER | 就诊记录 | NULL ✅待补 | 已有 |
| ods_8_216/HIS | OPERATION_MASTER | 手术信息 | NULL ✅待补 | **domain/grain/pk 均 NULL，可补** |
| ods_emr/MTL | EPR_INPATIENT_DOCINDEX | 存储住院病历索引信息，包括病历的状态 | NULL ✅待补 | 已有 |
| ods_emr/MTL | EPR_INPATIENT | 住院病历段落信息表（按年度分区） | NULL ✅待补 | 已有 |
| ods_emr/MTL | EMRSD_CONTENT | 内容表 | NULL ✅待补 | 已有 |
| ods_emr/JHEMR | JHMR_FILE_INDEX | 病历文书-业务类-新版本病历文件索引 | NULL ✅待补 | 已有 |
| ods_emr/JHEMR | JHMR_FILE_CONTENT_TEXT | 文件内容表 | NULL ✅待补 | 已有 |
| ods_ydhl/YDHL | MCS_DOC_FORM | 文书表单 | NULL ✅待补 | 已有 |

---

## 3. 阶段 1：数据完整性小修补（替代 v2 的登记阶段）

交付脚本：`backend/scripts/fix_datacenter_registration.py`（幂等；先 --dry-run）

### T1.1 UPDATE DATA_CENTER 系统（既有记录补字段，不新建）

`target_host='10.10.8.216'`（实测为 NULL）；`owner_department='信息中心'`（仅当 NULL）；description_cn 末尾追加 `；含 HIS 镜像/MTL/JHEMR 电子病历双源/SM 手麻/CDA 标准码映射，拆分源 ods_emr/ods_lis/ods_pacs/ods_sm/ods_ydhl 共用凭据 ods_8_216（交接文档 §3.1/§3.3）`。**不动其他 13 个系统。**

### T1.2 刷新 asset_source_schemas 统计（既有 78 行，不新建不删）

对 6 个 ods_* 源（31 行）：`table_count`/`column_count` 按 asset_tables/asset_columns 实算刷新（column 计数在 T1.3 完成后执行或按匹配后口径），`last_collect_at` 不动。schema_name_cn 已有值**不覆盖**；仅对仍为 NULL 且交接文档明确的补：无（实测关键区均已填）→ 本任务仅刷统计。

### T1.3 修复 26,894 行列 source_code 悬空（v3 新增，前置修复）

```sql
-- dry-run 先跑（期望 26894，且歧义校验=0）：
SELECT count(*) FROM asset.asset_columns WHERE source_code IS NULL;
SELECT count(*) FROM (
  SELECT schema_name, table_name FROM asset.asset_tables
  WHERE (schema_name, table_name) IN (SELECT DISTINCT schema_name, table_name FROM asset.asset_columns WHERE source_code IS NULL)
  GROUP BY 1,2 HAVING count(*)>1) x;
-- 正式执行：
UPDATE asset.asset_columns c SET source_code=t.source_code, system_code=t.system_code
FROM asset.asset_tables t
WHERE c.source_code IS NULL AND c.schema_name=t.schema_name AND c.table_name=t.table_name;
```

歧义校验非 0 → 停止报告。执行后期望剩余 NULL=0。同时回填 system_code（实测 NULL 列该字段情况由 dry-run 一并报告）。

### T1.4 幂等空操作检查

空 system_code 记录、his_ready 残留：`SELECT count(*) ... WHERE system_code='' ` / `WHERE source_code='his_ready_10_10_10_15'`，期望均 0，仅记录不操作。

### 阶段 1 验收

```sql
-- A1.1 期望 1（target_host 已补）
SELECT count(*) FROM asset.asset_systems WHERE system_code='DATA_CENTER' AND target_host='10.10.8.216';
-- A1.2 期望 0
SELECT count(*) FROM asset.asset_columns WHERE source_code IS NULL;
-- A1.3 期望 0（统计刷新后无偏差；以 HIS 为例）
SELECT count(*) FROM asset.asset_source_schemas s
WHERE s.source_code='ods_8_216' AND s.table_count <> (
  SELECT count(*) FROM asset.asset_tables t WHERE t.source_code=s.source_code AND t.schema_name=s.schema_name);
```

## 4. 阶段 2：核心表/列元数据补全（本计划主体）

交付物（CSV 均 `utf-8-sig`）：
- `开发起步包/数据资产_ODS核心表治理导入包/ods_core_tables.csv`（17 行，**含 source_code 列，3 源混合**）
- `开发起步包/数据资产_ODS核心表治理导入包/ods_core_columns.csv`（约 130 行，含 source_code 列）
- `开发起步包/数据资产_ODS核心表治理导入包/his_source_value_desc.csv`
- `backend/scripts/import_ods_core_governance.py`

### 4.1 ods_core_tables.csv（source_code 按 §1.3 实测归属照录）

表头：`source_code,schema_name,table_name,business_desc_cn,domain,grain,pk`

**UPDATE 规则（P3 修订）**：按 (source_code, schema_name, table_name) 定位；`business_desc_cn` 仅当库中**为 NULL 时写入**（实测 17 张全 NULL）；`table_name_cn` **一律不写**（保留既有）；domain/grain/pk 仅当库中为 NULL 时写入（实测仅 OPERATION_MASTER 需要）；`review_status` 仅当 NULL 时置 `'unreviewed'`（D4）；`name_cn_source`/`name_cn_status` **不动**（中文名非本批产物）。找不到行记警告，不新增。

business_desc_cn 内容（照录，与 v2 相同，此处不再重复 17 条全文——直接沿用 v2 §3.1 表中的 business_desc_cn 列措辞，唯一变化是归属源按 §1.3）：

- ods_8_216/HIS 11 张：沿用 v2 §3.1（PAT_VISIT「住院就诊记录核心表，三甲复审统计主锚点…」等）。
- ods_emr/MTL 3 张、ods_emr/JHEMR 2 张：沿用 v2 §3.1 对应措辞。
- ods_ydhl/YDHL.MCS_DOC_FORM：`临床营养筛查表单（源端 LUNA_MCS_SDSEY.MCS_DOC_FORM 的 ODS 汇聚，交接文档 §3.3）`。
- OPERATION_MASTER 补：`domain='手术'`、`grain='每就诊每手术一行（手麻系统侧主表）'`、`pk='PATIENT_ID|VISIT_ID+OPER_ID（待核）'`——**pk 不确定则留 NULL 并记警告，禁止编造**。

### 4.2 ods_core_columns.csv（关键列，约 130 行）

表头：`source_code,schema_name,table_name,column_name,column_name_cn,business_desc_cn,value_desc_cn,semantic_type,is_sensitive`

内容：**完全沿用 v2 §3.2 的逐表关键列清单**（PAT_VISIT 8 列、PAT_MASTER_INDEX 5 列含 NAME/INP_NO 敏感、DIAGNOSIS 7 列含 DIAGNOSIS_TYPE='3'/DIAGNOSIS_NO=1 口径、OPERATION 5、ORDERS 5、INP_BILL_DETAIL 5 含 ITEM_CLASS 'A'/'B'、DEPT_DICT 2、EXAM_MASTER 3、EXAM_REPORT 3、CLINIC_MASTER 3、OPERATION_MASTER 3、EPR_INPATIENT_DOCINDEX 4、EPR_INPATIENT 6、EMRSD_CONTENT 3（中文列名"段落编号/段落内容"）、JHMR_FILE_INDEX 6、JHMR_FILE_CONTENT_TEXT 2），唯一变化：MTL/JHEMR 行 source_code=ods_emr、MCS_DOC_FORM 行 source_code=ods_ydhl。

**UPDATE 规则**：按 (source_code, schema_name, table_name, column_name) 定位（**必须在 T1.3 完成后执行**，否则 ODS 列 source_code 为 NULL 匹配不到）；column_name_cn/business_desc_cn/value_desc_cn 仅当 NULL 时写入（实测全 NULL）；is_sensitive 按清单写（NAME、INP_NO）；review_status 置 'unreviewed'（NULL 时）；name_cn_source='handover_doc_20260727'、name_cn_status='pending_review'。匹配不到记警告（重点核对 MTL 中文列名大小写/全角）。

### 4.3 his_source_value_desc.csv（HIS 源端值域补充）

沿用 v2 §3.3 七行（MEDREC.PAT_VISIT.DISCHARGE_DISPOSITION2 '3'=死亡 等），source_code 改为 `his_source_10_10_10_15`（his_ready 已不存在）。仅写 NULL 的 value_desc_cn；his_source 列的 source_code 正常，不受 T1.3 影响。

### 4.4 导入脚本要求

`import_ods_core_governance.py`：`--dry-run` 先行；全程仅 UPDATE；输出 JSON 摘要 + `import_warnings.json`。依赖顺序：**T1.3 → 本脚本**。

### 阶段 2 验收

```sql
-- A2.1 期望 17（3 源合计）
SELECT count(*) FROM asset.asset_tables
WHERE business_desc_cn LIKE '%交接文档%'
  AND ((source_code='ods_8_216' AND schema_name='HIS') OR (source_code='ods_emr') OR (source_code='ods_ydhl'));
-- A2.2 期望 >=120
SELECT count(*) FROM asset.asset_columns WHERE name_cn_source='handover_doc_20260727';
-- A2.3 期望 0（D4）
SELECT count(*) FROM asset.asset_tables t JOIN asset.asset_columns c ON c.source_code=t.source_code AND c.schema_name=t.schema_name AND c.table_name=t.table_name
WHERE c.name_cn_source='handover_doc_20260727' AND (c.review_status='approved' OR t.review_status='approved');
-- A2.4 期望 >=2
SELECT count(*) FROM asset.asset_columns WHERE source_code='ods_8_216' AND schema_name='HIS' AND table_name='PAT_MASTER_INDEX' AND is_sensitive;
-- A2.5 期望 17（P3：中文名未被覆盖，name_cn_source 无 handover 表级标记）
SELECT count(*) FROM asset.asset_tables WHERE name_cn_source='已有资产'
  AND ((source_code='ods_8_216' AND schema_name='HIS' AND table_name IN ('PAT_VISIT','PAT_MASTER_INDEX','DIAGNOSIS','OPERATION','ORDERS','INP_BILL_DETAIL','DEPT_DICT','EXAM_MASTER','EXAM_REPORT','CLINIC_MASTER','OPERATION_MASTER'))
    OR (source_code='ods_emr' AND table_name IN ('EPR_INPATIENT_DOCINDEX','EPR_INPATIENT','JHMR_FILE_INDEX','JHMR_FILE_CONTENT_TEXT'))
    OR (source_code='ods_ydhl' AND table_name='MCS_DOC_FORM'));
```

## 5. 阶段 3：关系缺口登记与只读抽样验证

### 5.1 实测后缺口仅 3 条（v2 的 15 条大幅缩减）

写入 `asset_relation_reviews`（review_status='draft'、confidence='B'、validation_status='pending'、source_evidence='SQL与数据架构交接文档_20260727 §3.4'）：

| rel | from_source | from_table | from_columns | to_source | to_table | to_columns | join_condition | 说明 |
|-----|------------|-----------|--------------|-----------|---------|-----------|----------------|------|
| G1 | ods_8_216 | HIS.INP_BILL_DETAIL | PATIENT_ID+VISIT_ID | ods_8_216 | HIS.PAT_VISIT | PATIENT_ID+VISIT_ID | HIS.INP_BILL_DETAIL.PATIENT_ID = HIS.PAT_VISIT.PATIENT_ID AND HIS.INP_BILL_DETAIL.VISIT_ID = HIS.PAT_VISIT.VISIT_ID | 住院费用→就诊直接边（既有仅经 INP_SETTLE_MASTER 中介） |
| G2 | ods_8_216 | HIS.CLINIC_MASTER | PATIENT_ID | ods_8_216 | HIS.PAT_MASTER_INDEX | PATIENT_ID | HIS.CLINIC_MASTER.PATIENT_ID = HIS.PAT_MASTER_INDEX.PATIENT_ID | 门诊→主索引（交接文档 §3.4） |
| G3 | his_source_10_10_10_15 | MEDREC.PAT_VISIT | PATIENT_ID+VISIT_ID | ods_8_216 | HIS.PAT_VISIT | PATIENT_ID+VISIT_ID | 源端 MEDREC.PAT_VISIT → ODS HIS.PAT_VISIT 同名汇聚镜像 | 镜像关系非业务外键（doc 88 §2 口径）；from_system=HIS_SOURCE/to_system=DATA_CENTER；跨库不抽样验证，保持 draft |

### 5.2 已存在、禁止重复登记的关系（实测 37 条中与本计划相关者）

以下已存在且多为 verified，**执行 AI 不得重登**：
PAT_VISIT→PAT_MASTER_INDEX(verified) / PAT_VISIT→DIAGNOSIS(verified) / OPERATION→PAT_VISIT(verified) / PAT_VISIT→ORDERS(bounded) / PAT_VISIT→EXAM_MASTER(needs_split) / EXAM_MASTER→EXAM_REPORT via EXAM_NO(verified，**R07 键疑问已解：ODS 侧为 EXAM_NO**) / PAT_VISIT→DEPT_DICT 两条(verified) / CLINIC_MASTER→DEPT_DICT via VISIT_DEPT(verified) / PAT_MASTER_INDEX.INP_NO→MTL.EPR_INPATIENT_DOCINDEX(verified) / MTL.EPR_INPATIENT_DOCINDEX→HIS.PAT_VISIT via P_CLINIC_ID+P_INPATIENT_TIMES(verified) / DOCINDEX→EPR_INPATIENT(sample_verified) / EPR_INPATIENT→EMRSD_CONTENT UHID+段落编号(sample_verified) / JHMR_FILE_INDEX→PAT_VISIT(verified) / JHMR_FILE_INDEX→JHMR_FILE_CONTENT_TEXT(verified)。

遗留观察（记入交付报告，不处理）：ORDERS 边 validation_status='bounded'、EXAM_MASTER 边 'needs_split'、MTL 侧存在 P_INPATIENT_NO 与 P_CLINIC_ID 两条桥接路径（口径差异待业务裁决）。

### 5.3 抽样验证（D2；仅 G1、G2；G3 跨库不验证）

`verify_ods_core_relations_readonly.py`：沿用 v2 §4.2 规范（照抄 verify_his_relationships_readonly.py 模式、SQL 禁 `--` 注释、ROWNUM 限量、G1 降采样 1000 防超时、凭据用容器内 /etc/data-asset/credentials/ods_8_216 不打印、TNS 失败重试 3 次仍败记 skipped_ods_unreachable 不阻塞）。结果落盘 `开发起步包/95_ODS核心关系抽样验证结果.json`。

match_rate≥0.99 → 写入 asset_relations（rel_id 递增、confidence='B'、validation_level='sample_10k'、note=source_evidence），review 记 validation_status='sample_pass'+metrics；<0.99 → 'sample_failed' 不写入，列报告待裁决。

### 阶段 3 验收

```sql
-- A3.1 期望 3
SELECT count(*) FROM asset.asset_relation_reviews WHERE source_evidence LIKE 'SQL与数据架构交接文档_20260727%';
-- A3.2 期望 0（D4）
SELECT count(*) FROM asset.asset_relation_reviews WHERE source_evidence LIKE 'SQL与数据架构交接文档_20260727%' AND review_status<>'draft';
-- A3.3 期望 0~2（G1/G2 视验证结果；G3 不验证不转正）
SELECT count(*) FROM asset.asset_relations WHERE note LIKE 'SQL与数据架构交接文档_20260727%';
-- A3.4 期望 0（无重复登记：G1 验证通过后此查询应为 1 而非 2）
SELECT count(*) - 1 FROM asset.asset_relations
WHERE from_table='HIS.INP_BILL_DETAIL' AND to_table='HIS.PAT_VISIT';
```

## 6. 阶段 4：验证与交付

1. 容器内 `cd /app && python -m pytest tests/ -x -q`（重点 test_systems/test_tables/test_relations/test_graph）。
2. 前端核查：数据中心系统树；HIS.PAT_VISIT 表详情显示业务描述+关键列中文/值域+敏感标记；关系图谱费用直接边（若验证通过）。
3. 交付报告 `开发起步包/94_ODS核心表元数据补全交付报告.md`：覆盖清单、警告清单、验证结果、遗留观察（§5.2 末段）、未覆盖项。

## 7. 回滚方案（按批标记精确回退）

```sql
DELETE FROM asset.asset_relations WHERE note LIKE 'SQL与数据架构交接文档_20260727%';
DELETE FROM asset.asset_relation_reviews WHERE source_evidence LIKE 'SQL与数据架构交接文档_20260727%';
-- 表级：business_desc_cn 导入前实测全 NULL，回退置 NULL
UPDATE asset.asset_tables SET business_desc_cn=NULL
WHERE business_desc_cn LIKE '%交接文档%' AND source_code IN ('ods_8_216','ods_emr','ods_ydhl');
-- 列级：按批标记回退
UPDATE asset.asset_columns SET column_name_cn=NULL, business_desc_cn=NULL, value_desc_cn=NULL, is_sensitive=false, name_cn_source=NULL, name_cn_status=NULL
WHERE name_cn_source='handover_doc_20260727';
-- T1.3 的 source_code 回填为数据修复，如需回退以强制备份为准（见下）
```

**强制备份（阶段 1 动手前，容器内执行 pg_dump 导出 asset schema 相关 6 表）**，备份文件取回本机存 `开发起步包/数据资产_ODS核心表治理导入包/backup_asset_pre93_v3_<日期>.sql`。

## 8. Runbook（严格按序）

| 步 | 动作 | 通过条件 |
|----|------|---------|
| 0 | 重测 §1.2 关键数（sources=15、null 列=26894、核心表 17 张归属） | 与 §1.2 一致；不一致则停止报告（以活库为准重议） |
| 1 | 强制备份 | 备份文件非空 |
| 2 | fix_datacenter_registration.py（含 T1.1/T1.2/T1.4，先 dry-run） | A1.1、A1.4 |
| 3 | T1.3 列 source_code 回填（先 dry-run 两项计数） | A1.2=0、A1.3=0 |
| 4 | 3 个 CSV 照录 + import_ods_core_governance.py（先 dry-run 核对警告） | A2.1–A2.5 |
| 5 | import_ods_core_relations.py 写 3 条 draft（先查 §5.2 防重） | A3.1、A3.2 |
| 6 | 抽样验证 G1/G2 + 按 §5.3 处置 | 结果 JSON 落盘、A3.3、A3.4 |
| 7 | pytest 全量 + 前端核查 | §6-1/2 |
| 8 | 交付报告 94 | 含警告与遗留清单 |

**异常处理总则**：任何验收不过 → 停止、保留现场、如实记录；不得跳过验收伪造通过；不得擅自扩大改动范围；实测与本文不符时以活库为准并回报用户裁决。
