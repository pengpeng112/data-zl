> 类别：交接  
> 日期：2026-07-14
> 用途：交给**其他 AI / 接手人**继续工作的唯一速查清单  
> 权威执行入口仍是：`AGENTS.md` → `开发起步包/README.md` → `55` / `58` / `59`（本文件是**快照交接**，不取代上述入口）

## 2026-07-15 Plan90 最终交接

- 90号已完成：生产十个一级系统，正常目录4,331表，3,435个确认空对象排除，508个未知/视图保留待采集变化时复查。
- 最终镜像 `data-asset:20260715-row-presence-final`，Alembic `b4c5d6e7f8a9`，完整备份与321项全量测试证据见92§17；业务源DML/DDL为0。

# 66 多 AI 交接：未完成与待实现清单

## 置顶：2026-07-14 用户最新裁决（与下文冲突时以本节为准）

| 事项 | 最新决定 | 后续 AI 动作 |
|---|---|---|
| T13 凭据轮换/签字 | 不需要 | 取消，不阻塞发布；不得伪造记录 |
| TLS | 继续 HTTP | 取消，不阻塞；仅限当前内网，记录风险接受并适配 Cookie |
| Git push | 已明确授权 | 按 55 E1/E2 收敛、扫描、fetch 后普通 push；禁止 force-push，旧 tag 不移动 |
| Linux lock/wheelhouse/校验 | 执行 | 按 55 E4 |
| 干净断网部署演练 | 执行 | 按 55 E5 |
| 全量验收 | 执行 | 按 55 E6 |
| HRP 直连与 WA_* 补采 | 恢复执行 | 撤销旧“HRP 先不同步”；按 55 E7，只读补采。HRP 人员 apply 仍需 dry-run 和审批 |
| 已完成 HIS/ODS/周边/人员/夜间质量 | 做回归，不重复建设 | 按 55 E8，禁止重复全量人员 apply |
| 前端人工视觉验收 | 已完成 | 50 号转完成；只修明确缺陷 |
| 人员画像/审批/按钮/数据范围 | 继续开发 | 按 55 E9 与 43/59 |
| 配方在线编辑/自动建视图 | 继续开发 | 按 55 E10 与 54；只写平台库，默认生成 SQL，业务源库零 DDL |
| 运维写通道 | 保持关闭 | 只做默认关闭和拒绝测试，不启用生产开关 |

### 当前唯一执行顺序

`E1 工作区收敛 → E2 push → E3 生产 RBAC/挂载/关闭态 → E4 离线包 → E5 断网演练 → E6 全量验收 → E7 HRP 补采 → E8 数据链路回归 → E9 人员权限增强 → E10 配方库扩展 → E11 系统/数据资源/关系图谱一体化整改`。

E9–E11 产品代码、迁移和服务器发布已完成，原43/51/54/74/75/76号实施计划已归档为历史快照。当前仅从 `85_系统最终封板与全量验收详细执行计划.md` 接续角色矩阵、真实数据交互、归并apply、离线演练和Git收口；AI视图SQL按87号，三甲复审跨系统关系按88号执行。

### 2026-07-15 追加：90 号已实施

- **已完成**：十系统平行展示、取消「其他业务系统」、`b4c5d6e7f8a9` 迁移、8.83 系统归一 apply、前端 dist 发布、API 目录验收。交付 `92`。
- **勿恢复**：74/75/76 旧大类口径；91 仅历史复核。
- **仍待**：全库 live 空表限 1 探测 apply、生产镜像固化 pg_dump、浏览器人工角色矩阵、本机隧道全量 pytest、Git 收口。

每一项必须把实际命令、结果摘要、文件/SHA、失败和遗留更新回 `55` 执行日志及 README。不得依据本文件下方 2026-07-13 的旧“待办/暂停”文字覆盖本节。

## 0. 接手后 5 分钟必做

1. 读根目录 `AGENTS.md`（安全红线、源库只读、文档维护规则）。  
2. 读 `开发起步包/README.md`「当前完成度总览」。  
3. 读本文件 §1–§4。  
4. `git status` / `git log -5 --oneline` / `git tag -l "release-*"` 核对基线。  
5. **不要**把聊天记录或归档文档当成当前状态；以本文件 + 55/58/59 为准。

### 0.1 当前 Git / 发布基线（交接时点）

| 项 | 值 |
|---|---|
| 分支 | `master` |
| 工作区 | 业务线/体验线/导航导入后可能 **dirty 或新 commit**；以 `git status` 为准 |
| 基线 tag | **`release-20260713`**（后续功能可能在 tag 之后） |
| Tag 指向 | 见 `65` / `git rev-parse release-20260713` |
| 相对 origin | 本地可能 **ahead**；2026-07-14 用户已明确授权普通 push，执行前仍须 fetch 和秘密扫描 |
| 生产机 | `10.10.8.83`（Docker `data-asset-api` + Nginx:80）；已热更 L13–L16/导航数据 |

### 0.2 硬红线（违反即停）

| # | 红线 |
|---|---|
| 1 | HIS / ODS / HRP 等**业务源库只允许 SELECT**，禁止 DML/DDL |
| 2 | 密码、Token、身份证、姓名、电话、地址 **不进代码 / 日志 / git / 新文档** |
| 3 | 平台库写入须审批+审计；AI **不接** `asset_action_executors` 写执行器 |
| 4 | 迁移手写 `upgrade`/`downgrade`；表只在 `asset` schema |
| 5 | 前端 **pnpm**；typecheck = `tsc` + `vue-tsc` |
| 6 | 用户已授权普通 push；仍然**禁止 force-push**、擅自改生产密码和未经审批 `dry_run=false` 写平台库（人员入库已做过一次，勿重复全量覆盖） |
| 7 | HRP 直连/WA_* 只读补采已获授权；HRP 人员写平台库仍须 dry-run、差异报告和审批 |

---

## 1. 已完成（接手时请勿重复做）

> 下列成果可直接复用；重复全库探查 / 重复实现登录会浪费时间。

### 1.1 探库与资产（文档 + 机器可读包）

- 数据中心元数据 `08`、表关系 `09`–`15`、HIS 源端 `16`/`21`–`25`、药房 `19`、治理口径 `40`、HRP 复核 `52`（CSV 包已有）  
- 归一包 `数据资产_ODS_HIS归一优化包/`、关系配方种子 `数据资产_关系图谱/view_relation_recipes.json`  
- 证据报告：`62` 连通、`63` 元数据+dry-run、`64` 人员入库  

### 1.2 应用与登录（已上线 8.83）

- 本地账号登录：Argon2id + JWT Access + Refresh Cookie；迁移 `t6a7b8c9d0e1`  
- 前端登录页、本地账号管理；生产 Mock 关闭  
- 管理员：`platform-admin`（密码不在文档；用户可自助改密）  
- RBAC 种子、双凭证（JWT + ApiKey）中间件  

### 1.3 源库只读与人员

| 项 | 状态 |
|---|---|
| ODS `10.10.8.216:1521/orcl` | 连通；`ods_8_216` 已登记 |
| HIS `10.10.10.15:1521/his` | 连通；`his_source_10_10_10_15` 已登记 |
| 活库元数据快照 | ODS 核心 owner ~540 表；HIS 12 owner ~1237 表 |
| 人员主数据口径 | **`FXHIS.SYS_EMPLOYEE` 为主**（非 COMM）；桥接 `EMPLCODE≈STAFF.EMP_NO` ~98.5% |
| 平台 identity 写入 | **已 apply**：约 4260 人 / 816 科 / 6690 sources / 7831 person_depts（`64`） |
| Instant Client | 容器内需 `libclntsh.so → 19.1`（11.2 会 DPY-3010/DPI-1050） |

### 1.4 代码层整改（已进 tag）

- T3 QualityFinding 单状态、T4 只读 SQL 守卫、T5 快照 source_code、T7 归一包、T8 配方库只读  
- 发布 R1：health live/ready、调度时区、env 路径、offline README 步骤等  

---

## 2. 未完成清单（按优先级给其他 AI）

### P0 — 发布封板（`58` + `65`）

| ID | 事项 | 现状 | 接手动作 | 阻塞/依赖 |
|---|---|---|---|---|
| **R-PUSH** | 推送 Git 基线到 origin | 已获用户授权，待执行 | 按 E1/E2：收敛、扫描、fetch、普通 push；旧 tag 不移动，新增功能用新 tag | 远端分叉时停下报告 |
| **T13** | 凭据轮换（人工） | **用户取消** | 不执行、不阻塞、不伪造完成日期 | 无 |
| **R-TLS** | HTTPS / TLS 入口 | **用户接受内网 HTTP** | 不实施；记录风险接受并保持 Cookie/代理与 HTTP 兼容 | 无，本决定不适用于公网 |
| **R-MOUNT** | 容器凭据与 Oracle 客户端持久化 | **脚本已就绪**：`/etc/data-asset/scripts/ensure_oracle_ro_runtime.sh` + `run_data_asset_api.sh`；当前容器已链 `libclntsh.so→19.1` | 运维下次**完整重建**时务必用挂载命令；勿丢 credentials 卷 | 运维确认一次重建演练 |
| **R-RBAC-ENV** | 生产 `APP_RBAC_REQUIRE_BOUND_TOKEN=true` | example 已改 true；**服务器 env 可能仍 false** | 改 `/etc/data-asset/backend.env` 并重启容器；验证未绑定 Token 写接口 403 | 运维 |
| **T6 / R2–R3** | Linux 离线 wheelhouse + **断网干净环境演练** | 文档有；**未完整演练** | 在 Linux 构建机按 `deploy/offline/README.md`；wheelhouse **不进 Git** | 需 Linux 内网机 |
| **T14** | 全量验收 | 部分测过 | `pytest`（需 `APP_TEST_DB_URL` 含 test）、`alembic upgrade head`、`pnpm typecheck/build`、敏感串 grep | 测试库/隧道 |

**正式封板判定（2026-07-14 修订）**：代码安全 push + 生产 RBAC 加固 + 持久化挂载重建通过 + Linux 离线包可校验 + 断网演练通过 + 全量验收通过 + HTTP 风险接受有记录。T13/TLS 不再是阻塞。

---

### P0/P1 — 业务接入（`59`，HRP 除外）

| ID | 事项 | 现状 | 接手动作 |
|---|---|---|---|
| **L12-HRP** | HRP 直连/WA_* 补采 | 2026-07-14 用户已允许；ODS.HRP 镜像 21 表/928 字段已有（`64`） | 获取安全只读凭据，按 E7 增量补采并做镜像差异；人员 apply 仍需另行审批 |
| **L13** | 主数据差异复核 | **已完成**（`67`，2054 open 差异） | 人工在前端 resolve/ignore；L16 已可从差异提出写主档 |
| **L14** | 周边系统只读接入 | **已完成**（`67`，ODS 五源元数据） | 可选独立库直连；关系继续用 09–15 |
| **L15 / T12** | 质量规则夜间只读 | **已完成**（`67`，run+cron；前端可按 run_id 筛 findings） | 可收紧规则阈值；确认 02:00 调度仍注册 |
| **L16** | 同步闭环 | **人员+科室+批量已完成** | 批量提出/解决忽略/审批/执行（50 上限）；仍只写平台 PG |
| **L9+** | RBAC 资源矩阵收紧 | 基础角色有 | 生产绑定 Token、最小权限回归测试 |
| **L10-diff** | 活库快照 vs `08` 表名 diff | **已完成**（`69`） | 核心 owner 对齐；若要门户/BL 等进 live 再扩采集 owner |

---

### P1 — 产品体验与扩展

| ID | 事项 | 现状 | 接手动作 |
|---|---|---|---|
| **UI-50** | 前端 UI 人工验收 | **API 向验收已修**（`70`）；像素级视觉仍建议人眼 | 登录后按 70 §4 点一遍；主题/玻璃拟态等视觉微调 |
| **NAV-51** | 数据中心五层导航展示 | 2099 表已导入；**树默认懒加载** + 服务端搜表名 | 展开 Owner 加载表；搜 `PAT_VISIT` 走 search 结果区 |
| **RCP-54** | 关系配方库扩展 | 最小只读已有 | 扩展检索/展示；**不做**在线编辑/自动建视图（除非用户改范围） |
| **ID-43** | 人员权限增强 | 基础 identity + RBAC | 画像、审批流、按钮级权限等（`43`） |
| **OPS-45** | 运维写通道关闭态验证 | 代码就绪，生产必须关闭 | 验证三个开关为 false、请求被拒绝和审计正常；本轮禁止打开 |

---

### 延后（不抢主线）

| ID | 事项 | 文档 |
|---|---|---|
| **OPEN-44** | SQLGlot / SchemaCrawler / Soda / tbls 等开源集成 | `44` |
| **SSO** | 统一认证 / SSO 二期 | `59` M 后期 |
| **HRP 全量** | 5851 表治理、WA_* 8 张 need_fields 直连补采 | `52`/`64`；用户已搁置 |

---

## 3. 用户已确认的口径（接手必须遵守）

| 口径 | 内容 |
|---|---|
| 人员主数据 | **以 `SYS_EMPLOYEE` 为主** → 源库真表 **`FXHIS.SYS_EMPLOYEE`** |
| DOCTOR_USER 未匹配 | **忽略** |
| HRP | 允许直连与 WA_* 只读补采；人员同步写平台库仍须 dry-run 和审批 |
| 人员入库 | 用户已授权并完成一次平台 identity apply（见 `64`） |
| 源库写 | 永不写业务源库 |
| 发布线 | 本地 Git 基线 + tag 已做；**正式封板仍差人工项** |

---

## 4. 环境与坑（高代价）

| 主题 | 事实 |
|---|---|
| 本机 Windows | **不能直连** Oracle 业务库；经 8.83 或跳板 |
| 跳板 | `10.10.8.53:40022`；8.83→跳板可能 **无密钥 Permission denied** |
| ODS 账号 | DB 侧有写特权；平台必须 `validate_readonly_sql` |
| HIS 人员 | `COMM.SYS_EMPLOYEE` **不存在**；用 `FXHIS.SYS_EMPLOYEE` |
| Oracle thick | Client **≥19.1**；容器 `/opt/oracle`，默认链曾错指 11.2 |
| 大表 | `HIS.LAB_RESULT` 等禁全扫 |
| 测试 | `APP_TEST_DB_URL` 必须含 `test`；可用 8.83 库 `data_asset_test` + SSH 隧道 |
| 前端 | **仅 pnpm** |
| 同步到 8.83 | `deploy/scripts/sync_local_to_server.py`（需 `APP_SSH_PASSWORD` 等 env，**勿把密码写进仓库**） |
| 临时脚本 | `backend/scripts/_ro_*.py` 等 **gitignore**，勿提交 |

### 4.1 平台库关键对象（PostgreSQL `data_asset` / schema `asset`）

- 数据源：`asset_data_sources`（`ods_8_216`、`his_source_10_10_10_15`；`hrp_10_10_10_23` 登记但 enabled=false）  
- 人员：`asset_identity_*`（已有生产数据）  
- 元数据快照：`asset_metadata_snapshots` / `asset_metadata_column_snapshots`  
- 登录：`asset_auth_*`  
- Alembic：生产曾到 auth 迁移；功能迁移含 `m9`…`s5` 等，部署前核对 `alembic current`  

### 4.2 凭据位置（仅服务器，不进 Git）

```
/etc/data-asset/backend.env          # APP_DB_URL 等
/etc/data-asset/credentials/ods_8_216
/etc/data-asset/credentials/his_source_10_10_10_15
# HRP 未配置：hrp_10_10_10_23
```

---

## 5. 建议其他 AI 的执行顺序

### 路线 A — 收口发布（若用户优先「能正式上线」）

1. 按 E1 收敛工作区并完成秘密扫描。
2. 按 E2 安全 push 分支和新 tag（已授权）。
3. 服务器完成凭据挂载、Instant Client 和 `RBAC_REQUIRE_BOUND_TOKEN`。
4. 生成 Linux 离线包并做断网演练。
5. 完成全量验收；记录用户接受内网 HTTP，T13/TLS 不执行。

### 路线 B — 业务能力（若用户优先功能）

1. **E7** HRP 直连与 WA_* 只读补采，先资产差异，不直接人员 apply。
2. **E8** 回归 ODS/HIS/周边、人员、L13–L16 和夜间质量，避免重复全量执行。
3. **E9** 人员画像、权限审批、按钮级和数据范围权限。
4. **E10** 配方在线管理、版本审核、图谱联动和受控视图 SQL 生成。

### 路线 C — 质量与测试

1. 按 E4 生成 Linux 离线依赖与校验清单。
2. 按 E5 做断网干净环境演练。
3. 按 E6 补全 pytest/Alembic/typecheck/build/安全扫描和生产冒烟。

**不要做**：打开运维生产写通道、向业务源库任何写入/DDL、重复全量人员 apply、force-push。HRP 只读补采和普通 push 已获授权。

---

## 6. 关键文件索引（给其他 AI）

| 目的 | 文件 |
|---|---|
| 未完成总入口 | `55_系统未完成事项统一执行计划.md` |
| 发布 | `58`、`65`、`deploy/offline/README.md`、`deploy/README.md` |
| 登录与接入 | `59`、`60`、`61` |
| 源库证据 | `62`、`63`、`64` |
| 治理口径 | `40` |
| 连接 | `02`、`48` |
| 代码约束 | 根目录 `AGENTS.md` |
| 目录权威 | `开发起步包/README.md` |

---

## 7. 交接检查表（其他 AI 开工前自检）

- [ ] 已读 AGENTS + README + 本文件  
- [ ] `git status` clean 或已知差异  
- [ ] 知 tag `release-20260713`  
- [ ] 知 **HRP 不同步**、**源库只读**  
- [ ] 知人员已入库，勿无故全量重刷  
- [ ] 知普通 push 已授权，但禁止 force-push/移动旧 tag
- [ ] 知 T13/TLS 已取消，断网演练和全量验收仍必须执行
- [ ] 新增文档已编号并写入 README（本仓库文档规则）  

---

## 8. 一句话现状

> **截至 2026-07-14：系统可演示、可登录，ODS/HIS/周边、HIS 人员、夜间质量和前端视觉验收已完成；普通 push 已授权，T13/TLS 不再阻塞。当前按 E1–E11 完成生产 RBAC/挂载、离线包与断网演练、全量验收、HRP 只读补采、人员权限增强、配方库扩展，以及系统/资源/图谱一体化整改。**

---

*本交接由会话 AI 于 2026-07-13 根据仓库文档与已执行证据整理；若与活库/服务器即时状态冲突，以只读核查与 `git`/`docker` 实测为准。*

## 9. 2026-07-14 E9-E11 复核补充

- 本地代码已完成第二轮复核修订；后端无数据库测试 19 passed，前端 typecheck、Vitest 39 项及 build 通过，Alembic 离线 SQL 可生成至 `w9d0e1f2a3b4`。
- 平台数据库没有完成恢复或实际迁移：服务器 SSH 在认证前断开，且仓库内没有可恢复数据的 pg_dump/custom dump，仅有初始化脚本。
- 下一接手者不得把 `deploy/offline/init_db.sql` 当作现有测试库数据备份；应在服务器可达后取得真实备份或从可访问本地 PostgreSQL 导出，再执行恢复、迁移往返和数据库集成验收。
- 业务源库写操作为 0。

### 9.1 服务器执行结果

- 8.83 网络恢复后已完成平台库 custom dump、E9-E11 代码与前端发布，Alembic 当前为 `w9d0e1f2a3b4`，API 容器 healthy。
- 归并 dry-run：8 个数据源、3 个目标系统、0 冲突、未 apply。
- 生产镜像未安装 pytest，因此服务器全量数据库测试仍是明确遗留项；不要声称已通过。后续使用离线验收镜像执行，并补人工浏览器角色矩阵。
- 业务源库未写入。

### 9.2 本地与服务器同步基线

- 文件级对账证据统一见 `61_登录上线与本地服务器同步记录.md` 第 6 节。
- 2026-07-14 对账结果：后端受管文件 161/161、前端 dist 4703/4703，哈希差异和单边文件均为 0。
- 服务器正式镜像已固化为干净同步版本，容器 healthy，平台库 head 为 `w9d0e1f2a3b4`。
- 重要：服务器当前对应本地未提交工作区，不对应已有 Git tag；完成 commit/push/新 tag 前禁止用旧版本覆盖服务器。
