> 类别：待办

> 状态：待执行 | 优先级：P0 | 制定日期：2026-08-03
> 用途：将本文“执行提示词”整段原样交给执行 AI；不得删减安全限制

# 执行 111 号代码安全整改严格限制提示词

## 执行提示词

你现在只执行 `开发起步包/111_当前代码全面核查问题整改执行计划.md`，不得执行 112 号业务同步整改，不得自行发布生产。

### 一、开始前必须读取

按顺序完整读取：

1. 根目录 `AGENTS.md`。
2. `开发起步包/README.md`。
3. `开发起步包/55_系统未完成事项统一执行计划.md`。
4. `开发起步包/111_当前代码全面核查问题整改执行计划.md`。
5. 本提示词。

读取后先输出你理解的“允许事项、禁止事项、执行顺序”，确认与本文一致后直接执行本地整改，不要等待用户重复确认。

### 二、绝对禁止

任何一条违反都必须立即停止：

1. 禁止对生产平台库执行 INSERT、UPDATE、DELETE、MERGE、DDL、Alembic upgrade/downgrade。
2. 禁止对 HIS、JHEMR、CDMS、ODS、HRP 或其他业务库执行任何 DML/DDL、锁表、存储过程。
3. 禁止生产发布、容器重启、服务重启、Nginx 切换、夜间任务启用、写开关开启。
4. 禁止读取、打印、复制或提交密码、Token、私钥、连接串、患者/人员信息；只允许输出 secret 文件名或“存在/不存在”。
5. 禁止使用当前 `.env` 运行 pytest；禁止把非测试数据库当测试库。
6. 禁止用 `--noconftest` 绕过门禁运行普通测试。只有在 S1 门禁实现后，才可在显式隔离测试 URL 下把它作为“必须失败”的负向子进程测试。
7. 禁止新增 profile_summary 修复迁移。生产已只读确认 head=`f8a9b0c1d2e3` 且该列存在。
8. 禁止重写第三套角色有效期逻辑；必须复用 `backend/app/core/security.py::_effective_role_codes`。
9. 禁止删除、回退、覆盖不属于你的工作区修改；禁止 `git reset --hard`、`git checkout --`、force-push。
10. 禁止自行 commit、push、tag；用户另行明确授权前只保留可审查工作区改动。

### 三、已确认事实，不得重新猜测

- R01 属实：测试库保护仅放在 conftest 不够，直接 import `SessionLocal` 可绕过。
- R02 当前生产缺陷已核销：8.83 生产 revision 为 `f8a9b0c1d2e3`，`profile_summary` 存在。任务是治理误连环境，不是修生产 schema。
- R03 属实：`main._lookup_roles` 和 `auth_service.lookup_roles` 未过滤有效期；已有 `_effective_role_codes` 可复用。
- R04 为配置失败关闭问题：CORS origin 默认是 localhost、不是 `*`；methods/headers 当前为 `*`。
- 当前生产角色数据只有 1 条 active、无过期/未来角色；R03 是潜在越权，不得描述成已发生事故。
- 当前生产 JWT/RBAC 核心配置正常、写开关关闭，但代码仍需阻止未来弱配置启动。

### 四、固定执行顺序

必须按 `S0 → S1 → S2 → S3 → S4 → S5 → S6 → S7 → S8 → S9`，一次只做一个阶段。S1 未通过前不得相信任何数据库测试结果。

#### S0 冻结现场

- 记录 `git status --short`、分支、HEAD、tag、Alembic heads。
- 将改动分成已有用户改动和本次改动，不清理未知文件。
- 只列环境变量名，不输出值。
- 列出预计修改文件，严格限定在 111 号范围。

#### S1 测试数据库不可绕过门禁

- 门禁放在数据库 engine 建立前，不能只依赖 conftest。
- 检测到 pytest 进程或 `APP_ENV=test` 时，必须要求：`APP_ENV=test`、显式 `APP_TEST_DB_URL`、`APP_DB_URL` 与其完全一致、数据库名符合测试规则、目标不在生产/非测试拒绝清单。
- 普通生产/运维脚本且不处于 pytest/test 环境时不得被测试门禁误伤。
- 数据库测试改用统一 fixture；不得在测试模块自行创建全局生产 `SessionLocal`。
- 增加子进程负向测试：缺变量、dev/production 环境、生产 URL、伪测试库名、单文件、`--noconftest` 均须在建连前失败。
- 如果没有明确隔离 `APP_TEST_DB_URL`，停止数据库测试，只运行纯逻辑测试并报告阻断；绝不回退到 `.env`。

#### S2 环境和 schema 对账

- 不再修改生产 schema，不新增 R02 迁移。
- 找出代码为何可能解析到陈旧非测试环境；通过环境身份校验、数据库名拒绝清单和测试启动器修复。
- 只在显式隔离测试库验证空库 upgrade、备份恢复隔离库 upgrade、downgrade 一版再 upgrade。
- 生产数据库如需复核只能 SELECT/alembic current，不得 upgrade。

#### S3 有效角色统一

- 先为 `_effective_role_codes` 增加 active、disabled、future、expired、NULL 边界和 UTC 测试。
- 让 `main._lookup_roles`、`auth_service.lookup_roles`、JWT/API Key 授权调用统一实现。
- JWT roles 只作展示；授权查实时角色。
- 只允许单请求缓存。除非实现撤权主动失效，否则禁止跨请求 TTL 缓存。
- 验收必须证明旧 JWT 在撤权后立即 403。

#### S4 production 失败关闭

- 把现有 `auth_service` 请求期 lazy 校验抽成统一验证器，并在应用启动时调用。
- production 必须拒绝默认/短 JWT、RBAC 未绑定、origin 为通配或 localhost、methods/headers 过宽、写开关开启、凭据目录不合规、build ID/git SHA 缺失。
- 修改生产部署示例，不修改真实 secret。
- 不把 HTTP 部署误改成强制 TLS；用户已接受 HTTP，但 Cookie/CSRF 属性仍需验证。

#### S5 写接口权限

- 对 111 列出的写端点逐个加动作级权限，不得用“已登录”代替权限。
- 增加自动路由审计：POST/PUT/PATCH/DELETE 必须显式授权或位于极小公开白名单。
- 连接测试加 SSRF、网段、协议、端口、超时、并发和脱敏限制。

#### S6 异常与脱敏

- 禁止 API 返回 `str(exc)`。
- 客户端只收到稳定错误码、通用信息、correlation ID。
- 日志和数据库审计统一脱敏，测试必须注入包含密码、Token、连接串和个人信息的恶意异常文本。

#### S7 部署可复现

- 先确认 `sync_local_to_server.py` 是否仍被调用；废弃则提出归档，不直接删除；仍使用则先准备 known_hosts，再移除 `AutoAddPolicy`。
- 生成锁文件和离线资产时不得修改生产服务器。
- `run_data_asset_api.sh` 的关键检查不得 `|| true`。
- 断网演练只能在隔离环境；缺少隔离环境时提供命令和未执行说明，不得用生产代替。

#### S8 前端

- 保持现有 Vue 路径和 API 契约。
- 按路由拆 G6/ECharts，设置体积预算；不用 npm/yarn，只用 pnpm。
- 更新依赖必须保留 `pnpm-lock.yaml`，不得引入无关大版本升级。

#### S9 文档与 Git

- 更新 README 和 111 的真实状态，不新建平行总表。
- 不删除临时/未知文件；需废弃的文档只迁 `_archive/`，并先确认引用、登记索引。
- 执行秘密扫描、`git diff --check`；区分功能变化和行尾变化。
- 不为追求 clean 而清除用户已有改动。

### 五、验收命令

后端数据库测试只能在显式隔离变量齐全时运行：

```powershell
cd F:\python\数据资产\backend
.\.venv\Scripts\python.exe -m pytest tests/test_test_database_guard.py -q
.\.venv\Scripts\python.exe -m pytest tests/ -q
.\.venv\Scripts\python.exe -m pytest tests/ -q
.\.venv\Scripts\python.exe -m alembic heads
```

前端：

```powershell
cd F:\python\数据资产\frontend
pnpm install --frozen-lockfile
pnpm run typecheck
pnpm test
pnpm run build
```

不得把超时、跳过、环境阻断记为通过。全量 pytest 必须连续两次 0 failed/0 errors 才能标记完成。

### 六、必须停止并报告的情况

- 任何测试将连接非测试库或数据库身份无法证明。
- 需要生产迁移、发布、重启、secret、外网下载或业务库写权限才能继续。
- 发现当前工作区改动与任务冲突且无法保留。
- 发现秘密已进入 Git diff 或日志。
- 需要改变用户已确认的 HTTP、HRP 暂缓或业务同步口径。

停止时只报告阻断，不擅自绕过。

### 七、最终回报固定格式

1. 完成/未完成阶段。
2. 每个问题的修改文件和行号。
3. 实际执行命令及原始结果摘要。
4. 使用的数据库类型及是否隔离测试库。
5. 平台库写入计数、业务库 DML/DDL 计数。
6. 未执行项和原因。
7. 新发现风险。
8. `git status --short` 摘要。

禁止只回复“已完成”或“测试通过”。
