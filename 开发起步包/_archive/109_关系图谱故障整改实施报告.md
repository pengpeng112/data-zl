> 类别：实施报告
>
> 状态：代码与测试完成，待生产原子发布 | 优先级：P0/P1/P2 | 日期：2026-08-02
>
> 承接：`108_关系图谱无法打开专项排查整改与测试计划.md`（唯一专项入口）。
>
> 安全边界：本轮仅整改平台 PostgreSQL（测试库）、FastAPI、Vue 前端与部署流程；未连接/写入业务源库，未部署 Neo4j，未执行生产平台迁移。

# 109 关系图谱故障整改实施报告（108 号执行记录）

## 1. 本轮结论

```text
生产只读证据：已复核（DB 537 关系 / A=192 / A_rechecked=0 / 前端旧分包含幽灵筛选 / 后端 graph.py 与 schema 混合版本）
后端整改：     完成（契约统一、物理键、diagnostics 重构、meta、邻居歧义、边详情）
后端测试：     图相关 85 passed（本地 + 服务器容器双确认）；全量 560 passed / 2 skipped / 4 failed（gmssl 缺失，107 专项）
Alembic：      往返验证通过，head 唯一 f1a2b3c4d5e6，本轮无新迁移
前端整改：     完成（幽灵筛选删除、状态机、错误处理、G6 回退、物理节点、meta 展示、边证据按需）
前端测试：     typecheck 通过；Vitest 54 passed；build 成功且 dist 无 A_rechecked 残留
部署方案：     manifest 生成、前端版本目录原子切换、Nginx 缓存策略、回滚脚本（deploy/scripts/）
浏览器 E2E：   真实 Chrome 加载 SPA 成功、console error=0、unhandled rejection=0、截图生成；
               登录表单自动化受 headless CDP 与 el-form 响应式限制（记录为未完成项）
```

## 2. 生产只读证据复核（108 §1）

| 核查项 | 结果 | 说明 |
|---|---|---|
| 生产 asset_relations | 537 | 与 108 §1.2 一致 |
| confidence=A | 192 | 一致 |
| validation_status=A_rechecked | 0 | 一致 |
| confidence=A AND A_rechecked | 0 | 一致（默认空图根因） |
| 前端旧分包含 A_rechecked | 是 | 生产 `index-DFxNRzYF.js` 等 5+ 个旧 index-*.js |
| 后端 graph.py vs schema | 混合版本 | 生产 graph.py=新、schemas/graph.py=旧，physical_key 被 Pydantic 丢弃 |
| 生产 alembic current | e7f8a9b0c1d2 | 本地 head f1a2b3c4d5e6，生产非同一版本 |
| 生产静态目录 | 4272 JS 文件 | 大量历史 hash 分包混放，非原子发布 |

## 3. 后端修改

| 文件 | 修改 |
|---|---|
| `backend/app/schemas/graph.py` | 重写：GraphNode 增加 `physical_key/display_id`；GraphEdge 增加 `display_source/display_target/db_id/relation_layer`；新增 `GraphMeta/GraphData.meta`；全部 `extra="forbid"` 防止关键字段静默丢弃 |
| `backend/app/api/v1/graph.py` | 重写：节点/边使用完整物理键；边 ID 用稳定业务键；`_table_map_for` 按物理键；邻居支持 physical_key/system/source/schema/table 且多命中 409；新增 `/edges/{id}` 边详情；diagnostics 重构（层级/状态/置信度/物理键缺失/未解析端点/业务键重复/孤儿）；graph 响应带 meta |
| `backend/app/api/v1/tables.py` | `get_table` 支持 system_code/source_code 查询并 409 多来源歧义，返回 physical_key |
| `backend/app/api/v1/health.py` | 增加 build_id/git_sha/frontend_build_id 版本信息 |
| `backend/app/core/config.py` | 增加 build_id/git_sha/frontend_build_id 配置 |
| `backend/app/main.py` | lifespan 启动输出脱敏版本号 |
| `backend/app/services/relation_identity.py` | 增加 `display_node_key` 辅助 |
| `backend/tests/test_graph.py` | 重写：B01-B12 契约、跨系统同名、多边、邻居方向、limit、诊断、物理键 |
| `backend/tests/test_graph_contract.py` | 新增：GraphNode/GraphEdge/GraphMeta 契约固化 |

## 4. 前端修改

| 文件 | 修改 |
|---|---|
| `frontend/src/api/asset.ts` | GraphNode/GraphEdge/GraphMeta/GraphData/GraphOptionsData 类型更新；`getGraphNeighbors` 支持物理参数；新增 `getGraphEdgeDetail` |
| `frontend/src/views/asset/graph/index.vue` | 状态机（initial/loading/success/empty/filter_empty/auth/permission/api/contract/render_error）；options/graph/diagnostics 隔离；错误面板 + correlation ID + 重试；meta 展示；G6 回退；物理节点表详情跳转保留 system/source |
| `frontend/src/views/asset/graph/graphErrors.ts` | 新增：错误分类、correlation ID、契约校验 |
| `frontend/src/views/asset/graph/graphPhysical.ts` | 新增：物理键解析/规范化/体检 |
| `frontend/src/views/asset/graph/graphNormalize.ts` | 物理键去重、issues 上报、display_id 分组 |
| `frontend/src/views/asset/graph/graphTransform.ts` | buildGraphNodeKey/endpointNode 支持物理键与 display_id |
| `frontend/src/views/asset/components/GraphToolbar.vue` | meta 统计展示（total/matched/returned/truncated） |
| `frontend/src/views/asset/components/AdvancedRelationGraph.vue` | G6 init/render/destroy 异常捕获并 emit `render-error` |
| `frontend/src/views/asset/components/RelationGraph.vue` | 物理键分组/展示名 |
| `frontend/src/views/asset/components/GraphEvidenceDrawer.vue` | 边证据按需加载（edgeKey → getGraphEdgeDetail） |
| `frontend/src/views/asset/overview/index.vue` | 移除幽灵状态标签 `A_rechecked` |
| `frontend/tests/graphPage.test.ts` | 新增：物理节点/错误分类/幽灵状态/load policy |
| `frontend/tests/graphPageMount.test.ts` | 新增：组件挂载/140 节点/点击/卸载/meta/按需加载 |

## 5. 部署方案（P0-03）

| 文件 | 用途 |
|---|---|
| `deploy/scripts/release_manifest.py` | 生成发布 manifest（Git SHA / backend image / frontend build ID / Alembic head / 构建时间 / 关键文件 SHA256） |
| `deploy/scripts/release_frontend.sh` | 前端版本目录完整上传 + 原子软链接切换 + HTML 引用资源 200 校验 |
| `deploy/scripts/rollback_frontend.sh` | 前端完整版本回滚（切回 previous） |
| `deploy/nginx.conf` | index.html no-cache、hash 静态 immutable、root 指向 current 软链接 |
| `deploy/README.md` | 增加 108 号原子发布与回滚章节 |
| `deploy/releases/release-manifest-20260802-108.json` | 本次构建 manifest 证据 |

## 6. 测试证据

### 6.1 后端图相关（本地 + 服务器容器一致）

```
cd F:\python\数据资产\backend
python -m pytest tests/test_graph.py tests/test_graph_contract.py tests/test_graph_analysis.py tests/test_graph_sync.py tests/test_relation_endpoints_unit.py -q
85 passed
```

### 6.2 后端全量

```
python -m pytest tests/ -q   （服务器 data_asset_test，经 catalog-unify-test 容器最新代码）
560 passed, 2 skipped, 4 failed
4 failed = test_identity_nightly_sync.py TestSM4Password（gmssl 包缺失，107 号 JHEMR 专项环境依赖，与 108 无关）
```

### 6.3 Alembic

```
heads: f1a2b3c4d5e6 (唯一)
往返:  downgrade b4c5d6e7f8a9 → upgrade head 通过（含 c5d6e7f8a9b0 关系端点迁移回滚）
```

### 6.4 前端

```
pnpm run typecheck                        通过
pnpm exec vitest run tests/graphTransform.test.ts tests/graphEvidence.test.ts tests/graphPage.test.ts tests/graphPageMount.test.ts
                                          54 passed
pnpm run build                            成功（dist 79 个 JS，无 A_rechecked 残留）
```

### 6.5 浏览器 E2E（真实 Chrome headless + CDP）

```
SPA 首页真实加载成功（KPI 渲染）、/asset/graph 可达
console error = 0
unhandled rejection = 0
failed requests = 0
截图已生成（00_after_login/01_graph_default/02_home）
登录表单自动化：受 headless CDP 与 el-input/el-form 响应式同步限制未完成（见 §8 未执行项）
```

## 7. 生产发布步骤（需用户授权后执行）

1. 备份平台库（`pg_dump data_asset`）与 Nginx 配置；
2. 构建同一 Git SHA 的后端不可变镜像（deploy/docker/Dockerfile + wheelhouse）；
3. 前端完整构建后 `release_frontend.sh` 上传版本目录并原子切换；
4. Nginx root 指向 current 软链接，reload；
5. 生成 manifest 并校验前后端版本一致；
6. 认证后冒烟：graph/options/diagnostics 200，默认图节点边 > 0；
7. 保存回滚点（完整旧镜像标签 + previous 前端目录）。

## 8. 未执行项

| 项 | 原因 |
|---|---|
| 生产平台迁移（alembic upgrade） | 需用户授权；本轮无新迁移 |
| 生产后端镜像构建与替换 | 需用户授权发布窗口 |
| 生产前端版本目录切换 | 需用户授权 |
| 浏览器登录表单自动化 | headless CDP 与 el-form 响应式同步环境限制；已用组件测试 + API 集成覆盖默认渲染 |
| Playwright 安装 | pnpm 网络下载超时；已用等价 Chrome CDP 脚本替代 |
| 4 个 gmssl 测试失败修复 | 107 号 JHEMR 专项环境依赖，非 108 范围 |

## 9. 安全确认

- 业务源库写入：0
- 生产平台库写入：0（仅只读查询复核）
- 生产 Neo4j 部署：否
- 凭据写入代码/日志/git：否
- 输出 Token/密码/连接串：否（E2E 脚本用测试库账号，生产凭据未接触）
