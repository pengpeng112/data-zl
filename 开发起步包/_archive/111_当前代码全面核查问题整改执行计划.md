> 类别：待办
>
> 状态：待执行（已完成代码与生产数据库只读复核修订） | 优先级：P0 | 制定日期：2026-08-03 | 修订日期：2026-08-03
> 执行范围：`backend/`、`frontend/`、`deploy/`、测试与权威目录状态
> 安全边界：本计划不授权生产迁移、生产发布、业务源库 DML/DDL、生产夜间同步启用、写开关开启或 Git 强推

# 当前代码全面核查问题整改执行计划

## 1. 计划定位

本计划用于交给后续 AI 连续整改 2026-08-03 代码核查发现的问题。执行时必须同时遵守 `AGENTS.md`、`README.md`、`55_系统未完成事项统一执行计划.md` 以及 107/110 的身份同步 Phase D 门禁。

本计划只授权代码整改、测试库验证、脱敏测试和文档收口；不授权生产迁移、生产发布、业务源库 DML/DDL、生产夜间同步、写开关或 Git 强推。

## 2. 核查基线

### 2.1 已通过

- 前端 `pnpm run typecheck`：`tsc --noEmit` 与 `vue-tsc --noEmit` 均通过。
- 前端 Vitest：7 个测试文件、67 项通过。
- 前端生产 build：通过。
- 后端 `compileall`：通过。
- 后端明确无库纯逻辑测试：81 项通过。
- Alembic 本地迁移图唯一 head：`f8a9b0c1d2e3`。

### 2.2 未通过或阻断

1. 标准全量 pytest 因未设置合法 `APP_TEST_DB_URL` 被安全门禁拒绝，不能记为通过。
2. `--noconftest` 可绕过门禁；一次混合测试实际解析到了 `.env` 中的非测试平台库。插入因缺列失败并回滚，未形成提交，但隔离设计存在 P0 缺陷。
3. 一次误连的非测试库曾缺少 ORM 已使用的 `asset_identity_persons.profile_summary`；2026-08-03 对 8.83 当前生产平台库只读复核确认 revision=`f8a9b0c1d2e3`（head）且该列实际存在。因此不能再将 R02 表述为当前生产 schema 漂移，原现象应归入 R01 的错误环境路由/陈旧非测试库风险。
4. 本次未执行 Alembic 写迁移、生产发布或业务源库写操作。

## 3. 问题清单

| 编号 | 级别 | 问题 | 主要证据 | 完成判定 |
|---|---|---|---|---|
| R01 | P0 | `--noconftest` 可绕过测试库门禁 | `tests/conftest.py`、直接使用 `SessionLocal` 的测试 | 任意 pytest 入口均不能连接非测试库 |
| R02 | 已核销当前生产缺陷，转 R01/P1 环境治理 | 一次误连的非测试环境 schema 陈旧，不是当前生产迁移缺失 | 8.83 生产 `alembic current=f8a9b0c1d2e3 (head)`，`asset_identity_persons.profile_summary` 存在 | 找出并标记陈旧非测试环境；测试路由不可再命中；生产不新增修复迁移 |
| R03 | P0 | 停用、过期、未生效角色仍会被鉴权采用 | `main._lookup_roles`、`auth_service.lookup_roles` 未过滤；`core/security.py::_effective_role_codes` 已有正确过滤基础 | 所有鉴权入口复用统一有效角色实现 |
| R04 | P0（部分） | production 可带开发 JWT 密钥和宽松 RBAC 启动 | JWT/环境/RBAC 弱默认属实；CORS origin 默认仅 localhost、不是通配，但 methods/headers 为通配 | 生产弱配置启动必定失败，CORS 校验按 origin/method/header 分项判断 |
| R05 | P1 | 多个写接口缺少动作级权限 | candidates、tables、metadata、systems 部分端点 | 每个写端点显式权限或明确公开白名单 |
| R06 | P1 | 原始鉴权/连接异常可能返回客户端或落未脱敏日志 | `main.py` 直接返回 `str(e)` 等 | 客户端仅错误码和 correlation ID |
| R07 | P1 | 离线依赖不可复现 | 无 `requirements.lock`，离线流程使用下限依赖 | 锁文件、离线包、哈希和断网演练通过 |
| R08 | P1 | SSH 主机校验和启动门禁不严格 | `AutoAddPolicy`、Oracle 检查 `|| true` | 未知主机或运行时缺失必须失败关闭 |
| R09 | P1 | 权威文档状态冲突、目录孤儿 | README 中 108 状态冲突；55 横幅过期；`_decompile_tmp/` 未登记 | README/55/模块状态一致、目录无孤儿 |
| R10 | P1 | 工作区格式漂移和临时脚本未收口 | 大量 CRLF/LF 警告、未跟踪 `_*.py` | 功能 diff 可审计，临时文件不进发布 |
| R11 | P2 | 前端主包和图谱包偏大 | 主包约 2.00 MB，图谱包约 1.42 MB | 完成拆包并设置体积预算 |
| R12 | P2 | Browserslist/Baseline 数据过旧 | 构建警告 | 更新锁文件后构建无过期警告 |

## 4. 严格顺序

必须按 `S0 → S1 → S2 → S3 → S4 → S5 → S6 → S7 → S8 → S9` 执行。S1–S4 未通过前禁止生产迁移或身份同步 Phase D。

## S0：冻结现场

1. 记录分支、HEAD、tag、`git status --short`、Alembic head，保留所有既有协作者改动，不回退未知修改。
2. 将文件分为业务代码、迁移、测试、部署、文档、生成物、临时脚本和敏感配置。
3. 只输出 `.env` 变量名，不输出值；确认 `.env`、凭据和测试明细未跟踪。
4. 只记录当前 `.env` 的 test/non-test 属性，不再用它运行测试。
5. 形成保留/修改/归档/忽略清单；业务源库写操作必须保持 0。

## S1：不可绕过的测试数据库隔离

**修改范围**：`backend/app/core/config.py`、`backend/app/core/db.py`、`backend/tests/conftest.py`、数据库测试文件及 pytest 配置。

1. 在 SQLAlchemy `create_engine()` 前统一校验测试连接：检测到 pytest 进程或 `APP_ENV=test` 时，必须同时满足显式 `APP_TEST_DB_URL`、实际 `APP_DB_URL` 与测试 URL 完全一致、数据库名符合测试规则、目标不在非测试拒绝清单；测试进程若仍为 `APP_ENV=dev/production` 必须在建连前失败。
2. 防护不能只放 conftest；必须覆盖单文件运行和 `--noconftest`。
3. 数据库测试统一使用 `db_session`/`client` fixture，禁止测试模块自行创建全局 `SessionLocal`。
4. 失败清理先 `rollback()` 再删除，避免 `PendingRollbackError` 掩盖原始异常。
5. 增加子进程测试：缺失变量、伪造测试库名、生产 URL、标准 pytest、单文件、`--noconftest`。
6. 测试保护不得无条件作用于所有 `SessionLocal` 导入；普通生产/运维脚本不属于 pytest 且 `APP_ENV!=test` 时保持既有运行路径。为脚本增加回归测试，证明保护不会误伤合法的非测试启动，同时不允许脚本伪装测试进程绕过门禁。

**验收**：

```powershell
cd F:\python\数据资产\backend
.\.venv\Scripts\python.exe -m pytest tests/test_test_database_guard.py -q
.\.venv\Scripts\python.exe -m pytest tests/ -q
```

必须证明危险场景在建连前失败、非测试库写审计为 0、全量 pytest 连续两次不依赖残留数据。

## S2：ORM、迁移、数据库三方对账

1. 当前生产只读核查已完成：`alembic current=f8a9b0c1d2e3 (head)`，`profile_summary` 存在；生产没有 R02 所称缺列，不执行 upgrade 或修复迁移。
2. 识别先前误连的具体数据库环境，只记录脱敏环境标识、数据库名分类、revision 和结构状态；将其加入测试拒绝清单，禁止再次作为测试目标。
3. 已确认 `u7b8c9d0e1f2` 迁移存在、可达且内容正确；任何其他环境 revision 落后时按“部署未升级到 head”处理，不得为此重复创建迁移。
4. 只有某环境 revision 已包含 `u7b8c9d0e1f2` 但列仍缺失，才能判为版本误标/人工改库等真实 schema 漂移；先在隔离副本复现和查明原因，再决定是否新增手写幂等修复迁移。
5. 在隔离测试库核对表、列、类型、默认值、索引、唯一约束和外键；空白隔离库和备份恢复隔离库分别执行 `upgrade`，只在隔离库执行 `downgrade` 一版再 `upgrade head`。

**验收**：唯一 head；ORM 与隔离库无未解释差异；往返数据守恒；生产库不 downgrade。

### S2 数据库只读复核补充（2026-08-03）

- 平台库：生产环境，revision=`f8a9b0c1d2e3`，`profile_summary` 列存在。
- 角色数据：`asset_user_roles` 当前仅 1 条 active，未来生效 0、已过期 0；R03 是代码级潜在越权，不是已发生的角色脏数据事故。
- 生产配置：RBAC 绑定已开启、JWT 不是默认值且长度达标、运维/字典/身份/nightly/密码写开关均关闭；但 CORS origin 仍含 localhost/127.0.0.1，S4 的 production 配置收敛仍需执行。
- 本轮只执行 SELECT/元数据查询，平台库写入 0，业务源库 DML/DDL 0，未输出凭据或用户标识。

## S3：统一有效角色判定

**修改范围**：`backend/app/core/security.py`、`backend/app/main.py`、`backend/app/services/auth_service.py` 及 RBAC/登录/API Key 测试。

1. 以现有 `backend/app/core/security.py::_effective_role_codes` 为唯一基础实现，先补齐其边界测试，再让 `main._lookup_roles` 和 `auth_service.lookup_roles` 调用该函数；不得另写第三套过滤逻辑。统一过滤 `status='active'`、`valid_from <= now`、`expires_at > now`，正确处理 NULL 和 UTC 时区。
2. JWT roles 只能作为展示缓存，授权以数据库实时有效角色为准；API Key 与 JWT 复用相同查询。允许单请求内复用查询结果；跨请求 TTL 缓存只有在具备撤权主动失效机制时才能启用，否则会破坏“撤权立即生效”验收标准。
3. 增加撤权后旧 JWT、停用、过期、未来生效、无角色和管理员角色测试。

**验收**：撤权后无需等待 JWT 过期即 403；中间件、登录响应、`/auth/me` 角色一致；E9 矩阵全绿。

## S4：production 配置失败关闭

1. `APP_ENV=production` 时强制校验：JWT 密钥非占位且足够长度、绑定角色开启、CORS origin 不为通配且不得保留 localhost 开发地址、methods/headers 按生产最小集合收敛、运维/字典/身份写开关关闭、凭据目录存在且权限正确、build ID/git SHA 存在。当前 origin 默认值为两个 localhost 地址，并非 `*`；不得将 methods/headers 的 `*` 误写为 origin 通配。
2. 将 `deploy/backend.env.example` 改成安全 production 示例，开发示例单独维护。
3. 保留用户接受的 HTTP 部署，但校验 HttpOnly、SameSite、Cookie 路径和 CSRF。
4. 增加配置矩阵测试和容器弱配置启动失败测试。
5. 复用并前移 `auth_service.py` 现有 JWT 请求时 lazy 校验规则，使启动期和请求期调用同一验证器；禁止维护两套相互冲突的阈值或错误判定。

**验收**：占位密钥或弱 RBAC 无法启动；正式配置启动成功且写开关仍关闭。

## S5：所有写接口显式动作授权

为以下端点增加 `Depends(require_permission(...))` 和专门测试：

1. `candidates.py` promote/reject：关系审核权限。
2. `tables.py` 表/字段 annotation：资产注释维护权限。
3. `metadata_changes.py` 采集、重试、diff、事件更新：采集/治理权限。
4. `systems.py` peripheral bootstrap、连接测试、draft 测试：source manage/test 权限、频率限制和目标地址策略。
5. 自动生成路由审计：所有 POST/PUT/PATCH/DELETE 必须有显式权限或严格公开白名单。
6. 连接测试防 SSRF：协议、端口、目标网段、超时、并发和返回信息全部限制。

**验收**：匿名、普通、申请、审核、管理员、自审和越权矩阵全绿；未授权写端点统一 403；审计包含申请人/审核人/执行人。

## S6：异常和审计脱敏

1. 禁止返回 `str(exc)`；统一返回错误码、通用消息和 correlation ID。
2. 日志、`error_message`、`result_ref` 经脱敏器处理，清理连接串、用户、密码、Token、患者信息和 SQL 参数。
3. broad `except Exception` 按可恢复/业务/数据库/未知分类，未知错误只保留服务端堆栈。
4. 增加恶意异常文本测试，确保 API、日志和数据库审计不出现秘密原文。

## S7：部署和离线可复现

1. 在目标 Linux 同 OS/架构/Python 环境生成带哈希的 `requirements.lock` 并提交；离线流程禁止直接使用未锁定下限依赖。
2. 固化 Node/pnpm、pnpm offline store、wheelhouse、dist 和 `SHA256SUMS`。
3. 先确认 `sync_local_to_server.py` 是否仍在部署链使用：已废弃则迁入归档并移除调用；仍在使用则先配置、备份和验证受控 known_hosts，再移除 `AutoAddPolicy`，改为拒绝未知主机。不得在 known_hosts 尚未就绪时直接切换导致部署链不可用。
4. `run_data_asset_api.sh` 不得使用 `|| true` 忽略 Oracle、凭据目录、权限或挂载检查。
5. 解释凭据卷 rw 与业务源库只读的边界，按最小权限运行。
6. 在干净断网脱敏环境完成安装、迁移、重启、回滚和二次安装幂等演练。

**验收**：无公网依赖；缺 wheel、哈希不符、未知 SSH 主机、Oracle runtime 缺失均失败关闭；健康、登录、RBAC、只读连接、回滚通过。

## S8：前端性能和依赖

1. 使用 visualizer 分析包体，按路由动态加载 G6/ECharts；图谱详情和证据保持按需加载。
2. 为主入口、图谱异步包和 CSS 设置 gzip 体积预算，超限 CI 失败。
3. 用 pnpm 更新 Browserslist、caniuse-lite、baseline-browser-mapping，并重新执行：

```powershell
cd F:\python\数据资产\frontend
pnpm install --frozen-lockfile
pnpm run typecheck
pnpm test
pnpm run build
```

4. 在普通办公终端和受限带宽下验证首屏、图谱、空图和 SVG 降级。

## S9：文档、目录和 Git 收口

1. 统一 README 中 108/109 状态，更新 55 顶部最后复核日期和真实剩余项，不删除历史日志。
2. `_decompile_tmp/` 无有效资产则迁移 `_archive/` 并登记；仍需使用则补用途和清理条件。
3. 增加 `.gitattributes` 固定行尾，隔离纯行尾变化后重新审查功能 diff。
4. 对 `_metrics_snapshot.py`、`_run_push_tests.py`、`_server_test_medical_push.py` 等临时脚本逐一裁决，禁止混入发布提交。
5. 执行秘密扫描、`git diff --check`、迁移检查、前后端全量验收后分组提交；禁止 force-push。

**验收**：README 无孤儿/幽灵条目；功能 diff 可审计；工作区 clean；最终 SHA/tag 可追溯。

## 5. 最终门禁

| 领域 | 必须通过 |
|---|---|
| 测试隔离 | 任意 pytest 入口拒绝非测试库；非测试库写审计为 0 |
| 后端 | 全量 pytest 连续两次 0 failed/0 errors |
| 迁移 | 唯一 head；隔离库 upgrade/downgrade/upgrade；schema 无漂移 |
| 权限 | 全角色矩阵、过期/停用角色和自审/越权全绿 |
| 配置 | production 弱配置启动失败；正式写开关关闭 |
| 前端 | typecheck、Vitest、build、浏览器关键路径全绿 |
| 部署 | lock、wheelhouse、pnpm store、哈希、断网和回滚全通过 |
| 文档/Git | README、55、108–111 一致；秘密扫描通过；工作区 clean |
| 业务源库 | DML/DDL 始终为 0；Phase D 前夜间与写开关关闭 |

每阶段回报必须包含：阶段状态、修改文件/行号、实际命令和结果、数据库目标类型、写操作计数、新问题和下一阶段门禁。任何非测试库误连、迁移漂移、越权或秘密泄露都必须停止后续生产步骤，不得标记为环境通过。
