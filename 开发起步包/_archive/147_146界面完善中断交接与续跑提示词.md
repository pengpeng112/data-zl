> 类别：执行交接证据

# 146 界面完善中断交接与续跑提示词

> 状态：2026-08-24 用户要求当前 AI 停止连续开发并交给其他 AI。本文只记录接续事实和提示词，**不是新的执行计划或未完成事项入口**；任务范围、裁决和完成标准仍以 `146_界面完善详细执行计划_复核版.md` 为唯一权威，未完成事项总入口仍为 `55_系统未完成事项统一执行计划.md`。

## 1. 接手者最短读取顺序

1. 根 `AGENTS.md`。
2. `开发起步包/README.md`。
3. `开发起步包/55_系统未完成事项统一执行计划.md`。
4. `开发起步包/146_界面完善详细执行计划_复核版.md` 全文，重点读 §0.1、§3–§9、§11 最后三条记录。
5. 本文；然后直接从 C1 复核续跑，不重复 S0/A/B。
6. 涉及 query-center 时再核对 144 §16.1 的 S8/前端记录。

## 2. 当前准确进度

28 组中，**9 组已完成并在 146 §11 留有测试证据**；C1 只有部分代码，未达到 DONE；其余 18 组未开始。

| 阶段 | 任务组 | 当前状态 | 接手动作 |
|---|---|---|---|
| S0 | 基线冻结 | DONE | 不重复；沿用 146 §11 的 Git/测试/SHA 基线 |
| A | A1–A5 | DONE | 不重做；只在最终全量门禁中回归 |
| B | B1–B4 | DONE | 不重做；只在最终全量门禁中回归 |
| C | C1 | IN_PROGRESS | 先复核现有改动，补专项测试和必要修复后再判 DONE |
| C | C2–C3 | NOT_STARTED | 严格按 146 顺序实施 |
| D | D1–D5 | NOT_STARTED | C 阶段记录完成后实施 |
| E | E1–E11 | NOT_STARTED | D 阶段记录完成后实施 |

### 2.1 已完成的 A/B

- A1：候选前端死页/API 清理，`/asset/candidates` 隐藏函数式 redirect 保留 query。
- A2：permission 演示页、演示权限码和旧 SSO 示例清理。
- A3：RelationGraph layered 真正走 `buildLayered`，死代码和重复样式清理。
- A4：SQL 工作台错误/重试、对象型参数 JSON 校验、伪可写目标清理；未新增确认。
- A5：医学字典总览使用 `push_enabled`，诊断/手术分别统计。
- B1：systems 详情驱动编辑、独立 system_type、连接筛选分页/编辑/更多菜单、向导校验、Tab query 和资源树跳转。
- B2：persons classification/person_type 双维度 AND 与旧参数兼容；画像 rules/tag/错误态/ReStatCard。
- B3：metadata diff 动态来源、同来源快照、清旧值、交换和时间顺序校验。
- B4：query-center canonical 路由/`query.view`、类型化 API、SQL 空白示例、试运行表格、真实分页/month picker/中文状态、`v-perms`；后端查询/指标/产品读权限、AI context 与完整 SQL 细权限。

### 2.2 C1 已落但未验收的代码

- `backend/app/api/v1/permission_requests.py`：正在统一 `request_content/reason/requested_by/created_at/status`，mine/pending 分页；权限改为 `identity.permission_request.view/create/approve/execute`；新增 create/approve/reject/execute/revoke 审计并与业务写同事务。
- `backend/app/api/v1/permissions.py`：新增 C1 四个资源并给 identity_admin 默认角色补齐。
- `frontend/src/api/permissions.ts`：新增 PermissionRequest DTO/PageData 和分页参数。
- `frontend/src/views/identity/permission-requests/index.vue`：已恢复为正常 Vue/TS 页面，含发起申请、申请内容/申请人/原因/时间/状态、分页、loading、错误重试和防重复提交。
- 只跑过：前端双 typecheck PASS；既有 `backend/tests/test_permissions.py` 5 passed。**没有 C1 专项测试，没有阶段 build/full regression，故不得写 DONE。**

## 3. 接手后第一批必须做的事

1. 重新做目录自检和 `git status --short`，但不得清理、reset、checkout 或覆盖现有 144/146 dirty 工作。
2. 阅读 146 §11，确认 S0/A/B 已完成；以本交接的 C1 文件为最小修改范围。
3. 为 C1 增加后端申请、分页、审批、执行、自审、越权 403、审计测试，以及至少一个前端契约/组件测试；修复后跑 C1 相关回归。
4. 完成 C2 前再次运行 `alembic heads`。当前唯一 head 是 `g7b8c9d0e1f2`；若增加 `DictSystemItem.enabled`，新手写迁移必须接这个 head，禁止 `--autogenerate`。
5. 连续完成 C2、C3；只有 C1–C3 全部验收后，才向 146 §11 追加一条 C 阶段 DONE/BLOCKED 记录。
6. 随后按 D1–D5、E1–E11 连续执行并逐阶段记录，最终跑 146 §9 全门禁并更新 README/55/146。不要再建新的计划或平行状态报告。

## 4. 测试与环境事实

- S0 后端全量基线：`1032 passed / 1 skipped / 0 failed / 7 warnings`。
- S0 前端基线：`21 files / 125 tests passed`，双 typecheck/build/gzip 通过。
- A 完成后：前端 `22 files / 129 tests passed`，双 typecheck/build/gzip 通过。
- B 完成后：前端 `23 files / 133 tests passed`，双 typecheck/build/gzip 通过；B 后端相关回归 `52 passed / 0 failed / 5 warnings`；144 queryCenter 测试继续通过。
- C1 中断前最小验证：`backend/tests/test_permissions.py` 为 `5 passed / 0 failed / 5 warnings`；前端双 typecheck 通过。未跑 C 阶段全量 test/build。
- 既有 `127.0.0.1:55432` SSH 隧道曾可达 8.83 隔离库 `data_asset_test`。执行 pytest/alembic 时必须从受控环境构造测试 URL，并**同时**设置 `APP_TEST_DB_URL` 与 `APP_DB_URL`；不得打印凭据。隧道不可用即标 `BLOCKED_TEST_DB`，绝不改用开发/生产库。

## 5. 工作区归属与禁止动作

- S0 冻结时：HEAD `2a9a259e79aff422d14add89058b60c6ee566b6d`，已有 144 未提交成果和 145/146 review 资产；归属详见 146 §11 S0。
- 146 新增/续改文件以 146 §11 的 A、B、C 记录为准。高冲突文件仍包括 `query-center/queries/index.vue`、`api/query-center.ts`、`router/modules/asset.ts`、`api/dict.ts`、`permissions.py`、metadata/identity/dictionary 路由和 Alembic 链。
- 不得 Git commit/push/tag，不得部署、改服务器/调度/凭据或生产 apply。
- 不读取/持有业务源库凭据，不连接、探查或写业务源库。到本交接为止业务源库连接 0、写入 0；数据库访问仅限隔离测试库。

## 6. 可直接交给下一位 AI 的提示词

```text
你是 F:\python\数据资产 仓库 146 界面完善任务的接续执行 AI。不要重新评估或重做已完成阶段；请从当前 dirty 工作区连续完成剩余开发。

按序完整读取：
1. 根 AGENTS.md；2. 开发起步包/README.md；3. 开发起步包/55_系统未完成事项统一执行计划.md；
4. 开发起步包/146_界面完善详细执行计划_复核版.md 全文，重点 §0.1、§3–§9、§11；
5. 开发起步包/147_146界面完善中断交接与续跑提示词.md；涉及 query-center 再读 144 §16.1 S8/前端记录。

当前权威状态：S0、A1–A5、B1–B4 已 DONE，不得重复；C1 为 IN_PROGRESS，C2–C3、D1–D5、E1–E11 未完成。先复核并完成 C1：现有 permission_requests.py、permissions.py、frontend/api/permissions.ts、identity/permission-requests/index.vue 已有部分实现，但没有 C1 专项测试，不能直接标 DONE。补齐申请、分页、审批、执行、自审/越权 403、审计和前端契约测试，修复后再继续 C2、C3。C 全部验收后才追加 146 §11 的 C 阶段记录，然后连续执行 D、E；不要阶段间等待确认。

C2 涉及结构变更前必须执行 alembic heads；交接时唯一 head=g7b8c9d0e1f2。若加 DictSystemItem.enabled，必须手写迁移并接当前 head，禁止 --autogenerate。迁移只允许在 8.83 的 data_asset_test 隔离库经 SSH 隧道验证；同时导出 APP_TEST_DB_URL 与 APP_DB_URL，凭据不得进入命令输出/文档/日志。不可用则标 BLOCKED_TEST_DB，绝不借生产或开发库。

完整服从 146 的交互裁决：不新增确认，不改用户明确保留的确认；兼容 URL 不得 404且 query 原样传递；query-center 使用 canonical dot 权限和 v-perms；system_type 不复用 dbTypes；metadata diff-preview 必须零写；dict/general 以 /api/v1/dictionaries 和真实 DTO 为准；密码 8–18；ops 新终态 succeeded、executed 只读兼容；快照只软归档；table API 用 source_code 隔离。

平台新增/修改写接口必须：操作者来自登录态、require_permission、同事务治理审计、失败关闭、敏感字段脱敏。禁止业务源库连接/写入，禁止服务器/生产/部署/调度/凭据修改，禁止 git commit/push/tag。不得 clean/reset/checkout，保留 144 与 146 现有 dirty 成果；高冲突文件串行最小增量。

每组至少一个针对性测试；每完成 C/D/E 立即追加 146 §11 的真实状态、计数、文件和阻断。最终严格运行 146 §9：后端全量 pytest；安全可用时隔离库 alembic upgrade head；前端 pnpm install、test、双 typecheck、build；再更新 README、55、146。最终按用户要求只汇报 28 组 DONE/BLOCKED 矩阵、相对 S0 的文件归属、测试原始计数、真实阻断/风险、未授权未执行动作、业务源库零连接零写入证明。
```

## 7. 本次交接自身动作

- 新增本文并登记 README；在 146 §11 追加 C 中断事实。
- 未创建 C2 迁移，未执行 alembic upgrade，未运行生产/服务器/部署/Git 写操作。
- 本次交接前为避免留下被删除的半文件，已将 C1 权限申请页面重建并通过双 typecheck；这不等于 C1 DONE。
