> 类别：待办 / 交付提示词
> 状态：历史交付提示词；2026-07-14 后不得直接执行。当前请使用 `55_系统未完成事项统一执行计划.md` 的 E1–E10 和 `66_多AI交接_未完成与待实现清单.md` 置顶决策
> 最近修订：2026-07-12
> 配套文件：`55_系统未完成事项统一执行计划.md`（线性清单版，是唯一执行依据）
> 取代：`56_系统整改执行提示词.md`（分阶段版，已过时——它把大量已完成的 P0/P1 项当成待办）

---

# 线性整改执行提示词（交付给执行 AI）

你是资深安全 + 全栈工程师，接手医院数据资产治理平台（`F:\python\数据资产`，FastAPI + Vue3 + PostgreSQL + Alembic）的整改。T1-T15 已完成首轮执行，**不得按本提示词重复执行已完成的迁移或资产生成**；先以 55 号状态横幅和执行日志确定仍需处理的项，再连续处理可在当前环境验证或修复的事项。涉及 T11/T13 的人工审批或生产写入必须停在审批前。

## 一、最重要的前提：大部分工作已完成

以下是首轮执行前的已完成基线；后续状态以 55 号为准，**不要重做已落地的迁移或资产生成**：
- Token 哈希存储（迁移 `q3d4e5f6a7b8` + `governance.py:15` `token_hash` 列）
- `admin/init` 公开端点已删除
- 前端硬编码 Token `DEV_ASSET_API_TOKEN` 已删除
- `rbac_require_bound_token=False` 的未绑定 Token 兼容模式（生产环境必须通过 `APP_RBAC_REQUIRE_BOUND_TOKEN=true` 显式启用强制绑定）
- ops 操作人服务端派生（`ops_tools.py` 用 `current_user`，职责分离校验已在 `:276,:335`）
- 测试库强制 `APP_TEST_DB_URL`（`conftest.py:12-14`）
- 前端三大契约修复（关系复核 `res.data?.items` + 标题正常、metadata diff `snapshot_id_from/to`、dict/medical 路由）
- 调度真实调用采集（`main.py` `_run_metadata_collect` 调 `_collect_metadata_snapshot`）
- Nginx CORS 反射已删
- `deploy/offline/README.md` + `init_db.sql` + `deploy/systemd/data-asset.service` 已建

55 号 T1-T15 是执行历史和验收口径；当前仅处理其状态仍为未通过或阻塞、且当前环境可处理的项目。

## 二、开工前必读（顺序不可颠倒）

1. `AGENTS.md`（仓库根）——9 条硬约束 + 安全红线。
2. `开发起步包/README.md`——唯一权威目录。
3. `开发起步包/55_系统未完成事项统一执行计划.md`——**唯一执行依据**（线性清单版）。
4. 按任务需要再读 51/52/53/54 号模块文档。
5. **不要读 `46_文档完成状态总表.md`**（已归档且物理不存在）。
6. **不要读 `56_系统整改执行提示词.md`**（已过时的分阶段版）。

## 三、硬约束（违反即回滚）

1. HIS/ODS/HRP 业务源库只 `SELECT`，禁写；平台库写操作必须经审批+审计+白名单。
2. 密码/Token/身份证/姓名/电话/地址不进代码、日志、git。
3. 单一 `asset` schema，新表前缀 `asset_<模块>_<实体>`。
4. 迁移手写 `upgrade()`/`downgrade()`，禁 `--autogenerate`。
5. 前端必须用 pnpm；typecheck 同时过 `tsc --noEmit` 和 `vue-tsc --noEmit`；不改现有路径。
6. AI 工具不接入写操作。
7. 大表禁全扫（`ROWNUM <= N`）；保留旧表不删。

## 四、15 步线性执行清单（详见 55 号文件，这里是摘要）

| 步 | 内容 | 性质 |
|---:|---|---|
| T1 | 验证已完成 P0 改动跑得通（7 项验证，无新代码） | 验证 |
| T2 | 固化 Git 基线（master 大量未提交，分组 commit） | 唯一硬阻塞 P0 |
| T3 | 合并 `QualityFinding` 双状态字段（`quality.py:56,57` 真实剩余） | 新代码+迁移 |
| T4 | 只读 SQL 数据库级防护（只读事务+超时+方言校验） | 新代码 |
| T5 | 元数据快照按 `source_code` 隔离+索引 | 新代码+迁移 |
| T6 | 离线部署干净环境演练 | 演练 |
| T7 | ODS/HIS/HRP 资产归一优化包（依 51 号，D 类关系只进待验证图层） | 新开发 |
| T8 | 关系口径库（依 54 号，建 `asset_relation_recipes` + 导入 JSON） | 新开发+迁移 |
| T9 | HIS 人员/科室同步 dry-run（源库零写） | 演练 |
| T10 | HRP 核心字段补采（依 52 号，`WA_*` 8 张表） | 探查 |
| T11 | 平台库人员 upsert（前置：T9+审批） | 写平台库 |
| T12 | 质量规则生产夜间验证（只读，大表限定） | 验证 |
| T13 | 凭据轮换确认（人工配合，只记录完成日期） | 人工 |
| T14 | 全量验收（5 项命令实测，不得美化） | 验收 |
| T15 | 文档与状态收尾 | 文档 |

## 五、连续执行规则（关键）

1. **不要重复已完成步骤**：按 55 号当前状态处理可执行项；外部环境或人工前置未满足时如实记录，不得伪造执行结果。
2. **每步完成判定 5 条缺一不可**：① 改动有 `文件:行号`；② 验收命令实测通过；③ 结构变更配手写迁移；④ 至少一个测试；⑤ 更新 README + 55 号执行日志。
3. **不得把超时/失败写成通过**——如实记录，失败就修。
4. **仅在用户明确要求时提交**；提交前必须确认工作区中其他协作者改动不被纳入。
5. **遇到阻塞如实记录并继续处理无此前置的项**；T9 是 T11 的前置，T2 不是技术执行 T7-T12 的前置，但会阻塞发布基线固化。

## 六、验收命令（T14 全量验收用，其他步骤按需）

```
cd F:\python\数据资产 && git status
cd backend && .\.venv\Scripts\python.exe -m pytest tests/ -q
.\.venv\Scripts\python.exe -m alembic upgrade head
cd ..\frontend && pnpm run typecheck
pnpm run build
grep -rn "asset-platform-admin-2026\|admin123\|rectification_status\|http_origin" backend/ frontend/ deploy/
```

## 七、需人工配合（代码无法替代）

| 事项 | 原因 | 负责方 |
|---|---|---|
| T13 轮换数据库密码 `admin123` | 已进 git 历史 | DBA |
| T13 禁用旧固定 Token + 生成新 Token | 已进 git 历史 | 运维 |
| T13 预置 SSH `known_hosts` | 信任链由运维管理 | 运维 |
| T6 创建 `asset` schema 并授权 | DDL 需 DBA | DBA |

## 八、执行日志

在 55 号文件末尾"执行日志"区块逐项追加（格式见 55 号），每步记录：步号、日期、改动文件、commit SHA、验收命令与结果、遗留问题。

仅在 T14 全部验收通过、T2 基线已固化、T6/T9-T13 的环境与人工前置均已闭环后，才能把 55 号顶部状态改为 `🟢已完成`；否则保持进行中并登记阻塞原因。
