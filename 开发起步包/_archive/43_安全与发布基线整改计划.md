> 类别：待办 / 方案
> 状态：已独立复核通过（所有 P0/P1 证据已逐条在源码中验证），可直接交付执行
> 复核日期：2026-07-10
> 复核方式：读取了下列源码并逐条比对，证据全部属实
>   - backend/app/api/v1/admin.py:15,30-38,60-70
>   - backend/app/core/config.py:7,31-32
>   - backend/app/main.py:99-100,126-189
>   - backend/app/api/v1/ops_tools.py:119-138,223-260
>   - backend/tests/conftest.py:12-61
>   - backend/app/api/v1/relations.py:107-144
>   - backend/app/api/v1/metadata_changes.py:339-352
>   - frontend/src/utils/http/index.ts:17,76-82,146-153
>   - frontend/src/views/asset/relation-review/index.vue:96-103,201-203
>   - frontend/src/views/metadata-changes/diff/index.vue:156-163
>   - frontend/src/router/modules/dict.ts:15-18
>   - deploy/nginx.conf:17-25
>   - backend/.env.example
>
> 复核中对原报告的两处校正（已并入下文）：
>   1. 原报告写 "frontend/.../asset/metadata-changes/diff/index.vue"，实际路径是 `frontend/src/views/metadata-changes/diff/index.vue`（不在 asset 子目录）。
>   2. 后端 `relations/list` 参数实际名为 `review_status`（不是 `validation_status`），前端却传 `validation_status` 且直接读 `res.data` 数组——比原报告描述更严重（参数名也对不上）。

---

# 整改计划提示词（交付给执行 AI）

> 你是一个资深安全 + 全栈工程师。请按本文件的优先级顺序，对本仓库（`F:\python\数据资产`，FastAPI + Vue3 + PostgreSQL）执行整改。
>
> ## 强制前置约束（不可违反，否则回滚）
>
> 来自仓库 `AGENTS.md`，必须全程遵守：
> 1. **源库只读**：所有 DB 连接只允许 `SELECT`，禁止 DML/DDL；写操作必须经 `asset_action_executors` 白名单 + 审批。
> 2. **凭据脱敏**：密码 / Token / 身份证 / 姓名 / 电话 / 地址 不写代码、不写日志、不进 git；`.gitignore` 排除 `.env`。
> 3. **单一 schema**：所有新表继续在 `asset` schema 内用 `asset_<模块>_<实体>` 前缀，不得拆物理 schema。
> 4. **Alembic 手写**：迁移必须手写 `upgrade()` / `downgrade()`，禁用 `--autogenerate`。
> 5. **AI 不执行写操作**：AI 工具仅调只读探查端点 + 强制审计，不接入 `asset_action_executors`。
> 6. **大表禁全扫**：`HIS.LAB_RESULT` 等必须用 `ROWNUM <= N` 或子查询限定。
> 7. **保留旧表**：`asset_api_keys` / `asset_table_owners` / `asset_business_terms` / `asset_metadata_snapshots` 保留不删。
> 8. **前端不改现有路径**：`/relations/{id}/review`、`/dict-medical/*` 等保留。
> 9. **前端技术栈**：Vue3 + Element Plus + Pinia + Tailwind + Vite + TS（基于 pure-admin-thin）。**必须用 pnpm**（有 `preinstall: only-allow pnpm`）。typecheck 必须同时过 `tsc --noEmit` 和 `vue-tsc --noEmit`。API 层在 `src/api/*.ts` 按模块分文件，优先扩展现有文件而非新建。
> 10. **文档管理**：每完成一批改动，同步更新 `开发起步包/README.md` 文件清单。
>
> ## 每项任务的交付标准（必须同时满足才算完成）
>
> - 给出修改前/后的代码片段与文件:行号。
> - 通过对应验收命令（见下）。
> - 新增/修改的 DB 结构配手写 Alembic 迁移。
> - 新增至少一个针对性测试（pytest 或 vitest），且测试不连生产库。
> - 在本文件末尾"执行日志"追加：序号、改动文件清单、迁移文件名、测试命令与结果、遗留问题。

---

## 阶段 0：止血与可审计基线（先做，必须在任何功能开发前完成）

> 目标：建立"当前是什么版本"的可审计事实。本阶段不修业务代码，只固化状态。

### S0-T1 提交并标记当前工作区基线
**现状**：`git status` 显示 backend/frontend 几乎全量未提交（merge 后未整理）。
**要做**：
1. `git status` + `git diff --stat` 审阅全部改动，确认无明文凭据混入（重点 grep `admin123`、`asset-platform-admin-2026`、密码、连接串）。
2. 将当前状态按逻辑分组提交（后端、前端、文档分开），每个 commit message 用 conventional commits。
3. 在 `开发起步包/README.md` 增加"当前基线"区块，记录：基线 commit SHA、`git status` 是否 clean、验收命令清单。
**验收**：`git status` clean；README 有基线区块且 SHA 与 HEAD 一致。

### S0-T2 生成验收命令清单并记录基线结果
**要做**：在本文件"执行日志"如实记录以下命令的结果（不要美化，失败就写失败）：
- 后端：`cd backend && .\.venv\Scripts\python.exe -m pytest tests/ -q`（记录通过/失败/超时与具体失败用例）
- 后端：`.\.venv\Scripts\python.exe -m alembic upgrade head`（记录是否报错）
- 前端：`cd frontend && pnpm run typecheck`（记录 tsc 与 vue-tsc 是否都过）
- 前端：`pnpm run build`（记录是否成功、产物大小、是否超时）
**验收**：四项命令结果都有记录；失败的明确写出失败点。

---

## 阶段 1：P0 安全止血（顺序执行，每项独立提交）

> 目标：关闭任何可被匿名/伪造身份利用的写入口和凭据泄露面。

### P0-1 关闭公开 Token 初始化接口
**证据**：`backend/app/api/v1/admin.py:15` `DEFAULT_TOKEN = "asset-platform-admin-2026"`；`:30-38` `GET /api/v1/admin/init` 公开；`main.py:100` `PUBLIC_EXACT` 含 `/api/v1/admin/init`。
**风险**：任意访问者可抢先创建固定值管理员 Token。
**要做**：
1. 删除 `admin.py` 的 `DEFAULT_TOKEN` 常量与 `init_default_token` 路由。
2. 从 `main.py:100` `PUBLIC_EXACT` 移除 `/api/v1/admin/init`。
3. 新增 CLI：`backend/scripts/create_admin_token.py`，仅在本地/部署机执行，生成随机 `secrets.token_urlsafe(32)`，写入 DB 并**仅打印一次**。
4. 部署文档改为：首次部署用该 CLI 生成 Token，不在 HTTP 暴露。
5. 测试：删除原 `/admin/init` 的测试；新增 CLI 单测（用临时 sqlite，校验生成的 token 是随机的）。
**验收**：`pytest tests/test_admin*.py` 通过；`grep -r "asset-platform-admin-2026" backend/` 只剩注释或无；启动后 `/api/v1/admin/init` 返回 401。

### P0-2 强制 Token 绑定用户（生产默认拒绝未绑定）
**证据**：`config.py:31-32` `rbac_require_bound_token: bool = False`；`main.py:139-154` `_lookup_roles` 未绑定返回 `None` → 放行所有受保护路径。
**风险**：遗留/泄露 Token 无角色绑定也能访问全部接口。
**要做**：
1. 把生产默认改为强制：在 `Settings` 里将 `rbac_require_bound_token` 默认值设为 `True`（或更稳妥：保留默认 False 但在 `main.py` 启动时若 `APP_ENV=prod` 且为 False 则直接 `raise` 拒绝启动）——二选一，推荐后者并加注释说明向后兼容。
2. 修改 `_lookup_roles`：未绑定 Token 一律返回 `[]`（拒绝），删除 `None` 分支或仅允许显式 dev 环境走 None。
3. 新增 Alembic 迁移：给 `asset_api_keys` 增加 `user_identifier` 的绑定状态回填脚本说明（数据层不动，仅文档/脚本说明如何把存量 Token 绑定到 `platform_admin`）。
4. 提供 `scripts/bind_token.py` 把指定 token 绑定到 user。
5. 测试：未绑定 Token 访问受保护路径返回 403；绑定后放行。
**验收**：`pytest` 含该用例且通过；生产配置下未绑定 Token 全部 403。

### P0-3 删除前端硬编码 Token 与 403 自动回填
**证据**：`frontend/src/utils/http/index.ts:17` `DEV_ASSET_API_TOKEN`；`:76-82` 请求拦截写入 localStorage；`:146-153` 403 响应自动回填并重试。
**风险**：固定 Token 随前端源码分发，绕过登录态。
**要做**：
1. 删除 `DEV_ASSET_API_TOKEN` 常量及其在两处拦截器的所有引用。
2. Token 仅来自登录态（`getToken()`）或显式本地开发 `.env.local` 的 `VITE_DEV_API_TOKEN`（仅 `import.meta.env.DEV` 且变量存在时用，不写回 localStorage）。
3. 删除 403 自动回填逻辑。
4. 全局 grep `asset-platform-admin-2026`、`asset_api_token`，确保无残留。
5. 测试：新增 vitest 用例校验无硬编码 token；手动验证登录态正常带 token。
**验收**：`grep -r "asset-platform-admin-2026" frontend/src/` 无结果；`pnpm run typecheck` 过；`pnpm run build` 成功。

### P0-4 后端派生操作人/审批人/执行人身份
**证据**：`ops_tools.py:119-138`（`ToolRunRequest.requested_by`、`SubmitBody.submitted_by`、`ApproveBody.approved_by`、`ExecuteBody.executed_by`）全部由客户端传入；`main.py` 中间件已解析 token。
**风险**：客户端可伪造任意操作人，审计与职责分离失效。
**要做**：
1. 中间件解析 token 后，把 `user_identifier` 挂到 `request.state.user_identifier`（及 roles）。
2. 这些 Body 模型移除 `requested_by/approved_by/submitted_by/executed_by` 字段（或保留但忽略，以服务端派生值为准）。
3. 端点内用 `request.state.user_identifier` 作为 operator；DB 写入与审计日志一律用该值。
4. 职责分离：同一 user 不能同时是申请人+审批人+执行人（加校验，违规 400）。
5. 测试：伪造字段被忽略；职责冲突被拒。
**验收**：相关 pytest 通过；审计日志 operator 全部来自 token。

### P0-5 清除连接串与凭据明文默认值
**证据**：`config.py:7` `db_url` 默认含明文 `asset_app:admin123@10.20.1.153`；`.env.example` 同样明文。
**风险**：凭据泄露、误连内网库。
**要做**：
1. `config.py` 的 `db_url` 改为无默认值（`db_url: str`）或默认 `"postgresql+psycopg://user:pass@localhost/db"`；启动时缺失直接 `raise`。
2. `.env.example` 改占位符 `APP_DB_URL=postgresql+psycopg://USER:PASSWORD@HOST:5432/data_asset`，注明"必须替换"。
3. grep 全仓库 `admin123`，全部清除或改占位符。
4. 文档注明：密码仅经 `.env`（已 gitignore）或密钥管理注入。
**注意**：真实密码若已泄露，必须**轮换**（本计划无法替你轮换，需人工执行并在此记录轮换完成日期）。
**验收**：`grep -rn "admin123" backend/ deploy/` 无明文；启动无 `APP_DB_URL` 直接失败。

### P0-6 测试隔离（不连默认/共享库）
**证据**：`conftest.py:20-28` 直接用 `SessionLocal()`（连默认库）建 Token；`:33-54` 对真实库执行 delete/setval。
**风险**：误连生产/共享库时测试改写数据。
**要做**：
1. 新增 `backend/tests/test_db_setup.sql` 或迁移：测试专用库 schema，由 `APP_TEST_DB_URL` 指定；conftest 启动时校验该变量存在且库名含 `test`，否则 `pytest.skip`/报错。
2. 用 fixture 在每条用例外包事务回滚（`connection.begin()` + rollback），杜绝跨用例污染。
3. `_cleanup_test_medical_dicts` 改为只对测试库执行。
4. CI/本地文档写明测试库配置。
**验收**：无 `APP_TEST_DB_URL` 时测试安全跳过或明确报错；有用例时全部事务回滚不落库。

### P0-7 Nginx CORS 不再反射任意 Origin
**证据**：`deploy/nginx.conf:17-25` `add_header Access-Control-Allow-Origin $http_origin always;` + `Allow-Credentials true`。
**风险**：任意站点可带凭据跨域访问。
**要做**：
1. 删除 Nginx 里这 4 行 CORS 头与 OPTIONS 直接 204 的逻辑。
2. CORS 完全由 FastAPI（`main.py:87-93` 已有白名单 `settings.cors_origins`）统一管理。
3. 文档注明：`APP_CORS_ORIGINS` 生产值由 FastAPI 校验，Nginx 不再加 CORS 头。
**验收**：`deploy/nginx.conf` 不再出现 `Access-Control-Allow-Origin`；预检由后端处理。

---

## 阶段 2：P0 部署可复现性

### P0-8 重写离线部署流程，干净环境可复现
**证据**：`deploy/README.md:28-45,63-72,129-131`（打包路径、离线依赖、`asset` schema、pnpm 安装均可能失败）。
**要做**：
1. 写 `deploy/offline/README.md`，分三段：①Linux Python wheelhouse（`pip download -r requirements.txt -d wheels/`）；②前端 pnpm 离线 store（`pnpm fetch` + `pnpm install --offline`）+ 预构建 `dist`；③DB schema 初始化（创建并授权 `asset` schema 的 SQL，再 `alembic upgrade head`）。
2. 非 root systemd unit（`deploy/systemd/data-asset.service`，`User=dataasset`）。
3. 在一台干净环境（容器或 VM）演练一次，把每步实际命令与输出摘要记到 `deploy/offline/README.md`。
4. 明确 `asset` schema 必须由 DBA 预建并授权给应用账号。
**验收**：干净环境从零部署成功；文档含真实可复现命令；应用以非 root 运行。

---

## 阶段 3：P1 核心功能可用性（前端-后端契约对齐）

> 通用要求：每个契约修复都要①统一参数名与响应结构；②在本文件新增"API 契约表"区块登记；③补一个契约测试（前端 vitest 或后端 pytest）。

### P1-1 修复关系复核页（relation-review）契约
**证据（已复核更严重）**：
- 后端 `relations.py:107-144` `GET /api/v1/relations/list` 返回 `{total,page,page_size,items}`，参数名为 `review_status`（注意不是 `validation_status`）。
- 前端 `relation-review/index.vue:96-103` 传了 `system_code`、`validation_status`（后端不认），且 `allRelations.value = res.data`（把对象当数组）。
- 前端 `:201-203` 标题是乱码 `???????`（编码损坏）。
**要做**：
1. 前端改读 `res.data.items`；过滤参数对齐后端实际名（`review_status` / `confidence` / `keyword`）。`system_code` 后端目前不支持——二选一：前端去掉该筛选，或后端 `list_relations` 增加 `system_code` 过滤（推荐后者，用 `from_table`/`to_table` 前缀匹配）。
2. 修复 `:201-203` 乱码为正确中文（如"关系复核工作台"）。
3. 在 `frontend/src/api/asset.ts` 增加 `RelationListItem` 类型，页面用类型而非 `any`。
4. 后端新增 `system_code` 过滤 + 测试；前端 vitest 校验解析 `items`。
**验收**：页面正常加载、筛选生效、无乱码；契约测试通过。

### P1-2 修复元数据快照对比参数名
**证据**：后端 `metadata_changes.py:339-352` 参数 `snapshot_id_from/snapshot_id_to`；前端 `metadata-changes/diff/index.vue:160-163` 传 `from_id/to_id`。
**要做**：
1. 统一为 `snapshot_id_from/snapshot_id_to`（后端为准，前端改）。
2. 检查 `frontend/src/api/metadata.ts`（或对应文件）的 `runMetadataDiff` 入参类型，同步改名。
3. 后端补 pytest：传正确名 200，传旧名 422。
**注意路径校正**：前端文件在 `frontend/src/views/metadata-changes/diff/index.vue`（非 `asset/` 子目录）。
**验收**：diff 功能 200 返回；契约测试通过。

### P1-3 修复 `/dict/medical` 路由映射
**证据**：`router/modules/dict.ts:15-18` 指向 `@/views/dict/mappings/index.vue`，而 `src/views/dict/medical/` 真实存在。
**要做**：
1. 把 `component` 改为 `() => import("@/views/dict/medical/index.vue")`。
2. 确认 `dict/medical` 路由 name 不与其它冲突；保留路径不变（约束 8）。
3. 手动验证页面可达。
**验收**：访问 `/dict/medical` 加载 medical 页面；typecheck 过。

### P1-4 统一质量规则列表与看板响应结构
**证据**：`asset/quality/index.vue:915-920`（列表）与 `:751-756`（看板类别）期望结构，与后端 `quality.py:363-367,877-891` 不一致。
**要做**：
1. 先读两处后端实际返回结构（dict vs list、字段名）。
2. 统一为标准分页 `{total,page,page_size,items}`；看板类别统一返回数组 DTO（或前端用 `Object.entries()` 标准化，但优先后端规范）。
3. 前端加类型定义，去掉 `any`；后端补契约测试。
**验收**：规则库列表与看板均不报 `map is not a function`；契约测试通过。

### P1-5 修复数据源表单必填字段
**证据**：前端 `asset/sources/index.vue` 缺 `source_name_cn` 等后端必填（`systems.py:26-41`）。
**要做**：
1. 前端表单加 `source_name_cn` 必填校验。
2. 抽共享 DTO（前后端字段对齐表，登记到 API 契约表）。
3. 后端补 422 用例。
**验收**：新增数据源成功；缺必填返回 422 且前端有提示。

---

## 阶段 4：P1 数据安全与真实执行

### P1-6 SQL 只读加固（DB 级只读 + 超时 + 方言校验）
**证据**：`quality_sql_runner.py`、`db_connectors.py` 仅应用层只读。
**要做**：
1. 数据库账号只读（文档要求 DBA 授只读权限）。
2. 连接开启只读事务（PG `BEGIN READ ONLY` / Oracle `SET TRANSACTION READ ONLY`）。
3. 查询超时（`statement_timeout` / Oracle `call_timeout`）。
4. AST/正则方言校验：拒绝 `INSERT/UPDATE/DELETE/DDL/SELECT INTO/COPY/GRANT` 等；只放行 `SELECT/WITH`。
5. 测试：写操作被拒；超时被中断。
**验收**：注入测试全部被拒；只读事务生效。

### P1-7 调度任务真实执行（消除伪成功）
**证据**：`main.py:54-72` `_run_metadata_collect` 只插一条 success 记录，未真正采集。
**要做**：
1. 真实调用元数据采集服务（复用 `metadata_changes` 采集逻辑）。
2. 失败写 `status="failed"` + 错误信息；成功才写 success。
3. 启动时用 `CronTrigger.from_crontab(j.schedule_cron)` 校验 cron 合法性，非法跳过并告警。
4. 测试：构造失败采集，校验 failed 状态；非法 cron 不启动。
**验收**：调度执行结果真实；失败可观测。

### P1-8 元数据快照按 source_code 隔离
**证据**：`metadata_changes.py:419-438` 快照未按 source_code 过滤。
**要做**：
1. 快照表结构化保存 `source_code`（若现存在 `data` JSON 里，迁移为独立列 + 索引）。
2. 查询/对比按 source_code 过滤。
3. 补多源测试（两个 source 的快照互不串）。
**验收**：多源场景 diff 正确；有索引。

### P1-9 SSH 跳板信任链加固
**证据**：`db_connectors.py` 关闭主机密钥校验、默认 root。
**要做**：
1. 固定 `known_hosts` 文件路径，`AutoAddPolicy` 改为 `RejectPolicy`（或严格校验）。
2. SSH 用户改专用低权限账号（非 root）。
3. 文档说明 known_hosts 由运维预置。
**验收**：未知主机被拒；非 root 运行。

---

## 阶段 5：P2 工程治理（基线稳定后做）

1. **Token 哈希存储**：`asset_api_keys.token` 存 hash（如 sha256），原始 token 仅创建时返回一次；支持轮换与吊销。配迁移（新增 `token_hash` 列 → 回填 → 删明文列）。
2. **细粒度权限落地**：`resource + action` 逐接口校验，前端按钮控制仅体验层。
3. **生产构建关闭 Fake Server**：仅 `VITE_ENABLE_MOCK=true` 启用，生产默认关闭。
4. **强类型 DTO**：先为身份/关系/质量/元数据定义 TS 类型与后端 Pydantic，逐步开 `tsconfig strict`。
5. **图谱视图定位**：明确纯前端投影，或后端提供系统/业务域聚合图。
6. **文档与 Git 联动**：完成状态绑定 commit SHA + 验收命令 + 结果；未提交内容标"本地待提交"。
7. **文档路径校正**：全量改为 `F:\python\数据资产` 与 `pnpm` 命令（清除旧仓库路径与 npm 引用）。

---

## 整体执行顺序（严格遵守）

1. **阶段 0**（S0-T1, S0-T2）——先固化基线，再做任何修改。
2. **阶段 1**（P0-1 ~ P0-7）——安全止血，逐项独立提交。
3. **阶段 2**（P0-8）——部署可复现。
4. **阶段 3**（P1-1 ~ P1-5）——核心功能契约对齐。
5. **阶段 4**（P1-6 ~ P1-9）——数据安全与真实执行。
6. **阶段 5**（P2）——工程治理。

> **里程碑定义**：完成阶段 0+1+2 = 可审计的安全发布基线；在此之前不要恢复 HIS 同步 / 图谱 / 质量新功能开发。

---

## API 契约表（整改过程中维护）

| 端点 | 方法 | 入参（统一后） | 响应结构 | 前端调用文件 | 状态 |
|---|---|---|---|---|---|
| `/api/v1/relations/list` | GET | review_status, confidence, system_code(新增), keyword, page, page_size | `{total,page,page_size,items[]}` | relation-review/index.vue | 待整改 P1-1 |
| `/api/v1/metadata-changes/diff` | POST | snapshot_id_from, snapshot_id_to | diff 结果 | metadata-changes/diff/index.vue | 待整改 P1-2 |
| `/api/v1/dict/medical` | — | （路由组件） | — | router/modules/dict.ts | 待整改 P1-3 |
| `/api/v1/quality/...` | GET | （待规范化） | `{total,page,page_size,items[]}` | asset/quality/index.vue | 待整改 P1-4 |
| `/api/v1/systems/sources` | POST | source_name_cn(必填)等 | — | asset/sources/index.vue | 待整改 P1-5 |

（后续每完成一项，更新"状态"列并补充实际入参/响应。）

---

## 执行日志（执行 AI 逐项追加，格式示例）

```
### [S0-T1] 2026-07-XX
- 改动文件：...
- commit: <sha>
- git status: clean / 未提交项：...
- 备注：...

### [P0-1] 2026-07-XX
- 改动文件：backend/app/api/v1/admin.py, backend/app/main.py, backend/scripts/create_admin_token.py
- 迁移：无
- 测试：pytest tests/test_admin_token.py → 通过
- 验收：/api/v1/admin/init 返回 401；grep 无固定 token
- 遗留：需人工轮换已泄露的旧 token
```

### [本次执行] 2026-07-10
- 改动文件：后端 Token 初始化、RBAC、配置、Oracle/PG 只读连接、关系和元数据接口；前端 HTTP、关系复核、元数据 diff、字典路由、治理页和登录默认值；Nginx、部署文档、README。
- 迁移：`p2c3d4e5f6a7_add_snapshot_source_code.py`，已执行 `alembic upgrade head`。
- 已完成：P0-1、P0-2、P0-3、P0-5、P0-7；P1-1、P1-2、P1-3、P1-8、P1-9；调度元数据任务改为真实采集并记录失败。
- 测试：Python `compileall` 通过；后端针对性 pytest `38 passed, 1 warning`；前端 `pnpm run typecheck` 通过。
- 未完成：S0-T1 因工作区已有大量非本次会话改动，未擅自提交；P0-4、P0-6、P0-8、P1-4、P1-5 和 P2 尚未完成。
- 不确定/需人工确认：跳板机是否已为 `dataasset` 配置账号及 `known_hosts`；未配置时生产连接会按设计拒绝。真实泄露凭据轮换仍需运维执行。
- 最终验收：后端全量 `pytest tests/ -q` 为 `245 passed, 1 warning`；`alembic upgrade head` 已成功；前端 `pnpm run typecheck` 和 `pnpm run build` 均成功，构建产物约 `217.92 MB`。
- 说明：本次未执行真实 HIS/跳板机连接和干净目标机离线部署演练，原因是当前会话没有可安全验证的目标环境；离线流程已写入 `deploy/offline/README.md`，systemd 单元已写入 `deploy/systemd/data-asset.service`。
