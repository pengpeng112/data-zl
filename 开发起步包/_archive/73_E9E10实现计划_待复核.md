> 类别：方案 / 待复核
> 状态：待其他 AI 复核（本文件未经用户批准执行，是供审查的方案草案）
> 日期：2026-07-14
> 编写者：会话 AI（已完成 E1 收敛、E6 部分、源库探查 71 号）
> 用途：请接手 AI 复核 E9/E10 实现计划的合理性、完整性、与现有代码的兼容性，指出风险和遗漏

# 73 E9/E10 实现计划待复核文档

## 0. 为什么有这个文件

用户要求按 55 号 E1-E10 执行。**E2/E3/E4/E5/E6后端/E7/E8 部分因环境阻塞无法本会话完成**（详见 §1）。E9（人员画像+权限审批+按钮权限）和 E10（配方库 CRUD+审核）是纯代码开发，本会话可做，但在动手前用户要求**先生成方案文档交其他 AI 复核**，避免改坏现有功能。

本文件包含：① 当前进度与阻塞证据；② E9/E10 的完整调研结果（现状+缺口）；③ 我的实现方案；④ 待复核的决策点。请接手 AI 逐节审查后给出意见。

---

## 1. E1-E10 当前进度（2026-07-14 实测）

| 项 | 状态 | 证据/阻塞原因 |
|---|---|---|
| **E1** 工作区收敛 | ✅ 完成 | HEAD=`6b9d72b`，工作区 clean，2 个 commit（gitignore 扩展 + 文档同步），敏感扫描全绿 |
| **E2** 推送 | ⛔ 阻塞 | `git fetch` 报 `Permission denied (publickey)`——本机无 GitHub SSH key。本地领先 origin 16 commit，无法 push。需用户配置 SSH key 或改 HTTPS+token |
| **E3** 生产 RBAC+持久化 | ⏸ 需服务器 | 需在 8.83 改 `/etc/data-asset/backend.env` 设 `APP_RBAC_REQUIRE_BOUND_TOKEN=true`、重建容器、持久化挂载凭据卷 |
| **E4** Linux 离线包 | ⏸ 需 Linux | 需与目标兼容的 Linux 构建机生成 requirements.lock + wheelhouse + pnpm store |
| **E5** 断网演练 | ⏸ 需 VM | 需干净断网 Linux VM |
| **E6** 全量验收 | 🟡 部分 | 前端 typecheck ✅ build ✅ 安全扫描 ✅；后端 pytest/alembic 连不上平台库（10.20.1.153 本机不可达），需内网测试库 |
| **E7** HRP 补采 | ⏸ 需凭据 | 需 HRP 只读凭据（10.10.10.23），且用户曾决定"先不同同步"（55 号 §一已撤销为"可做"） |
| **E8** 回归核验 | 🟡 部分 | 71 号报告已有 ODS/HIS 连通、EXAM_NO 关系 100%、人员口径等核心证据；SSH 连接当前抖动（banner error） |
| **E9** 权限增强 | 📝 待开发 | 见 §3 |
| **E10** 配方库扩展 | 📝 待开发 | 见 §4 |

**关键事实**：E2-E8 的阻塞项都是环境/凭据类，不是代码问题。E9/E10 是唯一能本会话纯代码推进的项。

---

## 2. 已确认的用户决策（来自 55 号 §一 + 本会话 AskUserQuestion）

| 决策 | 内容 |
|---|---|
| E9 范围 | **全做**：人员画像 + 权限审批流 + 后端权限装饰器 |
| E10 范围 | **核心 CRUD + 审核**：model 重构/迁移、写 API（CRUD+提交/审核/启用/废弃）、前端列表+详情页。图谱联动和视图 SQL 生成留下一轮 |
| 按钮级权限 | 前端三件套（v-perms/hasPerms/`<Perms>`）已完备，缺**后端细粒度权限装饰器** |
| 安全 | 前端隐藏按钮不能替代后端授权；审批前不可生效；禁止自审批；业务源库零写 |
| HRP | 人员正式写入仍须 dry-run + 审批，不直接 apply |

---

## 3. E9 调研结果与实现方案

### 3.1 现状（已逐文件核实）

#### 后端 identity API（`backend/app/api/v1/identity.py`，共 ~1000 行）
- **只读**：departments(25)、persons(38)、accounts(71)、sync-diffs(107,129)、**person_profile(165)**、departments/{code}(238)、change-requests(463)、inconsistencies(999)
- **写（已有完整审批流范式）**：change-requests POST(440)/approve(490)/execute(511)、propose-master(772)/batch(791)、batch-approve(870)/batch-execute(911)
- **审批流实现**：`GovernChangeRequest`（draft→pending→approved→executed）+ 禁止自审批(L498) + `GovernAuditLog`
- **缺口**：`person_profile`(L165) 只是聚合 accounts/sources/departments，**无画像字段**（头像/简介/数据范围/标签）；无"编辑画像"端点（所有写走 CR 但只支持 name/dept/status 三字段 L557-589）

#### 后端 permissions API（`backend/app/api/v1/permissions.py`）
- 现有：seed(162)、resources(192)、roles(197,203)、matrix(229,239)、user-roles(259)、users/{id}/roles(275,PUT 直接授权无审批)、**me(308)**、api-keys(362,395)、audit(376)
- **`/permissions/me` 返回**(L320)：`{user_identifier, roles[], permissions[](形如"resource:action"), unbound_token, auth_via}`
- **缺口**：**无权限申请/审批流**（users/{id}/roles 是管理员直接替换，不走审批）；无"申请单"表

#### RBAC 模型（`backend/app/models/governance_base.py`）
- `AssetRole`(L8)：role_code(unique)/role_name_cn/role_type/description
- `AssetUserRole`(L21)：user_identifier/role_code/granted_by/granted_by/granted_at/expires_at——**无 source/status/request_id 溯源字段**
- `AssetRolePermission`(L33)：role_code/resource/action——**无唯一约束**（同一 role+resource+action 可重复，靠 seed 代码判重）
- `GovernChangeRequest`(L43)：通用审批流表（module/entity_type/request_payload/approval_status/requested_by/approved_by/executed_by），identity 和 dict 模块已复用，**权限申请可复用（module="permission"）**

#### identity 模型（`backend/app/models/identity.py`）
- `IdentityPerson`(L51)：person_code/person_name_cn/dept_code/job_title/person_type/employment_status/source_system——**无任何画像字段**（无 avatar/bio/data_scope/tags/email/employee_no/review_status/created_at）

#### 前端
- `views/identity/persons/index.vue`：列表 + **弹窗式**画像（L74-111，只读），无独立画像页
- `views/identity/roles/`、`views/identity/authorizations/`：角色矩阵 + 授权管理（用 hasPerms 控按钮）
- `views/permission/`：**全是模板演示页**（button/page 演示 v-auth/v-perms），无业务页
- **按钮级权限三件套已完备**：`v-perms`指令(directives/perms)、`<Perms>`组件(components/RePerms)、`hasPerms`函数(utils/auth.ts:182)、`*:*:*`通配
- 路由 `router/modules/identity.ts`：已有 meta.roles + meta.auths 权限控制范例（L42-51,62-71）
- **无独立 permission 路由模块**

#### 后端权限校验现状（`backend/app/core/security.py`，仅 9 行）
```python
def get_current_user(request: Request) -> str:
    user_identifier = getattr(request.state, "user_identifier", None)
    if not user_identifier:
        raise HTTPException(status_code=403, detail="Token 未绑定可信操作人")
    return user_identifier
```
- **只有"取当前用户"，无"校验权限"**。中间件 `_enforce_rbac`(main.py) 是**粗粒度 URL 前缀+角色**拦截（如 /api/v1/identity 需 identity_admin），**无 resource:action 细粒度**

### 3.2 E9 实现方案

#### E9-1 数据模型与迁移
1. `IdentityPerson` 加字段：avatar(Text)、bio(Text)、data_scope(JSONB)、tags(JSONB)、email(Text)、employee_no(Text)、review_status(Text 默认 unreviewed)、created_at(TIMESTAMP)
2. **新建 `asset_permission_requests` 表**：id/applicant/target_user_identifier/role_code/reason/status(draft/pending/approved/rejected/revoked)/approver/approved_at/valid_from/valid_to/created_at/updated_at
   > **待复核决策 A**：新建专用表 vs 复用 `GovernChangeRequest`(module="permission")。我倾向新建专用表（字段更清晰，有 valid_from/to），但复用 GovernChangeRequest 可减少表数量。请审查。
3. `AssetUserRole` 加字段：source(Text 默认 direct)、request_id(BigInteger 关联申请单)、status(Text 默认 active)
4. `AssetRolePermission` 加唯一约束 `UniqueConstraint(role_code, resource, action)`
5. 手写迁移 upgrade/downgrade，down_revision 接 `t6a7b8c9d0e1`（当前 head）

#### E9-2 后端权限装饰器（`core/security.py` 扩展）
新增 `require_perm(resource: str, action: str | None = None)` 依赖：
```python
def require_perm(resource: str, action: str | None = None):
    def dep(request: Request, db: Session = Depends(get_db)):
        user = get_current_user(request)  # 已校验绑定
        roles = getattr(request.state, "roles", [])
        # 查 AssetRolePermission 校验
        ...
        if not has_perm: raise 403 + 审计
    return dep
```
给 E9/E10 写端点加 `Depends(require_perm("permission.request.approve"))`。
> **待复核决策 B**：装饰器用 FastAPI Depends 还是装饰器模式？Depends 更符合现有代码风格（identity.py 全用 get_current_user = Depends）。请确认。

#### E9-3 权限申请审批流 API（新文件 `api/v1/permission_requests.py`）
- POST /permission-requests（提交，applicant 从 token 派生）
- GET /permission-requests/mine（我的申请）
- GET /permission-requests/pending（待审批，需 platform_admin/identity_admin）
- PATCH /permission-requests/{id}/approve（审批，禁止自审批，写 AssetUserRole source=request）
- PATCH /permission-requests/{id}/reject（驳回带 reason）
- PATCH /permission-requests/{id}/revoke（撤销）
- 全部 GovernAuditLog(module="permission")

#### E9-4 人员画像增强（扩展 identity.py）
- PUT /identity/persons/{person_code}/profile（走 GovernChangeRequest 审批，不直接写）
- 扩展 GET /identity/persons/{person_code} 返回新画像字段

#### E9-5 前端人员画像页（新建 `views/identity/profile/index.vue`）
独立画像详情页 + 编辑入口（提交审批）+ 路由 /identity/persons/:code/profile

#### E9-6 前端权限申请页（新建 `views/permission/requests/`）
我的申请 + 待审批 + 新建路由模块 `router/modules/permission.ts` + API 文件

#### E9-7 RESOURCE_CATALOG 扩展
加权限码：identity.profile.edit / permission.request.create / approve / revoke

---

## 4. E10 调研结果与实现方案（核心 CRUD + 审核）

### 4.1 现状

#### 后端 recipe model（`backend/app/models/recipe.py`，27 行）
`AssetRelationRecipe` 字段：id/recipe_id(unique)/status(默认 candidate)/domain/source_system/recommended_view_name/description/business_domain/primary_tables(JSONB)/joins(JSONB)/ai_readable/imported_from/created_at/updated_at
- **缺口**：无 version、无状态机(draft/submitted/approved/active/deprecated)、无 is_active、无 recipe_json(完整结构)、无 created_by/updated_by/reviewer/reviewed_at、无 content_hash、无 recipe_name

#### 后端 recipes API（`backend/app/api/v1/recipes.py`，104 行）
仅 3 个**只读**端点：GET /recipes(列表14)、GET /recipes/{id}(详情63)、GET /recipes/ai/context(AI上下文84)
- **缺口**：无 POST/PUT/复制/版本/提交/审核/启用/废弃

#### 迁移（`alembic/versions/s5f6a7b8c9d0_add_relation_recipes.py`）
建 1 张表，字段同 model。无版本表、无审计子表。

#### 种子数据（`view_relation_recipes.json`）
仅 1 条配方，结构丰富（primary_tables 8 张/joins 6 条/outpatient_subquery.sql_fragment/field_logic/do_not_use_as_primary_join/validation_evidence/hard_rules）。
- **问题**：导入脚本 `import_relation_recipes.py` L28-50 只写 8 个字段，**丢弃了** field_logic/do_not_use_as_primary_join/validation_evidence/hard_rules/sql_fragment 等关键字段

#### 前端
- **配方页完全缺失**（`views/asset/` 下无 recipe 目录，grep 全无）
- 路由 `router/modules/asset.ts` 无配方项
- 图谱组件 RelationGraph.vue 无配方联动

#### 54 号方案设计要求
- 状态机：draft→submitted→approved→active→deprecated，驳回回 draft
- 已批准版本不可原地覆盖，编辑必须新版本
- 状态变更记录操作者/时间/原因/content_hash
- 视图 SQL：生成→格式化→风险校验→影响分析→预览→审批→下载（本轮不做）
- 硬边界：业务源库禁止 CREATE VIEW/DDL；默认只生成 SQL 不执行

### 4.2 E10 实现方案（核心 CRUD + 审核，图谱联动和 SQL 生成留下一轮）

#### E10-1 model 重构 + 迁移
扩展 `AssetRelationRecipe`：version(Integer 默认 1)/is_active(Boolean)/status(新状态机)/recipe_name/recipe_json(JSONB 完整结构)/evidence_summary/risk_summary/created_by/updated_by/reviewer/reviewed_at/review_reason/content_hash + UniqueConstraint(recipe_id, version)
迁移 down_revision 接 E9 迁移。

#### E10-2 schemas（新建 `schemas/recipe.py`）
RecipeCreate/RecipeUpdate/RecipeReview/RecipeListItem/RecipeDetail

#### E10-3 写 API（扩展 recipes.py）
- POST /recipes（新增 version=1 status=draft）
- PUT /recipes/{id}（仅 draft 可改，重算 content_hash）
- POST /recipes/{id}/copy（复制新 recipe_id）
- GET /recipes/{id}/versions（版本列表）
- POST /recipes/{id}/submit（draft→submitted）
- POST /recipes/{id}/approve（submitted→approved，禁止自审批）
- POST /recipes/{id}/reject（submitted→draft）
- POST /recipes/{id}/activate（approved→active，旧 active 自动 deprecated）
- POST /recipes/{id}/deprecate（→deprecated）
- 写端点加 require_perm("asset:relation_recipe:admin")
- 全部审计 + content_hash

#### E10-4 导入脚本重写
改为写 recipe_json 整体结构，不再丢字段

#### E10-5 前端配方页（新建 `views/asset/relation-recipes/`）
index.vue 列表（筛选+操作列）+ 详情抽屉 + 路由 + API 文件

---

## 5. 待复核决策点（请接手 AI 重点审查）

| # | 决策点 | 我的倾向 | 风险 |
|---|---|---|---|
| A | 权限申请单：新建表 vs 复用 GovernChangeRequest | 新建专用表（字段清晰） | 复用可减少表，但 valid_from/to 字段要塞进 request_payload JSONB |
| B | 权限装饰器：Depends vs 装饰器 | Depends（符合现有风格） | 装饰器更直观但与 FastAPI 惯例不同 |
| C | recipe model：原地扩展 vs 新建版本子表 | 原地加 version 字段 + UniqueConstraint(recipe_id,version) | 旧数据 version 需回填 1，status 需迁移映射 |
| D | recipe 路由命名：保持 /recipes vs 改 /relation-recipes（54号要求） | 保持 /recipes（避免破坏现有 main.py 注册） | 与 54 号方案命名不一致 |
| E | 人员画像编辑：直接写 vs 走审批 | 走 GovernChangeRequest 审批（与现有 identity 写一致） | 审批流增加复杂度 |
| F | RESOURCE_CATALOG：保持硬编码 vs 改 DB 表 | 保持硬编码（本轮） | 新增权限码需改代码重新部署 |

## 6. 我没做、需要你确认的

1. **测试**：后端 pytest 需测试库（本机连不上），我会写测试代码但跑不了——你在内网验证。
2. **迁移验证**：alembic upgrade/downgrade 需平台库（本机连不上），我手写但无法本机验证往返。
3. **前端 typecheck/build**：本机可跑，我会保证通过。
4. **图谱联动、视图 SQL 生成**：明确留下一轮，本轮不做。

## 7. 请接手 AI 复核后回答

1. §3.2 E9 方案是否合理？有无遗漏或与现有代码冲突？
2. §4.2 E10 方案是否合理？状态机和版本设计有无问题？
3. §5 的 6 个决策点，你的建议是什么？
4. 有没有我漏掉的安全风险（尤其 E9 权限装饰器和 E10 审核流）？
5. 实现顺序（E9-1→E9-2→...→E10-5）是否合理，有无依赖倒置？

---

*本文件由会话 AI 于 2026-07-14 编写，未经执行，仅供复核。如确认方案可行，接手 AI 可按 §3.2/§4.2 实施。*
