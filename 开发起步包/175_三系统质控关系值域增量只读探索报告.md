> 类别：证据

# 三系统质控、关系与值域增量只读探索报告

> 日期：2026-09-02  
> 范围：LIS、超声内镜、Docare；承接 174 W3  
> 边界：业务源库仅经 `sjzc live` 执行数据字典查询和 2026 年窗口聚合；无患者明细、无 DML/DDL、业务源库写入 0

## 1. 质控结论

| 规则 | 2026 年窗口结果 | 判断 |
|---|---:|---|
| LIS 住院检验报告关键键完整性 | 392,287 条；`INP_NO/TEST_NO/REPORT_NO` 缺失均 0；`VISIT_ID` 缺失 11（0.0028%） | 发现低比例真实问题；可建立“LIS 住院报告 VISIT_ID 缺失”质控，但跨 HIS 覆盖率仍须分源取键后在受控执行器内比对 |
| 超声住院次数一致性 | 50,947 条；报告侧 `VisitID` 空/0 为 39；预约侧住院次数缺失 37；预约关联孤儿 37；两侧均有值时不一致 0 | 问题集中在预约关联/住院次数缺失，不是两侧有效值冲突 |
| Docare 急诊未排班默认住院次数 | 急诊 968 台；按完整手术键及 `STATE IN (2,3)` 关联后无有效排班 755 台，其中 `VISIT_ID=1` 为 750，非 1 为 5 | “默认 1”覆盖 99.34%，但存在 5 个例外，只能作为带容差/例外清单的质控，不能写成绝对约束 |

所有聚合均限定 `2026-01-01 <= 时间 < 2027-01-01`。本报告只保存计数，不保存患者或就诊键。

## 2. 关系复核

| 系统 | 关系 | 本轮证据 | 治理动作 |
|---|---|---|---|
| 超声 | `AnyImage.grid.BHosCheckUS.BookID = MedcareUS.dbo.预约登记.编号` | 2026 年住院窗口 50,947 条中 37 个关联孤儿；既有 82 号 U01 使用 `AccessNo` 的 10,000 样本 100% 命中 | `BookID=编号` 来自现行视图 SQL，可作为 `partial/candidate` 补充证据；不得替换 U01，也不得自动升正式关系 |
| Docare | `MED_OPERATION_SCHEDULE → MED_OPERATION_MASTER`：`PATIENT_ID+VISIT_ID+OPER_ID`，且排班 `STATE IN (2,3)` | 本轮用于急诊规则聚合；沿用 13/80 号已验证限定关系 | 仅补充规则适用范围，不新增重复正式边 |
| LIS | `CDR_INSPECTION_REPORT` 暴露 `INP_NO+VISIT_ID`；报告主线仍以 `reportid`/`report_id` 和既有 81 号关系为准 | 字段存在及键完整性已活库确认 | LIS→HIS 属跨物理源 D 类；本轮不写正式关系，后续由受控双源内存比对生成草稿证据 |

## 3. 值域候选

以下仅是 2026 年窗口的观测证据，提交平台时必须为 `pending`，不得由 AI 自动确认：

| 系统.对象.字段 | 观测值 | 证据 |
|---|---|---|
| `LIS.dbo.CDR_INSPECTION_REPORT.VISIT_TYPE_CODE/NAME` | `01=门诊`、`02=住院`、另有 NULL | 住院 392,287；门诊 137,974；NULL 7,577 |
| `LIS.dbo.CDR_INSPECTION_REPORT.REPORT_STATUS` | `完成` | 本窗口三个就诊类别组合均为完成 |
| `ULTRASOUND_ENDOSCOPY.AnyImage.grid.BHosCheckUS.PatSource` | 门诊、住院、体检、NULL | 窗口聚合实测 |
| `ULTRASOUND_ENDOSCOPY.AnyImage.grid.BHosCheckUS.Printed` | `T`、`F` | 窗口聚合实测；具体业务语义仍待厂商/业务确认 |
| `DOCARE.MEDSURGERY.MED_OPERATION_MASTER.EMERGENCY_INDICATOR` | `0`、`1`、NULL | 窗口聚合实测；本轮不把 0/1 含义自动确认为正式值域 |
| `DOCARE.MEDSURGERY.MED_OPERATION_MASTER.OPER_STATUS` | `-80,0,15,35,40,55,60,61` | 窗口聚合实测；35 完成、-80 取消仅可引用既有 ODS/149 证据，其余标注【值域待确认】 |

## 4. 系统记录边界

- 174 生产五表迁移尚未发布，因此本轮不能安全地把三项观测写入生产质量台账；禁止绕过迁移直接写库。
- 新关系均为既有关系的限定/补证或跨系统 D 类候选，按 40 号口径不直接改正式关系；候选详见同号结果 JSON。
- 值域候选只允许进入 pending；在生产导入前需走现有 value-domain 幂等导入/审核链。
- 这三个等待项从“表结构与 SQL 未核验”更新为“只读证据已取得；生产台账迁移/受控执行器与人工值域确认待办”。

## 5. 自检

- 源库 SQL：仅 SELECT/聚合。
- 时间范围：2026 年左闭右开。
- 大表：按时间窗口限制；未查询 LIS `lab_result`。
- 敏感信息：未输出姓名、证件、电话、地址、患者或就诊明细。
- 业务源库写入：0。
- 正式关系自动发布：0。
- 值域自动确认：0。

## 6. 生产同步记录（2026-09-03）

用户明确授权同步到服务器后，已按 174 runbook 完成：

- 生产库备份：`/opt/data-asset/backups/data_asset_pre_r175_20260903.dump`（约 11 MB，`pg_restore -l` 可读 1006 项）；
- Alembic：`c1d2e3f4a5b6 → d5e6f7a8b9c0`；
- 174 种子终态：17 Control、17 Detector；5 条会议问题、T7 monitoring gap、既有 6 条 open ProbeFinding 和最新 5 条 PASS 观测完成适配；二次 apply 零新增；
- 175 平台摄取：18 条值域候选全部 `pending`，18 条 live_probe 证据，1 条超声关系复核 `draft`，3 条聚合观测进入质量台账；二次 apply 为 18/18/1 均零新增、三观测均 duplicate；
- 三条问题单：`DQI-202609-000008`（LIS，0.002804%）、`DQI-202609-000009`（超声，0.076550%）、`DQI-202609-000010`（Docare，0.662252%），状态均为 `new`，等待人工确认/分派；
- 发布：后端镜像 `data-asset:r175-20260903`，前端 `r175-20260903` 原子切换；上一前端版本 `r169-20260831-storage-fix` 保留为 previous；
- 回读：容器 healthy、数据库 connected、三新 API 未鉴权 401、前端首页 200、Alembic 单头正确。

同步工具为 `backend/scripts/import_plan175_governance.py`：默认 dry-run，显式 `--apply` 才写平台；不连接业务源库，不发布正式关系，不确认值域。
