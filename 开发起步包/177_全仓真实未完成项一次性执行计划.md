> 类别：待办
>
> 状态：**v1.0 定稿待执行（2026-09-03 盘点）**
>
> 上位入口：`55_系统未完成事项统一执行计划.md`（本文件不取代 55）
>
> 制定日期：2026-09-03
>
> 编号说明：本号 **177 是文档编号**。156 报告里的「177 Vastbase 间歇连接黑洞」是历史故障代号，**不是**本文件。禁止混用。

# 177 · 全仓真实未完成项一次性执行计划

本文件给**另一名执行 AI**按批次落地。目标：把开发起步包里**现在仍然开放**的事项收成一份可执行清单，并给出文件范围、禁令、验收命令和等待域。

**一句话结论**：探库主线、关系底座、149 值域导入、165/166/167/169/171/174/175 生产发布大多已经完成。剩下的不是“再写一份总计划”，而是三类工作：①可在隔离库修的代码缺陷；②工作区已有、尚未提交/持久化的身份与质量代码；③必须用户或 DBA 拍板的等待域。

---

## 0. 执行角色、边界与禁止重做

### 0.1 角色

| 角色 | 做什么 |
|---|---|
| 执行 AI（本计划对象） | 按 R0→R7 推进**本计划标明可执行**的批次；每批留 checkpoint；不确定即 STOP 并写入 exceptions |
| 主 AI / 用户 | 生产发布、Git push、业务库写入、值域 confirm、CDMS 写字典、探查发现终态裁决 |
| 信息科 / DBA | 160 职称 SQL 包；凭据轮换 |

### 0.2 硬边界（违反即回滚并停止）

1. HIS / ODS / HRP / JHEMR / CDMS / LIS / PACS 等**业务源库只允许 SELECT**。禁止 DML/DDL/EXECUTE。CDMS `KESHID` 写入必须有用户明示授权（R5）。
2. 密码、Token、身份证、姓名、电话、地址不进代码、日志、git、新文档。
3. 新表只在 `asset` schema，前缀 `asset_<模块>_<实体>`。迁移手写 `upgrade()`/`downgrade()`，禁 `--autogenerate`。
4. 前端必须 `pnpm`。不改现有路径 `/relations/{id}/review`、`/dict-medical/*`。
5. **零 Git commit / push / tag**，除非用户在本会话明确授权。即使授权，也必须按 §3 分域提交，禁止一把梭。
6. **零生产发布、零改 cron、零改熔断 env**，除非用户明示。176 F-1 的 `APP_IDENTITY_CB_MAX_UPDATE=150` 已在产，本计划不得回退。
7. 值域永不自动 confirm。关系草稿不得由原执行者自批升正式。
8. 他人域零触碰：见 §3。禁止 `git checkout` / `git stash` 清掉别人的未提交改动。
9. 不为过测试弱化断言。失败如实记 WARN/BLOCKED，不改口径装绿。

### 0.3 禁止重做（已完成，不得当待办再跑）

下列事项在 2026-09-03 已有证据，执行 AI **不得**再全量探库、再导入、再发布：

| 事项 | 证据 | 终态 |
|---|---|---|
| Track A 探库 / 关系底座 | README 完成度总览；`08`/`09`/`40` | 已完成 |
| 139 四源 + 143 OA 导入 | `142`/`143` | 已完成；草稿待人工复核，不自批 |
| 149 值域种子生产导入 | 55 ✅ 2026-08-31；77 条 | 已完成 |
| 151 字典候选导入 | 164 R2；随 149 链 | 已完成（confirm 仍人工） |
| ECG 元数据生产导入 | 55 ✅ 2026-08-31；65 对象/1119 字段 | 已完成；2 条 D 级因目标不存在保持 blocked |
| 146 界面 28/28 | 164 R5 | 已完成 |
| 163 R0–R7 / 164 R0–R7 | `164_..._执行报告.md` | 代码/测试完成；R8 GUI 用户已撤销 |
| 165 E1–E6 + X1 建表 + X2 生产首轮探查 | 171 收尾批；run `probe-20260901-191051` | X1/X2 完成；剩 X3/X4 |
| 166 D1–D6 生产发布 | r166-20260830 | 已完成；P2 后置 |
| 167 巡查演示 + AI SQL | r167-20260830 | 已完成；真调度后置 |
| 169 图谱叠加/墨团修复 | r169 + r171 前端 | 已完成 |
| 171 T0→R4 + push + T5 实拍 | `171_..._执行报告.md` | 已完成 |
| 173 全栈模拟测试（只测不修） | `173_..._问题清单.md` | **测试已完成**；修复未做 |
| 174 质量台账开发 + **生产发布** | r175-20260903；迁移 `d5e6f7a8b9c0` | W1/W2 完成 |
| 175 三系统只读取证 + 生产 pending 值域/问题单 | 000008–000010；二次 apply 零新增 | 取证完成；confirm 仍人工 |
| 176 F-1 熔断阈值 | env `APP_IDENTITY_CB_MAX_UPDATE=150`；容器 b866ffee | 已执行；今夜验证未做 |
| 172 下乡 27 人权限 + FFREE3 46 户数据修复 | ALL_27_OK；adapter 热修 | 数据已修；**代码未入库/镜像未持久化** |

### 0.4 目录自检发现（本会话已核对）

| 类型 | 项 | 处理 |
|---|---|---|
| 幽灵条目 | README 仍列 `150_*.md` / `150_fill_form.py`，工作区 **git 状态为 D（已删）** | 本计划 W6；README 150 行改为“工作区已删，截止 2026-09-04” |
| 未跟踪但已登记 | 173–176 正文与结果 JSON 在 git 为 `??` | 随 Git 授权分域提交，不在本计划默认执行 |
| 过时文档 | `07_业务系统与探查开放问题.md` 仍写输血/无纸化为“待探” | 事实已过时（79/158/159 已探）；**不在本轮改 07**，除非用户要求文档卫生 |
| 撞号提示 | 156「177 Vastbase」≠ 本文件 177 | 全文只用“Vastbase 连接重试”称呼该故障 |

---

## 1. 真实未完成项总表（2026-09-03）

优先级定义：

- **P0-今晚**：只读观察或外部截止，错过会丢数据/错过窗口。
- **P0-代码**：隔离库可修，不碰生产。
- **P0-授权**：代码或数据已就绪，缺用户一句话。
- **P1**：有价值，不阻塞主线。
- **P2 / 后置**：明确另立项或人工池。

### 1.1 P0-今晚 / 外部截止

| ID | 事项 | 现状 | 执行 AI 动作 | 完成判定 |
|---|---|---|---|---|
| N1 | 176 今夜 02:00 主同步是否还熔断 | F-1 已把 `max_update` 调到 150；托管圈约 110 | **只读**查 `asset_identity_scheduler_runs` 最新 02:00 行：`status`、`circuit_breaker_triggered`、`candidates_total`、签名/职称子任务是否不再 `main_account_sync_not_successful` | success 且 update≤150；失败则 STOP 呈报，**不得自行再调阈值** |
| N2 | 150 医保课题截止 **2026-09-04** | README 仍列申报草稿；工作区正文与 `150_fill_form.py` 已删除 | 向用户确认：还做 / 放弃 / 从备份恢复。AI **不得虚构申报内容** | 用户书面裁决写入 55 |

### 1.2 P0-代码（默认可执行，隔离库）

| ID | 事项 | 位置 | 建议修法 | 验收 |
|---|---|---|---|---|
| C1 | 173 P1-1：`import170.py` 重灌不重置序列 | `开发起步包/output_r170/import170.py` | 重灌末尾对 `asset` schema 全部序列 `setval(seq, max(id), true)`。SQL 模板见 `173_全栈模拟测试_结果.json` 的 `env_repair` | 重灌后 `POST /api/v1/queries/ingest` 不再 500；`max(id)` 与 `last_value` 对账 |
| C2 | 173 P2-1：新建系统列表不可见 | `backend/app/services/asset_catalog.py` 约 205–216 行 `list_first_level_systems` 只扫 `CANONICAL_SYSTEMS` | **默认方案**：列表增补“库内存在但不在 CANONICAL 的系统”分区返回，预置系统仍排前面。禁止改成静默 403 除非用户确认“只允许 26 个预置系统” | 173 成功路径 `c_systems_upsert`：PUT 非预置编码 → GET 列表可见 |
| C3 | 173 P2-2：6 个写按钮漏 `v-perms` | `frontend/src/views/asset/probe-findings/index.vue` 约 238；`value-domains/index.vue` 约 269；`views/dict/general/index.vue` 约 191/218/247/288 | 同页已有写按钮怎么挂就怎么挂：`probe.finding.transition` / `value_domain.confirm` / `dict.general.manage` | 无权用户按钮不可点；有权路径不变 |
| C4 | 171 P2：图谱边标签 `[object Object]` | `frontend/src/views/asset/graph/RelationGraph.vue` 约 274、432 | G6 样式对象不要直接 `String()`；取 `label`/`name` 文本字段，缺省空串 | overview 聚合边可见中文/编码，不再出现 `[object Object]` |
| C5 | 173 P3-5：配方 create 接受非法 `primary_tables` | `backend/app/api/v1/recipes.py` | create 时校验表标识，非法键直接 422，不要拖到生成 SQL 才 400 | 非法 body 422；合法 create 仍 200 |
| C6 | 173 P3-3：`GraphToolbar.vue` 直接改 props | `frontend/src/views/asset/components/GraphToolbar.vue` 21–59 | 改为 emit 更新，禁止 mutate locate/filters props | eslint `vue/no-mutating-props` 该文件归零；图谱筛选仍可用 |

C1–C6 **默认同批做**。C6 若与 layout 他人域冲突则 WARN 跳过，不阻塞 C1–C5。

### 1.3 P0-代码（工作区已有，先复核再门禁，默认不发布）

这些改动 **已经在工作区**，不是从零开发。执行 AI 先 `git diff` 看懂，再补测，**不要重写一遍**。

| ID | 事项 | 工作区文件（禁碰范围外文件） | 风险 | 本计划默认 |
|---|---|---|---|---|
| I1 | CDMS 建户补 `FFREE3` | `backend/app/services/cdms_identity_adapter.py` | 生产容器已热修，镜像重建会丢 | 复核 + identity 测试；提交/镜像重建 = 等待域 G1 |
| I2 | JHEMR 登录方式/签名方式夜间子任务 | `backend/app/services/identity_login_sign_sync.py`（新）；`jhemr_identity_adapter.py`；`run_identity_modified_nightly.py`；`identity_sync_*.py`；`tests/identity_login_sign_sync/`；`tests/test_identity_sync*.py` | 会在夜窗写 JHEMR 登录/签名方式表（additive）。未授权不得开生产子任务 | 隔离库/单测跑绿；**生产接入 cron = 等待域 G2** |
| I3 | 176 F-2：例行 resync 不计入 `max_update` | `identity_sync_orchestrator.py` 的分类与 `check_thresholds`（以 176 报告 §4 为准） | 改熔断语义。F-1 阈值 150 先留着 | 代码+测试；生产部署 = 等待域 G3。F-3 子任务降级本轮可选，默认不做 |
| I4 | `APP_IDENTITY_CB_MAX_NEW=130` 临时值回落 50 | 生产 `backend.env` | 与 F-2 绑定。单独回落可能再次熔断 | **禁止本轮改 env**。等 G3 发布 F-2 后由用户授权回落 |

### 1.4 P0-授权 / 人工（执行 AI 只准备材料，不代做）

| ID | 事项 | 谁拍板 | 执行 AI 可做的准备 |
|---|---|---|---|
| A1 | 165 X3：生产 6 条 open 探查发现终态 | 用户在 `/probe-findings` | 只读列出 6 条（T1 工号缺失 81.40% 等），禁止 AI 点确认/误报/关闭 |
| A2 | 165 X4：探查调度周期 | 用户 | 呈报选项：关 / 每周 / 每月；不改 cron |
| A3 | 165 T7 PACSREPORT side-b | 用户授权补键后才能纳入 | 只读确认仍 BLOCKED；不猜申请键 |
| A4 | CDMS 004066「赋权仍失败」 | 用户先选表现 A/B/C（见 `174_CDMS无纸化赋权未成功_交付说明.md` §1） | 只读待命；**未选表现禁止写 KESHID / 改密码** |
| A5 | 赵慧工号 001172 vs 000110 | 用户/人事 | 不补权限 |
| A6 | 160 职称 125 人自动更新 + 44 人人工 | 信息科/DBA | 只读提醒包在 `backend/_oa_work/title2026/`（含身份证，禁止外传/入 git） |
| A7 | 144 生产黄金用例种子 + `FF_G5_EVAL_GATE` | 用户授权发布窗口 | 不跑生产 seed |
| A8 | 127：生产 Review 批准、配方 active、历史质量问题重建 | 用户逐项 | 不自批 |
| A9 | 凭据轮换：门诊 SSH、DRG hisserver、OA admin、ECG sa | 运维窗口 | 不碰凭据文件内容 |
| A10 | Git 分域提交 + 174/175/176 文档入库 | 用户 | 准备分域文件清单（§3），不 commit |
| A11 | 173 U1/U2/U3 设计裁决 | 主 AI | 呈报三选项，不改 CSRF/公开面除非裁决 |

### 1.5 P1 / 后置（本轮默认不做，列入报告附录）

| ID | 事项 | 说明 |
|---|---|---|
| P1-1 | 166 P2：对账矩阵看板、趋势曲线、QualityFinding 互链、AI 探查触发入口、值域批量导入 | 依赖 X4；另批 |
| P1-2 | 167 真定期巡查 / 更强内网模型 | 用户已裁“计划卡纯演示” |
| P1-3 | 154 B2 HIS 源端摄取、B3 CDMS SQL 分册 | 需对齐 152 草稿复核 |
| P1-4 | 159 三源仍 BLOCKED：护理 numas、微度 LIS、门诊预约 | 凭据失败，不重探除非新账号 |
| P1-5 | 138 后置：字段级/运行血缘、真实消费、质量闭环通知 | 用户已批后置 |
| P1-6 | 112 Phase D / 96 业务库写入 | 写侧保持关闭 |
| P1-7 | 157 ZFP 令牌 URL 服务端代理 | 上线前必须做；本轮不编码 |
| P1-8 | 168 P1：Minimap、视口历史、LOD 等 | 非 P0；169 已补五类截图，168「待终验」大部分已被 169/171 覆盖，**不要再按 168 §4 重做五类截图** |
| P1-9 | 人工复核池：约 98 条 relation review draft + 40 停用配方 + 曼陀罗 16 + 新输血 37 C 级 + 175 超声 1 条 | 独立复核流程 |
| P1-10 | 175 的 18 条 pending 值域人工确认 | 禁止自动 confirm |
| P1-11 | 174 W4：run 端点只校验+审计，不伪造执行 | 保持；不要实现假跑 |
| P1-12 | 130 A28 Dify 真实 E2E | 缺 Key/Workflow |
| P1-13 | 173 P3-1 死 API、P3-4 全仓 prettier、P3-6 空库需预建 schema、P3-7 平行类型、P3-8 as any | 低优先级卫生 |
| P1-14 | `legacy_unmanaged` 3756 人扩面 | 另立项 |
| P1-15 | DRG `10.10.10.14:1521/hisserver` 第二实例探索 | 未授权 |
| P1-16 | 44 开源工具 SQLGlot/Soda 等 | 非当前阻塞（仓库已有 sqlglot 离线层，不要重复选型） |

---

## 2. 工作区分域（执行前必读，防踩踏）

盘点时 `git status` 混了多域。**一次只动一个域。**

| 域 | 文件（示例） | 本计划 |
|---|---|---|
| 身份 | `cdms_identity_adapter.py`、`jhemr_identity_adapter.py`、`identity_login_sign_sync.py`、`identity_sync_*.py`、`run_identity_modified_nightly.py`、`permissions.py`（仅身份相关 hunk）、identity 测试、`backend/_r172_work/` | R3 复核；禁扩大到无关权限码 |
| 质量 174 | `quality_controls.py` / `quality_issues.py` / `quality_observations.py`、`quality_governance_*.py`、`models/quality_governance.py`、迁移 `d5e6f7a8b9c0_*`、`frontend/src/views/quality/`、`frontend/src/api/quality.ts`、`frontend/src/router/modules/quality.ts`、`plan174QualityLedger.test.ts`、`import_plan175_governance.py` | **生产已发布**。本轮不改功能，除非回归失败。文档/未跟踪源码等 Git 授权再提交 |
| 图谱/系统/173 修复 | `asset_catalog.py`、`RelationGraph.vue`、`GraphToolbar.vue`、probe-findings / value-domains / dict/general 的 v-perms、`output_r170/import170.py`、`recipes.py` | **R2 主战场** |
| 前端 layout / 竞赛截屏 | `layout/index.vue`、`lay-content`、`lay-setting`、`useDataThemeChange.ts`、`useTag.ts`、`store/modules/app.ts`、`captureMode.ts`、`competitionCaptureMode.test.ts`、`tools/capture_*.py` | **他人域，整域禁碰** |
| 文档 | 173–176、55、README、本文件 | 只允许追加 177 登记和本批执行日志 |

`frontend/src/router/modules/asset.ts`、`frontend/src/views/asset/probe-findings/index.vue` 可能同时有 173 修复与他人 hunk。改前先 `git diff` 该文件，**只补 v-perms，不重排、不改无关逻辑**。

---

## 3. 批次执行卡

总序：`R0 → R1 → R2 → R3 → R4 → R5(等待) → R6 → R7`。  
R1 若尚未到次日 02:00 之后，先做 R2/R3，R1 留到能读到夜跑结果再补。

每批必须产出 `开发起步包/output_r177/progress.md` 一节：完成项 / 命令输出摘要 / 偏离。失败写入 `output_r177/exceptions.json`。

### R0 开工自检（只读，必做）

**读（按序）**：`AGENTS.md` → `开发起步包/README.md` → `55` 顶部 📌 → **本文件全文** → 将要改的源文件。

**命令**：

```powershell
cd F:\python\数据资产
git status --short
git diff --stat
git log -5 --oneline
```

**核对**：

1. 工作区仍混多域 → 记录当前 SHA 与脏文件清单到 `output_r177/workspace_snapshot.txt`。
2. 隔离库：只使用 8.83 `data_asset_test` 隧道。本机 Windows 不直连业务库。`backend/.env` 可能指向 `10.20.1.153` 旧库，**脚本一律显式 URL**，禁止依赖默认 .env。
3. 隧道占用：164 曾发现 15432 被外部转发占用，改用 15433。若端口冲突，自建新本地端口，不要杀别人的转发。
4. 终止非本会话 pytest，避免互踩测试库。

**验收**：快照文件存在；未 checkout/stash；未连业务源。

**STOP**：找不到公钥 SSH `root@10.10.8.83`、隔离库连不上、工作区被人中途大幅改写且无法分域 → 停止。

---

### R1 176 夜跑只读核验

**时机**：2026-09-04 02:00 之后（或用户确认夜跑已结束）。

**通道**：`ssh -o BatchMode=yes -i C:\Users\Administrator\.ssh\id_ed25519_ai root@10.10.8.83`，再查平台库。凭据从服务器 `/etc/data-asset/backend.env` 推导，**口令不落盘、不进报告**。

**查什么（脱敏）**：

1. 容器 `data-asset-api` healthy；settings `identity_cb_max_update=150`。
2. 最新 nightly run：`status`、`circuit_breaker_triggered`、`candidates_total`、new/update/align/deactivate 计数。
3. 签名/职称/科室子任务是否仍 skipped。
4. 抽样：空签名账号是否开始被补（只记工号后四位掩码 + 成功/跳过原因）。

**禁止**：手动触发全量同步；改 env；补跑全院签名。

**验收**：

- PASS：主任务 success，未触发 max_update，子任务未因主失败被一刀切。
- FAIL：仍熔断 → 把 run_id、维度、计数写入报告，等用户，不自行调到 200。

---

### R2 173/171 缺陷修复（默认同批）

**文件白名单**（只许这些 + 对应测试）：

- `开发起步包/output_r170/import170.py`
- `backend/app/services/asset_catalog.py`
- `backend/app/api/v1/recipes.py`（仅 create 校验）
- `frontend/src/views/asset/probe-findings/index.vue`（仅 v-perms）
- `frontend/src/views/asset/value-domains/index.vue`（仅 v-perms）
- `frontend/src/views/dict/general/index.vue`（仅 v-perms）
- `frontend/src/views/asset/graph/RelationGraph.vue`（仅边标签两处）
- `frontend/src/views/asset/components/GraphToolbar.vue`（可选；冲突则跳过）
- 新增/修改的针对性测试（`backend/tests/` 或 `frontend/tests/` 新文件优先，少改旧文件）

**C2 产品裁决（已写入本计划，无需再问）**：列表必须能看到已入库的非 CANONICAL 系统。预置 26 个仍排前面。不要把 PUT 改成拒绝，除非后续用户推翻。

**C4 修法**：

```text
错误：String(edge.style) 或把 G6 样式对象当 label
正确：label = edge.label ?? edge.data?.label ?? edge.name ?? ""
     保证渲染文本是 string
```

**测试**：

```powershell
cd F:\python\数据资产\backend
.\.venv\Scripts\python.exe -m pytest tests/ -q --tb=line -x
# 若全量太长，R2 结束前至少：
# 1) 与 catalog/recipes 相关的专项
# 2) 全量不得新增 failed（相对开工基线）

cd F:\python\数据资产\frontend
pnpm run typecheck
pnpm test
```

**173 回归工装（有则跑，无则记 WARN）**：

- `backend/_r173_work/success_paths.py` 里 `c_systems_upsert`
- 重灌后随便打一个写审计端点不再 500

**验收**：C1–C5 全绿；C6 绿或 WARN 跳过；typecheck 双过；前端测试 0 failed。

**禁止**：改 layout、改质量台账、改身份适配器、跑生产、confirm 值域。

---

### R3 身份线复核（已有 diff，禁止重写）

**目标**：看懂并验证 I1/I2/I3，不是再发明一套同步。

**必读**：

- `176_夜间身份同步连续熔断根因排查与修复报告.md` §4
- `172_CDMS无纸化用户权限模型与下乡名单核对.md` §0 / §6
- `identity_login_sign_sync.py` 模块注释（additive：只补缺 `users_control_mode`/`sublogin`/`subsign`，不删已有方式）

**F-2 语义（必须按此，禁止把托管圈例行 resync 继续算 update）**：

1. 真正的字段变更 / 停用 / 真实新建号：计入既有熔断桶。
2. 托管在册、无实质变更的例行 resync：单独计数或不计入 `max_update`（比照 W10 的 `align_existing`）。
3. 存在性检查失败必须 fail-closed，回退保守阈值。
4. 新增测试：托管圈 110 人纯 resync **不得** `threshold_exceeded`；110 人真实 update 仍要熔断。

**测试**：

```powershell
cd F:\python\数据资产\backend
.\.venv\Scripts\python.exe -m pytest tests/test_identity_sync.py tests/test_identity_sync_122_unit.py tests/identity_login_sign_sync -q --tb=line
```

目录若因权限读不到，用 pytest 路径收集确认。身份专项必须 0 failed。

**禁止**：

- 改生产 env / 重建容器
- 把登录签名子任务接到 8.83 cron（G2）
- 写 CDMS / JHEMR
- 扩大 `legacy_unmanaged` 纳管面

**验收**：I1 模板含 FFREE3；I2 单测覆盖“缺则补、有则不动”；I3 测试锁定例行 resync 不熔断。报告写明「代码完成，生产未发布」。

---

### R4 门禁与文档（本计划收口）

在 R2+R3 之后：

```powershell
cd F:\python\数据资产\backend
.\.venv\Scripts\python.exe -m pytest tests/ -q
.\.venv\Scripts\python.exe -m alembic current
# 不在生产 upgrade；隔离库若 head 已是 d5e6f7a8b9c0 只记录

cd F:\python\数据资产\frontend
pnpm run typecheck
pnpm test
pnpm run build
```

**文档**（允许改）：

- 本文件执行日志 §8
- `55` 追加一行 📌 本批结果
- `README.md` 目录更新记录一行
- `开发起步包/177_全仓真实未完成项_执行报告.md` + `177_全仓真实未完成项_结果.json`（新建，同号）

**Git**：默认不做。若用户授权，按下面分组，一组一提交，`git show` 零越界：

1. `fix(173): import170 sequences + systems list + v-perms + recipe validate`
2. `fix(171): graph edge label object object`
3. `fix(identity): cdms FFREE3 + login/sign subtask + max_update semantics`（需用户点名）
4. `docs(174-177): reports and plans`（仅开发起步包文档）
5. 质量 174 源码（生产已有镜像，补仓库）单独一组，需用户点名

**禁止**把 layout/captureMode 塞进上述任何一组。

---

### R5 CDMS 赋权（默认 STOP，等用户）

用户还没在本会话选择 004066 的「不行」表现。执行 AI **开工时问一次**，未答则整批跳过。

| 用户选择 | 下一步（仍须二次授权才写库） |
|---|---|
| A 登录失败 | 只读核默认密码模板与 `LOGINERRORCOUNT`；不猜密文；呈报是否需要用户在 CDMS 客户端试登录截图 |
| B 能登但功能空白 | 只读核角色 GUID 是否在应用层被解析；不要改 AUTHMAPPING |
| C 管理界面显示未赋权 | 只读追界面数据源；H1 科室码：`040465` 不在 `KESHID`，HIS 名为创伤骨科手足一组。写 `KESHID` 必须单独授权，SQL 必须幂等、只插缺失码、带审计 |

**禁止**：未授权写 `KESHID`、改 `FPWD`、给赵慧补权限、扩大 27 人以外的 align。

工装索引：`开发起步包/output_r171/r172_*.py`（只读复用，不改生产脚本语义）。

---

### R6 等待域呈报包（只读整理，交给用户）

产出 `output_r177/wait_domain.md`，把 §1.4 的 A1–A11 做成勾选表，附：

1. 165 生产 6 条 open finding 的模板号、指标、窗口（无患者明细）。
2. 160：125 自动 / 44 人工；提醒 DBA 顺序 `00_backup → 01_update → 02_verify`。
3. 150：文件已删 + 截止日。
4. 复核池规模：不要重新 COUNT 全库除非只读平台 API 方便；引用 55 已有数字（正式关系 1329、复核草稿约 98）。
5. 175 的 18 条 pending 值域字段名单（来自 175 报告 §3），供人工 confirm。

---

### R7 总收口

- [ ] `output_r177/progress.md` 覆盖 R0–R6，无空白批（跳过的批写 SKIP+原因）
- [ ] 后端全量 0 **新增** failed；身份专项 0 failed
- [ ] 前端 typecheck + tests + build 过
- [ ] 执行报告 + `_结果.json`
- [ ] README / 55 已登记
- [ ] 零越权声明：未发布、未 push、未写业务源、未 confirm 值域、未点探查终态
- [ ] 他人域 layout/captureMode 哈希或 diff stat 与 R0 快照一致

---

## 4. 等待域（呈用户，本计划不执行）

| # | 事项 | 前置 |
|---|---|---|
| G1 | 提交并重建镜像，持久化 CDMS FFREE3 模板 | 主 AI 复核 `cdms_identity_adapter.py` |
| G2 | 生产接入 `jhemr_login_sign_sync` 夜窗子任务 | 用户授权；JHEMR 写侧 additive 已理解 |
| G3 | 发布 F-2 熔断语义，并授权把 `MAX_NEW` 从 130 回落 50 | F-2 测试绿；今夜 N1 已观察 |
| G4 | 165 X3 六条发现人工裁决 | 用户打开 `/probe-findings` |
| G5 | 165 X4 调度 | 用户定周期 |
| G6 | T7 PACS side-b | 业务提供申请键或授权改模板 |
| G7 | CDMS 写 `KESHID` 或核密码 | 用户选 A/B/C 后再授权 |
| G8 | 160 职称包 DBA 执行 | 信息科窗口；含身份证 |
| G9 | 150 课题 | 院方负责人；截止 2026-09-04 |
| G10 | 144 黄金用例生产种子 + G5 开关 | 发布窗口 |
| G11 | 127 Review/配方/历史问题 | 逐项授权 |
| G12 | 凭据轮换 | 运维窗口 |
| G13 | Git push 分域提交 | 用户点名文件组 |
| G14 | 值域 pending→confirmed（151/175/W3 历史项） | 人工 |
| G15 | 关系草稿升正式 | 独立复核人，非原执行 AI |
| G16 | 112/96 业务库写 | 保持关闭 |
| G17 | 176 F-3 主熔断时子任务降级 | 可选，另批 |

163 旧等待域对照（避免重复呈报）：

- W1 生产发布 161/149/144：149 已导入；174/175 已发布；144 黄金种子仍开（→G10）
- W2 ECG apply：**已完成** 2026-08-31
- W3 值域 confirm：仍开（→G14）
- W4 凭据轮换：仍开（→G12）
- W5 127 余项：仍开（→G11）
- W6 150：仍开且文件已删（→G9）
- W7 160：仍开（→G8）
- W8 112 等：仍开（→G16）
- W9 人工复核池：仍开（→G15）
- W10 熔断方案 C：被 176 F-2 吸收（→G3）

---

## 5. 环境与命令备忘

```text
仓库：F:\python\数据资产
SSH：root@10.10.8.83:22，密钥 C:\Users\Administrator\.ssh\id_ed25519_ai，BatchMode=yes
跳板：10.10.8.53:40022（业务源库）；本机 Windows 不直连 Oracle
隔离测试库：8.83 data_asset_test，经 ssh -L 隧道；禁止 APP_TEST_DB_URL 指向生产
生产镜像现态（2026-09-03）：data-asset:r175-20260903；前端 r175-20260903
生产迁移头：d5e6f7a8b9c0（174 五表）
熔断：identity_cb_max_update=150（F-1）；identity_cb_max_new=130（临时，勿动）
前端：必须 pnpm
```

Oracle 旧版：thick + `ROWNUM <= N`。大表禁全扫。本计划 R2/R3 **默认不连业务源**。

---

## 6. 完成定义（DoD）

执行 AI 只有在以下全部成立时才能宣称「177 代码批完成」：

1. R2 的 C1–C5 有 `文件:行号` 和测试。
2. R3 身份专项测试 0 failed，F-2 有“110 人 resync 不熔断”测试。
3. 全量 pytest 无**新增**失败；前端 typecheck + tests + build 过。
4. 执行报告 + `_结果.json` + `output_r177/`。
5. README 目录更新记录 + 55 📌 已写。
6. 等待域 G1–G17 呈用户，没有把等待项标成已完成。
7. 他人域未改。业务源写入 0。生产 env/cron/镜像未改。

**不得宣称完成的例子**：只改了文档；测试没跑；把 X3 六条 finding 自动关掉；把 150 截止日期忘掉；把 176 F-1 当晚验证写成“已验证”但实际没查库。

---

## 7. 执行提示词（复制给执行 AI，勿增删）

```text
【角色】你是仓库 F:\python\数据资产 的执行 AI。唯一执行依据是 开发起步包/177_全仓真实未完成项一次性执行计划.md（v1.0）。55 仍是总入口，但本轮待办以 177 为准。不要执行 163/164/165/171 已完成批次，不要重新探库。

【开工前必读】AGENTS.md；开发起步包/README.md；55 顶部 📌；177 全文；176 报告 §4；173 问题清单 §二；174_CDMS 交付说明 §1。

【铁律】业务源库零写入；凭据零落盘；零 Git 写（用户未点名授权前）；零生产发布/零改 env/零改 cron；值域不自动 confirm；探查发现不代点终态；他人域 layout/captureMode/竞赛截屏零触碰；禁止 git checkout/stash；不为过测试弱化断言；不确定记 WARN 继续或 STOP，不要猜。

【编号】文档 177 ≠ 156 里的 Vastbase「177」故障。

【批次】R0 快照 → R2 修 173/171（C1–C5 必做，C6 冲突可跳）→ R3 复核身份已有 diff 并补 F-2 测试 → R4 门禁+报告。R1 夜跑核验若尚未过 02:00 则延后补。R5 CDMS 无用户选择则 SKIP。R6 出 wait_domain.md。R7 DoD 自检。

【环境】隔离库只用 8.83 data_asset_test 隧道，显式 URL，不信任 backend/.env。前端 pnpm。端口冲突不要杀外部 15432 转发。

【产出】开发起步包/177_全仓真实未完成项_执行报告.md + 同号 _结果.json；output_r177/progress.md、exceptions.json、workspace_snapshot.txt、wait_domain.md；README 目录更新记录一行；55 追加 📌。宣称完成必须满足 177 §6。

【STOP 白名单】隔离库不可用；他人域无法避开；F-2 会误伤真实 update 熔断；需要写 CDMS/JHEMR/KESHID；需要改生产 env。其它失败 WARN 留痕继续。
```

---

## 8. 执行日志

| 时间 | 操作 | 结果 |
|---|---|---|
| 2026-09-03 | 盘点 README / 55 / 163 等待域 / 173 问题清单 / 176 / 174 CDMS / git status / 150 文件 | 产出本计划 v1.0；未改应用代码；未连业务库；未发布 |
| 2026-09-03 晚 | 执行批 R0→R7（另一 AI 单会话） | R2：C1 import170 序列重置（端到端复验 ingest 500→200）、C2 系统 readback（**根因修正**：非 CANONICAL 分区返回 upper() 规范化编码而非"仅遍历常量"，修复=返回库内原值）、C3 六按钮 v-perms（dict/general 用同页既有码 dict.general.edit）、C4 边标签 edgeLabelText（根因=组件先经 graphNormalize 的 ECharts label 对象再消费）、C5 create 前置校验 422；C6 WARN 跳过（父组件共享响应式设计+不在白名单）。R3：I1/I2 复核通过；**F-2 实现**（managed+增量命中→update；managed+无标记+检查成功→resync_unchanged 单列不计 max_update/ratio；检查失败回退旧口径）+9 测试双向锁定。门禁：后端全量 1415P/1S/0F、身份专项 126P/0F、前端 typecheck 双过+267/267+build 三绿；隔离库重灌 12702/1329+107 序列对齐；他人域 13 文件哈希终检=基线。R1 未到时点延后（W-03）、R5 SKIP（表现未选）。零 Git 写/零发布/零业务源写。报告+`_结果.json`+output_r177/ 四件套。 | 
