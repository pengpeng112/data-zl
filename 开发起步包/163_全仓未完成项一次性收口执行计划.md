> 类别：模块规划
>
> 状态：**v1.1 已批准待执行（2026-08-28 用户批准全计划；附录 A 已勾选，R2 按 E1→E6 全链执行）**——multi-review round-3 三方审查（kimi 22 条+codex 5 条+grok 缺席）18 项议题全部裁决闭合
>
> 上位入口：`55_系统未完成事项统一执行计划.md`
>
> 关联：`review/round-3/`（方案.md/审查_zcode.md/审查_kimi.md/检查_codex.md/分歧表.md/对辩.md/最终报告.md）；`161_..._执行报告.md`（R1 证据来源）；`151_..._执行计划.md`（R2 唯一依据）；`155/159/146` 号（R3/R4/R5 依据）
>
> 执行方式：**单执行者连续推进，允许 checkpoint 跨会话续跑**（round-3 分歧#13 裁决：不承诺单会话完成；R4 依赖夜间窗口与 R5–R7 解耦）
>
> 批准记录：2026-08-28 用户批准 163 号计划全文，附录 A 勾选生效（值域圈定规则按 §6 执行；E5' TREAT_RESULT 与全部 confirm 仍留人工）

# 全仓未完成项一次性收口执行计划（163）

## 0. 背景与范围

对 开发起步包 全部编号文档完成度盘点后（结论见 §3），把"AI 可执行"的未完成项组织为 R0–R7 八批一次性收口；需用户授权/人工裁决的项全部归 §7 等待域并尽量由执行 AI 预产呈报材料。**本计划不含生产发布、凭据轮换、业务源库写入、值域 confirm、Git push**。

## 1. 铁律（STOP 白名单——只有以下四类允许停）

1. 桌面/服务器/测试库路径不可达且重试一次仍失败（按既有 BLOCKED 口径登记后可停该批，其余批继续）；
2. 需要更改 §2 已锁定决策才能继续；
3. 需要对业务源库或生产平台库执行 §5 未授权的写操作；
4. 需要连接本计划未列出的数据库/服务器，或执行 Git 写操作。

**其余一切偏差（数字不符/映射为空/夜窗错过/某子项失败）一律 WARN**：写入 `output/r163/exceptions.json`（`{step, item, expected, actual, note}`）、progress.md 标 WARN 后继续下一批/下一项。禁止向用户提问，禁止为凑数修改扫描范围或决策。

其他硬约束：业务源库零写入（sjzc 受控只读、聚合优先、ROWNUM 限量、患者标识脱敏）；**探库任务一律夜间执行并加超时**（AGENTS 红线，R4 适用）；测试库仅限隧道 data_asset_test（不可达标 `BLOCKED_TEST_DB`，禁改用其他库）；凭据零落盘；零 Git 写；值域永不自动 confirm；预计零新迁移（如确需，手写 upgrade/downgrade 并在报告中显著标注）；前端必须 pnpm；**161 会话留下的工作区既有改动与脏文件原样保留，禁 checkout/stash/discard**（R1 基于其上执行）。

## 2. 已锁定决策（round-3 裁决，不可更改）

| # | 决策 | 依据 |
|---|---|---|
| Q1 | NF-1 修复方向=更新 plan127 s0 断言接受 `await loadData(1)`（F4 已发布新语义），**不回退 F4**；改前必须先复现失败并引用 git 证据 | 分歧#9（K 同意+C 强化）；55 📌 161 条官方建议 |
| Q2 | 151 E2 圈定：**附录 A 是用户点名勾选闸门**——用户批准本计划并单独勾选附录 A，R2 才执行 E2'–E6；未勾选则 R2 降级为仅 E1 探索。152 E5 的 19 条值域候选须逐条对照（表格进报告：19 行×命中/冲突/未命中），冲突与未命中一律入待定位清单；TREAT_RESULT 双编码冲突列为 E5' 专项只读核查呈报，裁决留人工。**当前状态：附录 A 已于 2026-08-28 勾选生效，R2 全链执行** | 分歧#10+对辩共识（K 转同意、C 让步、+K 表格化修正） |
| Q3 | 155 ECG 收口拆两档：**档一**（本计划执行）=元数据导入包构建+隔离库演练+dry-run 计数报告+关系候选清单文件；**档二**（等待域）=生产平台库 apply，须用户确认 dry-run 报告后放行，且执行时带完整门禁链（生产备份→dry-run→apply→二次 dry-run 收敛 0→/health 200） | 分歧#11（K 反对+C 反对，G 让步） |
| Q4 | 159 山大地纬重采：仅夜间窗口+显式超时；无日间 fallback；R4 未完成不阻塞 R5–R7；8.83 同步前先备份服务器旧副本 | 分歧#12/#13（K+C 一致） |
| Q5 | R2 E6 值域导出**仅从隔离测试库**，禁触生产；报告须显式说明 value_domains.json/148 与生产平台库的漂移现状 | 分歧#15 |
| Q6 | R6 生产 asset_api_keys 查询限定字段：key_name/user_identifier/enabled/last_used_at/绑定状态；不取 token 哈希本体；输出脱敏后入受控报告 | 分歧#16（C） |
| Q7 | 执行口径=单执行者连续推进、checkpoint 跨会话续跑；每批完成即验收全过才进下一批 | 分歧#13 |
| Q8 | 等待域与盘点修正：deptsync3 待办已闭合（p153 镜像+36b8743 双侧吸收，不再列）；137 三源阻断已被 139 承接闭合（历史事实不重写）；人工复核池计数=142 的 56 草稿+40 停用配方、OA 5 草稿（55 状态行合计 61）、曼陀罗 16 候选 | 分歧#3/#4/#5（证据裁决） |
| Q9 | 146 迁移数以 README 为准=2 个（h8d9e0f1a2b3、i9e0f1a2b3c4）；"161 修复 13 文件"出处=161 执行报告 §0 | 分歧#1/#7 |

## 3. 已核实事实表（禁止重新勘探）

| 事实 | 值 | 出处 |
|---|---|---|
| 161 状态 | 代码完成待主 AI 复核与发布；门禁=后端 1175 passed/1 skipped/**1 failed**（唯一失败=plan127 s0 NF-1）+前端 typecheck 0 错/168 tests/build 预算 PASS；13 变更文件清单在其执行报告 §0 | 161 执行报告+55 📌 |
| NF-1 证据 | `git show 36b8743` 含 `-await loadData(); +await loadData(1);`（F4 树点击重置页码）；plan127 测试自 d3e6b55 未改；开工前 HEAD=13ef9e8 即失败 | 161 报告 §4 新发现#1 |
| 151 约束 | E2 用户停点；字典一律 pending 永不自动 confirm；首批 ≤20 类 ≤200 条；隔离库隧道 55432（URL 由 backend/.env 动态构造）；152 E5 19 条候选中 TREAT_RESULT 为最高优先裁决项 | 151 全文+55 📌 152 条 |
| 146 剩余 | 28 组中 20 DONE；E2–E8/E10 八组剩余子项逐条登记于 146 §11 E（约 22 项，计数为规模估计，执行以 §11 E 原文逐条为准）；迁移 2 个 | README 146 行+146 §11 |
| 155 ECG | EDSChinaDB 57 表/931 列+cardioNet 2 表（中文列名）；快照存 8.83 exports；平台 columns 通道现 0 列；关系链 ORDER→STUDY→MEASUREMENT/REPORT/DATA + cardioNet 跨系统 D 级 | 155 报告+55 📌 |
| 159 待办 | ①夜间重采山大地纬索引（旧 273,631 已作废、SYSTEM 隔离）②新输血 37 候选+23 unresolved+SHA256SUMS 受控同步 8.83 `/opt/data-asset/evidence/newsrc7-20260827/`；口令轮换部分归等待域 | 55 📌 159 复核条 |
| 122/156/130 | 122 H7 第三夜归 130 只读验收（夜跑至今每日在跑）；156 待办①补跑全量已由 161 完成；177 采集工具级 6×2s 重试已有、adapter 级待核实（153 A6 落地后查）；130 大半已被 144/146/153/161 吸收（覆盖度待 R6 核对表） | 55 📌+README+分歧#6 |
| 熔断事件 | 08-28 已裁决方案 A（阈值临时 130）；**08-29 08:30 自动化任务**复核夜跑并回调阈值 50 | 55 📌 熔断条 |
| 环境通道 | 8.83 SSH 公钥可用（`C:/Users/Administrator/.ssh/id_ed25519_ai`）；测试库隧道 15432（161 实测）与 55432（151 约定）两种口径并存——执行以 backend/.env 动态构造为准，不通即 BLOCKED_TEST_DB | 161/162 会话实测 |

## 4. 环境与命令

- 产物根：`backend/_r163_work/`（脚本/导入包）与 `开发起步包/output_r163/`（报告与呈报材料）；progress 台账 `开发起步包/output_r163/progress.md`（表头 `| 时间 | 批 | 状态(DONE/WARN/BLOCKED) | 产物 | 数字摘要 |`）。
- 后端测试：`cd F:\python\数据资产\backend && .\.venv\Scripts\python.exe -m pytest tests/ -q`（须 `APP_TEST_DB_URL`，隧道见 §3；跑全量前清点并终止非本会话 pytest 进程）。
- 前端：`cd F:\python\数据资产\frontend && pnpm run typecheck && pnpm run test && pnpm run build`。
- 连库：一律经全局技能 sjzc（结构查询+受控限量只读）；服务器操作经上述公钥 SSH。

## 5. PART A 执行批次

### R0 开工与环境

1. 建产物目录与 progress 表头；git log -1 确认 HEAD（应为 13ef9e8 或其后续，若已前进需按内容定位法重对 R1 行号，不重做勘探）。
2. 隧道+测试库连通验证；跑 `pytest tests/security_audit -q` 冒烟并记录基线 B（后端全量基线=161 后 1175 passed/1 skipped/1 failed）。
3. 熔断复核时序分支：若当前时刻 **早于 08-29 08:30** → 登记"未到点，跳过"（WARN 不阻塞）；已过 → 只读核验该自动化任务结果与生产 env 阈值是否已回调 50（**只读**；任务失败则如实呈报，禁止代替重试或改阈值）。
4. 验收：progress 三行落盘；基线 B 记录。

### R1 测试基线归零（NF-1）

1. **先复现**：`pytest "tests/plan127/test_s0_contract_unit.py::test_table_list_system_filter_accepts_his_alias" -q` 确认失败；`git show 36b8743 -- frontend/src/views/asset/tables/index.vue | grep -A2 -B2 loadData` 引出 F4 证据（分歧#9：防止"测试迁就缺陷"）。
2. 修改 `backend/tests/plan127/test_s0_contract_unit.py` s0 断言：`await loadData()` → `await loadData(1)`（或等价包含断言），注释注明"161 NF-1：F4 树点击重置页码为已发布语义（36b8743），旧契约断言随之更新；不回退 F4"。
3. 验收：`pytest tests/plan127/ -q` 全绿（25 项）+ `pytest tests/ -q` 全量 **0 failed**（R7 复跑确认）；progress 登记。

### R2 值域扩展（151 E1–E6，附录 A 已勾选）

> **闸门状态（2026-08-28）**：用户已批准计划并勾选附录 A → **执行 E1→E6 全链**（含 E2' 自动圈定、E5' 专项核查）；E5/E5' 的 confirm 与 TREAT_RESULT 裁决仍留人工，其余按 151 §2.2 硬规则执行。

- **E1 探索（只读）**：按 151 §7 E1 逐字执行——sjzc 查 PORTAL_SYS_DICT 族（双 schema 结构+采样 ≤20 行+145 类全量 TYPE 清单）与 JHEMR EMR_FIRST_PAGE_ITEM_DICT（结构核对+distinct FIELD_NAME+每字段值数聚合+双 schema 差异）。产出 `151_两字典表探索与圈定清单_结果.json` 并登记 README。
- **E2' 自动圈定**：按附录 A 关键词规则从 145 类选 ≤20 类、JHEMR FIELD 映射目标字段；**152 E5 19 条候选逐条对照表**（19 行×命中/冲突/未命中，冲突与未命中一律入待定位清单）；圈定结果全部随报告呈用户追认（导入动作不等追认，因为一律 pending）。
- **E3 脚本+测试**：`backend/scripts/import_dict_value_domains.py`（--scope portal/jhemr、--dry-run、幂等复用 value_domain_service、字典一律 pending、confirmed 冲突只置位、批量审计一条、规模门禁 200）+ `tests/test_value_domain_dict_import.py`（幂等/不改 confirmed/冲突置位/规模门禁/dry-run 零写）。禁止改 126/144 语义。
- **E4 隔离库导入**：dry-run → 实导 → 再 dry-run 收敛 0；`pytest tests/ -q` 全绿（沿用 R1 后 0 failed 基线+新增）。
- **E5 待核值域核查**（151 §3 SQL，只读、聚合优先、ROWNUM 限量、PATIENT_ID 前 2 后 2 脱敏）：code9/手术标志 3；证据写入 note；**confirm 仅提示用户人工操作**。
- **E5' TREAT_RESULT 专项**（Q2）：JHEMR 1治愈…9其他 vs HIS COMM 字典双编码，只读聚合比对，产出专项呈报表（含两套编码全量对照+建议），**裁决留人工**。
- **E6 收口**：`python ..\tools\export_value_domains_json.py --regenerate-148` + `--check` PASS（**仅隔离库导出，禁触生产**；报告显式说明 value_domains.json/148 与生产平台库漂移现状）；151 计划补执行记录一节。
- 验收：151 §6 三命令全过+152 E5 对照表+E5' 呈报表在报告内。

### R3 ECG 收口·档一（155 ①②，不碰生产）

1. 从 8.83 exports 快照（SSH 只读拉取；不可达则经 sjzc live 限量补采）取 EDSChinaDB 57 表/931 列+cardioNet 2 表元数据，构建导入包（格式对齐 数据资产_四系统源端资产包 模式：manifest/objects/columns/import_plan）。
2. **隔离测试库演练**：导入 asset_tables/columns（幂等、审计、include_status=candidate），计数核验=931+cardioNet 列数。
3. **生产 dry-run 报告**（只读）：对生产平台库跑导入脚本 dry-run 模式，产出"将新增/变更计数表"，呈用户作为档二放行依据。
4. 关系候选**清单文件**（不写库）：ORDER→STUDY→MEASUREMENT/REPORT/DATA 链 C 级+cardioNet 跨系统 D 级，sql-relation-intake 候选格式，入 155 同号结果文件待人工走复核。
5. 产出：`155_ECG元数据导入与关系候选_结果.json`+报告；README 登记。
- 验收：导入包计数对照表+隔离库导入成功+dry-run 报告+候选全为清单文件（库中零新增行于生产）。

### R4 159 同步（夜间窗口批，与 R5–R7 解耦）

1. **夜间**用修订后采集器只读重采山大地纬索引（显式超时；替换已作废 273,631 旧结果；SYSTEM 对象隔离），产出新旧计数对照表。
2. 新输血 37 候选+23 unresolved+SHA256SUMS 受控同步 8.83 `/opt/data-asset/evidence/newsrc7-20260827/`（同步前 SSH 备份服务器旧副本；校验上传后 SHA256SUMS 一致）。
3. 159 报告补执行记录。**会话进行到 R4 时若不在夜间窗口**：登记"夜间执行清单"（含确切命令与参数）为 WARN，跳过继续 R5；夜间部分可由下一 checkpoint 会话补跑。
- 验收：新旧计数对照+服务器校验和一致+159 补记录。

### R5 146 剩余子项（22 项，以 146 §11 E 原文逐条为准）

逐组执行（同页一次触碰；每组完成即 `pnpm run typecheck && pnpm run test`）：
- E2 剩余 3 项（普通用户统计折叠/避免无必要 destroy/node.meta 副标题与截断测试）
- E3 剩余 2 项（任务列表服务端分页与跨页选择/状态中文）
- E4 剩余 3 项（Owner/术语表单校验强化/快照勾选/内部编号清理）
- E5 剩余 2 项（tables Schema 增量加载/业务域候选与清除筛选）
- E6 剩余 1 项（sync-diffs 动态来源/字段 diff/按钮收敛——注意 161 P2-2 已改其 doSync catch，勿回退）
- E7 剩余 2 项（runs 执行中轮询/audit before/after 前端抽屉展示）
- E8 剩余 5 项（sync-diffs 详情/批量处理/意见/全量 summary/共享 composable、mappings 失败反馈/可读空值/列配置/选项缓存/弹性高度、medical PushWizard 一组）
- E10 剩余 4 项（quality 页拆分共享组件/relations/lineage 错误态强化/ai-context 选择同步与导出失效修复/overview+welcome 失败态细化）
- 逐项完成即更新 146 §11 E 对应行（DONE+一行证据）。
- 验收：typecheck 0 错+Vitest 全过（≥168 基线）+146 §11 E 更新；单组失败记 WARN 继续，**不得为过测试弱化断言**。

### R6 核对与呈报（全只读，六项）

1. **130 覆盖度核对表**：130 的 A01–A28/§ 各条目 → 逐条标注"已被 144/146/153/161 吸收（引证据）/仍开放"；产出入报告，仍开放项转入等待域清单。
2. **122 H7 终态**：只读查夜跑 run 记录（平台库），登记 H7 第三夜及此后夜跑终态；若观察脚本路径仍有问题仅登记不修（超出范围记新发现）。
3. **177 adapter 级重试核实**：grep 153 A6 落地后的连接器代码——存在→登记证据行；不存在→等待域（不擅自加码）。
4. **unbound token 呈报**（Q6 字段限定+脱敏）：生产 asset_api_keys 只读查询，输出"unbound 且 last_used_at 近期"清单与绑定/停用建议（动作留人工）。
5. **死权限码完整清单**：全量扫描 permissions 目录中无端点挂载的码（含 ai.draft.execute，161 P3-1 已登记），产出裁决呈报表。
6. **149 状态确认**：P4 ingest 告警与前端 A/B 维持"后置"裁决登记；值域生产导入依赖单独授权（等待域）。
- 验收：六段"证据+结论"入总报告；每段有命令输出摘录。

### R7 总收口

1. 后端全量 `pytest tests/ -q` **0 failed**（含 R1 修复与 R2/R5 新增测试）；前端三件套全绿（typecheck/test/build+gzip 预算）。
2. 161 四项静态 grep 复验仍绿（loadRelations 直绑=0、`user = "system"`=0、tool-execute=ai.context.read 断言、ai.py 权限码）。
3. 总执行报告 `163_全仓未完成项一次性收口_执行报告.md`+同号 `_结果.json`（批次/证据/152E5 对照表/E5' 呈报表/exceptions/新发现）；README 目录更新记录+状态行、55 📌 各登记一行「代码完成，待主 AI 复核与发布」。
4. 等待域清单（§7）随报告完整呈用户。

## 6. 附录 A：值域圈定规则（**已勾选生效——2026-08-28 用户批准 163 计划时点名确认**）

勾选框：`[x] 本人已阅读并批准本附录，R2 按 E2'–E6 全链执行`（2026-08-28 批准；E5/E5' 的 confirm 与 TREAT_RESULT 裁决仍留人工）

1. **PORTAL_SYS_DICT_TYPE 145 类筛选关键词**（TYPE_NAME 匹配，命中且 ENABLE_STATE 有效者按优先序取前 ≤20 类）：离院方式/出院方式、性别、诊疗类型/住院类型、科室/病区、麻醉方式、切口愈合、抢救成功、血型、过敏、输血反应、入院途径、肿瘤分期/分型、护理等级、医生职称、费别/医保类型。未命中类型全部入"待定位清单"随报告呈报。
2. **JHEMR EMR_FIRST_PAGE_ITEM_DICT**：FIELD_NAME 含 离院方式/出院/手术/麻醉/切口 关键词者映射到 report.r_pat_visit/病案首页对应列；无把握的不导入，入待定位清单。
3. **152 E5 19 条候选对照（强制）**：附录规则圈定结果与 19 条候选逐条对照成表（19 行×命中/冲突/未命中）；**冲突与未命中一律入待定位清单**；TREAT_RESULT 冲突走 E5' 专项（不自动处理）。
4. 所有导入 status=pending（永不自动 confirm）、首批 ≤200 条、幂等、隔离库先行。

## 7. PART B 等待域（呈用户，本计划不执行）

| # | 事项 | 前置 | 呈报材料来源 |
|---|---|---|---|
| W1 | 生产发布窗口：161 全部修复（13 文件）+ 149 值域种子生产导入 + 144 黄金用例种子/G5 开关 + 163 各批成果 | 主 AI 复核 git diff；用户授权发布 | 161/163 执行报告 |
| W2 | ECG 生产平台库 apply（R3 档二） | 用户确认 R3 dry-run 报告；执行带完整链（生产备份→dry-run→apply→二次 dry-run 收敛 0→/health 200）；关系候选 draft 入库同批 | R3 dry-run 报告 |
| W3 | 值域 confirm（code9/手术标志 3/TREAT_RESULT 裁决；附录 A 圈定结果追认为可选复核） | 用户人工裁决 | R2 E5/E5' 呈报表+152E5 对照表 |
| W4 | 凭据轮换：门诊 SSH 口令（曾进 Git 历史）、DRG hisserver 口令、OA admin、ECG sa | 用户授权运维窗口 | 55 📌 159/155/143 各条 |
| W5 | 127 余项：生产 Review 批准、生产配方 active、历史质量问题重建、A25 视觉验收 | 用户逐项授权 | 130 覆盖度核对表（R6-1） |
| W6 | 150 医保局课题申报（**外部截止 2026-09-04**） | 院方确认负责人/团队 | README 150 行 |
| W7 | 160 职称执行包 | 信息科/DBA 执行（SQL 包在 backend/_oa_work，gitignored） | 160 执行包 |
| W8 | 112 Phase D 业务库写入 / 96 闭环验收 / DRG hisserver 探索 / 154 B2+B3 / 157 ZFP 上线前代理 / legacy_unmanaged 3,756 人扩面 / 146 域 audit limit | 各自授权或触发条件 | 55/README 对应行 |
| W9 | 人工复核池：142 的 56 草稿+40 停用配方、OA 5 草稿（55 状态行合计 61）、曼陀罗 16 候选、R4 新输血 37 候选、R3 ECG 候选 | 人工复核流程 | 各同号结果文件 |
| W10 | 08-28 熔断机制优化（方案 C：区分"现存用户首次纳管"与"真实新建号"） | 用户裁决（可并入 153 后续） | 55 📌 熔断条 |

已闭合不再列（Q8）：deptsync3 吸收（p153+36b8743 双侧完成）、137 三源阻断（139 承接）、156 待办①补跑全量（161 已跑）。

## 8. 完成定义（DoD，逐项自检）

- [ ] progress.md 覆盖 R0–R7 每批（DONE/WARN/BLOCKED），无空白批
- [ ] 后端全量 0 failed（基线=161 后 1175+新增）；前端三件套全绿
- [ ] R2 三命令全过（附录 A 已勾选）+152E5 对照表+E5' 呈报表
- [ ] R3 导入包+隔离演练+dry-run 报告+候选清单文件（生产库零写入）
- [ ] R4 夜间批完成或已登记"夜间执行清单"（不阻塞 R5–R7）
- [ ] R5 22 项逐项更新 146 §11 E（失败项 WARN 留痕）
- [ ] R6 六段呈报材料齐备
- [ ] 总报告+_结果.json+README/55 登记完成；等待域清单呈用户
- [ ] 零越权：未发布、未轮换、未 confirm、未写业务源库、未写生产平台库、零 Git 写

## 9. 执行提示词（复制给执行 AI，勿增删）

```text
【角色】你是本仓库（F:\python\数据资产）执行 AI，按 开发起步包/163_全仓未完成项一次性收口执行计划.md 单执行者连续推进 R0→R7（允许 checkpoint 跨会话续跑，中断后从最近 checkpoint 续跑）。

【开工前必读（按序）】AGENTS.md；开发起步包/163 号计划（唯一执行依据）；其 §3 事实表（禁止重新勘探）；R2 批另读 151 号计划全文；R5 批另读 146 §11 E。

【铁律】163 §1 四类 STOP 白名单之外一律 WARN 继续（exceptions.json+progress 标 WARN）；夜间探库+超时；业务源库零写入；测试库仅隧道 data_asset_test；凭据零落盘；零 Git 写；值域永不自动 confirm；161 会话既有工作区改动原样保留禁 checkout/stash；不为过测试弱化断言。

【闸门】163 附录 A 已勾选生效（2026-08-28 用户批准计划并点名确认）：R2 按 E1→E6 全链执行（含 E2' 自动圈定与 E5' TREAT_RESULT 专项）；值域一律 pending 永不自动 confirm，E5/E5' 的 confirm 与 TREAT_RESULT 裁决留人工。

【环境】测试库隧道与 APP_TEST_DB_URL 推导见 163 §3/§4 与 162 交接包（SSH 公钥 root@10.10.8.83；口令从 /etc/data-asset/backend.env 推导，只进会话环境变量）；跑全量前清点并终止非本会话 pytest 进程；前端必须 pnpm。

【节奏】R0→R1→…→R7 顺序推进；每批完成立即跑该批验收命令，全过才进下一批（R4 夜间例外：登记夜间清单后可先推进 R5）；每批输出 checkpoint（完成项/验收输出/偏离记录）。

【产出】163 §8 DoD 逐项自检后宣告完成；总报告+同号 _结果.json+README/55 各登记一行「代码完成，待主 AI 复核与发布」；等待域清单随报告呈用户。
```
