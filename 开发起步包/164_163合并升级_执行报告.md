> 类别：执行报告
>
> 状态：**代码完成/测试完成，待主 AI 复核与多方核查（2026-08-29 执行会话产出）**——按 164 号计划（v1.1）R0→R8 连续推进；R4 夜间批已登记夜窗清单待补跑，R8 批次 A 部分执行并登记 checkpoint（B–G 未执行，诚实口径）
>
> 上位入口：`55_系统未完成事项统一执行计划.md`；执行依据：`164_163合并升级_测试任务与值域界面与跨系统数据质控专项计划.md`（其 §9 提示词）+ `163_全仓未完成项一次性收口执行计划.md`（PART A 唯一依据）
>
> 机器可读：同目录 `164_163合并升级_结果.json`；过程台账 `output_r163/progress.md`、`output_r163/exceptions.json`、`output_r163/r4_night_checklist.md`、`output_r163/r6_130_coverage.md`
>
> 零越权声明：未发布生产、未写业务源库、未写生产平台库（仅只读查询）、未 confirm 任何值域、零 Git 写；ai-hms_qhd 仓零代码改动

# 163 合并升级（164）执行报告

## 0. 批次总览

| 批 | 内容 | 状态 | 摘要 |
|---|---|---|---|
| R0 | 开工与环境 | DONE | HEAD=13ef9e8；隧道/测试库连通；security_audit 冒烟 9 passed；熔断复核分支=未到点跳过（08:30 自动化另会话已自行登记复核结果） |
| R1 | NF-1 修复 | DONE | 复现→改断言 `await loadData(1)`（36b8743 证据）→plan127 25 passed |
| R2 | 值域扩展 151 E1–E6 | DONE | 圈定 8 主题 53 行 pending 导入隔离库收敛 0；code9/标志3/E5' TREAT_RESULT 三项核查呈报；导出+148 重生成 --check PASS |
| R3 | ECG 档一 | DONE | 65 对象/1119 列导入包+隔离演练精确匹配+幂等 0；生产 dry-run（零写入）报告；关系候选 9 条仅清单文件 |
| R4 | 159 同步 | WARN | 日间非夜窗：夜间执行清单已登记（output_r163/r4_night_checklist.md），夜窗补跑 |
| R5 | 146 剩余 22 项 | DONE | Luna 子代理执行 22/22 DONE；主 AI 独立复跑门禁确认（194 tests/typecheck 0 错/build 预算 PASS）；146 §11 E 已更新 28/28 组 DONE |
| R6 | 六项只读呈报 | DONE | 130 覆盖表/H7 终态/177 adapter/unbound 清零/死权限码 11 个/149 后置维持——全部有命令证据 |
| R7 | 总收口 | DONE | 后端全量 1183 passed/1 skipped/**0 failed**（25:19）；前端三件套独立复跑全绿；161 四项静态 grep 复验全绿；alembic 单头 b0c1d2e3f4a5 |
| R8 | ai-hms GUI | WARN（部分） | 批次 A 执行 7/26（通过 5/失败 2，P2×2+P3×1 缺陷），19 用例未执行；B–G 未开始；checkpoint 报告在 ai-hms_qhd/evidence/GUI-20260829-01/ |

## 1. R0 环境与基线

- HEAD=13ef9e8（与 163 §3 预期一致）；工作区 161 成果原样保留、零 checkout/stash。
- 测试库通道：**15432 被外部转发脚本占用**（122_fwd_external.py → 192.168.98.99:5432，属 ai-hms E2E 设施，非 8.83），改用本会话自建 ssh -L **15433** 隧道（trust 认证，零口令落盘）；`APP_TEST_DB_URL=postgresql+psycopg://postgres@127.0.0.1:15433/data_asset_test`。
- security_audit 冒烟 9 passed；基线 B=161 后 1175 passed/1 skipped/1 failed（引 163 §3，未重跑）。
- 熔断复核：会话时刻 08:04<08:30 → 按规则登记 WARN 跳过；08:30 自动化（automation-89b8f2ec）已在其自身会话执行并在 55 📌 登记：昨夜 RUN-4dc5f7206a7b 仍 failed（维度换为 max_change_ratio 109%>30%，max_new=130 已放行第一道；下一道 identity_cb_max_update=100 仍会拦），阈值保留，**待用户再裁决（方案 C 推荐/A2/D）**。

## 2. R1 NF-1 测试基线归零

1. 复现：`test_table_list_system_filter_accepts_his_alias` 1 failed；
2. 证据：`git show 36b8743` 含 `-  await loadData(); / +  await loadData(1);`（F4 已发布语义，163 Q1 裁决不回退）；
3. 修复：`backend/tests/plan127/test_s0_contract_unit.py:202` 断言改 `await loadData(1)` 并注明裁决出处；
4. 验收：plan127 目录 25 passed；全量 0 failed 由 R7 复跑确认。

## 3. R2 值域扩展（151 E1–E6，附录 A 已勾选）

### E1 探索（sjzc 受控只读）

- `PORTAL_SYS_DICT_TYPE` 145 类全量（PORTAL_USER）；字典行 PORTAL_USER=1215 行/122 类 vs HIS=1772 行/193 类；**50 类仅 HIS 有数据、0 类仅 PORTAL_USER 有**（以 PORTAL_USER 为准，差异入证据）。
- JHEMR `jhemr.emr_first_page_item_dict` 92 个 FIELD_NAME 全清单；实结构较老文档§176 多 boh_name/dict_name/del_flag；**关键发现：DISCHARGE_DISPOSITION 的 ITEM_VALUE=中文名（库内存值）、ITEM_TEXT=数字码（反向存储）**；`DischargeMethod`（4550 行）实为转归机构字典（机构名↔代码），非离院方式——无把握不导入。
- 产出：`开发起步包/151_两字典表探索与圈定清单_结果.json`。

### E2' 自动圈定（附录 A 关键词规则）

- PORTAL 命中 10 类中 9 类有数据：圈定 2 类（DEPARTURE_METHOD_CODE→PAT_VISIT.DISCHARGE_DISPOSITION、DISCHARGE_STATUS_CODE→DIAGNOSIS.TREAT_RESULT，均 pending）；TEND/hospType/hospState/hospPurpose/dept_type_code/Allergy×3 无单一可信目标列→**待定位清单**；性别/血型/麻醉/切口愈合/抢救/输血/肿瘤分期/职称/费别医保/诊疗类型 10 关键词 PORTAL 无对应 TYPE→未命中清单。
- JHEMR 按 离院/出院/手术/麻醉/切口 关键词圈定 6 字段：DISCHARGE_DISPOSITION(6 值)/DISCHARGE_CONDITIONS(5)/ANAESTHESIA_METHOD(27)/WOUND_GRADE(5)/HEAL(5)/OPERATION_TYPE(3)→目标 report.r_pat_visit/r_diagnosis_doct/r_operation_doct 对应列（live 核实存在）。
- **152 E5 19 条候选逐条对照表**（19 行×命中/冲突/未命中）已入 151 结果 JSON；冲突与未命中全部入待定位清单；TREAT_RESULT 走 E5' 专项。
- 圈定合计 53 行（≤200 门禁）/8 主题（≤20 门禁）；随报告呈用户追认（导入不等追认，一律 pending）。

### E3 脚本+测试

- `backend/scripts/import_dict_value_domains.py`：--payload/--scope portal|jhemr|both/--dry-run；复用 value_domain_service.find_by_key/evidence_duplicate/next_version/mark_conflict；字典一律强制 pending；confirmed 冲突不改写只置位+竞争含义证据；pending 漂移刷新+版本；规模门禁 200；单条 seed_import 审计。
- `backend/tests/test_value_domain_dict_import.py`：7 测试（dry-run 零写/幂等收敛/对照码含义/confirmed 不被覆盖只置冲突/规模门禁/未知 scope/审计一条）全绿。

### E4 隔离库导入

- dry-run(2+51)→实导 53 created→再 dry-run 收敛 0；后按生产正确顺序重放（种子先行）：种子 21 created（19 confirmed+2 pending）→字典导入 1 created+**1 confirmed_conflict**（见 §3.4）；全量 pytest 1183 passed/0 failed。

### E5 待核值域核查（只读聚合+限量+脱敏）

- **离院方式 code9**：HIS MEDREC.PAT_VISIT `='9'` 累计 **69 例**（2025-05-01～2026-07-02，远超 149 时代记录的 2 例）；JHEMR 字典实证 **9=其他**；抽样 5 例（PATIENT_ID 前2后2脱敏）S3 对账：4 例命中 JHEMR，其中 3 例 JHEMR=其他（一致）、**1 例 JHEMR=非医嘱离院（跨系统不一致实例）**、1 例无 JHEMR 行。→ 呈用户：code9 语义=其他（JHEMR 字典+多数实例一致），可 confirm；不一致实例另行核查。
- **手术急诊标志 3**：MEDREC.OPERATION 2025 分布 1=72,518/2=7,415/**3=6,109**/0=3/NULL=717；JHEMR OPERATION_TYPE 字典 **择期1/急诊2/期限3**——code3=期限手术（限期），非脏数据。151 §3.2 S2 明细抽样按 151 §3.3「字典证据优先」条款免除。→ 呈用户：可将 3=期限手术 补入 149 confirmed（人工操作）。
- PATIENT_ID 脱敏符合规定；未写任何业务库。

### E5' TREAT_RESULT 专项（裁决留人工）

- 两套字典全量对照：HIS COMM.DISCHARGE_DISPOSITION_DICT（治疗结果：1治愈/2转院/3死亡/4好转/5自愿/6其他/7未愈）vs JHEMR DISCHARGE_CONDITIONS（1治愈/2好转/3未愈/4死亡/9其他）——**2/3/4 三码完全错位**。
- 实际数据：HIS MEDREC.DIAGNOSIS.TREAT_RESULT **双编码混存**（数字码与中文并存：'2'=92.6 万、'好转'=26.7 万…）；按 SOURCE 拆分（SOURCE=2 组 vs NULL 组）数字码比例（260:104:50:6.5:6.1）与 JHEMR 中文档分布（272:110:52:6.5:6.3）**几乎一致**→实证实际数字码走 **JHEMR 语义**（2=好转），COMM 字典码表与该字段实际数据不符。
- 呈报表（两套编码全量对照+SOURCE 拆分证据+建议：确认「TREAT_RESULT 数字码按 JHEMR 语义解释」并在此字段 confirm JHEMR 语义值域；COMM 字典标注「勿用于 TREAT_RESULT」陷阱）**裁决留人工**。

### E6 收口与漂移说明

- `export_value_domains_json.py --regenerate-148` + `--check` PASS（仅隔离库）。
- **与生产漂移现状（Q5 要求显式说明）**：生产库（8.83 data_asset）从未导入 149 种子（p144-149 发布记录明示另行授权），生产 value_domains 表=空；仓库 value_domains.json 现为隔离库导出=**confirmed 18 条**（HEAD 版本 19 条）；差异 1 条=DISCHARGE_DISPOSITION#1：隔离库内 PORTAL 字典行（1=自行离开）与 149 种子 confirmed（1=医嘱离院）语义冲突→confirmed 行按设计置 conflicted、被注入链自动排除，**待人工裁决**（W3）。148 已重写为平台导出视图（当前反映隔离库状态）。
- 教科书案例：三方语义矛盾（PORTAL 字典 1=自行离开 vs 149 confirmed 1=医嘱离院 vs JHEMR 字典 医嘱离院=1）在隔离库按两计划设计的冲突机制正确落地——confirmed 未被改写、竞争含义入证据、注入链自动排除、裁决留人工。

## 4. R3 ECG 收口·档一

1. **导入包**（`backend/_r163_work/ecg_import_package/`：manifest/objects.csv/columns.csv/import_plan.json/relation_candidates.csv）：源=155 快照镜像（8.83 exports 本地副本）；65 对象（59 表+6 视图）/1119 列（EDSChinaDB 931+cardioNet 188）。
2. **隔离库演练**：`scripts/import_ecg_metadata.py`（自建 engine 显式 URL，复用 139 底座 upsert_tables/upsert_columns，include_status=candidate，单条审计）→ ecg 61 表/931 列、cardionet 4 表/188 列 **精确匹配**；二跑 inserts=0/updates=0 幂等收敛。
3. **生产 dry-run 报告（只读，零写入，事后核验生产 ECG 行=0）**：将新增 65 表对象/1119 列（全部 candidate）、0 更新、0 跳过——即 W2 放行依据：`--dry-run --url <生产 URL>` 输出存档于本报告 §4 附注。
4. **关系候选清单文件（库中零新增）**：`155_ECG元数据导入与关系候选_结果.json` + csv：ORDER_TBL→STUDY_TBL（ORDER_SEQ）等内部链 7 条 C 级 + 心电 ORDER_TBL.PATIENT_ID+VISIT_ID↔HIS 万能键、cardioNet 住院号+次数↔HIS 键 2 条 D 级（40 号口径延后人工）。
5. **重要工程发现（已入 exceptions.json）**：backend/.env 的 APP_DB_URL 指向 10.20.1.153（医院内网另一台旧 schema 平台库，本机可达）≠ 生产 8.83 本机库；脚本 --url 覆盖若晚于 app.core.db 模块导入将误连该库（dry-run 只读无副作用）。已改为脚本自建 engine 显式接 URL。**后续任何脚本一律显式 URL，勿信 .env**。

## 5. R4 159 同步（夜窗批）

日间非夜窗（163 R4-3 口径）：登记 WARN 并继续 R5–R7。夜间执行清单（确切命令与参数）见 `output_r163/r4_night_checklist.md`：修订版采集器上传（备份服务器旧脚本）→ `timeout 3000 python3 scripts/harvest_oracle_readonly.py --output snapshots/sddw_snapshot_r4.json`（修订版默认隔离 SYSTEM）→ 新旧 273,631 对照表 → relation_candidates.json/SHA256SUMS 受控同步（先备份后校验）→ 159 报告补执行记录。夜窗补跑后本批转 DONE。

## 6. R5 146 剩余子项（22 项）

- 执行方式：Luna 子代理按边界清晰任务书执行（frontend/ 与 146 §11 E 记录行），主 AI 复核。
- 结果：**22/22 DONE、0 WARN**（E2×3/E3×2/E4×3/E5×2/E6×1/E7×2/E8×5/E10×4 逐项证据见子代理报告与 146 §11 E 新增「R5 补完记录」21 行）。
- 主 AI 独立验收：`pnpm run typecheck`（tsc+vue-tsc）0 错；`pnpm run test` **28 files/194 tests passed**（基线 168→194 只增不减）；`pnpm run build` 47.85s 通过、gzip 预算 PASS（main 656.1/700、graph 404.2/430、CSS 98.6/110）。161 P2-2 doSync catch 有测试锁定未回退。
- 146 终态：**28/28 组 DONE，无 WARN、无遗留**。

## 7. R6 核对与呈报（全只读，六项）

1. **130 覆盖度核对表**：`output_r163/r6_130_coverage.md`——A01–A28 逐条：24 已吸收（引证据）/A14 现状符合（生产 27 条 query_schedules 全 enabled=false 实查）/A25 部分开放（W5）/A28 BLOCKED（受控 Key 未提供）/A20 持续治理项。仍开放项转等待域。
2. **122 H7 终态**：生产只读查夜跑记录：id30（08-27，success，one_off_backfill）/id31（08-28 failed，max_new 熔断）/id32（08-29 failed，max_change_ratio）——**H7=1/3**；08:30 自动化已登记待用户再裁决（方案 C/A2/D）。观察脚本路径无新问题，无需登记新发现。
3. **177 adapter 级重试核实**：**存在**——`app/services/jhemr_identity_adapter.py:306-322` 隧道等待循环（≤10s、0.2s 间隔、stderr 尾行 drain、fail-closed，153 A6 注释在案）。
4. **unbound token 呈报（Q6 字段限定）**：生产 asset_api_keys 总量 1、已绑定 1、**unbound=0**、近 30 天使用=0——161 P3-3 待办实际已清零，无需处置（呈报存档）。
5. **死权限码完整清单**（全量扫描 96 码，排除目录文件自身与前端引用）：**11 个无端点挂载**：ai.draft.execute、ai.draft.view、evaluation.view、feedback.review、ops.run.approve、ops.run.execute、ops.run.submit、ops.sql.audit、ops.sql.review、query.publish、query.recalc；其中 ops.run.* 与 ops.sql.review/audit 同时被 ops_admin/approver 角色默认授予（授而未挂）。**只登记不删**，裁决呈用户（D5 式）。
6. **149 状态确认**：P4（ingest 告警/影响分析）与前端 A/B 维持「后置/二期」裁决（149 行 3/109/161）；值域生产导入依赖单独授权（等待域，本次未动）。

## 8. R7 总收口门禁

- 后端全量：`pytest tests/ -q` → **1183 passed / 1 skipped / 0 failed**（1519.09s；含 R1 修复与 R2 新增 7 测试；161 基线的 1 failed 已归零，符合 A10 口径「如实记录」的反面——本仓无已知失败）。
- 前端三件套（主 AI 独立复跑）：typecheck 0 错 + **194 tests** + build PASS（gzip 预算全过）。
- 161 四项静态 grep 复验全绿：loadRelations 直绑=0；`user = "system"`=0；tool-execute→ai.context.read 断言在案（test_fine_grained_write_permissions.py:107）；ai.py 挂 ai.context.read/ai.sql.execute。
- alembic：单头 `b0c1d2e3f4a5`（163/164 零新迁移——151/ECG 导入均走既有表结构，无需迁移）。
- 隔离库终态：vd 19 confirmed/54 pending/1 conflicted（待裁决）+ ECG 65/1119 candidate——`--check` PASS。
- 执行插曲（如实记录）：首轮全量在 30% 处挂死 90 分钟——根因为本地 15433 隧道中断（12 连 E 后等待）；改用会话托管后台隧道+`-v` 流式日志重跑即全绿。**教训入台账：全量跑批前必须用托管隧道并监控进度。**

## 9. R8 ai-hms_qhd GUI（跨仓只测不改）

- 环境准入：E2E_BASE_URL=http://127.0.0.1:13000（经既有 13000 转发→192.168.98.31:3000）；/health 200；HEAD=74c7b47f；视口 1440×900；凭据走 .env.e2e.local（零输出零落盘）。
- **批次 A 部分执行：7/26 用例（通过 5/失败 2），19 未执行；缺陷 P2×2+P3×1**：
  - DEF-A-002（P2）登录按钮点击无响应（回车可提交，有替代路径）；
  - DEF-A-003（P2）全应用无退出登录控件（DOM 全文无 退出/logout）；
  - DEF-A-001（P3）空提交无必填提示；
  - 通过项：G-01 跳转登录/G-02b 错误口令清晰提示/G-19 中文 404/医生角色 fail-closed（直连 /user-management 出无权限页）/驾驶舱 console 0 错误。
  - 角色落地：admin→/dashboard、doctor→/cockpit?lens=doctor（PC-02/03 抽查通过）。
- 证据与批次报告：`ai-hms_qhd/evidence/GUI-20260829-01/`（result-summary.md + 8 张截图；含真实患者姓名的 1 张已按红线删除）；IAB 截图管线重影伪影已在报告声明，以 DOM/网络证据为权威。
- **B–G 批次未执行（诚实口径）**：依赖批次 A 补完与测试患者（甲～壬）就绪确认；checkpoint 已登记，续跑会话按 result-summary.md 未执行清单继续。
- 该仓零代码改动、零 SQL、零部署、零 push；缺陷只登记。

## 10. 完成定义自检

### 163 §8 DoD（8 项）

- [x] progress.md 覆盖 R0–R7 每批（R4=WARN 夜窗清单、R8=WARN checkpoint，无空白批）
- [x] 后端全量 0 failed（1183+新增；前端三件套全绿）
- [x] R2 三命令全过（dry-run 收敛 0/pytest 全绿/--check PASS）+152E5 对照表+E5' 呈报表
- [x] R3 导入包+隔离演练+生产 dry-run 报告+候选清单文件（生产库零写入，事后核验）
- [x] R4 夜间批：已登记「夜间执行清单」（不阻塞），夜窗补跑后闭合
- [x] R5 22 项逐项更新 146 §11 E（全部 DONE 无 WARN）
- [x] R6 六段呈报材料齐备（每段有命令输出/只读查询证据）
- [x] 总报告+_结果.json+README/55 登记；等待域清单呈用户
- [x] 零越权：未发布、未轮换、未 confirm、未写业务源库、未写生产平台库（只读查询+事后核验）、零 Git 写

### 164 §8 DoD 增量

- [x] R8：七批报告仅批次 A 部分完成（5 通过/2 失败+缺陷清单+最终结论「不通过（当前状态）」明确，零越权）——**其余批次未执行如实登记，交 checkpoint 续跑**（本项按诚实口径记 WARN，不虚标完成）
- [x] R2b/R9/R10 移交项：不在本计划执行（165/166 承接；164 §2 基线已作为 R2/E1 数据与 165 输入）
- [x] 163 PART A 原 DoD 8 项全过；总报告合并登记 README/55

## 11. 偏差与新发现（exceptions 汇总）

1. 15432 端口为 ai-hms E2E 外部转发（192.168.98.99:5432），非 8.83 隧道——本会话改用 15433 托管隧道。
2. 首轮全量 pytest 挂死于隧道中断（已定位并复跑全绿）——跑批规程教训。
3. backend/.env APP_DB_URL=10.20.1.153（旧 schema 平台库）≠生产 8.83 库——脚本误连风险已修复（自建 engine 显式 URL），对 .env 的信任度降级登记。
4. 生产 dry-run 首次误连 10.20.1.153：只读零副作用，已核验生产零写入后重做。
5. PORTAL 离院方式字典与 149 confirmed/JHEMR 三方语义冲突——隔离库按设计置 conflicted，归 W3 人工裁决。
6. code9 累计 69 例（远超 149 时代 2 例）且存在 1 例 HIS=9/JHEMR=非医嘱离院 的跨系统不一致实例——呈用户。
7. R6-4 unbound token 已清零（1 键已绑定、30 天未用）——161 P3-3 待办闭合。

## 12. 等待域清单（163 W1–W10 + 164 W11–W14，呈用户）

| # | 事项 | 前置 |
|---|---|---|
| W1 | 生产发布窗口：161 修复+149 种子生产导入+163/164 各批成果 | 主 AI 复核 git diff；用户授权 |
| W2 | ECG 生产 apply（档二） | 用户确认本报告 §4 dry-run；完整门禁链（备份→dry-run→apply→二次 dry-run 0→/health） |
| W3 | 值域 confirm：code9=其他、手术标志 3=期限手术、TREAT_RESULT 按 JHEMR 语义、PORTAL 离院方式行裁决（conflicted） | 用户人工 PATCH /confirm 或裁决废弃 |
| W4 | 凭据轮换（门诊 SSH/DRG/OA/ECG） | 用户授权运维窗口 |
| W5 | 127/130 余项（生产 Review 批准/配方 active/A25 视觉验收） | 用户逐项授权 |
| W6 | 150 医保课题申报（**外部截止 2026-09-04**） | 院方确认负责人/团队 |
| W7 | 160 职称执行包 | 信息科/DBA 执行 |
| W8 | 112 Phase D / 96 验收 / DRG 探索 / 154 B2+B3 / 157 ZFP / legacy 扩面 / 146 audit limit | 各自触发条件 |
| W9 | 人工复核池（142 56 草稿+40 停用配方、OA 5 草稿、曼陀罗 16、新输血 37、ECG 9 候选） | 人工复核流程 |
| W10 | 熔断语义优化（方案 C：区分现存用户首次纳管）——08:30 自动化推荐 | 用户裁决（可并入 153 后续） |
| W11 | ai-hms_qhd 缺陷修复（DEF-A-001/002/003 + 后续批次缺陷） | 该仓负责人（跨仓不越权） |
| W12 | 165/166 执行；生产建表 apply（165 X1）与首轮探查（X2） | 本报告多方核查+用户批准 |
| W13 | 治理工具（GE/Soda）引入审计 | 用户明确要求 |
| W14 | A1/A2/A3 业务侧整改（工号补录/报告回传/689 NULL） | 业务科室+信息科 |
| W15（新增建议） | R8 批次 A 补完与 B–G 批次（checkpoint：ai-hms_qhd/evidence/GUI-20260829-01/） | 下一续跑会话；测试患者甲～壬就绪确认 |
| W16（新增建议） | 10.20.1.153 旧平台库定位与 .env 治理（防误连） | 用户/负责人确认识别 |

## 13. 追加执行（2026-08-29 午后，用户授权"按规处理"）

### 13.1 W3 值域裁决（8 项，隔离库）

依据本报告 §3 E5/E5' 实证，经用户明示授权按规范处理：①DISCHARGE_DISPOSITION#1 冲突解除（采信 confirmed 1=医嘱离院，PORTAL 字典语义判错误、保留被否证据）；②code9 confirm=其他；③手术标志 3 confirm=期限手术（限期）；④TREAT_RESULT 五码按 JHEMR 语义 confirm（1治愈/2好转/3未愈/4死亡/9其他），含纠正 PORTAL 行 1=好转→治愈。全部走服务函数+版本链+证据+单条 w3_confirm 审计（operator=w3-user-delegated-20260829）。导出重生成：**confirmed 注入 26 条 / conflicted 0 / --check PASS**。生产库仍空，随 W1 发布窗导入（本报告 §12 W3 的"confirm 留人工"项就此闭合——裁决人=用户（授权），执行=AI（代操作））。

### 13.2 W10 熔断方案 C 落地并发布 8.83

- 代码：`_fetch_jhemr_existing_emp_nos`（管线计划阶段经 JHEMR adapter 批量存在性检查；**失败返回 None→全部按真实新建保守回退，绝不放宽门禁**）+ `_compute_change_stats(jhemr_existing=)` 分列 align_existing + `check_thresholds` 新维度 `max_align`（新 setting `identity_cb_max_align=150`，注释注明须低于 max_candidates=200 否则不可达）+ 熔断失败路径补写 report_summary（threshold+stats）便于诊断。
- 测试：`tests/test_identity_cb_align.py` 7 项（对齐不触发/超限触发 max_align/真新仍拦/ratio 排除对齐/旧 stats 兼容/分类重划/None 回退）。
- 门禁：identity 专项+**全量 1190 passed / 1 skipped / 0 failed**（24:49）。
- 发布（热修链，p153 同款）：代码包 tar→8.83 `/opt/data-asset/hotpatch/w10c-202608291223`→`docker build`（BASE=data-asset:p153-20260827224726）→镜像内冒烟（max_align=150；112 对齐场景不触发）→env 备份（backend.env.bak-w10c-20260829）→`run_data_asset_api.sh` 换容器→验证：**healthy、settings max_align=150、W10 符号可导入、333 路由无回归、日志净**。回滚：`DATA_ASSET_IMAGE=data-asset:p153-20260827224726` + 同脚本；旧镜像与 env 备份均在。
- **预期效果：今夜 02:00 cron 对 112 名 JHEMR 现存用户完成首次纳管对齐（08-28 方案 A 目标），max_candidates=200≥112、align 112<150、ratio=0。**
- 工作区 W10 代码改动（orchestrator/config/新测试）**未提交**（零 Git 写铁律），随下轮提交。

### 13.3 R4 排程与 R8 增量

- R4 夜窗补跑已排程今晚 22:00 一次性自动化（automation-a841ec6d，严格按 output_r163/r4_night_checklist.md），完成后 R4 转 DONE。
- R8 批次 A 增至 10/26（通过 7/失败 2/待人工 1）：新增 G-13 部分验证——空态正确+**DEF-A-004（P3）清空搜索不自动恢复需刷新**+分页控件竞态待人工；G-07 深链刷新通过。IAB 工具限制（标签轮次释放/点击可动性偶发超时）如实登记；报告 ai-hms_qhd/evidence/GUI-20260829-01/result-summary.md 已更新。


## 14. R8 移除（2026-08-29 用户指示）

用户明示「不需要再执行血透的」：R8 批自本节起终止——批次 A 已执行部分（10/26、缺陷 DEF-A-001~004、checkpoint）保留在 ai-hms_qhd/evidence/GUI-20260829-01/ 存档并移交该仓负责人（W11 保留）；B–G 与批次 A 补测不再执行；§10 DoD 的 R8 项改为「按用户指示终止（部分完成存档）」；W15 撤销。164 完成定义修订为 R0–R7+R4（R4 夜跑已排程）。
