> 类别：证据链
>
> 状态：**代码完成，待主 AI 复核与发布**（2026-08-28；执行依据=161 号计划 Q0→Q3 + 162 号交接执行包；HEAD=13ef9e8 全程未漂移）
>
> 关联：`verify/round-2/核查报告.md`（缺陷证据）、`162_平台升级核查缺陷修复交接执行文档.md`（执行放大器）、同号 `_结果.json`（机器可读）

# 平台升级核查缺陷修复·执行报告（161）

## 0. 执行概要

- 执行时间：2026-08-28（单会话一次性执行 Q0→Q3）
- 执行基线：HEAD=13ef9e8（与 162 交接包勘探时一致，全程零漂移）
- 环境：SSH 隧道 15432→8.83:5432（公钥通道）；`APP_TEST_DB_URL` 指向隔离测试库 data_asset_test（PostgreSQL 14.10，asset schema 101 表，无需重建）；凭据仅存会话环境变量，零落盘
- 变更文件（13）：后端 9（ai.py / auth.py / data_products.py / quality.py / db_connectors.py / query_parameter_validator.py / test_fine_grained_write_permissions.py / test_153_g1_g3.py / test_db_connectors_readonly.py / test_db_connectors_param_binding.py 之新增测试）+ 前端 4（relation-review / dict+identity sync-diffs / identity accounts）+ 新增前端测试 1 个文件

## 1. 逐批完成度（checkpoint 摘录）

### 【Q0 checkpoint】P0-1 + P0-2 — 全过

- 完成项：
  - P0-1 `relation-review/index.vue` 6 处绑定修复：`:385/:390/:397` 三个筛选 el-select `@change="loadRelations"`→`doSearch`；`:405` 关键词 `@keyup.enter/@clear`→`doSearch`；`:509` `@size-change="loadRelations"`→`() => loadRelations(1)`（`@current-change="loadRelations"` 按判定规则保留——首参是页码）。
  - P0-2 防回归测试两件：后端 `test_db_connectors_readonly.py::test_ssh_jump_remote_script_compiles`（源码级 findall 提取全部 `remote_script` 字面量逐个 `ast.parse`，锁 F1）；前端新增 `tests/plan161RelationReviewBindings.test.ts`（source-scan 断言四种直绑形态不存在 + doSearch/箭头包装在位，风格沿用 plan146StageA）。
- 验收：`pytest tests/test_db_connectors_readonly.py -q` → **9 passed**；`pnpm run typecheck` → **0 错**；`pnpm run test` → **27 文件 / 168 tests 全过**（较 round-2 基线 +1 文件 +2 tests，即新扫描测试）。
- 偏离：`tests/unit_standalone/`（他会话未跟踪产物）不在 conftest 纯逻辑豁免名单，免库冒烟不可行；改用豁免名单内的 `tests/security_audit` 冒烟（其中 1 失败 `test_ai_sql_execute_code_registered_and_granted` 为纯逻辑模式缺库的环境性失败，Q1 带库验收证实通过）。

### 【Q1 checkpoint】P1-1~P1-4 — 全过

- 完成项：
  - P1-1 pymssql 口径（取简版）：`db_connectors.py` pymssql 分支 fail-closed 断言 + `query_parameter_validator.py` sqlserver 分支风格冲突 warning（不阻塞）。
  - P1-2 `data_products.py` import_results 归因：`user = "system"` → `get_request_operator(request, default="system")`。
  - P1-3 `test_153_g1_g3.py` 新增 `test_a9_unique_violation_recognizes_real_pg_shapes`：psycopg 真实形态三断言（类名+pgcode 双命中 / 仅类名 / 非唯一负例）。
  - P1-4 `quality.py` A2 规则注释补 PG GROUP BY NULL 与 '' 不归并口径说明（行为不改）。
- 验收：`pytest tests/test_153_g1_g3.py tests/test_query_asset.py tests/security_audit/ tests/test_data_products.py -q` → **46 passed**。
- 偏离（执行中修正）：P1-1 初版断言条件过宽（`params 且无 %(` 即抛），全量门禁第一轮暴露误伤 153-A1 既有契约测试 3 个（它们用 `:name` 风格 + dict 透传）。收敛为「**SQL 含 `@name` 占位符**且带参且无 `%(`」才抛——恰好覆盖 round-2 P2 的真实冲突形态（144 校验器产出 @name、pymssql 只认 pyformat），A1 契约（`:name`）与无参资产零影响；并补正向门禁测试 `test_pymssql_rejects_tsql_named_binds_with_params`（断言拒绝发生在触碰驱动前）。

### 【Q2 checkpoint】P2-1~P2-3 — 全过

- 完成项：
  - P2-1 `identity/accounts/index.vue:189` doUnbind 确认文案 `row.account_code`→`row.account_id`。
  - P2-2 sync-diffs 动作 catch 统一走 `extractErrorDetail`：dict 页 `doSync`、identity 页 `doCollect/doSync/doReview` 共 4 处 + 两页新增 import（"前端同一页面一次触碰"已满足——两页本批一次改完）。
  - P2-3 `ai.py` tool-execute 权限码 `ai.sql.execute`→`ai.context.read`（附"未来新增真实执行工具时改挂 ai.sql.execute 并按工具名分流"注释）；`test_fine_grained_write_permissions.py:109` 期望码断言同步。
- 验收：`pnpm run typecheck`（0 错）+ `pnpm run test`（27/168 全过）；`pytest tests/security_audit/ tests/test_ai_collab.py -q` → **22 passed**。
- 偏离：P2-2 严格按 161 计划点名函数族（doSync/collect/review）执行；identity 页 updateStatus/doHisSync/doProposeMaster/batchPropose/batchSetStatus 与 dict 页 updateStatus 等未点名 catch 未动，避免超范围。

### 【Q3 checkpoint】P3-1~P3-3 + 全量门禁 — 完成（含 1 项新发现，见 §4）

- 完成项：
  - P3-1 `ai.draft.execute` 死码登记（`_结果.json`）：全库 grep 仅 `permissions.py:110`（目录条目"AI 草稿只读执行(旧)"）与 `:237`（角色授权清单）引用，无任何端点挂载——只登记不删。
  - P3-2 `auth.py` `_require_csrf` docstring 补 X-Requested-With 旁路既定前提说明（分歧表#15 裁决：文档化不改行为）。
  - P3-3 unbound token 运维待办写入 `_结果.json`（本会话零生产操作）。
- 全量门禁见 §5。

## 2. P1-P12 修复状态表

| 编号 | 严重度 | 状态 | 修复位置 | 修复后验证证据 |
|---|---|---|---|---|
| P1 | 高 | **已修复** | relation-review/index.vue :385/:390/:397/:405/:509 | 静态 grep `@change="loadRelations"`=0、其余直绑=0；`plan161RelationReviewBindings.test.ts` 2 tests 过；vitest 168 全过 |
| P2 | 中 | **已修复** | db_connectors.py pymssql 分支 fail-closed；query_parameter_validator.py warning | `test_pymssql_rejects_tsql_named_binds_with_params` 过；A1 契约 8 tests 全过（零误伤）；带参 sqlserver 资产现状 grep 确认无 @name 资产 |
| P3 | 低 | **已修复** | data_products.py:189 get_request_operator | grep `user = "system"`=0；test_data_products 在 Q1 46-pass 与全量 1175-pass 中全过 |
| P4 | 低 | **已修复** | identity/accounts/index.vue:189 account_id | typecheck 0 错；vitest 全过 |
| P5 | 低 | **保留（裁决）** | — | 分歧表#12：same_sql/sha256 为 144"可比对不可读"设计，登记不改 |
| P6 | 低 | **已修复** | test_153_g1_g3.py 新增真实 PG 形态测试 | `test_a9_unique_violation_recognizes_real_pg_shapes` 过（Q1 46-pass 内） |
| P7 | 低 | **已修复（仅注释）** | quality.py A2 规则注释块 | 行为不变；Q1/全量 quality 相关测试全过 |
| P8 | 低 | **已修复** | ai.py:505 ai.context.read + 注释；test_fine_grained_write_permissions.py:109 同步 | security_audit+ai_collab 22 passed；静态 grep :109 为新码 |
| P9 | 低 | **已修复（仅文档）** | auth.py _require_csrf docstring | 行为不变 |
| P10 | 低 | **已登记** | `_结果.json` 待办段 | unbound token 生产核查列为运维待办，本会话零生产操作 |
| P11 | 低 | **已修复** | dict/sync-diffs doSync；identity/sync-diffs doCollect/doSync/doReview | extractErrorDetail import ×2；typecheck 0 错；vitest 全过 |
| P12 | 低 | **已登记** | `_结果.json` 待办段 | 146 域 audit-logs summary limit，按边界不夹带，登记至 55/结果文件 |

## 3. 跳过项与原因

| 项 | 原因 |
|---|---|
| P5 same_sql/sha256 掩码 | 分歧表#12 裁决保留（改 null 反而破坏 diff 语义） |
| P12 audit_logs_summary limit | 属 146 域，161 §7 明确不夹带；已登记 55 待办池 |
| X-Requested-With 收紧 | 分歧表#15 裁决文档化（破坏非浏览器工具），仅 P3-2 注释 |
| A2 COALESCE 归并 | 分歧表#14 裁决接受口径微差，仅 P1-4 注释 |
| unbound token 绑定/停用 | 运维决策，仅登记（P3-3） |
| identity/dict 页未点名 catch（updateStatus/doHisSync/batch 等 7 处） | P2-2 计划仅点名 doSync/collect/review 函数族；不动以免超范围 |
| 其余 5 个 usePagedList 分页页的事件绑定 | 162 §2.2 判定：el-pagination 事件首参=页码，语义正确，保留 |

## 4. 新发现（未夹带修复，留主 AI/下一次核查裁决）

1. **plan127 s0 契约测试与 36b8743 F4 修复冲突（既有断裂，非本轮引入）**
   - 现象：全量门禁唯一失败 `tests/plan127/test_s0_contract_unit.py::test_table_list_system_filter_accepts_his_alias`——断言 `tables/index.vue` handleTreeClick 段内含 `await loadData()`。
   - 取证：`git show 36b8743` 显示该文件 `- await loadData();` → `+ await loadData(1);`（F4 树点击重置页码修复）；plan127 测试文件自 d3e6b55 未再改动；两文件 `git diff HEAD` 为空——**该失败在本轮开工前的 HEAD 即存在**。
   - 未复现原因：round-2 回归（1152/0F）跑在 36b8743 落库之前；36b8743 之后至本轮之前无人补跑全量（55 待办"153 完成后补跑全量"即指此事）。
   - 建议裁决方向：plan127 s0 断言更新为接受 `await loadData(1)`（F4 是经复核并已发布的新语义，向后覆盖旧契约）；不建议回退 F4（会重新引入丢页重置缺陷）。
2. **A1 契约与 P2 的口径细节**：153-A1 的 pymssql 契约测试使用 `:name` 占位符风格 + dict 透传（Fake 驱动层断言），而真实 pymssql 仅认 `%(name)s` pyformat；P1-1 断言因此收敛到 `@name` 冲突形态而非"一切无 `%(` 带参"。该口径已在代码注释与门禁测试中固化，如后续统一 sqlserver 占位符口径（如全面改 pyformat）需连带调整 A1 测试与 144 校验器。
3. **轻微**：`tests/unit_standalone/`（未跟踪，他会话产物）已在全量收集中执行且全绿，但不在 conftest `_PURE_LOGIC_SUBDIRS` 豁免名单内——免库环境跑 `pytest tests/unit_standalone` 会被门禁拦下；如需免库可执行，后续会话可把它加进豁免名单（本轮不动他会话产物）。
4. **轻微**：F1 防回归锁现存在两份——`test_db_connectors_param_binding.py::test_ssh_jump_remote_script_is_valid_python`（36b8743 加，inspect.getsource 版）与本轮 `test_db_connectors_readonly.py::test_ssh_jump_remote_script_compiles`（源码 findall 版，覆盖全部脚本字面量）。互补不冲突，保留。

## 5. 全量门禁输出摘录（Q3 末）

```text
后端：.\.venv\Scripts\python.exe -m pytest tests/ -q
  → 1 failed, 1175 passed, 1 skipped, 7 warnings in 1487.80s (0:24:47)
  唯一 failed = tests/plan127/test_s0_contract_unit.py::test_table_list_system_filter_accepts_his_alias
  （§4 新发现#1：既有断裂，36b8743 F4 与 plan127 旧契约漂移，非本轮改动引入；
   本轮首轮全量曾为 4 failed/1171 passed，其中 3 个 pymssql 误伤已按 §1 Q1 偏离记录修复后复跑）

前端：
  pnpm run typecheck  → tsc --noEmit && vue-tsc --noEmit --skipLibCheck，0 错
  pnpm run test       → Test Files 27 passed (27)；Tests 168 passed (168)
  pnpm run build      → gzip 预算 PASS：main 656.0KB≤700 / graph 404.2KB≤430 / css 98.1KB≤110

静态复核：
  grep -c '@change="loadRelations"' relation-review/index.vue → 0
  grep -cE '@(size-change|keyup\.enter|clear)="loadRelations"' … → 0
  grep -c 'user = "system"' data_products.py → 0
  test_fine_grained_write_permissions.py:109 → tool-execute 期望 {"ai.context.read"}
  ai.py:505 → dependencies=[Depends(require_permission("ai.context.read"))]
```

## 6. 边界遵守声明

零 alembic 迁移；零部署；零 Git 写操作（工作区原有脏文件原样保留）；零业务源库连接（仅 SSH 隧道→隔离测试库 data_asset_test 与服务器 env 文件一次只读 grep）；不删不改兼容路径与旧表；126/144/146/149/151/152/153 既有语义未改（新增测试均为追加）；零新第三方依赖；`ai.draft.execute` 只登记；未为通过测试删断言/弱化门禁（P1-1 断言收敛是修正误伤、目标场景 `@name`+带参仍 fail-closed 并新增正向门禁测试）；`verify/` 与 `review/` 只读未动；凭据零落盘。
