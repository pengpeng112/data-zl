> 类别：模块规划
>
> 状态：**交接执行包（2026-08-28 生成）**。上会话 AI 已完成全部勘探与环境实测，本文档供新会话 AI **一次性执行** 161 号计划 Q0→Q3，无需再次探索。
>
> 唯一执行依据：`161_平台升级核查缺陷修复一次性执行计划.md`（定稿）；本文档是其执行放大器——所有 file:line 已按当前 HEAD=13ef9e8 逐点复核，环境通道已实测。**若 161 正文与本文档实测有出入（行号偏移、文件名、路径），以本文档为准**。
>
> 证据链：`verify/round-2/核查报告.md`（P1-P12 证据；其 §5 记录用户已拍板**全部修复**，无需再确认范围）、`verify/round-2/分歧表.md`（18 项裁决；只读，禁改）。

# 平台升级核查缺陷修复·交接执行文档（162）

## 0. 执行方式（先读这段）

1. 新会话 AI 按序读：根目录 `AGENTS.md` → `开发起步包/161_...执行计划.md` → 本文档 → 开工。**§2 已核实事实禁止重新勘探**（已逐一 grep/sed 验证过，重复勘探浪费时间且可能误判）。
2. 节奏：严格 Q0→Q1→Q2→Q3，每批跑完 §3 对应验收命令、全过才进下一批；每批结束输出 checkpoint（完成项 / 验收输出摘录 / 偏离记录）。中断后从最近 checkpoint 续跑。
3. 铁律：除 §6 硬边界列出的禁止事项外，一切小偏差（行号再偏移几行、文案差异、基线数不完全等于 1152）**就地消化并在 checkpoint 记录后继续**，不停工、不向用户提问、不扩大或缩小范围。
4. 全部完成才算结束，不得只做一部分后自行收口。

## 1. 环境准备（2026-08-28 实测结论 + 操作命令；仅做一次）

### 1.1 SSH 隧道（通道已实测可用）

- 实测：`ssh -i C:/Users/Administrator/.ssh/id_ed25519_ai -o BatchMode=yes root@10.10.8.83 "echo SSH_OK"` 返回 `SSH_OK`（公钥通道正常，无需输密码）；本机端口 15432 当前**无**残留监听（netstat 已查）。
- 建隧道（后台常驻）：
  ```bash
  ssh -i C:/Users/Administrator/.ssh/id_ed25519_ai -fN -L 15432:127.0.0.1:5432 root@10.10.8.83
  ```
- 验证：`netstat -an | grep 15432` 出现 LISTENING 即通。

### 1.2 测试库连接串（凭据红线，逐字遵守）

- 连接串只从服务器取：`ssh -i C:/Users/Administrator/.ssh/id_ed25519_ai root@10.10.8.83 'grep ^APP_DB_URL /etc/data-asset/backend.env'`，把其中库名部分换成 `data_asset_test`，得到 `APP_TEST_DB_URL`（指向 127.0.0.1:15432 的隔离测试库）。
- **凭据只进当前会话环境变量**（PowerShell：`$env:APP_TEST_DB_URL = "<串>"`）。禁止写入任何文件、日志、报告、JSON、Git；报告里只允许出现"隧道 15432 → data_asset_test"这类无口令描述。

### 1.3 测试库 schema 体检与重建（按需）

- conftest 硬约束：`backend/tests/conftest.py:44-47` 要求环境变量 `APP_TEST_DB_URL`，且**显式拒绝** `APP_DB_URL`（防止误连生产）。
- 若测试报大量缺表（并行会话互踩过 schema）：对测试库执行 `DROP SCHEMA asset CASCADE; CREATE SCHEMA asset;`，然后在 backend 目录以 `APP_DB_URL=<测试库串>` 跑 `.\.venv\Scripts\python.exe -m alembic upgrade head` 重建（迁移到 b0c1d2e3f4a5）。无本机 psql 时可 `ssh root@10.10.8.83` 远端 psql，或用 backend venv 的 python + psycopg 临时脚本（脚本不落盘或用完即删）。

### 1.4 pytest 进程清查（今日发生过并行会话僵尸 pytest 干扰测试库）

- 全量跑之前用 PowerShell 清点并终止**非本会话**的 python pytest 进程：
  ```powershell
  Get-CimInstance Win32_Process -Filter "Name='python.exe'" |
    Where-Object { $_.CommandLine -match 'pytest' } |
    Select-Object ProcessId, CommandLine
  ```
- 长跑（全量约 27 分钟）期间可开守护循环周期复查，发现外部新拉起的 pytest 再清。

### 1.5 基线与冒烟

- 免库冒烟：`backend/tests/unit_standalone/` 子目录不连库（conftest.py:12 有豁免注释），可先 `.\.venv\Scripts\python.exe -m pytest tests/unit_standalone -q` 验证 venv 健康（实测 venv 为 Python 3.14.4）。
- 全量基线参考：round-2 核查实测 **1152 passed / 1 skipped / 0 failed（27m12s）**。注意：当前工作区有未提交的 `backend/tests/unit_standalone/` 新测试（§2.3），全量数字可能 **大于** 1152；验收标准是 **0 failed 且新增测试全过**，不是精确等号。

### 1.6 前端

- **必须 pnpm**（仓库 preinstall 强制 only-allow pnpm；npm/yarn 会失败）。实测 pnpm 11.9.0、`frontend/node_modules` 已装好，一般无需 install；如需要则 `pnpm install`。

## 2. 当前代码事实（2026-08-28 实测，HEAD=13ef9e8；以下行号可直接用）

### 2.1 缺陷点现状总表（P1-P12；全部仍开放=未修，无一遍已被 36b8743 提前修掉）

| 编号 | 现状 | 精确位置与修法落点 |
|---|---|---|
| P1（高） | **未修** | `frontend/src/views/asset/relation-review/index.vue`：`:385/:390/:397` 三个 el-select `@change="loadRelations"`；`:405` el-input `@keyup.enter="loadRelations" @clear="loadRelations"`；`:509` el-pagination `@size-change="loadRelations"`（同页 `@current-change="loadRelations"` 语义正确保留）。`doSearch` 已在该页 `:80` 附近从 usePagedList 解构好，语义=重置 page=1 再查（`frontend/src/composables/usePagedList.ts:62`） |
| P2（中） | 未修 | 校验器 `backend/app/services/query_parameter_validator.py:37`（sqlserver/mssql/tsql 分支按 `@name` 提取）；执行侧 `backend/app/services/db_connectors.py` pymssql else 分支 `~:604-609`（`cursor.execute(sql, params or {})` 前插断言） |
| P3（低） | 未修 | `backend/app/api/v1/data_products.py:189` `user = "system"`；`request` 参数已在函数签名（:187 附近）；助手 `get_request_operator` 在 `backend/app/core/security.py:80` |
| P4（低） | 未修 | `frontend/src/views/identity/accounts/index.vue:189` `row.account_code \|\| row.id` → 改 `account_id`（表格列 `prop="account_id"` 在 `:52`） |
| P5 | 裁决保留 | 分歧表#12：same_sql/双端 sha256 是 144"可比对不可读"设计，**不改不掩**，161 结果文件登记即可 |
| P6（低） | 未修 | `backend/app/services/intake_version_retry.py:15-21` `_is_unique_violation`（pgcode 23505 / 类名 UniqueViolation 两路径）；现有假 pgcode 测试在 `backend/tests/test_153_g1_g3.py:123-125` |
| P7（低） | 未修 | `backend/app/api/v1/quality.py:242` `_run_rule_col_null_comment`，注释块在 `:245-247`；只加注释不改行为 |
| P8（低） | 未修 | `backend/app/api/v1/ai.py:906-910` tool-execute 现挂 `require_permission("ai.sql.execute")` → 改挂 `ai.context.read`；期望码断言唯一在 `backend/tests/security_audit/test_fine_grained_write_permissions.py:107` |
| P9（低） | 未修 | `backend/app/api/v1/auth.py:75` `_require_csrf`，X-Requested-With 判断在 `:82`；只加注释 |
| P10（低） | 待办登记 | unbound token 运维确认项，写入 161 结果文件，不做任何生产操作 |
| P11（低） | 未修 | `frontend/src/views/dict/sync-diffs/index.vue`（3 处 catch）与 `frontend/src/views/identity/sync-diffs/index.vue`（9 处 bare catch：`:399,:415,:433,:446,:459,:472,:482,:495,:528`）；两页目前 **0 处** import `extractErrorDetail`（helper 在 `frontend/src/utils/errorMessage.ts:55`），需补 import |
| P12（低） | 只登记 | `backend/app/api/v1/governance.py:354` audit_logs_summary 无 limit——146 域背景项，**本计划不改**，登记至结果文件/55 待办池 |

### 2.2 对 161 计划正文的更正（以本节为准）

1. **测试文件名**：161 计划 P2-3 写的 `test_ai_execution_and_context_routes_permissions` **不存在**；tool-execute 期望码断言全库唯一在 `backend/tests/security_audit/test_fine_grained_write_permissions.py:107`：
   `assert route_permissions.get(("post", "/tool-execute")) == {"ai.sql.execute"}` → 同步改为 `{"ai.context.read"}`。
2. **页面路径**：无 `asset/` 前缀——`frontend/src/views/identity/accounts/`、`frontend/src/views/dict/sync-diffs/`、`frontend/src/views/identity/sync-diffs/`（relation-review / relation-rates / tables 三个在 `views/asset/` 下）。
3. **其余 5 个 usePagedList 页扫描结论**（P0-1 的"全试点页同模式扫描"已完成，勿重扫）：
   - `asset/tables:134`、`identity/sync-diffs:181`、`asset/relation-rates:113` → 均为 el-pagination `@current-change="loadData/loadXxx"`，首参=页码，**语义正确，保留**；
   - `dict/general:68/:121/:167`、`dict/sync-diffs:97` → el-pagination 统一 `@change="loadXxx"`。element-plus 实测版本 **2.11.5**，该统一事件签名为 `(currentPage, pageSize)`，首参=页码 → 按 161 判定规则"首参是页码的保留"，**本轮不动**（改 `() => loadXxx(1)` 属 size-change 重置增强，超出本计划最小面，不做）；
   - 因此 P0-1 实际只改 relation-review 一页 6 处绑定（见 §3 Q0）。

### 2.3 工作区脏文件（与本任务无关；禁改、禁提交、禁 checkout/stash）

- 已修改（M）：`backend/scripts/harvest_oracle_readonly.py`、`backend/scripts/harvest_outpatient_mysql_ssh.py`、`tools/analyze_newsrc7_view_relations.py`、`tools/fancyhis_attr_extract.py`、`开发起步包/55_…`、`开发起步包/README.md`、`开发起步包/157_…`、`开发起步包/158_…×2`、`开发起步包/_archive/README.md`、`开发起步包/数据资产_新HIS逆向资产包/*.json`
- 已删除（D）：`开发起步包/155_…`、`开发起步包/156_…`（系改名重编号为 159/160，未提交）
- 未跟踪（??）：`T`、`backend/tests/unit_standalone/`、`verify/`、`开发起步包/150_…`（docx/wps/py）、`159_…`、`160_…`、`161_…`
- 规则：161 执行只新增/修改本任务涉及文件；**零 Git 写操作**（不 add/commit/push/checkout/stash）；以上脏文件保持原样，交给用户与后续会话处理。

## 3. Q0→Q3 执行卡

### Q0 = P0-1 事件绑定 + P0-2 两个防回归测试

**P0-1** `frontend/src/views/asset/relation-review/index.vue`（共 6 处）：

| 行 | 现状 | 改为 |
|---|---|---|
| :385 | `<el-select … @change="loadRelations">` | `@change="doSearch"` |
| :390 | 同上（confidence 筛选） | `@change="doSearch"` |
| :397 | 同上（review_status 筛选） | `@change="doSearch"` |
| :405 | `@keyup.enter="loadRelations" @clear="loadRelations"` | 两个都改 `doSearch` |
| :509 | `@size-change="loadRelations"` | `@size-change="() => loadRelations(1)"` |
| :509 | `@current-change="loadRelations"` | **保留不动**（首参=页码，语义正确） |

判定规则（写进扫描测试的注释里）：模板事件首参是"选中值/事件对象"→ 必须改 `doSearch` 或箭头包装；首参是"页码"→ 保留。

**P0-2** 两件防回归测试（161 §2 P0-2，锁两个"测试盲区"）：

1. 后端：`backend/tests/test_db_connectors_readonly.py` 追加 `test_ssh_jump_remote_script_compiles`——用 `pathlib` 读 `backend/app/services/db_connectors.py` 源码，正则 `re.search(r'remote_script\s*=\s*r?"""(.*?)"""', src, re.S)` 提取 `:159` 的内嵌远端脚本，`ast.parse` 断言可编译（防 F1 类缩进损坏复发）。
2. 前端：`frontend/tests/` 新增 source-scan 测试（**风格照抄 `frontend/tests/plan146StageA.test.ts`** 的 readFileSync 读源码断言写法）：断言 relation-review/index.vue 源码中不存在 `'@change="loadRelations"'`、`'@size-change="loadRelations"'`、`'@keyup.enter="loadRelations"'`、`'@clear="loadRelations"'` 四种形态。

**Q0 验收**：`pnpm run typecheck && pnpm run test`（前端双过、新测试过）；`.\.venv\Scripts\python.exe -m pytest tests/test_db_connectors_readonly.py -q`（后端新测试过）。

### Q1 = P1-1 ~ P1-4（后端 4 项）

1. **P1-1 pymssql 占位符口径（取简版：执行侧断言 + 校验器 warning）**
   - `db_connectors.py` pymssql else 分支（~:604-609）在 `cursor.execute(sql, params or {})` 前加：
     `if params and "%(" not in sql: raise ValueError("pymssql 连接需 %(name)s 占位符风格绑定参数；当前 SQL 使用其他占位符，请修订（背景：verify/round-2 P2）")`
   - `query_parameter_validator.py:37` sqlserver 分支：提取 `@name` 的同时，若 SQL 含 `%( ` 则记 warning 日志（不阻塞、不报错）。
   - 动手前 `grep -rn "%(" backend/app --include="*.py" | grep -i sqlserver` 与资产侧快速确认：现有 sqlserver 资产均为无参形态，断言零误伤（round-2 已核实无带参 sqlserver 资产在用）。
2. **P1-2 import_results 归因**：`data_products.py:189` `user = "system"` → `user = get_request_operator(request, default="system")`；顶部确认已 import（`from app.core.security import get_request_operator` 或对齐现有 import 风格）。
3. **P1-3 A9 真实 PG 形态断言**：`backend/tests/test_153_g1_g3.py` 追加一条测试——在测试内定义 `class UniqueViolation(Exception): pass`（模拟 psycopg 真实类名形态），构造两个变体的 mocked IntegrityError：①orig 带 `pgcode="23505"`（真实 PG 两种属性都有）；②orig 仅有 UniqueViolation 类名、无 pgcode 属性（ getattr 默认 None 路径）。断言 `_is_unique_violation` 对两变体均 True。**保留**现有假会话测试不动。
4. **P1-4 A2 NULL/"" 口径注释**：`quality.py:245-247` 注释块追加一句："PG GROUP BY 下 NULL 与 '' 分组不归并（与旧内存归并口径存在微差）；如同表 system/source 混用 NULL/'' 需归并，改用 COALESCE——分歧表#14 裁决接受微差"。**行为不改**。

**Q1 验收**：`.\.venv\Scripts\python.exe -m pytest tests/test_153_g1_g3.py tests/test_query_asset.py tests/security_audit/ tests/test_data_products.py -q` 全过。

### Q2 = P2-1 ~ P2-3（前端 2 项 + 权限码 1 项）

1. **P2-1** `frontend/src/views/identity/accounts/index.vue:189`：`row.account_code || row.id` → `row.account_id || row.id`。
2. **P2-2 sync-diffs 文案统一**：dict、identity 两页的 doSync/collect/review 类动作的 catch 固定文案 → `extractErrorDetail(error, "…")`（保留各自动作原 fallback 文案作第二参）。两页需新增 `import { extractErrorDetail } from "@/utils/errorMessage"`（对齐仓内其他页面 import 写法）。identity 页 9 处 bare catch 行号见 §2.1 P11；**按"动作 catch"逐个对号，列表加载的 catch 已符合 E7/E8 语义则不动**。遵守"前端同一页面一次触碰"：本批把这两页一次改完，Q3 不再碰前端。
3. **P2-3 tool-execute 权限码（用户特别裁决）**：
   - `backend/app/api/v1/ai.py:906-910`：`dependencies=[Depends(require_permission("ai.sql.execute"))]` → `require_permission("ai.context.read")`；在函数 docstring 或装饰器上方加注释："当前 AVAILABLE_TOOLS 全部为只读组包工具，故整端点挂 ai.context.read；未来新增真实执行类工具时，改挂 ai.sql.execute 并按工具名分流"。
   - 同步 `backend/tests/security_audit/test_fine_grained_write_permissions.py:107` 期望码 → `{"ai.context.read"}`（全库唯一断言点，已核实）。

**Q2 验收**：`pnpm run typecheck && pnpm run test`；`.\.venv\Scripts\python.exe -m pytest tests/security_audit/ tests/test_ai_collab.py -q`。

### Q3 = P3-1 ~ P3-3（登记项）+ 全量门禁

1. **P3-1 死权限码登记**：`grep -rn "ai.draft.execute" backend/ frontend/src/` 确认仅权限目录/矩阵引用、无端点挂载 → 作为 D5 式登记写入 161 `_结果.json`（**只登记不删**）。
2. **P3-2 CSRF 前提文档化**：`backend/app/api/v1/auth.py:82` 上方或 `_require_csrf`（:75）docstring 补注释："X-Requested-With 任意非空值跳过 Origin 校验为既定前提（前端 axios 默认携带该头；本防护面向无自定义头的表单/工具类请求）——分歧表#15 裁决：文档化、不改行为"。
3. **P3-3 unbound token 运维待办**：写入 161 `_结果.json` 待办段：生产库查 `asset_api_keys` 中 unbound 且 `last_used_at` 近期使用者，决定绑定角色或停用（**本会话不做任何生产查询/操作**）。
4. 全量门禁：见 §4。

## 4. 全量门禁（Q3 末，全绿才算完成）

```powershell
# 前置：§1 隧道已建、$env:APP_TEST_DB_URL 已设、外部 pytest 已清
cd F:\python\数据资产\backend
.\.venv\Scripts\python.exe -m pytest tests/ -q        # 0 failed；新增测试全过（参考基线 1152 passed/1 skipped，可能略高）
cd F:\python\数据资产\frontend
pnpm run typecheck                                    # tsc + vue-tsc 双过、0 error
pnpm run test                                         # Vitest 全过、零回归（round-2 基线 166 tests）
pnpm run build                                        # 构建成功、gzip 预算 PASS
```

静态复核（逐条贴进执行报告）：

```bash
grep -c '@change="loadRelations"' frontend/src/views/asset/relation-review/index.vue   # 必须 = 0
grep -c '@size-change="loadRelations"\|@keyup.enter="loadRelations"\|@clear="loadRelations"' frontend/src/views/asset/relation-review/index.vue  # = 0
grep -c 'user = "system"' backend/app/api/v1/data_products.py                          # 必须 = 0
grep -n 'ai.context.read' backend/tests/security_audit/test_fine_grained_write_permissions.py  # :107 已是新码
```

## 5. 产出物（完成后落盘，缺一不可）

1. `开发起步包/161_平台升级核查缺陷修复_执行报告.md`：
   - 逐批完成度（Q0-Q3 checkpoint 摘录）；
   - P1-P12 修复状态表：每项含 修复后验证证据（文件：行 + 测试名 + 验收输出摘录）；
   - 跳过项与原因（P5/P12 不修只登记；§2.2 第 3 条保留的 5 页绑定）；
   - 新发现（执行中发现的新缺陷只登记、不夹带修复）；
   - 门禁输出摘录（四条静态复核 grep + pytest/tail 数字 + typecheck/test/build 结果）。
2. `开发起步包/161_平台升级核查缺陷修复_结果.json`（机器可读：批次/缺陷/文件/测试/门禁结果/登记项 P3-1、P3-3/P12 待办；`ensure_ascii=False, indent=2`；**禁止包含任何凭据**）。
3. `开发起步包/README.md`「目录更新记录」追加一行（格式 `| 日期 | 操作 | 内容 |`，最新行在表头下第一行）：`161 号计划 Q0→Q3 一次性执行完成（代码完成，待主 AI 复核与发布）…`。
4. `开发起步包/55_系统未完成事项统一执行计划.md` 顶部 📌 区追加一行：`161 号核查缺陷修复 Q0→Q3 代码完成，待主 AI 复核与发布；遗留待办：unbound token 运维确认、146 audit limit（均登记于 161 结果文件）`。
5. 完成后在 README 的 P0 分组（:69 附近）把 161 行状态改为"已执行待复核"，并登记本 162 文档行（若尚未登记）。

## 6. 硬边界（161 §1 七条 + 本仓补充；违反任何一条=执行失败）

1. 零 alembic 迁移（不改不动迁移文件）；
2. 零部署、零业务源库连接（测试库=127.0.0.1:15432 隧道，仅此一个库）；生产 8.83 只允许 §1.2 的 ssh 读 env 文件这一只读动作；
3. 零 Git 写操作（不 add/commit/push/checkout/stash；§2.3 脏文件保持原样）；
4. 不删不改兼容路径与旧表；不改 126/144/146/149/151/152/153 既有语义（本计划是 153 缺陷收尾）;
5. 不引入新第三方依赖；
6. `ai.draft.execute` 只登记不删；
7. **禁止为通过测试删断言、弱化门禁、加白名单或跳过脱敏**；
8. 不修改 `verify/` 与 `review/` 下任何评审产物（只读）；
9. 前端同一页面一次触碰（relation-review 在 Q0 一次改完；两 sync-diffs + accounts 在 Q2 一次改完）；
10. 凭据/患者敏感信息不进任何输出。

## 7. Checkpoint 纪律与中断续跑

- 每批结束输出：`【Qn checkpoint】完成项：…｜验收：…（命令+关键输出行）｜偏离：…（无则写"无"）`。
- 某批验收失败：停在当批修复到全过为止，**不带病进下一批**。
- 会话中断：新会话按最近 checkpoint 续跑（本文档 §2 事实与行号仍有效，除非 HEAD 变化——续跑前先 `git log --oneline -1` 确认仍是 13ef9e8，若已变则用 §2 的内容定位法重新对行号，不重做勘探）。

## 8. 背景速览（无需动作，防混淆）

- 上会话（2026-08-28）已完成 **另一仓库** `F:/datart/datart-1.0.0-rc.3` 的编号 37 计划 PART A（三甲指标按专业接入，产物 `tools/indicator-import/output/ch3-v2/` 13 项 + 38 项 Python 测试全过；PART B 等待域见该目录 `db-waiting-list.md`）。该仓任务与本 161 任务**零代码依赖**，执行本草时不要进入 datart 仓库改动任何文件。
- 153 号八批整改与 156 号子任务已随 36b8743 发布生产（8.83）；本 161 计划是其核查收尾，**发布仍由主 AI 另行执行**。
