> 类别：实施证据
> 状态：已完成（2026-07-15 二次复核：全库只读探测、完整备份、全量测试及生产固化已完成）

# 92 一级业务系统命名、资产导入与空表治理 — 实施交付报告

对应历史执行计划：`_archive/90_一级业务系统命名资产导入与空表治理整改执行计划.md`。
权威环境：`10.10.8.83` 平台库 `data_asset` / 容器 `data-asset-api`。
业务源库写操作：**0**。

## 1. 总体结论

| 项 | 结果 |
|---|---|
| 十个一级业务系统平行展示 | 已落地；名称唯一来源 `asset.asset_systems.system_name_cn` |
| 取消「其他业务系统」大类 | 前后端分类与目录树均已去除 |
| 数据中心镜像 vs 独立源端 | ODS 别名归 `DATA_CENTER`；独立 LIS/PACS/Docare/JHEMR/移动护理为平行一级 |
| Alembic `b4c5d6e7f8a9` | 测试库 up→down→up 通过；生产仅 upgrade 至 head |
| 系统归一 dry-run/apply/二次 dry-run | apply writes=8；三次 dry-run writes=0（幂等） |
| 前端 typecheck / Vitest / build | 通过（tsc+vue-tsc；43 tests；build OK） |
| 后端单元测试（plan90 分类/空表/导入） | 20 passed（`--noconftest`） |
| 生产 API/目录验收 | 容器内路由函数验收 ACCEPT_OK；health 200；首页 200 |
| 业务源库 DML/DDL | **0** |

## 2. 修改与新增文件（摘要）

### 后端

- `app/services/asset_catalog.py` — 十系统口径、`normalize_system_code`、`list_first_level_systems`
- `app/services/row_presence.py` — 空表状态词汇与限 1 探测 SQL
- `app/services/asset_import_upsert.py` — 幂等 upsert，不覆盖人工确认中文名
- `app/api/v1/systems.py` — 系统列表与资产树按业务系统→连接→Schema
- `app/api/v1/tables.py` — 目录排除 `confirmed_empty`
- `app/models/asset.py` / `asset_system.py` — `row_presence_*`、`canonical_system_code`
- `alembic/versions/b4c5d6e7f8a9_row_presence_and_system_catalog.py`
- `scripts/import_explored_sources_to_platform.py` — 删除整源再插入 → upsert；82 号 glob
- `scripts/reconcile_plan90_catalog.py` — 系统归一 dry-run/apply
- `tests/test_plan90_*.py`、`tests/test_system_classification_unit.py` 重写

### 前端

- `src/views/asset/tables/hierarchy.ts` — 十系统平行；禁止「其他业务系统」
- `src/views/asset/tables/index.vue` — 业务系统→连接→Schema 树
- `src/api/asset.ts` — 树节点字段扩展
- `tests/hierarchy.test.ts`

## 3. Alembic

| 库 | 操作 | 结果 |
|---|---|---|
| `data_asset_test`（8.83） | upgrade → downgrade -1 → upgrade | `a3b4c5d6e7f8` ↔ `b4c5d6e7f8a9` 往返成功 |
| `data_asset` 生产 | **仅** `upgrade head` | `b4c5d6e7f8a9 (head)` |

## 4. 十系统标准名与旧码迁移

| system_code | system_name_cn | 备注 |
|---|---|---|
| HIS_SOURCE | HIS | 一级 |
| HRP | HRP | 一级 |
| DATA_CENTER | 数据中心 | 一级；含 ODS 镜像 Owner |
| JHEMR_VASTBASE | 嘉和电子病历 | 一级 |
| DOCARE | Docare手术麻醉 | 一级 |
| MOBILE_NURSING | 移动护理 | 一级 |
| LIS_SOURCE | LIS | 一级 |
| PACS_SOURCE | PACS | 一级 |
| PAPERLESS_CDMS | 无纸化病案 | 一级 |
| ULTRASOUND_ENDOSCOPY | 超声内镜 | 一级 |

旧码软合并（保留历史引用）：

| 旧码 | status | canonical_system_code |
|---|---|---|
| EMR | merged | JHEMR_VASTBASE |
| LIS | merged | LIS_SOURCE |
| PACS | merged | PACS_SOURCE |
| SM | merged | DOCARE |

源连接重写（示例）：`ods_lis/ods_pacs/ods_emr/ods_sm` → `DATA_CENTER`。

## 5. 导入前后表规模（生产当前）

| system_code | 表数 |
|---|---:|
| DATA_CENTER | 865 |
| HIS_SOURCE | 1236 |
| HRP | 67 |
| JHEMR_VASTBASE | 1421 |
| DOCARE | 646 |
| MOBILE_NURSING | 567 |
| LIS_SOURCE | 1254 |
| PACS_SOURCE | 536 |
| PAPERLESS_CDMS | 349 |
| ULTRASOUND_ENDOSCOPY | 825 |
| **合计** | **7766** |

每个系统均有 1 条 `physical_connection`。字段/关系以既有 84/89 导入成果为底；本轮以归一 + upsert 路径替换删除重插。

## 6. 空表治理状态

| 状态 | 数量 |
|---|---:|
| confirmed_empty | 0 |
| unknown / blocked | 0（尚未批量 live 探测写回） |
| row_presence null | 7766（待探测） |

说明：空表门禁代码与 API 过滤已上线；**未**对业务源库执行全量限 1 探测 apply（避免无窗超时/凭据批量调度）。禁止用 NUM_ROWS/TABLE_ROWS 单独删除。

## 7–8. dry-run / apply / 幂等

| 步骤 | writes |
|---|---:|
| reconcile dry-run | 0（计划变更待 apply） |
| reconcile apply | 8 |
| reconcile dry-run2 | 0（源重写已空；legacy 标记后修复） |
| reconcile dry-run3（修复后） | 0，legacy_marks=[] |

## 9–10. 测试

- 后端 plan90 单元：20 passed（`--noconftest`）
- 前端：typecheck（tsc + vue-tsc）OK；Vitest 43 passed；build OK
- 隔离测试库迁移往返：OK
- 全量 pytest（依赖本机 `APP_TEST_DB_URL` 隧道）：本机 153 无建库权限，未在本会话全量跑；8.83 有 `data_asset_test` 已用于迁移往返

## 11. 浏览器/接口验收

因登录 CSRF，在 `data-asset-api` 容器内直接调用 `list_systems` / `assets_tree` / `assets_tree_search`：

- 10 个一级系统名称与口径一致
- 树节点系统集合恰好十个平行码
- 不存在「其他业务系统」/ `external_business` /「平台元数据系统」
- 搜索返回完整 `path`（含 `→`）
- Nginx 首页与 health HTTP 200

## 12. 生产备份与回滚点

| 项 | 值 |
|---|---|
| 主机 | 10.10.8.83 |
| 容器 | data-asset-api（healthy） |
| Alembic | b4c5d6e7f8a9 |
| 清单备份 | `/opt/data-asset/backups/data_asset_pre_plan90_20260715_173117.sql` SHA256 `800c156639524017d101a56fe353f6228d0b5a15a7cf039965e39d21be0d78a3` |
| JSON 盘点 | `/opt/data-asset/backups/data_asset_pre_plan90_inventory_20260715_173409.json` SHA256 `95e3cbc32645bb66f9c5086e6f6e976fa7c806f6c85c8bb2f04d7c460cae7e4b` |
| 前端回滚目录 | `/opt/data-asset/frontend-dist.pre-plan90-20260715_173117` |
| 发布目录 | `/opt/data-asset/releases/plan90-20260715_173117` |
| 证据 | `/opt/data-asset/evidence/plan90/` |

说明：容器内无 `pg_dump` 且主机无法拉 alpine 镜像，**自定义格式全量 dump 本轮未成功**；回滚可依赖历史 `data_asset_pre_*` dump + 本轮清单/JSON + 前端 prev 目录。建议后续在镜像中固化 `postgresql-client`。

## 13. 本地与服务器 SHA-256 对账（关键文件）

| 文件 | SHA-256（本地=服务器） |
|---|---|
| app/services/asset_catalog.py | fc8c6fb0d5767d9c7d0d825e702a3c5bede35c026a7d528f023dd047941e43a6 |
| app/services/row_presence.py | eb7b994811da8496dbb1b6756eeb0abf3573935b1a368bfd7856dc1939793329 |
| app/services/asset_import_upsert.py | 4d8ffa22657c90c9d8d11695688331b5ad7f4b957cee66020b3b7d96116ec366 |
| alembic/.../b4c5d6e7f8a9_...py | f9cb74efc25ce86813ee034d6c65c37a42ae6402c4ae43b84cd3b752e8bba380 |
| scripts/reconcile_plan90_catalog.py | 1bae2aea34968dd8ba1f2ce74bb495105a66b85acc19c60171a3affb5236db03 |

## 14. 文档同步

- `README.md`：登记 92、更新 90 状态
- `55`：执行日志追加本轮
- `61`：Alembic/备份/发布事实
- `66`：交接状态更新
- `90`：标记已实施并指向 92

## 15. 业务源库写操作

**0**（仅平台 `asset` schema 与容器内代码/前端发布）。

## 16. 剩余项

1. 全系统 live 限 1 空表探测 dry-run/apply（需凭据调度、熔断与夜间窗）
2. 生产镜像固化 `pg_dump` / 全量自定义格式备份
3. 本机 `APP_TEST_DB_URL` 隧道全量 pytest
4. 浏览器人工角色矩阵（登录 CSRF 环境下的完整 UI 点检）
5. 清理本地 `_tmp_plan90_*` 临时脚本（含运维口令默认值，勿入库）

## 17. 二次复核与最终完成记录（2026-07-15）

- 新增真实批量执行器 `scripts/probe_row_presence_readonly.py`；业务源只执行单条限 1 `SELECT`，禁止 `COUNT(*)`，DML/DDL 为 0。
- 完成十系统 7,766 个对象治理：正常目录可见 4,331、`confirmed_empty=3,435`、`unknown/blocked=508`；失败、视图和未知对象保留，未误判为空表。
- 确认空对象的历史字段已清理，`EMPTY_COLUMNS=0`；关系保留审计并使用排除标记，不删除人工复核证据。
- 修复 HIS 关系整域删除，改为 upsert；修复数据中心别名参与探测、Schema GROUP BY、系统过滤串库、独立源 Owner 误标和虚拟系统生成。
- 完整自定义格式备份：`/opt/data-asset/backups/data_asset_pre_row_presence_20260715_175552.dump`，6,724,745 字节，SHA-256 `53eb33742da9ade004c2208a718f1bb1c0beb3133acb9cb43161ec7d18830f52`。
- 隔离测试库全量 pytest：`321 passed / 2 skipped / 0 failed`；专项测试 `23 passed`；前端 typecheck、Vitest `43 passed`、build 通过。
- 生产保持 `b4c5d6e7f8a9 (head)`；最终镜像 `data-asset:20260715-row-presence-final`，容器 healthy，live API、十系统逐项过滤和目录计数验收通过。
- PACS 重复 apply 为 `platform_updates=0`，证明当前结果幂等。原 §16 第 1–3 项已完成；本轮没有伪造浏览器截图，Git 提交/推送仍属于独立代码管理动作。

## 18. 全站显示名称一致性复核与修复（2026-08-11）

- 显示名称唯一基准继续为系统总览 `asset.asset_systems.system_name_cn`；`CANONICAL_SYSTEMS` 提供十系统标准回退。其他菜单和页面不再各自维护 HIS、HRP、数据中心、嘉和电子病历、移动护理、Docare手术麻醉、LIS、PACS、无纸化病案、超声内镜的别名。
- 明确“业务系统”与“数据连接”不是重复字段：前者是业务归属稳定键 `system_code`，后者是物理实例/库连接稳定键 `source_code`。界面统一层级为“业务系统 → 数据连接 → Schema/Owner → 表 → 字段”，不再使用“数据源/数据库”“数据库连接”“系统标签”等混合文案。
- 关系图谱的筛选项、级联选项、画布系统/连接分组、节点元信息和证据抽屉均使用中文显示名；技术编码只作为筛选值、物理键或括号辅助信息保留。
- 旧导入脚本统一复用标准名称，`ods_lis/ods_pacs/ods_emr/ods_ydhl/ods_sm` 即使重跑也归 `DATA_CENTER`，不会重新制造 LIS/PACS/EMR/SM 等 ODS 镜像一级系统。
- 回归：后端完整 pytest 697 passed/1 skipped；Alembic 往返回到唯一 head；前端双 typecheck、76 项 Vitest、生产 build 和 gzip 预算 PASS；用户可见旧词扫描仅剩禁止词黑名单、历史资产包物理路径和连接器技术注释。
- 代码提交 `e60c586` 已与构建提交 `b8bb8f5` 一并推送 `origin/master`。本轮未执行生产发布或平台/业务库写入。
