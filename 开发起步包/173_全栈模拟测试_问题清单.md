> 类别：执行报告
>
> 状态：**已完成（2026-09-01，一次性单会话执行 T0→T6 全批；只测不修，问题待主 AI 裁决修复）**
>
> 上位计划：`173_全栈一次性模拟测试问题发现计划.md`（v1.0）；基线=171 完成态（pytest 1341/1s、vitest 253、生产 r171-20260901、HEAD 96ed027 实测一致）
>
> 证据目录：`开发起步包/output_r173/`（全部工装与原始报告）；偏差台账 `output_r173/exceptions.json`（W1–W5）

# 173·全栈模拟测试问题清单

- 执行者：一次性测试执行 AI（按 173 §7 提示词，round-8 20 项裁决遵守）
- 日期：2026-09-01（单会话）
- 基线：HEAD=96ed0270（=171 收口提交）；openapi 实测 **345 路径/393 操作**；实路由表 400 条（含 3 个 include_in_schema=False 真实别名）；隔离库 data_asset_test（asset schema 103 表，171 T2.5 重灌态 12767 表/1329 关系）
- **覆盖边界声明（诚实复述）**：本清单=模拟探针+静态扫描+隔离库写成功路径断言；**不含**真实浏览器交互（171 已做过六查，不重复宣称）、真实数据规模性能、服务层全量深扫、RBAC 全矩阵（做的是角色×路由抽查 68 次运行时检查）、前端视觉验收。黑名单端点（63 个，S0+T3 补录）仅做鉴权探针从未真实执行。

## 一、执行概览

| 线 | 方法与规模 | 结果 |
|---|---|---|
| C 契约比对 | 全仓 `http.*` 检索（含 views/composables，排除 types.ts）：289 occurrence / **286 抽取（99.0%）**/271 自动匹配+15 人工裁决；字段级静态 9 对+运行时交叉 71 个 GET 200 data-keys 采样 | **路径/方法级漂移 0**；1 项 P3（平行类型双声明） |
| A 规则扫荡 | 10 规则全跑（58 视图+全部 ts），每命中人工定性 | P2×1 组（6 按钮）、P3×7 组；v-html/硬编码/空catch/敏感串/内联事件/菜单死码全部 0 真命中 |
| S 全路由探针 | **1328 次探针覆盖 395 路由**（无token/坏token/必填缺失/类型错/分页边界/管理员态；黑名单两类；CSRF 三态+外源 Origin 403 验证） | **5xx=0；鉴权 200 旁路=0（6 个 2xx 均为 PUBLIC_EXACT 设计）；契约矛盾=0**（期望码表 CSRF 表述修正见不确定项 U1） |
| S 写成功路径 | 38 用例（throwaway+每例清理+回读+幂等+审计 delta），38 个平台库写端点 | **37/38 PASS**（唯一"失败"=P2-1 系统列表过滤，已升级为问题）；审计写入 16 用例 |
| 补充线 | RBAC 34×2=68 次运行时检查（viewer 只读角色 vs platform_admin）；全局健康 12 探针；typecheck/build/空库 alembic/只读 lint | RBAC **0 违规**；健康 **12/12 无 5xx**；typecheck 双过 exit 0；build 49.10s gzip 183.1/65.8 kB；空库 alembic 需预建 schema（P3）；lint 4236 errors（99% prettier 格式） |
| V 薄弱页补测 | 6 薄弱页（error/404、error/500、identity/authorizations、identity/departments、identity/roles、query-center/accuracy）源码断言 18 条，放 output_r173 不进门禁 | **18/18 过——薄弱页结构质量达标，0 新问题** |
| 环境终检 | 他人域 16 文件 hash-object 终检与 T0 基线**完全一致（零触碰证明）**；探针 token/临时库清理完毕（r173 keys 残留 0；审计表新增 76 行 append-only 留痕） | 通过 |

**偏差四桶计数（DoD）**：5xx=0｜鉴权旁路=0（真实）｜契约漂移=0（路径/方法级）｜写断言失败=1（→P2-1）。

## 二、问题清单

### P0（0 条，书面说明）

无。理由：全程未触碰生产/业务源库（探针仅 TestClient+隔离库）；未发现鉴权 200 旁路（公开端点均在 PUBLIC_EXACT 白名单且内容已验非敏感）；未发现干净环境下任何 5xx；无数据损坏。最重问题为 P1（测试基础设施级）。

### P1（1 条）

| # | 层 | 位置 | 描述 | 复现/证据 | 定性把握 |
|---|---|---|---|---|---|
| P1-1 | 测试基础设施（隔离库） | `output_r170/import170.py` 重灌链路；表现于 data_asset_test 全部写审计端点 | **import170 重灌未重置自增序列 → 隔离库上所有写审计的端点 100% 返回 500**。重灌带入了 194 条 `asset_govern_audit_logs` 行（含 id），但序列停在 17，nextval 从 18 起全部撞已存在主键。影响：171 T2.5 重灌后的隔离库上，任何写审计操作（queries/ingest、quality/rules、dictionaries/categories、permissions/roles、govern/user-roles、identity/change-requests、admin/keys PATCH 等 11 个端点实测 500）全部不可用；**下次在该库跑全量 pytest 必然大面积失败**（171 的 1341 绿发生在重灌之前的库状态）。171 T2.5 验签只对了 tables=12702/relations=1329 两个计数，未验序列；T3 六查只做 GET 未暴露 | 复现（修复前实测）：管理员 token `POST /api/v1/queries/ingest`（合法 body）→ 500 `数据库异常`；engine `handle_error` 事件捕获底层异常=`duplicate key value violates unique constraint "asset_govern_audit_logs_pkey"`（3/3 案例一致）；序列实测 `max(id)=194 vs seq last_value=17`。**本会话已做环境修复**：对 asset schema 全部 102 条序列执行 `setval(seq, max(id), true)` 后同套 38 用例 37/38 通过（success_paths_report.json 前后对照），证实根因唯一。修复建议：import170.py 重灌末尾对全部序列 setval（修复 SQL 见 `_结果.json` env_repair 字段） | 高（根因-修复-复验闭环） |

### P2（2 条）

| # | 层 | 位置 | 描述 | 复现/证据 | 定性把握 |
|---|---|---|---|---|---|
| P2-1 | 后端+前端（系统管理闭环断裂） | `backend/app/services/asset_catalog.py:205-216`（list_first_level_systems 仅遍历 CANONICAL_SYSTEMS 常量）；消费方 `frontend/src/views/asset/systems/index.vue:152`（listSystems 渲染列表）+ `:243`（upsertSystem 保存） | **新建系统返回 200 但永远不出现在系统列表**：PUT /api/v1/systems 创建任意非 CANONICAL 常量编码的系统成功（实测 r173sys 落库），GET /api/v1/systems 只返回 Python 常量 CANONICAL_SYSTEMS 内的编码（对 by_code 字典 get 不到即 continue）。系统管理页"新建→保存→列表刷新"流程对非预置编码静默吞掉结果（无报错、无提示、列表无此行） | 成功路径套件 c_systems_upsert：PUT 200 → GET /api/v1/systems（page_size=100）断言"readback missing r173sys"失败（success_paths_report.json）；清理后 DB 行存在证明创建成功；前端两调用点 file:line 如左 | 高（前后端证据链+实测；唯一保留意见：若产品意图即"只允许 26 个预置系统"，则缺陷是创建端点应拒绝而非静默成功） |
| P2-2 | 前端（按钮级权限不一致） | `views/asset/probe-findings/index.vue:238`（确认迁移）、`views/asset/value-domains/index.vue:269`（确认）、`views/dict/general/index.vue:191/218/247/288`（保存类目/标准项/系统项/映射） | **同页其他写按钮均有 v-perms，唯独 6 个写按钮漏挂**（probe-findings 同页 5 处、value-domains 4 处、dict/general 10 处）。后端 require_permission 仍兜底故非越权漏洞，属 UI 纵深防御缺口与页面内策略不一致（无权用户看到可点击按钮→点击后 403） | A 线扫描 frontend_scan_raw.json + 逐文件 v-perms 计数人工裁决；向导步骤导航按钮（ImportWizard:158,223、PushWizard:170）已判良性不列入 | 高（同文件对照即证） |

### P3（8 组，聚合限流）

| # | 层 | 位置 | 描述 | 证据 |
|---|---|---|---|---|
| P3-1 | 前端 | src/api/（asset/dict/identity/metadata/ops/query-center/recipes/user） | **24 个导出 API 封装运行时+测试双零引用**（死代码；其中 retryAiQualityJob/attachAiQualityResult/getSqlRun/listSqlRuns 后端端点存在但 UI 未绑定，属功能半接入信号） | 完整清单 frontend_scan_report.md §死 API |
| P3-2 | 前端 | asset/admin、identity/persons、identity/roles、ops/runs 四页 | 整页零 v-perms（路由级 auths 已限页），与 P2-2 三页策略不一致，建议统一按钮级策略 | frontend_scan_report.md |
| P3-3 | 前端 | `views/asset/components/GraphToolbar.vue:21-59` | lint 全仓唯一非 prettier 错误类：**20 处 vue/no-mutating-props**（直接改 `locate`/`filters` props，破坏单向数据流，潜在父子状态不一致） | lint.txt（只读 npx eslint） |
| P3-4 | 工程 | frontend 全仓 | 只读 lint 4236 errors/24 warnings，其中 4216 为 prettier 格式类（`pnpm lint` 的 --fix 可消）——lint 门禁现状记录，非功能缺陷 | _r173_work/lint.txt |
| P3-5 | 后端 | `app/api/v1/recipes.py`（create 配方） | **create 静默接受非法 primary_tables**（任意未知键被 dict-Body 忽略），错误延迟到 `versions/{v}/sql` 生成时才 400"配方包含非法或缺失的表标识"——校验缺口，前端已传对形状但后端不设防 | success_paths 首轮 c_recipes 创建"成功"但 sql 生成 400 的前后对照 |
| P3-6 | 后端部署 | alembic 迁移链 | **空库 `alembic upgrade head` 失败**（`schema "asset" does not exist`）——迁移不自包含，需手工预建 schema（162 §1.3 既有约定，属部署摩擦；补建 schema 后 103 表全建成功、version=c1d2e3f4a5b6） | alembic_empty.log / alembic_empty2.log |
| P3-7 | 前端 | `src/api/dict.ts` QualityFindingItem vs `views/asset/quality/qualityContracts.ts:90-93` | 同一响应两套平行 TS 类型声明（漂移温床） | contract_diff_report.md §四 |
| P3-8 | 前端 | 视图层 console 2 处 + as any 70 处 | 错误路径 console.error/warn（无敏感数据，可接受）与 as any 聚合计数 | frontend_scan_report.md |

## 三、不确定项（独立节，完成后单独呈报）

| # | 现象 | 不确定原因 | 两种可能解释 | 建议核查方式 |
|---|---|---|---|---|
| U1 | CSRF 三端点（login/refresh/logout）无 Origin 头实测**不**返回 403：login→422（进 body 校验）、refresh/logout→401（中间件先拦无 Bearer）；仅**外源 Origin**（http://evil-r173.example）实测 403。173 计划期望码表写的是"无 Origin=403 设计" | `auth.py:83-96` 注释表明：带 X-Requested-With 头放行是既定前提；无 Origin 且无 Referer 的请求被视为工具类放行至下一层。期望码表（源自 171 文档口径）与代码实现孰为准无法在本任务内裁定 | ① 设计即如此（CSRF 只防"带外源 Origin 的浏览器表单"，无头工具请求放行——注释支持此解释）；② 文档口径正确而实现漏了"无 Origin 也应 403"（登录 CSRF 防护弱化，但 login-CSRF 实际风险低因无 Cookie 会话建立前攻击面小） | 主 AI 复核 auth.py:60-99 与 111 S6 设计记录；若采②需补无 Origin+无 Referer 的拒绝分支并同步改 171 文档口径 |
| U2 | `GET /`与`GET /api/v1/ai/tools` 无 token/坏 token 均 200（main.py PUBLIC_EXACT 白名单） | 白名单是显式代码（PUBLIC_EXACT），但未见对应设计文档条目；ai/tools 返回 platform/policy/tools 目录（已验 keys 无敏感值），供 Dify/MCP 外部注册 | ① 有意公开（工具清单供外部 AI 注册发现，非敏感）；② 曾为调试方便加入后未复核公开面 | 主 AI 确认 167 号设计记录是否包含"tools 公开"；若否，考虑移入需鉴权或限流 |
| U3 | `PATCH /api/v1/govern/executors/{code}?enabled=` 的 enabled 是 **Query 参数（默认 true）**，json body 被静默忽略（governance.py:506-511） | API 形态合法（Query 开关），但"PATCH 带 body 调用得到 enabled=true"反直觉；前端 dict/ops 页若按 body 习惯调用会得到反向结果 | ① 设计选择（与 admin/keys PATCH 一致也是 Query）；② 应统一为 body 或至少对多余 body 422 | grep 前端对该端点的调用形态（本轮未发现前端调用）；主 AI 定 API 约定 |

## 四、修复优先级建议与回归提示

1. **P1-1（先修）**：import170.py 重灌末尾补全量 `setval`（建议直接复用本会话修复 SQL，见 `173_..._结果.json` env_repair）。回归：重灌后跑 `POST /api/v1/queries/ingest` 成功路径 1 例 + `select last_value, max(id)` 对账全序列。**注意：本会话已修复当前库的 102 条序列，该修复对后续会话即时生效。**
2. **P2-1**：二选一——list_first_level_systems 增补"非 CANONICAL 但存在于 asset_systems 的行"分区返回；或创建端点对非预置编码显式 403+提示。回归：成功路径套件 c_systems_upsert 的 readback 断言（当前唯一红）转绿。
3. **P2-2**：6 按钮补 v-perms（权限码同页既有用法照抄：probe.finding.transition/value_domain.confirm/dict.general.manage 对应码）。回归：A 线扫描器复跑 no_perms_write 计数归零（除已判良性向导按钮）。
4. P3 按序：P3-5（recipes create 校验 primary_tables）与 P3-3（GraphToolbar props 突变）改动小收益明确；P3-1 死 API 清理建议与 166/167 后置项一起裁决（部分可能反向接入 UI）。
5. 回归门禁建议沿用本会话工装：`backend/_r173_work/` 下 success_paths.py（38 用例）、rbac_health.py（68 检查）、probe_routes.py（1328 探针）可直接复跑；output_r173/vtests 可作薄弱页回归（不进门禁）。
