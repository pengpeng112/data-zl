> 类别：执行报告
>
> 状态：**R0→R7 一次性执行完成（2026-09-04 晚，单会话）；R1 夜跑核验 FAIL 已呈报（等 G3），R5 按计划 SKIP**
>
> 上位计划：`178_177收口后可执行改进一次性执行计划.md`（v1.1）；基线 HEAD=96ed027（177 收口态）
>
> 证据目录：`开发起步包/output_r178/`（progress.md / exceptions.json / workspace_snapshot.txt / nightly_n1.md / wait_domain.md）

# 178 · 177 收口后可执行改进执行报告

## 0. 一句话结论

N1 夜跑核验 **FAIL 已呈报**（09-04 02:00 `RUN-b3324c242e78` 仍熔断，维度从 max_update 位移到 **max_change_ratio**——F-1 放行了 update 维度但 110 例行 resync 的比例维度仍炸，根治=F-2 发布=G3，本轮未调阈值未热修）；P0 代码两项 **C6/C7 全部落地**（GraphToolbar 13 处 props v-model 受控 + changeDisplay 去突变，`vue/no-mutating-props` 归零；alembic env.py 幂等自建 asset schema，隔离库 no-op 实证）；P1 互链 **L1 ①②③全部落地**（台账↔探查发现双向跳转，零新增后端）；R5 CDMS 用户未选表现整批 SKIP；门禁：后端全量 **1419P/1S/0F** + 专项 17/0、前端 typecheck 双过 + **278/278** + build 预算三绿；零 Git 写/零发布/零业务源写/他人域 13 文件哈希零触碰。

## 1. 执行批次总览

| 批 | 状态 | 摘要 |
|---|---|---|
| R0 开工自检 | ✅ DONE | 快照+他人域 13 文件哈希基线；15432 被外部占用未杀、自建 15532；两问未获答复（W-01） |
| R1 夜跑核验 | ✅ DONE（结论 FAIL） | RUN-b3324c242e78 熔断维度 max_change_ratio；三子任务一刀切 skip；详见 nightly_n1.md（W-02） |
| R2 C6 GraphToolbar | ✅ DONE | 177 C6 的 WARN 遗留本批闭环：父子组件单向数据流改造 + plan178R2 5 用例 |
| R3 C7 alembic schema | ✅ DONE | env.py 两条路径接 `_ensure_asset_schema`；tests/alembic_env 4 用例；隔离库 no-op 实证 |
| R4 L1 互链 | ✅ DONE | ①两页 scoped-slot 正向链接 ②finding_id 消费 ③listQualityObservations 反查按钮；plan178R4 6 用例 |
| R5 CDMS | ⛔ SKIP | 用户未选 004066 表现（提问未获答复），G7 保持开放 |
| R6 等待域 | ✅ DONE | wait_domain.md：G1–G17 全 17 项 + N1 佐证 + 3 项新增等待（C6/C7/L1 待 G13） |
| R7 门禁文档 | ✅ 见 §4/§5 | 后端专项+隔离库 alembic+前端三件套全绿；报告/结果/目录登记齐备 |

## 2. 修复与核验明细

### 2.1 R1 · N1 夜跑只读核验（FAIL 呈报）

- 通道：`ssh root@10.10.8.83` → `docker exec data-asset-api python`（仅 SELECT；凭据经容器 env，零落盘零回显）。
- 环境：容器 Up 28h healthy；`APP_IDENTITY_CB_MAX_UPDATE=150`（F-1 在产）、`APP_IDENTITY_CB_MAX_NEW=130`（临时值未动）。
- **最新夜跑 `RUN-b3324c242e78`（2026-09-04 02:00，host_cron_modified_sync）：failed，circuit_breaker_triggered=True，维度 `max_change_ratio`**；candidates_total=110；签名/职称/科室三子任务全部 skipped（`main_account_sync_not_successful`）；空签名账号未回补。
- 归因（对照本地代码只读）：`check_thresholds` 顺序 max_candidates(200)→max_new(130)→max_update(150)→max_align(150)→max_change_ratio(0.3)。本夜 update=110<150 **F-1 对其目标维度生效**；但生产仍为 F-2 前语义，110 例行 resync 计 update → ratio=1.0>0.3 熔断。与 09-03 维度不同属**预期位移非新故障**（此前 max_update 先触发即中止）。
- 处置：未调阈值、未热进 F-2、未改 env/cron；等用户走 G3。证据 `output_r178/nightly_n1.md`。

### 2.2 R2 · C6 GraphToolbar 单向数据流（177 C6 遗留闭环）

| 改动 | 文件 | 验证 |
|---|---|---|
| `defineEmits` 增补 `update:locate`/`update:filters`；13 处 props v-model 全改 `:model-value` + 逐字段 `emit('update:*', { ...props, field: value })` 受控写法（**无 `{ field: val }` 字面键**）；`advancedVisible` 本地 ref 保持 v-model | `frontend/src/views/asset/components/GraphToolbar.vue` | `vue/no-mutating-props` **0 条**；plan178R2 源码锁 5 用例 |
| `changeDisplay` 从 `props.filters.layout_mode = command` 改为 `emit("update:filters", { ...props.filters, layout_mode })`（script 级 props 赋值归零） | 同上 | plan178R2 断言 `props.filters.layout_mode =` 不存在 + 全量 props 赋值正则禁令 |
| 父组件只加 `@update:filters`/`@update:locate` 监听 + `onUpdateFilters`/`onUpdateLocate` 两个 `Object.assign` 处理函数（保留 reactive 引用） | `frontend/src/views/asset/graph/index.vue` | plan178R2 断言父模板接线与两个函数体；图谱既有测试全绿 |

批内测试：`plan178R2 + graphPage + graphPageMount + plan169G2/G3/G4 + graphNeo4jP0` = **7 文件 47/47 绿**（graphNeo4jP0 的「搜索并聚焦」「高级筛选」`command="force"` 既有锁一条未动）。ESLint 余量 148 条全为该文件**既有** prettier 风格债（173 P3-4 明确不做，未触碰）。

### 2.3 R3 · C7 alembic 空库自建 schema

| 改动 | 文件 | 验证 |
|---|---|---|
| 新增 `_ensure_asset_schema(connection)`：`CREATE SCHEMA IF NOT EXISTS asset` + commit；online 在 `context.configure` 前调用；offline 在 `run_migrations` 前 `context.execute(text(...))` 同句（离线脚本含该 DDL） | `backend/alembic/env.py` | `pytest tests/alembic_env -q` **4/4**（DDL 幂等 / online 先建 / offline 先发 / 历史 revision 零触碰） |
| 仅追加 `"alembic_env"` 进 `_PURE_LOGIC_SUBDIRS` | `backend/tests/conftest.py` | 纯逻辑子目录不触发 APP_TEST_DB_URL 门禁 |
| 新增哨兵 conftest（plan144 模式）+ 源码锁测试 | `backend/tests/alembic_env/` | 不连库 |
| 隔离库核验：8.83 data_asset_test（自建 15532 隧道，URL 显式且全程变量传递不回显） | — | `alembic upgrade head` **no-op 通过**（无 Running upgrade；IF NOT EXISTS 在已有 schema 上不炸）；`alembic current` 仍 `d5e6f7a8b9c0 (head)` |

未改任何历史 revision；未 drop schema；无空 postgres 从零实验按计划跳过（W-04）。口径注记：隔离库 `alembic_version` 位于 public schema（alembic 默认），不影响结论（W-05）。

### 2.4 R4 · L1 台账↔探查互链（166 P2 最小切片，①②③无整项 WARN）

| 环节 | 实现 | 文件 |
|---|---|---|
| 纯函数 | `parseProbeFindingRef`（`^asset_probe_findings:(\d+)$`）/ `probeFindingLink`（`/probe-findings?finding_id=`）/ `probeFindingSourceRef` / `findingIdFromRouteQuery`（缺省非数字→null） | 新增 `frontend/src/views/quality/sourceRef.ts`（三页共用） |
| ① 正向 | 观测表来源列改 scoped-slot 读 `row.source_record_ref`：命中渲染 el-link（文案=`source_kind`）跳发现页；未命中纯文本 `-` | `quality/issue-detail/index.vue`、`quality/observations/index.vue`（各只改一列） |
| ② 消费 query | 补 `useRoute`；`watch(route.query.finding_id)` + onMounted 列表加载后消费；命中→打开既有详情抽屉；未命中→**同一 id 只 warning 一次**，不清筛选盲扫 | `asset/probe-findings/index.vue`（只在 177 v-perms hunk 之上追加，未重排未改流转） |
| ③ 反查 | 选中行后 `listQualityObservations({source_kind:'probe_finding',page:1,page_size:100})` 客户端匹配 `asset_probe_findings:{id}` 且 `issue_id` 为数字；total>100 补拉 page=2 一次；命中显示「查看质量台账」按钮→`/quality/issues/{issue_id}`；**未命中/403/网络失败一律隐藏按钮**（console.warn 级，不打错误态） | 同上（只 import 已有 API，零后端改动） |

测试：`plan178R4.test.ts`（6 用例：解析成功/失败、非法 finding_id 忽略、`asset_probe_findings:12` 拼链、三页接线源码锁）+ 验收回归 `plan174QualityLedger.test.ts` + `plan166StageD2.test.ts` = **22/22 绿**。

## 3. 用户两问与 R5

开工按 §3 R0 以 AskUserQuestion 问过一次（004066 表现 A/B/C；150 三选一），**未获答复**：R5 CDMS 整批 SKIP（G7 仍开，未写 KESHID/未改密码）；G9 记「仍开」（AI 不虚构申报内容）。

## 4. 门禁汇总

| 门禁 | 结果 |
|---|---|
| 后端专项（alembic_env + r177_fixes + F-2） | **17 passed / 0 failed**（隔离库显式 URL） |
| 隔离库 alembic upgrade head / current | no-op 通过；`d5e6f7a8b9c0 (head)` 与生产一致 |
| 后端全量 `pytest tests/ -q` | **1419 passed / 1 skipped / 0 failed**（34:13；隔离库 data_asset_test，177 基线 1415P/1S/0F + 本批新增 tests/alembic_env 4 用例） |
| 前端 typecheck | tsc + vue-tsc 双过（exit 0） |
| 前端 vitest 全量 | **44 文件 278/278**（177 基线 267 + 本批新增 11） |
| 前端 build | 主入口 657.2/700、图谱异步包 404.5/430、CSS 103.5/110 KB gzip **三绿** |
| 白名单合规 | 本批改动全部在 178 §3 白名单内；他人域 layout/captureMode/capture_*.py 13 文件哈希终检=R0 基线 |
| Git / 生产 | 零 commit/push/tag；零发布、零 env/cron/镜像改动；业务源库零连接零写入 |

## 5. DoD 自检（对照 178 §5）

1. ✅ R1 有 `nightly_n1.md`（run_id=RUN-b3324c242e78、熔断=True、维度=max_change_ratio）；未写「已验证通过」。
2. ✅ C6 已做：changeDisplay 无 props 赋值；模板无 `v-model="locate.` / `v-model="filters.`；R2 点名图谱既有测试 47/47 绿；两文件开工 git diff 干净故未跳过。
3. ✅ C7：env.py 含 `CREATE SCHEMA IF NOT EXISTS asset`（online+offline 双路径）；测试在 `tests/alembic_env/`；隔离库 `upgrade head` no-op 不炸。
4. ✅ L1：① scoped-slot 读 `row.source_record_ref`；② `useRoute` 消费 `finding_id`；③ `listQualityObservations` 反查后按钮命中显示/未命中与 403 **隐藏**（无整项 WARN）。
5. ✅ 前端 typecheck + tests + build 过；后端专项 0 failed；全量 pytest 结果见 §4（如实报数）。
6. ✅ 执行报告 + `_结果.json` + `output_r178/`（progress/exceptions/workspace_snapshot/nightly_n1/wait_domain）齐备。
7. ✅ README / 55 只在 R7 写一次。
8. ✅ 他人域 13 文件哈希=R0 基线；业务源写入 0；生产 env/cron/镜像未改；零 Git 写。
9. ✅ 等待域 G1–G17 全量呈报（wait_domain.md），无一项被标成已完成。

**未宣称完成**：N1 夜跑熔断（FAIL 呈报等 G3）；F-2 生产发布；CDMS 004066（R5 SKIP）；150 课题（G9 仍开）；C6/C7/L1 生产上线（代码完成，待 G13 提交+下次发布）。

## 6. 交付物与证据索引

- 代码：`GraphToolbar.vue`、`graph/index.vue`、`alembic/env.py`、`sourceRef.ts`（新增）、`issue-detail/index.vue`、`observations/index.vue`、`probe-findings/index.vue`、`tests/conftest.py`（一行）
- 测试：`frontend/tests/plan178R2.test.ts`（5）、`frontend/tests/plan178R4.test.ts`（6）、`backend/tests/alembic_env/`（4）
- 台账：`output_r178/`（progress.md / exceptions.json W-01～W-05 / workspace_snapshot.txt / nightly_n1.md / wait_domain.md）

## 7. 交接下一会话

1. **G3 是当前最高价值动作**：N1 已实证 F-1 只解决了一个维度；发布 F-2（工作区代码+9 测试就绪）后 110 人例行 resync 应恢复 success，签名/职称/科室子任务随之自动恢复；发布后 MAX_NEW 130 可回落 50（F-1 的 150 可回默认 100）。
2. C6/C7/L1 三项代码已绿，随 G13 四组分域提交（178 §3 R7 预案）入库；前端下次发布即互链可见。
3. 004066 / 150 仍等用户书面答复（wait_domain.md 第四节）。
