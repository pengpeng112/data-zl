> 类别：证据链
>
> 状态：**已执行完成**——四源资产经备份/dry-run/幂等门禁后成功导入 8.83 平台；二次 dry-run 全 0；抽查与健康检查通过。本报告为 139 号计划经用户重新授权后的最终收口（接续 140 号核查收口与 141 号交接）。
>
> 执行日期：2026-08-22（恢复执行窗口 09:05–10:05 本地）
> 执行计划：`139_四系统只读探查_视图关系治理与服务器升级一次性执行计划.md`
> 前序证据：`140_plan139四系统探查执行核查与收口报告.md`（含勘误补记）、`141_四系统只读探查执行交接与备份说明.md`
> 机器可读结果：`142_四系统资产导入与139收口执行结果.json`
> 授权依据：用户于 2026-08-22 明示"将该会话和当前会话最后的一次未完成的任务授权你完成"；记录见 `backend/_plan139_work/COORDINATION.md` 末尾

# 四系统资产导入与 139 收口执行报告（142）

## 0. 结论先行

1. 用户重新授权后由**单一执行者**（原执行会话延续）完成 139 号 S8–S12 剩余步骤与 140 号遗留修复项。四源只读成果已幂等导入 `10.10.8.83` 平台 `asset` schema：**+4 系统/+4 数据源/+1,731 表/+24,021 字段/+592 声明外键结构关系/+56 view-JOIN 复核草稿（全 draft）/+40 停用关系配方草稿/+729 视图依赖**。
2. 全部门禁通过：apply 前 dry-run 删除=0、正式 view-JOIN 新增=0、active recipe=0；apply 后二次 dry-run 新增/更新/删除**全部为 0**；每源 3 表/3 视图/3 依赖抽查全部命中（病理含 `pitaya.dbo.V_BLZLST`）；`/health`=200，容器未重启（仅资产导入，无运行时代码变更，不重建镜像）。
3. 分层治理严格落地：592 条新增正式关系全部是**数据库声明外键**（`declared_foreign_key` 证据层，139 §4.2 明确允许）；视图 JOIN 只进 `asset_relation_reviews` 草稿；复杂逻辑只进 `asset_relation_recipes` 停用草稿（batch active=0）；无跨系统连线。
4. 安全红线：四个业务源库 DML/DDL/EXECUTE=**0**；凭据零复制（仅 `credential_ref`）；患者明细零输出；秘密/PII 扫描干净。无 Git commit/push。
5. 140 号遗留修复完成：构建器分叉合并、140 勘误补记、被中断测试补完（全量 914 通过/7 失败全为 HEAD 既有）、alembic 往返 PASS。

## 1. 恢复执行依据与边界

- 140 号收口时用户裁决终止后续；本轮用户明确重新授权完成两会话最后未完成任务（140 会话的"明确有问题的修复"清单 + 本会话 S8–S12）。
- 单一执行者，无并行会话；原停止令与强制执行记录保留于 COORDINATION.md，未删改。
- 授权边界不变：源库只读、apply 仅限平台 asset schema、无运行时代码变更不重建镜像、不 Git push。

## 2. 四源最终状态

| 系统 | 引擎/版本 | 传输 | 状态码 | 对象（表/视图） | 字段 | 声明 FK | 视图依赖 | 平台导入 |
|---|---|---|---|---|---|---|---|---|
| CORE2DB 门诊病历 | MySQL 5.7.20 | ssl_disabled（仅该连接） | COMPLETED（例程元数据 `BLOCKED_ROUTINE_METADATA`；VIEW_TABLE_USAGE 不可见记 skipped_optional，依赖走解析层） | 531/37 | 8,066 | 592 | 50（解析） | ✅ |
| PHYSICAL_EXAM 体检 | SQL Server 2014 (12.0.2000.8) | tds 7.0（受控回退） | COMPLETED（JZCIS 277 例程定义不可见 `BLOCKED_ROUTINE_METADATA`） | 501/77 | 7,584 | 0 | 72 解析 + 引擎视图级 | ✅ |
| PATHOLOGY 病理 | SQL Server 2008 R2 (10.50.1600.1) | tds 7.0 | COMPLETED（pitaya 13 例程定义不可见 `BLOCKED_ROUTINE_METADATA`；`V_BLZLST` 已单列登记） | 264/30 | 5,166 | 0 | 21 解析 + 引擎 | ✅ |
| OCCUPATIONAL_DISEASE 职业病 | SQL Server 2014 (12.0.2000.8) | tds 7.0 | COMPLETED（无错误） | 269/22 | 3,205 | 0 | 20 解析 + 引擎 | ✅ |

排除库确认：体检仅 EIS/JZCIS/ZONEKINGNET（ReportServer* 未采）；职业病仅 tjdatabase4；病理仅 pitaya/QPIS；门诊病历仅 core2db。

## 3. 视图分析与关系分层

- 视图清单 166 条全覆盖（含定义可见性、定义 SHA-256、依赖数、跨库标记、UNION 分支、解析状态）；病理 `V_BLZLST`：view 角色、11 字段、3 解析依赖 + 3 引擎依赖。
- 关系候选 212 条（解析器修复字面量误报与别名吞 JOIN 后由 178 恢复至 212）：`existing`=0、`candidate`=56、`partial` 若干并入 review 层、`recipe_candidate`=84、`rejected`=0、`unresolved` 按视图记录原因（CORE2DB 派生表 JOIN、职业病逗号连接+函数包裹键等，未猜测补边）。
- 与平台正式关系查重：同向/反向/组合键冲突=**0**；`skipped_existing_review`=1（既有草稿）。
- 限量聚合验证（139 §4.4）：15 条体检源小表候选全部 validated（TOP 10000 去重键、只读会话、仅聚合计数，命中率 0.61–1.0）；27 条记 `VALIDATION_SKIPPED_RISK`（端点非表/表过大或未建索引）；54 条为合格小表但未入选上限批次（后续可增量执行）。

## 4. 平台导入（S8–S9）

- apply 前实时基线：15 系统/15 源/7,766 表/89,730 字段/568 关系/30 reviews/24 recipes；alembic head `aa11bb22cc33`（未照抄历史计数）。
- 备份（两份，均可验证）：
  - `data_asset_pre_plan139_20260822001107.dump`（8,607,945 B，SHA-256 `085b74f9…d5773`）
  - `data_asset_pre_plan139_apply_20260822013602.dump`（8,607,947 B，SHA-256 `64f3e14e…9328c`，apply 直接前置）
  - 恢复：`pg_restore -h 127.0.0.1 -U postgres --clean --if-exists -d data_asset <dump>`
- dry-run 人工审阅（apply 前）：tables 1,731 ins/0 upd；FK 592 ins/0 upd；eligible reviews 56 / recipes 40；**删除=0、正式 view-JOIN 新增=0、active recipe=0、来源身份冲突=0**（592 全为数据库声明 FK 结构关系，属允许层）。
- apply（确认串 `APPLY-PLAN139-FOUR-SOURCES` + run_id）：事务提交成功，review/recipe 序列同步 2 项，`source_writes=0`。

| 计数 | apply 前 | apply 后 | 增量 |
|---|---:|---:|---:|
| 系统 | 15 | 19 | +4 |
| 数据源 | 15 | 19 | +4 |
| 表/视图对象 | 7,766 | 9,497 | +1,731 |
| 字段 | 89,730 | 113,751 | +24,021 |
| 正式关系 | 568 | 1,160 | +592（全部 declared_foreign_key） |
| 关系复核 | 30 | 86 | +56（本批全 draft） |
| 关系配方 | 24 | 64 | +40（本批全 draft/is_active=false） |
| 视图依赖（批次） | — | 729 | +729（source_file=批次标签，可整批替换） |

## 5. 幂等复验与抽查（S10–S11）

- **S10 判定**：本轮无运行时代码变更（修改仅限采集/解析/导入脚本与测试，均不在应用镜像内，以 docker cp 临时执行），故**仅资产导入，未重建镜像、未切前端、未重启容器**（容器 Up 46h 未中断）。
- 二次 dry-run：tables 0/0、FK 0/0、eligible_reviews=0、eligible_recipes=0、relations_delta=0、view_dep 替换=0——**新增/更新/删除全部为 0**。
- 抽查（元数据 only，不查患者行）：四源各 3 表/3 视图/3 依赖全部 present 且角色/字段数正确（如 CORE2DB.BL_MJZBL view 87 列、JZCIS.DBO.JCXX 128 列、V_BLZLST 11 列）。
- 健康：`/health` 200；容器 healthy。批次状态：reviews `draft`×56、recipes `draft`×40 且 active=0。

## 6. 测试门禁

| 项 | 结果 |
|---|---|
| plan139 专项（纯逻辑） | 54/54 PASS |
| 后端全量 pytest v2（隔离库 `data_asset_test_139`，正确布局+哨兵 env） | **914 passed / 7 failed / 3 skipped**；7 个失败全部在 HEAD 干净基线复现（plan127×2、security_audit×2、core_import×3，属 130 号计划遗留），dirty 侧新增失败=0 |
| Alembic 往返 | PASS：`aa11bb22cc33 → h5c6d7e8f9a0 → aa11bb22cc33` |
| 秘密/PII 扫描 | 资产包 26 文件 0 命中；快照内密码模式已脱敏 `***`、18 位命中均为 `'000000000000000000'` 占位常量 |

## 7. 140 号遗留修复项（引用会话未完成任务）

1. **构建器分叉合并**：保留测试所面向的 `build_plan139_asset_package.py`（生成现行资产包，manifest generator 已指向）；`plan139_build_package.py` 移入 `backend/_plan139_work/plan139_build_package.py.superseded` 保留审计，不再形成竞争入口。
2. **140 勘误补记**：S8 备份/dry-run 实际已执行、08:14 后仍有活动、资产包为 08:08 重建版（212 候选）、15 项验证已成功——四项事实以追加方式更正，原文未改。
3. **被中断测试补完**：全量 pytest v2 + alembic 往返完成（见 §6）。
4. 08:14 强制停止收尾确认：沙箱容器已按停止令停止；本轮恢复执行时重新启用并于收口后清理；生产容器全程未触碰。

## 8. 遗留事项（仅登记）

1. 7 个 HEAD 既有测试失败与 `ready_his` 弱凭据评估属 130 号计划范围，未纳入本轮。
2. 54 条合格未验证候选可后续增量执行 §4.4 验证；56 条 review 草稿待人工独立复核（同一执行者不批准自己的候选）。
3. 本机 dirty 改动是否 commit 由用户裁决（141 §3 清单 + 本轮新增：142 报告/结果 JSON、140 勘误、COORDINATION 授权记录、import_result.json、分叉脚本归位）。

## 9. 证据位置

- 本机资产包：`开发起步包/数据资产_四系统源端资产包/`（含 import_result.json，14 类文件 + sources/ 四源子目录）
- 本机报告：本文件 + `142_四系统资产导入与139收口执行结果.json`
- 服务器证据：`/opt/data-asset/evidence/plan139-20260822T000829Z/`（750/640，SHA256SUMS 已刷新，含 dry-run×2/import_result/pytest_v2/alembic/sampling/configs/snapshots）
- 备份：见 §4；快照同源副本 `backend/_plan139_work/*_snapshot.json`
