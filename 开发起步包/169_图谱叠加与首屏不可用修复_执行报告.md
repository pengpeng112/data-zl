> 类别：应用开发执行证据
>
> # 169 关系图谱叠加与首屏不可用修复 执行报告（G1–G5）
>
> 计划：`169_关系图谱叠加与首屏不可用修复执行计划.md`（v1.0，基于 multi-verify round-3 四方核查裁决）
> 执行窗：2026-08-30（单执行者连续推进；验收环境=隔离库 12702 表生产真实数据）
> 结果：**隔离库复核完成并已生产发布（2026-08-31）**

## 1. 结论

- **六项缺陷全部修复且验收过线**：首屏 overview **23.5s→0.012s**（1959 倍，中位三次实测）；首屏错误面板消失且工具栏常驻可自救；**用户主诉"内容叠加"实测逆转——explore 邻域中心区暗密度 99.5%→1.5%**（1 跳约 30 节点、2 跳 80-100 节点均散开、标签可读、中心表突出，截图+AI 目检双证）。
- 门禁全绿：后端全量 **1327 passed / 1 skipped / 0 failed**（28m20s，基线 1323+新增 4）；前端 **238 tests / 37 files**（新增 9：G2×4+G3×3+G4×2）+typecheck 双过+build gzip 预算 PASS（656.6/700、404.5/430、101.3/110）。
- 像素硬门槛（铁律 4）：A2/A3/A4 三场景中心 1/3 暗密度 **1.5%/1.5%/0.6%**，全部 <35% 阈值，未调阈值。
- 五类真实浏览器截图（168 欠账补齐）：A1 首屏 / A2 搜索聚焦 / A3 2 跳 / A4 增量展开 / A5 Inspector+图例，全存 `output_r169/screenshots/`。

## 2. 修复明细（对照计划 §2）

| 项 | 修改 | 验收 |
|---|---|---|
| P1 后端 N+1 | `graph.py` 聚合分支加载 `_EndpointResolver.load(db)` 并两处传 `resolver=`（与 object 层同款，只增参数） | curl 三测 0.032/0.012/0.012s（中位 0.012s <1.5s）；源码护栏测试锁死防回归 |
| P1b object 层 | 无需独立改（resolver 后实测 0.014s <3s） | 实测通过 |
| P5 meta 口径 | 非 object 分支：total=过滤后关系数（1329）、matched=参与配对关系数、returned=聚合边数、truncated=聚合落差 | 断言 total==1329（seed 环境==3）非表数；空关系库安全 |
| P1 前端死循环 | ①index.vue 工具栏 v-if 去掉 isErrorState（错误态并存可切模式）②getGraphOverview 单独 timeout 30s ③失败自动降级 system 层重试一次（shouldDowngradeOverview 纯函数，手动重试/重置恢复资格） | A1 实拍无错误面板+toolbar=true 并存；4 项新测试 |
| P2 force 坍缩 | force 分支按白名单重写：nodeStrength -280→**1200**、gravity 10→**1**、edgeStrength 0.45→**50**、nodeSize 88→**68**（对齐真实直径，preventOverlap 生效）、maxSpeed 3；删除 alpha/alphaDecay/alphaMin/forceSimulation/nodeSpacing 全部死配置 | 白名单对照测试（越集即败）+参数范围测试；像素门槛三场景 PASS；目检节点散开标签可读 |
| P4 标签死配置 | 删 labelWordWrap/labelMaxWidth/labelMaxLines/labelTextOverflow（文本已预换行） | 119 号回归测试按新契约更新（断言死配置 undefined+预换行 `\n` 仍在） |
| P3 SVG 降级 | ①explore+centerTable 落 buildRadial（中心辐射，与 G6 radial 对齐）②buildLayered 行距动态 `max(92, 46+maxLines*15+24)` 按组内标签行数 | 挂载实测 5 行标签 y 间距 ≥140px（原 92） |
| W4 顺手修 | GraphInspector 中文名四级回落（table_name_cn→字符串 label→label.formatter→display_id），不再渲染 label 对象 JSON | typecheck+实拍 Inspector 正常 | 

## 3. 验收证据

- **性能**：`curl overview?level=system` 三次 0.032/0.012/0.012s（隔离库 12702 表/1329 关系）；object 层 0.014s。
- **像素门槛**（`output_r169/验收_原始输出.json`）：A2 1.5%、A3 1.5%、A4 0.6%（阈值 35%，round-3 修复前同场景 99.5%）。
- **目检**（读图 AI 双证）：A2=25-35 节点散开、PAT_MASTER_INDEX/ORDERS 等标签可辨、PAT_VISIT 中心突出（黑底金边）；A3=80-100 节点仍散开无堆叠无裁切。
- **五类截图**：`screenshots/A1_overview_first_screen.png`（首屏出图无错误面板）、`A2_explore_focus_1hop.png`、`A3_explore_2hop.png`、`A4_incremental_expand.png`（含一次真实 neighbors 增量展开 API）、`A5b_inspector_opened.png`（Inspector 打开+图例 8 芯片）。
- **回归**：后端全量 1327/0 failed；前端 238/238+typecheck+build 预算（最终 build 含全部修复）。
- 新增测试 13（后端 4+前端 9）≥ 计划要求 10。

## 4. 验收工装坑与登记（详见 exceptions.json W1–W5）

1. 伪会话三坑（pure/el-segmented 类名、refresh 401 队列、pytest 清库殃及 token/基准数据）逐一排除，最终走 auth.ts 迁移期 cookie 通道注入 token 确定性解决（未动产品代码）。
2. 基准数据 12702 表被 G1 全量 pytest 夹具清空一次，从 `verify/round-3/graph_data_export.json` 重灌（166 附录 A.2 预告行为的重演实证）。
3. 119 号旧断言与 P4 计划内删除冲突，按新契约更新（断言从"死配置存在"改为"死配置绝迹"，非弱化）。

## 5. DoD 自检（169 §5）

- [x] P1/P1b/P2/P3/P4/P5 落地+新测试 13≥10；G1–G5 checkpoint 齐
- [x] overview 中位 0.012s（<1.5s）；meta total=关系数断言过（seed==3/生产规模==1329 语义）
- [x] 三场景像素 <35% 实测记录；五类截图齐
- [x] 后端全量 0 failed；前端 typecheck+vitest+build 预算全绿
- [x] 报告+`169_图谱修复_结果.json`+README/55 登记；生产发布等待域呈用户

## 6. 主 AI 复核与生产发布（2026-08-31）

复核发现旧截图工装存在假阳性：A2/A3 未携带五段物理键、A4 请求曾为 422，不能作为邻域验收证据；已修正验收脚本并补上真实邻域响应门禁。另修复 `globalSearch` 在 overview 节点混入 explore 的问题：仅发送五段物理键，无物理键时替换数据集并重置 explore 状态。干净发布目录前端 37 files/240 tests，双 typecheck、build 与 gzip 预算均通过；后端全量 1329 passed/1 skipped/0 failed。

生产发布：备份 `/opt/data-asset/backups/data_asset_pre_r169-20260830_20260831.dump`，后端 `data-asset:r169-20260830` healthy，前端 current=`r169-20260830`、previous=`r167-20260830`；`/health` 数据库 connected/build_id 正确，overview 200（0.228s）。

同时完成 AI 写 SQL 生产修复：正式关系状态兼容 `verified/sample_verified` 等现行值；上下文按完整 prompt 字节预算收缩并在超限时明确 422；前端请求超时提升至 120s、错误详情可见。生产真实 `POST /api/v1/ai/ai-sql/generate` 返回 200（18.916s），`executed=false`、正式关系 4 条、payload 10255 bytes；生成 SQL 未执行。

## 7. 等待域

保留既有人工审核的 SQL 草稿执行链、未来真定时调度与更强模型切换；生产回滚为镜像 `data-asset:r166-20260830` + 前端 previous 链接，数据库备份已留存。
