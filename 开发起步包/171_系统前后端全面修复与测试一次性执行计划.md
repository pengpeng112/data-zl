> 类别：模块规划
>
> 状态：**v1.1 定稿待执行（2026-08-31 multi-review round-7 裁决定稿；v1.0 草案经 kimi 审查 3 条阻断修正全部采纳；grok/codex 缺席见 分歧表）**
>
> 上位入口：`55_系统未完成事项统一执行计划.md`
>
> 关联：`review/round-7/`（方案/审查/分歧表/最终报告）；169 号（图谱修复，生产已发布 r169）；167 号（AI SQL 预算修复，在工作区未提交）；166 附录 A.2（pytest 清库行为）

# 系统前后端全面修复与测试一次性执行计划（171）

## 0. 目标与现状结论（2026-08-31 实测）

对系统全面分析后的一次性修复+测试闭环。**分析结论**：

1. 生产已由并行会话升级至 **data-asset:r169-20260830（healthy）+ 前端 r169-20260831-storage-fix**——图谱 23.5s 阻断缺陷已在产修复（本计划改为**复核发布完整性**）。
2. **工作区 34M/35?? 未提交**，混五个域：①169 图谱修复（生产已上但源码未提交！）②167 AI SQL 上下文预算修复（未提交未发布）③登录签名同步新功能（**他人半成品，禁止本计划代提交**）④layout captureMode 截图模式（他人域）⑤system-map 新页面（今日新增，含 1 个已修 bug）。
3. 前端当前 245 tests 中 systemMap 曾 1 失败（接口结构混用），**已修复 4/4 过**，待全量复验。

## 1. 铁律

1. **他人域零触碰**：`identity_login_sign_sync*`、`run_identity_modified_nightly.py`、`identity_sync_*.py`、`jhemr_identity_adapter.py`、`layout/**`、`useTag/useDataThemeChange/app.ts` 等登录签名/layout 域文件**不提交、不修改、不 stash**（保持工作区原样）；本计划只提交清单见 F1。
2. 测试通过标准一律 **"0 failed 且与执行前基线记录一致"**，禁止硬编码数量断言（防并行会话增删测试造成假失败/假通过）。
3. pytest 全量会清空隔离库（166 附录 A.2）：**任何全量 pytest 之后、浏览器验证之前必须执行重灌**（§3 T2.5）。
4. 生产发布/Git 写逐项授权；隔离库显式 `APP_TEST_DB_URL`；源库只读；pnpm；每批验收过才进下一批；偏差 WARN 落 `output_r171/exceptions.json` 继续。

## 2. 修复清单（按批）

| 批 | 内容 | 说明 |
|---|---|---|
| **R1 提交完成态（需授权 git commit，不 push）** | 仅提交：①169 域五文件（graph.py、AdvancedRelationGraph.vue、RelationGraph.vue、GraphInspector.vue、graph/index.vue、graphLoadPolicy.ts+tests/graph 相关+output_r169 报告）②167 域（ai.py、ai_context_builder.py、test_ai_sql_context.py）③system-map 新页面（views/system-map/、router asset.ts、tests/systemMap.test.ts）④仓库卫生（.gitignore 追加 `开发起步包/docs/`、`~$*`、`.release_*/`、`backend/_r163_work/`、`frontend/*.log`、`backend/_mverify_backend.log`）+README/55 登记 | 每文件 diff 复核归属；**一个域一个 commit**（3-4 个），他人域文件绝不入 |
| **R2 生产发布（需授权）** | 后端：ai.py 预算修复（167）入产（scp+docker cp+restart 或 hotpatch 镜像 r171）；前端：system-map 页面随 dist 原子切换 `r171-<date>`（previous=r169-storage-fix） | 166/169 发布先例流程 |
| **R3 测试补强** | ①Inspector displayName 专项断言（进 plan169G3.test.ts 或新文件）②system-map 菜单可见性查（T3 清单第⑥项）③depth 控件选择器实验记录（文档性，不强求） | 小项合并一批 |
| **R4 遗留核对（只登记不改代码）** | 55 号核对：165 X2（夜间探查仍待授权）、165 T7、168 P1、166 P2 状态行；生产 r169 发布完整性（镜像 healthy/前端 current 指向/overview 耗时已实测 1ms 级拒未授权、graph 修复在产验证=授权用户实测一次截图） | 输出到执行报告 |

## 3. 详细测试计划（一次性执行顺序，每步含通过标准）

### T0 环境与基线（先记录后动工）
```powershell
# 隧道+隔离库非空且与最近重灌记录一致（不写死数字）
cd F:\python\数据资产\backend
.\.venv\Scripts\python.exe -c "import psycopg;c=psycopg.connect('postgresql://postgres@127.0.0.1:15432/data_asset_test',connect_timeout=8);cur=c.cursor();cur.execute('select count(*) from asset.asset_tables');print('tables=',cur.fetchone()[0])"
# 通过：连接成功且 tables>0（为 0 则先跑 开起步包/output_r170/import170.py 重灌）
# 基线记录：git rev-parse HEAD、git status --short 计数、pnpm vitest --run 的当前 files/tests 数（写入 output_r171/baseline.txt）
```

### T1 前端门禁
```powershell
cd F:\python\数据资产\frontend
pnpm run typecheck   # 通过：exit 0（tsc+vue-tsc 双过）
pnpm run test        # 通过：0 failed（files/tests 数与 baseline 一致或仅增不减）
pnpm run build       # 通过：gzip 预算 main<=700 graph<=430 css<=110
```

### T2 后端门禁（全量）
```powershell
cd F:\python\数据资产\backend
$env:APP_TEST_DB_URL="postgresql+psycopg://postgres@127.0.0.1:15432/data_asset_test"
.\.venv\Scripts\python.exe -m pytest tests/ -q
# 通过：0 failed；若失败→按失败测试所属文件归属定位：169/167/system-map 域=本计划修；他人域=登记 exceptions 不修不代提交
```

### T2.5 重灌隔离库（**强制，不可跳过**——T2 夹具已清库）
```powershell
cd F:\python\数据资产\backend
$env:APP_TEST_DB_URL="postgresql+psycopg://postgres@127.0.0.1:15432/data_asset_test"; $env:APP_DB_URL=$env:APP_TEST_DB_URL; $env:APP_ENV="test"
.\.venv\Scripts\python.exe "..\开发起步包\output_r170\import170.py"
# 通过：输出 imported: ... tables=12702 relations=1329 ... token ready
```

### T3 浏览器级集成六查（隔离库真实数据）
起栈（uvicorn 显式 URL + `pnpm dev`）→ 跑 `开发起步包\output_r170\screenshot170.py`（工装已有）+ 补查：
1. `/#/asset/system-map`：KPI 六卡真实数、六步卡片、点击跳转（进 6 个目标路由各返回 200/组件渲染）
2. `/#/asset/graph`：overview 秒级出图无错误面板；搜索聚焦 HIS.PAT_VISIT 节点散开（中心暗密度 <35%，复用 169 度量）
3. `/#/value-domains`：列表+冲突 Tab（166 功能无回归）
4. `/#/asset/quality`、`/#/asset/admin`：旧页面无回归
5. 登录/未授权冒烟：无 token 访问 API 401/403
6. **菜单可见性**：platform_admin 登录态左侧菜单含"数据资产系统图"；仅 `asset.overview.view` 权限用户可见（filterNoPermissionTree 逻辑，可用测试或源码断言）
通过：六项全过+截图落 `output_r171/screenshots/`

### T4 修复闭环
- R1 每个提交：`git show --stat` 核对文件清单=计划清单（零他人域文件）
- R3 ①：新断言由红转绿记录
- .gitignore 生效：`git status --short` 不再出现 docs//~$//_r163_work/.release_*

### T5 生产验证（R2 发布后）
- 生产 `/#/asset/graph` 首屏 <3s 出图（授权用户或带凭据实测）
- `/#/asset/system-map` 生产可达且 KPI 为生产真实数
- 容器 healthy、前端 current=r171-<date>、previous 保留可回滚

### 总通过标准
T0 基线记录在案 + T1 三绿 + T2 全量 0 failed + T2.5 重灌成功 + T3 六查全过 + T4 闭环记录 +（授权后）T5 生产复验。

## 4. 执行顺序

T0 → T1（含 R3②菜单断言若走测试）→ T2 → T2.5 → T3 → R1 提交（授权）→ R2 发布（授权）→ T5 → R4 核对 → 执行报告+`171_结果.json`+README/55 登记。
（R1 也可提前至 T1 前提是用户先授权；默认测试先行提交在后。）

## 5. 边界与风险

- 他人半成品域全程不动；其测试若在 T2 失败→登记归因，不修不删。
- system-map 页面未入产前，生产无该路由（404 预期）——T5 依赖 R2。
- 生产已 r169：本计划不重复发布图谱修复，仅复核。
- git push 不做（commit 后留用户/主 AI 复核推送）。

## 6. DoD

- [ ] R1 三个域提交完成且 git show 核对零越界；.gitignore 生效
- [ ] T1/T2/T2.5/T3/T4 全过且证据落 output_r171/
- [ ] R2 发布完成（授权后）+T5 生产复验
- [ ] R4 遗留核对表 + 执行报告 + `171_结果.json` + README/55 登记
- [ ] 他人域文件工作区状态与 T0 基线完全一致（零触碰证明）

## 7. 执行提示词（复制给执行 AI，勿增删）

```text
【任务】一次性执行 开发起步包/171_系统前后端全面修复与测试一次性执行计划.md（v1.1）的 T0→R4。
【前置】round-7 分析定稿：生产已 r169（图谱已修，只复核）；工作区混他人半成品（登录签名/layout 域）。
【必读】171 号全文；review/round-7/最终报告.md+分歧表.md（裁决依据）；AGENTS.md；166 附录 A.2。
【铁律】171 §1 四条：他人域零触碰（不提交不修改不 stash，T0 基线对照自证）；通过标准=0 failed+基线一致（禁硬编码数量）；全量 pytest 后必须 T2.5 重灌再 T3；生产/git 逐项授权；偏差 WARN 落 output_r171/exceptions.json 继续，禁止提问。
【产出】171 §6 DoD 逐项自检；执行报告+171_结果.json（T0 基线/各批门禁原始输出/六查截图索引/git show 清单/他人域零触碰对照）；README/55 登记；P2 呈用户。
```
