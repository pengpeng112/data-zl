# 执行提示词：28_核查与整改计划 执行整改

> 复制下方 "完整提示词" 全段发给执行 AI。

---

## 完整提示词（直接复制粘贴给执行 AI）

```
你的任务：按照《开发起步包/28_核查与整改计划.md》对 `F:\python\data-asset-platform\` 后端 + 前端工程进行整改，让平台上线的所有计划模块真正可见、可用。

工作目录：
- 代码仓库：F:\python\data-asset-platform
  - 后端：F:\python\data-asset-platform\backend
  - 前端：F:\python\data-asset-platform\frontend
- 文档仓库：F:\python\数据资产\开发起步包

绝对必读（按顺序阅读，不得跳过）：
1. F:\python\数据资产\AGENTS.md（仓库性质、探库硬约束、安全红线、文档管理规则）
2. F:\python\数据资产\开发起步包\27_平台二次整改计划.md 之「第 20 节 结构决策记录」（已固化决策，不得重新讨论）
3. F:\python\数据资产\开发起步包\28_核查与整改计划.md 全文，重点看「第 10 节 复核补充」（优先级高于 1-9 节，遇冲突以第 10 节为准）
4. F:\python\数据资产\开发起步包\README.md 文件清单（启动自检用，每会话先做目录自检）

执行约束（不可违反，违反即整改失败）：
1. 不新建物理 schema——所有新表继续建在 `asset` schema，命名 `asset_<模块>_<实体>`。
2. Alembic 迁移必须手写，严禁 `--autogenerate`；每张新表都必须有 `upgrade()` 和 `downgrade()`。
3. P11 运维任务继续用 `asset_ops_tool_runs`（不另建审批表）；P12/P13 的 change-requests 复用 `asset_govern_change_requests`（module 字段区分）。
4. AI 工具边界：`tool-execute` 若实现，仅代理只读探查端点 + 强制审计，绝不透传任何写操作。
5. 不得破坏现有功能：
   - 保留旧 MVP 表（`asset_api_keys`/`asset_table_owners`/`asset_business_terms`/`asset_metadata_snapshots`）
   - 保留现有 `/api/v1/dict-medical/*`、`/api/v1/relations/{id}/review` 路径不变
   - 保留 11 个现有前端页面，仅在表详情/数据质量/治理管理/AI 协作 4 个页面内增补功能，不整体重写
   - `app/models/governance.py`（旧 MVP 模型）不要并入新治理模型文件
6. 安全红线：源库只读、运维写操作必须走 `asset_action_executors` 白名单 + 审批 + 审计、审计日志脱敏、姓名/身份证/电话/地址不进 AI 上下文。
7. 每完成一个模块：跑 `pytest tests/ -v`；补该模块集成测试；更新 `docs/api.md` 路径表。
8. 整改完成后：更新 28 号文件「第 11 节 完成状态追踪表」对应行的「状态」列。

执行顺序（严格按优先级路线图，28 号 §10.6）：

阶段一（阻塞，必须最先做）：
A. 后端表补建（28 号 §10.2 M-1~M-8）：
   - asset_dict_categories / asset_dict_standard_items / asset_dict_system_items / asset_dict_item_mappings / asset_dict_versions（P13 通用字典，建议放新文件 app/models/dict_general.py）
   - asset_quality_tasks / asset_quality_metrics（追加到 app/models/quality.py）
   - asset_relation_reviews（追加到 app/models/asset.py 或新文件；按 27 §5.3 字段建表，包含 relation_scope/from_*/to_*/join_condition/relation_desc_cn/business_logic_cn/confidence/validation_status/review_status/reviewer/reviewed_at/review_note/source_evidence 等字段）
   - 每张表配 Alembic 迁移文件 + downgrade
B. 修 Bug B1/B2：
   - app/services/metadata_diff.py:37 把 table-added/removed 时 system_code 用 "" 占位改为从快照字段读
   - app/api/v1/metadata_changes.py:39 在 collect-metadata 之前校验 source_code 是否在 asset_data_sources 中存在
C. 前端补 4 个新顶层菜单 + 页面（28 号 F-T1, F-T2~F-T5, F-T10）：
   - 新建 frontend/src/router/modules/ops.ts（运维工具，菜单 rank=11）
   - 新建 frontend/src/router/modules/identity.ts（人员与科室，rank=12）
   - 新建 frontend/src/router/modules/dict.ts（字典中心，rank=13）
   - 新建 frontend/src/router/modules/metadata.ts（变更检测，rank=14）
   - 新建 frontend/src/api/ops.ts、identity.ts、dict.ts、metadata.ts（API 客户端封装）
   - 新建 frontend/src/views/ops/ 目录含 3 页、views/identity/ 含 4 页、views/dict/ 含 4 页、views/metadata-changes/ 含 3 页
   - 参照现有 frontend/src/router/modules/asset.ts 和 views/asset/ 的写法、组件库（Element Plus + ECharts + @pureadmin/table）、API 调用模式（src/api/asset.ts）
D. 前端补 3 个数据资产内子页面（28 号 F-T6~F-T8）：
   - 在 frontend/src/router/modules/asset.ts 中追加 children：systems / sources / relation-review 三个路由
   - 新建 views/asset/systems/、views/asset/sources/（含资产树）、views/asset/relation-review/ 三个页面

阶段二（端点补齐 + 业务逻辑）：
E. 后端补齐遗漏端点（28 号 §10.3 全部，先补端点骨架，再补业务逻辑）：
   - app/api/v1/ai.py：新增 POST /tool-execute（只读代理+强制审计）
   - app/api/v1/ops_tools.py：补 /runs/{id}/reject、/runs/{id}/execute、/runs/{id}/audit 三个端点；并把 approve 的 approved_by 从 Query 参数改成 BaseModel body（B3）
   - app/api/v1/identity.py：补 /departments/{dept_code}、POST /collect-sources、PUT /accounts/bind、POST /change-requests、PATCH /change-requests/{id}/approve、POST /change-requests/{id}/execute、GET /inconsistencies
   - app/api/v1/dict_medical_api.py：补 PUT /code-sets、PUT /items、POST /change-requests 系列、GET /versions
   - 新建 app/api/v1/dict_general_api.py（通用字典 + categories + standard-items + system-items + mappings CRUD + ai-suggest-mappings + import）按前缀 /api/v1/dictionaries/*
   - app/api/v1/metadata_changes.py：补 GET /metadata-changes/{id}/impact
   - app/api/v1/governance.py：补 /enum-values CRUD
   - app/api/v1/governance_ops.py：补 POST /scheduler/jobs
F. 种子数据脚本（28 号 §10.5 S-1~S-6，建议放 scripts/seed_*.py，参照现有 scripts/import_candidates.py 模式）：
   - scripts/seed_systems.py：插入 DATA_CENTER/HIS_SOURCE/LIS/PACS/EMR/MOBILE_NURSING/SM
   - scripts/seed_data_sources.py：插入 ods_8_216 / his_source_10_10_10_15 等
   - scripts/seed_enum_values.py：补 review_status / approval_status / validation_status / account_status / dict_status 全量值
   - scripts/seed_quality_rules.py：补 27 §10 的 8 条规则
   - scripts/seed_ops_tools.py：补 msg_resend / order_status_check / sync_status_check 3 条模板（enabled=false 默认）
   - scripts/seed_roles.py：补 platform_admin / asset_viewer / ops_admin / identity_admin / approver 5 个角色
G. 后端业务逻辑补齐（28 号 §2）：
   - app/api/v1/systems.py:174 check 源真正调用 DatabaseConnector.test_connectivity()
   - app/api/v1/identity.py collect-sources 真正连 HIS 读 dept_dict + 临时人员，连 HRP 读正式人员
   - app/api/v1/dict_medical_api.py 差异采集（连 HIS/EMR 读字典表比对）
   - app/services/sync_executor.py 新建（run_sync 通用框架）
   - APScheduler 接入：app/main.py 启动时建单实例，从 asset_scheduler_jobs 读 trigger_mode=scheduled 的记录注册定时任务
H. 多数据库连接器（28 号 §10.4 C-1~C-6）：
   - app/services/db_connectors.py 新增 MysqlConnector / SqlServerConnector / PostgresConnector（海量数据库先留 stub）
   - app/services/metadata_collector.py 对应新增 MysqlMetadataCollector / SqlServerMetadataCollector / PostgresMetadataCollector
   - requirements.txt 追加 pymysql / pyodbc / psycopg[binary] 依赖

阶段三（安全加固 + 现有页面增补）：
I. RBAC 中间件启用：app/main.py 的 auth_and_logging_middleware 增加 role+permission 校验（或先实现函数供前端菜单按角色过滤）
J. 审计脱敏：app/api/v1/governance.py 的 _write_audit 增加 mask_sensitive 调用
K. Token 过期：asset_api_keys 加 expires_at 字段 + 中间件校验
L. 频率限制：加 slowapi 中间件，对 /api/v1/ai/tool-execute 等敏感端点限流
M. 前端现有页面增补（28 号 F-T9~F-T10）：
   - views/asset/table-detail/index.vue 增加中文注释编辑 Dialog
   - views/asset/quality/index.vue 增加按系统分组钻取
   - views/asset/admin/index.vue 增加快照对比→变更事件入口
   - views/asset/ai-tools/index.vue 增加 system-context 选择器

阶段四（测试与文档）：
N. 补集成测试（28 号 §4.2）：tests/test_ops_tools.py / test_identity.py / test_dict_medical.py / test_metadata_changes.py，每文件 ≥10 条
O. 文档：
   - 更新 backend/docs/api.md，把所有新增/补齐端点写入
   - 新建 开发起步包/29_扩展点契约.md（D-A）：列出新增数据库类型/字典类型/运维模板/定时任务/执行模式分别要改哪些文件
   - 更新 backend/.env.example，补 CREDENTIAL_ENCRYPT_KEY / APScheduler 时区等
   - 更新 开发起步包/README.md（如新增 29 号文档需登记）
P. 数据生命周期管理（28 号 §2.8）：app/core/config.py 加 SNAPSHOT_RETENTION_DAYS / EVENT_RETENTION_DAYS 配置，注册 job_type=data_lifecycle 的清理任务

验收命令（每个阶段结束跑一遍）：
- 后端测试：cd F:\python\data-asset-platform\backend; .\.venv\Scripts\python.exe -m pytest tests/ -v --tb=short
- 前端 Lint & 类型检查：cd F:\python\data-asset-platform\frontend; pnpm lint; pnpm typecheck
- 前端构建：cd F:\python\data-asset-platform\frontend; pnpm build
- 启动后端 + 前端 dev，访问浏览器确认左侧能看到 5 个顶层菜单（原有"数据资产" + 新增"运维工具" / "人员与科室" / "字典中心" / "变更检测"）
- 通过 /docs 检查所有新增端点可访问、返回 200

完成判定：
1. 28 号 §11「完成状态追踪表」所有任务状态都填了 [完成 YYYY-MM-DD HH:MM]
2. pytest 全部通过
3. pnpm lint 和 pnpm typecheck 无错误
4. pnpm build 成功生成 dist
5. 浏览器实际打开平台，能看到 5 个顶层菜单 + "数据资产" 内至少 14 个子页面

重要提醒（容易踩坑）：
- PowerShell 调 Python 时遇到中文乱码不要慌；文件编码本身 UTF-8 没坏，命令行输出乱码不影响代码
- 旧 MVP 模型文件 app/models/governance.py 含 admin 模块用的 ApiKey/TableOwner/BusinessTerm/MetadataSnapshot，**不要**把它合并到 governance_base.py 或删除
- 数据库 PostgreSQL 在本机，密码 admin@123，URL 中编码 admin%40123
- 不要把源库密码、Token 写入代码/git，用 .env
- 如果遇到不确定的设计决策，先读 27 §20 节；不要重新讨论已固化的决策
- 如果某个端点的现有实现路径与计划不一致，**保留现有路径**，不要无脑按计划改名（除非用户明确要求）；缺什么补什么，不要改已有的
- 每次会话开始先做"目录自检"（按 AGENTS.md 文档管理规则）

遇到 27/28 都没覆盖的新问题：在 28 号文件 §10 内追加新条目并标注「执行中补充」，按最高级原则（单一 schema / 不破坏现有 / 安全红线）决策；不要偏离主路线图去搞新功能。
```