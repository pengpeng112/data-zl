> 类别：发布 / 门禁  
> 日期：2026-07-13  
> 依据：`58_发布部署与代码同步整改计划.md`、`55` T2/T13/T14

# 65 发布门禁与凭据轮换清单

## 1. 本轮可代码完成 vs 人工完成

| 门禁 | 本轮状态 | 说明 |
|---|---|---|
| Git 发布基线 | **已闭合（本地）** | tag `release-20260713` → `a10f22a19d7eaf24a7fec12c2ce03b4021b7006e`；工作区 clean；**未 push** |
| 代码侧敏感串 | **通过** | 工作区 `backend/.env` 未跟踪；临时探查脚本已 `.gitignore` |
| 生产 Mock 关闭 | **通过** | `frontend/build/plugins.ts` 仅 dev 启用 |
| readiness `/health` | **通过** | DB 失败 503 |
| 调度时区 | **通过** | `APP_SCHEDULER_TIMEZONE` |
| 离线文档步骤 | **文档就绪** | `deploy/offline/README.md` |
| 凭据轮换 | **待人工签字** | 见 §3（代码无法代做） |
| TLS | **待运维** | 当前 8.83 仅 HTTP:80 |
| Linux wheelhouse + 干净断网演练 | **待内网构建机** | Windows 开发机不可替代 |
| 生产 `APP_RBAC_REQUIRE_BOUND_TOKEN=true` | **待运维改 env 并重启** | 见 §4 |

## 2. Git 基线验收命令

```powershell
cd F:\python\数据资产
git status --short          # 期望：空（或仅明确未纳入的资产）
git log -1 --format="%H %s"
git tag --list "release-*"
```

禁止进入 Git：`.env`、私钥、`wheelhouse/`、`frontend/dist/`、`backend/scripts/_ro_*` 等临时脚本。

## 3. T13 凭据轮换（人工，必须签字）

> 历史 `admin123`、固定 Token 等**可能已在 git 历史**中出现；删除工作区文件不能撤销历史泄露。

| # | 动作 | 责任人 | 完成日期 | 证据位置（无密文） |
|---|---|---|---|---|
| 1 | 轮换平台 PostgreSQL 密码（`postgres`/`asset_app`） | DBA | ____-__-__ | 密钥柜/工单号：________ |
| 2 | 更新 8.83 `/etc/data-asset/backend.env` 中 `APP_DB_URL` 后重启 API | 运维 | ____-__-__ | `systemctl`/`docker` 重启记录 |
| 3 | 禁用 git 历史中的固定 ApiKey / 旧 Token；仅保留 `create_admin_token.py` 新签 Token | 运维 | ____-__-__ | Token 清单（摘要前 8 位） |
| 4 | 平台管理员自助改密（`platform-admin` 等） | 管理员 | ____-__-__ | 登录成功截图/审计 |
| 5 | 轮换/确认 ODS、HIS 只读账号密码；更新 `/etc/data-asset/credentials/*`（600） | DBA | ____-__-__ | 凭据文件 mtime |
| 6 | SSH `known_hosts` 预置跳板机/源库主机密钥（去掉 AutoAdd 依赖） | 运维 | ____-__-__ | known_hosts 路径 |
| 7 | JWT：生产改为 RS256 或轮换 `APP_JWT_SECRET` | 运维 | ____-__-__ | env 配置项名 |

**判定**：上表日期与负责人齐全 → T13 可标完成。AI 不得代填虚假完成日期。

## 4. 8.83 生产配置加固清单

| 配置 | 推荐值 | 当前建议 |
|---|---|---|
| `APP_ENV` | `production` | 确认 |
| `APP_RBAC_REQUIRE_BOUND_TOKEN` | `true` | **改完须重启容器** |
| `APP_OPS_WRITE_ENABLED` | `false` | 保持关闭 |
| `APP_AUTH_COOKIE_SECURE` | HTTPS 后为 `true`；纯 HTTP 保持 `false` | 内网 HTTP 暂 false |
| Instant Client | 镜像/entrypoint 固定 `libclntsh.so → 19.1` | 当前容器内临时修复 |
| 凭据卷 | `-v /etc/data-asset/credentials:/etc/data-asset/credentials:ro` | **重建容器前必须加** |
| backend.env | 0640，不进 Git | 已在服务器 |

## 5. 离线包与干净演练（R2/R3）剩余步骤

1. 在 **Linux** 构建机检出 `release-*` tag。  
2. `pip download` 生成 `wheelhouse/` + `SHA256SUMS`（不进 Git）。  
3. 开发机已构建的 `frontend/dist` 随包携带（不进 Git）。  
4. 断网 VM 按 `deploy/offline/README.md` 装到 `/health` 200。  
5. 发布单记录：tag SHA、包哈希、Alembic head、DB 备份路径。

## 6. 本轮 Git tag 约定

- 建议 tag：`release-20260713`（annotated）  
- 含义：登录上线 + 只读接入 + 人员入库 + 发布整改代码基线  
- **不等于** 正式生产封板：须 §3 人工项 + 可选 TLS/离线演练后，方可标「正式发布」

## 7. 回滚要点（摘录 58）

1. `current` 软链接切回上一 release  
2. 平台库优先恢复迁移前 dump  
3. 禁止故障现场覆盖运行目录、禁止删除备份  
