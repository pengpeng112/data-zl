> 类别：证据链
>
> 状态：**已执行完成**——医院 OA（万户 ezOFFICE）只读探查、工作流引擎关系专题验证与平台幂等导入全部完成；二次 dry-run 全 0；抽查与健康检查通过。
>
> 执行日期：2026-08-22
> 上位计划：`139_四系统只读探查_视图关系治理与服务器升级一次性执行计划.md`（OA 作为第五源按同一管线与分层治理扩展）
> 前序证据：`142_四系统资产导入与139收口执行报告.md`；OA 探查（本日）见 `backend/_oa_work/oa_analysis_summary.json`
> 机器可读结果：`143_OA资产导入与工作流引擎验证执行结果.json`
> 授权依据：用户 2026-08-22 指示"请你进行 2 和 3 的操作"（纳入平台 + 引擎关系专题验证），接续同日 OA 只读探查授权

# OA 资产导入与工作流引擎关系验证执行报告（143）

## 0. 结论先行

1. 医院 OA（`10.10.10.69:1433`，SQL Server 2008 R2，万户 ezOFFICE，schema `ezoffice`）按 139 同一管线与分层治理纳入平台：**+1 系统/+1 数据源/+1,685 对象/+29,956 字段/+141 声明外键结构关系/+5 复核草稿（全 draft，含限量验证证据）/21 视图依赖**；二次 dry-run 全 0，抽查全命中，`/health` 200，容器未重启（仅资产导入）。
2. **工作流引擎关系专题验证 12/12 全部成功**：4 条未声明引擎链路（历史任务/活动、运行任务、执行实例 → 流程实例）命中率 **99.96%–100%**，2 条业务链路（待办→人员 99.91%、组织成员→人员 100%）；4 条引擎链作为带验证证据的 review 草稿入库，与声明 FK 重复的 2 条被正确去重（`skipped_existing_formal=2`）。
3. 管线零分叉：构建器/导入器以**配置驱动扩展** OA（`--systems` 子集 + OA profile + 独立确认串 `APPLY-PLAN139-OA-SOURCE`），四源历史行为不变；新增 3 项测试，`tests/plan139` 共 **57/57 通过**。
4. 安全红线：OA 源库零写入（只读会话/仅元数据 SELECT/聚合验证零患者值）；`admin` 为高权限账号，仅一次性只读使用，**强烈建议轮换口令**并改用专用只读账号；凭据仅存 8.83 受控文件。

## 1. 来源与采集

| 项 | 值 |
|---|---|
| 端点/引擎 | `10.10.10.69:1433`，SQL Server 2008 R2（10.50.1600.1），TDS 受控回退 7.0 |
| 业务库 | `oa`（ReportServer/ReportServerTempDB 排除） |
| 产品识别 | 万户 ezOFFICE（schema `ezoffice` 1,674 表 + `dbo` 7 表；`whir$`/`ez`/`wf` 前缀、`EZ_FLOW_*` 引擎、`OA_HOS_*` 医院定制） |
| 采集结果 | 1,681 表/4 视图/29,956 字段/1,349 PK+UK/**141 FK**/1,506 索引/26 过程/8 触发器；0 错误；`source_writes=0` |
| 凭据 | `/etc/data-asset/credentials/oa_sqlserver_10_10_10_69.readonly`（600 root:root，admin 高权限，一次性只读） |

结构要点：836/1,681 表估算 0 行（未启用表单 `whir$t3xxx` 604 张 + 未用模块）；`his_*` 12 张为医学考试模块且全空（非 HIS 对接）；日期快照表（`*_202xxxxxx`）按治理口径标记 pending 不删除；`WF_SEQUENCE` 7,620 万行为发号器，任何查询禁触。

## 2. 工作流引擎关系专题验证（用户指令 3）

方法：139 §4.4 同款限量聚合验证——`DISTINCT TOP (10000)` 子键 + LEFT JOIN 父表计数、READ UNCOMMITTED + LOCK_TIMEOUT + DEADLOCK_PRIORITY LOW、仅聚合计数、零患者值，在 8.83 容器内执行。

| 类别 | 关系 | sampled/matched | 命中率 |
|---|---|---|---|
| 声明 FK | EZ_FLOW_RU_TASK.EXECUTION_ID_ → EZ_FLOW_RU_EXECUTION | 10000/10000 | 1.0 |
| 声明 FK | EZ_FLOW_RU_TASK.PROC_DEF_ID_ → EZ_FLOW_RE_PROCDEF | 1634/1634 | 1.0 |
| 声明 FK | EZ_FLOW_RU_IDENTITYLINK.TASK_ID_ → EZ_FLOW_RU_TASK | 0/0（运行期空） | — |
| 声明 FK | EZ_FLOW_RU_VARIABLE.EXECUTION_ID_ → EZ_FLOW_RU_EXECUTION | 10000/10000 | 1.0 |
| 声明 FK | EZ_FLOW_ID_MEMBERSHIP.USER_ID_ → EZ_FLOW_ID_USER | 0/0（未启用） | — |
| 声明 FK | WF_ACTIVITY.WF_WORKFLOWPROCESS_ID → WF_WORKFLOWPROCESS | 1/1 | 1.0 |
| **引擎链** | EZ_FLOW_HI_TASKINST.PROC_INST_ID_ → EZ_FLOW_HI_PROCINST | 10000/9996 | **0.9996** |
| **引擎链** | EZ_FLOW_HI_ACTINST.PROC_INST_ID_ → EZ_FLOW_HI_PROCINST | 10000/10000 | **1.0** |
| **引擎链** | EZ_FLOW_RU_TASK.PROC_INST_ID_ → EZ_FLOW_HI_PROCINST | 10000/10000 | **1.0** |
| **引擎链** | EZ_FLOW_RU_EXECUTION.PROC_INST_ID_ → EZ_FLOW_HI_PROCINST | 10000/10000 | **1.0** |
| 业务链 | WF_WORK.WF_CUREMPLOYEE_ID → ORG_EMPLOYEE.EMP_ID | 3195/3192 | 0.9991（3 孤儿≈离职人员） |
| 业务链 | ORG_ORGANIZATION_USER.EMP_ID → ORG_EMPLOYEE.EMP_ID | 6275/6275 | 1.0 |

结论：**EZ_FLOW 引擎四条核心链路可作为高置信血缘主线**（历史任务/活动、运行任务、执行实例均以 `PROC_INST_ID_` 汇聚到 `EZ_FLOW_HI_PROCINST`，即 Activiti 语义的本库实现）；4 条引擎链已作为 review 草稿入库并携带上述验证证据；声明 FK 6 项仅作证据记录（已随 141 条 FK 结构关系入库）。开放项：表单数据（`whir$t3xxx`）经 `WF_WORK.WORKTABLE_ID/WORKRECORD_ID` 桥接流程（无静态列可证实，未连线索，待专项）。

## 3. 平台导入（用户指令 2）

- 扩展方式：`build_plan139_asset_package.py`/`import_plan139_sources_to_platform.py` 增加 OA profile 与 `--systems` 子集（默认全量行为不变）；确认串新增 `APPLY-PLAN139-OA-SOURCE`（四源串保持历史不变）；验证器 `_meta` 回填全限定表名。
- 门禁：实时计数（19/19/9497/113751/1160/86/64）→ 备份 `/opt/data-asset/backups/data_asset_pre_oa143_20260822075047.dump`（9,014,716 B，SHA-256 `6b1798a2…f17109`）→ dry-run 审阅：**删除=0、正式 view-JOIN 新增=0（141 全为声明 FK 结构层）、active recipe=0、来源冲突=0、2 条候选与声明 FK 重复被正确去重** → apply（`APPLY-PLAN139-OA-SOURCE` + run_id `oa-20260822`）。
- 二次 dry-run：tables 0/0、FK 0/0、eligible 0/0、relations_delta=0——**全 0**。
- 抽查：3 表/2 视图直接命中 + 第 3 视图（tManager）经依赖边证实；141 FK 在库；5 条草稿全 draft 且 **5/5 携带 bounded_validations 证据**；`/health` 200、容器 Up 未重启。

| 计数 | 前 | 后 | 增量 |
|---|---:|---:|---:|
| 系统/数据源 | 19/19 | **20/20** | +1/+1 |
| 表/视图对象 | 9,497 | **11,182** | +1,685 |
| 字段 | 113,751 | **143,707** | +29,956 |
| 正式关系 | 1,160 | **1,301** | +141（全部 declared_foreign_key） |
| 复核草稿 | 86 | **91** | +5（全 draft） |
| 关系配方 | 64 | 64 | 0（无复杂视图配方合格项） |
| 视图依赖（批次） | — | 21 | +21 |

## 4. 产物与证据

- 本机：`开发起步包/数据资产_OA源端资产包/`（含 import_result.json；**gitignore，表结构不入 Git**）；`backend/_oa_work/`（快照/分析/验证原始结果，gitignore）。
- 服务器：`/opt/data-asset/evidence/oa-143-<时间戳>/`（750/640 + SHA256SUMS：configs、包导出、dry-run×2、import_result、validation_results、抽样结果）；备份见 §3；容器 `/app/scripts` 已同步扩展后脚本。
- 测试：`tests/plan139` 57/57（+3 新增：OA 命名空间/导入 profile 与确认串/未知确认串拒绝）。

## 5. 遗留事项（仅登记）

1. `admin/admin@123` 高权限弱口令且曾在聊天出现——**建议立即轮换**，长期接入改用专用只读账号（db_datareader+VIEW DEFINITION+DENY EXECUTE），平台连接登记为 needs_readonly。
2. 5 条引擎/视图草稿待人工独立复核（同一执行者不自批）；表单-流程桥接（WF_WORK.WORKTABLE_ID/WORKRECORD_ID → whir$t3xxx）需专项验证后再连线索。
3. 本轮代码/文档改动未 commit/push（资产包与快照按既定策略不入 Git）；`OA办公系统` 中文名为产品识别默认名，可由人工修正。
