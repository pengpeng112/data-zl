> 类别：执行报告
>
> 状态：**已完成（2026-09-01；T0→R4 一次性闭环；生产前端已切 r171-20260901、167 后端核验已在产；收尾批已补：git push 19d2027..b942cca 已上 GitHub、T5 浏览器级生产实拍完成、165 X2 生产首轮探查完成（日间豁免经用户授权）；他人域零触碰）**
>
> 上位计划：`171_系统前后端全面修复与测试一次性执行计划.md`（v1.1，round-7 裁决定稿）
>
> 机器可读证据：`171_系统前后端全面修复与测试_结果.json`；偏差台账 `output_r171/exceptions.json`（W1-W9）

# 171 执行报告：系统前后端全面修复与测试一次性闭环

## 0. 概要

按 171 v1.1 顺序 T0→T1→T2→T2.5→T3→R3→R1→R2→T5→R4 全部走完：

| 门禁 | 结果 |
|---|---|
| T0 基线 | 隧道重建+生产只读重导+隔离库重灌（tables=12702 relations=1329）；git/哈希/vitest 基线落 `output_r171/baseline.txt` |
| T1 前端 | typecheck 双过；vitest **253/253 0 failed**（基线 245+R3 新增 8，仅增不减）；build gzip 656.8/404.5/102.3 全过 |
| T2 后端 | 全量 **1341 passed / 1 skipped / 0 failed**（27m43s，显式 APP_TEST_DB_URL；失败归因案件 0） |
| T2.5 重灌 | 强制执行：import170 tables=12702 relations=1329 token ready |
| T3 六查 | 六项全过+截图 15 张落 `output_r171/screenshots/`；**过程抓获并修复 2 个 169 域活体缺陷（§3）** |
| R3 补强 | plan171R3.test.ts 8 用例（含对 HEAD 旧版实测红→绿记录）；depth 控件记录 |
| R1 提交 | 4 提交（169/167/170/171-卫生），`git show` 逐一核对零他人域越界；.gitignore 生效 |
| R2 发布 | 前端原子切换 **r171-20260901**（previous=r169-20260831-storage-fix，ASSETS_OK+nginx reload）；167 后端经 md5+进程时间三证**已在产运行**（W7，未重启容器） |
| T5 复验 | 容器 healthy、current 指向、端到端 200、入口 JS hash 一致、鉴权 401 在位；生产 KPI=导出数双证；浏览器级截图归 P2 |
| R4 核对 | 165T7 维持 BLOCKED/168P1、166P2 维持开放/r169 完整性四项过（截图件归 P2）；165X2 于收尾批补完（§10） |

## 0.1 收尾批速览（2026-09-01 晚补执行，详见 §10）

git push 已上 GitHub（19d2027..b942cca）；T5 浏览器级生产实拍两页完成（system-map 六 KPI 全渲、graph 首屏 14+ 节点分散不再墨团）；165 X2 生产首轮探查完成（12 模板 11 过 1 BLOCKED，新增 6 条 open 发现+审计 198 号，约 35s）；临时 token/隧道已清零；新发现 P2：图谱聚合边标签渲染 `[object Object]`（§10.5）。

## 1. T0 基线与工装修复（W1）

隧道断开→按 166 附录 A.1 重建；`output_r170` 工装 export170/import170 的 import 行写错模块（`AssetDataSource/AssetSystem` 实在 `asset_system.py`），且 `export170.json` 本地缺失——修正后从生产容器**只读**重导（26 系统/17.8 万字段/77 值域/192 审计/422 质量规则）并重灌，输出与 171 T2.5 预期签名一致。他人域 19 文件 `git hash-object` 基线落 `baseline.txt`。

## 2. 门禁结果（T1/T2/T2.5）

- **T1**：typecheck 首跑揪出 system-map `tone="success"` 类型错（round-7 只修了测试未跑 typecheck）→ 域内改 `accent`（不动 15 页面共用的 ReStatCard），W2。终态 typecheck exit 0 / vitest 253/253 / 预算三绿。
- **T2**：全量 1341/1/0（较 166 时代 +53 均为各域新增测试，W9 核对清单一致；无失败→无归因案件，他人域测试全绿）。
- **T2.5**：铁律③执行，重灌成功后才开始 T3。

## 3. T3 六查与两处活体缺陷根因修复（W6，169 域）

六查全过（详见 `_结果.json` §t3）。首轮 169 工装复跑发现 explore 模式下"图谱接口请求失败"面板持续可见而**后端全 200、网络零失败**——逐层定位（pg 无非 200→requestfailed 空→最小复现锁定"搜索聚焦后必现"）：

1. **误报根因双叠**：`globalSearch` 把 `focusNode` 画布竞态失败经 `classifyGraphError` 归为 api_error；且成功路径从不清 `errorInfo`，面板一经触发永不消失（`v-if="errorInfo"`）。
2. **RePageHeader 未导入**：graph 页模板用 `<RePageHeader>` 无 import，页头静默丢失（Vue warn 唯一线索）。

修复（均在 169 域文件内）：focusNode 独立 try 隔离；面板改 `v-if="errorInfo && isErrorState"`；补 import。`t3_repro_panel.py` 复验三检查点面板均消失、警告清零；plan169G2 旧断言锁的是缺陷行为，随修复**加严**（非弱化）；全套门禁复跑全绿。修复后密度复测：overview 1.3% / 聚焦 8.7% / 1→2 跳 4.6%（阈值 35%）。

工装局限如实登记（W4/W5）：169 验收脚本 A3（点已选档不触发 change）、A4（双击命中随布局浮动）为状态/位置依赖，以聚焦探针 B1-B4 等价复验（同端点真实请求+密度门槛）收口；复跑覆盖了 `output_r169/验收_原始输出.json`（原值以 169 报告引文为准）。

## 4. R1 四域提交（git 写，用户任务书授权，未 push）

| commit | 域 | 文件数 | 要点 |
|---|---|---|---|
| `da7255e` | 169 图谱 | 18 | graph.py N+1 修复等生产已验证源码收口+**171 T3 两活体缺陷修复**；asset.ts 仅 hunk1（overview 30s） |
| `bec3ad4` | 167 AI SQL | 7 | 预算修复收口；asset.ts 仅 hunk2（generateAiSql 120s） |
| `8c49b32` | 170 system-map | 3 | 页面+路由+测试 |
| `d12aaee` | 171 卫生+R3 | 2 | .gitignore（docs//~\$/.release_*/_r163_work；*.log 原有全局覆盖）+plan171R3.test.ts |

零越界核对：四提交 `git show --name-only` 过他人域正则（identity/conftest/layout/useTag/useDataThemeChange/store/modules/app/captureMode）**零匹配**；`api/asset.ts` 双 hunk 以补丁按域分别 staging（169 批 6 行/167 批 7 行），提交后工作区与 HEAD 零 diff；域清单按"每文件 diff 复核归属"扩至实际足迹（W3）。**他人域 19 文件终检哈希与 T0 基线完全一致、stash=0——零触碰证明成立。**

## 5. R2 发布与 T5 生产复验（W7/W8）

- **167 后端**：发布前侦察发现生产 `ai.py`/`ai_context_builder.py` md5 与工作区一致（r169 会话 08-30 22:55 热补），容器 08-31 08:56 启动晚于热补 10h→**运行时已加载**。R2 后端由"发布"改为"在产核验"，避免无意义生产重启。
- **前端**：dist（门禁版构建，入口 `index-CHucUjDA.js`）→ `releases/r171-20260901`，HTML 引用资源完整性 ASSETS_OK，原子切换 current（previous=r169-20260831-storage-fix 保留可回滚），nginx -t+reload。
- **T5**：容器 healthy；首页/入口 JS 200 且 hash 与本地一致；system-map 路由在 bundle；API 无 token 401（graph/ai）伪 token 403；生产 KPI 真实数=T0 生产导出数（26/12702/1329/422/77）与隔离库 T3 渲染同数双证。浏览器级生产截图在收尾批经一次性 token 补齐（§10.2）。

## 6. R4 遗留核对（只登记不改代码）

| 项 | 状态 | 证据 |
|---|---|---|
| 165 X2 生产首轮夜间探查 | **收尾批已完成（2026-09-01 日间豁免经用户授权，§10.3）** | run `probe-20260901-191051`：12 模板 11 过 1 BLOCKED（T7 预设）、finding_new=6、审计 198 号、发现页 UI 实拍 |
| 165 T7 | 维持 BLOCKED | run_probe.py side-b 阻断机制仍在（PACSREPORT 无申请键） |
| 168 P1 | 维持开放 | Minimap/视口历史/LOD/review 队列/跨系统快捷层未动 |
| 166 P2 | 维持开放 | 对账矩阵/趋势/互链/AI 探查入口/批量导入未动 |
| r169 发布完整性 | 过 | 镜像 healthy；current 现 r171（r169 归 previous 保留）；overview 1ms 级（round-7 实测）；授权用户截图件归 P2 |

## 7. 红线自检

他人域零触碰（哈希终检+stash 0+四提交零越界）；源库零连接（仅平台生产库只读导出）；生产写操作仅前端版本目录+软链切换与（侦察后取消的）后端重发，无业务库写、无 DML/DDL；凭据零落盘（仅用既有公钥通道）；pnpm 全程；隔离库显式 `APP_TEST_DB_URL`；禁提问、禁弱化断言（plan169G2 为加严非弱化）；偏差 W1-W9 全部落 `output_r171/exceptions.json` 继续执行。

## 8. 呈用户（P2 后置清单；1/2/4 已在收尾批闭环，见 §10）

1. ~~生产浏览器级验收~~ **已闭环**：收尾批经一次性 token 实拍 `/#/asset/system-map` 与 `/#/asset/graph` 首屏（§10.2），建议用户用真实账号再走一遍确认观感。
2. ~~git push~~ **已闭环**：19d2027..b942cca 已推 GitHub（§10.1）。
3. **他人域处置**：登录签名/layout 半成品（19 文件）保持未提交原样，归原会话（用户已确认保留）。
4. ~~165 X2 夜间探查授权~~ **已闭环**（§10.3）；剩余：**X3 终态人工裁决**（生产已有 6 条 open 发现待用户在 /probe-findings 界面裁决）、X4 常态化调度周期（用户定）、T7 side-b 补齐、168 P1/166 P2 后置项维持开放。
5. （可选）169 验收工装 A3/A4 改确定性触发（W5）。
6. **新 P2**：图谱 overview 聚合边标签渲染为 `[object Object]`（§10.5，两行级修复，待用户点头后随下批发布）。

## 9. DoD 逐项自检（171 §6）

- [x] R1 三（四）域提交完成且 git show 核对零越界；.gitignore 生效
- [x] T1/T2/T2.5/T3/T4 全过且证据落 output_r171/
- [x] R2 发布完成（前端原子切换；167 后端在产核验 W7）+T5 生产复验（浏览器级截图 P2）
- [x] R4 遗留核对表+执行报告+`171_结果.json`+README/55 登记
- [x] 他人域文件工作区状态与 T0 基线完全一致（零触碰证明）

## 10. 收尾批（2026-09-01 晚，主会话补执行；用户已授权 push/生产验证/X2 日间豁免）

### 10.1 git push（§8-2 闭环）

直连 22 端口 3 次断流（fake-ip 代理 198.18.0.116 截获 `reading sideband packet`），改走 443 复用 + 仓库 deploy key 显式指定：

```
GIT_SSH_COMMAND="ssh -p 443 -i .ssh/deploy_key -o IdentitiesOnly=yes" \
  git push ssh://git@ssh.github.com:443/pengpeng112/data-zl.git master
```

结果 `19d2027..b942cca`（R1 四提交 da7255e/bec3ad4/8c49b32/d12aaee + 文档 6d354d6 + 资产同步 b942cca），`git status` ahead 0。deploy key 仅对 pengpeng112/data-zl.git 有效，未写任何新凭据文件（.ssh/deploy_key 为既有设施）。

### 10.2 T5 浏览器级生产实拍（§8-1 闭环）

生产探针通道：`ssh -fN -L 18432:127.0.0.1:80` + 一次性 ApiKey `verify-t5-r171-prod`（platform_admin，实拍后已删）。Playwright 1920×1080 两页：

| 页面 | DOM 证据 | 视觉核验 |
|---|---|---|
| `/#/asset/system-map` | statCards=6、无错误面板、菜单在 | 六 KPI（26 系统/12767 表/178547 字段/1329 关系/186 规则/26 值域）+六步治理链路卡齐全、无报错空白 |
| `/#/asset/graph` 首屏 | canvas=4、无错误面板、菜单在 | 14+ 系统节点分散清晰、连线与图例正常——**169 墨团修复在生产生效** |

截图 `output_r171/screenshots/T5_prod_system-map.png`、`T5_prod_graph_first_screen.png`；DOM 证据 `t5_prod_实拍.json`。注：system-map KPI 表数 12767 较 T0 导出 12702 多 65，为 09-01 生产资产同步（b942cca）后的正常增长。

### 10.3 165 X2 生产首轮探查（§8-4 部分闭环）

- **授权与豁免**：用户 2026-09-01 明示"X2 现在就可以跑"——按 165 v1.2 先例记一次性日间豁免（源侧仍走 8.83 受控连接器只读；单模板 120s/整轮 60min 内置上限不变；此后全量 run 仍一律夜间）。
- **前置核验**：生产库 `asset_probe_findings`/`asset_probe_runs` 两表已在（随发布迁移 apply，X1 建表完成）、0 轮 0 发现；容器缺 sjzc 脚本与模板目录 → 沿用 E3 同款拓扑（本机执行器 + sjzc 受控连接器），写侧经新隧道 15434 指向生产 `data_asset`（口令仅内存流转未落输出）。
- **执行**：`run_probe.py --today-cut`（窗口 2026-08-01→09-01，与 E3 首轮同窗可比），run_id `probe-20260901-191051`，12 模板 11 ok + T7 计划内 BLOCKED，11 个模板合计约 35s（远低于上限）。
- **结果**：finding_new=6 / updated=0 / relapse=0，全部 open 待 X3 人工裁决；`asset_govern_audit_logs` id=198（operator=probe:probe-20260901-191051）。

| # | 探查项 | 类型 | 实测值 | 阈值 | 严重度 | 与 E3 隔离库对照 |
|---|---|---|---|---|---|---|
| 1 | EXAM.EXAM_MASTER.DOCTOR_USER 缺失率 | R-REF | 81.40% | >1 | P2 | 81.37%（<0.1pp） |
| 2 | MEDREC.OPERATION.ANESTHESIA_DOCTOR 缺失率 | R-REF | 10.22% | >1 | P3 | 10.58% |
| 3 | TREAT_RESULT HIS↔JHEMR 语义桶差异率 | R-XSYS | 46.25% | >5 | P2 | 43.22% |
| 4 | EXAM 医生姓名与 STAFF_DICT 不一致率 | R-REF | 4.81% | >1 | P2 | 4.93% |
| 5 | EXAM 报告时间回写率（缺口） | R-CNT | 34.29% | >10 | P1 | 34.48% |
| 6 | LAB↔LIS 月度数量对账差 | R-CNT | 6.66% | >2 | P2 | 5.80% |

六项与 E3 同窗基线全部 <3pp，生产/隔离库口径一致性成立。UI 实拍 `X2_prod_probe-findings.png`（6 行渲染、run_id 可见、零报错，DOM 证据 `x2_prod/x2_findings_实拍.json`）。run JSON `x2_prod/probe-20260901-191051.json`。

### 10.4 收尾清理（全部完成并复核）

生产库 `DELETE asset_api_keys/asset_user_roles` 各 1 行（verify-t5-r171），复核剩余 0；隧道 18432/15434 关闭（netstat Listen 0；15432 隔离库隧道与他人 11433 隧道未动）。探查写侧仅 probe 两表+审计一行，无业务表 DML。

### 10.5 新发现 P2：图谱聚合边标签 `[object Object]`

T5 实拍中发现：overview 聚合边上的标签渲染为 `[object Object]`（图例正常、节点正常，仅边标签）。根因：`RelationGraph.vue:274/432` 对 `edge.label` 直接取值/`String()`，而该路径下 label 为 G6 样式对象（`{text:...}`）而非字符串。修复方向（两行级，待用户点头随下批发布）：取值前归一 `typeof label === "object" ? label.text : label`，两处同改+vitest 断言。不影响数据正确性，纯显示。
