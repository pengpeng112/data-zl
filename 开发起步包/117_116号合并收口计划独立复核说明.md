> 类别：待办
>
> 状态：待复核 | 优先级：P0 | 制定日期：2026-08-04
> 用途：交给其他 AI 独立复核 116 号合并收口执行计划，不授权执行修改
# 116 号合并收口计划独立复核说明

## 1. 复核目标

请对以下文件和当前代码进行独立、逐项、证据化复核，判断 116 号计划是否完整、顺序是否合理、是否遗漏阻断项、是否存在错误结论或不安全授权。

本次只做 review，不执行代码修改，不执行数据库 DML/DDL，不进行生产发布，不开启任何写开关，不执行正式同步，不执行 Git push。

## 2. 工作区和必读路径

工作区绝对路径：

```text
F:\python\数据资产
```

必须先读取：

```text
F:\python\数据资产\AGENTS.md
F:\python\数据资产\开发起步包\README.md
F:\python\数据资产\开发起步包\55_系统未完成事项统一执行计划.md
F:\python\数据资产\开发起步包\116_系统剩余事项合并收口执行计划.md
```

必须交叉读取：

```text
F:\python\数据资产\开发起步包\85_系统最终封板与全量验收详细执行计划.md
F:\python\数据资产\开发起步包\97_关系图谱与字典下发复核整改计划.md
F:\python\数据资产\开发起步包\100_Neo4j_S0阻断问题修复与PoC放行计划.md
F:\python\数据资产\开发起步包\101_临床诊断映射导入同步与前端工作台一次性整改计划.md
F:\python\数据资产\开发起步包\111_当前代码全面核查问题整改执行计划.md
F:\python\数据资产\开发起步包\112_诊断手术人工维护自动同步与HIS人员双目标同步整改执行计划.md
F:\python\数据资产\开发起步包\113_执行111号代码安全整改严格限制提示词.md
F:\python\数据资产\开发起步包\114_执行112号双同步专项整改严格限制提示词.md
F:\python\数据资产\开发起步包\115_112号双同步专项整改完成记录.md
```

需要检查的主要代码路径：

```text
F:\python\数据资产\backend\app\services\dict_medical_push.py
F:\python\数据资产\backend\app\services\dict_sync_executor.py
F:\python\数据资产\backend\app\services\dict_sync_worker.py
F:\python\数据资产\backend\app\services\medical_code_push.py
F:\python\数据资产\backend\app\services\identity_sync_orchestrator.py
F:\python\数据资产\backend\app\services\identity_sync_executor_bridge.py
F:\python\数据资产\backend\app\services\cdms_identity_adapter.py
F:\python\数据资产\backend\app\services\jhemr_identity_adapter.py
F:\python\数据资产\backend\app\services\his_identity_sync.py
F:\python\数据资产\backend\app\core\config.py
F:\python\数据资产\backend\tests\dict_sync_112
F:\python\数据资产\backend\tests\test_identity_sync.py
F:\python\数据资产\backend\alembic\versions
```

## 3. 复核顺序

严格按以下顺序复核：

1. 复核 116 是否正确合并 85/97/100/101/111/112，是否遗漏原计划中的 P0 阻断。
2. 复核 55 与 116 的优先级、状态和入口关系，特别检查 F1/F11 状态差异。
3. 复核测试库门禁、Alembic 往返和全量测试前置条件。
4. 复核生产失败关闭、RBAC、权限矩阵、敏感信息和 Git 收口要求。
5. 复核关系图谱、Neo4j、字典导入和字典下发阶段的依赖顺序。
6. 复核诊断/手术人工审核后自动同步链路：outbox、worker、预检、冲突、事务、回读、补偿和审计。
7. 复核 HIS→CDMS/JHEMR 人员同步：分类、角色、科室、凭据、Phase D、双目标独立事务和 nightly 门禁。
8. 复核 `serial_no` 方案是否被错误地描述为已确定；没有 DBA/厂商结论时必须保持阻断。
9. 复核 U1–U9 待确认项是否足够、是否有可以用代码或只读数据库证据直接确认的项目。

## 4. 必查业务口径

确认 116 是否准确保留以下用户已确认口径：

- 诊断/手术人工维护并审核后即时同步，不走夜间任务。
- HRP→目标同步本期暂缓。
- HIS `STOP_FLAG=0` 启用、`1` 停用。
- JHEMR `isstop=0/NULL` 启用、`1` 停用。
- HIS 重复编码：0 行新增；1 行完全一致幂等跳过；1 行不一致冲突；多行冲突。
- JHEMR 对照表实际名称为 `diagnosis_contrast_dict`、`operation_contrast_dict`。
- CDMS：`FID=HIS工号`、`FUSER=admin`、`FUPDATEUSER` 为空。
- CDMS：FTYPE 0/2/3/5/10 的语义和 `100005`、`A00001`、出院权限 `1`。
- CDMS FTYPE 8/32 禁止自动创建。
- 护士角色 `e09c6b5410ed4eefbb5bb57cea51ab0a`。
- 医师角色 `a1c9192fbe31423fab2dce6f81791b88`。
- 科主任角色 `4b814c646d244a2885d073130491e3f6`，但不能给所有医师自动追加。
- 用户已处理 JHEMR 账号权限；复核时只检查代码是否禁止 DELETE，不重复索要业务确认。

## 5. 必查安全问题

检查 116 是否明确禁止：

- 生产/业务源库 DML、DDL、锁表和存储过程。
- 使用生产库替代测试库。
- 使用 `--noconftest` 绕过测试门禁。
- 将随机数或未经裁决的 `MAX+1` 当作 JHEMR `serial_no` 最终方案。
- 客户端提交任意 SQL、表名、连接串或 action。
- 未审核计划执行目标库写入。
- 将一方成功伪报为双目标成功。
- 回读失败后标记 succeeded。
- 把凭据内容写入代码、日志、Markdown 或 Git。
- 未经用户批准开启 nightly、字典写开关、身份写开关或 JHEMR 密码写入。

## 6. 需要独立判断的待确认项

请重点判断以下项目是否确实无法由当前代码/只读证据确认：

1. JHEMR `serial_no` 的最终取号方式，以及原业务系统是否并发写入。
2. HIS 中识别科主任的字段、值域和追加角色规则。
3. 独立测试库 `APP_TEST_DB_URL` 是否已经提供且确实为测试库。
4. 目标测试账号、credential_ref、secret 文件是否存在、权限是否为 0600、是否可连接。
5. HIS/JHEMR 字典写策略和专用账号是否已登记。
6. Phase D 批准版本、回滚方案和小批样本是否齐全。
7. 55 所列剩余项与 85/100/101 的测试阻断状态是否一致。

如果某项可以通过仓库代码、既有快照或受控只读 SQL 确认，应列出证据路径和 SQL；不得直接把“未确认”当作理由要求用户重复提供已经存在的信息。

## 7. 复核限制

- 默认只读本地文件和代码。
- 如确需服务器数据库核查，只能执行限量、脱敏、只读 `SELECT` 或元数据查询。
- 禁止 DML/DDL、锁表、全表扫描、创建对象、修改权限、读取或输出密码密文。
- 禁止修改工作区文件；如发现问题，只报告文件和建议补丁。
- 禁止启动生产 worker、nightly、字典下发或人员 apply。

## 8. 输出格式

请按以下格式返回：

### A. 结论

说明 116 号计划：通过 / 有条件通过 / 不通过。

### B. 必须整改的问题

按严重程度排序：P0、P1、P2。每条包含：

- 问题编号；
- 文件绝对路径和行号；
- 事实证据；
- 风险；
- 具体整改建议；
- 是否阻断下一阶段。

### C. 计划遗漏或顺序问题

列出 116 没有覆盖、重复、冲突或依赖顺序错误的事项。

### D. 待用户/DBA/厂商确认

只列真正无法从代码和只读证据确认的事项，注明确认人和确认内容，不要求提供密码或密文。

### E. 验收命令和结果

列出实际执行的命令、是否执行、结果和失败原因。失败不能写成通过。

### F. 安全审计

明确说明：是否执行 DML/DDL、是否访问生产写接口、是否开启开关、是否读取或输出 secret、是否修改文件。

## 9. 复核完成标准

只有同时满足以下条件，才能说“116 号计划复核通过”：

1. 所有 P0 阻断均有明确处理步骤和验收证据。
2. 测试库、目标测试库、生产库边界清晰。
3. serial_no、科主任识别、测试账号、secret、写策略和发布窗口没有被 AI 擅自假设。
4. 诊断/手术链路与人员夜间链路没有混为同一触发机制。
5. HRP 暂缓口径没有被重新纳入本期阻断。
6. 未授权生产写入、Git 强推和凭据暴露风险为零。
