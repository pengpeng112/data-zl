> 类别：执行计划
>
> 状态：历史验收计划（主体结果已由123号后续执行覆盖；当前不得再按本文件“尚未完成”表重复执行）
> 最新事实：隔离库迁移往返、后端679 passed/1 skipped连续两次、前端门禁、F2/F4/G7/G8、F10 apply及历史签名补跑均已完成；当前真实剩余仅按55号和123号判断，包括R8离线阻断、H7连续观察及共享工作区Git收口。
>
> 创建日期：2026-07-15

# 系统最终封板与全量验收详细执行计划

## 1. 计划定位

本计划用于完成当前系统最后的代码修复、角色矩阵、产品闭环、生产门禁、离线部署、Git 收口和发布交接。执行完成后，系统才可以从“已部署、可使用”升级为“测试全绿、证据完整、可重复发布”。

执行 AI 不得重新设计 E9–E11，不得重复采集或导入 77–84 号已经完成的资产，不得把业务源库写开关打开。

执行依据按优先级排序：

1. `AGENTS.md`
2. `开发起步包/README.md`
3. `开发起步包/55_系统未完成事项统一执行计划.md`
4. 本计划
5. `58_发布部署与代码同步整改计划.md`
6. `_archive/74_平台功能与资产导航统一整改执行计划.md`（已完成实现历史，仅在定位旧决策时读取）
7. `84_周边业务库连接资产与关系平台迁移报告.md`

冲突时以安全红线、55 号当前用户裁决及本计划的收尾边界为准。

## 2. 当前真实基线

### 2.1 已完成，禁止重复

- 生产平台已部署于 `10.10.8.83`，API 镜像为 `data-asset:20260714-plan76`，Alembic head 为 `y1f2a3b4c5d6`。
- E9 人员画像与权限审批、E10 配方版本与审核、E11 系统连接/资源树/图谱的主要代码和迁移已发布。
- 诊断手术字典已导入平台。
- HRP 已导入 67 表、841 字段。
- JHEMR、无纸化、Docare、LIS、超声内镜、PACS 资产已迁移平台。
- 平台当前对账：14 系统、14 数据源、7,197 对象、120,395 字段、478 关系。
- 业务源库均保持只读；77–84 号探库及导入过程源库写操作为 0。
- 用户取消 T13 人工凭据轮换，接受内网继续使用 HTTP。
- 运维业务源库写开关必须继续关闭。

### 2.2 当时尚未完成（历史基线，最新状态以123号§6.1为准）

| 编号 | 优先级 | 工作 | 当前证据 | 完成标准 |
|---|---|---|---|---|
| F1 | P0 | 全量 pytest 修复 | `240 passed / 40 failed / 3 errors` | 独立测试库全量 pytest 0 failed、0 errors |
| F2 | P0 | E9 角色矩阵 | API/UI 已发布，缺完整实测 | 匿名、普通、申请、审核、管理员、越权、自审禁止全部通过 |
| F3 | P0 | E10 配方闭环 | 功能代码已发布 | 创建、编辑、复制、提交、审核、激活、停用、并发冲突、SQL 安全全部通过 |
| F4 | P0 | E11 浏览器真实数据验收 | 新资产已导入 | 系统、连接、库/Owner、表、字段和图谱均正确显示 |
| F5 | P0 | Git 收口与推送 | 工作区大量未提交 | 分组 commit、普通 push、新 tag、工作区 clean |
| F6 | P1 | 生产 RBAC/挂载门禁 | 容器 healthy，缺完整重建证据 | bound token、403、持久卷、Oracle Client、登录刷新全部验证 |
| F7 | P1 | 离线包与断网演练 | 有部分 wheelhouse | 干净 Linux 断网从零部署成功 |
| F8 | P1 | SQL Server 保存连接测试 | 外部采集成功，生产镜像缺 ODBC | LIS、超声内镜从平台页面测试成功或实现安全兼容测试器 |
| F9 | P1 | 关系待核 | 内镜键、跨库关系未验证 | 保持候选或形成新的只读验证证据，禁止伪造强关系 |
| F10 | P1 | 历史系统归并 apply | dry-run 0 冲突 | 数量守恒、平台备份、apply、页面复核；业务源库零写 |
| F11 | P1 | Alembic 往返验证 | 生产已 upgrade | 独立测试库完成 upgrade/downgrade/upgrade，生产不降级 |

## 3. 强制安全边界

1. HIS、HRP、ODS、JHEMR、CDMS、Docare、LIS、超声内镜、PACS 等业务源库只允许 `SELECT`。
2. 禁止对业务源库执行 INSERT、UPDATE、DELETE、MERGE、TRUNCATE、CREATE、ALTER、DROP、CALL 或存储过程。
3. 运维写通道生产开关保持关闭；本计划不授权开启业务源库写操作。
4. 密码、Token、身份证、姓名、电话、地址不得进入代码、报告、命令输出或 Git。
5. 平台数据库正式写入前必须备份；业务源库不做任何结构迁移。
6. 不得安装 pytest 到正式运行镜像；继续使用独立测试镜像。
7. 不得为了测试通过删除、跳过、xfail 或弱化测试。
8. 不得使用 Mock 数据冒充生产浏览器或数据库验收。
9. 不得 force-push、移动旧 tag、重写共享 Git 历史。
10. 不得覆盖用户或其他 AI 的未提交文件；先识别和分组。

## 4. 严格执行顺序

必须按 `S0 → S1 → S2 → S3 → S4 → S5 → S6 → S7 → S8` 顺序执行。每阶段结束必须检查 `git diff`、运行阶段测试并记录证据；代码自身失败必须在进入下一阶段前修复。

## S0：基线冻结与目录自检

### S0.1 目录自检

1. 完整读取 AGENTS、README、55、58、74、84、85。
2. 列出 `开发起步包/` 顶层文件，与 README 比对。
3. 孤儿文件补登记或迁入 `_archive/`；幽灵条目修正。
4. 禁止创建新的状态总表；结果证据优先直接更新 85。

### S0.2 工作区盘点

执行：

```powershell
cd F:\python\数据资产
git status --short
git diff --stat
git diff --check
git log -5 --oneline
git tag -n
git remote -v
```

按以下类别登记文件，不立即提交：

- E9–E11 产品代码；
- 75/76 连接、运维、字典代码；
- 77–84 探库、导入脚本和证据；
- Alembic 迁移；
- 测试；
- 部署文件；
- 应归档的临时脚本；
- 无关或来源不明改动。

### S0.3 生产基线

只读记录：容器名、镜像、健康状态、Alembic head、平台五项总数、凭据文件权限。禁止输出环境变量值和凭据内容。

验收：形成计划修改/保留/归档/提交文件清单；不得在 S0 改业务代码。

## S1：修复全量 pytest

### S1.1 固定测试环境

1. 使用独立测试数据库，禁止连接生产平台库执行破坏性 fixture。
2. 使用现有 `data-asset:20260714-e9e11-test` 或基于当前代码构建新的测试镜像。
3. 记录 Python、pytest、SQLAlchemy、PostgreSQL 版本及数据库 head。
4. 测试库从生产脱敏结构/种子克隆或运行稳定种子脚本；禁止依赖测试执行顺序。

### S1.2 分类修复

先运行并保存失败节点：

```powershell
cd F:\python\数据资产\backend
.\.venv\Scripts\python.exe -m pytest tests/ -q --maxfail=0
```

按四类处理：

1. 缺历史资产种子：在 `backend/tests/conftest.py` 或专用 fixture 中创建最小、确定、可回滚的系统/数据源/表/字段/关系种子。
2. 配方 `content_hash`：测试通过公开 Service/API 创建合法配方，不直接构造缺必填字段的旧模型。
3. 治理/运维状态机预期：以当前 74–76 号正式状态机为准修订实现或测试；必须验证非法跃迁仍拒绝。
4. 数据隔离错误：每个测试自行准备并清理数据，禁止假设生产 ID 或固定自增 ID。

优先检查文件：

- `backend/tests/conftest.py`
- `backend/tests/test_recipes*.py`
- `backend/tests/test_governance*.py`
- `backend/tests/test_ops*.py`
- `backend/app/services/recipe_service.py`
- `backend/app/api/v1/recipes.py`
- `backend/app/api/v1/permission_requests.py`
- `backend/app/api/v1/ops_tools.py`

### S1.3 完成标准

- 全量 pytest 0 failed、0 errors。
- 不得出现新增 warning 中的凭据、连接串或个人信息。
- 单独重跑 E9/E10/E11 测试仍通过。
- 连续执行两次全量 pytest，第二次不得因残留数据失败。

## S2：E9 人员画像与权限角色矩阵

### S2.1 测试身份

在独立测试库创建：普通用户、申请人、审核员、管理员、无权限用户。每个账号使用测试凭据，禁止记录真实密码。

### S2.2 必测矩阵

| 场景 | 预期 |
|---|---|
| 匿名访问人员画像写接口 | 401/403 |
| 普通用户查看授权范围内画像 | 200 |
| 普通用户修改画像 | 生成审批申请，不直接改主档 |
| 申请人查看“我的申请” | 仅返回本人范围 |
| 申请人审批自己的申请 | 403 |
| 审核员批准/驳回 | 状态正确、审计完整 |
| 已撤回申请再次审批 | 409/业务拒绝 |
| 无按钮权限直接调用 API | 403 |
| 过期角色/范围外数据 | 403 或列表过滤 |
| 管理员合法操作 | 成功并记录审计 |

检查后端 FastAPI `Depends(require_permission(...))` 是否覆盖所有写接口；前端隐藏按钮只能作为体验层，不能替代后端鉴权。

重点文件：

- `backend/app/api/v1/identity.py`
- `backend/app/api/v1/permission_requests.py`
- `backend/app/api/v1/permissions.py`
- `backend/app/core/security.py`
- `frontend/src/views/identity/persons/`
- `frontend/src/views/identity/permission-requests/`
- `frontend/src/api/identity.ts`
- `frontend/src/api/permissions.ts`

完成标准：自动化 API 矩阵通过；浏览器逐角色截图或文字证据齐全；审计日志不含敏感信息。

## S3：E10 配方真实闭环

### S3.1 状态机

使用真实平台资产字段完成：

`新建草稿 → 编辑 → 复制新版本 → 提交审核 → 审核通过 → 激活 → 停用`

同时验证：驳回后允许按规则修改；已激活版本不可被原地覆盖；申请人不能自审；并发提交返回 409；相同 `recipe_id + version` 不重复。

### S3.2 SQL 安全

- 允许生成受控 SELECT/CREATE VIEW 预览文本。
- 禁止实际对业务源库执行 CREATE VIEW。
- 拒绝多语句、注释绕过、DML、DDL、存储过程和危险关键字。
- 引用的系统、库、Schema、表、字段必须存在于资产目录。

重点文件：

- `backend/app/models/recipe.py`
- `backend/app/schemas/recipe.py`
- `backend/app/services/recipe_service.py`
- `backend/app/api/v1/recipes.py`
- `frontend/src/api/recipes.ts`
- `frontend/src/views/asset/relation-recipes/`

完成标准：API、服务测试和浏览器闭环全部通过；业务源库写 0。

## S4：E11 真实数据与浏览器验收

### S4.1 连接与资源层级

逐一检查 HIS、HRP、ODS、JHEMR、无纸化、Docare、LIS、超声内镜、PACS：

- 系统 → 物理端点 → 数据库/服务 → Schema/Owner → 表/视图 → 字段；
- 同一 IP/端口不重复显示物理连接；
- 超声内镜同一端点保留 6 个数据库；
- PACS 同一端点保留 10 个数据库；
- Docare 保留 3 个 Owner；
- 密码不回显，只显示 configured 和脱敏用户名；
- 搜索、筛选、分页、懒加载、空状态和错误状态正常。

### S4.2 图谱

验证系统级、Schema 级、表级图谱：

- 稳定节点 ID 不只使用表名；
- 同名表跨库不碰撞；
- verified、partial、candidate、cross-system-pending 分层显示；
- 节点、边、图例、诊断、定位、缩放、重置可用；
- 无数据和接口错误时不白屏；
- 大图使用懒加载/限量，不一次返回全量 7,197 对象。

重点文件：

- `backend/app/api/v1/systems.py`
- `backend/app/api/v1/graph.py`
- `frontend/src/views/asset/systems/index.vue`
- `frontend/src/views/asset/sources/index.vue`
- `frontend/src/views/asset/graph/index.vue`
- `frontend/src/api/asset.ts`

完成标准：浏览器验收矩阵逐项记录真实结果；发现缺陷必须修复后重测。

## S5：连接测试、归并和迁移往返

### S5.1 SQL Server 保存连接测试

优先实现不污染正式镜像的兼容方案：

1. 评估在独立连接测试 sidecar 中使用 `pymssql`；或为正式镜像离线加入经过校验的 ODBC Driver + `pyodbc`。
2. 不允许运行时联网安装依赖。
3. SQL Server 测试连接必须设置只读意图、超时、锁等待限制，只执行 `SELECT 1`。
4. 错误信息脱敏，禁止回显用户名、密码和完整连接串。
5. LIS 与超声内镜在页面点击测试均返回成功并写入运维事件日志。

如果采用 sidecar，需修改部署文件、健康检查和离线包；如果采用正式镜像驱动，必须控制镜像增量并做安全扫描。

### S5.2 历史系统归并

1. 运行 `normalize_business_systems.py` dry-run。
2. 输出旧→新映射、冲突、孤儿引用和数量守恒。
3. 备份平台库。
4. 仅在 0 未处理冲突时执行：

```powershell
python backend/scripts/normalize_business_systems.py --apply --confirmation NORMALIZE-BUSINESS-SYSTEMS
```

5. apply 后检查资源树、连接数量和资产数量不丢失。

不得把不同真实 IP 的独立 JHEMR、Docare、LIS、PACS 等强行合并为同一系统；归并规则以真实端点为准。

### S5.3 Alembic 往返

只在独立测试库：

1. `alembic heads` 确认唯一 head。
2. 从上一个 revision upgrade 到 head。
3. downgrade 到上一个 revision。
4. 再 upgrade head。
5. 对关键表、约束和数据进行前后对账。

生产库禁止 downgrade。

## S6：生产安全门禁与容器重建

1. 生产设置 `APP_RBAC_REQUIRE_BOUND_TOKEN=true`。
2. 未绑定 Token 调受保护接口必须返回 403。
3. 管理员和合法角色仍可正常登录、刷新和访问。
4. 确认 HTTP 下 Cookie 配置不会因误设 Secure 导致刷新失败。
5. 确认三个运维写开关均为 false。
6. 完整重建 API 容器，验证：
   - `/etc/data-asset/credentials` 持久存在且权限 600；
   - Oracle Client 挂载有效；
   - Oracle/MySQL/Vastbase 保存连接仍 connected；
   - SQL Server 连接测试按 S5 方案可用；
   - API/Nginx/登录/资源树正常。

生产变更前备份平台库和部署配置；记录镜像摘要，不记录环境变量值。

## S7：离线包与断网部署演练

### S7.1 产物

- Linux `requirements.lock`，包含确定版本和哈希；
- 后端 wheelhouse；
- 前端 pnpm 离线 store；
- 前端 dist；
- Docker 镜像 tar；
- Oracle Client/SQL Server 测试依赖说明；
- `SHA256SUMS`；
- 安装、升级、回滚脚本和 README。

### S7.2 干净断网演练

在全新 Linux VM/容器断开公网后：安装依赖、恢复脱敏测试库、Alembic upgrade、启动 API/Nginx、登录、RBAC、资源树、图谱、连接测试、回滚、二次安装幂等。

禁止连接生产业务源库完成演练；使用脱敏测试配置。

完成标准：所有安装步骤不访问公网，校验和一致，失败可回滚。

## S8：Git 收口、推送和最终报告

### S8.1 归档与秘密扫描

- `_debug_*`、`_hot_*`、一次性上传包、临时凭据文件不得提交。
- 有复现价值的脚本按 ASCII 描述性名称保留。
- 过时文档迁入 `_archive/` 并登记，不直接删除。
- 对密码、Token、私钥、连接串、身份证等执行扫描；报告只写命中数量和已整改路径，不输出内容。

### S8.2 分组提交

建议顺序：

1. E9–E11 模型、迁移和后端；
2. E9–E11 前端；
3. 测试修复；
4. 数据连接与导入脚本；
5. 部署与离线包说明；
6. 77–85 文档证据和目录维护。

每组提交前运行相关测试并检查 diff。不得把用户无关改动混入提交。

### S8.3 推送

1. `git fetch origin`。
2. 如果远端领先、分叉或冲突，停止并报告；禁止强推。
3. 普通 push 当前分支。
4. 不移动 `release-20260713`；创建新的不可变 release tag。
5. 推送 tag，核对本地/远端 SHA。
6. 最终 `git status` 必须 clean。

## 5. 全量验收命令

后端：

```powershell
cd F:\python\数据资产\backend
.\.venv\Scripts\python.exe -m alembic heads
.\.venv\Scripts\python.exe -m pytest tests/ -q
```

前端：

```powershell
cd F:\python\数据资产\frontend
pnpm install --frozen-lockfile
pnpm run typecheck
pnpm run test
pnpm run build
```

必须额外执行：

- Alembic 独立测试库 upgrade/downgrade/upgrade；
- E9 角色矩阵；
- E10 状态机、并发和 SQL 安全；
- E11 浏览器真实数据矩阵；
- 未绑定 Token 403；
- 凭据接口不回显；
- SQL Server/MySQL/Oracle/Vastbase 保存连接测试；
- 敏感信息扫描；
- 离线包 SHA256；
- 断网部署；
- 容器重建与持久卷；
- Git 本地/远端一致性。

## 6. 失败与阻塞处理

- 代码或测试导致失败：必须修复，不能标记环境阻塞。
- 外部网络、凭据或维护窗口阻塞：记录命令、时间、脱敏错误、替代验证；继续完成可独立工作。
- 生产写入、业务源库变更、开启运维写开关、强推 Git：不在本计划授权范围，必须停止。
- 一个外部步骤失败不得阻止其他阶段的本地开发和测试。
- 不得伪造通过，不得用旧报告替代本轮复测结果。

## 7. 最终交付格式

执行 AI 最终必须报告：

1. 总体是否达到封板标准。
2. F1–F11 每项状态和证据。
3. 修改、新增、归档文件清单。
4. pytest、typecheck、前端测试、build 的真实结果。
5. Alembic head 和往返结果。
6. E9 角色矩阵结果。
7. E10 配方闭环和 SQL 安全结果。
8. E11 真实数据与图谱验收结果。
9. SQL Server 连接测试实现方式和结果。
10. 生产 RBAC、容器重建、凭据卷和运维开关状态。
11. 离线包、校验和与断网演练结果。
12. Git commit、分支、tag、远端 SHA 和 clean 状态。
13. 平台资产数量守恒结果。
14. 业务源库写操作必须为 0。
15. 尚未完成项及真实阻塞原因。
16. README、55、61、66、85 是否同步。

只有 F1–F11 均完成，所有必测命令通过，生产健康，工作区 clean 且远端同步后，才能将本计划状态改为“已完成”。

## 8. 执行日志

### [S0] 2026-07-15 基线

- 工作区：大量 E9–E11 / 75–76 未提交改动 + 77–84 文档；未在 S0 改业务逻辑。
- Alembic 代码 head：`y1f2a3b4c5d6`。
- `APP_DB_URL` 指向 `10.20.1.153/data_asset`（asset_app），`data_asset_test` **不存在**，且 `asset_app` **无 CREATE DATABASE** 权限。
- `10.10.8.83:5432` 本机连接超时；本机 `postgresql-x64-17` 已启动但无可用密码。
- 生产基线以 85 §2.1 为准（平台 14 系统/7197 对象等）；本轮未连业务源库写。

### [S1] 2026-07-15 测试修复（代码侧）

已按失败分类修订（**未伪造全量通过**）：

| 类别 | 处理 |
|---|---|
| 缺种子 | `conftest.py` 增加 `seed_minimal_assets` / `seeded_client`；图谱测试改用种子 |
| 配方 status/hash | `test_recipes.py` 对齐 draft→submitted→approved→active→deprecated；`content_hash` 走 `recipe_hash` |
| 治理自审 | 创建 CR 时 `requested_by` 固定为 Token 用户；审批用例改为 DB 插入不同申请人 |
| 管理端 422 | `test_admin.py` create key 补 `user_identifier` |
| 权限列表 404 | 每测先 upsert 角色再赋权 |

单元测试（`--noconftest`，无需测试库）：**38 passed**。
全量 pytest：**阻塞** — 缺少合法 `APP_TEST_DB_URL`（库名含 test 且可连通）。

### [S3/S5 部分] 2026-07-15

- E10：配方状态机与 SQL 安全单测 + API 流测代码已补。
- F8 SQL Server：`SqlServerConnector` 优先 pyodbc，失败回退 **pymssql**；错误脱敏；`requirements.txt` 增加 `pymssql`。
- 前端：`typecheck` PASS，Vitest **39 passed**。

### [阻塞 / 未执行]

| 项 | 原因 |
|---|---|
| F1 全量 pytest | 无 `data_asset_test`，无法 upgrade 测试库 |
| F2 E9 浏览器矩阵 | 需测试库账号 + 生产/联调浏览器 |
| F4 E11 浏览器真实数据 | 需联调环境 |
| F5 Git push/tag | 工作区 dirty；建议分组 commit 后由用户确认再 push |
| F6 生产 RBAC/容器重建 | 需 8.83 运维窗口与凭据 |
| F7 离线断网演练 | 需 Linux 构建机 |
| F10 归并 apply | 需平台备份 + 明确授权 |
| F11 Alembic 往返 | 依赖独立测试库 |

业务源库写操作：**0**

### [S1/F1/F11 复核完成] 2026-07-15

- 纠正原阻塞判断：`10.10.8.83` 已存在可隔离使用的 `data_asset_test`，并有独立验收镜像；执行前已备份测试库，未修改生产平台库和任何业务源库。
- 首轮全量结果为 `270 passed / 28 failed / 2 skipped`；按真实失败逐项修复测试隔离种子、权限资源编码、配方 SQL 校验、运维自审测试和严格 Token 断言。
- 新增手写迁移 `z2f3a4b5c6d7`：删除历史遗留的 `recipe_id` 单列唯一索引并按非唯一索引重建，保留 `(recipe_id, version)` 联合唯一约束，修复配方复制新版本 500。
- 恢复并加强曾被弱化的图谱契约测试，覆盖节点、边、端点元数据、字段映射、系统/来源筛选和七种 view mode；图谱定向测试 `11 passed`。
- 隔离测试库全量 pytest：`300 passed / 2 skipped / 0 failed`，耗时 241.06 秒。
- 前端：`pnpm run typecheck` 通过；Vitest `39 passed`；`pnpm run build` 通过。
- Alembic：在生产备份恢复出的临时隔离库完成 `y1f2a3b4c5d6 -> z2f3a4b5c6d7 -> y1f2a3b4c5d6 -> z2f3a4b5c6d7` 往返，最终索引确认非唯一；临时库已删除。生产库未 downgrade。
- 额外发现：完全空白库从最早 baseline 起迁移仍依赖预先创建 `asset` schema，属于历史安装前置条件，不影响本轮增量迁移往返；应纳入 F7 干净离线部署演练修订。
- 业务源库写操作：**0**。

仍未封板：F2/F4 浏览器角色与真实数据矩阵、F5 Git 收口、F6 生产 RBAC 与容器重建、F7 离线断网演练、F8 生产镜像 SQL Server 真实连接测试、F9 待核关系、F10 归并 apply 尚未完成。

### [F6/F8 生产发布复核] 2026-07-15

- 发布前完成平台库备份：`/opt/data-asset/backups/data_asset_pre_z2_202607150915.dump`。
- 生产平台库升级至 `z2f3a4b5c6d7 (head)`；生产未执行 downgrade。
- 离线加入 `pymssql 2.3.11`，生产镜像更新为 `data-asset:20260715-f1-r2`；正式镜像仍不安装 pytest。
- 针对 SQL Server 2012 增加受控的 TDS 7.0 二次协商回退；单元测试 `3 passed`。
- 生产保存连接真实只读测试：LIS 与超声内镜均 `connected via pymssql`，仅执行连通性查询，业务源库 DML/DDL 为 0。
- API、Nginx、首页静态资源和平台数据库健康检查通过；前端产物已同步并保留 `frontend-dist.pre-f1` 回滚副本。
- `APP_RBAC_REQUIRE_BOUND_TOKEN=true`、`APP_OPS_WRITE_ENABLED=false`；凭据目录权限 700、凭据文件 600；Oracle Client 与凭据卷挂载有效。
- 宿主机 Docker NAT 链异常导致端口映射失败，部署脚本已改为 host 网络且 Uvicorn 仅监听 `127.0.0.1:8000`，避免对内网直接暴露 API。

F6、F8 改为完成。仍未封板：F2/F4 浏览器角色矩阵、F5 Git 收口、F7 完整离线包和干净断网演练、F9 候选关系人工核查、F10 归并 apply。
