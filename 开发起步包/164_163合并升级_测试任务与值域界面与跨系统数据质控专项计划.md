> 类别：模块规划
>
> 状态：**v1.1 待多方核查（2026-08-28 修订）**——在已批准的 163 号计划（v1.1）之上合并 ai-hms_qhd GUI 测试任务与数据质控方向。**v1.1 修订：原 R2b（值域界面）、R9（质控普查）、R10（质控设计）三批整体移交并细化至 `165_数据问题AI探查与入库执行计划.md` 与 `166_值域与探查问题展示导出功能开发计划.md` 两个独立计划**（用户 2026-08-28 指示拆分）；本文执行序列收敛为 **R0→R1→R2→R3→R4→R5→R6→R7→R8**（163 全部批次 + R8 血透 GUI 测试），§2 大体分析实证作为 165 的基线继续有效。核查通过后交执行 AI 按 §9 提示词执行。
>
> 上位入口：`55_系统未完成事项统一执行计划.md`
>
> 关联：`163_全仓未完成项一次性收口执行计划.md`（PART A 唯一依据，**本文不重复其内容**）；`165/166 号计划`（质控两计划）；`F:/python/前后端代码/ai-hms_qhd/docs/testing-execution-plan.md`（R8 唯一依据）；`152_JHEMR病案首页回写HIS链路梳理`（165 R-XSYS 链路依据）
>
> 执行方式：单执行者连续推进、checkpoint 跨会话续跑（同 163）；R4 夜间窗口与日间批解耦

# 163 合并升级：测试任务+值域界面+跨系统数据质控专项（164）

## 0. 定位与结构

164 = **163（已批准，原样执行，一字不改）** + 一个在执增量（R8 血透 GUI 测试）+ 两个已拆分移交的增量（R2b 值域界面→166；R9/R10 数据质控→165/166，见头部 v1.1 修订）。执行总序：**R0→R1→R2→R3→R4→R5→R6→R7→R8**。163 的铁律、STOP 白名单、附录 A 勾选态、DoD 对 PART A 全部有效并延伸适用本文新增批次。

## 1. 增量铁律（在 163 §1 之上新增）

5. R8 全程遵守 ai-hms_qhd 仓库自身 `docs/testing-execution-plan.md` §2 安全红线与 §3 工作规则（真人 GUI、五态结果、不 SQL/不部署/不推送；测试写入仅限测试环境测试患者；凭据走 E2E_* 环境变量零落盘）；该仓与数据资产仓**互不写代码**——缺陷只登记不修复。
6. R9 连库普查一律夜间窗口+显式超时；聚合优先、日期窗口限界、不导出患者明细（与 163 Q4 同源）。
7. R10 产出为**设计文档与选型报告**（165 号草案），本轮不实现质控平台编码——实现立项待多方核查+用户批准后另行计划。

## 2. 2026-08-28 连库大体分析结论（本计划实证基础，执行 AI 禁止重跑核实、只可扩展）

> 通道：sjzc live 受控只读（his_source_10_10_10_15 / jhemr_vastbase_10_10_8_177），全部聚合查询，窗口 2026-01-01 起。以下数字为方案证据，写入 R9 普查基线。

| # | 主题 | 实测结果 | 质控含义 |
|---|---|---|---|
| A1 | **工号-姓名一致性**（EXAM.EXAM_MASTER.DOCTOR_USER/DOCTOR_NAME ↔ FXHIS.SYS_EMPLOYEE 与 COMM.STAFF_DICT 双字典交叉） | 2026 年申请 379,980 条：**工号缺失 315,952（83.2%）**；非空工号 64,028 条**字典命中率 100%**，其中**姓名不一致 3,821（5.97%）** | 两类真实缺陷：关键字段缺失（R-DOM/R-NULL 规则）与引用不一致（R-REF 规则）；用户举例场景实锤 |
| A2 | **检查流转断点**（EXAM_MASTER 申请→执行→报告时间回写） | 申请 379,980 → 已执行 379,755（99.93%）→ **REPORT_DATE_TIME 回写仅 134,924（35.5%）** | HIS↔PACS/RIS 报告回传链路存在巨大断点（需按月×检查类型钻取归因：未回传/不回写该字段/当日未出报告）→ R-CNT 对账规则首批对象 |
| A3 | **JHEMR↔HIS 病案首页离院方式回传对账**（152 链路活化） | 2026 年出院两侧分布：医嘱离院 39,032↔39,002（差 30）、非医嘱 1,432↔1,431、死亡 255↔255、转院 26↔26、其他 10↔10、转社区 3↔3；**HIS 侧 NULL 689 例**（JHEMR 侧 0） | 回传总体健康（差<0.1%），残余=HIS 侧 689 例未填+按月钻取差量定位 → R-XSYS 字段级规则首批对象 |
| A4 | 值域佐证 | HIS DISCHARGE_DISPOSITION code9=10 例（佐证 149/151 code9 待核规模极小）；code1 占 94.6% | code9 可走"低频人工抽样"收口而非全量核查 |

## 3. ~~R2b 值域前端界面~~ → 已移交 166 号计划（F1/F2/F3）

**v1.1：本批整体移交 `166_值域与探查问题展示导出功能开发计划.md`（其 F1–F3 为本节内容的细化），本文不再执行。**

**依据**：149 §8 方案 B（建议版）+"系统当前没有值域的界面，需要加上"。

1. **表详情页值域区块**：表/字段详情页展示关联值域（code/meaning/domain_kind/scope_condition/陷阱提示/version_no/来源），无值域字段显示"暂无值域"。
2. **值域管理页**（新路由 `value-domains`，权限 `value_domain.read`）：分页列表（system/schema/table/column/code/meaning/status/version_no 筛选）+ 详情抽屉（证据链：source_type/snippet_ref/method/competing meanings）+ 冲突列表页（`conflicted=true`，#76 既有 API）+ pending 待确认计数徽标。
3. **人工确认入口**：详情抽屉内 confirm/deprecate 按钮（权限 `value_domain:confirm`；调用方=登录人，仅提示性操作留痕——**执行 AI 本身不得 confirm 任何值域**，仅实现界面）。
4. 前端遵守 163 铁律（pnpm、typecheck 双过、新页面一次建完）；API 消费 149 既有 `/api/v1/value-domains` 全套端点，**不新增后端路由**（确需小改走 161/163 既有约束审批式最小改）。
5. 验收：`pnpm run typecheck && pnpm run test && pnpm run build` 全绿+新增 ≥3 个组件测试（列表/冲突/详情抽屉）；R2 导入的 pending 值域在界面可见可筛。

## 4. R8 ai-hms_qhd GUI 系统测试（跨仓库批次）

1. **唯一依据**：`F:/python/前后端代码/ai-hms_qhd/docs/testing-execution-plan.md`（54 路由/130 用例/7 批次 A–G/证据规范/缺陷分级/汇总模板），其 §14 执行提示词为本批操作规程。
2. 执行顺序=其 §11：批次 A（准入/登录/角色/通用壳）→B（患者中心+详情 22 项）→C（透析全流程 DE-01–16+CL）→D（排班与资源）→E（质控统计配置）→F（打印/Pad/移动端）→G（外部依赖与非功能）。批次 A 阻断时暂停 B–G；B 的测试患者就绪才进 C。
3. 环境：`E2E_BASE_URL` 与 `E2E_*_USERNAME/PASSWORD` 全部环境变量注入零落盘；测试患者只用别名甲–壬；写操作仅测试环境测试数据；P0/P1 立即停止写操作升级。
4. 产出：该仓 gitignored 证据目录（`evidence/GUI-YYYYMMDD-NN/`，脱敏截图+请求证据）+ 每批次 `docs/testing-phase-report-template.md` 报告 + 总汇总表（通过率公式按其 §13，禁止用通过率掩盖未执行）。
5. **边界**：不改 ai-hms_qhd 任何代码；不 SQL/不部署/不 push；缺陷只登记（ID/等级/复现/证据）转交该仓负责人；**本批不计入数据资产仓的测试门禁**，只产出报告。
6. 验收：7 批报告齐+汇总表+缺陷清单（P0/P1 数明确）+最终结论（通过/有条件通过/不通过）。

## 5. ~~R9 质控数据现状普查~~ → 已移交 165 号计划（E2 模板库+E3 首轮探查）

**v1.1：本批整体移交 `165_数据问题AI探查与入库执行计划.md`（其 §3 模板库 12 条与 E3 首轮夜间探查为本节内容的落地），本文不再执行。**

~~把 §2 的 4 条演示查询扩展为**完整对账矩阵首轮基线**~~（保留原文供 165 引用）：

1. **R-REF 引用一致性行情**：EXAM 工号缺失/姓名不一致按月×检查类别（EXAM_CLASS 中文口径）钻取；同规则扫 LAB（检验报告表医生字段）、OUTP/INP 医生字段、OPERATION 手术医生字段——每域拿 缺失率/不一致率/孤儿率 三指标。
2. **R-CNT 数量对账**：①检查线全类型按月申请/执行/报告三漏斗（归因 A2 断点：区分当日未出/长期未回写）；②检验线 HIS 申请侧（LAB_TEST_MASTER 系）↔ ODS LIS 侧结果表（先探 ODS LIS schema 表清单与键，再按月对账）；③PACS 线同理（ODS PACS owner）。
3. **R-KEY 键覆盖**：近 90 天 HIS 出院/门诊就诊号抽样（≤1000，服务端聚合 IN）在 JHEMR r_pat_visit、ODS LIS/PACS 的命中率；反向抽样一次。
4. **R-XSYS 病案首页回传**：A3 升级为按月×字段级（discharge_disposition/治疗结果 TREAT_RESULT/手术/诊断/日期字段，152 E5 清单为字段集），两侧聚合拉平对比表；HIS 689 例 NULL 按月钻取定位来源月份。
5. **R-DOM 值域合规**：149 值域库 confirmed 条目 vs 各源库实际值分布（每条值域一条聚合 SQL，异常值比率）。
6. 产出：`开发起步包/165_跨系统数据质控平台设计_数据现状普查.json`+报告（对账矩阵总表：域×指标×窗口×基线值×疑似根因分类）。
- 约束：夜间+超时；每查询聚合优先；结果零患者明细；BLOCKED 项（如表未采集/权限不足）如实登记跳过。

## 6. ~~R10 跨系统数据质控平台开发方案~~ → 已移交 165+166 号计划

**v1.1：本批拆解移交——探查入库与数据模型归 `165 号计划`（其 §2 数据模型+五类模板+执行器即本节"复用底座"设计的落地）；展示/导出/看板归 `166 号计划`（P1 必做+P2 后置清单，含 AI 巡库复发检测的界面呈现与工具选型保留为 165/166 联合附录）。本文不再单独产出设计文档。**

**方向**（用户 2026-08-28 确立）：跨系统关联缺口、写入不一致、数量对账、值域→关联→血缘→流转分析、AI 巡库回归检测、JHEMR 回传一致性、GitHub 治理工具引入评估。

设计骨架（165 号草案须按 R9 实测细化）：

1. **架构原则——复用现有底座，不另起炉灶**：连接器与只读门禁（db_connectors/sjzc）、**规则载体=查询资产（126/144：每条质控规则=一条参数化 SQL 查询资产，窗口参数化，结果行数/聚合值=指标）**、指标引擎（144 真实计算）、异常落 QualityFinding（127/153 既有模型，按 run 快照）、血缘=asset_lineage_edges（138）+关系图谱、值域库（149）、AI 上下文注入（144/149）。新增仅三件：①一致性规则库（rule_type×五类×参数模板）；②复发检测器（同 rule+object 的 finding 在 resolved 后再现→relapse 标记+首次/末次/复发计数）；③跨系统流转视图（把 R-CNT 对账指标挂到血缘边：每跳"发出↔收到"数与差量）。
2. **五类规则**（R9 基线即种子）：R-REF（A1）/R-CNT（A2）/R-KEY/R-XSYS（A3）/R-DOM（A4）。
3. **AI 巡库回归检测**：规则按计划跑（夜间），finding 带 run_id；复发=新增 `relapse_count`+趋势看板（问题"改了又改回来"可视化为同对象多 run 峰谷）；可与 130 既有的 Dify AI 质控页对接（呈报不自动改 finding，同 130 U10 边界）。
4. **GitHub 工具调研与选型报告**（165 附录）：Great Expectations（Python 期望套件，语义可借鉴、离线可装则可试点）/Soda Core（YAML 检查）/Apache Griffin、Deequ（Spark 系，重，不适内网 Oracle11 场景，预期排除）/OpenMetadata、DataHub（元数据+血缘+质量一体，Java/Python 重，与自研平台功能重叠，预期仅借鉴数据模型）。**选型倾向**：不整库引入（内网离线+零新依赖偏好+已有底座），借鉴 GE 期望语义实现轻量规则层；若用户要求引入，走单独授权与离线依赖审计。
5. **分期**：P1 规则引擎+五类规则各 2–3 条（R9 基线直接转化，含 A1/A2/A3 三条已实证）+对账看板页；P2 复发检测+趋势；P3 流转视图（血缘边挂指标）；P4 工具试点（可选）。每期独立计划另行批准。
- 产出：`开发起步包/165_跨系统数据质控平台设计.md`（含 R9 基线、规则模板 SQL、数据模型增量、API/页面清单、分期与验收）——**多方核查对象之一**。

## 7. PART B 等待域增量（在 163 §7 W1–W10 之上）

| # | 事项 | 前置 |
|---|---|---|
| W11 | ai-hms_qhd 测试缺陷修复（R8 缺陷清单移交该仓负责人） | 该仓授权（跨仓不越权） |
| W12 | 质控两计划（165 探查入库/166 展示导出）核查与执行；生产建表 apply（165 X1）与首轮探查（X2） | 本计划多方核查+用户批准（165/166 各自 §等待域） |
| W13 | 治理工具（GE/Soda 等）引入与离线依赖审计 | 用户明确要求时单独授权 |
| W14 | A1/A2/A3 缺陷的业务侧整改（工号补录流程/报告回传补写/689 例 NULL 补填） | 业务科室+信息科决策（平台只呈报） |

## 8. 完成定义（DoD 增量，叠加于 163 §8）

- [ ] ~~R2b 界面上线~~ → 移交 166（其 §6 DoD F1–F3）
- [ ] R8 七批报告+汇总+缺陷清单齐，最终结论三选一明确，零越权（未改该仓代码/未碰生产数据）
- [ ] ~~R9 对账矩阵~~ → 移交 165（其 §4 E3 与 §7 DoD）
- [ ] ~~R10 设计文档~~ → 移交 165+166
- [ ] 163 PART A 原 DoD 8 项全过；总报告合并登记 README/55

## 9. 执行提示词（核查通过后复制给执行 AI，勿增删）

```text
【角色】你是跨两仓执行 AI（主仓 F:\python\数据资产 + 测试仓 F:\python\前后端代码\ai-hms_qhd），按 开发起步包/164_163合并升级_测试任务与值域界面与跨系统数据质控专项计划.md（v1.1）连续推进。

【开工前必读（按序）】F:\python\数据资产\AGENTS.md；开发起步包/164 号计划（总纲，v1.1：R2b/R9/R10 已移交 165/166，不在此执行）；开发起步包/163 号计划（PART A 唯一依据，R0–R7 原样执行）；其 §3 事实表与本仓 164 §2 大体分析结论（禁止重跑核实）；R2 批加读 151 号计划；R5 批加读 146 §11 E；R8 批加读 ai-hms_qhd/docs/testing-execution-plan.md 全文与其 §14 提示词。

【总序】R0→R1→R2→R3→R4→R5→R6→R7→R8；单执行者连续推进、checkpoint 跨会话续跑；每批完成即跑该批验收，全过才进下一批（R4 夜间批按 163 口径登记夜间清单后可先推进日间批）。质控方向（值域界面/探查入库/展示导出）按 165→166 两计划另行执行，不在本提示词范围。

【铁律】163 §1 四类 STOP+其余一律 WARN 继续；164 §1 增量（R8 遵守其仓红线、缺陷只登记不修复、两仓互不写代码）。业务源库零写入；值域永不自动 confirm；凭据零落盘；零 Git 写；161 会话既有工作区改动原样保留。

【环境】数据资产仓测试库隧道与 APP_TEST_DB_URL 见 162/163；ai-hms_qhd 测试环境与账号走 E2E_* 环境变量（负责人提供，禁止猜测生产地址）；连库一律经 sjzc 受控通道。

【产出】164 §8 DoD 逐项自检；总报告+同号 _结果.json；README/55 登记「代码完成/测试完成，待主 AI 复核与多方核查」；等待域 W1–W14 清单呈用户。
```
