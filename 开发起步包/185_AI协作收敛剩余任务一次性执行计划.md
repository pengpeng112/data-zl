> 类别：待办
>
> 状态：**v1.1 定稿待执行（2026-09-06；经外部 AI 逐条审核修订——16 条意见采纳 16，其中 3 处产物 bug 已由修订会话先行修复）**
>
> 上位入口：`55_系统未完成事项统一执行计划.md`；承接 `183_AI协作指令收敛与重复流程自动化开发计划.md`（S1-S6，**执行入口已移交本文件，禁止按 183 正文开工**）与 `184_AI协作复盘_高频流程沉淀与中断分析报告.md`（已完成项禁止重做）
>
> 制定日期：2026-09-06（v1.0）／ 同日 v1.1 修订（审核意见全量采纳）

# 185 · AI 协作收敛剩余任务一次性执行计划（v1.1）

## 0. 角色、边界与开工方式

| 角色 | 做什么 |
|---|---|
| 执行 AI（本计划对象） | 按 R0→R7 推进标明可执行的批次；不确定记 WARN/STOP |
| 用户 | Git push、生产发布、业务源库写、cron/env、值域 confirm、探查终态 |

**开工环境（Windows 本机）**：shell 是 Git Bash；PowerShell 会话内用 `bash -lc "..."` 调本计划命令。Python 一律 `backend\.venv\Scripts\python.exe`，不写裸 `python`。

**硬边界（违反即回滚并停止）**：

1. 业务源库零写入；**平台库（生产 PG）零写入**——本计划 C1-C7/N1/N2/S6 全部只读，不写审计行；隔离库 `data_asset_test` 仅在授权测试批使用。
2. 零 Git push；本地提交允许且必须按 §3 R7 白名单显式路径分组。
3. 零生产发布/零改 cron/零改 env；**01:45–02:30 夜跑窗口不连生产容器**（避开 cron）。
4. 他人域零触碰 = `tools/dev_env.sh --domain-baseline` 的 13 文件 **加上 R0 冻结的"非本计划已改文件"清单**（当前至少含：`cdms_identity_adapter.py`、`jhemr_identity_adapter.py`、`identity_login_sign_sync.py` 及身份测试、`.agents/skills/*/SKILL.md` 收窄修订、未跟踪的 `frontend/src/layout/captureMode.ts`、`frontend/tests/competitionCaptureMode.test.ts`、`output_r180/`、`output_r181/`、`r178_dist.tar.gz`——并行会话域）；禁 checkout/stash。
5. 不为过测试弱化断言；失败如实记 WARN/BLOCKED。
6. 184 工具**已修复可用**（dev_env.sh `fi;;` 与 ops-runbook `password=pwd` 两个 bug 已由修订会话修复并验证）；若执行中再次发现 184 产物 bug，允许**最小修复**（语法/变量级），修了记 WARN 修复，不算重做 184，不改工具结构。

**R0 阅读切片（防上下文过载，禁止全文通读 README/55）**：AGENTS.md 全文（180 行）→ README 仅「当前入口」表 + 本文件相关行 → 55 仅顶部 2026-09-06 📌（含 round-9 flush 结论）→ 183 仅 §3 开发批次 → 184 仅 §0 分工与 §7 补记 → `.agents/skills/ops-runbook/SKILL.md`。

---

## 1. 任务总表（v1.1 修订点以【v1.1】标注）

### 1.1 P0-代码（默认可执行）

| ID | 事项 | 位置 | 要点 | 验收 |
|---|---|---|---|---|
| C1 | 183-S2：目录与编号检查器（**唯一检查器**） | 新建 `tools/check_doc_index.py` | 识别编号正文/同号 `_结果.json`/同号输出目录/README 组合登记/归档条目；查孤儿、幽灵、**重复编号、缺类别**；`--fix` **默认 dry-run**，写 README 须显式文件列表参数。【v1.1】`tools/register_doc.py --check` 子命令改为**调用本检查器的同一套解析函数**（或直接废弃 --check 转发提示），消除双规则漂移；register 保留"登记模板生成"职责 | 对当前 `开发起步包/` 零误报：组合登记条目（如 `61–67、71、87–89` 行）不算幽灵、其覆盖的编号文件不算孤儿；已知良性白名单仅两项：CLAUDE.md 转发、150 号已删仍登记；180 号同号多文件形态正确识别。`--check` 幂等 |
| C2 | 183-S3：测试环境预检器 | 新建 `tools/check_test_environment.py` | 【v1.1】**只包装 `backend/app/core/database_guard.py` 既有规则**（validate_test_database_url/denylist/APP_DB_URL 一致性），不 import `tests/db_guard/`、不从 bash 抽平行 Python；加隧道探测。三态：`pure_logic_ready`（无库可跑）/ `integration_ready`（URL 合法且 15432 在听——**被占用视为就绪（复用语义）**，占用者未知记 WARN 非 FAIL）/ `migration_ready`（再加可清理证明）；不打印凭据、不杀进程、不跑 pytest/TRUNCATE | 五类用例：缺 URL / 生产 URL / 共享不可清理库 / 端口被占（预期 integration_ready=WARN 就绪）/ 合法隔离库 |
| C3 | 183-S1：跨会话接手 Skill | 新建 `.agents/skills/project-task-resume/`（SKILL.md ≤150 行 + `references/` 三案例） | 输入=用户当前要求+git status+README 当前入口+55 顶部 📌+相关执行报告；输出=当前目标/已完成证据/未完成步骤/文件归属/可继续项/需批准项/阻塞条件。【v1.1】①Skill 内**调用 C1/C2 产物**做目录与环境判断，不重写；②三案例回放放 references/：147（路径 `_archive/147_146界面完善中断交接与续跑提示词.md`，已归档非现行）、177、180；③**交付含 AGENTS.md 路由一行**（触发词：接手/续跑/恢复中断任务）；④静态校验：ZCode 无 `skill-creator/scripts/quick_validate.py`，改用人工清单（frontmatter/路径存在性/≤150 行）留档 | 三案例只读回放：复用完成项、指出可继续项、保留批准点；AGENTS 路由行可被后续会话发现 |
| C4 | 183-S4：多 AI 证据薄包装 | 新建 `tools/multi_ai_evidence.py` | CLI 存在性预检、round 编号、UTF-8 归一、SHA-256、缺席状态与结果清单；全缺席生成明确未完成报告。【v1.1】R3 须附**只读核查一步**：全局 `~/.zcode/skills/multi-review|multi-verify/SKILL.md` 是否引用本脚本；未引用则在报告标 **"S4 调用链未接"**，不宣称 S4 完成（改全局技能不在本计划范围） | 模拟单缺席/全缺席两用例收口正确 + 引用核查结论如实 |
| C5 | 183-S5：指令漂移检查 | 新建 `tools/check_instruction_drift.py`（只读） | 扫描 AGENTS/CLAUDE/项目 Skill/全局 Skill/55 顶部的批准、停止、只读、全量测试、历史授权关键词；四条语义规则（源库只读不放宽 / 平台 active 与人工批准不混用 / 历史授权≠当前授权 / 局部阻塞不宣称完成）。【v1.1】验收**不要求零高危**：高危项如实报告；白名单仅计划点名项（CLAUDE 转发、150 幽灵） | 跑通、JSON 可解析、四规则各有触发用例；发现项列表留档即过 |
| C6 | ops-runbook Skill 事后质检 | `.agents/skills/ops-runbook/SKILL.md` | 【v1.1】样板 bug（password=pwd）已由修订会话修复；本项为复核：frontmatter/引用路径（tools/dev_env.sh、179/180 文档）/代码块变量名目检；**发现错误允许最小修复**（语法/变量级，记 WARN） | `output_r185/skill_check.md` 留档 |
| C7 | 目录基线（用 C1 跑） | `tools/check_doc_index.py` | 【v1.1】**降级为 C1 的首次实跑留档**，不与 register_doc --check 并列双验收（后者已并入 C1） | 首份基线 JSON 留档 `output_r185/doc_index_baseline.json` |

### 1.2 P1-只读核对（v1.1 重写口径）

| ID | 事项 | 口径 |
|---|---|---|
| N1 | 夜跑三行对账（**禁止用"最新一行"代替夜跑判定**） | ① 09-06 02:00 cron 行 `RUN-69e87f7f27dd`：预期 **failed / max_change_ratio**（水位死锁，与 `output_r180/nightly_d.md` 对账即过，**不重做根因分析**）；② 09-06 08:44 排水行 `RUN-2df6cd6db381`：预期 **success、update=110**（真实 update 本就不出现 resync_unchanged，见 `output_r180/flush/progress.md`）；③ 09-07 02:00 cron 行：**尚无则记 SKIP 待 09-07，不得宣称 F-2 验证完成**（flush 方案 R5 观察点）。容器查询走 ops-runbook §1 标准件，**避开 01:45-02:30 窗口** |
| N1b | docare 每日任务首份清单核对（独立项，不与 F-2 绑验收） | 只读 `/opt/data-asset/evidence/docare_mismatch/list_20260906.md`：确认存在且形态正确（自动修订/dry 标注/人工裁决分组）；异常只呈报 |
| N2 | c0632940 孤儿排班核对 | 只读 Docare `MED_OPERATION_MASTER`（键：PATIENT_ID='c0632940', VISIT_ID='3', OPER_ID='1'）确认 STATUS 仍=0；处置建议（人工在手麻系统作废）写入报告；走 `.agents/skills/docare-anesthesia-readonly-sql` 技能，禁止猜列名 |

### 1.3 183-S6（维持只读盘点）

`~/.zcode/skills/skill-creator` 与插件缓存 `~/.zcode/cli/plugins/.../skill-creator*` 的加载关系盘点+迁移建议；不移动、不改名、不删除任何全局技能。

---

## 2. 工作区分域

| 域 | 文件 | 本计划 |
|---|---|---|
| 新工具 | `tools/check_doc_index.py`、`tools/check_test_environment.py`、`tools/multi_ai_evidence.py`、`tools/check_instruction_drift.py`、`tools/tests/`（**自带 no-op conftest**，防根级 autouse 清库误伤） | 主战场 |
| 新 Skill | `.agents/skills/project-task-resume/`（含 references/） | C3 |
| 既有文件微调 | `tools/register_doc.py`（--check 并入 C1 解析）、`AGENTS.md`（仅 C3 路由一行）、`.gitignore`（新 skill 目录白名单，照 ops-runbook 先例） | 允许 |
| 文档 | `开发起步包/output_r185/`、185 §6 执行日志、55/README 登记 | 收口 |
| 他人域（R0 冻结清单） | layout/captureMode/竞赛截屏 13 文件 + 身份线脏文件 + skills 收窄修订 + output_r180/181 + 构建产物 | **整域禁碰** |

---

## 3. 批次执行卡（v1.1 执行序）

总序：`R0 切片自检 → R1（C1+C2）→ R2（C3）→ R3（C4+C5）→ R4（C6+C7）→ R5（N1/N1b/N2 只读）→ R6（S6 盘点）→ R7 收口`。允许跨会话，checkpoint 写 `开发起步包/output_r185/progress.md`。

**R0**：按 §0 切片阅读；`bash tools/dev_env.sh --domain-baseline 开发起步包/output_domain_baseline.txt`；`git status --short` 全量冻结为他人域清单（追加到基线文件）；快照 `output_r185/workspace_snapshot.txt`；清点非本会话 pytest 进程。

**R1**：C1+C2 实现与单测（`tools/tests/` 自带 no-op conftest）。

**R2**：C3 Skill + references 三案例 + AGENTS 路由行 + `.gitignore` 白名单。

**R3**：C4 脚本与用例 + 全局技能引用只读核查；C5 实现与四规则用例，首跑留档 `output_r185/drift_report.json`。

**R4**：C6 复核留档；C7 用 C1 实跑基线留档。

**R5**：N1 三行对账 + N1b docare 清单 + N2 孤儿排班（全部只读；结论写 `output_r185/nightly_read.md`）。

**R6**：S6 只读盘点。

**R7 门禁与收口**（【v1.1】收窄）：

```powershell
# 必跑（本计划直接产物）
cd F:\python\数据资产\backend
.\.venv\Scripts\python.exe -m pytest tests/tools_tests 相关新测试 -q        # 按 tools/tests 实际路径
.\.venv\Scripts\python.exe -m pytest tests/db_guard tests/alembic_env -q    # 既有纯逻辑护栏
cd F:\python\数据资产\frontend
pnpm run typecheck; pnpm test; pnpm run build
# 可选（能跑则跑）：后端全量——前提三件：显式 APP_TEST_DB_URL、已清点他人 pytest、跑完重灌 import170
```

- 前端三件套失败且**归因他人域文件**（如未跟踪 captureMode 测试）→ 记 WARN 附证据，**不阻塞 C1-C5 验收**。
- 后端全量「0 新增 failed」的**基线=相对 R0 快照时刻**（他人脏文件导致的失败不计新增）。
- 产出：`开发起步包/185_执行报告.md` + 同号 `_结果.json` + `开发起步包/output_r185/`（progress / exceptions / workspace_snapshot / skill_check / doc_index_baseline / drift_report / nightly_read）；README/55 登记（`backend\.venv\Scripts\python.exe tools\register_doc.py 开发起步包/185_....md` 生成模板）。
- **Git 三组（零 push，显式路径 add + 每组 `git show --name-only` 对照白名单）**：
  1. `feat(tools): doc-index / test-env / multi-ai / drift checkers + tests + register_doc --check merge`
  2. `feat(skill): project-task-resume + AGENTS routing + gitignore whitelist`
  3. `docs(185): report + output_r185 artifacts`
- 并行会话域文件（身份线/skills 修订/output_r180/181/tar.gz）**一律不入任何一组**。

---

## 4. DoD（v1.1）

1. C1-C7 全落地或有 WARN 原因；N1 按三行口径出数（09-07 行缺失记 SKIP）；N1b/N2/S6 有只读结论。
2. 新工具单测 + db_guard + alembic_env 全绿；前端三件套过或他人域 WARN 留证；若跑了全量，0 新增 failed（基线=R0 快照）且已重灌。
3. `check_doc_index.py --check` 首份基线零误报（白名单仅 CLAUDE 转发/150）。
4. 报告+`_结果.json`+`output_r185/` 齐备；README/55 登记；三组提交零越界；零 push。
5. 他人域（13 文件+冻结清单）哈希或 status 与 R0 一致；业务源写入 0；**平台库写入 0**；生产 env/cron/镜像未动。

---

## 5. 执行提示词（v1.1，复制给执行 AI）

```text
【角色】你是仓库 F:\python\数据资产 的执行 AI。执行入口是 开发起步包/185_AI协作收敛剩余任务一次性执行计划.md（v1.1）。183 号已移交本计划执行、禁止按其正文开工；184 已完成项禁止重做（其工具 bug 已修复，若再发现允许语法/变量级最小修复并记 WARN）。
【开工】Git Bash 环境（PowerShell 用 bash -lc）。按 185 §0 阅读切片读必读文件（禁止全文通读 README/55，防上下文过载）。
【铁律】业务源库与平台生产库零写入；零 push；零生产发布/零改 cron/env；01:45-02:30 不连生产容器；值域不自动 confirm；他人域=13 文件+R0 冻结的 git status 非本计划改动（身份线/skills 修订/output_r180、181 一律禁碰）；禁 checkout/stash；不为过测试弱化断言；Python 用 backend\.venv\Scripts\python.exe。
【批次】R0 切片自检+冻结他人域 → R1 C1(唯一目录检查器,register_doc --check 并入)+C2(包装 app/core/database_guard,端口占用=就绪) → R2 C3 Skill+references 三案例(147 在 _archive)+AGENTS 路由行 → R3 C4(含全局技能引用只读核查,未接如实标注)+C5(不要求零高危,白名单仅点名项) → R4 C6 复核+C7 基线留档 → R5 N1 三行对账(02:00 failed/08:44 排水 success/09-07 缺则 SKIP,禁用最新一行判定)+N1b docare 清单+N2 孤儿排班(键 V3/OPER1/STATUS=0,走 docare 技能) → R6 S6 只读盘点 → R7 必跑门禁(新工具单测+db_guard+alembic_env)+前端三件套(他人域失败记 WARN)+登记+三组白名单提交。
【产出】开发起步包/185_执行报告.md + 同号 _结果.json + 开发起步包/output_r185/ 全套；README/55 登记；宣称完成必须满足 185 §4。
【STOP 白名单】隔离库不可用且 C2 用例受阻；他人域无法避开；需写业务源库/平台生产库/生产 env；N1 实测与 output_r180/nightly_d.md 矛盾（停并呈报，不自行改写既有证据）；dev_env.sh 损坏且最小修复无效。其它失败 WARN 留痕继续。
```

---

## 6. 执行日志

| 时间 | 操作 | 结果 |
|---|---|---|
| 2026-09-06 | 184 复盘收口后制定 v1.0 | 移交 183 S1-S6 未执行项+184 修复项 |
| 2026-09-06 | 外部 AI 审核 16 条意见全量核实采纳，修订 v1.1 | 采纳要点：Windows 入口/切片阅读；**平台库改零写入**；他人域扩展+冻结清单；N1 重写为三行对账（F-2 首夜预期已被 round-9 flush 推翻：02:00 failed=max_change_ratio 水位死锁、08:44 排水 success、观察点=09-07）；C1 唯一检查器（register_doc --check 并入）；C2 包装 database_guard+端口占用=就绪；C3 补 AGENTS 路由/调用 C1C2/147 归档路径/quick_validate 降级；C4 调用链核查；C5 去"零高危"；C6 定位为事后质检；C7 降级；R7 门禁收窄+基线定义+白名单提交；产物路径统一 开发起步包/output_r185/；tools/tests 自带 conftest。**修订会话已先行修复两个产物 bug**：dev_env.sh `fi;;` 重复（bash -n 过+domain 链路实测）、ops-runbook `password=pwd`→`p`。183 在 README 的状态同步改为「执行入口已移交 185」 |
