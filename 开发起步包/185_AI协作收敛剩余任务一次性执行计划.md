> 类别：待办
>
> 状态：**v1.0 定稿待执行（2026-09-06）**
>
> 上位入口：`55_系统未完成事项统一执行计划.md`；承接 `183_AI协作指令收敛与重复流程自动化开发计划.md`（S1-S6）与 `184_AI协作复盘_高频流程沉淀与中断分析报告.md`（已完成项不再重做）
>
> 制定日期：2026-09-06

# 185 · AI 协作收敛剩余任务一次性执行计划

本文件给**另一名执行 AI**单会话（或连续多会话）落地。目标：完成 183 号 S1-S6 中尚未执行的全部批次 + 184 号复盘发现的修复项 + 仓库卫生收尾。**184 已完成的不再重做**（ops-runbook Skill、tools/dev_env.sh、tools/register_doc.py、tools/export_zcode_conversations.py、AGENTS 路由行、五项复盘分析均已交付并提交）。

---

## 0. 角色、边界与禁止

| 角色 | 做什么 |
|---|---|
| 执行 AI（本计划对象） | 按 R0→R7 推进标明可执行的批次；不确定记 WARN/STOP |
| 用户 | Git push、生产发布、业务源库写、cron/env、值域 confirm、探查终态 |

硬边界（违反即回滚并停止）：

1. 业务源库零写入；平台库只写测试/审计类表；隔离库只用 8.83 `data_asset_test`。
2. 零 Git push；本地提交允许且必须分域（见 §3 R7 分组）。
3. 零生产发布/零改 cron/零改 env；零值域 confirm；探查发现不代点终态。
4. 他人域零触碰：`frontend/src/layout/**`、`captureMode.ts`、`store/modules/app.ts`、`tools/capture_*.py`、`competitionCaptureMode.test.ts`；禁止 checkout/stash。
5. 不为过测试弱化断言；失败如实记 WARN/BLOCKED。
6. **环境装配先 `source tools/dev_env.sh`**（建隧道+推导 APP_TEST_DB_URL，复用既有 15432 转发）；跑全量 pytest 前清点非本会话 pytest 进程（184 §7 刚发生互踩实例）。

---

## 1. 任务总表

### 1.1 P0-代码（默认可执行）

| ID | 事项 | 位置 | 要点 | 验收 |
|---|---|---|---|---|
| C1 | 183-S2：目录与编号只读检查器 | 新建 `tools/check_doc_index.py` | 识别编号正文/同号 `_结果.json`/同号输出目录/README 组合登记；查孤儿、幽灵、重复编号、缺类别；`--fix` 仅补本次指定文件的 README 登记。**与既有 `register_doc.py` 协同：register=登记模板生成，check=一致性检查，二者共用编号解析正则，不得各写一套** | 对当前 `开发起步包/` 零误报（组合登记的结果文件不得误报孤儿）；`--check` 幂等 |
| C2 | 183-S3：测试环境预检器 | 新建 `tools/check_test_environment.py` | 输出 `pure_logic_ready`/`integration_ready`/`migration_ready` 三态+可运行命令；检查 APP_TEST_DB_URL 存在/含 test/非 denylist、APP_DB_URL 一致性、15432 占用；不打印凭据；不杀进程、不跑 pytest/TRUNCATE/迁移。**复用 `backend/tests/db_guard/` 既有规则与 `tools/dev_env.sh` 的 URL 推导逻辑（可 import 或抽公共函数，防双份漂移）** | 五类用例：缺 URL/生产 URL/共享不可清理库/端口占用/合法隔离库 |
| C3 | 183-S1：跨会话接手 Skill | 新建 `.agents/skills/project-task-resume/SKILL.md` | 输入=用户当前要求+git status+README 当前入口+55 最新状态+相关执行报告；输出=当前目标/已完成证据/未完成步骤/文件归属/可继续项/需批准项/阻塞条件。不复制整份 README/55；历史授权不作当前授权 | 三个真实案例回放验收：147 中断续跑、177 隔离库阻塞、180 分段授权（只读回放，不改生产） |
| C4 | 183-S4：多 AI 证据流程脚本 | `tools/multi_ai_evidence.py`（薄包装） | CLI 存在性预检、round 编号、输出 UTF-8 归一、SHA-256、缺席状态与结果清单；不自动裁结论；全缺席时生成明确未完成报告 | 模拟单缺席/全缺席两用例收口正确 |
| C5 | 183-S5：指令漂移检查 | 新建 `tools/check_instruction_drift.py`（只读） | 扫描 AGENTS/CLAUDE/项目 Skill/全局 Skill/55 顶部中的批准、停止、只读、全量测试、历史授权关键词；语义规则四条（源库只读不放宽/平台 active 与人工批准不混用/历史授权≠当前授权/局部阻塞不宣称完成）；输出 JSON+短 MD | 对当前仓库跑一次，报告 0 个高危冲突（已知良性项列入白名单） |
| C6 | 184 修复项：ops-runbook Skill 静态校验 | `.agents/skills/ops-runbook/SKILL.md` | frontmatter 合法性（name/description/触发词）、引用路径存在性（tools/dev_env.sh、179/180 文档）、代码块语法目检 | 校验脚本或人工清单留档 `output_r185/skill_check.md` |
| C7 | 184 修复项：`register_doc.py --check` 基线 | `tools/register_doc.py` | 当前应零孤儿（184/185 登记后）；若报告孤儿按提示补登记；幽灵条目除 150（已知已删仍登记）外为零 | `--check` 输出留档 |

### 1.2 P1-核对（只读，产出报告）

| ID | 事项 | 要点 |
|---|---|---|
| N1 | 09-06 夜跑与 docare 每日任务首夜读数 | 只读查 `asset_identity_scheduler_runs` 最新行（F-2 首夜预期 success+resync_unchanged 出现）+ `/opt/data-asset/evidence/docare_mismatch/list_20260906.md`（首份自动清单）；FAIL/异常只呈报不处置 |
| N2 | c0632940 孤儿排班核对 | 只读查 `MED_OPERATION_MASTER` 的 V3/OPER_ID=1 排班态是否仍在（STATUS=0）；处置建议写入报告（作废动作需人工在手麻系统做，AI 不碰） |

### 1.3 183-S6 的处置（降级为调研，不实施）

183-S6 原文针对 `~/.codex/skills/`（Codex 生态路径）。本环境为 ZCode（`~/.zcode/skills/`）。执行 AI 只做**只读盘点**：列出 `~/.zcode/skills/skill-creator` 与插件缓存 `~/.zcode/cli/plugins/.../skill-creator` 的加载关系，输出迁移建议到报告；**不移动、不改名、不删除任何全局技能**。

---

## 2. 工作区分域

| 域 | 文件 | 本计划 |
|---|---|---|
| 新工具 | `tools/check_doc_index.py`、`tools/check_test_environment.py`、`tools/multi_ai_evidence.py`、`tools/check_instruction_drift.py` 及配套测试 | 主战场 |
| 新 Skill | `.agents/skills/project-task-resume/` | C3 |
| 既有工具微调 | `tools/register_doc.py`、`tools/dev_env.sh`（仅当 C1/C2 需要抽公共函数） | 允许小改+补测 |
| 文档 | 185 执行日志、55/README、184 §7 不得回改结论 | 收口用 |
| 他人域/身份/layout/质量 174 | 既有 dirty | **整域禁碰** |

---

## 3. 批次执行卡

总序：`R0 自检 → R1（C1+C2 工具先行）→ R2（C3 Skill）→ R3（C4+C5）→ R4（C6+C7+C1 验收）→ R5（N1+N2 只读核对）→ R6（S6 调研）→ R7 门禁+报告`。

### R0 开工自检（只读）
读 AGENTS → README → 55 📌 → 183 全文 → 184 全文 → 本文件；`source tools/dev_env.sh`；`bash tools/dev_env.sh --domain-baseline`；`git status` 快照到 `output_r185/workspace_snapshot.txt`；清点非本会话 pytest。

### R1 工具先行（C1、C2）
按 §1.1 要点实现；两脚本均需最小单元测试（`tools/tests/` 或 pytest 可发现位置；纯逻辑、无库）。验收命令示例：
```powershell
python tools/check_doc_index.py --check
python tools/check_test_environment.py   # 三态输出
```

### R2 接手 Skill（C3）
写 SKILL.md（≤150 行，含触发词/输入输出/边界/三案例回放法）；静态校验留档。

### R3 证据与漂移（C4、C5）
实现+用例；`check_instruction_drift.py` 对当前仓库全量跑一次，结果归档 `output_r185/drift_report.json`。

### R4 校验与基线（C6、C7、C1 复验）
ops-runbook/register_doc 校验留档；`check_doc_index.py --check` 零孤儿基线。

### R5 只读核对（N1、N2）
容器内只读查询（连库标准件见 ops-runbook Skill §1，本地文件+`<` 重定向，禁 heredoc 中文）；结果写 `output_r185/nightly_read.md`。

### R6 S6 调研（只读盘点）
输出迁移建议，不动文件。

### R7 门禁与收口
```powershell
cd F:\python\数据资产\backend
.\.venv\Scripts\python.exe -m pytest tests/ -q          # 显式 APP_TEST_DB_URL；无库环境先跑纯逻辑子目录
cd F:\python\数据资产\frontend
pnpm run typecheck && pnpm test && pnpm run build
```
产出：`185_执行报告.md` + `_结果.json` + `output_r185/`（progress/exceptions/workspace_snapshot/各校验留档）；README 登记（用 `python tools/register_doc.py 开发起步包/185_....md` 生成模板）+ 55 📌。
Git 分域提交（零 push）：① `feat(tools): doc-index/test-env/multi-ai/drift checkers + tests`；② `feat(skill): project-task-resume`；③ `docs(185): report`。禁止把 layout/身份/174 混入。

---

## 4. DoD

1. C1-C7 全落地或有 WARN 原因；N1/N2 有只读读数；S6 有调研结论。
2. 后端全量 0 新增 failed；前端三件套过；新工具单测绿。
3. `check_doc_index.py --check` 零孤儿（150 白名单除外）。
4. 报告+`_结果.json`+`output_r185/` 齐备；README/55 登记；分域提交 `git show` 零越界；零 push。
5. 他人域哈希与 R0 基线一致；业务源写入 0。

---

## 5. 执行提示词（复制给执行 AI，勿增删）

```text
【角色】你是仓库 F:\python\数据资产 的执行 AI。唯一执行依据是 开发起步包/185_AI协作收敛剩余任务一次性执行计划.md（v1.0）。不要重做 184 号已完成项（ops-runbook Skill、dev_env.sh、register_doc.py、export_zcode_conversations.py、AGENTS 路由行）。
【开工前必读】AGENTS.md；README.md；55 顶部 📌；183 全文；184 全文；185 全文；.agents/skills/ops-runbook/SKILL.md。
【铁律】业务源库零写入；零 push；零生产发布/零改 cron/env；值域不自动 confirm；他人域 layout/captureMode/竞赛截屏零触碰；禁 checkout/stash；不为过测试弱化断言；开工先 source tools/dev_env.sh 并建他人域基线；全量 pytest 前清点非本会话 pytest；容器传脚本用本地文件+< 重定向禁 heredoc 中文。
【批次】R0 自检 → R1 C1+C2（check_doc_index / check_test_environment，复用 dev_env 与 db_guard 逻辑）→ R2 C3（project-task-resume Skill，147/177/180 三案例回放验收）→ R3 C4+C5（multi_ai_evidence / check_instruction_drift）→ R4 C6+C7（ops-runbook 静态校验、register_doc --check 零孤儿基线）→ R5 N1+N2 只读核对（夜跑/docare 首夜读数、c0632940 孤儿排班）→ R6 S6 只读调研 → R7 门禁+报告+分域提交。
【产出】185_执行报告.md + 同号 _结果.json；output_r185/ 五件套；README/55 登记；三组分域提交零越界。宣称完成必须满足 185 §4。
【STOP 白名单】隔离库不可用；他人域无法避开；需要写业务源库/生产 env；183-S6 试图让你移动全局技能。其它失败 WARN 留痕继续。
```

---

## 6. 执行日志

| 时间 | 操作 | 结果 |
|---|---|---|
| 2026-09-06 | 184 复盘收口后制定本计划 | 移交 183 S1-S6 未执行项+184 修复项；未改代码 |
