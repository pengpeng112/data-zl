# 115_112号双同步专项整改完成记录

> 类别：专项整改完成记录
> 日期：2026-08-03
> 关联：`114`（执行提示词）、`112`（整改执行计划）、`110`（Phase C 完成记录）、`118`（独立复核交接）
> 状态：本地代码整改完成 + 纯逻辑测试全绿；**生产/业务库未授权写入，双同步未正式启用**
> 2026-08-04 独立复核修复：字典写后回读 + sequence 标识符校验（见文末 §九）；不确定项见 `118`

## 一、本轮整改范围

按 `114` 严格限制，仅执行**本地代码整改 + 隔离测试库迁移 + mock/只读验证 + 文档更新**。

**Git 状态勘误（2026-08-04 独立复核）**：相关功能代码已进入**本地** commit（如 `9e550e9` 及后续提交链，`master` 曾 ahead origin），**未 push / 未打 release tag**。早期“全部未 commit”表述已过时；工作区仍保留用户其他未审核改动（Excel/资产 CSV/行尾等），不得混提交。

## 二、A 线（诊断/手术人工维护审核后即时同步）完成项

| 计划项 | 实现 | 关键文件 |
|---|---|---|
| A1 持久化任务机制（outbox） | 新增 `asset_dict_sync_outbox` 事件表：`business_key` 幂等唯一、`status/attempt/max_attempts/next_retry_at/lease_expires_at/lease_holder/last_error_masked`；审批即入队（与审批同事务） | `backend/app/models/dict_sync_outbox.py`、`alembic/versions/c0ffee12dead_add_dict_sync_outbox.py`（手写迁移，禁 autogenerate） |
| A1 worker（租约/重试/死信/恢复） | `claim_ready_events` 用 `SELECT ... FOR UPDATE SKIP LOCKED` 租约；指数退避 + 封顶；`should_dead_letter` 达上限转死信；`reset_expired_leases` 崩溃后恢复重放 | `backend/app/services/dict_sync_worker.py` |
| A2 同目标共享事务执行器 | 新增同目标多动作**单连接单事务**执行：`_run_target_transaction` 在一连接上按序执行全部动作，全部成功才 commit、任一失败整包 rollback，杜绝"逐动作独立 commit 导致部分写入" | `backend/app/services/dict_sync_executor.py` |
| A2 审批→即时派发链路 | `approve_plan` 同事务入队 outbox；API `POST /plans/{id}/approve` 调 `_dispatch_approved_plan`（服务端按目标派发）；`APP_DICT_MEDICAL_PUSH_ENABLED=false` 时仅返回 dry-run 预览，不触业务库 | `backend/app/services/dict_medical_push.py`、`backend/app/api/v1/dict_medical_push_api.py` |
| A3 多行冲突检测 | `check_exists_remote` 返回全部候选行；同键多行 → `plan_status=conflict`（含 distinct stop states），人工裁决，不再自动 skip/write | `backend/app/services/medical_code_push.py` |
| A3 serial 取号白名单化 | **移除 `MAX(serial_no)+1` 回退**；不要求与目标表建立 database default/关联，但运行期仍只允许 DBA 指定的并发安全取号来源（`APP_JHEMR_SERIAL_WHITELISTED_SEQUENCE`，默认空 → 取号即失败关闭） | `medical_code_push.py`、`backend/app/core/config.py` |
| A3 写后回读 | **2026-08-04 独立复核前**：仅依赖 DML 成功后 commit，缺同连接 SELECT 回读（已记为偏差）。**复核后已修**：`readback_actions_on_conn` 在 commit 前同连接校验 0 行/多行/停用态；失败整包 rollback 且 run=`failed`+`reconcile_required` | `medical_code_push.py`、`dict_sync_executor.py` |

## 三、B 线（HIS 人员夜间同步 CDMS/JHEMR）完成项

| 计划项 | 实现 | 关键文件 |
|---|---|---|
| B1 编排器接线 bridge | `_apply_cdms_target`/`_apply_jhemr_target` 在**全部写入门禁通过**后才调用 `identity_sync_executor_bridge` 执行真实写入，随后回读确认；任一 gate 不过 → 仅登记 `pending_reconcile`，绝不触业务库 | `backend/app/services/identity_sync_orchestrator.py` |
| B1 写入门禁（fail-closed） | `check_write_gates` 八项门禁默认全关：`identity_sync_enabled`、HMAC key、CDMS/JHEMR 写凭据文件、JHEMR 密码写、CDMS FID 语义确认、FTYPE 写禁、无 DELETE；`run_gate_checks` 并入写入门禁 | `identity_sync_orchestrator.py` |
| B2 CDMS 最小权限 fail-closed | `execute_cdms_apply` 强制 `APP_IDENTITY_SYNC_ENABLED` + `APP_IDENTITY_CDMS_FID_SEMANTICS_CONFIRMED`；FTYPE 8/32 永久禁止；分类白名单 doctor/nurse/pharmacist | `backend/app/services/identity_sync_executor_bridge.py`、`config.py` |
| B2 JHEMR 最小权限 | `execute_jhemr_apply` 强制密码写开关（SM4 交叉验证前关闭）；代码永不发 DELETE（`no_delete_actions` gate 恒真） | `identity_sync_executor_bridge.py` |

## 四、T1 验证（纯逻辑子进程测试，无测试库/生产库依赖）

新增 `backend/tests/dict_sync_112/`，沿用 111 子进程 + no-op conftest 范式，并登记入根 `conftest.py._PURE_LOGIC_SUBDIRS`：

| 测试 | 覆盖 |
|---|---|
| `test_dict_outbox_policy.py` | 幂等键稳定、指数退避封顶、死信判定、状态分类（成功/失败重试/死信）、错误脱敏截断 |
| `test_dict_executor.py` | 同目标共享连接按序执行、任一步失败即抛错（调用方整包回滚）、`validate_push_sql` 拒绝 DELETE/MERGE/多行 VALUES/IN-list/注释、白名单 sequence 默认关闭 |
| `test_identity_write_gates.py` | bridge fail-closed：全局关 → CDMS/JHEMR apply 直接 failed；非法分类拒绝 |
| `test_orchestrator_write_gates.py` | 编排器 `check_write_gates` 默认全红、关键 gate 存在且关闭 |
| `test_additional_boundaries.py` | serial 白名单默认关闭、JHEMR 多表/目标边界和执行器失败关闭补充回归 |

**结果：`21 passed, 1 skipped`**（`tests/dict_sync_112 tests/security_audit tests/startup_check tests/db_guard tests/role_effective` 全绿）；`app.main` 导入正常；Alembic head = `c0ffee12dead`（down_revision `f8a9b0c1d2e3` 正确）；后端 `compileall` 通过。

## 五、本轮未执行（需人工/生产授权）

- ❌ 未对任何生产/业务库执行 DML/DDL、锁表、存储过程。
- ❌ 未开启 `APP_DICT_MEDICAL_PUSH_ENABLED` / `APP_IDENTITY_SYNC_ENABLED` / nightly / 任何写开关。
- ❌ 未创建 sequence/唯一约束/数据库用户；用户确认不要求 sequence 与目标表建立 default/关联，但运行期仍需 DBA 指定并发安全取号来源后才可配置 `APP_JHEMR_SERIAL_WHITELISTED_SEQUENCE`。
- ❌ 未做 Alembic 实跑迁移（本机无隔离测试库）；迁移文件已按 114 手写，待测试库就绪后执行 `alembic upgrade head`。
- ❌ 未 push/tag；功能代码可能已在本地 commit 链中（见 §一勘误）；用户其他未审核改动全部保留、不得混提。

## 六、遗留与下一步

1. 隔离测试库就绪后：`alembic upgrade head` 实跑 `c0ffee12dead`，再用测试库验证 outbox 租约/重放与同目标事务 rollback 语义。
2. CDMS `FID=HIS工号`、`FUSER=admin`、`FUPDATEUSER` 为空及 FTYPE 0/2/3/5/10 映射已由用户 SQL 和活库聚合确认；适配器已按 `FID` 回读、`FUSER=admin` 写入、FTYPE=10 值 `1` 修正。2026-08-04 用户最终确认：护士角色 `e09c6b5410ed4eefbb5bb57cea51ab0a`、医师角色 `a1c9192fbe31423fab2dce6f81791b88`，均与当前平台迁移映射一致；科主任附加角色为 `4b814c646d244a2885d073130491e3f6`，仅在源端可可靠识别科主任时追加。生产环境仍需显式设置 `APP_IDENTITY_CDMS_FID_SEMANTICS_CONFIRMED=true`，默认值继续保持 false。
3. JHEMR `serial_no` 不要求与表建立数据库关联；由 DBA 指定并发安全取号来源后配置 `APP_JHEMR_SERIAL_WHITELISTED_SEQUENCE`。HIS `STOP_FLAG` 与 JHEMR `isstop` 均按用户确认采用 `0=启用、1=停用`，停用动作需在隔离目标库验证。
4. 双目标真实联调（A 线诊断/手术、B 线人员）需在测试库先行，验收通过后才进入生产启用决策。
5. 用户确认 JHEMR 身份写账号权限已处理，后续验收只核对业务账号不得拥有或执行 DELETE，不再重复索要该项业务确认。

## 七、代码评审（AI review）反馈修复（2026-08-04）

评审对 A/B 线整改代码复核发现 3 个 medium 问题，已逐一修复并补回归断言：

| # | 评审发现 | 修复 | 涉及文件 |
|---|---|---|---|
| 1 | 脱敏工号 `12****34` 被当作 `display_name` 写入 CDMS/JHEMR 业务表 | 新增 `_display_name()`：仅取 `IdentityPerson.person_name_cn` 真实姓名；取不到即 `RuntimeError` → 登记 `pending_reconcile` 并返回 failed（fail-closed），绝不把掩码当姓名 | `backend/app/services/identity_sync_orchestrator.py` |
| 2 | 重试会把真实写入失败误记成成功/幂等跳过：失败路径写入 `pending_reconcile` 后，重试时被 `idempotency_key` 命中判为 `idempotent_skip`，掩盖故障 | 新增 `_register_managed()` 幂等 upsert 帮助器；`_apply_cdms_target`/`_apply_jhemr_target` 的幂等跳过仅对 `status=="active"` 生效；`pending_reconcile` 行在重试时被 UPDATE 并重新 apply（唯一约束不冲突，动作精确重放） | `identity_sync_orchestrator.py` |
| 3 | `dispatch_target` 未在执行器边界复检全局写开关，仅依赖源 `write_policy` | `dispatch_target` 入口新增 fail-closed：`APP_DICT_MEDICAL_PUSH_ENABLED=false` 直接返回 `disabled`，即使由 outbox worker（非审批 HTTP 路径）驱动也不写 | `backend/app/services/dict_sync_executor.py` |

修复后回归：新增 `tests/dict_sync_112/test_review_fixes.py` 覆盖执行器边界开关、掩码不可作姓名、pending 不判成功三点。**T1 全绿：`20 passed, 1 skipped`**（`tests/dict_sync_112 tests/security_audit tests/startup_check tests/db_guard tests/role_effective`）；`app.main` 导入正常；后端 `compileall` 通过。生产/业务库仍零写入，写开关保持关闭，未 commit/push/tag。

## 八、执行 AI 复核确认（2026-08-04 续）

执行 AI 逐项对照 `114` 与本记录对 112 各阶段源码做二次核验，结论与本记录一致：

| 阶段 | 核验点 | 源码证据 | 结果 |
|---|---|---|---|
| P0（前验收） | 111 基线门禁 | S1/S3/S4 子进程测试 | 通过 |
| A1 | outbox 事件表 + worker 租约/重试/死信/恢复 | `dict_sync_outbox.py`（`business_key` 唯一、lease 字段全）、`dict_sync_worker.py` `claim_ready_events` 用 `FOR UPDATE SKIP LOCKED`、`reset_expired_leases` 崩溃恢复、`should_dead_letter` 封顶 | 符合 |
| A2 | 审批同事务入队 + 同目标单连接单事务 | `dict_medical_push.py:161-164`（`enqueue_plan_dispatch` 在 `approve_plan` 事务内、随后 commit）；`dict_sync_executor.py:208` `dispatch_target` 单连接执行、全部成功才 commit、异常整包 rollback；API `POST /plans/{id}/approve` 仅持久化 outbox，HTTP 请求同步派发被禁用（worker-only） | 符合 |
| A3 | 多行冲突 + serial 白名单 + 写后回读 | 多行 conflict + serial 白名单仍成立；写后回读见 §九（commit 前 `readback_actions_on_conn`） | 符合（回读已于 2026-08-04 补齐） |
| B1 | 编排器 gate 通过才调 bridge、否则 pending_reconcile | `identity_sync_orchestrator.py:821/910` `check_write_gates`、`_register_managed` 幂等 upsert（active 才幂等跳过）；`run_gate_checks` 并入 | 符合 |
| B2 | bridge fail-closed + 回读 + 禁 DELETE | `identity_sync_executor_bridge.py`：`_require_identity_sync_enabled`/`_require_phase_d_approval`/`_require_password_write_enabled`/`_require_cdms_semantics_confirmed`；`execute_cdms_apply`/`execute_jhemr_apply` 后接 `execute_*_readback`；CDMS `CDMS_FORBIDDEN_FTYPE={8,32}`；JHEMR 分类白名单、无 DELETE | 符合 |
| T1 | 纯逻辑测试全绿 | 本机重跑 `tests/db_guard tests/role_effective tests/startup_check tests/security_audit tests/dict_sync_112` | `22 passed, 1 skipped`（含 §九新增用例） |
| T1（前端） | typecheck 无回归 | 本机 `pnpm run typecheck` | EXIT=0 |

**复核后状态不变**：生产/业务库零写入、双同步开关关闭、serial 取号来源未配置（默认失败关闭）、**本地已 commit / 未 push/tag**。剩余阻断（隔离测试库迁移实跑、生产序列/唯一约束、Phase D secrets、双目标真实联调）与第六节一致；不确定项清单见 `118`。

## 九、独立复核确定项修复（2026-08-04）

| # | 问题 | 修复 | 测试 |
|---|---|---|---|
| 1 | 字典 `dispatch_target` 写后无独立回读即标 succeeded | 同连接 `readback_actions_on_conn`，**commit 前**校验；失败 rollback + run failed | `tests/dict_sync_112/test_readback_and_sequence.py` |
| 2 | sequence 名直接拼 SQL，缺标识符校验 | `validate_sequence_identifier`；PG 侧 `nextval(%s)` 绑定；非法配置 fail-closed | 同上 + `test_additional_boundaries` |

本机复跑：`tests/db_guard tests/role_effective tests/startup_check tests/security_audit tests/dict_sync_112` → **22 passed, 1 skipped**。未连生产/业务库，写开关保持关闭，未 push/tag。
