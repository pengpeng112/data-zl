> 类别：证据  
> 日期：2026-07-13  
> 范围：体验线 — 前端 UI 走查修订 + 数据中心五层导航（`50`/`51`）

# 68 体验线：UI 走查与五层导航核查修订

## 1. 结论

| 项 | 结果 |
|---|---|
| 五层导航 | **已修订**：大类 → 系统/库 → Owner → 表 → 字段（懒加载） |
| ODS 内部分区 | **已支持**：主源 `ods_8_216` 按 owner 拆 HIS/LIS/PACS/EMR/护理/手麻/CDA/其他 |
| HIS 源端 | **独立大类**，不再与 ODS 混层 |
| 图谱 D 类图例 | **已补齐**（虚线灰紫 + 文案对齐 51 口径） |
| 首页 KPI | **按当前业务数据改写**（人员 4260、复核 2054 等） |
| 响应式 | 表目录 <1100px 单列；图谱工具条既有断点保留 |
| typecheck | **通过**（`tsc` + `vue-tsc`） |
| 人工浏览器视觉 | 仍建议人眼点一遍登录/首页/表目录/图谱（本环境未做真实浏览器点击） |

## 2. 核查发现（修订前）

| # | 问题 | 影响 |
|---|---|---|
| N1 | 表目录树仅「HIS / 数据中心」粗分，无 `system_category` | 不满足 51 五层与 ODS 内拆 |
| N2 | `default-expand-all` 全展开 | 大树卡顿 |
| N3 | 点击「系统」节点把 `DATA_CENTER` 伪码当 `system_code` 过滤 | 列表易空 |
| N4 | 表头无「系统大类 / 系统库」语义列 | 浏览口径不清 |
| N5 | 图谱 D 类仅有勾选文案，无图例色条 | 与 51「必须单独图例」不符 |
| N6 | 首页 KPI 仍写 HRP 7 万表等过时数字 | 指挥中心信息失真 |
| N7 | 窄屏双栏布局未适配 | 体验差 |

## 3. 修订内容

### 3.1 后端 `GET /api/v1/assets/tree`

- 增加字段：`system_category` / `system_category_cn` / `source_system` / `source_system_cn`
- ODS 主汇聚源按 **namespace/owner** 拆成多条逻辑节点（HIS/LIS/PACS/EMR/YDHL/SM/CDA/other）
- 支持 `system_category` 查询参数
- 文件：`backend/app/api/v1/systems.py`

### 3.2 前端表目录

- 五层树：`category → system → schema → table → column`
- 顶部 **大类 CheckTag 筛选**
- 仅默认展开大类+系统层（`default-expanded-keys`）
- 列表列：系统大类、系统/库、Owner、表…
- 响应式单列
- 文件：`frontend/src/views/asset/tables/index.vue`、`hierarchy.ts`、`api/asset.ts`

### 3.3 图谱与首页

- `GraphToolbar`：A/B/C/D/候选 **图例行**；D 类提示改为「可展示但不可当 A 类依据」
- `welcome` KPI 对齐当前：8 系统、4260 人员、2054 待复核、质量 L15 等
- 系统总览副标题对齐五层映射

## 4. 验收清单（建议人工浏览器再点）

| 步骤 | 期望 |
|---|---|
| 打开 `/asset/tables` | 左侧出现 ODS / HIS / 其他业务 等大类 |
| 展开 ODS | 见 HIS 抽取区、LIS 抽取区等 |
| 点 Owner | 右侧表列表按 schema 过滤且非空（在有元数据时） |
| 点表 | 字段预览加载；双击进表详情 |
| 搜索 `PAT_VISIT` | 树过滤可见相关节点 |
| 打开 `/asset/graph` | 可见 A/B/C/D 图例；勾选 D 类后告警为 warning 口径 |
| 打开 `/welcome` | KPI 数字与副文案反映当前阶段 |
| 窄窗口 | 表目录改为上下布局 |

## 5. 未纳入本轮 / 残留

| 项 | 说明 |
|---|---|
| 真实浏览器像素级视觉 | 需人眼确认青蓝主题、登录玻璃拟态等 |
| 全量 `asset_tables` 未导入时树为空 | 需后台导入归一包/快照到 `asset_tables` 后导航才有体量 |
| 图谱分组选项文案 | 已有「按系统大类」；后端 graph 若未回填 category 仍可能显示编码 |
| 深色主题全站 | 50 方案默认浅色 + 首页深色；未改 global dark 策略 |

## 6. 命令

```powershell
cd F:\python\数据资产\frontend
pnpm run typecheck   # 本轮已过
```

## 7. 状态建议

- `50`：代码升级 + 本轮修订完成；**人工视觉仍可再签**  
- `51`：五层导航前端+树 API **已接续验收项**；归一包本身此前已生成  

*验收以浏览器实测为准；若库中无表元数据，请先跑资产导入或 collect 再验导航体量。*
