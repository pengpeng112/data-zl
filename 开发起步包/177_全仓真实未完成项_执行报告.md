> 类别：执行报告
>
> 状态：**R0→R7 一次性执行完成（2026-09-03 晚，单会话）；R1 夜跑核验按计划延后，R5 按计划 SKIP**
>
> 上位计划：`177_全仓真实未完成项一次性执行计划.md`（v1.0）；基线 HEAD=96ed027（171 收口）
>
> 证据目录：`开发起步包/output_r177/`（progress.md / exceptions.json / workspace_snapshot.txt / wait_domain.md）

# 177 · 全仓真实未完成项执行报告

## 0. 一句话结论

R2 六项缺陷五修一跳（C6 白名单冲突 WARN 跳过）、R3 身份线三项复核/实现全部落地（**F-2 熔断语义代码+9 测试本批完成，生产未发布**）；门禁全绿（后端全量 **1415P/1S/0F**、前端 typecheck 双过 + **267/267** + build 预算三绿、身份专项 126P/0F）；隔离库已重灌（12702 表/1329 关系/107 序列对齐）。R1（今夜 02:00 熔断验证）未到时点按计划延后；R5 CDMS 因用户未选表现整批 SKIP；Git/发布/值域 confirm/探查终态零越权。

## 1. 执行批次总览

| 批 | 状态 | 摘要 |
|---|---|---|
| R0 开工自检 | ✅ DONE | 快照+他人域 13 文件哈希基线落 `output_r177/workspace_snapshot.txt`；15432 隧道自建；无他人 pytest；未 checkout/stash |
| R1 夜跑核验 | ⏸ DEFERRED | 2026-09-04 02:00 未到（W-03），夜跑后按 177 R1 口径只读补验 |
| R2 缺陷修复 | ✅ DONE（C6 WARN） | C1–C5 全修 + 针对性测试（后端 `test_r177_fixes.py` 4 用例、前端 `plan177R2.test.ts` 5 用例） |
| R3 身份线 | ✅ DONE | I1/I2 工作区 diff 复核通过；I3 F-2 实现 + `test_identity_cb_resync_f2.py` 9 用例；身份专项 126P/0F |
| R4 门禁文档 | ✅ DONE | 后端全量 1415P/1S/0F；alembic current=d5e6f7a8b9c0（隔离=生产）；前端三件套全绿；重灌+序列对账 |
| R5 CDMS | ⛔ SKIP | 用户未选 004066 表现 A/B/C（W-04 → G7） |
| R6 等待域 | ✅ DONE | `output_r177/wait_domain.md`（A1–A11 + 165 六条明细 + 150 三选一 + G1–G17 快照） |
| R7 DoD | ✅ 见 §5 | 他人域 13 文件哈希终检与 R0 基线一致 |

## 2. R2 修复明细（含实证根因修正）

| # | 修复内容 | 文件 | 验证 |
|---|---|---|---|
| C1 | import170 重灌末尾 DO 块对 asset schema 全部 nextval/identity 列 `setval(seq, greatest(max(id),1), true)`，setval 后 `last_value>=max(id)` 硬校验（不齐 RAISE 整体失败） | `开发起步包/output_r170/import170.py` | 实跑重灌 107 序列全重置、对账 behind=0、`POST /api/v1/queries/ingest` **500→200**、audit max(id)=194=last_value |
| C2 | **实测根因修正**：非 CANONICAL 分区循环自 6e5dc55 已存在，真缺陷是返回 `by_code` 的 **upper() 规范化键**（建 `r177sys` 列表显示 `R177SYS`，回查/详情 404）。修复=返回 `row.system_code` 库内原值 + connection_count 真实计数（原硬编码 0） | `backend/app/services/asset_catalog.py`（list_first_level_systems） | 复现实验（PUT→GET 断言失败→修复→过）+ `test_r177_fixes.py` 2 用例（混合编码可见、预置系统仍排前） |
| C3 | 6 写按钮补 v-perms：probe-findings 确认迁移=`probe.finding.transition`；value-domains 确认=`value_domain.confirm`；dict/general 4 个保存=`dict.general.edit`（**非 177 草案的 dict.general.manage——该码不存在**，按同页既有码） | 三个 index.vue | `plan177R2.test.ts` 3 用例；存量锁 plan166StageD2 B3 计数 3→4（库存更新，新按钮纳入保护） |
| C4 | **根因链**：RelationGraph 自身先经 graphNormalize（ECharts 风格 `label:{show,formatter,...}` 对象）再消费，274 行取对象/432 行 `String(对象)` → `[object Object]`。新增 `edgeLabelText()`：字符串 label → `label.formatter` → `from_columns` 字符串 → 空串；两处消费点改用 | `frontend/src/views/asset/components/RelationGraph.vue`（注意：实际路径在 components/ 非 177 所写 graph/） | `plan177R2.test.ts` 2 用例（helper 兜底链 + 禁止裸 String(edge.label)） |
| C5 | create 前置校验：`validate_recipe_tables` 非法标识 422（空表仍是合法草稿）；提取逻辑共享 `extract_recipe_table_names` 供 generate_select_sql 复用 | `backend/app/api/v1/recipes.py` + `backend/app/services/recipe_service.py` | `test_r177_fixes.py` 2 用例（非法 422 / 合法+空表 200） |
| C6 | 未修（WARN 跳过） | — | 父组件 graph/index.vue 共享响应式对象设计，纯子组件改 emit 必破坏功能，父组件不在白名单（W-01） |

## 3. R3 身份线（代码完成，生产未发布）

- **I1（CDMS FFREE3）复核通过**：模板常量 + INSERT 列 + 建户 dict + align 幂等补齐（`NVL(FFREE3,' ')<>'1'` 才写）四处齐全，与 172 §6 一致。持久化=G1（Git+镜像重建）。
- **I2（JHEMR 登录/签名方式子任务）复核通过**：`identity_login_sign_sync.py` additive 语义明确（只补缺 control_mode/sublogin/subsign 0/2/4、默认标记仅无默认时补、绝不删已有）、fail-closed、20000 行上限；adapter `apply_login_sign_gaps` FOR UPDATE 行锁+存在性复核+单事务；runner/audit/status/log 五处接线与签名/职称子任务同款门控。生产接 cron=G2。
- **I3（176 F-2）本批实现**：
  - 语义（`identity_sync_orchestrator.py:_compute_change_stats`）：托管候选带 `modified_time`（HIS MODIFIEDTIME 增量命中，真实变更）或 JHEMR 存在性检查失败（None，**fail-closed 回退旧口径**）→ `update`；无增量标记且检查成功 → `resync_unchanged` 单列（不计 max_update/max_change_ratio，仍受 max_candidates 约束）。
  - `check_thresholds`：update 维度语义注释更新；ratio 分子自然排除 resync。
  - 测试（`tests/test_identity_cb_resync_f2.py`，9/9）：**110 人纯 resync 不熔断** ✓；**110 人真实 update 仍熔断**（阈值 100）✓；检查失败回退 ✓；105 例行+5 变更拆桶 ✓；ratio 排除 ✓；max_candidates 兜底 ✓；未托管候选不受影响 ✓。
  - 兼容性：存量 `test_new_vs_update_split`（None 路径）与 W10 align 全套测试不受影响。
- 生产发布 + MAX_NEW 130 回落 50 = G3（发布后 F-1 的 150 阈值亦可回落默认 100）。

## 4. 门禁汇总

| 门禁 | 结果 |
|---|---|
| 后端全量 `pytest tests/ -q` | **1415 passed / 1 skipped / 0 failed**（32min；171 基线 1341+174 生产批 1397 之后仅增不减，本批新增后端测试 13） |
| 身份专项（177 指定三件 + align + F-2） | **126 passed / 0 failed** |
| 后端专项（systems/recipes/catalog + r177） | 49 passed / 0 failed |
| alembic current（隔离库） | `d5e6f7a8b9c0 (head)`（与生产迁移头一致，未做 upgrade） |
| 前端 typecheck | tsc + vue-tsc 双过（0 错） |
| 前端 vitest | **267 / 267**（含 plan177R2 新 5 用例；plan166StageD2 B3 库存更新 3→4） |
| 前端 build | 主入口/图谱异步包/CSS gzip 预算三绿 |
| 重灌复验 | systems=26 / tables=12702 / relations=1329 / value_domains=77 / quality_rules=422 / **107 序列 behind=0** |
| 白名单合规 | 本批改动全部在 177 R2/R3 白名单内（唯一延伸：C5 共享校验函数落在 recipe_service.py，语义仍限 create 校验）；他人域 layout/captureMode 13 文件哈希终检=R0 基线 |

## 5. DoD 自检（对照 177 §6）

1. ✅ R2 C1–C5 有文件:行号与测试（见 §2；C1 为工装脚本+端到端复验）。
2. ✅ R3 身份专项 0 failed；F-2 有"110 人 resync 不熔断"测试（9 用例双向锁定）。
3. ✅ 全量 pytest 无新增失败（1415/1S/0F）；前端 typecheck+tests+build 过。
4. ✅ 本报告 + `_结果.json` + `output_r177/`（progress/exceptions/workspace_snapshot/wait_domain）齐备。
5. ✅ README 目录更新记录 + 55 📌 已写（随本批）。
6. ✅ 等待域 G1–G17 已呈报（wait_domain.md），未把等待项标成完成。
7. ✅ 他人域未改（哈希终检一致）；业务源写入 0；生产 env/cron/镜像未改；零 Git 写。

**未宣称完成**：R1 夜跑验证（未到时点，非"已验证"）；165 X3 六条 finding（未代点）；150 课题（等用户三选一）；F-2 生产发布（仅代码+测试）。

## 6. 交付物与证据索引

- 代码：`import170.py`（序列重置）、`asset_catalog.py`、`recipes.py`+`recipe_service.py`、`identity_sync_orchestrator.py`（F-2）、`RelationGraph.vue`、3 个 index.vue（v-perms）、`plan166StageD2.test.ts`（库存更新）
- 测试：`backend/tests/test_r177_fixes.py`（4）、`backend/tests/test_identity_cb_resync_f2.py`（9）、`frontend/tests/plan177R2.test.ts`（5）
- 台账：`output_r177/progress.md`、`exceptions.json`（W-01～W-04）、`workspace_snapshot.txt`、`wait_domain.md`

## 7. 交接下一会话

1. **R1（明日）**：02:00 后只读查 `asset_identity_scheduler_runs` 最新行（status/circuit_breaker_triggered/candidates_total/update 计数/子任务 skipped 原因）——F-1 生效预期 success 且 update=110<150；若仍熔断呈报不自行调阈值。
2. R5：开工先问用户 004066 表现 A/B/C，未答整批跳过。
3. Git 授权后分域提交（177 §3 R4 五组，本批改动归 173/171/identity 三组；layout/captureMode 禁入任何一组）。
