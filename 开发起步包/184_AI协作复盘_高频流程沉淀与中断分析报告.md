> 类别：执行报告
>
> 状态：**已完成（2026-09-06，五项复盘任务一次执行；与 183 号计划互补）**
>
> 数据基础：35 个会话档案（tools/export_zcode_conversations.py 导出，11,531 条消息）+ 两轮只读子代理模式分析 + 项目 exceptions 台账

# 184 · AI 协作复盘：高频流程沉淀、脚本化、指令查重、中断分析与回归裁决

## 0. 与 183 号计划的分工

183 号（并行会话制定）规划指令收敛/接手 Skill/目录检查器/环境预检器（S1-S6）。本报告完成其未覆盖的**运维样板维度**：基于真实会话数据把高频 AI 样板沉淀为 ops-runbook Skill 与工具脚本，并交付全量复盘证据。互补不重叠；S3/S2 设计时可直接吸收本批 dev_env.sh / register_doc.py。

## 1. 会话档案分析结论（任务 1/2/4 的数据基础）

35 个主会话（2026-07~09）两类子代理扫描合并：

**用户高频 Prompt（≥3 次）**：①一次性执行编号计划（~25 次）②生成提示词给其他 AI（~16）③multi-review/multi-verify 交叉核查（~9，已技能化 ✓）④发布/升级到服务器（~16）⑤个案工号/患者修复（~14）⑥新系统只读探索登记（~6）⑦按口径取数（已由 query-governance-intake 覆盖 ✓）。

**AI 侧重复样板（按频次）**：

| 样板 | 会话数 | 处置 |
|---|---|---|
| SSH+docker exec+python 连库 | 29 | → **ops-runbook Skill §1**（连接标准件+norm 序列化） |
| 隧道 15432+测试库 URL 推导 | 23 | → **tools/dev_env.sh**（source 即用） |
| 备份→改→回读→审计四步 | 17 | → ops-runbook §2（含 pg_dump 方言坑） |
| 文档四步登记 | 13 | → **tools/register_doc.py**（半自动模板） |
| 他人域哈希基线 | 8 | → dev_env.sh --domain-baseline/check |
| 夜跑核验 | 7 | → ops-runbook §4（SQL 模板） |
| 镜像发布链 | 5 | → ops-runbook §3（九步 runbook） |

## 2. 任务 1 产出：ops-runbook Skill

`.agents/skills/ops-runbook/SKILL.md`——生产运维四件套标准件：§1 容器内连库探查（含 LOB/bytes norm 标准件、heredoc 编码禁令）；§2 业务库受控写修复五步（侦察→预检→备份→单事务对账→回读审计）；§3 生产镜像发布九步（与 179/180 详细版互指）；§4 夜跑只读核验模板；§5 已踩坑速查表。已登记 AGENTS 技能路由（发布/夜跑/容器连库/受控修复触发）。

## 3. 任务 2 产出：三个工具脚本

| 工具 | 功能 | 验证 |
|---|---|---|
| `tools/dev_env.sh` | `source` 即建隧道（复用不杀）+推导 APP_TEST_DB_URL（口令不回显）；`--domain-baseline/check` 管他人域 13 文件基线 | 基线记录+两次校验通过 ✔ |
| `tools/register_doc.py` | 查最大/下一编号；按文档名生成四步登记模板；`--check` 目录自检（孤儿/幽灵） | 本报告自身用其登记 ✔ |
| `tools/export_zcode_conversations.py` | 会话库导出（本次复盘的数据源） | 35 会话/11,531 条/11MB ✔ |

cron 层不新增（identity 02:00、docare 错配 00:10、D1 automation 已覆盖；「值域导出 7 天时效提醒」列建议不实施）。

## 4. 任务 3 结论：指令与长期记忆查重

- 项目根 `CLAUDE.md`＝转发入口（单一权威 AGENTS.md，防双源漂移）✔ 无重复正文；
- 全局 `commands/multi-review.md`＝薄转发到 Skill ✔；
- 全局 skills（multi-review/multi-verify/sjzc/wechat）与仓库 skills 分工清晰；
- AGENTS 本体：无过期快照类内容；本次仅新增 ops-runbook 路由一行；183 号会话已完成的指令收敛（查询治理拆分、技能收窄、显式触发）不重复。
- **修正**：AGENTS「验证命令」节与 conftest 纯逻辑子目录清单一致性已核（`alembic_env` 已收录，178 自补）✔。

## 5. 任务 4：中断点清单（Agent 卡点与预防，均已映射处置）

| 类别 | 卡点（会话证据） | 预防/处置 |
|---|---|---|
| 环境 | pnpm EPERM/僵尸 node（003）；15432 占用；backend/.env 指旧库 | dev_env.sh 复用转发；显式 URL 规则保留 |
| 数据工具 | pg_dump 方言 URL+role root（017/023）；bytes/LOB 序列化（017 等 4 会话） | ops-runbook §2/§1 标准件 |
| 编码 | heredoc 中文 GBK 损坏（031） | Skill 禁令：本地文件+`<` 重定向 |
| 并行互踩 | 测试库 schema 被清/僵尸 pytest（006/019 等 6 会话）；他人域 hunk（031/017）；发布窗口竞态（180 先于守卫提交） | --domain 基线；全量前清点 pytest；改动尽早提交占位 |
| 上下文 | README 44k tokens 超限（3 会话）；工具输出 30-60KB 截断 | Read offset 分段；stdout 日志转读 |
| 流程语义 | F-2 熔断死锁（028，已修）；水位不推进；子任务一刀切 | F-2 已上产；夜跑核验模板入 Skill |
| 授权等待 | 选项卡未答（014）；DBA/厂商依赖 | 「未答=不越权记 SKIP」规则保留 |
| 行为偏差 | 评估请求被执行替代（024） | 规则保留：用户描述问题时先给评估 |

## 6. 任务 5：回归结果与规则去留裁决

回归矩阵（HEAD=2361e90 基线）：

| 门禁 | 结果 |
|---|---|
| 后端纯逻辑（alembic_env+identity_login_sign_sync+plan127，无库） | **44 passed / 0 failed** |
| 后端 DB 专项（test_r177_fixes+identity_cb_resync_f2+identity_cb_align） | 见 §7 补记 |
| 前端 typecheck（tsc+vue-tsc） | 双过 |
| 前端 vitest | **278/278** |
| 新工具实测 | dev_env.sh ✔ / register_doc.py ✔ / export ✔（§3） |

**规则裁决表**（依据失败案例）：

| 规则 | 裁决 | 证据 |
|---|---|---|
| 脚本一律显式 APP_TEST_DB_URL | **保留** | .env 旧库陷阱多会话反复踩 |
| 不杀他人 15432 转发 | **保留** | 164/171/177 会话惯例，防互踩 |
| 他人域零触碰+哈希终检 | **保留** | 171/177/178 三批验证防越界 |
| conftest 纯逻辑子目录豁免 | **保留** | alembic_env 收录验证无库能力 |
| pytest 后重灌 import170 | **保留** | 171 T2.5/177/183 会话惯例 |
| 「未答=不越权」 | **保留** | 014 会话 R5 SKIP 正确处置 |
| heredoc 传中文长脚本 | **废除→改文件传输** | 031 GBK 损坏事故 |
| 候选废除项 | 无 | 未发现被失败案例证伪的既有规则 |

## 7. 补记（收口回填）

| 项 | 结果 |
|---|---|
| 后端 DB 专项（r177+resync_f2+align） | **21 passed / 0 failed**（首跑曾现 4 errors，复跑归零——根因=与一个仍在收尾的同任务后台进程互踩隔离库，即 §5「并行 pytest 互踩」模式的当场复现，证据成立） |
| 前端 build | 主入口/图谱异步包/CSS gzip 预算三绿（typecheck、278/278 此前已过） |
| README/55 登记 | 随本批完成（register_doc.py 生成模板走查 ✔） |
| commit | 收尾批次两组：`feat(tools+skill)` 工具与 ops-runbook、`docs(184)` 本报告 |
| 后续执行计划 | 剩余项（183 号 S1-S6 执行 + 本批修复项）移交 **185 号执行计划** |
