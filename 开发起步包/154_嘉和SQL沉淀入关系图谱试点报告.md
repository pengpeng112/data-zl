> 类别：证据
>
> 状态：已完成（2026-08-27，T4 试点；multi-verify round-1 核查裁决后执行）
>
> 上位入口：`三仓库检查处理提示词-数据资产.md`（T4）；核查链 `verify/round-1/`（核查报告 P1/P2/P5/P8 修正已全部吸收）
>
> 关联文档：`.agents/skills/sql-relation-intake/SKILL.md`（流程与分级口径）、`77_JHEMR海量数据库只读探查与资产导入报告.md` + `数据资产_JHEMR_Vastbase资产包/`（查重权威集）、`152_JHEMR病案首页回写HIS链路梳理_结果.json`（E6 去重对象）、嘉和 `sql\02-嘉和通用接口.md` / `sql\04-上报工具.md`（只读证据源）

# 嘉和 SQL 沉淀入关系图谱试点报告（154）

## 1. 试点设计与核查修正

- **选册**：主册 `02-嘉和通用接口.md`（EMR jhemr 库，视图族+写链证据最丰富），辅以 `04-上报工具.md` 对照。原任务包建议 04 为唯一主册，核查（grok）指出其 JOIN 证据弱且含 UPDATE；裁决（D10）换 02 为主、04 为辅。
- **查重集修正**（核查 P1，三方一致）：任务包原指 `数据资产_资产包\relationships.csv`（ODS/HIS 体系，104 行）——**指错**。按分册目标库绑定：02/04 → `数据资产_JHEMR_Vastbase资产包\relationships.csv`（283 条正式关系，全部 view_dependency 型）；03 分册（若后续批次）→ `数据资产_HIS源端资产包`。
- **分级口径**（核查 P5）：按技能五级 existing / candidate / partial / recipe_candidate / rejected 交付，附资产包置信字母（B/C/D）注记。
- **基数修正**（核查 P2）：`sql\README.md` 自述"151 条"为中间筛选口径（中间产物易失、不可复算）；四分册在库编号语句合计 **35 条**（13+8+7+7），分批方案以此为基数。
- **红线遵守**：嘉和仓库全程只读；活库仅限量 SELECT/pg_views 元数据（sjzc live 通道，152 号先例）；**relationships.csv / catalog.json / 平台资产零写入**。

## 2. 试点输入（≤20 条）

完整可执行语句 13 条（02 册 #1b/2/4、#5×4 视图族、#6×4 视图族；04 册 #5/6/7）+ 活库新提取视图 DDL 4 份（view_zdhc 419 字符 / v_cdauploadbc 293 / v_hlht 624 / **v_jinchuangbl 9,990**——P0 悬案关键视图首次完整落档）。02 册 #7/#8 标注 PH 片段级，按任务规则跳过摄入、仅作写链证据引用。

## 3. 流程执行结果

1. **确定性解析**（extract_sql_relations.py，system_code=JHEMR_VASTBASE）：19 表、8 条候选（全部 PAT_VISIT→JHMR_FILE_INDEX，ON 条件被 `::text` 转换包裹未自动提取——技能文档明示此类需人工补全）；SHA-256 留档于 parse.json。
2. **元数据核验**（sjzc live information_schema，2026-08-27）：18 个目标对象 **17 存在 / 1 缺失**——`jhemr.interface_log` 在 177 只读副本全库不存在（02 册 #1 审计 INSERT/查询的目标表；疑仅存在于 179 本机主库或未建，标 `pending-verify` 待 179 侧核实）。另 `v_hisicu1` 不存在（与 152 E1 结论一致，该取数语句判 rejected）。
3. **人工补充关联**（DDL 精读）：v_jinchuangbl 三表 JOIN 键、v_hlht 双键逗号连接、v_cdauploadbc 反连接——基表 JOIN 键级事实为包内零覆盖的新增价值。
4. **正式关系查重**（对 283 条 view_dependency）：
   - `pat_diagnosis→view_zdhc`、`pat_visit/jhmr_file_index/jhmr_templet_class→v_jinchuangbl`、`pat_visit/pat_master_index→v_hlht`、`v_cdaupload/dc_cda_uploadrecord→v_cdauploadbc` **共 8 条全部已存在**（77 号 pg_views 静态证据）。
   - 基表↔基表 JOIN 边：包内 **0 条**（该包关系模型为 view_dependency 单型）。
5. **与 152 E6 去重**：E6 八条候选（7 条 D 级 + 1 条 existing，jhemr 回写视图→HIS MEDREC 跨系统写血缘）与本试点（JHEMR 库内部）**零重叠**；本试点全部落在 152 未覆盖的 JHEMR 内部对象。

## 4. 候选清单与分级

| # | 关系/配方 | 键 | 证据 | 分级（技能五级+置信） |
|---|---|---|---|---|
| 1-8 | 8 条视图依赖（见 §3.4 清单） | 取数键 patientid/visitid | 02 册 #4/5/6（JHGetData.xml 程序出处）+ 活库 DDL 2026-08-27 | **existing**（新证据补充：从"pg_views 静态"升级为"静态+程序消费方出处"） |
| 9 | `pat_visit ⋈ jhmr_file_index` | patient_id + visit_id（::text 包裹） | v_jinchuangbl 多分支 UNION JOIN（DDL 实录） | **recipe_candidate**（B）——包内零覆盖的 JOIN 键配方 |
| 10 | `jhmr_file_index ⋈ jhmr_templet_class` | mr_class = mr_class_code | v_jinchuangbl LEFT JOIN（DDL 实录） | **recipe_candidate**（B） |
| 11 | `pat_visit ⋈ pat_master_index` | patient_id + inp_no（双键） | v_hlht 逗号连接（DDL 实录） | **recipe_candidate**（B）——注意与 HIS 侧同名表关系的口径区分（JHEMR 副本内部） |
| 12 | 工具→`dc_cda_uploadtask` 写链 | patient_id+visit_id | 02 册 #3 INSERT（JHWebInterface InsertTask，院码+PROJECTCODE）+ 04 册 #2/3 UPDATE 状态机（FileDataUpload） | **candidate**（D，单系统写血缘；消费键=UPLOADSTATE/PROJECTCODE） |
| 13 | `App_Code→pat_visit` 字段回写 | patient_id+visit_id | 02 册 #7 片段级 UPDATE（mr_doctor_part_status/mr_nurse_part_status=3、is_read、medicalinsurpersonalno 读取） | **candidate**（D，写血缘；与 #12 同为接口侧回写，可合并为一条配方） |
| 14 | `interface_log` 摄入 | — | 02 册 #1 | **rejected**（元数据未过：177 副本无此表；pending-verify 179 侧） |
| 15 | `v_hisicu1` 取数 | — | 02 册 #5 族 | **rejected**（活库无该对象；152 E1 已证） |

分级分布：existing 8 / recipe_candidate 3 / candidate(D) 2 / rejected 2。

## 5. 全量沉淀分批方案建议（基数=在库 35 条编号语句）

| 批 | 分册 | 目标库/system_code | 查重权威集 | 预估产出 | 备注 |
|---|---|---|---|---|---|
| B1（本试点，已完成） | 02+04 | EMR jhemr / JHEMR_VASTBASE | JHEMR 资产包 283 条 + 152 E6 | existing 8 + 配方 3 + 写链 2 | 管线已验证 |
| B2 | 03-老HIS-satrda | HIS Oracle 正式 / HIS_SOURCE | **数据资产_HIS源端资产包**（33 条）+ 152 E6 八条 | 以 existing/证据补充为主（与 152 的 22 条 PBD SQL 同源，重叠度最高）；132 表地图为表级使用证据非关系 | 含 DML——仅解析为写链证据，**禁活库执行** |
| B3 | 01-无纸化CDMS | CDMS Oracle 10.10.10.93 / PAPERLESS_CDMS | 平台元数据 + 79 号无纸化证据 | 候选以 CDMS 内部为主；唯一 JOIN（t_mss_printqueuedetail）在此册 | 跨系统（CDMS↔EMR）按 40 号 D 级延后口径 |

节奏建议：每批 ≤20 条、产出候选清单 JSON 交人工审核（asset_relation_reviews 草稿由主 AI 走平台流程），**审核通过前不动 relationships.csv / catalog.json**；B2 前先与用户确认 152 E6 草稿的复核进度避免双头。

## 6. 附带修复与移交

- `.agents/skills/sql-relation-intake/SKILL.md` 资产路由已补一行（按分册目标库选包，防默认 ODS 包误查重——核查 P9/grok 发现的缺口）；`extract_sql_relations.py` 默认查重路径与方言参数化改造**留作建议**，不在本次动代码。
- `v_jinchuangbl` 完整 DDL（9,990 字符）已落本报告配套结果 JSON，嘉和侧 docs/21 的 P0 悬案（占位符丢失）后续可直接引用该口径。

## 7. 自检声明

实际打开核实：嘉和 `sql\README.md`、`02/04` 两分册全文；JHEMR 资产包 relationships.csv（查重 6 组 grep + relation_type 全量统计）；`verify/round-1/` 核查包与本轮全部裁决文件；152 结果 JSON E6 段。仅推断未直接核实：`interface_log` 在 179 主库的存在性（无直连权限，标 pending-verify）；"151 条"中间产物（文档自注易失）。
