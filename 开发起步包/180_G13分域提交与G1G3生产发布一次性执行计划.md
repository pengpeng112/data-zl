> 类别：待办
>
> 状态：**v1.0 定稿待执行（2026-09-05，交一次性执行 AI「5.3 flash」；授权范围由用户在提示词中点名）**
>
> 上位入口：`55_系统未完成事项统一执行计划.md`；执行细则承接 `179_G13分域提交与G1G3生产发布runbook.md`（A 段分组以本文为准：**不拆 hunk**，整文件归组+提交信息注明混域，即 179 §2.2 预留备选路线）
>
> 制定日期：2026-09-05；基线 = 178 收口态（HEAD=96ed027，工作区含 177/178 未提交改动）

# 180 · G13 分域提交与 G1/G3 生产发布一次性执行计划

本文给一次性执行 AI（5.3 flash）单会话执行。**写给弱模型的约定：全文步骤机械执行，禁止自创步骤；每步有命令、预期输出、失败动作；任何与本文不符的事实 = STOP 写 exceptions，不要猜。**

## 0. 授权门槛（执行 AI 先读）

本文覆盖四段，用户会在提示词【授权范围】里点名（A / A+B / A+B+C / A+B+C+D 之类）。**未点名的段一律不做；【授权范围】为空 = 立即停止。**

| 段 | 内容 | 风险 |
|---|---|---|
| A | G13 分域提交（9 组本地 commit，**默认禁止 push**） | 低（本地可逆） |
| B | G1+G3 后端发布：镜像 r178-20260905 重建容器 + FFREE3 持久化核验 + MAX_NEW 130→50 | 高（生产容器切换） |
| C | 前端 r178 原子切换（previous 保留） | 中（秒级可回滚） |
| D | 次日夜跑只读验证 + （预授权时）MAX_UPDATE 150→100 | 低（只读为主） |

## 1. 硬边界（违反任一 = 立即停止并回滚已做的生产动作）

1. **零 push / 零 checkout / 零 stash / 零 reset**，除非【授权范围】点名 push。
2. **他人域 13 文件绝不 `git add`**：`frontend/src/layout/**`（5 个 M）、`frontend/src/store/modules/app.ts`、`frontend/src/layout/captureMode.ts`、`frontend/tests/competitionCaptureMode.test.ts`、`tools/capture_*.py`（5 个 ??）。
3. **`git add` 一律用显式文件清单**；仅 §3 标注"可目录 add"的 4 个目录例外，且 add 前必须先跑该目录的 `git status --short` 确认无杂物。
4. 生产红线：**不改 cron**（只许 `crontab -l` 读）；**不改 Dockerfile**；**不删镜像/容器**；env 修改前必须先 `cp` 备份；业务源库（HIS/ODS/CDMS/JHEMR 等 Oracle）**零连接零写入**。
5. 每组 commit 后必须 `git show --stat` 核对文件清单零越界；出现禁入文件 = STOP。
6. 容器重建后 health 失败 = 立即按 §6 回滚到 r175 镜像，然后 STOP。
7. 凭据零落盘：SSH 只用公钥；不回显任何口令/Token；`backend.env` 内容不打印（只允许 grep 变量名）。
8. 值域不自动 confirm；探查发现不代点终态；150 课题/004066 不夹带任何处理。

## 2. R0 开工自检（必做，只读）

**读**：AGENTS.md → 本文件全文 → `开发起步包/179_...runbook.md`（背景）→ `178_..._执行报告.md` §0/§4/§7。

**命令（本地）**：

```bash
cd F:/python/数据资产
git rev-parse HEAD            # 预期 96ed0270ad854d32008ba6c5b792d1de87112714
git status --short            # 预期 = §3.A0 盘点表，多任何未知文件 = STOP
git stash list                # 预期空
git log -3 --oneline          # 预期首行 96ed027
```

**产出**：`开发起步包/output_r180/workspace_snapshot.txt`（上述四条命令输出 + `date`）。
**STOP**：HEAD 不是 96ed027；工作区出现盘点外的新脏文件；`frontend/src/views/asset/components/GraphToolbar.vue` 等本批文件不在 M 列表（说明 178 改动丢失）。

## 3. A 段 · G13 分域提交（9 组，按序；每组 = add → commit → 验证）

### A0 盘点（确定式核对，不做数量估算）

**核对 1（缺一即 STOP）**：A2–A9 各组 add 清单里的**每个文件**都必须出现在 `git status --short`（M 或 ??）。某个清单文件不脏 = 工作区被他人动过，STOP。

**核对 2（多一即 STOP）**：`git status --short` 全量减去 A2–A9 清单后，剩余必须**恰好**是：

- 他人域 13 文件：M=`frontend/src/layout/components/lay-content/index.vue`、`lay-setting/index.vue`、`layout/hooks/useDataThemeChange.ts`、`layout/hooks/useTag.ts`、`layout/index.vue`、`store/modules/app.ts`；??=`frontend/src/layout/captureMode.ts`、`frontend/tests/competitionCaptureMode.test.ts`、`tools/capture_ai_patrol_gif.py`、`tools/capture_competition_ai_demos.py`、`tools/capture_competition_screens.py`、`tools/capture_four_governance_screens.py`、`tools/capture_governance_gifs.py`
- 工作目录（不入库）：??=`backend/_r172_work/`、`backend/_r175_work/`、`review/round-7/`、`review/round-8/`、`verify/round-3/`、`frontend/审查_grok.err`、`frontend/审查_grok.raw`、`tools/s9_e2e_r174.py`、`开发起步包/output_r180/`（R0 自建）

出现任何其它条目 = STOP，写 exceptions.json 说明差异。

### A1 `chore(hygiene): ignore one-shot work dirs`

1. 编辑 `.gitignore`：文件末尾追加两行（无空行夹入）：
   ```
   backend/_r172_work/
   backend/_r175_work/
   ```
2. `git add .gitignore && git commit -m "chore(hygiene): ignore one-shot work dirs"`
3. 验证：`git show --stat HEAD` 仅 `.gitignore`。

### A2 `feat(174): quality governance ledger backend`

```bash
git add backend/app/api/v1/permissions.py backend/app/main.py \
  backend/alembic/versions/d5e6f7a8b9c0_add_quality_governance_ledger_174.py \
  backend/app/api/v1/quality_controls.py backend/app/api/v1/quality_issues.py \
  backend/app/api/v1/quality_observations.py backend/app/models/quality_governance.py \
  backend/app/services/quality_governance_service.py backend/app/services/quality_governance_adapters.py \
  backend/app/scripts/__init__.py backend/app/scripts/seed_quality_governance.py \
  backend/tests/test_quality_governance_api.py backend/tests/test_quality_governance_service.py \
  backend/tests/security_audit/test_fine_grained_write_permissions.py
git commit -m "feat(174): quality governance ledger backend (5 tables, 3 API prefixes, seed)"
```

验证：`git show --stat HEAD` = 14 个文件，全部在清单内。**迁移文件必须在本组**（与 main.py 路由注册同提交点）。

### A3 `feat(175): governance import scripts`

```bash
git add backend/scripts/import_plan175_governance.py backend/scripts/import_ecg_relation_reviews.py
git commit -m "feat(175): lis/us/docare governance + ecg review import scripts"
```

### A4 `fix(177): catalog readback, recipe validation, edge labels, v-perms`

```bash
git add backend/app/services/asset_catalog.py backend/app/api/v1/recipes.py \
  backend/app/services/recipe_service.py \
  frontend/src/views/asset/components/RelationGraph.vue \
  frontend/src/views/asset/value-domains/index.vue frontend/src/views/dict/general/index.vue \
  backend/tests/test_r177_fixes.py frontend/tests/plan166StageD2.test.ts \
  frontend/tests/plan177R2.test.ts
git commit -m "fix(177): catalog readback upper-key 404, recipe create 422, edge label [object Object], v-perms (probe-findings v-perms line rides with quality frontend commit)"
```

注：`probe-findings/index.vue` 的 177 v-perms 行不拆 hunk，随 A8 整文件入库（提交信息已注明）。

### A5 `feat(identity): FFREE3, login/sign subtask, F-2 resync semantics`

```bash
git status --short backend/tests/identity_login_sign_sync/   # 预期只有 .py 文件；出现 __pycache__ 属正常（已被全局 ignore 规则排除），若仍列出则 STOP
git add backend/app/services/cdms_identity_adapter.py backend/app/services/jhemr_identity_adapter.py \
  backend/app/services/identity_sync_audit.py backend/app/services/identity_sync_log.py \
  backend/app/services/identity_sync_status.py backend/app/services/identity_sync_orchestrator.py \
  backend/app/services/identity_login_sign_sync.py \
  backend/scripts/run_identity_modified_nightly.py \
  backend/tests/conftest.py backend/tests/test_identity_sync.py \
  backend/tests/test_identity_sync_122_unit.py backend/tests/test_identity_cb_resync_f2.py \
  backend/tests/identity_login_sign_sync/
git commit -m "feat(identity): CDMS FFREE3, JHEMR login/sign subtask, F-2 resync_unchanged circuit breaker semantics (conftest.py also registers alembic_env dir for the following commit)"
```

注：`conftest.py` 含 178 的 `"alembic_env"` 注册行，随本组整文件入库（提交信息已注明，A6 的测试目录随后到达）。

### A6 `fix(alembic): create asset schema if not exists`

```bash
git status --short backend/tests/alembic_env/   # 预期只有 conftest.py / test_schema_ddl.py
git add backend/alembic/env.py backend/tests/alembic_env/
git commit -m "fix(alembic): ensure asset schema exists before online/offline migrations"
```

### A7 `fix(178): GraphToolbar one-way data flow`

```bash
git add frontend/src/views/asset/components/GraphToolbar.vue frontend/src/views/asset/graph/index.vue \
  frontend/tests/plan178R2.test.ts
git commit -m "fix(178): GraphToolbar stops mutating props (update:locate/update:filters + Object.assign in parent)"
```

### A8 `feat(quality): 174 ledger frontend + 178 probe cross links`

```bash
git status --short frontend/src/views/quality/   # 预期：issue-detail/、observations/、issues/、controls/、sourceRef.ts 等 .vue/.ts，无其他杂物
git add frontend/src/api/quality.ts frontend/src/router/modules/quality.ts frontend/src/router/modules/asset.ts \
  frontend/src/views/quality/ frontend/src/views/asset/probe-findings/index.vue \
  frontend/tests/plan174QualityLedger.test.ts frontend/tests/plan178R4.test.ts
git commit -m "feat(quality): 174 ledger frontend (issues/controls/observations/issue-detail) + 178 probe-finding cross links (includes 177 C3 v-perms line in probe-findings)"
```

### A9 `docs(173-178): reports, plans, output evidence dirs`

```bash
git status --short 开发起步包/ | head -50   # 预期全是 173–178 报告/计划/output 目录；不得出现 ~$ 开头锁文件（.gitignore 已覆盖 ~$*）
git add 开发起步包/
git commit -m "docs(173-178): full-stack test findings, CDMS/174/175/176/177/178 reports, plans and output evidence dirs"
```

### A 段收口验证（必做）

1. `git status --short` 残留**必须恰好**为：他人域 13 文件（6 M + 7 ??）+ `backend/_r173_work` 等已被 ignore（不显示）+ `review/round-7/`、`review/round-8/`、`verify/round-3/`、`frontend/审查_grok.err`、`frontend/审查_grok.raw`、`tools/s9_e2e_r174.py`、`开发起步包/output_r180/`。多/少都 = STOP 对照排查。
2. `git log --oneline -9` = A1–A9 九条。
3. 快速冒烟（不连库，~1 分钟）：
   ```bash
   cd backend && ./.venv/Scripts/python.exe -m pytest tests/alembic_env -q        # 预期 4 passed
   cd ../frontend && pnpm exec vitest run tests/plan178R2.test.ts tests/plan178R4.test.ts tests/plan177R2.test.ts   # 预期 16 passed
   ```
4. 残留快照追加到 `output_r180/workspace_snapshot.txt`。

## 4. B 段 · G1+G3 后端发布

### B0 只读侦察（服务器，全部只读；任何一项查不到 = STOP）

```bash
SSH="ssh -o BatchMode=yes -i C:/Users/Administrator/.ssh/id_ed25519_ai root@10.10.8.83"
$SSH "docker ps --format '{{.Names}} {{.Image}} {{.Status}}' | grep data-asset"          # 预期 data-asset-api ... r175-20260903 healthy
$SSH "docker images --format '{{.Repository}}:{{.Tag}}' | grep data-asset | sort"        # 必须含 data-asset:r175-20260903（构建基底+回滚点）
$SSH "find /etc/data-asset /opt/data-asset -maxdepth 4 -name 'run_data_asset_api.sh' 2>/dev/null"   # 记录 run 脚本绝对路径
$SSH "ls /opt/data-asset/hotpatch/ 2>/dev/null | tail -5; df -h / | tail -1"             # 热修目录先例 + 磁盘（<2G = STOP）
$SSH "crontab -l | grep -i -E 'identity|nightly'"          # 只读记录夜跑 cron 怎么调脚本（容器内路径 or 宿主路径）——禁止编辑
$SSH "readlink /opt/data-asset/frontend-dist/current; ls /opt/data-asset/frontend-dist/releases | tail -3"   # C 段用
$SSH "ls /opt/data-asset/*.dump /root/*.dump 2>/dev/null | tail -3"                      # 备份目录先例
```

### B1 代码打包上载

```bash
cd F:/python/数据资产
mkdir -p /tmp/r178pkg && tar czf /tmp/r178pkg/code.tar.gz backend/app backend/alembic backend/alembic.ini backend/scripts deploy/docker/Dockerfile.hotpatch
scp -o BatchMode=yes -i C:/Users/Administrator/.ssh/id_ed25519_ai /tmp/r178pkg/code.tar.gz root@10.10.8.83:/opt/data-asset/hotpatch/r178-20260905.tar.gz
$SSH "mkdir -p /opt/data-asset/hotpatch/r178-20260905 && tar xzf /opt/data-asset/hotpatch/r178-20260905.tar.gz -C /opt/data-asset/hotpatch/r178-20260905 && ls /opt/data-asset/hotpatch/r178-20260905"
# 预期列出：backend  deploy
```

### B2 生产库备份（无迁移，保险）

```bash
$SSH 'set -e; U=$(grep -oP "(?<=APP_DB_URL=postgresql\+psycopg://)[^:]+" /etc/data-asset/backend.env); D=$(grep -oP "data_asset[^?]*" /etc/data-asset/backend.env | head -1); pg_dump -h 127.0.0.1 -U "$U" "$D" > /opt/data-asset/data_asset_pre_r178_$(date +%Y%m%d).dump && ls -la /opt/data-asset/data_asset_pre_r178_*.dump | tail -1'
```

预期：dump 文件 >5MB。失败（口令墙）= **STOP B 段**（A 段成果不受影响）。注意该命令不回显口令。

### B3 镜像构建 + 镜像内冒烟（先不切容器）

```bash
$SSH "cd /opt/data-asset/hotpatch/r178-20260905/backend && docker build -f ../deploy/docker/Dockerfile.hotpatch --build-arg BASE_IMAGE=data-asset:r175-20260903 -t data-asset:r178-20260905 . 2>&1 | tail -3"
# 预期 Successfully tagged data-asset:r178-20260905
$SSH "docker run --rm --entrypoint grep data-asset:r178-20260905 -c FFREE3 /app/app/services/cdms_identity_adapter.py"                # 预期 ≥1
$SSH "docker run --rm --entrypoint grep data-asset:r178-20260905 -c resync_unchanged /app/app/services/identity_sync_orchestrator.py" # 预期 ≥1（F-2 在镜像）
$SSH "docker run --rm --entrypoint grep data-asset:r178-20260905 -c 'CREATE SCHEMA IF NOT EXISTS' /app/alembic/env.py"                 # 预期 ≥2（online+offline）
```

任一 grep 无输出（exit 1）= 镜像缺内容，STOP，不切容器。

### B4 切换容器 + 健康门禁

```bash
RUNSCRIPT=<B0 记录的绝对路径>
$SSH "DATA_ASSET_IMAGE=data-asset:r178-20260905 bash $RUNSCRIPT"        # 脚本会 stop/rm 旧容器并按既有挂载重建
sleep 20
$SSH "docker ps --format '{{.Names}} {{.Image}} {{.Status}}' | grep data-asset-api"   # 预期 r178-20260905 (healthy)
$SSH "curl -fsS http://127.0.0.1:8000/health"                            # 预期 JSON 200
$SSH "curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8000/api/v1/quality-issues; curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8000/api/v1/systems; curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8000/api/v1/quality-observations"   # 预期三行全是 401
$SSH "docker exec data-asset-api python -m alembic current"              # 预期 d5e6f7a8b9c0 (head)
$SSH "docker exec data-asset-api python -c 'from app.core.config import settings; print(settings.identity_cb_max_update, settings.identity_cb_max_new)'"   # 预期 150 130
```

health 失败 = 按 §6 回滚，STOP。

### B5 夜跑脚本投递（依 B0 cron 侦察结果）

- cron 若引用**容器内路径**（形如 `docker exec data-asset-api python /app/...run_identity_modified_nightly.py`）：
  `$SSH "docker cp /opt/data-asset/hotpatch/r178-20260905/backend/scripts/run_identity_modified_nightly.py data-asset-api:/app/scripts/run_identity_modified_nightly.py"`（容器内路径以 B0 侦察为准，cp 前先 `docker exec ... md5sum <目标>` 记录旧值）。
- cron 若引用**宿主路径**：同款 cp 到宿主目标（先 `.bak-r178` 备份）。
- 完整记录到 `output_r180/release_evidence.md`。禁止改 crontab 本身。

### B6 G3 · MAX_NEW 130→50

```bash
$SSH "cp /etc/data-asset/backend.env /etc/data-asset/backend.env.bak-maxnew-20260905 && sed -i 's/^APP_IDENTITY_CB_MAX_NEW=.*/APP_IDENTITY_CB_MAX_NEW=50/' /etc/data-asset/backend.env && grep -c '^APP_IDENTITY_CB_MAX_NEW=50' /etc/data-asset/backend.env"   # 预期 1
$SSH "DATA_ASSET_IMAGE=data-asset:r178-20260905 bash $RUNSCRIPT"; sleep 20
$SSH "docker exec data-asset-api python -c 'from app.core.config import settings; print(settings.identity_cb_max_update, settings.identity_cb_max_new)'"   # 预期 150 50
```

**MAX_UPDATE 150→100 不在本段**——必须等 D 段夜跑 PASS 且用户预授权（D2）。

## 5. C 段 · 前端 r178 原子切换

```bash
cd F:/python/数据资产/frontend && pnpm run build 2>&1 | tail -6        # 预期 gzip 三预算 PASS
grep -oE 'index-[A-Za-z0-9_-]+\.js' dist/index.html | head -1          # 记录入口 hash
tar czf /tmp/r178pkg/dist.tar.gz -C dist .
scp -o BatchMode=yes -i C:/Users/Administrator/.ssh/id_ed25519_ai /tmp/r178pkg/dist.tar.gz root@10.10.8.83:/opt/data-asset/frontend-dist/r178-dist.tar.gz
$SSH "mkdir -p /opt/data-asset/frontend-dist/tmp-r178 && tar xzf /opt/data-asset/frontend-dist/r178-dist.tar.gz -C /opt/data-asset/frontend-dist/tmp-r178"
$SSH "bash <B0 找到的 release_frontend.sh 或 /opt/data-asset/deploy/scripts/release_frontend.sh> r178-20260905 /opt/data-asset/frontend-dist/tmp-r178"   # 脚本自带 ASSETS 校验+原子切 current+保留 previous
$SSH "nginx -t && nginx -s reload"                                      # 若 nginx 非 host 进程，按 B0 侦察结果等价执行
$SSH "curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1/; curl -s http://127.0.0.1/ | grep -oE 'index-[A-Za-z0-9_-]+\.js' | head -1"
```

预期：首页 200；入口 hash 与本地记录**逐字相同**。脚本 ASSETS 校验失败 = 按 §6 回滚前端。

## 6. 回滚链（任何 B/C 失败时执行，然后 STOP）

- 后端：`$SSH "DATA_ASSET_IMAGE=data-asset:r175-20260903 bash $RUNSCRIPT"` + health 验证；
- env：`cp /etc/data-asset/backend.env.bak-maxnew-20260905 /etc/data-asset/backend.env` + 重建 + 回读 130；
- 前端：`bash .../rollback_frontend.sh`（或 `ln -sfn releases/r175-20260903 /opt/data-asset/frontend-dist/current`）+ `nginx -s reload`；
- DB：本批无迁移；极端情况用 B2 dump。

## 7. D 段 · 次日夜跑验证（02:00 后；可另会话执行）

只读（与 178 R1 同款，经容器 SELECT）：

```bash
$SSH "docker exec data-asset-api python -" <<'PY'
import os
from sqlalchemy import create_engine, text
c = create_engine(os.environ["APP_DB_URL"]).connect()
r = c.execute(text("select run_id,status,circuit_breaker_triggered,circuit_breaker_dimension,started_at from asset.asset_identity_scheduler_runs order by started_at desc limit 1")).fetchone()
print(r)
print(c.execute(text("select subtask_code,status from asset.asset_identity_sync_subtasks where run_id=:x order by id"), {"x": r[0]}).fetchall())
PY
```

- **PASS 判定**：最新行 started_at 为当日 02:00、status=success、CB=false；子任务 signature/user_dept/education_title 状态≠skipped（若 G2 已随 B5 生效，还应出现 login_sign 行）；report_summary 出现 `resync_unchanged` 维度 = F-2 在产实证。
- PASS 且【授权范围】含 D2：执行 MAX_UPDATE 150→100（`cp backend.env backend.env.bak-cbmaxupd2-<date>` → sed → 重建 → 回读 100 50）。
- FAIL：run_id/维度/计数写入 `output_r180/nightly_d.md`，STOP，**不改任何阈值**。

## 8. E 段 · 收口文档（所有授权段完成后一次写）

- `开发起步包/output_r180/`：progress.md（每段命令摘要+结果）、exceptions.json（编号 W-xx）、release_evidence.md（B0–C 段关键回读输出，脱敏）、nightly_d.md（若做 D）。
- 新建 `开发起步包/180_G13提交与G1G3发布_执行报告.md` + 同号 `_结果.json`。
- README：目录更新记录一行 + 180 行状态改执行结果；55：追加一条 📌。**只在此处写一次。**

## 9. DoD（宣称完成必须全部成立）

1. A 段：9 组 commit 全部存在、`git show --stat` 零越界、残留 status 恰为 §3.A 收口清单、alembic_env 4 passed + 前端 16 passed。
2. B 段（若授权）：镜像 r178 在产、FFREE3/resync_unchanged/alembic DDL 三冒烟 ≥1、health 200、三 401、alembic current 不变、settings 终值 150/50、DB 备份存在。
3. C 段（若授权）：previous=r175 保留、首页 200、入口 hash 与本地一致。
4. D 段（若执行）：夜跑结论 PASS/FAIL 如实记录；D2 仅在 PASS+预授权时执行。
5. 零 push（除非点名）；他人域 13 文件未入任何 commit；业务源零连接；cron/Dockerfile 未改；全部证据落 output_r180/。
6. 不得宣称完成：push（没做就是没做）、G2 cron 接线（只投递脚本不接 cron）、004066/150。

## 10. 执行提示词（复制给 5.3 flash，勿增删；【授权范围】由用户填写）

```text
【角色】你是仓库 F:\python\数据资产 的一次性执行 AI（5.3 flash）。唯一执行依据是 开发起步包/180_G13分域提交与G1G3生产发布一次性执行计划.md（v1.0）。机械执行其 §2–§8 批次，禁止自创步骤；任何与计划不符的事实=STOP 并写 output_r180/exceptions.json，不要猜。

【授权范围】<用户填写：A / A+B / A+B+C / A+B+C+D（含D2则写 A+B+C+D2）；是否允许 push 单独点名，未点名=禁止 push>
【授权范围】为空或缺失时：立即停止，什么都不做。

【开工前必读】AGENTS.md；180 全文（§1 硬边界与 §6 回滚链是保命条款）；179 runbook（背景）；178 执行报告 §0/§4/§7。

【铁律】零 push/零 checkout/零 stash/零 reset（除非授权点名 push）；他人域 13 文件绝不 git add；git add 用显式清单（仅 180 标注的 4 个目录可目录 add 且先查 status）；生产：不改 cron、不改 Dockerfile、不删镜像容器、env 先备份再改、业务源库零连接；容器 health 失败=先按 §6 回滚再 STOP；凭据零落盘、backend.env 内容不打印；值域不 confirm、探查发现不代点终态；150/004066 零夹带。

【批次】R0 快照 → A 段 9 组提交（每组 add→commit→git show --stat 验证；A 收口残留清单核对 + alembic_env/前端三文件冒烟）→ B0 只读侦察（缺任一项=STOP）→ B1 上载 → B2 DB 备份（失败=STOP B 段）→ B3 镜像构建+三 grep 冒烟（缺内容=STOP 不切容器）→ B4 切容器+六项回读 → B5 按 cron 侦察投递夜跑脚本（docker cp，不改 crontab）→ B6 MAX_NEW 130→50（备份→sed→重建→回读 150/50）→ C 段前端构建/上载/release_frontend.sh r178-20260905/nginx reload/hash 回读 → E 段收口文档。D 段（若授权）次日 02:00 后另会话只读验证，PASS 且预授权才做 MAX_UPDATE→100。

【环境】仓库 F:\python\数据资产；SSH 公钥 root@10.10.8.83（-i C:\Users\Administrator\.ssh\id_ed25519_ai -o BatchMode=yes）；生产镜像基底/回滚点 data-asset:r175-20260903；新镜像 data-asset:r178-20260905；run 脚本以 B0 侦察为准；前端根 /opt/data-asset/frontend-dist（current/previous/releases）；前端必须 pnpm；本批零代码改动（不许改任何应用源码，唯一允许的文件编辑=A1 的 .gitignore 两行）。

【产出】output_r180/（workspace_snapshot.txt、progress.md、exceptions.json、release_evidence.md、nightly_d.md 若做 D）；开发起步包/180_G13提交与G1G3发布_执行报告.md + 同号 _结果.json；README 目录更新记录一行 + 180 行状态更新；55 追加一条 📌（只在 E 段写一次）。宣称完成必须满足 180 §9 DoD。

【STOP 白名单】SSH 不通；HEAD≠96ed027 或工作区与 §3.A0 盘点不符；r175 基镜像/run 脚本找不到；DB 备份失败；镜像冒烟 grep=0；重建后 health 失败（回滚后停）；前端 ASSETS 校验失败（回滚后停）；git show 出现禁入文件；cron 调用方式无法判定。其它失败记 WARN 继续。
```

## 11. 给用户的使用说明（本段不是执行 AI 的待办）

1. 把 §10 整段复制给 5.3 flash，**填好两处【授权范围】**（删掉重复行保留一行）。最小授权=`A`（只提交，最安全）；推荐=`A+B+C`；`D` 段次日再说。
2. push 想让它做必须点名（例：`A+push`），否则它必须零 push；push 走 171 先例（ssh.github.com:443 + deploy_key）。
3. B 段会**切换生产容器**（约 30–60 秒不可用，选低峰执行）；失败自动进 §6 回滚链。
4. 执行完读它的 `180_..._执行报告.md`；D 段 PASS 后再授权 MAX_UPDATE 150→100（一句话即可）。

## 12. 执行日志

| 时间 | 操作 | 结果 |
|---|---|---|
| 2026-09-05 | 依据 179 runbook + deploy/ 工装事实（run_data_asset_api.sh / release_frontend.sh / Dockerfile.hotpatch / 164 热修链先例），起草 180 执行计划 | 仅文档；零 Git/零生产/零业务源；A 段采用 179 §2.2 备选路线（不拆 hunk），并把 sync_local_to_server.py 排除（口令通道，违反凭据零落盘）改用公钥 tar+scp |
