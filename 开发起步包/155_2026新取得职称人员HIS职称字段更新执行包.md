> 类别：证据链（HIS 职称字段更新执行包，敏感明细仅存本地 gitignored 目录）
>
> 状态：执行包已就绪（2026-08-27），待信息科/DBA 执行（平台 AI 通道只读，不执行 DML）
>
> 上位入口：`55_系统未完成事项统一执行计划.md`
>
> 关联文档：`124_HIS职称同步JHEMR执行与复用说明.md`（LEVLCODE→EmployeeTitle 字典→JHEMR education_title 既有链路）、`149/151`（值域知识库；EmployeeTitle 字典即 PORTAL_SYS_DICT TYPE_CODE 之一，151 首批圈定候选）

# 2026 年新取得卫生中初级职称人员 HIS 职称字段更新执行包（155）

## 0. 背景与数据源

- 名单：人事经微信下发《2026.8.26 2026年新取得卫生中初级职称人员名单(1).xlsx》，共 **178 人**（身份证号+姓名+专业技术资格+专业+取得时间 2026-04-26+证书编号），9 档职称：主管护师 58、护师 39、主治医师 33、技师 16、主管技师 15、药师 7、主管药师 4、技士 3、药士 3；重名 1 组（王琪×2，身份证不同）。
- 目标：按值域对照更新 HIS 源库（10.10.10.15）`FXHIS.SYS_EMPLOYEE.LEVLCODE`（职称码）与 `COMM.STAFF_DICT.TITLE`（职称名），更新前备份。

## 1. 值域映射（EmployeeTitle 字典 live 实测，2026-08-27）

来源 `PORTAL_USER.PORTAL_SYS_DICT WHERE TYPE_CODE='EmployeeTitle'`（60 行，经 sjzc 受控只读通道查询），本次涉及 9 档码值全部唯一且 ENABLE_STATE=Y：

| 职称 | LEVLCODE | | 职称 | LEVLCODE |
|---|---|---|---|---|
| 主治医师 | 233 | | 主管护师 | 253 |
| 药师 | 244 | | 护师 | 254 |
| 药士 | 245 | | 技士 | 265 |
| 主管药师 | 243 | | 技师 | 264 |
| 主管技师 | 263 | | | |

参考：255=护士、234=医师、235=医士、497=检验师（用于旧值判读）。

## 2. 现状核对结论（全部经 sjzc live 只读，输出脱敏）

| 分类 | 人数 | 说明 |
|---|---|---|
| **A 自动可更** | **125** | 身份证在 SYS_EMPLOYEE 唯一命中，且迁移合理（同系列升级 111 + 现行空→新 14）；全部经 EMPLCODE↔EMP_NO 桥接在 STAFF_DICT 有行 |
| B1 已是目标 | 10 | LEVLCODE 与 TITLE 均已是目标值，无需更新 |
| B2 迁移异常待人工 | 14 | 跨系列（如 技师264→主治医师233、护士255→技师264）、降级（232→233）等，证书名单为准但建议人工再核（明细见本地文件） |
| B3 身份证多行 | 2 | 同一身份证在 SYS_EMPLOYEE 命中 2 行，需人工确认目标行 |
| C1 身份证未命中-姓名有候选 | 18 | 多为 IDENNO 为空或占位（111…）：按姓名找到候选 EMPLCODE，需人工确认 |
| C2 完全未找到 | 10 | 两表均无（含按姓名），可能未建人员档案 |
| 姓名差异提示 | 1 | 身份证命中但库内名字"常圆圆"vs 名单"常园园"，判定同一人，已按身份证保留在 A 组 |

- STAFF_DICT 侧：A 组 125 人中 124 人 TITLE 文本需同步更新（1 人已正确）；B1 组 TITLE 无漂移。
- A 组含 2 人 VALIDSTATE=0（非有效账号），已保留更新（证书事实）并标记。

## 3. 执行包位置与内容（本地敏感目录，不进 git）

`backend/_oa_work/title2026/`（.gitignore 已覆盖，含身份证号，禁止外传/入 git）：

| 文件 | 内容 |
|---|---|
| `00_backup.sql` | 建备份表 `FXHIS.SYS_EMPLOYEE_TITLEBAK_20260827`、`COMM.STAFF_DICT_TITLEBAK_20260827`（CTAS 全列快照） |
| `01_update.sql` | 125 条 `UPDATE FXHIS.SYS_EMPLOYEE SET LEVLCODE=…, MODIFIEDTIME=SYSDATE`（**EMPLCODE+IDENNO+旧值三重防护**，旧值不符则 0 行更新）+ 124 条 `UPDATE COMM.STAFF_DICT SET TITLE=…`（EMP_NO+NAME+旧 TITLE 防护） |
| `02_verify.sql` | 计数验收（应 125/124）+ 逐人明细核对 |
| `03_rollback.sql` | MERGE 从备份表恢复 |
| `groups.json` / `classify.json` | 全部分类明细（脱敏 MID+EMPLCODE+新旧值） |
| `raw/title_list_2026.json` 等 | 名单与查询原始证据 |

## 4. 执行要求（承接：信息科/DBA）

1. 顺序固定：`00_backup` → 核对备份表行数（应 125/124）→ `01_update` → `02_verify`；问题即 `03_rollback`。
2. 执行窗口建议夜间；执行人、时间、影响行数留档（本文件 §5 回填）。
3. **AI 不执行本包**：平台与 AI 通道对业务源库只读（AGENTS 红线）；本包由持写权限的信息科/DBA 执行。
4. 更新后无需手工同步 JHEMR：夜间人员同步将按新 LEVLCODE→EmployeeTitle 字典自动刷新平台 IdentityPerson.job_title，124 号职称子任务自动推送 jhemr.users.education_title（幂等）。
5. B2/B3/C1/C2 共 44 人不进本包：B2 人工确认后可加跑（SQL 模板同款）；C 组需先补建/补全人员档案（IDENNO 空）再更新。

## 5. 执行结果回填（执行后填写）

- **2026-08-27 授权执行尝试（用户明示授权后）**：经 8.83 平台写通道（`write_credential_ref=his_medical_dict.write`，账号 `xtwh`，112 字典推送专用）探测——`UPDATE FXHIS.SYS_EMPLOYEE` 报 ORA-00942（零授权）、`UPDATE COMM.STAFF_DICT` 报 ORA-01031（仅 SELECT）、无 CREATE TABLE 权限。**权限不足，0 行写入，事务无残留**。
- 已完成的准备：①行级文件备份 `pre_update_backup.json`（125+124 行旧值，键=工号，不含身份证）经只读通道导出，与 00–03 SQL 包一并归档 `8.83:/opt/data-asset/evidence/title-update-20260827/`（0600）；本地 `backend/_oa_work/title2026/`。
- 待决（二选一）：①DBA 按最小授权补 `GRANT SELECT,UPDATE(LEVLCODE,MODIFIEDTIME) ON FXHIS.SYS_EMPLOYEE TO XTWH; GRANT UPDATE(TITLE) ON COMM.STAFF_DICT TO XTWH;` 后由平台通道重试（125 号签名先例）；②DBA 直接在证据目录按 00→01→02 顺序执行（03 备用回滚）。
- 执行人/时间/行数：待填（上述①或②完成后）
- 验收输出：待填
- 异常行（旧值防护 0 行更新者）：待填

## 6. 与 151 的联动

EmployeeTitle 字典即 `PORTAL_SYS_DICT` 的 TYPE_CODE（60 行），属于 151 首批圈定清单的强候选；151 E1 探索完成后该字典应作为第一批导入值域库的字典类型（dict_table 证据、pending），此后此类"码值对照"不再需要临时 live 查询。
