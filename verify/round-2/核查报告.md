# 核查报告（verify/round-2 · 2026-08-27 当日平台升级）

> multi-verify 模式 A（变更核查+回归）。核查对象：当日 153 号 P0-P7 八批 + 156 号子任务（基线 5185bd5，核查包 `verify/round-2/`）。
> **核查期间重大事件**：22:32 主 AI 提交 36b8743（含 5 项复核缺陷修复）并于 22:52 发布 8.83 生产（13ef9e8）。本报告结论已计入该事实——已修复项标注证据关闭，仍开放项进问题清单与 161 号修复计划。

## 一、问题清单

### 已修复并发布（证据闭环，无需动作）

| 编号 | 原严重度 | 位置 | 问题 | 修复证据 |
|---|---|---|---|---|
| F1 | 高 | db_connectors.py 远程脚本 | A4 编辑致 ssh_jump 内嵌脚本缩进错乱，远端 IndentationError，跳板 Oracle 取数全灭（测试盲区：字符串脚本+FakeConnector） | 现场 `ast.parse` PASS；commit 36b8743 |
| F2 | 中 | metadata_collector.py | A4 探针使无 SQL 限制的采集多取 1 行、硬顶变 10001 | commit 含该文件 25 行夹紧修复 |
| F3 | 中 | queries.py get_query | 详情端点旧版直出 SQL（144 只掩了列表，详情是漏点）；掩码后 admin 详情只见"需权限"文案 | `test_get_query_includes_sql_for_admin_without_include_sql_flag` 在库；admin 不传 include_sql 亦回原文 |
| F4 | 中 | tables/index.vue:85,766 | 工具栏改 pageSize 把页大小当页码；树点击丢 page=1 重置 | 现场 grep：`@change="onPageSizeChange"`、`await loadData(1)` |
| F5 | 低 | test_query_asset.py | B2 掩码分支无自动化（admin client 只测原文） | `_query_viewer_client` 掩码断言已加（:270-276） |

### 仍开放（建议修复，按严重度）

| 编号 | 严重度 | 位置(文件:行) | 问题 | 证据 | 改进建议 | 阻断 |
|---|---|---|---|---|---|---|
| P1 | **高** | frontend/src/views/asset/relation-review/index.vue:385,390,397,405,509 | 三个筛选 el-select `@change="loadRelations"`、`@keyup.enter/@clear`、`@size-change` 直接绑 composable 的 `loadData(targetPage?)`：选中值("A"/"HIS")被当页码→后端 `page: int Query(ge=1)` **422，筛选功能整页报废**；size-change 传 30/50/100 跳错页。**生产 8.83 在跑** | Kimi 独抓+ZCode 现场 grep 坐实（发布后仍在）；166 Vitest 无模板事件覆盖 | 筛选/回车/清空改绑 `doSearch`；size-change 改 `() => loadRelations(1)`；其余 4 试点页同模式扫描 | 是（功能性缺陷） |
| P2 | 中 | db_connectors.py:605 × query_parameter_validator.py:16,38 | 【背景交叉·144×153】A1 后 pymssql 分支需 `%(name)s` pyformat dict 绑参；144 校验器对 sqlserver 按 `@name` 提取占位符。带参 sqlserver 查询资产执行必失败且无门禁拦截 | Kimi 独抓（两文件行号） | 校验器对 sqlserver 带参 fail-closed（明确报错），或统一占位符口径文档化 | 否（当前无 sqlserver 带参资产在用） |
| P3 | 低 | data_products.py:189 | D2 收敛漏网：import_results 硬编码 `user="system"`，审计 created_by 丢真实操作人（其余 3 处已收敛） | Kimi 独抓+现场 grep 仍在 | 补 `get_request_operator(request, default="system")` | 否 |
| P4 | 低 | identity/accounts/index.vue:189 | doUnbind 确认文案取 `row.account_code`，表格实际字段是 `account_id`→恒显示 id | Kimi 独抓+现场核验 | 改 `row.account_id`，与 api/identity.ts 类型对齐 | 否 |
| P5 | 低 | queries.py diff 端点 | 掩码后仍返回 `same_sql` 布尔与双端 `sql_sha256`（可推断版本间 SQL 是否相同） | Kimi | 保留（哈希为 144"可比对不可读"设计，改 null 破坏 diff 语义）；登记即可 | 否 |
| P6 | 低 | intake_version_retry.py:17-21 | `_is_unique_violation` 只认 pgcode 23505/类名 UniqueViolation，SQLite 识别不出→A9 重试路径在 SQLite 测试库不可达（零真实覆盖） | Kimi | 补 mocked IntegrityError 单测（假 pgcode 已有，补真实 PG 形态断言） | 否 |
| P7 | 低 | quality.py A2 下推 | PG GROUP BY 不归并 NULL 与 ""：同表 system/source 混 NULL/"" 时旧版归并一组、新版拆两条 finding | Kimi | 采集侧先统一（NULL→""）或接受口径微差并注释 | 否 |
| P8 | 低 | ai.py tool-execute | 只读工具 `sql_risk_scan/list_tools` 也被 `ai.sql.execute` 罩住（执行码套读接口，与 B5 裁决#8 精神相悖）；`ai.draft.execute` 无端点引用成死码 | Grok 独抓+grep 坐实 | 只读工具分流 `ai.context.read`（或 tool-execute 内按工具名分流）；ai.draft.execute 入 D5 式登记不删 | 否 |
| P9 | 低 | auth.py:82 | 任意非空 X-Requested-With 仍跳过 Origin 校验（历史行为；前端默认带此头，SPA 实际不走 Origin 匹配） | Grok | 文档化该前提（本次不改，改了破坏非浏览器工具） | 否 |
| P10 | 低 | 运维确认项 | 未绑定角色的 API Token 此前可调 propose-sql/system-context/export-context（原无权限码），现 403。若有外部自动化用未绑定 token 调用会断流 | ZCode 自审 Z2 | 生产查 unbound key last_used_at；必要时绑定角色 | 否 |
| P11 | 低 | dict/sync-diffs、identity/sync-diffs | 同步/采集按钮 catch 固定文案，未走 extractErrorDetail（与列表加载不对称） | Grok | 顺手统一 | 否 |
| P12 | 低 | 【背景·146】governance.py audit-logs/summary | 全量筛选结果拉内存统计无 limit | Kimi | 加 limit 或 SQL 聚合 | 否 |

## 二、回归结论

| 命令 | 结果 | 耗时 |
|---|---|---|
| 后端 `pytest tests/ -q`（隔离测试库） | **1152 passed / 1 skipped / 0 failed** | 27m12s |
| 前端 `pnpm run typecheck` | 0 errors（tsc+vue-tsc） | — |
| 前端 `pnpm run test` | 26 文件 / 166 tests 全过 | ~73s |
| 前端 `pnpm run build` | gzip 预算 PASS | — |
| 生产只读核验（8.83 psql） | `ai.sql.execute` 已授 platform_admin+quality_admin；dict_admin 9 条 dict.medical.*（seed 完整） | — |

> 回归盲区说明：ssh_jump 缩进（F1）与 relation-review 事件绑定（P1）均为测试不覆盖路径——前者是字符串内嵌脚本，后者是模板事件实参。161 号计划含对应防回归测试。

## 三、各方参与与文件

| 参与方 | 状态 | 产物 |
|---|---|---|
| GLM(ZCode) | 起草+自审 13 条+回归+裁决 | 变更说明.md、审查_zcode.md（哈希 0f8635…）、diff_1/2/3 |
| Kimi | 在场，12 条（独抓 P1/P2/P3/P4） | 审查_kimi.raw |
| Grok | 在场，9 条（独抓 F1 并列/P8/P9） | 审查_grok.raw |
| **Codex** | **缺席**（Reconnecting 4/5 断连，输出 0 字节） | 检查_codex.err 留档 |

分歧与裁决：`分歧表.md`（18 项：12 项证据闭环/共识、1 项证据裁决让步、5 项低优先开放；无对辩轮——无"无法证据裁决的残留冲突"）。

## 四、总体结论

当日升级（153 八批+156）主体质量合格：安全收口（B1/B2/B5）、性能改写（C1/C3/C4）、收敛（D 批）与前端两批经四方核查未发现设计级错误；两个高危（跳板脚本缩进、relation-review 事件绑定）中前者已被主 AI 修复发布，**后者仍在线上**（P1，筛选即 422）。生产 RBAC seed 完整无锁死风险。建议按 161 号一次性计划修复 P1 及顺手项。

---

**请逐条复核：回复"修复 编号"/"驳回 编号"/"暂缓 编号"，拍板后 GLM 按结论处理。**
（P1 建议必修；P2-P12 可按 161 号计划打包处理或逐条拍板。）

---

## 五、用户拍板记录（2026-08-27 深夜）

用户复核结论：**全部缺陷确认需要修改**（P1-P12 + P3 登记项），授权按 `开发起步包/161_平台升级核查缺陷修复一次性执行计划.md` 一次性执行；执行交由另一 AI 按提示词完成，GLM 不直接动代码。技能停点解除，进入 161 执行阶段。
