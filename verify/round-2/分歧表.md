# 分歧表（verify/round-2 · 2026-08-27 深夜）

立场缩写：G=GLM(ZCode 起草方/153执行方)、K=Kimi、R=Grok、C=Codex(缺席)。
裁决类型：事实=可用命令/行号证据裁决；取舍=设计决策。

> 重要背景：核查进行中（22:32），主 AI 提交 36b8743「…并修复 153 复核缺陷」并于 22:52 发布 8.83（13ef9e8）。其修复覆盖：ssh_jump 缩进、元数据采集夹紧、查询详情默认返回 SQL、表目录分页重置、B2 掩码测试。下表裁决已计入该事实。

| # | 议题 | G | K | R | 类型 | 裁决（证据） |
|---|---|---|---|---|---|---|
| 1 | A4 ssh_jump 远程脚本缩进损坏 | Z1 未抓到（自审盲区） | **高**·确认（compile 验证 line29 unexpected indent） | **高**·确认（ast.parse 验证 line28） | 事实 | **K/R 一致成立，G 漏检认账**。证据：`ast.parse` 输出 `unexpected indent`；`git show HEAD`(5185bd5) 基线正常。**已被主 AI 22:32 修复**（现场 ast.parse PASS）。教训：字符串内嵌脚本无编译器/测试覆盖 → 计划加 compile 冒烟测试 |
| 2 | relation-review 筛选事件实参当页码→422 | Z8 泛泛提"交互风险"未定位 | **高**·确认（:385,390,397,405,509 五处绑定） | 未列 | 事实 | **K 成立**。证据：`grep -n "loadRelations" relation-review/index.vue` → `@change="loadRelations"`(el-select 传值) + `@size-change="loadRelations"`(传页大小) + `@keyup.enter/@clear`；composable `loadData(targetPage?)` 首参当页码。**主 AI 22:32 未修此项**（发布后现场 grep 仍在）→ 生产缺陷 |
| 3 | A4 外溢：metadata_collector 无 SQL 限制多取 1 行 | Z1 中·提出 | 并入#1（"最多多 1 行无害"） | 中·确认（500/5000/10000 三处） | 事实 | **R 成立（G 同向）**；K 低估硬顶 10001 细节。**主 AI 已修**（commit 含 metadata_collector.py 25 行） |
| 4 | C3 列表 active_version 旧版是否直出 SQL | — | 「旧版列表直出完整 SQL」(低) | 未涉列表只谈详情 | 事实 | **K 让步**：基线 5185bd5 的 list_queries 本就用 `_serialize_version_for_read(include_sql=False)`（P3 改造前源码 + diff_1 上下文可证）。列表口径未变；变的只有详情端点（旧 `_serialize_ver` 直出→泄漏，属 B2 族漏洞） |
| 5 | 详情端点 SQL 可见性（关泄漏 vs admin UX） | Z3 低（前端不消费 diff） | 低·需裁决（kimi#9） | 中·确认（grok#3：前端不传 include_sql） | 取舍+事实 | **共识：关泄漏正确、admin UX 回归真实**。证据：HEAD 详情用 `_serialize_ver`（带 SQL）vs 列表已掩码——旧详情是漏点。**主 AI 已修**：admin 不传 include_sql 也返回原文（测试 `test_get_query_includes_sql_for_admin_without_include_sql_flag`） |
| 6 | B5 生产 seed 依赖 | Z6 中 | 无问题+自报风险2 同向 | 中（grok#4） | 事实 | **已消**：生产 8.83 实测 `asset_role_permissions` 含 `ai.sql.execute→{platform_admin, quality_admin}`、dict_admin 9 条 dict.medical.*（psql 只读查询 2026-08-27 深夜） |
| 7 | B2 掩码分支测试缺失 | Z11 类似 | 低（kimi#6…即 grok#6） | 低（grok#6：admin client 只测原文） | 事实 | 三方一致成立。**主 AI 已修**（新增 `_query_viewer_client` 掩码断言） |
| 8 | pymssql `%(name)s` vs 144 校验器 `@name`（sqlserver 带参资产必炸） | 未抓到 | 中·【背景交叉】(kimi#5) | 未列 | 事实 | **K 成立（唯一发现方）**。证据：`db_connectors.py:605` pymssql dict 绑参需 pyformat；`query_parameter_validator.py:16,38` sqlserver 按 `@name` 提取。开放项 |
| 9 | tool-execute 只读工具挂执行码；ai.draft.execute 成死码 | 未抓到 | 未列 | 低（grok#8） | 事实+取舍 | **R 成立**：`grep ai.draft.execute` 仅目录+ai_user 矩阵，无端点。开放项（登记不删，D5 同口径） |
| 10 | data_products import_results 归因丢失（恒 "system"） | 未抓到 | 低（kimi#6） | 未列 | 事实 | **K 成立（唯一发现方）**：`data_products.py:189 user="system"`（其余 3 处已收敛）。发布后现场 grep 仍在 → 未修 |
| 11 | accounts doUnbind 用 row.account_code（实际字段 account_id） | 未抓到 | 低（kimi#11） | 未列 | 事实 | **K 成立**：表格列为 account_id（:52），:189 取 account_code。未修 |
| 12 | same_sql/双端 sha256 掩码后仍暴露 | 未抓到 | 低（kimi#7） | 未列 | 取舍 | K 提出；哈希本就是"可比对不可读"设计（144 口径），信息量有限。**裁决：保留，登记即可**（改 null 反而破坏 diff 语义） |
| 13 | _is_unique_violation 不认 SQLite | 未抓到 | 低（kimi#8） | 未列 | 事实 | K 成立。生产 PG 系；影响=测试库该路径不可达。低优先补 mocked 单测 |
| 14 | A2 下推后 NULL 与 "" 不再归并同组 | 未抓到 | 低（kimi#10） | 未列 | 事实 | K 成立（PG GROUP BY 语义）。旧 Python 分组 `c.system_code or ""` 会归并。影响=同表 system/source 混 NULL/"" 时拆两条 finding。低 |
| 15 | X-Requested-With 任意值旁路（历史） | 未抓到 | 未列 | 低（grok#7） | 取舍 | R 提出且自认历史行为。**裁决：登记文档化**，不在本轮改（改了会破坏非浏览器工具） |
| 16 | unbound token 失去 propose-sql/system-context | Z2 中 | 同向（并入 B5 无问题段） | 未列 | 事实 | 行为变化属实。生产影响待运维确认（查 unbound key 的 last_used_at）→ 计划项 |
| 17 | sync-diffs 同步/采集 catch 固定文案（与列表不对称） | 未抓到 | 未列 | 低（grok） | 取舍 | R 成立。顺手项 |
| 18 | 【背景·146】audit_logs_summary 全量拉内存无 limit | 未抓到 | 低（kimi#12） | 未列 | 事实 | K 成立，背景项进计划 |

## 对辩轮说明

无满足对辩条件的残留冲突：#4（唯一实质分歧）由基线源码直接证据裁决（K 让步）；#1/#2/#5/#6/#7 均已命令验证或生产实测闭环。按技能规则跳过对辩轮，节约额度。

## 各方贡献小结

- **Kimi**：12 条问题清单，独抓 #2 高危（relation-review）、#8 pymssql 口径、#10/#11 归因与字段漂移；唯一一处误判（#4 列表旧口径）经证据让步。
- **Grok**：9 条清单，独抓 #1 高危（ssh_jump，与 K 并列首功）+#9 tool-execute 细节；#3/#5 与 G/K 交叉验证一致。
- **GLM 自审**：13 条草案，Z1（A4 外溢方向正确但未定位缩进）、Z6（seed 依赖，后被生产实测闭环）有效；漏检 2 个高危属执行方盲区，已认账。
- **Codex**：缺席（Reconnecting 4/5 断连，输出 0 字节，stderr 留档）。
