# T6 建议文本（仅建议，不执行）

## ① 153 号整改 与 149 值域导入：执行顺序与依赖

**建议顺序：153 先行（含复核+发布），149 值域种子导入随后，两者不同窗口。**

- 依赖关系：两者无硬阻塞——149 种子只写平台库 `asset_column_value_domains` 三表（b0c1d2e3f4a5 已于 2026-08-26 随 p144-149 上生产，表已就绪）；153 是零迁移代码整改。但 153 的 P1 安全收口会改 AI 上下文端点的权限码与异常脱敏路径，149 导入的值域正是经这些端点注入——先 153 后导入，可避免"导入后立刻换代码"造成的验证口径混乱。
- 推荐链：153 执行（免费 AI 按提示词）→ 主 AI 复核批摘要+git diff+全量门禁 → 153 发布（沿用 p144-149 管线：备份→构建→零迁移→切换→冒烟）→ 149 种子导入（幂等脚本+审计，dry-run 先行）→ 151 E2 圈定（引用 152 E5 清单）。
- 安全窗口：149 导入是平台库数据写入（非业务源库），选夜窗执行即可；**不要与 153 发布共用同一 8.83 维护窗口**（发布窗口只做发布，数据导入独立窗口独立回退点）。
- 回退：153 有旧镜像+DB 备份三件套；149 种子幂等可 `--check` 复跑，导入错误行可按 version_no 定向 deprecate，不需整库回滚。

## ② OA SQLServer admin 凭据轮换方案框架（143 号遗留）

**目标**：用专用只读账号替换 admin 直连（10.10.10.69 万户 ezOFFICE SQLServer），消除高权限口令长期驻留风险。

1. **建账号（DBA 侧）**：在 OA SQLServer 新建只读账号（如 `data_asset_ro`），仅授予 143 号探查涉及 schema（EZ_FLOW 等）的 SELECT；禁 DDL/DML；设独立强口令入密码管理渠道。
2. **平台切换（受控凭据文件）**：8.83 `/etc/data-asset/credentials/oa_sqlserver_10_10_10_69.readonly`（0600）原子替换为 `新账号:口令`——先写 `.tmp` 同目录再 `mv`，避免半写态；平台连接器按 source_code 解析，无需改代码。**凭据原文不进任何文档/日志/Git**。
3. **验证**：sjzc live 对 OA 跑 3-5 条 143 号同款限量聚合（HI_TASKINST 计数、WF_WORK→ORG_EMPLOYEE 命中率），结果与 143 基线一致即通过；失败立即回退第 2 步的旧文件内容。
4. **观察期**：旧 admin 凭据文件保留副本（受控目录）7 天，仅验证期使用；确认无其他消费方后删除副本并在 55 登记。
5. **收口**：通知 OA 厂商/DBA 对原 admin 口令做轮换或禁用平台侧使用；55 📌 登记完成。
6. **回退**：任一步失败恢复 `.readonly` 旧内容重跑验证；账号权限不足时补 GRANT 而不是升权。

## ③ wx_ltjl / 嘉和 入口文件与瘦身意见（被影响方视角）

**wx_ltjl**：
- 现状三件套（AGENTS.md + INDEX.md + CHANGELOG）结构清晰，建议保持；瘦身方向是 AGENTS.md 中的**操作细节下沉**（如解密三步命令、oa_client 用法示例移 `docs/使用说明/`，AGENTS 只留路由与红线），与数据资产侧"AGENTS=规则、README=状态"的分工对齐。
- 对本仓的依赖锚点请保持稳定：`F:\python\wechat-local-backup-search\HANDOVER.md` 路径、`wechat-chat-records` 技能名、HIS 知识库绝对路径——三者在《三仓库互通地图》已引用，改名需同步地图（本仓 AGENTS 启动自检已加此触发行）。
- 跨仓登记规则（INDEX/CHANGELOG）勿改，本仓吸收通道依赖它定位新证据。

**嘉和**：
- `START_HERE.md` 作为唯一入口符合其逆向排障定位，建议瘦身聚焦两点：①docs/26 系统库映射表与 `sql\README.md` 的环境标注（179 本机/177 副本/mycon2）保持互一致——本仓 154 号试点直接消费这些标注，漂移会误导查重选包；②`sql\` 分册的"151 条"自述建议改为在库编号口径（35 条）+中间产物说明，与地图口径统一。
- `PROMPT-三仓库检查处理-嘉和.md` 执行后，建议其 CHANGELOG 与本仓 verify/round-1 互链，形成三仓任务包闭环审计链。
- 对本仓依赖锚点：`sql\` 四分册路径与"出处:程序集+方法"格式、docs/26 映射——保持稳定，本仓 B2/B3 分批摄取直接依赖。
