• 先并行核查基础事实：CLAUDE.md、地图文件、两资产包、152 号 JSON、AGENTS.md 行数。

• 最后补两项：README 中对 AGENTS.md 章节的引用、154+ 编号占用情况。

• 核查完毕。以下为逐项结论（全部为只读核查，未改动任何文件）。

  # 任务包（T1–T6）独立核查报告

  ## T1 补根级 CLAUDE.md — 【确认】
  - 断言"根无 CLAUDE.md"属实：`Glob CLAUDE.md` 无匹配（含隐藏目录默认搜索）。
  - 动作与 AGENTS.md 无冲突。严重度：无。注意 T1 产物本身也需按四步登记 README（任务包已要求）。

  ## T2 地图时效防护 — 【确认】
  - `三仓库互通地图.md:12-14,18` 确实含全部六组时点数字：20 系统（:18）、11,182 表 / 143,707 字段 / 47+ 验证关系（:13）、31 篇 docs / 151 条 SQL（:14）；头部仅有"创建：2026-08-26"（:6），无口径日期、无过期触发机制。
  - 动作 3 落点"AGENTS.md 启动自检节"存在，追加一行不冲突。
  - 风险（低）：T2 要弱化的"151 条 SQL"本身就是问题数字（见 T4/C3），弱化前建议先修正基数。

  ## T3 收录边界判定 — 【确认，一处措辞不严格（低）】
  - `wechat-local-backup-search` 确实不在 L2 地图（grep 全文无命中），在 `F:/python/Wx_ltjl/AGENTS.md:47,93,101,157,184` 有描述 — 核心断言成立。
  - 但"**只在** wx_ltjl AGENTS.md 描述"不严格：本仓 `三仓库问题清单与评审提示词.md:66`、`开发起步包/README.md:244` 也提及。不影响动作，仅措辞。
  - 地图确实无收录判据小节（通读全文确认）。
  - `F:\python\JHEMR`（程序安装目录，dll/exe）与 `嘉和\JHEMR\`（含 EMR单机版/JHEMR/JHWebServer/互联互通上报工具（新）源码拷贝）均存在（ls 实证）；两者关系按 T3 自带"待核实"降级即可。严重度：低。

  ## T4 嘉和 sql/ 沉淀试点 — 【有问题（中）+ 有疑问（中）】

  **问题 1：查重文件指向错误（变更说明 C9 疑点成立，中-高）**
  - 任务包 T4 动作 2 指定与 `开发起步包\数据资产_资产包\relationships.csv` 查重。该文件实测 104 行（含表头）、14 列、内容是 **ODS/数据中心 HIS schema 体系**（如 `HIS.PAT_VISIT→HIS.PAT_MASTER_INDEX`）。
  - 而 T4 建议的 04-上报工具分册全部是 **EMR 库 jhemr 对象**（文件头注明"台账库=EMR jhemr"：`DC_CDA_UPLOADTASK`、`v_cdaupload`、`v_jinchuangbl`、`V_HLHT`）。jhemr 对象在 ODS 资产包中根本不存在，**查重将系统性零命中、形同漏查**。
  - 正确目标应为 `开发起步包\数据资产_JHEMR_Vastbase资产包\relationships.csv`（实测 284 行，`jhemr.*` view_dependency，如 `jhemr.diagnosis→jhemr.ba_manbing`）。
  - 附加风险：两 CSV **列结构不同**（前者 id/domain/join_condition/validation_* 14 列；后者 from_table/to_table/relation_type/confidence/… 7 列），查重逻辑需分别适配。
  - 补充印证 R2：03-老HIS-satrda 分册是 HIS Oracle（MEDREC 写链），若执行 AI 换册，查重目标又不同 — 任务包"单一查重集"假设不成立，建议按分册目标库拆分。

  **问题 2：152 号 E6 去重对象（基本确认，低）**
  - `152_..._结果.json:3983` 起 `e6_relation_candidates.candidates` 确有 8 条，但严格说是 **7 条 D 级 + 1 条 "existing（无需新增）"**（emr_first_page_item_dict→4 回写视图）。任务包称"八条 D 级候选"近似正确；试点报告去重应按 8 条候选清单对齐。且这 8 条是跨系统写血缘（jhemr→MEDREC），**两个 relationships.csv 都不覆盖**，只能与 152 JSON 本身去重 — 任务包这一点（动作 3 要求与 152 E6 去重）是对的。

  **疑问 3：「四分册共 151 条 SQL」数字无法复核（中）**
  - 四分册实测合计仅 306 行：编号条目（`-- N.`）35 条、SQL 语句起始行 45 条、带"出处"标记 27 行 — 均远低于 151。
  - 151 出自 `嘉和\sql\README.md:4`"全量候选 3,538 条已筛至 151 条带出处核心语句"，但其中间产物 `C:\temp\jh_t6\with_method.txt` 自注"易失"，**151 无法从分册正文复算**。
  - 影响：T4 动作 3 要求"全量 151 条的分批沉淀方案"，基数可能虚高约 3 倍；建议试点报告先按实际 ~35-45 条重估分批方案。严重度：中。

  ## T5 AGENTS.md 瘦身 — 【确认，引用风险低；回退风险属实（中）】
  - 现状数字确认：`wc -l` = 198 行；进度清单实测 17 条 ✅ + 1 条 ⏭（"约 20 行"近似）；导航表 24 个数据行（"25 行"近似）。
  - **引用扫描结果（好消息）**：55 号对 AGENTS.md 的引用全部是通用约束（`55:280` 约束 6 大表禁全扫、`55:333` 源库只读、`55:956` 启动规则），**无任何文档引用进度清单或导航表的具体条目**；全仓 grep "当前阶段目标/进度清单" 仅命中 `README.md:244`（任务包登记行本身）。T5 动作 2 的压缩在引用层面安全。
  - 变更说明 R1 属实：`git status` 显示 `AGENTS.md` 和 `开发起步包/README.md` 均为未提交修改（M），git 回退确会连带丢失未提交改动 — 必须按 T5 动作 4 用**备份副本**回退，禁用 git checkout。严重度：中。
  - 建议：T5 压缩时保留"硬约束 6 条编号"原样（55:280 按编号引用"约束 6"）。

  ## T6 仅建议 — 【确认】
  - 与 55 号现状无冲突；R4（凭据轮换建议不得粘贴真实凭据）为执行纪律问题，任务包红线已覆盖。
  - R5 确认：153 已占用（`153_平台一次性完善与bug修复执行计划.md`），154+ 无占用，T4 试点报告编号 **154 可用**。

  ## 汇总（按严重度）

  | 级别 | 项 | 结论 |
  |---|---|---|
  | 中-高 | T4 查重目标 | 指向 ODS 资产包错误，04 分册应查 `数据资产_JHEMR_Vastbase资产包/relationships.csv`；且两 CSV 列结构不同 |
  | 中 | T4/C3 「151 条」 | 分册实测仅 ~35-45 条，151 无法复算，分批方案基数虚高 |
  | 中 | T5 回退方式 | AGENTS.md 工作区 dirty，禁 git 回退，必须备份副本 |
  | 低 | C5/T3 措辞 | "八条 D 级"实为 7D+1existing；"只在 wx_ltjl AGENTS.md"不严格 |
  | 确认 | T1/T2/T6/C4/C10 | 断言与事实一致 |

  **给执行前的最小修正建议**：T4 动作 2 改为"按分册目标库选择查重集（04→JHEMR_Vastbase 资产包；03→另议），并同时与 152 E6 八条候选去重"；T4 动作 3 的分批方案以实际条目数为准；T2 弱化数字前先修正"151 条"。

