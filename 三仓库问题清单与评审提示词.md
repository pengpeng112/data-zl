# 三仓库问题清单与交叉评审提示词（v1）

> 创建：2026-08-27 ｜ 发起：用户 + ZCode(GLM-5.3)
> **用法**：把本文件全文粘贴给每个审查 AI（Kimi / Grok / Codex / Claude 等），要求其独立按 §6 交付格式输出结论；分歧由主会话对辩裁决，最终由用户拍板后统一执行修订。
> **评审 AI 权限**：默认**只读审查**——可读取本机三仓库任意文件核实证据，但**不修改任何仓库文件、不动生产系统、不执行 Git 写操作**。若用户单独授权写入，必须遵守 §7 红线与各仓库登记规则。

---

## 0. 一句话任务

验证本文所列 **8 个并行问题 + 1 组挂起事项**的判断是否成立、原因是否准确、拟议方案是否最优、有无遗漏与风险，输出可执行的修订建议与优先级排序。

## 1. 背景（评审 AI 必读）

本机三个工作目录分工不同、深度互通，由多个不同 AI 会话接力维护（ZCode / WorkBuddy / Claude / Kimi / Grok 等）：

| 仓库 | 路径 | 角色 |
|---|---|---|
| **wx_ltjl** | `F:\python\Wx_ltjl` | 运维知识与沟通中枢：HIS 日常问题知识库（K001-K073）、微信/企微聊天记录检索、OA 工单获取、周报生成 |
| **数据资产** | `F:\python\数据资产` | 数据与连接中枢：20 系统统一连库（sjzc 技能）、表结构/元数据/表间关系资产包、值域知识库、平台 backend/frontend（生产在 10.10.8.83） |
| **嘉和** | `D:\Users\Administrator\Desktop\嘉和` | 嘉和系逆向排障中枢：EMR(JHEMR)/无纸化/老HIS/上报工具的 31 篇排障 docs、反编译 SQL 资产库（151 条带出处） |

**互通体系（2026-08-26 刚建成，三级分层）**：

- **L1**：各仓库启动文件内嵌速查块（~9 行）——`F:\python\Wx_ltjl\AGENTS.md`、`F:\python\数据资产\AGENTS.md`、`D:\Users\Administrator\Desktop\嘉和\START_HERE.md`
- **L2**：单一权威互通地图 `F:\python\数据资产\三仓库互通地图.md`（三仓库定位/共享枢纽/任务路由矩阵/互相吸收通道/协作规则）
- **L3**：各仓库深处文档（按需读）
- 共享枢纽：sjzc 连库技能（`~/.zcode/skills/sjzc/`）、8.83 平台与受控凭据、wechat-chat-records 聊天记录技能、8.53 跳板

**各仓库登记处**（改谁家的文件就在谁家登记）：wx_ltjl → `INDEX.md`；数据资产 → `开发起步包\README.md` 目录更新记录；嘉和 → `CHANGELOG.md`。

**本次评审由来**：互通骨架建成后，主会话分析出以下并行问题，用户要求交由多个 AI 交叉验证修订，避免单一 AI 视角盲区。

## 2. 评审总需求（每个问题都按此四问）

1. **成立性**：问题是否真实存在？主会话的描述与原因是否准确（可开文件核实）？
2. **方案最优性**：拟议方向是否最优？有无更轻、更省 token、更不易漂移的做法？
3. **遗漏与副作用**：该修订会破坏哪些既有依赖（自动化任务、其他 AI 会话的阅读契约、登记规则）？
4. **优先级**：结合成本/风险/收益给出排序建议。

## 3. 问题清单（跨仓库体系类）

### A1. 启动文件对 AI 工具的覆盖缺口

- **现状**：Wx_ltjl 有 `AGENTS.md`(190 行) + `CLAUDE.md`(24 行，内容不同，仅讲 output 归档规范)；数据资产只有 `AGENTS.md`；嘉和只有 `README.md` + `START_HERE.md`，**无任何工具会自动加载的入口文件**。
- **原因**：三仓库由不同 AI 会话接力建设，各工具自动读取的入口不同（ZCode→AGENTS.md，Claude→CLAUDE.md，WorkBuddy→自有 memory 目录），建仓时未统一约定。嘉和 CHANGELOG 中存在 `Claude(k3,本会话)` 记录，证明 Claude 确实在该仓库工作过（当时无启动文件可读）。
- **风险**：新会话（尤其 Claude 类）在数据资产/嘉和打开时读不到启动上下文，各仓库"会话必读启动文件"的治理设计被绕过。
- **需求（待评审）**：验证各仓库应补哪些入口文件。拟议：数据资产与嘉和各补一个 3-5 行 `CLAUDE.md`，内容仅"先读 AGENTS.md / START_HERE.md"。请评估：最小转发文件 vs 完整副本？Wx_ltjl 现有 CLAUDE.md 与 AGENTS.md 内容分叉是否也应收敛为转发？

### A2. L2 互通地图无时效防护

- **现状**：`三仓库互通地图.md` 含大量时点数字（20 系统、11,182 表、143,707 字段、151 条 SQL、31 篇 docs、47+ 验证关系），无口径日期标注，无过期触发机制。
- **原因**：地图为 2026-08-26 从各仓库文档汇总的快照；各源数字随仓库演进变化，地图无同步机制，也未挂进数据资产已有的"启动自检"流程。
- **风险**：数字过时后地图自身变成新的"过时文档"——恰是各仓库规则反复防范的对象。
- **需求（待评审）**：设计防过期机制。拟议三件套：①头部加"口径快照日期"；②地图内数字尽量弱化（"约/N+"），精确值留在各仓库权威文件；③数据资产 AGENTS"启动自检"节加一行"涉及跨仓库能力变更时检查 L2"。请验证哪些数字必须精确、哪些可弱化，以及③是否足够（是否需要嘉和/wx_ltjl 侧也挂钩）。

### A3. "互相吸收"通道已定义但未兑现（价值最大项）

- **现状**：L2 地图 §4 定义了 5 条吸收通道，实际只兑现过 1 条的局部（数据资产 152 号报告消费嘉和侧 22 条 PBD SQL 产出 E4-E6）。
- **A3a 嘉和 `sql\` 151 条 SQL → 数据资产关系图谱**：`嘉和\sql\` 四分册（CDMS/通用接口/老HIS/上报工具）每条带程序集+方法出处；数据资产已有 `.agents\skills\sql-relation-intake\SKILL.md`（SQL 解析→元数据核对→查重→候选分级）。**原因**：通道 2026-08-26 才正式定义，无排期。**风险**：反编译 SQL 有占位符未替换、片段级不可执行、跨环境（Oracle/Vastbase/EMR 库）标注问题，批量入库可能污染关系资产。**需求（待评审）**：给出分批沉淀方案（先哪一册、每批多少条、验证门槛、回滚方式），评估投入产出。
- **A3b 嘉和排障结论 → wx_ltjl HIS 知识库**：嘉和 docs/28（首页打开慢）、29（保存慢）、30（性别宏未知）格式接近知识库条目（现象/根因/涉及表/SQL）。**原因**：无入库口径定义——这三篇根因多在 EMR 库（Vastbase）与客户端，是否属"HIS 运维台账"范畴存疑。**需求（待评审）**：先定义入库判据（涉及 HIS Oracle 表？还是"医院系统日常问题"都算？），再决定转换批次。
- **A3c wx_ltjl 知识库 → 数据资产映射同步**：`HIS问题表映射.csv` 双向同步规则已写，但知识库 2026-08-24 建成后尚未发生条目变更，同步机制未实测。**需求**：验证是否需要在知识库头部加"变更后须同步 CSV"的显式提示。

### A4. L2 地图收录边界未定义（第四目录问题）

- **现状**：`F:\python\wechat-local-backup-search`（企微本地库解密，产出三仓库都在用的解密方案与 HANDOVER.md）只在 wx_ltjl AGENTS.md 里描述，不在 L2 地图；`F:\python\JHEMR`（EMR 程序目录）与 `嘉和\JHEMR\`（仓库内拷贝）的关系也未在地图说明。
- **原因**：地图初版按用户点名的三仓库建，未定义"什么级别的目录才入图"。
- **需求（待评审）**：给出收录判据（如：被≥2 仓库依赖的能力源头目录才入图），并判定 wechat-local-backup-search 是否应入图、以何种粒度（独立条目 vs 归入 wx_ltjl 条目内一行）。

## 4. 问题清单（各仓库专属）

### B1. [wx_ltjl] AGENTS.md 工作流 A/B 与既有文档重复（~70 行）

- **现状**：AGENTS.md 内联"标准工作流 A/B"完整代码块，与 `docs\交接与计划\AI交接文档.md`、`docs\使用说明\聊天记录获取指南.md` 高度重复。
- **原因**：AGENTS.md 定位"新会话直接按此执行"，交接文档是展开版，两者并行生长。
- **风险**：三处修改不同步会漂移；且存在每周一 09:00 自动化任务（`automation-1787193960856`）依赖此流程——**压缩前必须确认自动化读的是 AGENTS.md 还是技能副本**（`~/.workbuddy/skills/weekly-work-report/` 内还有脚本副本）。
- **需求（待评审）**：验证自动化的实际依赖链后，给出"摘要+指向"的压缩方案或维持现状的理由。

### B2. [wx_ltjl] HIS 知识库持续更新机制未闭环

- **现状**：知识库 2026-08-24 建成（84 条台账→K001-K073），设计上以聊天记录检索为持续更新渠道，但无更新触发规则（何时/多频率/谁触发/新条目走什么流程）。
- **需求（待评审）**：给出轻量更新机制建议（如：周报流程顺带扫描新故障→候选条目→用户确认入库），避免知识库一次性建成后停滞。

### C1. [数据资产] AGENTS.md 过重（~198 行）

- **现状**：「当前阶段目标」的 ✅ 进度清单（约 20 行）与 `55_系统未完成事项统一执行计划.md` 重复（55 号已是权威入口）；导航表 25 行与 `开发起步包\README.md` 重复。
- **原因**：历史演进中 AGENTS.md 承担了状态汇报功能。
- **风险**：压缩可能影响新会话对"已完成工作"的快速感知；但维护双份清单更易漂移。
- **需求（待评审）**：给出保留/压缩边界（拟议：AGENTS 只留阶段目标 3 行 + 55 号指针 + 安全红线，进度全走 55 号）。

### C2. [数据资产] 已定稿计划与授权事项堆积

- **现状**：153 号整改计划（P0 高危修复：pymssql 参数绑定/CSRF 精确匹配等）已定稿未执行；149 值域种子生产导入明示"须另行授权"未做。
- **原因**：均为需用户授权的生产侧动作，AI 侧无法自行推进。
- **需求（待评审）**：验证两项的依赖关系与执行顺序（153 是否应先于 149？有无相互阻塞），给出安全执行窗口建议。

### D1. [嘉和] 生产修复方案待批准

- **现状**：docs/28 病案首页打开慢的根因已破案（`V_OPERATION_DICT` 手术级别标量子查询无索引，单条 20.5s，对照实验复现），修复=单索引方案（含核验/回滚 SQL），标注"待用户批准"。
- **需求（待评审）**：验证该索引方案的风险评估是否完备（写生产 EMR 库、锁表时长、Vastbase 语法），是否可直接批准或需补充前置条件。

### D2. [嘉和] docs/27 计划被网络阻塞

- **现状**：反编译工具落地与 EMR 客户端深挖计划（docs/27 v2 + 配套 PROMPT）就绪，但依赖的 DB 直连/网络未通（10.10.8.178 TCP 假通等，docs/27 §1.7）。
- **需求（待评审）**：验证是否有不依赖网络的子任务可先行拆出执行，避免整计划挂起。

## 5. 挂起与授权事项汇总（并行债务，供排序）

| 事项 | 仓库/出处 | 状态 | 阻塞点 |
|---|---|---|---|
| OA SQLServer admin 凭据轮换 | 数据资产 143 号报告 | 未做（安全债） | 需用户授权建只读账号 |
| 153 号整改执行 | 数据资产 | 已定稿 | 需用户授权生产变更 |
| 149 值域种子导入 | 数据资产 | 未做 | 明示须另行授权 |
| docs/28 索引修复 | 嘉和 | 方案完备 | 待用户批准 |
| docs/27 深挖计划 | 嘉和 | 就绪 | 网络未通 |

**需求**：对上表给出统一优先级排序（特别关注 OA 轮换是否应置顶——高权限 admin 账号正被只读查询长期使用）。

## 6. 交付格式（评审 AI 按此输出）

1. **结论表**（每个问题一行）：

| 编号 | 结论（成立/部分成立/不成立） | 关键证据/理由 | 修订建议（一句话） | 风险/依赖 |
|---|---|---|---|---|

2. **逐题详述**：成立性论证（引用你核实到的文件与行为证据）、推荐方案（具体到改哪个文件哪一节、加什么删什么）、被否决的备选方案及理由。
3. **遗漏问题清单**：主会话未列出但你在核实过程中发现的三仓库互通/治理问题（同样按"现状/原因/需求"格式）。
4. **全局优先级**：所有问题+挂起事项的统一排序（P0/P1/P2），附排序依据。
5. **自检声明**：列出你实际打开核实过的文件清单；未核实之处明确标注"未核实，仅基于本文描述推断"。

## 7. 红线（评审 AI 必须遵守，违反即停）

1. **只读审查**：不修改三仓库任何文件、不移动/删除文件、不执行 Git 写操作。
2. **生产零接触**：不连生产库、不重启服务、不触发调度；所有数据库/服务器仅允许引用文档记载的信息，不做实测（如需实测另获用户授权）。
3. **凭据纪律**：口令/密钥/token 不复制进你的输出；发现明文凭据只报告位置不复述内容。
4. **隐私**：患者 ID/姓名/病历内容不进入评审输出。
5. **结论纪律**：区分"已核实（给出文件+位置）"与"推断"；不给无证据的猜测背书。
