# ============================================
# 开发与测试依赖（不进生产 wheelhouse）
# 安装：pip install -r requirements-dev.txt（会同时装 requirements.txt）
# ============================================
-r requirements.txt

pytest>=8.0
httpx>=0.27
