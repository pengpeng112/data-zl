# ============================================
# 生产运行依赖（下限声明）
#
# 锁定策略（发布时强制执行，见 58_发布部署与代码同步整改计划.md R2）：
#   1. 在与目标同 OS/架构/Python 的 Linux 构建机执行：
#        pip install pip-tools
#        pip-compile requirements.txt -o requirements.lock --generate-hashes
#   2. 离线打包用锁定文件下载 wheel：
#        pip download -r requirements.lock -d offline/wheels
#   3. 目标机用 wheelhouse 安装：pip install --no-index --find-links=offline/wheels -r requirements.lock
#   4. requirements.lock 纳入 Git；requirements.txt 仅作下限声明，不直接用于生产安装。
#
# 测试依赖（pytest/httpx 等）见 requirements-dev.txt，不进生产 wheelhouse。
# ============================================

fastapi>=0.110
uvicorn[standard]>=0.29
# FastAPI UploadFile/File 表单解析（诊断映射 Excel 上传）
python-multipart>=0.0.18
SQLAlchemy>=2.0
pydantic-settings>=2.2
# MySQL 连接器
pymysql>=1.1
# SQL Server 连接器
pyodbc>=5.0
# SQL Server fallback when ODBC driver is unavailable in container (plan 85 F8)
pymssql>=2.2.11
# PostgreSQL 连接器（当前应用库已使用）
psycopg[binary]>=3.1
# Oracle 连接器（HIS 源库 10.10.10.15 / 数据中心 10.10.8.216）
oracledb>=2.0
python-dotenv>=1.0
pypinyin>=0.53
# JHEMR 初始密码加密（SM4/ECB/PKCS7）
gmssl==3.2.2
alembic>=1.13
# 定时调度
apscheduler>=3.10
# 频率限制
slowapi>=0.1
# 本地账号登录：密码哈希 + JWT
argon2-cffi>=23.1
python-jose[cryptography]>=3.3
# HIS 签名写入 JHEMR 前等比缩放并统一为 JPEG
Pillow>=10.3,<13
# 诊断映射 Excel 导入（服务层懒加载，仍属于生产运行依赖）
openpyxl>=3.1.2,<4
